# Tracked backlog — the ONE debt/tails registry (durable; git-tracked)

Seeded 2026-08-13 from the carried tails (NEXT_SESSION_PROMPT is gitignored — THIS file is the
durable copy; the prompt mirrors it). Rules: dated rows; one row per item; audits VERIFY rows
(status changes land here with evidence), never re-discover them; closing a row = a dated state
edit, never a deletion. New debt found anywhere lands here the same session.

**Re-verified 2026-09-06** (session 2026-09-06g, Track D4 of the owner-ordered docs + memory
hygiene sweep). Range **T1–T115**, 115 rows, no gaps (T91 added later the same session by the main agent; T92–T96 added 2026-09-06h from the first visual-sweep contact sheets and the sunset light-path trace; T103–T104 added 2026-09-06l; T105 added 2026-09-07; T106–T107 added 2026-09-07b from the phone/descent CPU profiles; T108–T114 added 2026-09-07c from the visibility/occlusion audit — `audits/audit-occlusion-2026-09-07.md`; T110/T111/T112 FIXED and T106 re-shaped 2026-09-07d; T106 FIXED 2026-09-07e).
- **12 CLOSED**: T23 · T25 · T38 · T41 · T44 · T45 · T54 · T55 · T74 · T75 · T78 · T79.
- **13 settled without further work**: ACCEPTED RISK T17 / T24 / T26 · ACCEPTED v1 T19 · WON'T-FIX T27 ·
  DOCUMENTED T14 · DEFERRED T4 / T51 · WATCH T13 / T15 / T20 · GUARDED T33 · PARTLY FIXED T34.
- **66 OPEN**, of which **18 are PARKED by owner order**: T1 · T16 · T29 · T31 · T42 · T46 · T47 ·
  T50 · T52 · T56 · T57 · T59–T64 · T77 (T51 is parked too, but it counts above as DEFERRED).

Three things this pass fixed or recorded:
- **The table is 5 columns.** Rows T59–T70 shipped 2026-08-27 with only 3 — no Pointer, no State.
  Both columns were reconstructed 2026-09-06 from NEXT_SESSION_PROMPT, DECISIONS and each row's
  own text, and each says `Columns added 2026-09-06`. No new measurement was taken for them.
- **A `DECISIONS <date>` pointer resolves in `DECISIONS.md` OR `DECISIONS_ARCHIVE.md`.** Compaction
  rounds move whole eras to the archive, so grep both files before calling such a pointer dangling.
- **T88–T90 are mirror-check promotions**, not new discoveries: each was an open tail in
  NEXT_SESSION_PROMPT whose only registry home was a row that has since closed.

| ID | Since | Item | Pointer | State (dated) |
|----|-------|------|---------|---------------|
| T1 | 2026-08-13 | Real-device DEEP check (perf/memory/fps numbers on iPhone + Pixel). SCOPE GREW with the U-era (dated edit 2026-08-18, audit-2 D10): + U1 gesture feel (pinch hardening on real touch) · U2 governor/soak on device · U3 touch on the 2D map · U4 cone legibility at phone DPI · U5 cold-network closest-first visual + mid/low queue-caps A/B (12/3 · 8/2 are first-guess knobs) | MOBILE_PLAN §6 M0 + UPLIFT_PLAN §5 | OPEN — owner-present exit gate for the whole U-era; numbers unmeasured **2026-09-06 (dated edit):** the owner opened an AWS Device Farm project (iPhone 17 Pro pool, 1,000 free minutes) and offers a Pixel 6 Pro over adb; the flow is BUILT — `tools/devicefarm/ios-baseline.mjs` (Remote Access + Appium endpoint, Safari, `__debugFeed.snapshot()`, kill ramp, soak) and `verify-perf-baseline.mjs --device` (adb) — awaiting AWS credentials on the Mac; neither has run on a device. **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED, owner orders 2026-08-25 / 2026-08-27 / 2026-09-01). Still the U-era exit gate and still unmeasured on a device. |
| T2 | 2026-08-13 | Release canaries: `/m` served in prod + `/_wix/pages.json` lists it; `/api/sbdb` first egress | NEXT_SESSION_PROMPT §1 | OPEN — rides next `wix release` **— re-verified 2026-09-06: OPEN; the pointer no longer resolves.** `NEXT_SESSION_PROMPT §1` does not exist — that file is rewritten every session and its sections are named, not numbered. The durable home is NSP §"Standing, unchanged" (RELEASE GATE).  **2026-09-10: the host is LIVE (owner + curl): `/m` served 200 and `/_wix/pages.json` lists it — those two halves PASS; `/api/sbdb` first egress still unverified (a bare GET reads 400 — needs a real query).** |
| T3 | 2026-07-17 | Prod login `#p=` hash round-trip smoke (fallback `?p=` + normalize) | DECISIONS 6.9 line | OPEN |
| T4 | 2026-07-17 | Phase-6 DoD tail: one real purchase loop (owner manual dashboard step) + premium plan rename | IMPLEMENTATION_PLAN §6/§6.9 | DEFERRED post-M6 — owner ruling 2026-08-13: skip for now, push long after M6. 2026-08-13 audit: the "Pricing Plans app install (`APP_NOT_INSTALLED`)" sub-item was DONE 2026-07-17 (DECISIONS: installed by owner, plan `5874dba8-…`) — row corrected |
| T5 | 2026-07-17 | Ground CHECKERBOARD + flicker 1500→200 km (owner screenshot-evidenced) | mem:bugs/ground-checkerboard-flicker | OPEN — plan drafted |
| T6 | 2026-07-14 | Owner A/B verdict BLD chip (CLASSIC vs OSM2WORLD); if o2w wins: draco 23× + untagged-height defaults | mem:project/wip-2026-07-14-osm2world-adapter | OPEN — both live on R2 |
| T7 | 2026-07-14 | HLOD coarse tier (enriched bbox building-empty above ~20 km) | dnipro-enrichment/DNIPRO_3D_ENRICHMENT_PLAN.md | OPEN |
| T8 | 2026-07-14 | dayArcs skyline fold (seam: `planFeed.profileSample()`; 2026-08-14: second seam `store/plan.profileBins` + `sampleBins` — rail-context half LANDED via the §3.1.D trace; the 3D dayArcs fold itself remains) | mem:project/wip-2026-07-14-pass3-obstruction-moat | OPEN — rail half done |
| T9 | 2026-07-14 | WS4-D subject shadow timeline | same memory as T8 | OPEN |
| T10 | 2026-07-12 | GTAO tune/enable + Pass-1 tier A/B on a weaker box | rendering/RENDERING_QUALITY_PASS.md | OPEN — GTAOPass wired default-OFF |
| T11 | 2026-08-10 | Astro taste-pass: `SKY_TARGET.ringGain` 0.55 · `ASTERISMS.highlightAlpha` 0.55 · `STARS.bvTintAmount` 0.6 · TRAIL weight/alpha | DECISIONS 2026-08-10 | OPEN — owner taste |
| T12 | 2026-08-10 | Galilean-moon points on the Jupiter disc (render-table wish) | archive/ASTRO_ENGINE_PLAN.md | OPEN — unbuilt |
| T13 | 2026-08-10 | WATCH: one non-converged FPV auto-aim glide during rapid consecutive re-tracks (not reproduced in isolation) | DECISIONS 2026-08-10 | WATCH |
| T14 | 2026-07-21 | Vite dep-cache staleness after wix-dev restart (chunks 504 "Outdated Optimize Dep"; move `node_modules/.vite` away + restart) | NEXT_SESSION_PROMPT tails | DOCUMENTED workaround — no fix planned |
| T15 | 2026-07-15 | SavedPlaces transient Wix Data WDE0054 watch | DECISIONS 2026-07-15 | WATCH |
| T16 | 2026-07-15 | Milky-way 8k ≈ 134 MB VRAM on DESKTOP (mobile solved 2026-08-13 via 2k tier; desktop 4k fallback parked) | tuning MILKYWAY notes | OPEN — parked |
| T17 | 2026-07-10 | Esri + CARTO + OpenFreeMap ToS = accepted POC risks; re-check before commercial release | ARCHITECTURE §9 | ACCEPTED RISK — dated |
| T18 | 2026-07-14 | grid-10 orphan R2 keys (harmless leftovers) | mem:project/wip-2026-07-14-osm2world-adapter | OPEN — cosmetic |
| T19 | 2026-07-12 | Street-label occlusion vs buildings (oblique smear accepted v1) | S7 digest | ACCEPTED v1 |
| T20 | 2026-07-12 | One-off dark streak 1.3 km (phase55-53, unreproduced) | DECISIONS S7 batch | WATCH — unreproduced |
| T21 | 2026-07-10 | Mid-range-phone ARW decode benchmark + peak WASM heap (empirical-validation carry) | IMPLEMENTATION_PLAN §Empirical validation | OPEN — pairs with T1 |
| T22 | 2026-07-12 | README demo URL still omitted (contest rewrite deliberately withheld it) | README | OPEN — owner call |
| T23 | 2026-08-13 | Author `conventions/contracts.md` (Hyrum contract-strings inventory) | checklists/docs.md item 11 | CLOSED 2026-08-13 — authored by audit #1 (`conventions/contracts.md`); later audits diff it |
| T24 | 2026-08-13 | astro@5.18.2 carries 5 XSS advisories with fixes only in 7.x (C4 pins Astro 5); audit #1 verified ZERO reachable sinks (no define:vars / server islands / element spread props / transition:* in any .astro file — all dynamic UI is client:only React). FORMULA EXTENDED (dated edit 2026-08-18, audit-2 B2 — the re-check trigger FIRED at 5 .astro files): + grep `set:html` (guide.astro carries 4, SAFE today: static guideContent input, escapeHtml on every text run, link targets regex-locked `[a-z0-9-]+` — re-verify those three properties on any guide.astro change) + the prod manifest registers `/_server-islands/[name]` (unreachable today: zero island components — re-check if one ships) | audits/audit-full-2026-08-18.md B2 | ACCEPTED RISK — dated; re-check on ANY .astro growth or new advisory, with the extended formula. **RE-VERIFIED 2026-08-22g** (the trigger FIRED: `guide.astro` gained a topic outline, goal routes, a `<ul>` list renderer, an `h1`, image anchors and a bundled search script). All three properties HOLD and are now MACHINE-CHECKED every `npm test` by `test/pages/guideAstroShape.test.ts`: exactly 4 `set:html` sinks, each `set:html={inlineHtml(`; both `inlineHtml` branches escape; the sole emitted attribute is `href="#${run.target}"` with `run.target` regex-locked to `[a-z0-9-]+`. New renderers emit plain nodes, never a fifth sink. Zero `server:defer`/`define:vars`/view-transition directives across a GLOBBED sweep of every `.astro` in `src/` (so a NEW page is covered the day it lands). The added `<script type="module">` is a bundled client script, not a server island — it touches no `/_server-islands/[name]` sink and builds DOM nodes, never `innerHTML` (browser-asserted, `verify-guide.mjs` check 14) |
| T25 | 2026-08-13 | Point/ellipse sky-target impostor RENDER never eyeballed (ring widening + tracked-DSO flows verified 08-03/08-10; no shot of the point/ellipse treatments themselves) | archive/ASTRO_ENGINE_PLAN.md:166 + audit finding C2/D8 | CLOSED 2026-08-13 (Phase 8a browser pass) — POINT eyeballed on the new GC target (broken-hairline ring + faint point, verify-shots/phase8a-09-gc-reticle-crop.png) · ELLIPSE eyeballed on M31 at real 178′×70′ extents + widened ring (phase8a-10-m31-ellipse-crop.png), both in FPV headed Chrome |
| T26 | 2026-08-13 | `/api/upload-url` unbounded: no `sizeBytes` cap / mint rate limit / quota linkage (auth required; media storage is the one unmetered write surface) | audit finding B9 | ACCEPTED RISK — owner ruling 2026-08-13: stays unbounded (auth suffices for this stage); re-open only on abuse evidence or commercial release |
| T27 | 2026-08-13 | B2 tail: `frame-p5-tester` password rotation (old value in git history) | audit finding B2 | WON'T-FIX — owner ruling 2026-08-13: ignore (test-only member, no privileged data); re-open only if the account gains privileges |
| T28 | 2026-08-18 | U-era + guide taste tails (durable copy of the gitignored NSP list, audit-2 D10): U4 aim-cones v2 (GL edge labels + skyline sub-bands) · U2 cold-load settle glide · P7 card copy pass · U5 first-guess knobs (fpvBiasK 1.5, caps 12/3 · 8/2 — A/B on device, pairs T1) · GUIDE G3 remainder (owner taste pass on chapters/copy/shot crops; coach-mark journeys = optional owner call; folded from archive/GUIDE_PLAN.md) | UPLIFT_PLAN §5 + archive/GUIDE_PLAN.md G3 | OPEN — owner-taste class; no engineering blocker |
| T29 | 2026-08-18 | U5 tile-tier idiom extraction seam (audit-2 Track A OF-2): the ~33-line capture-defaults/apply-tier glue is duplicated ×3 across buildings/enrichedBuildings/imageryGround — extract `captureTileDefaults/applyTileTier` into scene/tilePriority.ts. (2026-08-18o note: the "touch of all three" trigger technically FIRED with U6 — foveation glue threaded through all three modules and added a FOURTH repeated pattern (fovea handle wiring, ×3); extraction deferred to its own slice so the verified U6 diff stayed minimal — the extraction shape now includes the fovea glue) | audit-full-2026-08-18.md §Fitness (restraint-now ruling) | OPEN — trigger FIRED (U6); schedule as its own small slice **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED). |
| T30 | 2026-08-18 | audit-1 B7 remainder (audit-2 B4 status check): `/api/photos` GET/DELETE still hardcode `limit: PIN_QUOTA_FREE` and GET's `.limit(50)` caps `used` — a premium member reads 100 where the display could say 1000 | api/photos.ts:73,240 | OPEN — display-only; act if MyPins ever renders "N/limit" (reuse POST's knownLimit) — owner call |
| T31 | 2026-08-18 | Trap→test tail (audit-2 C2 remainder): (c) heightAt CONSUMER fence (every live `terrainHeightAt` read must route through clampGroundM/sampleGroundM/easeSeatM — would have caught A1 at write time) · (e) dayArcs material-contract assert · the 18h/18i pure-fn extraction candidates (aim-cone edge-label math, search-select steer guard) | audit-full-2026-08-18.md C2 | OPEN — fences (a)(b)(d)(f) landed 2026-08-18 (test/components/globe/fences.test.ts + test/styles/hiddenPairs.test.ts); these need modest refactors first **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED). |
| T32 | 2026-08-18 | `planner.ts:76` Observer elevation clamps the floor (−400) but has NO CEILING — astronomy-engine throws `Invalid elevation` above 100,000 m (astronomy.js:4809) when fed the camera's eyeAboveGround at orbit; browser-caught during the U6 verify (/m: scene-time set while at LEO in 2D), non-fatal via the B26 throttled logger. Fix = one-line upper clamp (~10 km observer ceiling) + a planner test | DECISIONS 2026-08-18o | **CLOSED 2026-08-22e (registry edit 2026-09-11, audit #4 K1):** F1 shipped `planElevationsM()` as the ONE writer of both observer heights (a second throw site `SearchRiseSet` included) with a positive control pinning the un-clamped throw — DECISIONS_ARCHIVE 2026-08-22e ends "Backlog: T32/T35–T40 CLOSED" but this row was never edited. Audit #4 re-verified at HEAD: `planElevationsM` imported at `bestSpotTrack.ts:75`, `planner.ts:107-113`. |
| T34 | 2026-08-21 | Ground-LRU **rest-trim churn**: ~600 Esri GETs per 2D↔FPV leg, cache resting at exactly `minBytesSize`. PRE-EXISTING and #15-adjacent — surfaced (and quantified) while killing the QA-7b rebuild storm, which it is NOT. Candidate levers: mode-aware LRU floors · a flip-freeze of the rest-trim. iOS network cost is the concern | DECISIONS 2026-08-21h; audit-3 report | PARTLY FIXED 2026-08-26 (RC20) — flip bank `QUALITY.lruBank` ships mid/low. **M13 answered: the DESKTOP half does not exist** — on `high` the ground cache rests at 109.8 MB against a 322.1 MB floor, so the trim condition never arises. The mid/low GET count is still unmeasured on device (T1) |
| T35 | 2026-08-22 | **Radar/cone extraction seams** (audit-3 A1-7/A1-8, quantified beyond Track E's jscpd tips): `bandFor` ×3 hand-maintained copies + `bandFutureInk` ×2 with NO cross-file fence → extract to a three-free `lib/geo/radarBands.ts`; GL side `aimCones↔focalCone` ≈47 duplicated lines (incl. a byte-identical `makeLineMaterial`/`makeFlatMaterial` pair) → `scene/tangentOverlay.ts`; canvas side `MapWindow↔MiniMap` ≈95 lines (the 29-line jscpd hit is its tip) → `panels/radarCanvas.ts`. Additive extract-then-delegate (Gall), no rewrite. **Distinct from T29** | audit-3 report §Findings A1-7/A1-8 | **CLOSED 2026-08-22e (registry edit 2026-09-11, audit #4 K1):** F5 shipped all four seams (jscpd 35→33 clones, 1.14→1.06 %); audit #4 re-verified at HEAD: `lib/geo/radarBands.ts`, `scene/tangentOverlay.ts`, `panels/radarCanvas.ts` all present. The T29 U5-foveation-glue extraction the row's later note adds stays its own parked row. |
| T36 | 2026-08-22 | **Radar anchor-ladder divergence across the three surfaces** (audit-3 A1-6 + A2-1, found INDEPENDENTLY by two tracks; the refutation attempt FAILED — the only `setMapWindowOpen(false)` is the Escape handler, so exiting FPV any other way leaves the map open): GL uses `placement → tempPin → view focus`; the chart uses `(fpv?camGeo) → tempPin → camGeo → focus`, so outside FPV it anchors on the camera NADIR (km away at tilt) and the focus tail is dead code; a placed PHOTO owns the GL radar but not the chart's | `MapWindow.tsx:215-218` vs `StylizedTiles.ts:3959-3962` | **CLOSED 2026-08-22e (registry edit 2026-09-11, audit #4 K1):** F4 shipped `lib/geo/aimAnchor.aimAnchorFor()` (FPV eye → placed photo → temp pin → view focus) on all three surfaces + the published-anchor read seam; audit #4 re-verified at HEAD: `lib/geo/aimAnchor.ts` present. |
| T37 | 2026-08-22 | **MapWindow chart tiles have no error path** (audit-3 A1-4): a 404/timeout is cached `{ok:false}` FOREVER, never retried on a later pan, and nothing warns — the degrade-path-visibility rule. One-line fix: `entry.img.onerror` → `console.warn` + `cache.delete(url)` so a later pan retries | `MapWindow.tsx:281-306` | **CLOSED 2026-08-22e (registry edit 2026-09-11, audit #4 K1):** A1-4 shipped — NOT the row's literal one-liner (a bare `cache.delete` would 20 req/s-storm at the 20 Hz FPV repaint): `failedAtMs` + `TILE_RETRY_MS` 30 s cooldown + a warn capped at 8; audit #4 re-verified at HEAD (`MapWindow.tsx:66,92,306`). |
| T38 | 2026-08-22 | **Per-frame waste on the map/cone surfaces** (audit-3 A1-10/A1-11/A2-2): focalCone disposes + reallocates BOTH BufferGeometries every frame while the aim stick is held (`HFOV_EPS_DEG 0.1` is below the stick's rate) · both canvas radars call `getComputedStyle` per paint (~320 forced style reads/s at 20 Hz) for theme-static values · the /m PiP's second `renderer.render` re-runs the SHADOW MAP every frame (three only skips it when `shadowMap.autoUpdate === false`) on the most heat-constrained surface. All three are cheap, local fixes | audit-3 report A1-10/A1-11/A2-2 | CLOSED 2026-08-25 — all three sub-items verified FIXED at HEAD by the charter reconciliation: focalCone rewrites pre-allocated buffers (`scene/focalCone.ts:66-77`) · radar painters carry zero per-paint `getComputedStyle` (`panels/radarCanvas.ts`; remaining hits are one-time font reads at `scene/aimCones.ts:217`, `scene/streetNames.ts:137`) · the /m PiP pass is wrapped in `shadowMap.autoUpdate=false` (`GlobeCanvas.tsx:588-593`). Device numbers still unmeasured — that half stays under T1. NEW remainder found the same pass: the PiP still runs a FULL second `renderer.render` every frame while the map window is open — tracked as charter slice RC19 under T54, not re-opened here |
| T39 | 2026-08-22 | **`plannedView.hFovDeg` is unclamped in 4 of its 7 writers** (audit-3 A2-3): only the photo seed and `integratePlanned` clamp to `FOCALCONE.minHFovDeg 3 / maxHFovDeg 120`. Measured breaches: 1.27° (/m portrait at max zoom-in) and 122.4° (landscape at min zoom) reach `plannedView` via the FPV-exit seed → out-of-band cone on all three surfaces + a ~1624 mm AIM-stick readout. Fix once at the store seam (`store/camera.setPlannedView`) + a test at aspect 0.46 / 2.17 | `StylizedTiles.ts:2146,3250,3260,4048` | **CLOSED 2026-08-22e (registry edit 2026-09-11, audit #4 K1):** F2 shipped `clampPlannedView` applied once at `store/camera.setPlannedView` + tests reproducing both measured breaches; audit #4 re-verified at HEAD (`store/camera.ts:450`). |
| T40 | 2026-08-22 | **Docs debt from audit-3 Track D not executed this session**: `globe-tuning.md` documents NONE of the batch-#4→#7 tunables and lacks the sticky-overlay-px + injected-GLSL-header traps (D5) · `verify.md` names ZERO of the six harness classes `checklists/tests.md` item 10 requires, and omits the synthetic two-finger-twist recipe used by 9 scripts (D9) · `UXBATCH4_PLAN.md` fully shipped → `archive/` **after** D1/D7 re-home the `public/sw.js` description it uniquely carries (D8) · DECISIONS **compaction round 4 is DUE** — §Recent is 135 KB vs the 147 KB that triggered r3, and the "ladder parked" carve-out expired at U8; exact boundary measured: move `:319`→`:879` (34 rows, ≈79.5 KB) (D16) · append-marker re-drift (D13) · `knip.json` content drafted (D15) · guide re-shoots, 11 of 12 stale (D10) | audit-3 report §Fix-session slicing | OPEN — exact edits pre-written in the report. **DATED EDIT 2026-08-24d:** three sub-items moved. (i) `globe-tuning.md` — the batch-#4→#7 section WAS written, then **re-staled**: it names zero of the 54-leaf `BESTSPOT_SCORING_V1` / `tuning.BESTSPOT` families; a §BESTSPOT section + the renderOrder-ladder trap landed 2026-08-24d, so this sub-item is CLOSED for the second time and the pattern (a tunable family lands, the doc does not) is now the thing to watch. (ii) **compaction round 4 HAPPENED** 2026-08-22 (141.7 → 62.6 KB); what is now DUE is **round 5** — §Recent measured **146.7 KB** on 2026-08-24, above its own ~140 KB trigger, having re-grown 84 KB in two days; the natural carve is 2026-08-22j and older → `DECISIONS_ARCHIVE` §Moved, which returns §Recent to ≈44 KB. (iii) append-marker drift **recurred a third time** (the marker at `:357` sits below four dated entries) — re-seated 2026-08-24d, and the root cause is structural: appends go to the top and nobody re-seats the marker **2026-09-06f: compaction round 5 is 3× OVERDUE — §Recent 418 KB / 895 lines vs the ~135 KB trigger; scheduled as Track D1 of the owner-ordered one-session hygiene sweep (NEXT_SESSION_PROMPT 2026-09-06f): move verbatim 2026-08-21 → 2026-09-05 (through MS8) byte-identical to the archive with md5 + line/byte counts, one ≤ 12-line digest per moved era, the T77 era stays verbatim.** **— re-verified 2026-09-06: OPEN.** Round 5 runs as Track D1 of the 2026-09-06g hygiene session; the round-5 line (boundary, md5, line/byte counts) is appended by the session that performs the move, not by this check. **DATED EDIT 2026-09-06g — round 5 DONE:** verbatim 2026-08-21 → 2026-09-05 (through MESH SUITE MS8) moved byte-identical to `DECISIONS_ARCHIVE.md` §Moved 2026-09-06 — **md5 `4260321dc13ba45b11c3de0bb5c8856e`, 865 lines / 410,237 B** (two spans of the old file, lines 365–1185 and 1195–1238, because the append-marker sat between them); 12 era digests appended to §Per-phase digests; the append-marker re-seated directly under the §Recent heading (fourth drift); §Recent 437.0 KB → 27.0 KB; DECISIONS.md 466,847 → 69,668 B. The compaction sub-item of this row is CLOSED. Still OPEN from the original row: the `globe-tuning.md` currency debt (the 2026-08-27 ULTRA tunables are also undocumented there — E3 check 2026-09-06g) and the guide re-shoots (D10). Next trigger: §Recent past ~135 KB. |
| T33 | 2026-08-18 | UPSTREAM 3d-tiles-renderer 0.4.28: ImageOverlayPlugin `_onTileVisibilityChange` reads `tileInfo.get(tile).range` UNGUARDED (ImageOverlayPlugin.js:230 — every other consumer in the file checks `.has` first); a tile disposed mid-fade TypeErrors on fade-complete, aborting that dispatch's remaining listeners. U6 region flips hit it reliably; guarded listener swap shipped in `imageryGround.ts` | imageryGround.ts U6-guard comment + globe-tuning.md traps | GUARDED locally — re-verify/remove on version bump; candidate upstream PR |
| T41 | 2026-08-22 | **TWO focal readouts, two SENSOR AXES, one view** (found while re-shooting the guide, audit-3 fix session; mechanism CORRECTED 2026-08-22f after reading `Joystick.tsx:115-117`): the FPV HUD prints `focalFromVerticalFov(hud.fovDeg)` — the 35 mm-equivalent across the frame's **HEIGHT** (24 mm) — while the AIM stick's mm footer prints `focalMmFromHFov(hFovNow)` across its **WIDTH** (36 mm). **Inside FPV both derive from the SAME live vertical FOV** (`hFovNow = s.fpvHud ? horizontalFovDeg(s.fpvHud.fovDeg, s.fpvHud.aspect) : plannedView.hFovDeg`), so there is no second source of truth — the divergence is purely the axis. They AGREE at 3:2; a portrait phone (aspect 0.46) makes them read **23 MM and 75 MM for the same view**, visible together in `public/guide/fpv-m.webp`. Six consumers ride the height path, one the width path | `FpvControls.tsx:124` · `FpvHud.tsx:105` · `Joystick.tsx` footer | **CLOSED 2026-08-22g — owner ruled ACCEPTED AS-IS 2026-08-22e ("i am ok for now with this and understand the reasoning"); the DOCS obligation is now discharged in BOTH registers.** READER: guide topic `fpv-focal-axes` "Why two focal numbers" (`guideContent.ts`, placed after `fpv-focal`, crosslinked from `move-aimstick` and `fpv-hud`) — deliberately states NO literal MM pair, because the ratio is aspect-dependent and any literal becomes the next drift. ENGINEER: `conventions/globe-tuning.md` §Traps, dated row naming both constants (`FULL_FRAME_HEIGHT_MM = 24` at `sensors.ts:146`; the `18 =` 36/2 width half inside `focalMmFromHFov`, `plannedView.ts:21-25`), the FULL consumer list of each (six height / one width), the `Joystick.tsx:115-117` proof that one FOV feeds both, and the trap it prevents (a seventh consumer on the wrong axis, or "reconciling" the two). Re-open as a code item only if the owner later wants one convention |
| T42 | 2026-08-22 | **Guide re-shoot remainder (audit-3 D10)**: 4 of the 12 shots were re-taken on a warm cache through the NEW reproducible `scripts/shoot-guide.mjs` (`orbit`, `fpv`, `fpv-m`, `target`) and one NEW shot added (`fpv-map` for the `fpv-map-controls` topic). The other 8 still date from 2026-08-15: `welcome` · `time` · `skymenu` · `plan` · `find` · `sunsets` · `upload` · `shell-m` (shell-m is 2026-08-20, the least stale). Each needs a recipe added to `RECIPES` in that script — the crops and what each frame should contain are owner-taste calls | `scripts/shoot-guide.mjs` · `mem:project/wip-2026-08-22-audit3-fixslices` | OPEN — 8 remaining; the harness exists. **2026-08-22g additions, from the guide-finalization pass:** (a) every `GuideMedia` now DECLARES its `w`/`h` and `guideContent.test.ts` reads the real webp header — a re-shoot that changes a crop now fails the suite instead of drifting, so the declared size must be updated in the same commit; (b) `upload.webp` was found to show the DROP ZONE, not "a decoded RAW projected at its capture spot" — the caption was corrected to match the shipped image, so this shot's re-take is now a CONTENT decision (shoot the projection, or keep the drop zone and the new caption stands); (c) `orbit.webp` needs NO re-shoot for the camera-deck fix (mtime 2026-08-22 03:34 already shows three rows) — only the copy was wrong, and it is fixed; (d) the crops remain owner-taste, which is why this stayed open **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED). |
| T43 | 2026-08-22 | **FPV far-field fidelity — 26 ranked gaps + a 21-slice ladder, ALL UNIMPLEMENTED** (owner fenced the session to research: *"do not implement anything in this session"*). Produced by a 65-agent read-only investigation over six pipeline dimensions with per-gap adversarial refutation — **43 of 58 proposed gaps survived, 15 were refuted** (the refuted set is listed in the report so it is never re-discovered). The three reported symptoms resolve as: **"rough textures afar" = CAUSE IDENTIFIED** (the draped ground composite is built `generateMipmaps=false` with `anisotropy=1` and sampled at grazing incidence — `RegionImageSource.js:112-114`, `Texture.js:810`; `maxAniso` is computed at `StylizedTiles.ts:238` and handed to baseEarth + streetNames but NEVER to `attachImageryGround`); **"misaligned heights" = CAUSE IDENTIFIED** (`heightAt` takes `intersectObjects(tiles.group.children)[0]` = the TOPMOST hit, bypassing the shipped tileset BVH and including coarse ancestors that `TilesFadePlugin` keeps parented for the full 700 ms of every refine — and the patch attaches as deeper children of those ancestors, so the stale parent wins by construction, `imageryGround.ts:483-493`); **"floating buildings" = FOUR mechanisms, all confirmed present, NONE measured against each other** — the bake authors 20×20 km on ONE tangent plane at u=0 (0.7 m @ 3 km → 15.6 m @ the corner), GLO-30 is a **DSM** so every footprint sample contains its own roof, global OSM buildings are never re-seated over the patch, and the per-feature seat sweep has no look bias. **Do not start coding from this row — start from §6 (14 open measurements), especially M5**, which is the single measurement that separates the four floating mechanisms and decides the slice order. Explicitly REFUTED as a floating cause: depth-buffer precision (it cannot displace geometry vertically). Note one consequence of the same-session `everest` bake: that region is terrain-only (`variants: []`), so the global OSM path leaves its buildings unseated over 30 m-accurate ground — captured as F3/M8, pre-existing globally, newly visible there | `.claude/claude-docs/rendering/FPV_FIDELITY_AUDIT_2026-08-22.md` · DECISIONS 2026-08-22h · `mem:project/wip-2026-08-22-owner-3slice` | OPEN — report complete, **zero code written**; subsumes nothing (T34 and RENDERING_QUALITY_PASS P12 are cited by it and stay their own rows). **DATED EDIT 2026-08-25:** consolidated into `rendering/RENDERING_CHARTER_2026-08-25.md` (T54) — every gap re-verified against HEAD 1a486c9 with fresh anchors. Since-the-audit deltas: #16-tracking/#9/S1 shipped **ULTRA-gated** via T44/T45 (their baseline halves are charter §4 A/B items); **S10's premise REFUTED** (desktop imagery availability-capped — errorTarget 0.05 → zero extra tiles, `tuning.ts:584-587`); S12 rephrased to "generalize the shipped ULTRA edge seam"; S17's surface grew to 22 raw-ShaderMaterial instances / 14 modules (+3 from `bestSpotSheet.ts`). **Start implementation from the CHARTER'S ladder, not this row or the audit's §5**; the audit stays the mechanism reference and its §4 refuted table stands in full |
| T44 | 2026-08-22 | **HQ 3D MAP — the owner asked for it, it was BUILT IN FULL, MEASURED INERT, and removed rather than shipped as a decoration.** Owner order 2026-08-22i: *"HQ bright and crisp hidef map load both in 2d and 3d … you tilt and keep amazing textures from 2d map projected onto all terrain geometry"*, desktop-only, off by default. Implemented exactly as designed — a bounded `SphereRegion` on its own `LoadRegionPlugin`, deliberately NOT widening `mapFlatNow()` (which would drag the photographic grade, bloom, the shadow rig and the depth-test standdown with it — a C2 breach). Verified attached, correctly placed at the view focus 18 km out along a 72° tilt, at the right radius, with `mapFlat` still false. **It produced ZERO extra tiles at 895 m and at 5,969 m.** The control that settles it: forcing the GLOBAL ground `errorTarget` to **0.05** also produced zero, so the ground is **availability-capped** (Esri z19 + patch L13), and `GROUND.errorNearAlt` is **60 km** — below that the 3D target already sits at its finest (2). **Conclusion: on desktop there is NO unspent imagery-refinement headroom, so the premise "the 2D chart holds detail the tilted 3D view lacks" is false.** What the owner actually sees when tilting is grazing-angle minification — corroborating T43 §1.1 from the opposite direction. **THE FEATURE IS NOT DEAD, THE LEVER WAS WRONG**: the delivering lever is **anisotropy** on the ground drape composites (built `generateMipmaps=false` / `anisotropy=1`; `renderer.capabilities.getMaxAnisotropy()` measured **16** on the owner's machine). Ship it as T43 slice **S1**, gated behind a re-instated HQ chip so it stays opt-in — which also lets S1 land without changing anyone's default. The chip/pref/store/CSS/gate scaffolding is straightforward to re-add; the design is in the workflow plan and the removal is one commit to read | **`ULTRA_PLAN.md` §1 (the handover)** · `rendering/FPV_FIDELITY_AUDIT_2026-08-22.md` §1.1 + S1 · DECISIONS 2026-08-22i | **CLOSED 2026-08-22j — SHIPPED.** Both halves landed under the `ULT` chip (the owner's "HQ chip or fold under ULT?" question resolved as FOLD — their own words say "manual ULTRA mode"). **§1a** = a NEW `uFtwPhoto3d` uniform driving the same `photo` term the chart uses, with `dayK` deliberately untouched; **§1b** = anisotropy 16 stamped at CREATION by wrapping `TiledRegionImageSource.prototype.fetchItem` (the unique choke point — BOTH the compose `CanvasTexture` and the `.clone()` fast path return through it, and the library exports neither the class nor a hook). Browser-proven: composites go 1 → 16 taps as new tiles stream, and OFF returns them to the library default 1. Note `minFilter` was ALREADY `LinearMipmapLinearFilter` — the gate three needs — so one field was sufficient. Mip chains deliberately NOT built (transparent-border bleed + tile-seam clamping). See DECISIONS 2026-08-22j · `mem:project/wip-2026-08-22-ultra-track`. Prior note (kept — it is the finding that unblocked the row). The owner's own words split it in two: **"very grayish" = the photographic grade** (`photo` at `imageryGround.ts:377`, separable from the `dayK` force at `:372` — drive it from a NEW uniform and day/night stays real) and **"less resolution" = anisotropy**, NOT tile depth |
| T45 | 2026-08-22 | **ULTRA shadows + atmospherics — "push the shadow game to the limit"** (owner 2026-08-22i, next-session order). Wants, in ULTRA only: much crisper and more physically plausible BUILDING shadows (*"current shadows ok for representation purposes, but they are quite naive and linear"*), better sunrise/sunset AND moonlight, and — the named killer feature — **TERRAIN THAT CASTS SHADOWS** (*"amazing looks in high mountain regions"*). Owner explicitly rules OUT ray tracing (*"even macbook m3 may die"*) and asks for a survey of what game/3D engines actually do that a browser can hold. Ranked candidate set + the codebase-specific traps for each are in `ULTRA_PLAN.md` §2: **S2 PCSS/PCF soft shadows FIRST** (contact-hardening penumbra is most of the "physical" feeling at the smallest blast radius) → S5 bias/normal-offset/size tuning → **S3 terrain casts** (the ground is unlit `MeshBasicMaterial` + `ShadowMaterial` twins, so it RECEIVES but never CASTS; the cast shadow camera must cover a far larger extent than buildings, which couples it to any cascade split — prototype at the Everest bake) → S4 aerial perspective on ground+buildings (absent entirely today, `scene.fog` unused, the sky dome's `skyHazeBelow` is geometrically unreachable at `camera.far*0.45`; the single biggest sunrise/sunset lever, C2 risk both ways) → S1 CSM only if S2+S5 fall short (three ships `csm/CSM.js` but it rewrites light setup and injects into every receiving material — ~19 raw ShaderMaterials here). **Recommend REJECTING real-time GI for v1 with reasons stated.** HARD CONSTRAINTS: `shadowMapSize`/`shadowsEnabled`/`AO.enabled` are CONSTRUCTION-TIME (`GlobeCanvas.tsx:116,:222-226`; `tuning.ts:314`), so ULTRA must either edge-apply through `QUALITY.ultraDesktop` or size the rig at BOOT from the persisted pref — no third path; tier `high` stays byte-identical (`quality.test.ts` locks it); moonlight keeps its K&S-1991 model; per-frame ms measured before/after (opt-in ≠ 12 fps). Known-relevant audit gaps already verified: #17 FPV shadows switch OFF on any at-or-above-horizon look · #16 `HemisphereLight` uses ECEF +Y not local up and never tracks the ephemeris · #9 no aerial perspective anywhere · T10 GTAO wired default-OFF **OWNER STEER 2026-08-22i (second message), after accepting the GI rejection:** *"just make sure that whatever we choose makes transitioning from day -> dusk -> night and vice versa more epic, not only in terms of shadows but general realistic atmosphere and global light feel."* **That is the ACCEPTANCE CRITERION for the whole row and it re-ranks the work** — shadows are one contributor, not the objective. S4 (aerial perspective) is promoted to co-primary, and a NEW light-transport sub-track outranks CSM and terrain-casts: **S9** replace the `dayK = smoothstep(EARTH.termBand, sunUpDot)` ramp at `imageryGround.ts:369` — a smoothstep over a DOT PRODUCT, almost certainly the root of "naive and linear" — with the REAL sun elevation and the twilight thresholds (−0.833/−6/−12/−18°) the ephemeris layer already computes for the planner and the scrubber bands, giving each band its own colour/intensity response (ground + buildings + key light must move TOGETHER or they disagree at the terminator — the same incoherence that made forcing `dayK` a C2 breach in §1a) · **S10** key/ambient/hemisphere track the ephemeris, fixing audit gap #16 (`HemisphereLight` is along ECEF +Y, not local up, and never tracks the sun — wrong everywhere but one meridian) · **S11** ease `toneMappingExposure` with sun elevation (static 1.0 today; live-writable, no recompile; the cheapest "epic" lever there is). Revised order: **S9 → S11 → S4 → S10 → S2 → S5 → S3**, CSM only if S2+S5 fall short. **GI stays rejected CONDITIONALLY** — if the timelapse still reads flat after S4+S9–S11, re-open with irradiance probes over the STATIC base earth only (a streaming tileset cannot hold baked probes). **JUDGE AS A TIMELAPSE, NEVER SINGLE FRAMES**: fixed pose over a city and over Everest, scrub continuously day→golden→civil→nautical→astronomical→night and back, ULTRA off vs on, per-frame ms recorded across the same sweep | `ULTRA_PLAN.md` §2 · `rendering/FPV_FIDELITY_AUDIT_2026-08-22.md` gaps #9/#16/#17 · T10 | **CLOSED 2026-08-22j — SHIPPED, and the plan was WRONG about its own S2.** Owner ruling that unblocked it: *"even if it is sub 15FPS but graphics fidelity improves … worth it, user enables it in it's own volition anyways"* — which supersedes the row's own "opt-in ≠ 12 fps" constraint. Measured +18% frame time (33 → 28 fps at a city, dev build). Shipped in the owner's re-ranked order: S9 twilight-band day curve → S11 exposure ramp → S4 aerial perspective → S10 ephemeris hemisphere (audit gap #16 killed) → S2 soft shadows → S5 8192² map → **S3 TERRAIN CASTS** (browser-proven at Everest). **FIVE source-verified findings changed the plan**: `PCFSoftShadowMap` is DEAD CODE in three 0.185 (S2 as written was a no-op; the real lever is `shadow.radius` on a per-pixel-rotated Vogel disk, and it is LIVE) · `shadow.bias`'s unit is a FRACTION of the near→far range, so ULTRA's 96 km depth range would have turned −2e-4 into −19 m of peter-panning (now derived from METRES) · terrain casting fails SILENTLY without `shadowSide = FrontSide` · `mapSize` is LATCHED (a runtime set is a no-op — hence the boot-read) · a shadow target costs 2× a depth-only reading (8192² ≈ 512 MiB). CSM / PCSS / VSM / GI all REJECTED with reasons recorded. See DECISIONS 2026-08-22j · `mem:project/wip-2026-08-22-ultra-track` |
| T46 | 2026-08-22 | **LEO umbral SPOT never drawn** — the one gap the 2026-08-22k eclipse ship left OPEN, deliberately and with the reason recorded. From orbit a solar eclipse should darken a ~100 km spot on the globe; the shipped model darkens the observer's whole world, which is correct at street level (you are INSIDE the umbra) and wrong from space. Rather than lie at both scales, the ground's `uFtwEclipse` is ALTITUDE-GATED on the same `ATMOSPHERE.skyFullAlt/skyGoneAlt` ramp the sky regime uses (`StylizedTiles.ts` `stepEclipse`), and `scene/baseEarth.ts` — the orbital ellipsoid — is deliberately NOT wired at all. Drawing the real spot is contained: `baseEarth` already holds `uMoonDir` and the fragment's geodetic normal, so it needs a `uMoonDistM` uniform and the umbra/penumbra cone test the pure lib already implements (`discCoverage` + the Meeus shadow-cone radii). NOT a regression — this scale has never had an umbra | `src/lib/ephemeris/eclipse.ts` · `scene/baseEarth.ts` · `StylizedTiles.ts` `stepEclipse` · DECISIONS 2026-08-22k | OPEN — scoped, not started; the pure math it needs already exists and is almanac-tested **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED). |
| T47 | 2026-08-22 | **ECLIPSE taste pass with the owner** (the tail of the 2026-08-22k ship — the T11-class "numbers are shipped, the judgement is not" row). Four knobs in the `ECLIPSE` block of `tuning.ts`, in the order most likely to be asked for: **`umbraLight` 0.055** — how bright the eclipsed moon stays inside Earth's umbra. The honest physical number is ~1e-4 of a full moon (10–12 magnitudes down), which would render invisible; this constant IS the eye-adaptation fudge, and C2 is satisfied by applying it ONCE, in one named place, rather than smearing it through the shader · **`shadowSoftFrac` 0.09** — the umbral edge may want softening (a real umbra has no sharp top; the lunar limb by contrast is genuinely knife-sharp and must stay so) · **`daylightFloor` 0.04** — how dark totality goes · **`coronaGain` 1.15** — deliberately just over `BLOOM.threshold` 0.9 so bloom carries the outer streamers, but it must still read on the paths where bloom is OFF (tier `low`, coarse pointer, flat map). Also judge the PARTIAL-phase silhouette: it reads mid-grey rather than black because earthshine is MAXIMAL at new moon (physically right, but it is the first thing the eye lands on). Judge it as a SCRUB through the 2026-08-12 Burgos totality, never as single frames | `tuning.ts` §ECLIPSE · `verify-shots/eclipse-0{1..6}-*` · DECISIONS 2026-08-22k | OPEN — owner-taste; shots already captured, `scripts/verify-eclipse.mjs` re-runs the whole sweep **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED). |
| T48 | 2026-08-24 | **FPV mini-map draws culverted drains as visible watercourses.** `minimapFeed.ts:93` skips `line.tunnel` ("invisible in the world → invisible on the map"), but the waterway branch hard-codes `tunnel: false` (`vectorTiles.ts:528`), so a culverted stream is never skipped and paints as an open watercourse. PRE-EXISTING; found and deliberately scoped OUT during the BEST SPOT parser widening, which is ADDITIVE BY CONTRACT and may not change consumer behaviour (`vectorTiles.ts:26-34`). The honest field already exists — `VecLineFeat.brunnel` (`vectorTiles.ts:62-63`) carries the raw OpenMapTiles value for BOTH kinds. **Fix = read `brunnel === "tunnel"` at the two consumer sites (`minimapFeed.ts:93`, `vectorFeatures.ts:312`), NOT by flipping `tunnel`** — flipping it would delete the drain from the mini-map instead of hiding only the culverted run. The source comment says "Recorded 2026-08-24" and until now there was no registry row to record it in | `vectorTiles.ts:54-61` · DECISIONS 2026-08-24c | OPEN — cosmetic; one consumer-side read |
| T49 | 2026-08-24 | **RE-SCOPED 2026-08-27 — THE TASTE PASS RAN AND REFUTED ITS OWN TARGET LIST. READ `.claude/claude-docs/bestspot/MEASUREMENTS.md` §7 BEFORE TOUCHING ANY KNOB.** All four named primary targets below were **measured NOT to move the case the owner actually reported**: `accessSoftExponent: 0` → pick still 0 · `lCeilDeg: 30` → 0 · `altScaleDeg: 30` → **field BYTE-IDENTICAL to base** · `depthTrustRadiusM: 200` → 0. Two further corrections to the row as written: the leaf count is **55**, not 54 (`CLASS_TABLE`; the test pins only `>= 50`, and `SWEEP_MODE_SCHEDULE.md` D7 counts 53 — verify then pin, do not edit the number); and **"every one is a recompose" is FALSE** — `trackWeight.*` was reclassified `reweigh` → `rescore` on 2026-08-26g (a live defect: `reweigh` never rebuilds the track while `V` integrates against `w`), `trackWeight.topAltDeg` ships class **`resweep`**, and ZERO `reweigh` leaves remain. The real blocker is **T59**. Original row follows, for provenance only: **BEST SPOT taste pass — the T11/T47-class "numbers are shipped, the judgement is not" row.** `BESTSPOT_SCORING_V1` has **54 tunable leaves** and the shipped values for several are unswept judgements rather than solved numbers. Named primary targets: `graze.conf` (terrain 1.00 · building 0.90 · deck 0.90 · tree 0.45, tree clamped ≤0.6) · `graze.reliefHiDeg` 0.40 · `displayLo/displayHi` 0.15/0.90 (these come from the AS-BUILT hero numbers, not a solved disc, and they decide whether a 1.7 m disc reads as "honestly empty" or "broken") · `worth.effectiveFloor` 0.35 (R7, explicitly "proposed, tunable") · `inkMin/inkMax` (**`inkMax` > ~0.40 crosses `BLOOM.threshold` 0.9 and the sheet smears** — a cross-family coupling) · `veilMin/Max` · `contourStep`. Cheap by construction: every one is a **recompose** (0.3–3 ms, exactly ONE job) or a repaint, never a rebuild — that is what landing S3a before the solver bought. Console: `__globe.bestSpotTuning({...})` · `.ab(a,b)` (rank delta + Spearman ρ) · `.export()` (paste-ready patch). **Judge at BOTH lifts**: a 1.7 m central-Dnipro disc is legitimately 97.7 % black with max 0.381 — that is physics, and the top-8 markers are the product — while a 56.7 m sheet has mean score byte 159. A pass that looks at only one end tunes the wrong end | `BESTSPOT_PLAN.md` §AS BUILT · NEXT_SESSION_PROMPT | OPEN — owner-taste class; needs the owner present |
| T50 | 2026-08-24 | **BEST SPOT is UNVERIFIED on Wix cloud.** Tier at ship is LOCAL + BROWSER (`scripts/verify-bestspot.mjs` 100 PASS, shots `bestspot-01…08`); prod is dark behind the owner's nameserver gate so nothing has run there. Specific cloud risks, none of them exercised: the module worker's `new Worker(new URL(…), {type:"module"})` under the Wix CDN's bundling and Content-Type handling (the same class of unknown that `public/sw.js` carries in T2/#15) · the 126 KiB / 50.6 KiB gz worker chunk on a cold cache · whether the 3 m rung's 356 ms holds on the hosting tier's CPU budget. **Rides the same `wix release` as T2's canaries — verify in the same pass, not a separate one** | DECISIONS 2026-08-24c · NEXT_SESSION_PROMPT §Standing | OPEN — blocked on the release gate **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED, "T50 cloud verify"). The release gate it waits on is still shut (NSP §Standing: prod is dark until the nameserver → TLS → OAuth chain completes).  **2026-09-10: prod is no longer dark (DECISIONS 2026-09-10b) — the cloud verification can run against `https://www.plux.today` next session.** |
| T51 | 2026-08-24 | **BEST SPOT `/m` twin (S8) and MapWindow/MiniMap DOM-canvas twin (S9) deferred by owner ruling.** Tracked, not forgotten: Phase 8's own build rule (`IMPLEMENTATION_PLAN.md:192`, "desktop surface … → mobile surface in the twin M-phase") makes a mobile twin a standing obligation rather than an omission, and the desktop-only gate is enforced AT THE READ (`StylizedTiles.ts:809` `bestSpotAllowed = !isMobileShell && !coarsePointerShell`), so the feature is currently invisible on `/m` by construction. S8 flips that gate and owes a sheet twin; S9 owes the DOM-canvas surfaces (the expanded chart + the FPV mini-map) a painter, since they cannot read the GL sheet. Un-park with the rest of the parked ladder | `BESTSPOT_PLAN.md` §10 S8/S9 · owner ruling 2026-08-23 | DEFERRED — owner ruling, not debt **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED, "S8/S9 twins (T51)"). |
| T52 | 2026-08-24 | **SIX suspected-false invariants in the BEST SPOT math, raised by the Lean specification pass and NOT yet reproduced.** Two siblings (unclamped `graze.conf.*`, unfloored `weights.*`) were confirmed executably and FIXED the same session; these six are code-reading arguments and must reproduce before they are believed. **S2 (highest priority)** — the fused `solveTerms` pass may NOT equal `cellScore` at a finer azimuth lattice: the pipeline sweeps azimuth `i` then scores `i−1`, so only ONE azimuth of lookahead is ever filled. At the shipped 0.25° step the outermost column midpoint is ~0.95 lattice steps out, so ±1 suffices; at `azStepDeg 0.125` — documented as "the desktop-high option" and asserted reachable — it is ~1.90 steps and the ring cannot hold `j+2`, so the two paths would pick different rays. Reproduce by running both on a `{azStepDeg: 0.125}` track. · **S1** — the hull cache uses TWO different equivalence relations (`snapAz` rounds to 1e-4°, the acceptance test is bit-exact `===`); they coincide only because `azStepDeg 0.25` is dyadic, and a miss falls through to a silent rebuild that nothing reports. No test asserts `hullBuilds < K` after a DAY step. · **S9** — `bestSpotSolver.test.ts:1677` promises "the values must be identical, not merely close" and asserts only `0 ≤ want ≤ 1`: a pin that cannot fail, same class as audit-3's five. · **S8** — `refractionK`/`earthRadiusM` are folded into the hull but are not in its cache key (latent: not a profile leaf today). · **S6** — `composeScores` computes `wDotT * (1/wTotal)` while `cellScore` computes `wDotT / wTotal`; not bit-identical in IEEE, though both docstrings say "bit-identical" (the claim is really about key ITERATION ORDER — reword the prose, the 1e-12 tolerance is right). · **S5** — `grazeStepRadii` differs per-cell vs per-solve by design, but `CellScore.grazeStepRadii`'s docstring describes the other one. Also unpinned: `bestSpotTrack.ts:388-390` asserts "`f(a) ≤ this` is a geometric identity" — the whole justification for the horizon-ceiling weight having no free scale — with **no test at all** | `FORMAL_VERIFICATION.md` §5 · `formal/Ftw/` | OPEN — S2 first; `fast-check` + the fused-pass metamorphic relation reproduces or kills it **— re-verified 2026-09-06: PARKED** (NEXT_SESSION_PROMPT §PARKED). |
| T53 | 2026-08-24 | **The verification ladder from the 2026-08-24d toolchain evaluation — five items that pay for themselves inside a week, none adopted yet.** (1) **`fast-check` + `@fast-check/vitest`** — 30 min; `@fast-check/vitest@0.4.1` peer-deps `vitest ^4.1.0` against this repo's `^4.1.9`, an exact match. 1,905 tests assert on 1,905 NAMED POINTS; this searches the space between them, and the magnets here are the 0°/360° azimuth seam, degenerate/collinear hulls, and `horizonSweep`'s use of NaN as its in-band unknown marker. (2) **Differential vs JPL Horizons, cached as JSON fixtures** — `astronomy-engine` is the primary implementation so it cannot be its own oracle. Already run live: sun over Dnipro agrees to **0.5″** in azimuth; altitude looked 1.5′ off until `APPARENT='REFRACTED'` was passed, after which **sub-arcsecond in both** — the trap is the ORACLE'S DEFAULT, and it is now documented before it costs a session. (3) **Metamorphic properties** — the first one to write is the fused pass vs `cellScore` over generated DSMs (kills or confirms T52/S2). (4) **`robust-predicates`** — Shewchuk's exact `orient2d` IS the turn test in `horizonSweep`'s monotone stack; note the port uses a DOWNWARD y-axis. (5) **`glslangValidator`** as a lint gate — `sky.ts` (619 lines) and `atmosphere.ts` (231) are untested as programs; `glsl.test.ts` covers only float-literal formatting. Worth a day once: **Stryker SCOPED to `bestSpotMetric.ts` + `horizonSweep.ts`** (~5–20 min cold; **Node here is v20.19.2 so pin `@stryker-mutator/vitest-runner@9.6.1` — v10 requires Node ≥22**) to find out whether the golden tables DISCRIMINATE rather than merely pass · **`pixelmatch`** reference-image diffing (~80 % of the rig exists in `verify-chrome.mjs`; `sharp` is already a devDependency; macOS headless Chromium uses the REAL GPU) · **Wolfram Engine** fixtures. **Say no to** interval arithmetic in JS (no `fesetround`, and WASM has no rounding-mode instruction at all) and to Gappa/Why3/Frama-C/Flocq/Daisy (all require rewriting the kernel in another language) | `FORMAL_VERIFICATION.md` §4 | OPEN — ordered; start with (1) then (3) |
| T54 | 2026-08-25 | **RENDERING CHARTER — the execution charter for the autonomous graphics-engine uplift session** (authored by a 7-agent design/prep pass: 4 owner-bug root-cause investigations + 2 audit-reconciliation passes vs HEAD 1a486c9 + a mobile/ULTRA inventory). Contents: **four owner bugs root-caused** — **B1** Esri "Map data not available" is an HTTP-200 placeholder JPEG (2,521 B, ETag `"vvvvvvvvvvvvf"`, live-probed; z19 coverage at Everest is a sub-km island) that the failure-only retry path can never see, pinned by `force-cache` + `sw.js`; fix = fetch-wrapper detect → parent-quadrant crop-upscale (RC5) · **B2** the totality square = the corona outer power law truncated at ~0.003 linear by the sun-quad boundary + the unconditional ±1/256 dither with no discard in the sun fragment; fix = radial window-to-zero + epsilon discard, `stars.ts:168-171` idiom (RC1) · **B3** the sunset snap = `castShadow` boolean flip at sun elev +0.46° with the key at its daily max 2.025, plus a same-frame sun→moon source flip and a phantom below-horizon key lighting walls all night; fix = `shadow.intensity` ramp (verified live uniform in three 0.185) + handoff lerp (RC2), "real dusk" baseline package = owner A/B (AB1) · **B4** the `!!focusHit` shadow-kill gate (survived ULTRA verbatim, `StylizedTiles.ts:4023`) + ortho framing tied to camera altitude (foreground coverage needs pitch ≥ ~59° base / ~42° ULTRA; outside the box = fully lit, hard edge); fix = gate kill + surface-projected fallback (RC3) + rig-only `_shadowFocus` framing (RC4). Plus: the reconciled audit ladder (seat core RC6-11, bake ×3 regions RC12-17), pipeline/mobile (governor split RC18, PiP RC19, T34 lever RC20, on-demand render RC21), ULTRA tails (eclipse-haze scale RC23, dome seam RC24, capped mips RC25, chip-flip UX RC26), depth held-until-evidence (RC28), owner A/B queue AB1-AB7, and the final as-built `RENDERING_ARCHITECTURE.md` (RC30) | `.claude/claude-docs/rendering/RENDERING_CHARTER_2026-08-25.md` | **DATED EDIT 2026-08-26d — THE CHARTER IS CLOSED. RC16 and RC21, the last two rows, both shipped; no RC row is open.** RC16 shipped **without the margin bake / crossfade ring the audit sized it for**, because `scripts/bake/measure-straddlers.mjs` showed the counter that justified that cost — `droppedOutside` — was **two populations wearing one name**: 96–99.9 % of it is DISJOINT geometry from OSM2World's relation-recursed extract (median 761 m / 40 km / 36 km out, max 55 / 653 / 149 km), correctly dropped, and only **123 / 61 / 1** features were real straddlers whose drop left a half-building notch. The straddlers were **already harvested** (`way["building"](bbox)` returns whole INTERSECTING ways), so there was no data gap and no re-fetch: one rule in `lib/geo.mjs` — **ownership by bbox INTERSECTION, placement by clamped centroid** — replaced the two bakers' disagreeing inline rules (zero coverage before; `dnipro` ships both sides of that A/B seam). Predictions written before the re-bake all hit to the unit (+123 / +61 / +1) and the extruder bakes came out **byte-identical**. LIVE on R2, with the recovered OSM ids found in the shipped sidecars. **Residual left on purpose and measured:** an owned feature still pokes outside the prism where Cesium also draws it — 0.03–0.42 % of features, median 3–10 m; a duplicate sliver, not a hole, and growing the prism to fix it punches a real hole. RC21 ships **OFF**: the charter's "predicate covering every animating source" is refused on the recon's own numbers (40+ sources / 20 files / ~14 no-snap eases; a false negative is a FROZEN GLOBE), and `pipCache`'s heartbeat shape is reused instead — `maxStaleMs` 200 ms is the safety net, `restMs` 6,000 ms is **sized** at 6.2 × `ULTRA.exposureTauMs` and unit-pinned, both rollbacks are unit-LOCKED, and it buys **GPU and power, not CPU**. The default flip is an owner call pending an ULTRA-timelapse soak. **Prior status 2026-08-26c — Group D is half done: RC13 (base skirt) and RC17 (meta.json sidecar consumption) SHIPPED across all five building bakes and browser-proven (`scripts/verify-bake-ladder.mjs` 8/8); RC15 (DSM→DTM) is **REFUTED BY MEASUREMENT** for buildings and re-scoped to canopy as T58. STILL OPEN: RC16 and RC21 — nothing else.** RC13 ships as a TRANSLATION of the existing wall rim, not an appended course of quads: **+0 vertices** against the +59 % bake-wide / +78 % on `Building` an appended skirt measures on the o2w soup, so audit S13's own "+≤10 %" clause is met at 0 %. Its two guards came from data, not assumption — `min_height` masses are left alone (that gap is drawn on purpose), and the o2w `Cliff`/`RetainingWall` classes come back at `minY === maxY === 0` where a skirt would hang a curtain off a zero-height line. RC17 unifies the two bakers' split sidecar schemas into one (`scripts/bake/lib/meta.mjs`, schema 2) with `base`/`top` MEASURED from emitted vertices, and replaces `ENRICHED.overrideMinPickHeightM` as the pick fence with a per-feature class token: **89 non-building features reclaimed in one central-Dnipro view**, and the constant's own comment was wrong by ~7× (claimed 4.5 % street furniture; the Chernobyl o2w bake is 30.6 %, including 273 pickable-and-rescalable transmission pylons). The correctness hinge is an ORDERING fix: the fetch plugin now resolves the model behind its sidecar, so class and true base are present at `load-model`, where the pristine capture and the U8 override re-apply both happen. **Three defects with no charter row were fixed on the way**: the o2w dedupe keyed on the glTF node NAME (which OSM2World fills from the OSM `name` tag) and silently dropped +231/+24/+18 distinct buildings; a re-bake could never have reached a returning browser (`immutable` glbs + reused filenames → `?v=<tilesetVersion>` on every content uri, stripped back off for the persistence key); and `droppedOutside`/`droppedPolygon` were logged but never persisted, so RC16 had no baseline to regress against. **M11 was taken and it is what refutes RC15** — see T58 for the numbers and for the half that survives. **Prior status 2026-08-26 — Group E + RC25 + RC30 SHIPPED. `scripts/verify-rendering-charter.mjs` **85/85 ALL PASS**. STILL OPEN: Group D (RC13/RC15/RC16/RC17) and RC21 — nothing else.** RC18 splits the tier apply so a governor PROMOTE's tile levers land inside FPV while the renderer half parks (browser-proven: tileTier=high against tier=low, DPR unchanged, ZERO overlay rebuilds); a DEMOTE never splits, and the safety argument is a unit test over the tier TABLE's monotonicity, not prose. RC20 ships the ground-LRU flip bank for mid/low and **ANSWERS M13: the desktop half of T34 does not exist** (on `high` the cache rests at 109.8 MB against a 322.1 MB floor, so the trim condition never arises). RC19 caches the /m PiP into a render target plus a per-frame blit — and **the banked colour note was backwards**: three forces `NoToneMapping` for anything rendered into a render target, so the blit is the only place tone mapping can apply and `toneMapped` must stay TRUE. RC25 ships the capped chain — **browser-proven 357/357 composites at exactly 4 levels, VRAM ×1.328125 = 85/64 to the digit, off-state proven by RETURNING to it** — after four charter premises died (the `TEXTURE_MAX_LEVEL` crux does not apply to three's immutable texStorage2D path; the artefact is a double alpha multiply, not a colour bleed; the dominant seam is a C0 discontinuity between composites that no filter removes; and 4 levels is the ceiling the ≤33 % budget allows, not a range). **A measurement rewrote RC25's implementation:** the `getImageData`-plus-JS-filter version cost **5.36 ms per composite on the main thread** and stalled the page so hard that CDP evaluations stopped returning for fifty minutes; canvas `drawImage` halving is **0.06 ms, 89× cheaper**, and is the correct filter because canvas 2D composites in premultiplied alpha. RC30 shipped as `rendering/RENDERING_ARCHITECTURE.md`. **RC21 was deliberately NOT built** — recon mapped 40+ per-frame visual-change sources across 20 files, ~14 asymptotic eases with no snap, and a false negative is a frozen globe; it also established that `tilesHandle.update()` must run every frame regardless, so the slice buys GPU and power, not CPU. **Group D is UNBLOCKED, not blocked** — R2 credentials, Java 21, the OSM2World jar, 1.7 GB of Overpass caches and the three existing bakes were all confirmed present 2026-08-26. Prior status: **PARTIALLY SHIPPED 2026-08-25c/d/e — Groups B (RC1–RC5), C (RC6–RC11) and F's four implementable slices (RC23/RC24/RC26/RC27) DONE and browser-proven, plus RC29 and RC22 — 70/70.** **RC12 IS REFUTED — do not build it:** M5 measured the tangent-plane curvature residual the per-cell re-seat cannot absorb at **0.568 m against 14.20 m rms of within-cell relief (4.0 %)** at the 3.5–4 km ring, because every cell is re-seated onto terrain at its own centre so the rise is bounded by the CELL radius, never the bake radius (`__globe.enrichedSeats().m5` re-derives it). **RC28 also has no evidence** — RC0 measured DEPTH_BITS 24 over a 1.0 m near plane and no shimmer case appeared in ten browser legs. **RC25 and RC18/RC19/RC20/RC21 are DEFERRED, not refuted**, each with a reason on record; RC19 carries a banked design finding — half-rate is NOT viable because `composer.render()` overwrites the PiP rect every frame, so it needs a render target plus a textured-quad blit. Also refuted by measurement: **M7's fading-parent mechanism** (`hitsPerSample` exactly 1.00 over 37,742 at-rest + 9,874 mid-refine samples; the parent won ZERO times — RC6 ships as cheap insurance, not as a fix for anything observed). Two OPEN TAILS from Group C: RC7's look-cone convergence is 50.3 % vs the audit's S4 bar of 0.9 (instrument why the drain defers — gate rejections are counted, null-terrain deferrals are not), and RC9's warm-restore leg is browser-UNVERIFIED (banking proven by squeezing the LRU; **distance alone never evicts** — the 6 km bake fits inside the desktop cap). New evidence for owner A/B **AB4**: RC4's near-level shadow framing trades 0.78 → 2.44 m per shadow texel for shadows where there were none (`SHADOWS.viewFitK: 0` reverts the extent, keeps the re-centring). Group F's headline: both ULTRA seams closed on ONE principle — **under an eclipse, and at the terrain/sky junction, ULTRA's day-driven additions ride the ground's own effective value rather than authoring a second curve** — with EXACT off-states asserted (`=== 0`, not `< ε`). RC27 recorded a coupling nobody chose: **if `AO.enabled` is ever flipped on, AO becomes an ULTRA lever by accident**, because `updateAoEnabled()` gates on `tier === "high"` and the ULTRA chip pins the tier to `high`. And RC11's own `overflows` counter caught its capacity constant being undersized by 5× (20k → 100k; the real Dnipro working set is 39,302 buildings + 60,527 tree instances). DECISIONS 2026-08-25c/d/e; `mem:project/wip-2026-08-25-rendering-charter-groupBC` + `…-groupF`; parked alongside: the BEST SPOT tails (T52/T49/T50) by owner order 2026-08-25 |
| T55 | 2026-08-26 | **Dnipro's shipped enriched bakes predate the `roof:height` semantics fix and are now drifted from the code that made them.** `inferBuilding` treated OSM `height` as the EAVE and stacked `roof:height` on top, so every co-tagged building rendered taller by exactly `roof:height` — `height` in OSM is the TOTAL. Fixed 2026-08-26 in `scripts/bake/lib/buildings.mjs` (+5 regression tests) while baking Chernobyl, where it doubled the New Safe Confinement to a 220 m ridge. Blast radius measured over every cached Overpass response: **164 / 128,649 in Dnipro**, 0 / 25,510 in St Albans, 1 / 1,275 in Chernobyl. Chernobyl was re-baked; **Dnipro was not.** | scripts/bake/lib/buildings.mjs:72-96 · test/bake/bake.test.ts | **CLOSED 2026-08-26c** — exactly as predicted: RC13 + RC17 forced a Dnipro re-bake and T55 rode it for free. Both variants re-baked, re-uploaded and curl-verified live on R2 (`dnipro-real-4`, `dnipro-o2w-3`); the 164 co-tagged footprints now render their tagged total. St Albans needed nothing and Chernobyl was already correct. |
| T56 | 2026-08-26 | **The Chernobyl terrain patch serves L12, one level shallower than dnipro/everest, and the ceiling is mago's heuristic rather than the data.** Copernicus GLO-30 coarsens LONGITUDE sampling above 50° N to hold ~30 m on the ground, so `N51_E030` is 2400×3600 px where `N48_E035` and `N27_E086` are 3600×3600. mago 1.14.2 clamps `-max` on pixels-per-degree, not ground metres, so the same ~30 m source lands at L12: asked 13, got 12. Measured ground sampling is 29.2 × 30.9 m (finer than Dnipro's 20.7 × 30.9 m is square); L12's 64-seg posting is ~48 × 54 m, so ~a third of the source's detail is unused. | scripts/bake/cities/chernobyl.json maxDepthNote · src/lib/globe/regions.ts | OPEN — LOW, and possibly WONTFIX: relief across the probe tile is 29 m of Polesian floodplain and measured mesh-vs-source spread is 2.3 m, so the missing level buys very little here. The only route to L13 is resampling the COG to 3600 px of longitude, which the 2026-08-18 ruling forbids in Node (geotiff.js's writer emits geo-tags mago ignores → the mosaic landed at −180/90). A non-Node resampler (gdalwarp) would settle it; worth doing only before baking a region above 50° N that HAS relief. **— re-verified 2026-09-06: PARKED, and superseded by T75.** The pointer `scripts/bake/cities/chernobyl.json` no longer exists (deleted 2026-09-02d, git `878e782`), and T75's 2026-09-02e record deletes the whole Chernobyl region — the `regions.ts` entry, both city configs, the geoid grid and 1,785 R2 objects. This row is moot unless the region is re-baked. |
| T57 | 2026-08-26 | **Pripyat's self-seeded street trees are unmapped, so the zone's most distinctive feature is under-rendered.** The bake plants 166,599 trees from OSM `natural=wood` / `landuse=forest` polygons, which cover the surrounding forest correctly — but the trees standing IN Pripyat's courtyards, streets and rooftops after 40 unmanaged years are not polygons anywhere in OSM, so the city itself renders far barer than it is. | scripts/bake/cities/chernobyl.json vegetationNote · README §Upgrade tiers 4 | OPEN — this is exactly the canopy-height-raster tier (ETH 10 m / Meta-WRI 1 m, both CC-BY) the bake README already specs: sample the raster per candidate cell, keep cells > ~3 m canopy, height = raster value, same `packTreeInstances` contract. Needs a GeoTIFF/COG reader in the bake — which the terrain pipeline now HAS (`geotiff` is a dependency, used by bake-terrain.mjs), so the blocker recorded in the README ("a dependency this pure-Node pipeline deliberately avoids") is stale. Chernobyl is the best possible test case. **— re-verified 2026-09-06: PARKED, and superseded by T75.** Same cause as T56: the pointer `scripts/bake/cities/chernobyl.json` was deleted 2026-09-02d and the region itself was deleted 2026-09-02e. The canopy-raster tier it argues for survives as T58, which is region-independent. |
| T58 | 2026-08-26 | **The DSM's building signature is inert, but its CANOPY signature is not — RC15 should be re-aimed at trees.** M11 measured (`scripts/bake/terrain/measure-dsm-signature.mjs`, read-only, with a negative control on random non-building pixels without which the number cannot be read): footprints score a median **+0.34 m** over local bare earth at Dnipro and **+0.49 m** at Chernobyl, and their whole tail is control-dominated (share above 5 m: 6.4 % vs the control's 6.2 %). The mechanism is that **95.7 % of Dnipro's footprints cover exactly ONE source pixel** — at a 20.5 × 30.9 m posting a median ~200 m² building is a third of a pixel, so the roof contributes ~4 % of its own height. Contiguous wood/park polygons are the opposite case, because they fill hundreds of postings: **+1.01 m median at Dnipro (n 365, p95 10.36) and +3.78 m at Chernobyl (n 77, p75 7.63, p95 14.37)** — four decades of unmanaged Polesian regrowth. Chernobyl is where the bake plants 166,599 trees ON TOP of a surface that already IS the canopy. | `scripts/bake/terrain/measure-dsm-signature.mjs` · audit gap #11 / S18 · DECISIONS 2026-08-26c | OPEN — and it **supersedes RC15's footprint half, which is refuted**: do not build the footprint punch. A canopy punch is a different and smaller intervention (the WBM + a canopy mask, no OSM footprint rasterisation) but is blocked by the SAME 2026-08-18 ruling against writing DEM rasters in Node, whose recorded cause (geotiff.js geo-tags mago ignored → the mosaic landed at −180/90) may be a `GeographicTypeGeoKey` footgun rather than a mago limitation — `geotiffwriter.js:537-541` overwrites a caller-supplied `ModelTiepoint` with the whole-globe default when that key is omitted. **That is a code-reading inference, not a re-test; the ruling stands until someone runs the one-tile Chernobyl experiment.** Compounds with T57. |
| T59 | 2026-08-27 | **OWNER DECISION, and it BLOCKS the whole BEST SPOT ranking effort: should `access.soft.unknown` stop pricing IGNORANCE as BADNESS?** The owner's hand-picked cell is charged `access.soft.unknown = 0.45` (a 0.673x multiplier) because the landcover raster has **no class** for where he stood — measured, not inferred: `{unknown: 1}` ALONE reproduces the whole-ladder-off value exactly (0.2176 → 0.3235), while freeing `majorRoad`/`road` moved the field's best cell and left his **byte-identical**. With the visibility gate forced FULLY OPEN his ceiling is `S_max = A_soft^0.5 · M_eff = 0.673 x 0.512 = **0.345** < 0.378` (the shortlist's entry price) **at a PERFECT preference of 1.0** — so no gate change, no window change and no new framing term can reach the top 8 while this rung stands. **It contradicts a doctrine the engine enforces everywhere else** (`notchDepthDeg = -Infinity` because "ignorance is not depth"; UNKNOWN is "a RENDER CLASS, never a low score", `BESTSPOT_SPEC_V2.md` §3.4; the 2026-08-26g canopy withdrawal). Options: `1.0` (ignorance is free) · `0.9` (matching `green`) · leave `0.45` · make it a RENDER class rather than a multiplier. **It re-scores every unclassified cell in every region, which is why it is the owner's call.** Measured cost on the shipped field: `MEASUREMENTS.md` §4 arm E2 (field best 0.400 → 0.4118, shortlist reshuffles, a marker lands 39 m from his pick). Constant at `src/lib/geo/bestSpotScoring.ts` `access.soft.unknown`. | `src/lib/geo/bestSpotScoring.ts` `access.soft.unknown` · `bestspot/MEASUREMENTS.md` §4 arm E2 · DECISIONS 2026-08-27 | OPEN — owner decision, not urgent (NEXT_SESSION_PROMPT 2026-09-01: "BEST SPOT is SUFFICIENT as implemented (T59 open, not urgent)"); PARKED with BEST SPOT. Columns added 2026-09-06 |
| T60 | 2026-08-27 | **`F_peak` — the missing dual of `notchAt`, now a PRECONDITION with a measured target.** `F_gap` scores sky BETWEEN two masses and gives **exactly 0** for a lone spire at `az*`, so nothing in the metric can score "the body passes behind a monument" — which is the shot the owner took. Measured: his `F` is **0.4059** (his weakest term, 85th pct); taking it to ~1.0 buys `0.30 x 0.594 = 0.178` of preference → **0.810**, landing within **2 %** of rank 4. Design is `bestspot/SWEEP_MODE_MAP.md` **slice 2, which survives its own doc's supersession verbatim**: the kernel, nine leaves, `CLASS_OF` rows, `clampResolved` loop, Lean `peakBound` twin, twenty golden rows committed RED first. **The trap that makes the naive dual silently wrong: `Number.isFinite(width)`** — the notch's `Infinity` collapses to 0, the peak's INVERTS (`slender = prom/inf = 0` → `1 - smoothstep(5,8,0) = 1`), i.e. a ridge scoring FULL credit. Blocked on T59. | `src/lib/geo/bestSpotScoring.ts` §`F_gap`/`notchAt` (:183) · blocked on T59 | OPEN — blocked on T59; PARKED with BEST SPOT. Columns added 2026-09-06 |
| T61 | 2026-08-27 | **`verify-bestspot.mjs`'s D8 cross-model block is red at 96/101 and its fixture is stale — re-derive the pin, do not loosen it.** Confirmed red on CLEAN master by stashing a whole batch. The 2026-08-24 fixture (hero rank-1 `S` 0.065, skyline 40.31 deg) predates RC16's straddler recovery and RC17's pick-height removal, which moved the geometry both models share; measured now `S` 0.3159 / 0.97 deg, so the check's PRECONDITIONS fail. Hill-climb for a cell the CURRENT geometry genuinely walls in, **re-measuring each candidate >= 2x after quiescing** — the objective is noisy (hero `S` 0.3161 → 0.3338 over four runs). **Never re-derive it against `__globe.plan()` alone** (see T62). Also fix the two unguarded `[0]` at ~`:916`/`:945` that turn an empty shortlist into a `TypeError`, aborting at ~check 66 of 101 and silently skipping the last four sites. Known-red banner is now in the script header. | `scripts/verify-bestspot.mjs` D8 block (known-red banner in the script header) · `bestspot/MEASUREMENTS.md` | OPEN — not urgent; PARKED with BEST SPOT. Columns added 2026-09-06 |
| T62 | 2026-08-27 | **The PLANNER is missing the Monument of Glory — `__globe.plan()` and the BEST SPOT disc disagree, and the disc is right.** At the owner's exact cell the planner's 120-bin profile (3 deg/bin) reports the skyline toward the moon as **0.09-0.14 deg** — flat — where a 20 m mass stands **87 m away at 125.7 deg**. His photograph is the arbiter: the moon is in the frame, resting on the monument. Consequence already recorded in T61: the D8 cross-model check must never be re-derived against the planner alone, and `scripts/probe-bestspot-r1.mjs` (built entirely on `plan().binAltDeg`) is **unsound for near-field masses** — keep it for the window arithmetic only. Root cause unknown; suspect bin width or a near-field distance/trust cut in `horizonProfile`. | `scripts/probe-bestspot-r1.mjs` · `__globe.plan()` / `horizonProfile` 120-bin profile | OPEN — root cause unknown; PARKED with BEST SPOT. Columns added 2026-09-06 |
| T63 | 2026-08-27 | **`L` saturates: the owner's cell is percentile 1.0000 with ZERO cells strictly better, and 11,503 cells tie at the display ceiling.** Measured by reading `S ≡ L` directly (whole preference weight on `l`, ladder off, worth 1, gate open). Either the metric already agrees with him far more than anyone assumed — `L` is the ONE term he is not beaten on — or `1 - smoothstep(dipFloor, lCeilDeg=5, alt*)` saturates so early that it stops discriminating and contributes a flat 0.30 to almost a third of the disc. Both readings are actionable and they point opposite ways, so **measure the distribution before touching `curves.lCeilDeg`** (and note that `lCeilDeg: 30` was already measured INERT on the reported case, T49). | `src/lib/geo/bestSpotScoring.ts` `curves.lCeilDeg` (:426) · `bestspot/MEASUREMENTS.md` | OPEN — measure the distribution before touching the knob; PARKED with BEST SPOT. Columns added 2026-09-06 |
| T64 | 2026-08-27 | **Three unenforced-bound defects found by the Lean hypothesis audit are STILL LIVE IN THE TREE.** From `bestspot/SWEEP_MODE_SCHEDULE.md` §4, and the payoff of the formalization was these HYPOTHESES, not the proofs: (a) `graze.conf.*` has a ceiling (`confMax`) and **no floor**, so `graze.conf.terrain: -1` sanitizes and persists verbatim and **inverts the monotonicity** the Lean `preference` theorem assumes; (b) `curves.accessSoftExponent` has **no clamp at all** — `clampLeaf`'s `default: return value` is fail-OPEN; (c) the third row of the audit table. **The one generalizing test worth more than any theorem** is also specified there: a hostile-leaf sweep over `scoringLeafPaths()` asserting every numeric leaf at `-1` and `1e6` lands in a declared range or sits in a set-equal `NO_CLAMP_BY_DESIGN` allowlist. | `bestspot/SWEEP_MODE_SCHEDULE.md` §4 · `src/lib/geo/bestSpotScoring.ts` `graze.conf.*` | OPEN — all three defects still live in the tree; PARKED with BEST SPOT. Columns added 2026-09-06 |
| T65 | 2026-08-27 | **Cascade VRAM + reach are owner A/B, and the numbers are ready.** The ladder adds **+168 MB** (4096² + 2048²) on top of cascade 0's 536 MB and **+3.0/+3.3 ms** (mountain/city, +7-10 %). Two open taste calls: (a) is the FAR cascade's 260 km reach worth its 34 MB, given ULTRA's own haze has washed the field to `hazeMaxK` 0.72 by ~160 km — measured, it only ACTIVATES on near-level looks; (b) should cascade 0 drop to 4096² per RC27 now that it no longer has to stretch to hold the whole view (that would make the whole ladder CHEAPER in VRAM than the pre-batch single 8192² map). Rollback ladder is written on `ULTRA.cascades`. | `src/components/globe/tuning.ts` `ULTRA.cascades` (:1122) | OPEN — owner A/B; queued behind the shadow issue (NEXT_SESSION_PROMPT 2026-09-01). Columns added 2026-09-06 — 2026-09-06i: unblocked by the T96 ruling (the base rig gets no cascades — the ladder stays ULTRA; A/B when slice BASE lands). |
| T66 | 2026-08-27 | **The dusk model's taste knobs, for the owner's timelapse pass.** All shipped and coherent, none owner-judged: `keyExtinctCurve` (how fast the key dies through the last 3°) · `skyLevelCurve` (how dark the far field goes — the term whose ABSENCE was the defect) · `afterglowCurve` (peak 0.55 at −2°) · `airRayleighK` 0.9 (raise toward 2 to flatten the sky's directional contrast back out; that IS the old look) · `airWarmSwing` 0.85 (0 = the old direction-independent tint) · `groundAmbientK` 0.68 vs the legacy `EARTH.dayGradMin` 0.78. Also **two shipped values were RE-ANCHORED and deserve a second opinion**: `ULTRA.hemiCurve` (was 1.15 at 0°, i.e. brighter at sunset than at noon) and `hemiTintK` 0.6 → 0.22. Judge as a TIMELAPSE, per the standing ULTRA rule; `scripts/probe-dusk.mjs` shoots the toward/away pairs. | `src/components/globe/tuning.ts` `ULTRA.keyExtinctCurve` (:1182) · `skyLevelCurve` (:1205) · `afterglowCurve` (:1219) | OPEN — owner taste; queued behind the shadow issue. Columns added 2026-09-06 **— RULED 2026-09-06i: flatten the rises through 0° (exposure/haze/afterglow); gate = the ladder luma monotone non-increasing. Ships with slice BASE.** **— LANDED 2026-09-06k: exposure plateau 0°→−2°, haze flat on [0,+0.5°], afterglow re-anchored + the dome band × `uFtwSkyLevel`; the ladder gate reads 0.00 codes worst rise on BOTH rigs (terrain-less earth, §15.2). CLOSE once re-read with terrain.** **CLOSED 2026-09-06k2 as ruled: the 0° bump is gone (ULTRA 78.0 → 68.6; base 0.00 codes with terrain). The remaining rise on the ULTRA rig is the release band, T100.** |
| T67 | 2026-08-27 | **`GOLDEN.keyBrighten` now stacks with the new extinction curve and peaks the key at ×1.29 around 9° elevation.** Measured in the shipped sweep (`keyLevel` 1.000 → **1.294** → 0.902 → 0.002). It is defensible — golden hour IS a warm swell, and the owner's complaint was explicitly "below 3-4 degrees", which is now 0.90 and falling — but two warm/bright terms authored years apart now multiply, and nobody has judged the product. Cheap check: whether `keyExtinctCurve`'s 6° anchor (0.85) should absorb the swell instead. | `src/components/globe/tuning.ts` `GOLDEN.keyBrighten` (:336) | OPEN — owner taste; queued behind the shadow issue. Columns added 2026-09-06 |
| T68 | 2026-08-27 | **The "sun set behind a MOUNTAIN" half of the owner's afterglow ask is NOT shipped, and the reason is honest.** He asked for the afterglow "immediately when sun is set behind horizon (or far away other object like mountains and very far buildings)". `ULTRA.afterglowCurve` is evaluated at the GEOMETRIC solar elevation, so a sun hidden behind a ridge at +2° geometric produces no afterglow at all. A terrain-horizon sampler DOES exist and is O(1) — `lib/geo/horizonProfile` builds a 120-bin (3°) per-azimuth skyline marched 60 m to 30 km with curvature minus refraction, and `sampleProfile` is an interpolated lookup. **The catch that stops it being a one-liner:** `planFeed` builds a profile only for a photo apex or an FPV eye ("a focus anchor has no eye"), time-slices it at `PLAN.terrainBinsPerFrame` 3 / `meshesPerFrame` 2 (40+ frames to ready), rebuilds on 25 m of drift, and is coverage-gated at `PLAN.minCoverageForGaps` 0.5. **Nothing cheap exists for a free orbit.** The shippable subset: when `planFeed.profileSample()` is non-null and coverage is met, evaluate the afterglow at `sin(sunAltDeg − sampleProfile(sunAzDeg))` instead of `sinSunFocus`; otherwise fall back exactly as today. One lookup per frame, no shader change. | `src/components/globe/tuning.ts` `ULTRA.afterglowCurve` (:1219) | OPEN — unbuilt; queued behind the shadow issue. Columns added 2026-09-06 |
| T69 | 2026-08-27 | **The aerial perspective erases directional contrast at distance, and no amount of surface fixing will reach it.** `ftwAerial` is a function of camera distance and the VIEW-RAY/sun angle only — it never reads the surface normal, so a building's sunlit wall and its shadowed wall (same world position to within metres) get the SAME air-light, and `mix(col, inScatter, f)` REPLACES fraction `f` of the surface with it. Measured front:back in red on one building's two walls, camera facing the sun: 3° / 2 km / dark drape **1.13**; 3° / 10 km **1.043**; 0° / 30 km / satellite **1.0025** — beyond ~5 km a wall in full sun and a wall in total shadow are the same colour to within one 8-bit code value. `f` reaches the `hazeMaxK` 0.72 cap at ~130 km, where only 28 % of the surface's own colour survives. The 2026-08-27c pass fixed the SURFACE (the emissive floor, the ambient azimuth) and deliberately did not touch this, because the mix form is physically right and only looked wrong while `col` was a self-lit floor. **If the far field still flattens, the lever is `hazeMaxK` as a CURVE rather than a scalar** (falling toward ~0.35 near the horizon: a low-sun far field should read as silhouette against the glow, not as glow), authored as a uniform so the ULTRA-off value stays the same constant and the comparator is exact. | `ftwAerial` (shared aerial term) in `src/components/globe/scene/buildingMaterial.ts` | OPEN — queued behind the shadow issue. Columns added 2026-09-06 |
| T70 | 2026-08-27 | **`atmosphere.ts` mixes two coordinate frames in the dome's Mie lobe, and the comment above it claims otherwise.** `Ds` is ellipsoid-SCALED (`normalize(vec3(D.xy, D.z * a/b))`) while `uSunDir` is raw ECEF, so `dot(Ds, normalize(uSunDir))` is not the angle the comment says it is. Bounded by `a/b` = 1.003364, i.e. **up to ~0.19°** — invisible, which is why this is a cleanliness row and not a bug row. Either scale the sun direction the same way before the dot, or amend the comment to state that only the view ray is scaled and the mismatch is accepted. Found by the 2026-08-27c audit; the same audit's line-number citations were shifting under a live worktree, so re-verify the anchor before editing. | `src/components/globe/scene/atmosphere.ts` — the dome's Mie lobe | OPEN — a one-line fix or a comment amendment; re-verify the anchor first (the 2026-08-27c line numbers shifted under a live worktree). Columns added 2026-09-06 |
| T71 | 2026-08-27 | **The BEST SPOT panel's building warning names a control that does not exist on its own shell.** `NO_BUILT_GEOMETRY_LINE` reads `⚠ NO BUILDING GEOMETRY REACHED THIS DISC — TURN ▦ 3D DETAIL ON, OR WAIT FOR TILES`, but `▦ 3D DETAIL` is the **/m LAYERS** chip; BEST SPOT mounts only from `index.astro` and is gated desktop-only by the orchestrator, where the same layer is the camera deck's **BLD**. A user who reads the line has nothing labelled that way on screen. One-line copy fix; the guide (`spot-limits`) documents the mismatch verbatim until it lands, so fixing the string means editing that tip too. Found while writing the BEST SPOT guide chapter, 2026-08-27d — a docs read, not a browser run | `src/components/panels/BestSpotPanel.tsx:179-180` · `src/lib/guide/guideContent.ts` `spot-limits.tip` | OPEN — cosmetic, one string |
| T72 | 2026-08-27 | **The guide's four new subjects ship with NO screenshots.** The BEST SPOT chapter (8 topics), the two eclipse topics, `move-ultra` and `trust-detail` are text-only: `guideContent.test.ts` pins `allMedia().length === 13` and no shot exists for the heatmap, the shortlist rows, the eclipse list or an eclipsed sky. A shoot needs `scripts/shoot-guide.mjs` + `wix dev` + a solved disc and a scene time parked on a real totality, then the webp `w`/`h` declared, the count in the test raised, and every path added to `scripts/warm-prod-assets.mjs`. Rides T42's re-shoot session rather than earning its own | `public/guide/**` · `scripts/shoot-guide.mjs` · `scripts/warm-prod-assets.mjs` · `test/lib/guide/guideContent.test.ts:185` | OPEN — queued behind T42 |
| T73 | 2026-09-01 | **DBG debug-HUD tails (feature SHIPPED and 17/17 browser-verified — these are the deliberate leftovers).** (1) The imagery-z row is min/max only — a full per-level histogram costs one more loop over the same `overlayInfo → tileInfo → calculateLevel` reach and would show the level DISTRIBUTION the QA-7 lever family actually moves. (2) `geoLabels` is the one label layer with no census accessor (streetNames and vectorFeatures now have theirs). (3) The harness (`scripts/verify-debughud.mjs`) has no `/m`-negative leg — the fence proof (no `.ct-dbg`, no `.dbg-panel` on the mobile shell with a desktop-set `debugHud:true` blob) is code-read, not browser-asserted. (4) Owner taste pass queued: metric density, group order, default window size, sparkline height. | `DEBUG_HUD_PLAN.md` §AS BUILT · `lib/globe/debug{Feed,Catalog,GpuTimer}.ts` · `scripts/verify-debughud.mjs` · `mem:project/wip-2026-09-01-dbg-hud` | OPEN |
| T74 | 2026-09-01 | **MESH SUITE — the owner-ordered 3-direction track (D1 spatial gizmos · D2 world-synced overrides · D3 user-uploaded models), PLANNED, implementation not started.** Slice ladder MS0–MS6; D2 is an ACTIVATION (endpoint + LWW + dirty-tracking written and tested, zero callers, provisioning never run); D1 rides a pristine-snapshot + absolute-recompose extension of `applyFeatureSeats`; D3 = Wix Media bytes + `UserModels` record + R2 reserve, GLB canonical with client-side normalize + auto-decimate. ★ MS0 blockers: owner governance/caps rulings + 4 Wix MCP platform unknowns (public MODEL3D urlExpirationDate · wixstatic CORS/Range for GLB · per-file cap · ingest readiness). Two doc drifts to fix at MS0: contracts.md §2 key grammar (`featureId`, not `osmId`) and §7 "LWW by `updatedAt`" (it is structural `_id` upsert). | `MESH_SUITE_PLAN.md` · `mem:project/wip-2026-09-01-mesh-suite-plan` · `src/lib/globe/bldgOverrides.ts` · `src/pages/api/building-overrides.ts` | PLANNED 2026-09-01 → **MS0 DONE + MS1 BUILT 2026-09-02** (probe + substrate; §6) → **MS2 BUILT 2026-09-02d** (the gizmo UI: TransformControls on the ghost rig, context menu + op strip + G/R/S/E, per-op revert, `verify-meshedit.mjs` 14 legs; §7) → **MS3 BUILT 2026-09-02f** (D2 ACTIVATED: `BuildingOverrides` provisioned live with the v2 spatial fields, OSM-keyed `_id` (dual key), paged public GET + member POST, the merge policy in `lib/globe/bldgSync.ts` — local pending wins · shared wins over my synced copy · a reset of a shared edit is a tombstone — the OSM recovery sweep + `reapplyOverrides()` in the engine, sign-in-gated SYNC in the chip foot / menu / a standalone pill, the `_ftw_override` byte ladder + origin badge + hover note; `verify-meshedit.mjs` legs 15–18 against the LIVE collection; §8). Next: MS4 (the D3 upload pipeline). → **MS4 BUILT 2026-09-02h** (the D3 upload pipeline: dropzone fork, `store/modelUpload`, `lib/models/*` load→audit→decimate→pack→thumbnail, `/api/upload-url` kind:"model" = the first mime allowlist, `UserModels` PROVISIONED (24 fields) + `/api/models` descriptor-verified GET/POST/DELETE, our own thumbnail (the platform's is a permanent 403), `verify-modelupload.mjs` 8/8; §9). → **MS5 BUILT 2026-09-02i** (D3 placement: the public cover read `/api/world-models` on a new `gh5` column + the owner `PATCH /api/models`; `store/userModels` + `store/modelEdit`; `scene/userModels.ts` — ENU frame → anchor → body → ground-fitted GLB, closest-first residency under a triangle budget, eased terrain seat, own pick, shadows; the MS2 gizmo on the model's own rig (uniform scale, no lift, a move folds into a new placement); the FPV session + `ModelEditChip`; PLACE ON GLOBE click-to-place; the MDL chip ON by default carrying the physical-density warning; DBG `models.*`; guide `fpv-models`; `verify-usermodels.mjs` 10 legs; §10). **Owner batch MS5b ordered 2026-09-02j (§11, BEFORE MS6):** metres on the SCALE rows (buildings `w × d m`, models `w × d × h m`, current vs original) · per-edit RELATIVE rails (move 100 m per edit, scale 0.1×–10× per edit about the committed value, no absolute caps; loose sanity rail on read/sync; the tile-volume culling caveat to decide) · the context menu closing on right-button RELEASE (a right press claims `fpvDragId`, the tap-release disarms — no `e.button` guard; fix + real right-button legs in both harnesses) · orbit drag slow / barely moving AFTER an FPV edit (the detached gizmo's invisible 100 km drag plane stays in the scene at the edited building; GlobeControls' first-hit pivot raycast lands on it — remove the helper from the scene while nothing is attached, both instances; pin with an orbit-drag leg). → **MS5b BUILT 2026-09-02l** (§11.5: metres on both SCALE rows via `footprintM` / `sizeM3` + one `formatDims`; per-edit rails `EDIT_MOVE_MAX_M` 100 / `EDIT_MIN_K..MAX_K` 0.1–10 about the committed transform, compounding, under a loose sanity rail 5 km / 0.001–1000× / lift 25; the right-button guard + `menuConsumesPress`; the gizmo helper in the scene only while attached, `inScene` + `helperRoot()` seams; real right-click + orbit-drag harness legs, world counters RELATIVE to boot; receipt in DECISIONS 2026-09-02l). Then MS6. **MS6 BUILT 2026-09-02m** (plan §13: the MODELS tab on MY PINS — rename / hide / delete / stand beside · the placement PATCH open to every member (LWW, `editorMemberId`) · orbit hover/click · DEV-seed two-member harness leg). The MESH SUITE ladder MS0–MS6 is COMPLETE; open with the owner: the lift seat, haze chaining default, the sanity/rail numbers (§11.5 + §13.4). **2026-09-03 MS7 BUILT (owner order after testing MS6): the LIFT seat — `UserModels.tU` (27 fields, live), a vertical drag on the model gizmo's Y arrow railed onto `[liftFloorM(height × scale), 50 m]` (a quarter of the scaled model — never under 0.5 m — stays above the seat, so a sunk model is always pickable), MOVE's ↺ lands it, RESET ALL zeroes it; MY PINS · MODELS rows gained GOTO (stand beside) + RESET (RESET ALL's twin through ONE PATCH); `verify-usermodels` 20 legs. Plan §14.** → **MS7 BUILT 2026-09-03** (the lift seat + GOTO / RESET; plan §14) → **MS8 BUILT 2026-09-05** (pitch / roll; plan §15) → **TRACK CLOSED by owner ruling 2026-09-05** ("deployed and tested manually; this concludes all mesh-related work at the moment") — the open taste calls of §11.5 / §13.4 / §14.4 / §15.4 stay parked with it **— re-verified 2026-09-06: CLOSED** (DECISIONS 2026-09-06f: "the MESH SUITE (T74, T78) is CLOSED"). |
| T75 | 2026-09-02 | **`verify-chernobyl.mjs` is RED on master — a PRE-EXISTING terrain-height regression at the ChNPP pose, NOT MS1.** From `#p=51.3893,30.0988,1200,20,55`, `__globe.terrainHeightAt` answers **−557.2 / −562.8 / −560.3 / −555.4 / −603 m** (identical to the digit on every run) where the Polesian truth is 105–175 m — while the o2w cells (14) AND the GLO-30 patch tiles (10, L10–L12) both stream 200. The numbers are a constant across 3.5 km, i.e. one coarse flat CWT ancestor is the nearest hit, and it never refines within the harness window (nor in 15 s of a separate probe). Comparator 2026-09-02: the harness fails IDENTICALLY with the MS1 diff stashed (bare master `2d38658`), so the regression predates MS1; last recorded PASS 2026-08-27. Suspects, by what touched the ground path since: PR #88 (`imageryGround.ts` +244, `terrainSkirt.ts` surface-cap clip) and PR #90 (`imageryGround.ts`/`terrainPatch.ts` DBG counters). Buildings there seat at `clampGroundM(−557) = 0` → a user-visible sink/float at Chernobyl. Repro: fresh headless Chrome :9333 + `node scripts/verify-chernobyl.mjs 9333` (17 s). | `scripts/verify-chernobyl.mjs` · DECISIONS 2026-09-02b · `mem:project/wip-2026-09-02-mesh-suite-ms0-ms1` | **CLOSED — DROPPED by owner ruling 2026-09-02c**: all verification and support for the Chernobyl slice is dropped ("a curious test, nothing special"); no bisect. Mechanics DONE 2026-09-02d: `scripts/verify-chernobyl.mjs` deleted, the Chernobyl leg dropped from `verify-bake-ladder.mjs`, the bake README re-pointed at the ladder, rosters cleaned (MESH_SUITE_PLAN §4a-4, NSP, DECISIONS). **2026-09-02e — owner confirmed; region DELETED:** `regions.ts` entry, `cities/chernobyl{,-o2w}.json`, the geoid grid, the guide copy, and 1,785 R2 objects (146 + 150 + 1,489; 0 remain) via the new `scripts/bake/delete-r2.mjs`. Local gitignored bake dirs (~136 MB) left for the owner's own `rm -rf` (the tool gate blocked it). FULLY CLOSED. **The Dnipro slice is the highest priority in any feature.** **— re-verified 2026-09-06:** the pointer `scripts/verify-chernobyl.mjs` is gone from the tree (deleted 2026-09-02d, git `878e782`) — expected, and kept as provenance. |
| T77 | 2026-09-02 | **Post-MS6 ARCHITECTURE + PERFORMANCE audit and revamp (owner order 2026-09-02k): rendering + scene management, with the user-model scale question as a lead-in.** The scale analysis and the lever list live in `MESH_SUITE_PLAN.md` §12 (texture VRAM is the first cliff, then draw calls × the shadow pass, then arrival hitches; the 200-row world page is a functional cliff; instancing / a GLB cache / KTX2 / LOD + impostors / screen-space priority / tier-aware VRAM budgets are the levers). The audit replaces the §12 estimates with DBG readings (`models.*`, `frame.calls`, `frame.tris`, `mem.*`) before deciding. | `MESH_SUITE_PLAN.md` §12 · `mem:project/wip-2026-09-02-mesh-suite-ms5` | **NEXT** — MS5b + MS6 landed 2026-09-02m; LEAD-IN DONE 2026-09-02n: `rendering/ENGINE_STATE_2026-09-02.md` (verdict, gap analysis Rq-1..17, non-regression contract, audit order §11) + `rendering/WEB_RESEARCH_PROMPT_2026-09-02.md` (owner runs it on web Claude → `rendering/WEB_RESEARCH_REPORT_<date>.md`); audit step 1 = MEASURE (DBG at Dnipro/ULTRA/Everest//m × 0/6/24 models, then the iPhone 17 Pro); **2026-09-05: the owner ran the prompt — `rendering/WEB_RESEARCH_PERFORMANCE_RESULT_2026_09_05.md` (24 ranked levers, Rq-1..17, a do-not-do list, 8 experiments) — and the reconciled execution plan is `rendering/T77_AUDIT_PLAN_2026-09-05.md` (the merged lever ledger, the 8 seams resolved, the MEASURE protocol, the slice ladder A shadows → B seats → C streaming → D mobile → E later). NEXT SESSION = step 1 MEASURE (READ-ONLY on `src/`; `scripts/verify-perf-baseline.mjs` on a HEADED Chrome; the phone by the owner's hands) → `rendering/MEASUREMENTS_<date>.md`; **owner 2026-09-05c:** the desktop MEASURE session as planned + the device-free proxies (`renderer.info.memory` bytes per pose; desktop Safari for WebKit correctness); the 17 Pro is borrowed later for a short window (`rendering/IPHONE_BASELINE_CHECKLIST_2026-09-05.md`, ~35 min); a **cloud device farm is APPROVED (paying OK)** for the real-phone stages before then **2026-09-06 (dated edit): step 1 MEASURE DONE** — `rendering/MEASUREMENTS_2026-09-05.md` (81 + 12 matrix cells, the shimmer + reseat baselines, three CPU profiles; two DEV read seams `window.__debugFeed` + `__globe.seatSettle()`; harnesses `verify-perf-baseline.mjs`, `verify-temporal-stability.mjs`, `probe-cpu-profile.mjs`, `t77-model-ramp.mjs`). Its §12 SUPERSEDES the plan's slice order: NEW slice 0 (the orbit frame — T79 + T80) before A (shadows, gate = the shimmer baseline) before B (seats — T81). The phone (1b) part A done, part B UNKNOWN. **2026-09-06: step 1b DONE on both phones (`MEASUREMENTS` §11), slice 0 / T79 BUILT (`T77_SLICE0_ORBIT_FRAME_2026-09-06.md`); next = T80 bloom (owner call on pixels at `high`) → slice A shadows.** **— re-verified 2026-09-06: PARKED 2026-09-06f** by owner order, for one docs-and-memory hygiene session (DECISIONS 2026-09-06f; NEXT_SESSION_PROMPT §"THE PARK"). State at the park: step 1 MEASURE done · step 1b done on both phones (the iOS ramp and soak did not classify — T83) · slice 0 / T79 CLOSED · T80 not started · slices A–E not started. Resumes at T80, which needs the owner's pixels-at-`high` ruling first, then slice A. **— RESUMED 2026-09-06h: T80 measured (moot), slice A A0–A3 shipped ULTRA-on/base-identity (demand-driven cascade 0, the shimmer reframed: the light FRAME rotates, not the box — MEASUREMENTS §14.1), slice B NEW-3 + 5 + 6 + 4a–4e + 5c shipped (stall gone, collapses 4,277 → 0 — §14.2), the sunset release fixed ULTRA-only (§14.3, `rendering/SUNSET_LIGHTPATH_2026-09-06.md`), the owner's pose catalogue + `verify-visual-sweep` built. Next: slice C (streaming quiet, the drain, `frame.cpu` 4.4 at the FPV eye), T80 direction g, the base-rig ruling (T96).** **— 2026-09-06j/k/k2: the six slices integrated + gated (DECISIONS k2). — 2026-09-06l: E1 + E2 run → levers 2 and A1 CLOSED (with E3, all A-rest calls decided); T80-h fused bloom shipped (fpv frame time 19.2 → 14.2 ms from the pre-T80g chain); T99 closed; the GPU-timer finding (T104). Next: the owner calls (T100 · T101 · T92 · T93 · the T80 pass-count ruling), the streaming measurement (levers 9–11), the phones (T83).**  **— 2026-09-07j: LEVER 11 BUILT — the vector-tile parse off the main thread** (`lib/geo/vtileWire.ts` flat typed-array wire + `vtileParseWorker.ts` + `vtileParseClient.ts`; a structured clone of the nested parse costs as much as the parse — 60.6 vs 60.8 ms over 25 real Dnipro tiles — the wire's main-thread seat is 3.1): desktop descent A/B, vector tiles in the hitch frames 169 → 0.8 ms, app in hitches 230 → 140; sweep draw-count 13/13, `vector.*` rows identical per pose (MEASUREMENTS §26.1–26.2). **T115 FIXED** (§26.3). **The Pixel reads of both are OWED** (the phone was not attached). Next: the tails T93 / T102 / T103 / T114-ESRI, the owner calls in one batch. **2026-09-08b owner rulings: lever 4 `compileAsync` CLOSED, not built (0.7 % / 1.4 % of the hitch frames); lever 8 the terrain BVH APPROVED — build next; the lean load budgets 3 / 1.5, the flip bank (112 MB), `treeLocateBudgetMs` 0.5, `skylineBehindAlpha` 0.35 all KEPT; T113 stays parked.** **2026-09-08: LEVER 11 READ ON THE PIXEL — holds** (`probe-cpu-profile --leg descent --device`: no vector-tile parse symbol in any table of the profile, app in the hitches 691 → 649 with vector tiles 199 → 0; `probe-vtile-worker --device` 12/12 worker, 0 inline, seat max 0.7 ms, worker max 41.4 ms). What the phone leg shows next: `applyFeatureSeats` 427 ms of the 8 s leg (the biggest app item), the controls raycast 1.17 s (`rawHeightAt` 315 + `stepTiltGlide` 138 in the hitches — the terrain BVH, lever 8), the ephemeris in the hitch frames 86 ms (new, small). MEASUREMENTS §27.1–27.2. **2026-09-08c: LEVER 8 BUILT — the terrain BVH** (`lib/globe/terrainBvh.ts`, in-house because the lockfile pins the Wix registry unreachable off-VPN; a flat median-split tree, built lazily per terrain tile on first raycast, dropped with the tile, unit-pinned BYTE-IDENTICAL to three’s `Mesh.raycast` on six tile shapes; `GROUND.terrainBvh` kill switch; DBG `terrain.bvh.*`). Desktop descent A/B (stash idiom): whole-leg controls **800 → 246 ms**, `intersectTriangle` **198 → 30 ms**, hitch-frame controls **117 → 65 ms**; render byte-identical (sweep draw-count 14/14, the everest diff confined to UI chrome). THE PIXEL READ IS OWED (`probe-cpu-profile --leg descent --device`). |
| T76 | 2026-09-02 | **`verify-pin-reframe.mjs` is RED on master — PRE-EXISTING, environment-shaped.** From the 987 km oblique start over Dnipro the harness reads `terrain heightAt(pin) = −2047` at selection AND at the end (`loaded=false`: the pin's tiles never refine inside the harness window on a fresh headless profile), so the stale and live plane centres coincide at NDC (0,0) and the assertion "the correction fired / a shift was avoided" cannot be exercised — it fails with "lands centred (no correction needed)". Comparator 2026-09-02h: identical on bare master `376da48` (stash) and on the MS4 tree, so not MS4's (MS4 touches no scene module). Same shape as the closed T75 (a coarse ancestor as the nearest terrain hit) — but this is DNIPRO, the owner's priority. Repro: fresh headless Chrome :9333 + `node scripts/verify-pin-reframe.mjs 9333` (15 s). Candidates: a warm-profile run (the `verify-ultra` cold-cache confound, DECISIONS 2026-09-02d) before any bisect; the harness could wait for `loaded=true` as a precondition. | `scripts/verify-pin-reframe.mjs` · `mem:bugs/pin-arrival-reframe` · DECISIONS 2026-09-02h | OPEN — not urgent; classify warm vs cold first **— re-verified 2026-09-06: OPEN.** Still red and still pre-existing: T79's 2026-09-06e harness list reads "pin-reframe RED = T76 pre-existing". |
| T78 | 2026-09-03 | **MESH SUITE MS8 (owner order 2026-09-03, after MS7): add VERTICAL rotation (pitch / roll) to user models — today ROTATE is YAW-ONLY.** The record stores a single `rotDeg` (yaw about local +Y; `scene/userModels.ts writeBody` writes a pure-yaw quaternion, the ROTATE gizmo shows only the Y ring, `rigToTransform` reads yaw as `2·atan2(qy,qw)`, `clampModelEdit` collapses to yaw). Wanted: rotate about the horizontal axes too. **Same principles as MS7 (the lift):** saved / synced / processed correctly (a new record shape — pitch+roll columns or a stored quaternion — provisioned in `scripts/provision-collections.mjs` FIRST); no regression; RESET ALL / MOVE-↺ zero it; the chip readout + the pinned label carry it; LWW open to every member (C6 unchanged). **The binding constraint:** the gizmo rig + `FeatureTransform` (`rigToTransform`/`transformToRig`) are SHARED with BUILDINGS, which stay yaw-only (the §4a no-regression contract) — keep the 3-axis rotation MODEL-SPECIFIC the way MS7 kept the building lift rail byte-identical (a model-only `clamp`/read-back path, the building ROTATE untouched). Grow `verify-usermodels` with pitch/roll legs (mind the headless vertical-gizmo traps recorded in `mem:project/wip-2026-09-03-model-lift-goto-reset`). | plan §15 (as-built) · `mem:project/wip-2026-09-05-model-pitch-roll-ms8` · DECISIONS 2026-09-05 | **BUILT 2026-09-05** — `pitchDeg` / `rollDeg` provisioned LIVE (29 fields); the YXZ triple with a canonical read-back; the gizmo's `tilt` instance (X + Z rings, full-quaternion read); a SLERP ease; the lift floor TILT-AWARE (a flip is held up a quarter); ROTATE owns the tilt (↺ / RESET ALL / list RESET stand it upright); vitest 2,444 · astro 0/0/8 · knip 0 · `verify-usermodels` PASS 21 legs · `verify-meshedit` PASS with the building's yaw-only ring pin · `verify-bldg-override` PASS. Open taste calls in plan §15.4 (none blocking); owner 2026-09-05: deployed + tested manually, mesh track closed **— re-verified 2026-09-06: CLOSED** (DECISIONS 2026-09-06f: "the MESH SUITE (T74, T78) is CLOSED"); the §15.4 taste calls are parked with the track. |

| T79 | 2026-09-06 | **Every orbit (`#p=`) pose is CPU-bound in the CONTROLS' per-frame full-scene raycast — 31–47 ms of main thread per frame at a STATIC pose, at every tier (the `/m` chart included: 36–39 ms with 9 draw calls).** `GlobeControls.update()` → `adjustCamera` → `_getPointBelowCamera` → `raycaster.intersectObject(scene)` (3d-tiles-renderer 0.4.28 `EnvironmentControls.js:995/1059/1461/1739`), twice per frame while `adjustHeight` is on; the controls were given the WHOLE scene (`StylizedTiles.ts:1086`), every fill mesh keeps three's brute-force `Mesh.raycast`, the enriched fills are non-indexed soups (`getVertexPosition` 32 % of the frame). V8 profile: 84 % of the orbit frame, 73 % at Everest; FPV (adjustHeight off) reads 2 ms. Levers: a terrain-only raycast target (`controls.setScene`), a `raycaster.layers` mask (the pick raycaster keeps its own), `three-mesh-bvh` on the terrain, or the memoised `heightAt` (D4). Expected gain: the whole 30–47 ms. Owner call: the terrain-only target changes `cameraRadius`'s rooftop clearance (it clears buildings today). | `rendering/MEASUREMENTS_2026-09-05.md` §0/§7 · `verify-shots/perf/cpu-orbit-*.cpuprofile` | OPEN — the first fix slice (slice 0); READ-ONLY measured, nothing built **BUILT 2026-09-06 (T77 slice 0, `rendering/T77_SLICE0_ORBIT_FRAME_2026-09-06.md`): the ATTRIBUTION above was wrong — `scripts/probe-below-camera.mjs` puts 15.9 of the 21 ms per call in the BASE EARTH (`baseEarth.ts:183` `SphereGeometry(1, 384, 384)`, 294 k tris, a live default `raycast`), 3.1 ms in the enriched cells, 2.1 in OSM tiles, 0.02 in terrain. Fix = the below-camera GATE (`lib/globe/belowCameraGate.ts` + `scene/pluxGlobeControls.ts`, exact by construction — 400-scene property test; no owner call needed, rooftop clearance untouched). Receipt: orbit dt 44.5 → 18.1 ms, cpu 43.3 → 1.2; Everest 32.2 → 17.9; `/m` 35.8 → 12.1; city 49 → 37 (GPU-bound); FPV unchanged; 32 cells / 0 failures. Phones (pre-fix): iPhone 17 Pro 74–105 ms, Pixel 6 Pro 78–109 ms cpu at every orbit pose (`MEASUREMENTS` §11). **CLOSED 2026-09-06e** — the §8 harness list green against the gated build: charter 85/85 (sweep pinned to midday) · ultra 28/28 · meshedit PASS · usermodels 21 · cab 65/65 · pin-reframe RED = T76 pre-existing. **— re-verified 2026-09-06: CLOSED** (DECISIONS 2026-09-06e). |
| T80 | 2026-09-06 | **Bloom is the largest GPU consumer: `UnrealBloomPass` off = −13 ms GPU at the Dnipro FPV eye (25.1 → 11.8), −21 at orbit, −22 at Everest, −14 at the city, −25 under ULTRA — 23–63 % of the GPU frame at DPR 2.** The desktop FPV frame IS the GPU (dt ≈ gpu, cpu 2 ms), so this is the desktop FPV frame-rate lever; the plan ranked "half-res, fewer-mip bloom" 16th. Owner call: a half-res bloom changes pixels at `high` (the byte-identical law) — ULTRA-first or an explicit ruling. | `MEASUREMENTS_2026-09-05.md` §4 | OPEN — slice 0, second lever **— re-verified 2026-09-06: OPEN and BLOCKED on the owner's ruling** about changing pixels at `high` (DECISIONS 2026-09-06f). T77 resumes on this row, before slice A. **— MEASURED 2026-09-06h: the half-res mip chain (`bloomScale` 0.5, bright target 800×475) recovers 1.6 of 13 ms at the FPV eye and 1.5 of 13.8 at orbit (MEASUREMENTS §14.4); the cost is the full-res additive blend into the 4× MSAA read buffer + the forced resolve. The pixels-at-`high` ruling is MOOT for this lever; new direction = the composer buffer contract (blend after the resolve / non-MSAA bloom source). `ScaledBloomPass` + `bloomScaleForTier` + `__quality.bloomScale()` + the `bloomCheap` cell shipped as the instrument (identity at `high`, unit-fenced).** **— direction g BUILT 2026-09-06k (`scene/resolvedComposer.ts`; rt1 demoted, −195 MB VRAM; `bloomMsaa` A/B cell; `probe-bloom-path.mjs`). The timed gate (fpv GPU ≤ 15 ms) and the pixel probe are UNRUN (T98). If short of 15: fuse the copy into the blend, then the blend into OutputPass (T80-h).** **Measured 2026-09-06k2 (headed, one boot): on 19.8 / `bloomMsaa` 22.5 / off 9.8 / cheap 18.4 — direction g = −2.7 ms; gate ≤ 15 NOT met; the blur chain is ~8 of the remaining 10 ms → T80-h = the BLUR (half-res mips + fused blend).** **— T80-h BUILT + SHIPPED 2026-09-06l (`BLOOM.path` "fused": `ScaledBloomPass.deferBlend` + `scene/fusedOutput.ts`; the premise was wrong — no blur level ever ran at full res, `UnrealBloomPass.js:102-103`; the fused path removes the two full-res draws, copy + blend, and never binds `resolvedTarget`). Timed (MEASUREMENTS §16.3): fpv `frame.gpu` msaa 22.4 → resolved 19.8 → fused 18.3 (off 10.0); **frame time dt p50 19.2 → 15.4 → 14.2 ms (off 12.1)**, ULTRA 21.0 → 18.0 → 15.9. Pixels: byte-identical at west-sunset, 0.13 % at Δ ≤ 2 / none > 3 at the cityscape (frozen, noise 0). The ≤ 15 gate is MET on frame time and unmet on `frame.gpu`, which the `bloomMips` cell shows over-counting pass-heavy chains by ~6 ms (T104). Bloom now costs 2.1 ms of frame time; what is left is pass COUNT (owner ruling — changes pixels). Recommend CLOSE as a frame-time lever.** **OWNER RULED 2026-09-06m: option (a) — CLOSED as a frame-time lever** (fused bloom = 2.1 ms of frame time; the pass-count lever is not taken). The row closes formally when the T104 gate re-statement lands (NEXT_SESSION §OWNER RULINGS 1). **CLOSED 2026-09-06n (owner ruling 2026-09-06m, option a): bloom = 2.1 ms of FRAME TIME on the fused path (T80-h; `bloomFused` 14.2 vs `bloomOff` 12.1 ms dt p50, ULTRA 15.9 vs 14.4); the pass-count lever changes pixels and is not taken; the ≤ 15 ms gate is MET on dt p50 (T104). MEASUREMENTS §17.1.** |
| T81 | 2026-09-06 | **Seats never settle, and the ease STALLS 8.3 cm short by construction.** `applyFeatureSeats` writes only when `|next − applied| ≥ 0.01` while `seatStep` eases 12 % per frame (`ENRICHED.reseatEaseK`), so a residual under 0.01 / 0.12 = 0.0833 m is never written again — every leg of `verify-temporal-stability --reseat` ends with the near residual at exactly 0.083 m. Around it: 130 terrain-epoch bumps (1.9/s) during an orbit arrival and 166 (4.8/s) at the FPV boot, each dumping the memo city-wide; at the FPV eye the round-robin writes 41 features a frame in 98 % of frames for 70 s with 1,210 plausibility rejections and a city-wide residual p95 of 30 m at the end — the poisoned-pair loop is a steady state. User models seat in 6 frames (not the problem). | `MEASUREMENTS_2026-09-05.md` §9 · `verify-shots/perf/temporal-warm-*.json` | OPEN — slice B's measured defects (with plan levers 5, 6, 7) — 2026-09-06h: the 8.3 cm stall is GONE (`seatLand`, arrival end 0.000 m); the FPV-eye city-wide gate is NOT met while tiles stream (§14.2) → carried into slice C. |
| T82 | 2026-09-06 | **Harness environment: an in-page rAF-driven promise stalled > 90 s in 3 of 27 boots (the first city boot; two orbit boots with 6 models) while the page answered later evaluates in 50 ms; not reproduced by a 2.4-min heartbeat at the same pose; `Inspector.targetCrashed` never fired.** `verify-perf-baseline.mjs` now isolates a boot failure (replaces the target, continues), writes artefacts per cell, and its sampler has a watchdog that records `rafStalled` instead of hanging. Cause UNVERIFIED (class: verify-environment). | `MEASUREMENTS_2026-09-05.md` §13 | OPEN — watch; classify on the next run |
| T83 | 2026-09-06 | **The iPhone 17 Pro FPV page dies 40–60 s after load (Device Farm, Safari 26.3.1; 3 sessions).** Every `#f=` boot answered the seams and was read at ~40 s (dt 17 ms at the 60 Hz cap, 2 ms CPU), then Safari's remote debugger stopped answering for good — with 6 seeded + 3 world helmets (the kill ramp's first step, twice) AND with 0 seeds during the soak's synthetic look-around; a second plain `#f=` boot in one session read fine, so neither the reload nor the models trigger it. Kill (jetsam) vs JS hang UNKNOWN — the console session videos + device syslog decide (`19deb1e4-67fe-435e-8ad1-cecddd6a8d78`, `c90c3fd4-e386-47bc-9b30-539bd7cb1d09`, farm3). First hypothesis: a late background load landing (the S5 8k base-earth texture swap ≈ +250 MB GPU; the streaming LRU). The Pixel 6 Pro survived six ~2.5-min boots. The kill ramp and the soak curve (plan levers 14–16's gates) stay UNMEASURED on iOS until this is classified. | `rendering/MEASUREMENTS_2026-09-05.md` §11 · `tools/devicefarm/ios-baseline.mjs` (hardened: `connectionRetryCount 0`, stall classifier, `fpv,fpv`, `--ramp 0`) | OPEN — watch the videos first; then a `--poses fpv --soak-min 3` run with the 8k swap disabled (`EARTH` boot textures only) as the A/B **RE-MEASURED 2026-09-07 on the T79/T80-g/h/C-1/T101 tree (MEASUREMENTS §19): REPRODUCED — the ramp's first step (6 seeded rows, reload the `#f=` eye) took the boot marker and the debugger stopped answering inside 120 s, twice; T80's −245 MB VRAM did not change it, so the late-GPU-landing hypothesis is weakened. Session `…/ed840495-122f-4465-82a0-cb803fc13d7a/00000` — its console video + device syslog are the next read (the API lists no artifacts while STOPPING). Everything else on the iPhone is fixed by the desktop slices: orbit / city / everest 9–13 fps → 60 / 50 / 60 at `mid`, 3–4 ms CPU; `/m` 9 → 34 fps but still 26 ms CPU (its own main-thread cost, `probe-cpu-profile` on `/m` next). The Pixel was not attached — its half is owed.** **CLASSIFIED 2026-09-07b (MEASUREMENTS §21.1): a JETSAM KILL, per-process limit.** All four sessions' device syslogs carry the same line — `memorystatus: com.apple.WebKit.WebContent [pid] exceeded mem limit: ActiveHard 2048 MB (fatal) … killed by jetsam reason per-process-limit` (2,097,154 KB) — with the WebContent process 176 / 198 / 88 / 70 s old and the kill 14–25 s after the SECOND `#f=` load in that process (Safari kept ONE WebContent for every same-origin navigation of the run; today's process was born at the first pose). It is not system pressure (2.9 GB of pages free) and not a JS hang. The desktop twin (`scripts/probe-memory-footprint.mjs`, the 17 Pro's profile): ONE FPV page holds ~510 MB of renderer footprint, flat over 3 min (peak 592 while streaming); the SAME tab navigated fpv → /m → fpv stacks 557 → 752 → **1,196 MB**, a forced GC leaves 926 with the ArrayBuffer backing store doubled (130 → 260 MB) — the previous documents stay resident. So T83 is the multi-document-per-process shape (the harness's about:blank hops, a user's reloads / route changes), not a single FPV page; the fifth session (`--poses fpv --ramp 0 --soak-min 3`, `9b042dcb-e2af-4631-9fa2-609747b0d27b`) read the same line — pid 99 s old, the kill 63 s after the SECOND load (the soak re-boots the eye) — five for five. Still unmeasured: a single page's survival (every first page was navigated away at 34–45 s) → a `--soak-no-reboot` run. Levers: dispose on `pagehide` (renderer + tile caches + the 8k images) so a cached document holds little; `Cache-Control: no-store` on the HTML disables the page cache outright. **RE-SHAPED 2026-09-07c — ONE page dies on its own.** `ios-baseline --soak-no-reboot` (new: soak the fpv page already up, bail on the first dead SNAP) — the single `#f=` page of session `…/7a86c553-dd48-4f99-8aaf-1a95a70c032e/00000` RELOADED at page age 129 s under the synthetic look-around, no second load; at page age 99 s its `renderer.info` read 2 textures / 5 geometries = a WebGL context lost and restored (a GPU-side eviction first). The desktop twin under the same gesture (`probe-memory-footprint --look --phone`, new `--look`) grew **661 → 1,540 MB in 83 s** tracking the tile caches (107 → 335 MB; geometry 96 → 292 MB, ArrayBuffers 173 → 639) — ~3.9 MB of renderer footprint per cached tile-MB, and `mid`'s caps let the three caches hold 832 MB (rest at 624). **Two levers BUILT:** (1) `QUALITY.leanMobile.{lruBytesMB 48, enrichedLruBytesMB 128, groundLruBytesMB 112}` through `lruCapBytesForLean` (every tier, incl. `high`'s null) — the desktop twin plateaus at 8.6 / 84 / 96 MB cached, geometry 163 MB, footprint ~1.05–1.18 GB (was still climbing at 1.54); (2) `RENDERER.releaseOnPageHide` — `GlobeCanvas` runs its (now idempotent, named) `teardown()` + `renderer.forceContextLoss()` on `pagehide` and reloads on a bfcache `pageshow`, for the multi-document shape. **FIXED 2026-09-07c — FARM A/B (MEASUREMENTS §22.5):** same phone, pose, gesture and tree — caps ON: `/m` → `#f=` (the SECOND load in the process) → an in-place soak ALIVE at page age 269 s with the caches resting at 8.6 / 84 / 97 MB; caps OFF (the same tree with the lean caps at 100,000 MB): 124 / 187 MB and climbing at 92 s, the debugger dead before page age 115 s. The `pagehide` release rode the ON run (not isolated by its own A/B). Watch: dt p50 18 → 26 ms over the 4-min look-around (streaming hitches ~1 /s — T106's family, not memory); the RC20 flip-bank's 2D↔FPV re-fetch under the smaller phone caps is unmeasured. |
| T84 | 2026-09-06 | **The `/m` chart keeps 12 ms of main thread per frame with the controls' raycast gated to ~0** (`m.u0.mid.m0.on` cpu 12.0 / dt 12.1 at 82 fps on the M3 Pro; was 35.2 / 35.8). A second per-frame CPU consumer on the /m route — a phone question (the Pixel read 109 ms cpu at /m pre-fix; the gate takes ~100 of it). Profile with `probe-cpu-profile.mjs --pose m` after the gate. | `verify-shots/perf/baseline-t79-dsf2-2026-09-05T23-26-00.md` | OPEN — slice C/D material |
| T85 | 2026-09-06 | **Owner call: `baseEarth` `raycast = () => {}`.** The backdrop sphere (1.9 km under the terrain) is the only decoration with a live raycast; nooping it removes the 15 ms line item even with the gate off, but changes two behaviours: the controls' pointer picks (drag pivot / zoom point) through tile GAPS and over unloaded ground fall to the ellipsoid instead of a surface 1.9 km deep, and the controls' floor over unloaded ground becomes the ellipsoid. Both corrections, both changes. Optional hygiene — the gate makes the cost moot. | `rendering/T77_SLICE0_ORBIT_FRAME_2026-09-06.md` §6 | OPEN — owner call, not blocking |
| T86 | 2026-09-06 | **`verify-perf-baseline.mjs --device` reads 0 for the bld/gnd LRU + visible columns on the Pixel** (enriched reads fine; the iPhone tool reads all three). A feed-id resolution gap in the device path, not the engine. Also: Android Chrome's `performance.memory` is QUANTIZED (every cell 202 MB) — the heap column is not a measurement on a phone. | `verify-shots/perf/baseline-pixel6pro-dsf2-2026-09-05T23-13-05.md` | OPEN — harness tail |
| T87 | 2026-09-06 | **Knowledge-store hygiene debt (owner order 2026-09-06f → a one-session sweep before T77 resumes):** `mem:core` is **90 KB vs its 12 KB cap** (7.5×; narrative paragraphs duplicate the wip leaves and the DECISIONS digests — "one writer per fact"); 126 `project/wip-*` leaves (re-counted 2026-09-06g; the brief said 130) / 1.4 MB (leaves are kept by policy; each must be ≤ 10 KB); `claude-docs/rendering/` has 12 files / ~500 KB and **no `README.md` entry point** (CLAUDE.md §Knowledge says every bundle owns one) with superseded twins un-bannered (`WEB_RESEARCH_PROMPT_2026-09-02` vs its RESULT; `RENDERING_QUALITY_PASS` vs the charter; the phone checklist §A/§B done → `MEASUREMENTS` §11); `ARCHITECTURE.md` / `IMPLEMENTATION_PLAN.md` unreconciled with the MESH SUITE + T77 as-built; this registry is 94 KB and its OPEN rows' pointers were never link-checked. Plus the owner's feature ask riding the same session: the in-app guide (`src/lib/guide/guideContent.ts`, 126 topics, finalized 2026-08-22g) vs every feature shipped since — inventory → gap table → topics under the lint, `no-slop` on every string. | `NEXT_SESSION_PROMPT.md` §"THE NEXT SESSION" (the full charter, tracks E · D1–D5, gates, traps) · `references/audit-mode.md` · `references/checklists/docs.md` · `mem:memory_maintenance` | OPEN — the next session **— re-verified 2026-09-06: OPEN, in progress** — session 2026-09-06g is the session this row describes. **Correction, measured 2026-09-06:** this row's "126 topics" is wrong, and so is the same figure in `NEXT_SESSION_PROMPT.md:37` ("83 KB, 126 topics"). `src/lib/guide/guideContent.ts` is **86,053 bytes** and holds **12 chapters / 86 topics** — 98 `GUIDE_INDEX` nodes, which is also the search-index doc count (`test/lib/guide/guideSearch.test.ts:76`). Track D5's gap table must be built against 86, not 126. |
| T88 | 2026-09-06 | **RC7's look-cone convergence reads 50.3 % against the audit's S4 bar of 0.9, and the instrumentation cannot say why.** The tile drain defers, but only gate rejections are counted — null-terrain deferrals are not — so the shortfall has no attributed cause. Carried in NEXT_SESSION_PROMPT §"OPEN TAILS ELSEWHERE" since 2026-08-26; its only registry home was T54, which is CLOSED, so the 2026-09-06 mirror check promoted it to its own row. | `rendering/RENDERING_CHARTER_2026-08-25.md` RC7 · T54 (the charter row, closed) | OPEN — instrument the deferral counters first |
| T89 | 2026-09-06 | **RC9's warm-restore leg is browser-UNVERIFIED.** Banking is proven by squeezing the LRU, and distance alone never evicts — the 6 km bake fits inside the desktop cap — so the warm-restore path has never been exercised in a browser. Same mirror-check promotion as T88: NEXT_SESSION_PROMPT lists it as an open tail, T54 (its only home) is CLOSED. | `rendering/RENDERING_CHARTER_2026-08-25.md` RC9 · T54 (the charter row, closed) | OPEN — needs one browser leg |
| T90 | 2026-09-06 | **`readiness` never advances after POST: a PENDING user-model row stays PROCESSING in the MODELS tab forever.** Recorded as an MS-later tail when the MESH SUITE closed (NEXT_SESSION_PROMPT lines 293 and 339). Its only registry home was T74, which is CLOSED, so the 2026-09-06 mirror check promoted it to its own row. Not re-checked against the running app in this session — the state below is the 2026-09-05 record, not a new observation. | `MESH_SUITE_PLAN.md` (MS0 ingest-readiness unknown, carried in T74) · NEXT_SESSION_PROMPT §"OPEN TAILS ELSEWHERE" | OPEN — MS-later tail; re-check before any user-model work |
| T91 | 2026-09-06 | **RC16 residual straddler duplicate** — after RC16 (2026-08-26d) an owned building that pokes outside its cell's bbox is still drawn twice out there: 212 dnipro · 229 dnipro-o2w · 96 st-albans · 111 st-albans-o2w features (0.03–0.42 %), poking a median 3–10 m. A coincident-sliver duplicate, not a hole; the crossfade ring RC16 shipped without is the intended fix — growing the prism instead punches a real hole. Carried in NEXT_SESSION_PROMPT since 2026-08-26 with no row (audit 2026-09-06g, D4-5). | DECISIONS 2026-08-26d (`DECISIONS_ARCHIVE.md` §Moved 2026-09-06) · `rendering/RENDERING_CHARTER_2026-08-25.md` RC16 · T54 | OPEN — cosmetic; queue behind the T77 slices. |
| T92 | 2026-09-06 | **`dnipro-cityscape` (owner pose `#p=48.46008,35.07720,553,276.7,74.9&t=1788707940553`, sun 8°): a large translucent magenta wedge covers the right third of the frame and a black band hugs the left edge.** First seen on the visual-sweep contact sheet (`verify-shots/sweep/full2/sheet-01.png`, `pre-slices/`). Suspects, unverified: the aim/focal cone (`scene/aimCones.ts` / `focalCone.ts`) drawn at orbit scale, or the BEST SPOT sheet. Reproduce with `node scripts/verify-visual-sweep.mjs --ids dnipro-cityscape --sheet`. | `scripts/lib/poses.mjs` `dnipro-cityscape` · the sheet | OPEN — new 2026-09-06h; classify first (cone vs sheet vs shadow slab), then a sliced fix. **— 4 probes ready 2026-09-06k (`probe-focalcone/sheets/groundfade/horizonband.mjs`, verdict lines, house :9333); UNRUN (T98).** **CLASSIFIED 2026-09-06k2: the wedge IS the focal cone's fill (`probe-focalcone` cone-off removes it); the 'black band' is the river in shadow. Fix = a scale/alpha rule for the cone at cityscape altitude — owner taste.** **OWNER RULED 2026-09-06m: option (a) — fade the FILL, keep the two boundary rays**; the driver (tilt vs altitude vs screen coverage) is chosen from the sheets in the fix session. NEXT. **DONE 2026-09-06n: `FOCALCONE.fillTiltFadeStartDeg` 50 → `fillTiltFadeEndDeg` 70 on the LIVE orbit tilt (`focalConeFillTiltK`, exactly 1 below 50°; `scene/focalCone.ts` + `stepAimCones`; 8 tests). `probe-focalcone`: tilt 74.9° → fill alpha 0, edge alpha 0.700. MEASUREMENTS §17.4. CLOSED pending the post sheet.** |
| T93 | 2026-09-06 | **`everest-orbit-73` (owner: tilt 73° shows the complex horizon): a hard black band plus halftone dither runs along the whole horizon at ULTRA off AND on; `everest-orbit-52` does not show it.** Also on the 200 mm zoom sweep base pose (`dnipro-fpv-zoom-sweep`): a black polygon wedge intrudes top-right, and hard LOD terracing at every stop. Contact sheet evidence as T92. Suspects: the terrain skirt / horizon fade (`lib/globe/terrainSkirt.ts`), the base-earth backdrop seam at grazing tilt, the building reveal dither. | `scripts/lib/poses.mjs` `everest-orbit-73`, `dnipro-fpv-zoom-sweep` | OPEN — new 2026-09-06h; the owner's "your tilts never showed the horizon" case, exactly. **— same probes cover it (`probe-horizonband`, `probe-groundfade`); UNRUN (T98).** **CLASSIFIED 2026-09-06k2: the band is the BASE EARTH's limb shading between the terrain's loaded reach and the horizon (ground-off keeps it; far ×8/skirt/earth toggles do not move it); the halftone is the far tiles' reveal dither. Fix = tint the limb to the horizon haze, or extend the reach at high tilt (cost).** **OWNER RULED 2026-09-06m: option (a) — tint the base earth's limb to the horizon haze at grazing angles** (the terrain skirt's haze inputs, no second haze; reach unchanged). NEXT. **RE-CLASSIFIED + FIXED 2026-09-06n: the band is FAR TERRAIN hazed toward a directional in-scatter darker than itself (earth-off / atmo-off / far ×4 leave it; ground-off replaces it with the earth) — `ftwAerial`'s LEVEL lobe now relaxes to isotropic past `ULTRA.limbStartM` 40 km (fully by `limbEndM` 120 km); rows 22–28 % luma 84–89 → 149–153, the near field byte-identical by construction (`aerialLimb.test.ts`; orbit-52's top rows at the ramp's foot, Δ ≤ 6). MEASUREMENTS §17.5. The zoom sweep's FPV wedge is a separate shape, still OPEN under this row.**  **THE FPV HALF READ 2026-09-07j (the VPN on, the two probes RUN at last):** `probe-horizonband` at `dnipro-fpv-zoom-sweep` — the far ladder ×1…×8 monotonic, `camFar` 180 km vs Everest's reach 374 km (the documented geometry), the ×1 frame at the base pose (191 mm, heading 98°) clean at full size: no black polygon top-right; the 8-heading strip of sweep `post2-2026-09-07j` shows no wedge at any stop, where the 2026-09-06h strip carried the reveal-cap halftone over the whole ground. `probe-groundfade` 30 s: `uFtwFade` settled at 1.0000 (`loadEnd` 1) at the zoom sweep AND at `everest-orbit-73` — no permanent halftone. **NOT REPRODUCED on the j tree**; closed as a watch (the sheets are read every sweep). |
| T94 | 2026-09-06 | **The canvas is not frame-deterministic, so pixel goldens cannot gate the byte-identical-`high` law.** Two `captureScreenshot`s of ONE settled boot two rAF apart differ in ~30 % of pixels (max Δ 74), four seconds apart 84 %; the UI chrome is pixel-stable. Sources (unranked): frame-seeded dithers (building reveal, ground), the animated sky/stars, streaming. Needed: a DETERMINISTIC CAPTURE seam (`__globe.freezeFrame(on)`: fixed dither seed, sky animation paused, shadow rig latched) so `verify-visual-sweep --compare --tolerance 0` becomes a real gate; until then the law is read from the tunables' identity branches + the unit fences. | `scripts/verify-visual-sweep.mjs` (`--golden`/`--compare`), `conventions/verify.md` §The view catalogue | OPEN — new 2026-09-06h; the accelerator's missing half. **— SEAM BUILT 2026-09-06k: `src/lib/globe/frameFreeze.ts` + `__globe.freezeFrame(on, parts?)`, 12 clocks/holds, sweep `--freeze` (default under --golden/--compare) with a per-pose byte self-check. The inventory run + the byte gate are UNRUN (T98). Not held: `frameCount` cadences; star `Math.random()` = T95.** **GATE PROVEN 2026-09-06k2: 14/14 poses byte-identical under `--freeze`; the sweep now measures before freezing, freezes in two phases (`--reveal-settle-s`), and re-boots a pose before its descent leg (T102).** |
| T95 | 2026-09-06 | **Governor instability at `dnipro-cityscape`: the same pose + pref boots to `mid`/31 fps in one run and holds `high`/54 fps in another** (headless :9333). Also `seatSettle().nearMaxResidualM` at the zoom-sweep base read 22.65 m in one run and 0.08 m in the next. Both smell like boot-order races (the tile storm vs the governor's EMA window; a coarse-tile seat that later refines). | `verify-shots/sweep/full2/report.md` | OPEN — new 2026-09-06h; watch across the post-slice sweeps before chasing. **— 2026-09-06k: the T94 seam makes the byte gate a WITHIN-boot instrument; cross-boot needs a seeded PRNG for the star build (changes the picture) — owner call.** |
| T96 | 2026-09-06 | **The BASE rig (ULTRA off) keeps every dusk/shadow defect the ULTRA rig fixed, by the byte-identical-`high` law**: the key never dims (2.025 flat to the horizon, `StylizedTiles.ts` sun arm), the shadow field fades from +3.46° and dies at +0.46°, the dome band is `max(dayK, hGold)`; charter item AB1. The 2026-09-06h sunset fix (F1–F3) and slice A's demand-driven rig + metric bias ship ULTRA-ON / base-identity with live tunables (`SHADOWS.rigKeySnapTexels`, `biasTexels`, …; `__globe.shadowRig()`), so the base flip is ONE commit once the owner rules. | `scratchpad`→`rendering/SUNSET_LIGHTPATH_2026-09-06.md` §9 F5 · `test/components/globe/duskShadeRatio.test.ts` ("the defect, preserved exactly") | OPEN — **OWNER RULING**: may the base rig take the ULTRA light/shadow model (recommended)? **— RULED YES 2026-09-06i: the base rig takes the ULTRA light/shadow model (cost levers stay ULTRA). Slice BASE, first item of the next session.** **— LANDED 2026-09-06k (slice BASE): `ULTRA.baseTakesLook` + `lookOn()`; unit 2,692 green; ladder 18/18 (on) / 18/18 (off, one check re-pointed for the 5 km box's ramp); charter/ultra/shimmer/sweep UNRUN — Cesium ion WAF-blocked (T98). DECISIONS 2026-09-06j+k.** **Browser gates 2026-09-06k2 with terrain: ladder off 18/18, on 17/18 (T100), charter re-pointed, ultra 30/30, base fpv.u0 churn p50 0.0000. The T96 look is on the post sheet (west-sunset FPV renders the true dusk on the base rig).** |
| T97 | 2026-09-06 | **The RESOURCE BUDGET is now machine-checked** (`verify-chrome.mjs` exit 3 · `ensureBrowser()` throws · `resource-watchdog.mjs`), after session j's six dev servers + six Chromes froze the 36 GB machine. Remaining gap: nothing stops a raw `wix dev` in a second checkout, and the watchdog is opt-in (start it at session boot). | `conventions/verify.md` §THE RESOURCE BUDGET · `mem:project/dev_environment` · DECISIONS 2026-09-06j+k | OPEN — hygiene: a SessionStart hook line that starts the watchdog and prints `--budget`. |
| T98 | 2026-09-06 | **`api.cesium.com` returns HTTP 403 "Request blocked" (CloudFront WAF) from the owner's Dnipro ISP IP** — with or without the token, curl or Chrome, no VPN up — so no Cesium terrain / OSM building tile loads and every terrain-dependent browser gate is vacuous (`terrainEpoch` 0; charter 68/82 with 0/39,415 seated). Fine at 15:29 the same day; leading cause = a rate-based WAF rule tripped by session j's six globes streaming ion at once. | DECISIONS 2026-09-06j+k · MEASUREMENTS §15 | OPEN — **poll first thing next session** (`curl -s -o /dev/null -w "%{http_code}" https://api.cesium.com/v1/assets/1/endpoint`); if still 403 it is the owner's: VPN / another egress / Cesium support. Until then the browser lane is dead. **LIFTED 17:55 (403 → 401 after ≈2 h) with no change on this side — the rate-based WAF shape is confirmed. Stays OPEN as a recurrence watch: poll before every browser session; never fan globes out.** **CORRECTED 2026-09-06k3 (owner): it lifted ONLY when the owner connected Proton VPN (Finland; egress AS9009) — a block on the Dnipro ISP address/ASN that also blocked the ion dashboard login, not a rate limit. Standing: the VPN is a PRECONDITION for any terrain gate from this network; the boot check is `ipinfo.io/country` ≠ UA + the ion curl; no macOS CLI — `open -a ProtonVPN`, then the owner clicks Connect.** **THE PHONE LANE 2026-09-07b:** `adb reverse` carries only `localhost:4321` — the Pixel fetches ion over ITS OWN network, and the Dnipro mobile address is blocked too: both Pixel runs (2026-09-05 §11 and the first of 2026-09-07) read a BARE BASE SPHERE (317k tris, `visible gnd 0`) at city / everest / `/m` as if it were the pose — the owner caught it. `verify-perf-baseline --device` now asks ion from the phone's page before the first boot (403 → exit 3, "enable the phone's VPN") and FAILs any non-FPV cell that settles with no ground tiles visible. §11's Pixel city / everest / `/m` rows are void; §21.2 has the real ones. |
| T99 | 2026-09-06 | **Boot allocates the composer targets twice**: `composer.setPixelRatio()` (`GlobeCanvas.tsx` ~:389) runs `setSize(drawingBufferW, drawingBufferH)` — DPR applied to already-DPR'd pixels, a 4× over-allocation for a few ms — before `onResize()` corrects it. Pre-existing; transient; the device class T83 is about. | t80g report §Risks 3 | **CLOSED 2026-09-06l** — the redundant `composer.setPixelRatio()` is gone (the composer takes the renderer's ratio in its constructor; `onResize()` sizes once) and `fusedBloom.test.ts` fences the boot block. Magnitude CORRECTED: at that moment the canvas is still 300×150 (no `renderer.setSize` yet), so the doubled-DPR allocation was 1200×600, a few MB — three small allocations, not a 4× full-size peak. |
| T100 | 2026-09-06 | **The ULTRA rig's shadow-field release band is steep: 1.000 at −0.14° → 0.636 at −0.5° → 0 at −0.9°, and the shadowed ground brightens +4.9 codes at −0.5° (ladder luma 70.1 → 75.0) as it lets go**; the charter's RC2 fade step reads 0.025–0.051 against a 0.05 bar for the same reason. The authored T66 rises are gone; this is the fade band (`shadowLengthK` / the 0.0093 band) itself. The base rig ramps earlier (its 5 km box) and reads 0.00 codes. | MEASUREMENTS §15.5 · `verify-ultra-dusk --ladder --ultra 1` (17/18) | OPEN — **owner call**: widen the band or start it near +0.2° so the release spreads over ~3 rungs; the T66 gate then reads ≤ 2 codes on both rigs. **OWNER RULED 2026-09-06m: option (b) — start the band at ~+0.2°, keep its width, ULTRA-only.** Gate: ladder `--ultra 1` 18/18 alone, `--ultra 0` still 18/18, RC2 fade ≤ 0.05. NEXT. **BUILT 2026-09-06n as ruled (`ULTRA.shadowReleaseStartSin` sin +0.2°, `shadowReleaseBandSin` 0.0093, `ULTRA_FIELD_RELEASE` on the chip's reach condition; ladder 19 checks, T100 check both rigs) — and the gate is NOT met: T66 worst rise 4.94 → 4.23 (moved to the −0.14° rung); A/B arms E-a (band to the gate) 4.67, E-b (+0.5°, to the gate) 3.82. The rise is the OVERLAY retiring with `directShareK` at the −0.5° rung (the k2 diagnosis was wrong: overlay 0.164 → 0 while the field was still 0.636); the field band cannot fix it. OWNER CALL: the overlay's retirement (the F1 bound on `keyExtinctCurve`'s last step) or re-state T66 for the flat ULTRA look. MEASUREMENTS §17.2. Base ladder 19/19 byte-identical.** **OWNER RULED 2026-09-06o: option (a) — spread the OVERLAY's retirement (the `keyExtinctCurve` tail to true sunset, or an eased overlay release over +0.2 → −0.83°); gate = the ULTRA ladder T66 ≤ 2 at every rung, base 19/19 byte-identical, RC2 ≤ 0.05, ultra 30/30, the sunset pair by eye. NEXT (first slice of the next rendering session).** **CLOSED 2026-09-07 (a) — the overlay's OWN extinction tail: `duskLight.overlayReleaseK` (`keyLevel(+0.2°) × x^0.9` from +0.2° to the gate, `max()`-ed with `directK` on the chip's cascade reach; `ULTRA.overlayReleaseStartSin` / `overlayReleasePow`) and ruling (b)'s field band SUPERSEDED back to the disc (`shadowReleaseStartSin` at its identity): §17.2's four arms showed the ShadowMaterial's per-cascade mask product zeroes the overlay wherever the field is 0, so no field band could reach the −0.5° rung. Ladder `--ultra 1` **19/19, T66 1.48** (series 67.4 68.4 68.6 70.1 71.5 73.0), `--ultra 0` 19/19 byte-identical, charter 84/85 (RC2 chip-off, unchanged), ultra 30/30, the −0.5° frame indistinguishable by eye. MEASUREMENTS §18.** |
| T101 | 2026-09-06 | **C-1's deep-answer deferrals thrash at the orbit arrival: `rejected +39,629` over the 479-frame leg (was 0)** — every cell's plane refines at once, 6 cells resample per frame (`reseatDeepResampleMaxPerFrame`), the rest re-queue and are rejected again next frame. Accuracy is fine (end 0.000 m, collapses 0, city p95 29.7 vs 34.3), the FPV eye reads +0 as designed; this is sampling budget burnt on re-rejections while streaming. | MEASUREMENTS §15.5 · c1 report §Risks | OPEN — hold a deep-answer feature OUT of the queue until its cell has resampled (a per-cell `deepPending` flag), or scale the cap with the number of dirty cells; measure `frame.cpu` at the arrival before and after. **OWNER RULED 2026-09-06m: option (a) — the per-cell `deepPending` hold.** Gate: arrival `rejected` from +39,629 to ≈ cell count; FPV eye +0 / 0 / 0.000 kept. NEXT. **DONE 2026-09-06n: `ENRICHED.reseatDeepPendingHold` + `deepAnswerVerdict()` (seatQuiet) + `CellSeat.deepPending`; arrival `rejected` +39,629 → +2 (`deepHeld` +2, pendingCells@end 0, end 0.000 m, collapsed 0); FPV eye rejected +2 / collapsed 0 / near end 0.000. MEASUREMENTS §17.3. CLOSED.** |
| T102 | 2026-09-06 | **After a frozen capture + thaw, a `requestFly` issued from the harness does not fly (37 app frames, top pose held), while the same call on a never-frozen page flies 31,730 → 1,598 m in 3 s and a 12 s clock freeze followed by a thaw in a bare probe ALSO flies.** The sweep now re-boots the pose before a descent leg, so the gate is green; the cause (something in freeze → two `captureScreenshot`s → thaw) is not isolated. | DECISIONS 2026-09-06k2 · `verify-visual-sweep.mjs` leg re-boot | OPEN — bisect with the bare probe + the self-check shots; suspect the RC21 frame gate's quiet latch or `controls.update(0)`'s library-side dt.  **ISOLATED + FIXED IN THE HARNESS 2026-09-07j:** not the freeze at all — `/json/new` ACTIVATES the tab it opens, and the composer target (opened lazily by the first pose's self-check) sent the pose tab to the BACKGROUND: `document.hidden` true, the engine's rAF throttled, `flight.active()` never true (a bare probe reproduces it with nothing but a second target; `Page.bringToFront` on the pose tab cures it — 31,762 → 1,670 m in 2.5 s). `verify-visual-sweep`: `getComposer()` hands the front back to the pose tab, the leg runner fronts the tab before its recorder, `--no-leg-reboot` (new) drives the thawed page and now ARRIVES (652 rAF rows, 1,531 m); the re-boot stays the default so the leg's golden conditions hold. The RC21 gate and `controls.update(0)` were never involved. |
| T103 | 2026-09-06 | **The `/m` shell's frozen-frame self-check is nondeterministic: `legacy-m` re-shot 490 px (0.091 %, max Δ 140) two rAF apart under the T94 freeze** on a tree identical to k2's 14/14, then 14/14 again on the next run. The sweep diffs the whole page capture, and the `/m` chrome carries CSS animations (the location dot, the joystick ring) the canvas freeze cannot hold. | `verify-visual-sweep` pre-2026-09-07 report §Frame freeze | OPEN — either capture the canvas only for the self-check, or hold the shell's CSS animations under the freeze (a `[data-frozen]` root attribute the shell's keyframes pause on). Not a render regression.  **FIXED IN THE HARNESS 2026-09-07j:** the sweep pauses every document animation through the CDP Animation domain (`Animation.setPlaybackRate` 0 under the clock pin, 1 at the thaw — `pauseAnimations()`, recorded as `freeze.cssAnimations`); no product code. `legacy-m` self-check 2/2 byte-identical (0 px) where the same session's pre-fix run read 64 px. The `dnipro-descent` 1,444 px / Δ 1 self-check residue seen once in 07h recurred once here — intermittent, sub-LSB, not this row. |
| T104 | 2026-09-06 | **`frame.gpu` (`EXT_disjoint_timer_query`) over-counts multi-pass post chains on the ANGLE/Metal stack: dropping six tiny bloom passes (`nMips` 5 → 2, 3.5 % of the chain's pixels) read −6.6 ms on the timer and 0.0 ms on frame time (dt 15.2 vs 15.0, fps 66 vs 67)**; a per-frame GPU duration cannot exceed the frame interval at 67 fps, so the bracket includes command-buffer queueing/overlap. Every T77 GPU number since MEASUREMENTS §2 carries this shape for the bloom cells; the shadow and geometry cells (single big passes) agree with dt. | MEASUREMENTS §16.3 · `verify-perf-baseline --bloom-mips` | OPEN — re-state the T77 GPU gates on **dt p50** (or corroborate each with dt in the harness table: add a `dt−gpu` column and flag cells where gpu > dt), and never read a pass-count lever off `frame.gpu` alone. **OWNER RULED 2026-09-06m: re-state the T77 GPU gates on frame time (dt p50)** — the `dt−gpu` column + the dated MEASUREMENTS note + the plan gate supersession; NEXT. **DONE 2026-09-06n: `verify-perf-baseline` prints `dt−gpu (T104)` (console + md table, `!` when gpu > dt); every §15.5/§16.3 bloom-on cell re-read at −2.6…−4.4 ms, every bloom-off cell +2…+3 (MEASUREMENTS §17.1); the T77 plan's row-0 and T80-g gates superseded to dt p50. CLOSED — the instrument stays; the rule is in the column's own comment.** |
| T105 | 2026-09-07 | **`verify-ultra-dusk.mjs` read its two positionals (port, shot dir) as `argv[2]` / `argv[3]` verbatim**, so the documented `9333 --ladder --ultra 1` wrote every rung's shot into a directory literally named `--ladder` in the repo root (24 shots, untracked, found by `ls`), and `--ladder --ultra 0` without a port would have dialled port "--ladder". | `scripts/verify-ultra-dusk.mjs:26-28` | **FIXED 2026-09-07**: the positionals are the non-option args in order (an `--ultra` value never fills one); the stray shots moved to `verify-shots/t100a/`. Pattern to keep: every harness that takes positionals must skip option-shaped args (the sweep's port sniffer already did). |
| T106 | 2026-09-07 | **The descent's hitches on a phone are the ENRICHED cell's `load-model` handler, not the glTF parse** (MEASUREMENTS §21.3, `probe-cpu-profile --leg descent --device` on the Pixel 6 Pro, terrain on): 57 hitch frames of 146 (dt p95 317 ms, max 849), and inside them `enrichedBuildings.ts` `load-model` (the per-cell traverse, fingerprint pass, U8 recovery) **2.26 s**, `enrichedMask.ts` `vertexKeyToRunWithCollisions` + `mapSegmentsToRuns` **0.80 s**, the OSM `buildings.ts` load handler 0.33 s, `vectorTiles` parse 0.20 s, `rawHeightAt` raycasts 0.29 s — glTF parse **1.4 %**. Desktop `high` agrees in shape at 4× the speed (§21.3: seats + their `bufferSubData` 0.8 s, load handlers 0.4 s, parse 0.7 %). **Lever 10 (worker glTF decode) is CLOSED, not built — aimed at 1 % of the time.** | `src/components/globe/scene/enrichedBuildings.ts:1187` (`load-model`) · `src/lib/globe/enrichedMask.ts:111,129` · `scene/buildings.ts:110` · `scripts/probe-cpu-profile.mjs --leg descent` | OPEN — a design slice under MESH_SUITE_PLAN §4a's no-regression contract: (a) bake the mask runs / fingerprints into the cell sidecar (plan lever 7's shape), (b) or slice the handler across frames (one cell per frame, the birth-stamp dissolve already hides a late seat), (c) `parseVectorTile` to a worker. Measure with the same probe on the Pixel. **RE-SHAPED 2026-09-07d (MEASUREMENTS §23.4): the two hot loops were three's `EdgesGeometry` (1,832 ms Pixel, by leaf) and the string-keyed mask (873 ms) — both pure functions of the cell's floats. BUILT: `lib/globe/fastEdges.buildFastEdges` (three's algorithm on integer keys, element-identical output + the source vertex per endpoint) + `enrichedMask.segmentRunsFromSources` (the same party-wall rules on exact-bit keys), wired through `scene/edgesGeometry.buildEdgesGeometry` into BOTH handlers. In-page A/B on the resident cells (`__globe.enrichedBench`): desktop 33 cells 527 → 57 ms edges, 240 → 17 ms mask; the phone twin (4×) 865 → 114, 455 → 33; mismatch 0 / 0 across 50 real cells (`fastEdges.test` pins prisms, party walls, gables, slivers, indexed, soup, −0). Still OPEN for the tail: the biggest cell's WHOLE handler in one frame (65 ms on the twin under 4×; the fingerprint pass ~0.3 s Pixel) — the two-phase slice (b), one cell per frame under a ms budget, edges on their own birth stamp; measure on the Pixel with `probe-cpu-profile --leg descent --device` when it is back (the leaf `EdgesGeometry` must read ≤ the OSM-only residue, `vertexKeyToRunWithCollisions` 0).** **FIXED 2026-09-07e — the two-phase handler (MEASUREMENTS §24): phase 1 synchronous (cell record, material swap, F1 birth, trees; `handlerMaxMs` 0.4 / 0.8 ms), phase 2 one unit per mesh on `lib/globe/loadQueue` drained by `update()` under `ENRICHED.loadBudgetMs` 6 / `loadBudgetMsLean` 3 per frame, nearest cell first, sticky mid-flight; the unit's phases 0 edges (`createEdgesBuilder` — `fastEdges` RESUMABLE, own F1 birth) · 1 attribution (`createSegmentRunAttributor`, resumable) · 2 CSR/spans/bounds · 3 fingerprints (by run) · 4 the part + the footprint locate (by feature) · 5 registration + RC9 + overrides + `touchCell` (atomic); `dispose-model` cancels. The A/B on the descent leg (`scripts/probe-load-phase2.mjs`): worst drain desktop 30.3 → **8.5 ms**, phone twin 85.9 → **8.0 ms** (bar < 16); identity `enrichedBench(50)` 0/0; >33 ms frames 20 → 10 desktop. The cost: the twin's whole-city landing drains over ~4.4 s at 3 ms/frame (§24.2; `__globe.enrichedLoadBudget(ms)` live, DBG `buildings.loadPending` / `loadMaxMs`). The Pixel read (`probe-cpu-profile --leg descent --device`) is still owed — the device was away this session.** **READ ON THE PIXEL 2026-09-07f (MEASUREMENTS §25): T106 a+b hold on the device — dt p95 316.7 → 50.0 ms, hitch main thread 6.73 → 3.93 s, the enriched handler gone from the callers; and the OSM handler was the biggest app item left (410 ms in the hitches, three's `EdgesGeometry`: every b3dm position is INTERLEAVED and had been failing the fast path's gate, desktop too). FIXED 2026-09-07f: `positionsF32` de-interleaves (identity pinned, `edgesGeometry.test`); the OSM handler is two-phase on its own `loadQueue` under `BUILDINGS.loadBudgetMs` 3 / 1.5 (`__globe.buildingsLoad()`, DBG `buildings.osmLoad*`); the edge map is a typed table and both builders take a per-queue SCRATCH (the per-unit ~9 MB + a 300k-entry `Map` was the GC that made a 3 ms budget read 13.5 on the phone). Pixel worst drain: enriched 13.5 → **6.7 ms**, OSM atomic → **3.1 ms**; desktop `high` 8.5 → 7.0. OWED: the post CPU profile at thermal status ≤ 1 (§25.5 — the phone locked itself), and the `ensureLocated` one-shot is now TIMED (`enrichedLoad().locateMaxMs`) for the parts-first order.** |
| T107 | 2026-09-07 | **`controls.getPivotPoint` was called EVERY FRAME by two steps** — a centre-screen raycast through the terrain TIN's triangle loop (no BVH; the T79 gate arms only inside `_getPointBelowCamera`): `stepMobile2dLocks` on `/m` measured the live tilt with it before its own deadband (87 % of the `/m` main thread — the iPhone's 26 ms and the Pixel's 55–58 ms per frame, MEASUREMENTS §19 / §21.2), and `stepTiltGlide` re-found its rotation pivot per frame (27–33 ms in every arrival frame of the descent, the leg's largest single hitch source on desktop `high`). | `src/components/globe/StylizedTiles.ts` `stepMobile2dLocks`, `stepTiltGlide` · `tuning.ts` `CONTROLS.tiltGlidePivotRefreshMs` · `test/components/globe/mobile2dLocks.test.ts` | **FIXED 2026-09-07b**: the 2D lock judges its deadband from the ellipsoid normal under the camera (≤ 0.003° from the pivot's) and raycasts only in frames that rotate; the tilt glide finds its pivot once per glide and refreshes it every 500 ms (a landing tile still moves it). Pixel `/m` 59.3 ms / cpu 57.6 → **16.6 ms / cpu 1.1** (16 → 60 fps, no demotion); desktop descent hitches 86 → 64 frames, `stepTiltGlide` 497 → 74 ms. The remaining per-glide raycasts cost ~100 ms each on the Pixel — the cheaper pivot is a terrain BVH or a DEM lookup (lever 8). |
| T108 | 2026-09-07 | **User models occluded nothing** (audit-occlusion A1): `planFeed.collectMeshes` walked only the two tile groups and `bestSpotFeed.flattenTin` the three, while `attachUserModels` adds its group straight to `scene` — a placed GLB in front of the FPV eye changed zero radar bands, zero scrubber samples, zero FIND / THIS FRAME rows, zero heat-map cells; nothing invalidated on a model change. `MESH_SUITE_PLAN.md:271`'s §4a contract only required edits not to BREAK the sweep. | `audits/audit-occlusion-2026-09-07.md` A1 · `scene/userModels.ts` `occluderRoot()` / `occluderEpoch()` · `scene/planFeed.ts` · `scene/bestSpotFeed.ts` · `test/components/globe/planFeed.test.ts` | **FIXED 2026-09-07c**: both feeds visit `userModels.occluderRoot()` (the profile sweeps its edges without the OSM mask rejection, instanced meshes inside a GLB skipped; BEST SPOT flattens it into SOLID outside the surveyed/extruded provenance count; a hidden group — the MDL chip off — sweeps nothing) and watch `occluderEpoch()` (bumped on resident/unload, chip, committed seats, rebase, every live drag frame) with their quiet windows. Tests: planFeed 9, bestSpotFeed +2, userModels +1. Browser: UNVERIFIED this session (no model placed under a harness). |
| T109 | 2026-09-07 | **The plan feed never re-swept a tile that landed after the sweep, and every rebuild blanked the published profile** (audit A2 + A3): its one invalidation was the enriched re-seat epoch (Dnipro-only) — outside Dnipro nothing past the 25 m eye deadband; `bestSpotFeed` had fixed the same hole for itself on 2026-08-24 (its D1) with `builtEpoch` / `terrainEpoch` the orchestrator already computed two lines below. And `startBuild` nulled `binsMirror`, so radars fell to unfractured bands and the scrubber trace to "unknown" for the 40 + meshes/2 frames of every sliced rebuild — walking FPV re-crossed 25 m before a 400-mesh sweep finished. | audit A2/A3 · `scene/planFeed.ts` (`shown`, `startFrame`, the epochs, `debug().carried`) · `tuning.ts` `PLAN.streamQuietFrames` 90 · `PLAN.carryProfileDistM` 60 · `test/components/globe/planFeed.test.ts` | **FIXED 2026-09-07c**: `PlanFeedCtx` carries `terrainEpoch · builtEpoch · modelsEpoch`; a change after the build started re-sweeps the SAME anchor after 90 quiet frames (one burst = one rebuild); the last complete profile stays published through any rebuild while the new eye is within 60 m of its eye (= `AIMCONES.skylineGuardM`, test-locked ≤), null beyond as before. `rebuildDistM` stays 25 m (a smaller deadband restarts the ~200-frame sweep before it completes while walking; the lever is `meshesPerFrame`, which phones cannot afford). Browser: the desktop sweep re-ran green (13/14 self-check, T103); the radar under a landing tile UNVERIFIED by eye. |
| T110 | 2026-09-07 | **3° skyline bins under a long lens** (audit B1): `PLAN.azBins` 120 → a 600 mm frame (≈ 3.4° wide) sits on 1–2 interpolated bins; `raiseBin` takes the max, so a mast raises a whole bin to its tip (over-block) and a 0.5° gap between two towers is lost (under-block). The 3° justification (`tuning.ts` — a 150 m building at 3 km) is a skyline criterion, not a frame one; the in-frame tests quantize FOV to 0.5°. | audit B1 · `lib/geo/horizonProfile.ts` · `lib/geo/occlusion.ts` · `scene/planFeed.ts` | OPEN — recommended: a second FINE bin array for MESH edges only (720 bins = 0.5°; the terrain march stays at 120 — it is the expensive half), `sampleProfile` = max(terrain interpolated, fine bin). Gate: `occlusion.test` with a 0.5° gap at 600 mm. Owner call on cost/benefit. **FIXED 2026-09-07d (owner: per the recommendation; MEASUREMENTS §23.1).** `PLAN.azBins` 1440 (0.25°, 16 bins across a 500 mm frame) on BOTH shells (`azBinsLean` 1440); terrain still marched at `PLAN.terrainAzBins` 120 and folded in by `foldCoarseProfile` (known-aware); the edge walker projects each vertex once, fills the bins between samples (`raiseSpan`), scales its subdivision cap with the bin count and is resumable under a TIME budget (`sweepMeshEdgesSliced`, `PLAN.sweepBudgetMs` 3 / 1.5 lean, a 64-triangle deadline check). Desktop: the whole mesh phase 56 ms over 22 frames, worst frame 3.2 ms; the phone twin (4× throttle) ~250 ms over ~130 frames, worst 3.2 ms → mobile keeps the full precision. At the 200 mm pose 14 % of the horizon read > 0.5° lower than the 3° box-max (the over-block the audit named). `occlusion.test` +4 (a 1° gap at 1 km resolved at 0.25° and lost at 3°; a mast raising only its own bins; the canyon wall hole-free; the sliced walk ≡ the whole walk), `horizonProfile.test` +3, `planFeed.test` +5. |
| T111 | 2026-09-07 | **The FPV HUD's body chips, the day arcs / sky trail, the eclipse rows, the dark-sky windows, the meteor showers and the scrubber's sun/moon CURVES use the geometric horizon only** (audit B2): `FpvHud.tsx:36` / `StylizedTiles.ts` `altDeg > FPV.bodyMarkerMinAltDeg`, `dayArcs.ts:22-25` (a documented ruling: the analytic fade owns occlusion), `showers.ts`, `eclipse.ts`. `planFeed.profileSample()` was built for "the dayArcs skyline fold" and has one caller — a null check. Only the depth-tested GL discs (`skyGhosts`, `skyTarget`) hide behind a building. | audit B2 | OPEN — owner taste call: fold `profileSample()` into the HUD's up/down and the day-arc alpha; a "behind the skyline" badge on shower / eclipse rows. Gate: the sweep's `dnipro-fpv-zoom-sweep` sheet with a body behind the skyline. **FIXED 2026-09-07d (owner: per the recommendation; MEASUREMENTS §23.3).** HUD rows carry a `BEHIND SKYLINE` badge and the edge chips a `fh-chip--behind` state from `store/plan.sun/moon/target` (`marker.up` stays geometric — GOTO's rise scan; the badge needs `skylineKnown` and a real obstruction `skylineAltDeg > 0`); the scrubber's sun/moon CURVES dash where the skyline hides the body on both shells (`AltSample.azDeg`, `curvePathsByState`); `✕ SKYLINE` on meteor rows, `· BEHIND SKYLINE` on eclipse rows (`EclipseRow.peakAzDeg`, both shells, parity-fenced) and NEXT SESSIONS rows (`useSkylineAt`); the day arcs and the target trail multiply `aFade` by `DAYARC.skylineBehindAlpha` 0.35 behind the skyline at rebuild, re-folded when the profile changes (the 2026-07 read-THROUGH ruling superseded, not deleted — the path still reads; `__globe.dayArcsFold()`). Verified: `verify-shots/t111/03-street-sun-behind-hud.jpeg` (+ the `/m` twin `04-`): sun 102/145 and moon 137/145 arc vertices folded at the street eye. |
| T112 | 2026-09-07 | **Six consumers read `profileBins` raw**, skipping `skylineBinsFor`'s honesty gate (coverage ≥ 0.5 + the 60 m eye guard) that the three radars honour (audit B3): `TimeScrubber.tsx:229`, `MobileTimeDock.tsx:163`, `FindPanel.tsx:214,259`, `FindSheet.tsx:136`, `PlanSheet.tsx:153,346`, `FrameCard.tsx:109`. After T109's carry policy a published profile is always complete and ≤ 60 m from the anchor, so the residual is the coverage gate alone. | audit B3 · `lib/geo/horizonProfile.ts:131` | OPEN — route the six through `skylineBinsFor` once the owner accepts that a < 50 %-coverage profile then reads "unknown" instead of "clear". Gate: `horizonProfile.test` extended per consumer. **FIXED 2026-09-07d (owner ruling: "best effort even below 50 %"; MEASUREMENTS §23.2).** Honesty moved from the profile to the BIN: `store/plan.profileKnown` mirrors the evidence flags beside the bins; `sampleBinsKnown` answers exactly where a bin has evidence (holding a known bin's value over ITS OWN span, never sagging toward the floor) and `null` where it has none; `skylineSamplerFor` (the one gate — real eye + guard distance, the A1-16 coverage clause GONE) feeds the three radars (`fractureRunsBySkyline` keeps `null` samples in the run = a plain band there) and `mirrorSampler` the six consumers (fenced by `test/components/skylineConsumers.test.ts`: every `profileBins` reader reads `profileKnown` and never calls `sampleBins`); `frameFinder`/`sunEventFrame`/`traceStates` classify per sample ("unknown" only where no sample had evidence; a dotted `trace-unknown` path). `PlanBodyState.skylineKnown`. `PLAN.minCoverageForGaps` stays for BEST SPOT's own patch gate only. Seen live: a 0.49-coverage first build published and used, 1.00 after the re-sweep. |
| T113 | 2026-09-07 | **BEST SPOT (parked): the enriched-bbox mask prism is applied in the horizon profile and NOT in the DSM** (audit B4 — coarse OSM ancestor tiles the renderer fragment-clips can raise the SOLID layer inside Dnipro, `buildDsm` has no `rejectPlanes`), and **`localDsm.addFloatingSolid` is dead code** (B5 — zero callers; `dsm.floating` always empty, `bandCapacity` 0, `SRC_DECK` / "ON A BRIDGE" can never fire while `localDsm.ts:22-26` names the owner's hero deck). | audit B4/B5 · `lib/geo/bestSpotWorker.ts:760` · `lib/geo/localDsm.ts:657` | OPEN — rides the next BEST SPOT session: thread `bboxClipPrismEcef` into `flattenTin` for the OSM group; either wire the deck path or delete it. Impact not browser-measured. |
| T114 | 2026-09-07 | **`heightMemo.invalidateRegionRad` drops nothing when a terrain tile carries no `boundingVolume.region`** (audit C1, pre-existing from lever 6): only the 60 s `maxAgeMs` backstop refreshes the memo then. The CWT path is region-bearing (`QuantizedMeshPlugin.js:359`); the ESRI placeholder path is unverified. | audit C1 · `lib/globe/heightMemo.ts:170` | **FIXED 2026-09-07f (half):** the early return is COUNTED — `HeightMemoStats.regionless`, DBG `terrain.memo.regionless` (warn above 0), `heightMemo.test` asserts it; the browser half (that every terrain-tile event on the CWT and ESRI paths reads 0 there) is a read of that row on the next descent, not a test.  **CLOSED 2026-09-07j — the browser half READ 0** on all 14 catalogue poses of sweep `post-2026-09-07j` (Dnipro AND Everest, 24–162 region invalidations each, `terrain.memo.regionless` 0 everywhere). The "ESRI placeholder path" is the IMAGERY overlay's RC5 sentinel fallback, not a terrain-tile source: the terrain tileset is the CWT quantized mesh at every pose, always region-bearing, so no region-less terrain source exists in the stack. The counter stays as the watch (warn > 0). |
| T115 | 2026-09-07 | **`ensureLocated`'s TREE loop is the last unbudgeted locate on the load path** (MEASUREMENTS §25.5): on the Pixel's descent the one-shot fired 58× with 0 FEATURES (every cell was located before its parts registered, so phase 4 located the features resumably) and its 7.5 ms max was the tree-instance loop — an `ecefToGeodetic` per instance, atomic, in `sampleTrees`' frame. | `scene/enrichedBuildings.ts` `ensureLocated` · `enrichedLoad().locateMaxMs` | OPEN — S: a per-cell tree cursor with the phase-4 deadline idiom, or locate the trees inside the load unit (they are phase 1 today).  **FIXED 2026-09-07j — `locateTrees`: chunks of 256 through the pure `lib/globe/treeLocate.ts` (bit-identical to `Vector3.applyMatrix4` + `ecefToGeodetic`, `treeLocate.test.ts`), the first chunk of a frame always running, further chunks under the later of the reseat deadline and `ENRICHED.treeLocateBudgetMs` 0.5 ms; `ensureLocated` keeps the features; every `t.latDeg` reader sits below the call in a loop that skips an unlocated set (`fences.test` T115). Desktop descent: worst call 0.5 ms, 39,971 instances in 10.9 ms total, tree-seating throughput identical to HEAD (MEASUREMENTS §26.3). DBG `buildings.treeLocateMaxMs` / `treeLocatePending`. **The Pixel read is owed** (`probe-cpu-profile --leg descent --device` + `probe-load-phase2 --device`: `treeLocateMaxMs` < the frame budget). **CLOSED 2026-09-08 — READ ON THE PIXEL:** `probe-load-phase2 --device` 89 calls · 177 chunks · 34,578 instances · 21 ms · **worst call 0.6 ms** (the atomic loop's 7.5), `treeLocatePending` 5 at the leg's end (drains at rest), identity 0/0 (MEASUREMENTS §27.3). |
| T116 | 2026-09-07 | **FIXED 2026-09-07h — the enriched TREE seats "landed" every frame forever (a float32 round-trip), so the tileset's seat epoch never went quiet and BEST SPOT's streaming re-solve could never fire on `/m`.** `t.appliedM` is a `Float32Array`; the float64 target `seatM − cell.seatM` is not representable, `seatLand` returned the exact target, the array stored its float32 neighbour, `next !== applied` was true again next frame by ~1e-7 m — one tree set per cell uploading its whole instance matrix to the GPU every frame at a SETTLED pose, `applyIdle` never true, `seatEpoch` +60/s (a T77 C-1 regression since 2026-09-06k; the desktop's first BEST SPOT solve usually landed after its tiles so nothing was visible there). Fix: `seatLandF32` (`lib/globe/enrichedMask.ts`) rounds the landed value to float32; "off target" is `|target − next| ≥ snap`. `enrichedMask.test.ts` reproduces the loop (300/300 writes) and pins the fix (≤ 70). The `seatSettle().enriched.applyReasons` ledger (per-reason counters) is the seam that found it. | `lib/globe/enrichedMask.ts` `seatLandF32` · `scene/enrichedBuildings.ts` tree apply · `__globe.seatSettle().enriched.applyReasons` | FIXED 2026-09-07h — browser: `/m` seat epoch flat, the disc re-solves (`probe-bestspot-mobile-seat.mjs`); desktop sweep draw-count gate 13/13, `verify-bestspot` 96/101 by design. Owner may want the FPV frame-time re-read on the Pixel: one fewer 3.8 MB instance upload per frame. |
| T117 | 2026-09-07 | **AR LOOK-AROUND — the real-iPhone tier.** Built and verified end-to-end on the desktop's phone twin (`verify-ar-look.mjs` 39/39: every rung driven by synthetic samples through the real listeners; the CDP virtual absolute-orientation sensor produced no page events on the headless build). UNVERIFIED on a device, in this order: (1) `webkitCompassHeading` when the phone is held UPRIGHT — the design gates the compass fusion on the top edge's ground heading (`FPV.arCompassMinTopHoriz` 0.35 ≈ tilt ≤ 70°) and lets the gyro carry above it; a real iPhone decides whether the gate can open toward 0.2 or must close; (2) the permission sheet under Appium (the farm leg `--legs ar` tries `acceptAlert`); (3) `webkitCompassAccuracy`'s real range vs the 25° ceiling; (4) gyro drift over 5–10 min still (the EMA re-absorbs it only when the phone dips); (5) the Pixel over adb: `deviceorientationabsolute` fires (rung 1) and `requestPermission` resolves without a prompt (Chrome 151+). The owner's own iPhone through the tunnel is the sure path for (1)–(4). **Farm read 2026-09-07h (`devicefarm-mobile-h-2026-09-07T19-01-41.json`): (2) is VERIFIED-NEGATIVE for Appium's `element.click()` — WebKit's `requestPermission()` REJECTED ("requires a user gesture to prompt"; the chip said TAP AGAIN, the right copy) and without the grant iOS delivered ZERO `deviceorientation` events, so nothing else could be read on the farm. Next farm attempt: a NATIVE tap (`mobile: tap` at the chip's native coordinates through XCUITest) may count as a gesture; else the owner's phone.** | `scene/arLook.ts` · `lib/sensors/*` · `mobile/FpvControls.tsx` `ArLookToggle` · `scripts/verify-ar-look.mjs` · `tools/devicefarm/ios-baseline.mjs --legs ar` | **Pixel 6 Pro VERIFIED by the owner 2026-09-07i ("looks and works well" — rung 1 + the heatmap). iPhone: the owner's real-world feedback is PENDING (arrives later; an open ear, not a blocker — act on it when it comes).** |
| T118 | 2026-09-07 | **BEST SPOT on `/m` — the first solve is a REFUSAL by construction.** On `/m` the disc arms before the building tilesets (detached in 2D mode until `bestSpotArmed()` re-attaches them) have streamed, so the first job returns `no-built-geometry` (the honest "NO BUILDING GEOMETRY REACHED THIS DISC" line) and the field only lands after the streaming debounce (90 quiet frames) + a second job: 11 s to the finest rung on the phone twin vs ~1.5 s of real solve. Owner call: (a) hold the FIRST post on `/m` until the OSM tileset reports its first resident meshes (a `bestSpotFeed` ctx term, `buildingsLoad().meshes > 0`), or (b) keep the honest refusal and shorten the quiet window for the first heal. | `scene/bestSpotFeed.ts` `postSolve` · `StylizedTiles.ts` `bestSpotArmed` · `verify-bestspot-mobile.mjs` "finest N ms" | **FIXED 2026-09-08 — owner ruling (a), generalised to BOTH shells: THE READINESS HOLD.** A DUE solve (the first post, a disc move, a streaming rebuild) waits while `StylizedTiles.sceneStreamPending()` reads > 0 — per attached tileset: no root yet (the frame after `/m` re-attaches them) + the library's `isLoading` latch + `queued + downloading + parsing`, plus the two two-phase `load-model` queues (`loadPending()`, a new integer read on both handles) — a THUNK the feed evaluates only while a solve is due (the first cut read it every frame and cost the twin ~3.5 ms/frame); ceiling `BESTSPOT.holdMaxMs` 20 s, then the solve posts with what is resident. The tier keys stay put while held (the branch re-enters), a rebuild inside a hold is carried (`pendingRebuild`), disarming clears it; `held` is an engine-written store field; DEV `bestSpot().hold` + DBG `planning.bs.held / heldFrames / streamPending`. **The chip (owner: "the simplest loading indicators on both shells"):** `controls/bestSpotCopy.bestSpotProgress` — `LOADING THE SCENE…` (held) · `COMPUTING…` (`solving` / `tilesPending` / `refining`) · `✓ DONE` (nothing running and a first ink landed; never before first ink — the twin read a false DONE for one poll) · silent while off — rendered by `BestSpotPanel` (`.bsp-progress`) and `BestSpotSheet` (`.m-bsp-progress`), busy states spin a ◌. Measured (MEASUREMENTS §27.4): twin HEAD first post = the refusal at 0.6 s, finest 14.2 s, 2 jobs → tree: held ~8–12 s, first ink REAL (1 + 4), finest 9.2–13.9 s, **1 job**; the Pixel: held 3.0 s, first ink 3.9 s real, finest 8.1 s, 67/67. Unit: `bestSpotFeed.test` T118 block (6) · `bestSpotProgress.test` (4). |
| T119 | 2026-09-08 | **The desktop DEBUG window "somehow loses its drag handle", and its resize handle sat at the TOP instead of the bottom-right corner (owner report 2026-09-08).** Two causes. (1) THE CASCADE: both grips carry `.tip` (the tooltip anchor) and tips.css says `.tip { position: relative }` at the SAME specificity as the grips' `position: absolute`, so emission order decided it — `wix dev` injects drag-grip.css last (the harness screenshots were always clean), a BUILD emits tips.css last on every route (the dist manifest: `.tip{position:relative}` after `.drag-grip{position:absolute}` for `/`, `/m`, `/guide`), so in a build both grips went IN-FLOW in the column-flex window: ◢ took its static slot at the top-left and ⠿ (`bottom: calc(100% + 3px)` on a relative box) shifted a whole panel-height above the viewport. App-wide (every DragGrip host), most visible on the tall centred DBG window. (2) THE CLAMPS: the drag floor let the panel's top reach 4 px while the 14 px tab floats 3 px ABOVE the top edge (the tab at −13…1 px — unhoverable, its double-click reset unreachable, the bad offset remembered for the session), and a centre-anchored resize lifted the top edge by half the growth with no bound. | `styles/drag-grip.css` · `ui/DragGrip.tsx` · `styles/tips.css:10` | **FIXED 2026-09-08:** `.drag-grip.tip, .resize-grip.tip { position: absolute }` — a compound selector (0,2,0) that outranks `.tip` whichever order the chunks land (the rebuilt dist carries it); `GRIP_CLEAR_PX` 22 — `clampDragOffset` keeps that much viewport above the panel's top, `clampResize` stops a centre-anchored growth where the top would cross it (a top-anchored PLAN/FIND window is merely slack there). Both pure, `test/components/dragGrip.test.ts` (5). Browser (dev, CDP-driven): dragged 900 px up the panel stops at top 22 with the tab at 6–20 px; the ◢ sits at the window's bottom-right (right/bottom within 4 px) and grows the width; a drag follows 1:1; `verify-debughud` 17/17. Note: at the clear line the corner refuses to grow the HEIGHT (growth would lift the top) — drag the window down first. |
| T120 | 2026-09-08 | **The two-finger TWIST never existed on the globe (owner report 2026-09-08b, the Pixel: "not responsive, or zooms instead of rotating, and rotates COUNTER to the fingers").** `3d-tiles-renderer` 0.4.28's `PointerTracker` measures no inter-pointer angle; `EnvironmentControls` classifies a two-finger touch ONCE (first move past 2 px·DPR — 7 px on the Pixel) as ZOOM (separation) or ROTATE (a parallel drag: the MIDPOINT translated) and latches it for the gesture. A rigid twist moves neither → WAITING, nothing happens; a human twist drifts → ZOOM (5× by `CONTROLS.zoomSpeed`) or ROTATE, whose azimuth is `−(midpoint drift·2π/H)` — the orbit convention, inverted against the fingers. In `/m` 2D the north lock then wound back whatever leaked (`touchRotate` needs state 2). The MapWindow chart had a correct twist since batch #4 (absolute inter-pointer angle, 1:1). | `lib/globe/twistTracker.ts` · `StylizedTiles.ts` `stepTouchTwist` (pre-`controls.update`) + `stepMobile2dLocks` · `tuning.ts` `CONTROLS.twistArmDeg` 4 / `twistGain` 1 · `test/lib/globe/twistTracker.test.ts` · `scripts/verify-mobile-batch-2026-09-08.mjs` §5a | **FIXED 2026-09-08b:** a repo-owned tracker (touch only) accumulates the UNWRAPPED inter-finger angle since the pair formed; past 4° it is ARMED and the orchestrator applies the angle 1:1 about the library's own pivot (the midpoint's ground hit) with the WORLD-FOLLOWS-FINGERS sign (`x = −Δ`; a clockwise twist → the heading decreases), on a library-ROTATE frame cancelling its midpoint-drift azimuth exactly (read pre-update); an armed twist ending zeroes `rotationInertia`; the 2D lock treats a live twist as a rotation (free heading). Pinch and twist compose (zoom stays the library's); never in FPV / a flight. Twin: 30° → Δ −30.0° in 2D and −30.1° in 3D, 40° with 36 px of drift → −40.0°, a pinch still zooms and does not turn, FPV untouched (109/109). **The Pixel read is OWED** (the phone dropped off adb mid-session) — the sign is derived and twin-verified; the owner's thumb is the device tier. The library's parallel-drag rotation (orbit sign) is left as it was. |
| T121 | 2026-09-08 | **BEST SPOT's lift had NO debounce and the disarm leaked.** Every `setLiftM` was a new T1 key on the next frame → `postSolve` → the WHOLE TIN re-flattened on the main thread (ground + OSM + enriched + user models, fresh typed-array copies, transferred) — 60/s under a slider finger. Disarming cancelled in flight only behind `livePack !== null \|\| lastMirrorSig !== ""` (a job posted and disarmed before its first rung ran to completion), and a 1 m REFINE cancelled mid-flight left the closure's `refining` latched TRUE forever (`onRefined` was the only clearer and the cancel drops that post) — the desktop REFINE button a permanent no-op after one such disarm. The worker's `resident` (TIN copies, parsed tiles, per-rung DSM/land grid/terms/hulls) and its never-evicting `tileCache` survived any disarm for the session. | `scene/bestSpotFeed.ts` (the lift branch, the `!armed` branch) · `tuning.ts` `BESTSPOT.liftDebounceMs` 220 / `workerIdleDisposeMs` 45 000 · DBG `planning.bs.liftDeferFrames / liftDebounced / workerDisposes` · `bestSpotFeed.test.ts` (+5) | **FIXED 2026-09-08b (owner ask 4):** a T1-ONLY change (lift, on a solved disc, no move/day/rebuild) posts after 220 ms of stillness — trailing edge, the keys wait, the last value wins; a move, a day step, a rebuild and the FIRST solve are never delayed (the 55 ms first ink stands). Disarm cancels unconditionally, un-latches `refining`, and after 45 s of disarm releases the worker (`client.dispose()` — `ensure()` re-spawns on the next post; a quick off→on stays warm). Twin: 30 lift values → 1 solve (unit); an encoder hold of 700 ms deferred 19 frames and cost ≤ 2 solves; five arm/disarm cycles ≤ 5 solves, 0 in flight (109/109). |
| T122 | 2026-09-08 | **The desktop attribution line clipped again at 1100 px in the map-open bar** — the version stamp (2026-09-08) added ~14 characters to the 265-character `nowrap` list and the A1-3 wrap threshold (60rem) no longer covered it (`verify-qaslice-cab` A1-3 FAIL at 1100, PASS at 820/700; the same on HEAD by the stash A/B — pre-existing to this session, introduced by the stamp). | `src/pages/index.astro` the `@media (max-width: …)` wrap rule | **FIXED 2026-09-08b:** the threshold is 72rem (1152 px) — the bar wraps and grows, the scrubber lift tracks it; `verify-qaslice-cab` ALL PASS. |
| T123 | 2026-09-08 | **iPhone 17 Pro still dies of a jetsam kill + page reload after 2–5 min (owner report 2026-09-08b)** — BEST SPOT and the compass work on the real phone, then the page reloads after several FPV entries/exits at different Dnipro spots and several heatmap recalculations. T83's lean caps (48/128/112 MB) and `releaseOnPageHide` moved the death from ~60 s to minutes; the residue is either a remaining growth (the solver worker's resident TIN + tiles per centre, the FPV working set per spot, the enriched cells, GPU-side memory lingering ~470 MB per page) or the practical ceiling of a 2 GB-per-process WebContent. Owner: best effort, NO overfitting that regresses the app for the iPhone's sake. | `tools/devicefarm/*` (`--legs bestspot,ar` + a new STRESS leg: N × FPV in/out at N spots + N heatmap arms, `performance.memory` + the syslog jetsam line per stage) · `QUALITY.leanMobile.*` · `bestSpotFeed` `workerIdleDisposeMs` (45 s — the heatmap's own residue is now bounded) | **CLASSIFIED 2026-09-09 (MEASUREMENTS §30.1–30.2): GROWTH by CACHE RESIDENCY → the 2 GB ceiling, no leak.** The farm stress leg (one /m page, FPV in/out at 4 Dnipro spots + heatmap arms) died in cycle 4 at ~250 s — the syslog: `WebContent [557] exceeded mem limit: ActiveHard 2048 MB (fatal) … 2097283KB`. Frame time flat, tile LRUs flat UNDER their lean caps, `renderer.info.memory.geometries` +150 per spot: the ENRICHED CELLS (mesh + edges + trees) of every spot stay alive because `/m` 2D DETACHES the tileset and a detached TilesRenderer never evicts (its cache is frozen under a 134 MB cap it never reaches); a forced eviction on the twin freed them (283 → 137 geometries) — disposable, not leaked. The 2 GB is spent outside the byte-accounted caps (decoded imagery textures behind the 97 MB ground LRU, parse-side copies, the JS heap +25 MB/spot). **LEVER (a) RULED (owner 2026-09-09b): release the detached enriched + buildings caches when /m drops to 2D, then re-run the farm stress leg (gate: 8 cycles alive).** (b) decoded-byte ground cap and (c) a /m-only enriched floor stay on the shelf unless (a) alone does not hold. Owner's rule stands: no overfitting that regresses the app. **FIXED 2026-09-10 to the owner's gate (MEASUREMENTS §31; DECISIONS 2026-09-10).** Lever (a) built (`lib/globe/detachedRelease.ts`, `MOBILE2D.releaseDetachedTiles` / `releaseDetachedGraceMs` 2500, both handles' `releaseCache()`): twin fpv-out geometries 404→774 → 94→198, caches 0 on every rest, worst drain 9.6 ms; farm run 1 (a only): the residency growth GONE but the page still met the 2 GB ceiling at the EIGHTH exit (~413 s vs cycle 4 / 250 s before). The memory-infra dump (`scripts/probe-memory-dump.mjs`) named the plateau — the browser's own image machinery (`cc/image_memory` at its 500 MB cap + 100–127 MB of composite-canvas backing stores) beside ~100 MB of app caches; **(b) VOID as framed** (the library already accounts DECODED texture bytes), **(c) moot at rest** after (a). **Lever (d)** built instead (`lib/globe/compositeCanvasRelease.ts`, `GROUND.releaseCompositeCanvas`): the overlay's per-tile composite `<canvas>` loses its backing store at the library's own dispose (`width = height = 0`) instead of at GC (~450 born per stress cycle; an IOSurface in the iOS footprint). **Farm run 2 (a + d): `alive 8 cycles, flat`** — page age 450 s, lru 0/97/0 and geometries 92→166 on every rest, dt p95 17 ms. Watch item: the twin's ~35 MB/cycle renderer creep (§31.6) if the owner's own phone still dies on longer sessions. |
| T124 | 2026-09-08 | **User meshes sometimes DISAPPEAR in iPhone mobile FPV (owner report 2026-09-08b)** — e.g. the model "altanka" from `#f=48.463651,35.039833,1.7,304.9,1.7,10.8&t=1788874369380`; a rendering / distance / residency glitch (the MS5 tri-budget residency, the FPV near plane at a 10.8° FOV, or the lean caps evicting the GLB). | `scene/userModels.ts` (residency, LOD) · `QUALITY.leanMobile` · the farm iPhone (`--poses` at the owner's pose) | **NOT REPRODUCED 2026-09-09 (MEASUREMENTS §30.3)** — farm iPhone 17 Pro at the exact pose on a fresh page: "altanka" `ready`, seated (91.1 m), 11,826 tris at 5/15/30/60/90 s, the dome visible in the 30 s / 90 s screenshots; resident at every FPV stage of the stress run too. The "sometimes" most likely rides the T123 death spiral (a page near the cap failing a GLB / texture load, or the post-kill reload) — re-check once a T123 lever is in. Leg: `ios-baseline.mjs --legs t124`. **RE-CHECKED 2026-09-10 with levers (a)+(d) in: `resident`** (altanka `ready`, seated 91.1 m, 11,826 tris at 5/15/30/60/90 s on a fresh farm page). Stays NOT REPRODUCED; with T123 fixed to the gate the death-spiral reading stands — closes with the owner's own phone. |
| T125 | 2026-09-08 | **REGRESSION (owner 2026-09-08b): edited building meshes on the map are no longer highlighted differently.** The MS3/U8 committed tint (a building carrying a height override / spatial edit is tinted so the edit is visible) stopped showing. Suspects, in order: T77 slice C-1's settled-cell skip (`ENRICHED.reseatIdleSkip` — a tint written inside `applyFeatureSeats` never lands on a cell that is idle before the edit commits), the two-phase `load-model` handler (the tint set in phase 1 before the fill exists), the `positionsF32` de-interleave (the colour attribute untouched). | `scene/buildingMaterial.ts` (the fill shader) · `scene/enrichedBuildings.ts` (`setOverrideTint`, `featureState.tintByte`) · `tuning.ts` `ENRICHED.overrideTintGlow` · `test/components/globe/buildingMaterial.test.ts` · `scripts/verify-bldg-override.mjs` §8 | **FIXED 2026-09-08c:** the CAUSE was not any of the suspected paths — the byte attribute (`_ftw_override`) and its writer were correct on the GPU at all hours (verified: `tintByte` 255 at 22:30). The tint was ONE albedo pull at `<color_fragment>` (`diffuseColor.rgb = mix(…, accent, K)`), a DIFFUSE term — and the city's night identity is a dark mass (no night emissive, R3), so 24 % of ~0 is ~0 and an edited building read like its neighbours after dark. The fill shader now reads the K ladder TWICE through one `ftwOverrideK()`: the albedo pull by day AND a per-channel FLOOR in linear light after `<opaque_fragment>` (before the aerial haze — after tone mapping it would be wrong in the direct-to-backbuffer pass) at `accent × K × ENRICHED.overrideTintGlow` (0.22 — 0.5 read as a neon slab at the dusk/night exposure on the twin). By day a lit face sits above the floor (byte-identical, sweep draw-count 14/14); at night the dark mass lifts to a faint teal. `featureState` gained `tintByte` (the GPU byte, not the `f.ov` cache — the old harness read only the cache and could not see a broken upload). Both shells (one shader). Twin: night edited rgb [3,61,54] vs neighbour [3,21,13], Δ 41. |
| T126 | 2026-09-08 | **Mesh edits: UNDO + "drop this session's edits" (owner 2026-09-08b, both shells).** (1) An UNDO button that undoes the LAST edit action (height, scale, move, rotate, a reset). (2) Beside SYNC, a button that drops just the CURRENT SESSION's edits — for the selected mesh, and a global one for every mesh on the map — leaving the persisted (synced) state as it was; the existing revert-to-original actions stay. | `store/bldgEdit*` / `store/userModels*` (an action journal per session) · `panels/BuildingEditChip.tsx` · the mobile edit chips · `lib/models/*` | **FIXED 2026-09-09** (MESH_SUITE_PLAN §16 = the design + as-built): NEW `lib/edit/editJournal.ts` (one entry per COMMIT, 1..N full-state steps, per-target UNDO with the unchanged-since guard, the baseline re-based onto every non-dirty state, the DROP as one undoable entry) + `store/editJournal.ts` (ONE instance for buildings AND models, the mirror, the one-shots); `commitBldgTransform` / `commitPlacement` journal; `restoreBldgRow` / `restorePlacement` are the un-journaled restore paths; both chips: `↶ UNDO` · `DROP SESSION` · menus + `DROP ALL SESSION EDITS`; the pill (`MESH EDITS`) shows for dirty rows or session edits, never while a model is armed; Ctrl/Cmd+Z. Verified: vitest (journal 15, chips 23, store 14) · `verify-meshedit` T126 leg (chip UNDO restores the row VERBATIM incl. `t`, DROP, Ctrl+Z, unwind ×3) · `verify-usermodels` leg 4b · `verify-mobile-batch` §7 (the buttons TAPPED on the twin; the Pixel read in this session). Owner taste call: the /m chip-foot buttons are 19 px tall (as RESET ALL was) — a 44 px cell rule would re-shape the foot. |
| T127 | 2026-09-08 | **Mobile map: a quick touch anywhere else CLEARS the set FPV point (owner 2026-09-08b).** With a temp pin on the map, a stray tap clears it — wrong. The explicit ✕ CLEAR PIN button is the clear; a plain touch / a drag of the map must leave the pin alone; a double-tap sets a new place (works today — do not break). | `lib/globe/emptyMapClick.ts` (NEW, the pure rule) · `StylizedTiles.ts` `onPointerUp` (the empty-map branch) · `test/lib/globe/emptyMapClick.test.ts` · `scripts/verify-mobile-batch-2026-09-08.mjs` §6 | **FIXED 2026-09-08c:** the ONLY clear on the tap path was `onPointerUp`'s empty-map branch (a ≤ `clickDragPx` release that hit no pin / model / marker), shared by both shells. Extracted the rule to `emptyMapClickAction({ isMobileShell, hasTempPin, viewingSavedPin })` → `"clear-pin" | "keep-pin" | "deselect" | "none"`: on `/m` a set pin returns `keep-pin` (still shadowing the deselect, as the desktop clear did), desktop unchanged. Double-tap-sets is untouched (native `dblclick` → `dropTempPinAt` lands after both pointerups); ✕ CLEAR PIN (`SceneActions.tsx`) is the one clear. Twin + PIXEL (the Pixel, real glass): a long press drops the pin, a stray tap on open map keeps it, a map drag keeps it, a second long press moves it, ✕ clears; a real double-tap on glass sets it (INFO). |
| T128 | 2026-09-08 | **Mesh edits: single-axis SCALE on an imported user mesh scales the whole model uniformly (owner 2026-09-08b, both shells)** — buildings scale each axis independently as intended; the user-mesh path collapses the three scale components to one. | `lib/models/modelPlacement.ts` (`ModelTransform {sx,sy,sz}`, `clampModelEdit` per axis, `uniformScaleFrom` deleted, `formatModelScale`/`scaledSizeM3`) · `scene/userModels.ts` (`body.scale.set`, three eases) · `StylizedTiles.ts` (the model gizmo instance) · `store/userModels.ts` · `lib/wix/modelRecords.ts` (+ `scaleX`/`scaleZ` columns) · `panels/ModelEditChip.tsx` · `MyModelsTab.tsx` · `scripts/provision-collections.mjs` (columns provisioned live) · `scripts/verify-usermodels.mjs` leg 5 | **FIXED 2026-09-08c** (owner overruled the MS5 uniform-scale design): `ModelTransform.scale: number` → `sx/sy/sz` (the building's shape); `clampModelEdit` rails each axis on its own per-edit band (`clampEditK(start.sx, raw.sx)` …); the scene writes `body.scale.set(sx,sy,sz)` and eases three lerps; the DB keeps `scale` as the HEIGHT (sy) factor and gains `scaleX`/`scaleZ` (legacy uniform rows read (k,k,k); PATCH keeps a `scale` uniform alias); the chip/label show the per-axis triple. 19 files, 115 focused tests + a shader/wire pin. Twin (real gizmo drag, 21 legs): the X box → sx 7.35, sy/sz 1, rig body scale [7.35,1,1], row "22.0 × 3.00 × 5.00 m (7.35 × 1.00 × 1.00)". Both shells (shared engine). |
| T129 | 2026-09-08 | **Retire the library's two-finger parallel-DRAG rotation on the 2D map (owner ruling 2026-09-08b).** With the twist (T120) as the one rotation gesture, a two-finger sideways drag should PAN the 2D map like every other map app instead of rotating it with the orbit sign (fingers right ⇒ map counter-clockwise). 3D keeps its two-finger tilt/orbit. | `StylizedTiles.ts` `stepMobile2dLocks` (`touchRotate` — the library's ROTATE state on 2D) · `EnvironmentControls` ROTATE (azimuth from the midpoint drift) · `verify-uxbatch4.mjs` ("two-finger drag rotated the map" — to be rewritten as "pans") · `tuning.ts` MOBILE2D | **FIXED 2026-09-09**: `MOBILE2D.twoFingerPan` (true; kill switch) — `stepTouchTwist` cancels the library's azimuth AND altitude for the 2D touch ROTATE pre-update and applies the midpoint delta as a DRAG on the pivot's ground plane (the library's own `_updatePosition` math, incremental); `rotationInertia` zeroed; `touchPan2dLive` keeps the north lock armed. 3D / FPV untouched; the twist composes. `verify-uxbatch4` leg 8 rewritten (compass stays N + the focus moved west) PASS · `verify-mobile-batch` twin 127/127. Device tier = the owner's thumb (no two-finger injection on the Pixel). |
| T130 | 2026-09-08 | **The extensive device campaign (owner order 2026-09-08b): the Pixel + the AWS Device Farm iPhone 17 Pro, STRESS and FEATURE legs, minutes budget secondary to quality.** Feature legs: every mobile surface (the tab bar's live/long-press, the AR chip + bubble, the ◎ SAVE cell, the encoder, the twist in 2D/3D, FIND, BEST SPOT arm/disarm/recompute, FPV in/out at several Dnipro spots, user meshes at the T124 pose). Stress legs: the owner's jetsam sequence (T123) with memory + syslog per stage, thermal-aware. | `tools/devicefarm/*` · `scripts/verify-mobile-batch-2026-09-08.mjs --device` · the farm harness's `--legs` | **RUN 2026-09-09** — `ios-baseline.mjs --legs t124,stress` on the farm iPhone 17 Pro (17.9 + 11.2 device minutes; the tunnel + `wix dev --allowed-hosts`): T123 CLASSIFIED (growth by residency → the ceiling; §30.1–30.2), T124 NOT reproduced (§30.3); the Pixel `verify-mobile-batch --device` ×3: 112 PASS · 2 FAIL (device-tier variances in the AR "sensors dying" and the encoder timing lines; T126 §7 PASS on real glass). Still owed from the owner's "extensive": the feature legs on the FARM iPhone beyond bestspot/ar (the tab bar, SAVE, the encoder, FIND) and the T123 lever + its re-run. Two-finger gestures stay owner's-thumb on the Pixel. **2026-09-10: the T123 re-runs DONE** (three farm sessions: stress with (a) → 7 cycles + a kill; stress with (a)+(d) → alive 8, flat; t124 → resident). Still owed: the farm FEATURE legs (the tab bar's live/long-press, SAVE, the encoder, FIND, the twist through W3C actions). |
| T131 | 2026-09-10 | **The moon (and sun) drew IN FRONT of far terrain** — owner pose `f=35.591887,139.872382,208.9,255.2,0.8,6.4`: the moon setting behind Mt Fuji (107 km) painted over the silhouette. Root cause: the impostor is anchored at `SKY.impostorFarFrac 0.5 × far`, and below 2 550 m GlobeControls pins far at ~180 km, so the disc sat at 90 km — in FRONT of the mountain in the depth buffer; the analytic ellipsoid-horizon fade cannot see terrain. | `scene/sky.ts` (`IMPOSTOR_VERTEX_GLSL` + the moon vertex shader) · `tuning.ts` `SKY.impostorFarFrac` doc · `test/components/globe/fences.test.ts` §2026-09-10b | **FIXED 2026-09-10b**: both vertex shaders pin depth to the far plane (`gl_Position.z = gl_Position.w`, three's skybox idiom) — the anchor keeps its position/scale roles, any rendered terrain/building wins at every altitude. House-Chrome shot: the disc now cut by Fuji's ridge (`verify-shots/interlude-fuji-moon-fix2-045s-crop.png`). Both shells, zero cost. |
| T132 | 2026-09-10 | **The horizon WHITE BAND** (owner: "a very bright white bloom/halo/band on the horizon … a hard cut to white light", regular + ULTRA; Fuji at 100 km a blown white silhouette). Root cause: the sky dome in camera-anchored mode is an ADDITIVE sphere at `ATMOSPHERE.domeFarFrac 0.45 × far` with the depth test on — every terrain pixel BEYOND 81 km (any camera ≤ 2 550 m) had the whole horizon-sky colour added on top of its own aerial haze, crossing the bloom threshold (MEASUREMENTS §17.5 had read it as "the 205-luma sky rows over far terrain"). Second half: the far plane is fitted to the horizon distance, so ridges beyond it are CLIPPED with a hard edge against the sky. | `scene/atmosphere.ts` (`uDome`, the vertex pin) · `scene/glsl.ts` `ftwAerial` (`uFtwFarM`, the far fog) · `scene/imageryGround.ts` (`uFtwFarM` per frame) · `tuning.ts` `ULTRA.farFogStartFrac/EndFrac` · fences §2026-09-10b | **FIXED 2026-09-10b** (both halves): the dome's depth is pinned to the far plane in sky-dome mode only (adds light where NOTHING was drawn; the orbital shell untouched), and the ground's aerial term dissolves the last 20 % before the far plane fully into its in-scatter colour so the clip lands on sky-coloured pixels. Tele pose: sky luma 200, far terrain 181, no blown pixels with bloom on or off (`interlude-horizon-tele-fix-018s`, `everest-wide-final2`). Both shells. |
| T133 | 2026-09-10 | **Moonlight washed out and milky** (owner pose `f=35.793588,136.740582,102.9,211.1,-13.7,80.0&t=1790256707424`: full-moon terrain an overexposed grey-white mass, no relief). Root causes (all in the ground grade, terrain is UNLIT): the 3D photographic de-grade (`ULTRA.photo3dK` 0.6 — gain 0.84, desat 0.21, neutral cast) was live ALL NIGHT; under the LOOK the night floor was `0.4 × skyLevel(0.03)` = 0.012, so the one albedo-scaled night term was gone; the moon FILL (`moonFillK` 0.7, up·moon, albedo- and normal-blind) sat at ~0.13 linear — brighter than any albedo term — under an exposure of 1.46. | `scene/imageryGround.ts` (the hoisted `night`, the photo gate, `nightFloorSkyMin`, the normal-aware fill, `moonSheenK`) · `tuning.ts` GROUND `nightFloorSkyMin 0.15 · moonFillK 0.05 · moonFillNormalK 0.6 · moonSheenK 0.45 · ambientNightK 0.012`, `SHADOWS.moonGroundOpacity 0.75` · `test/components/globe/moonlightTerrain.test.ts` (the JS twin: albedo-carried, aspect-carried, dim) · `duskShadeRatio.test.ts` (the floor twin) | **FIXED 2026-09-10b**: the 3D de-grade goes out with the sun (`photo3d × (1 − night)`; the /m chart keeps its photo look), the floor keeps a texture-carrying base (0.4 × 0.15), the fill is small and leans 0.6 onto the surface normal, the sheen carries relief. Full-moon targets (twin): a moon-facing rock ≈ 30 % of its daytime self, facing/away 1.8×, snow/rock 2×, a new-moon forest a silhouette. House-Chrome shot: `interlude-moonlight-fix3-028s.jpeg`. Both shells. Taste levers named in tuning. |
| T134 | 2026-09-10 | **Terrain refinement stalls — 10 s … 1 min+ with no visible progress, "until I moved"; look-back re-blur** (owner, desktop regular + ULTRA, worst on long lenses at distant terrain). FOUR mechanisms measured on the house Chrome (`verify-shots/interlude-*-stream.json`, `probe-stream/-stall/-who/-reset/-cpu.mjs` in the session scratchpad): (1) `ImageOverlayPlugin` PRELOADS every tile's composite imagery on `tile-download-start` at FIFO priority `-performance.now()` — below every terrain download — while the five parse slots are held THROUGH the composite and pick tiles by screen error: the parse queue sat at 374/5 and composites flat for 38 s while 1,042 image GETs drained at 27/s (a warm cache hides it — hence "random"); (2) a tile parsed onto a FULL cache is discarded without a `needs-update`, and nothing re-traverses while the camera is still — the ULTRA working set at the Everest zoom filled the 452 MB cap exactly (usedSet 420 = itemSet 420); (3) on a cold imagery cache 5 slots × a network-bound composite = 6 tiles/s (75/s warm), worse at Everest where the placeholder heal chain adds sequential GETs per image; (4) a VIRTUAL SPLIT RUNAWAY: the plugin clips leaf tiles into virtual children on the main thread and twice in five runs a wave arrived whose cost doubled per call (rAF gaps 0.7 → 95 s; a CPU profile 50 s of `getClippedData` in 59 s) — a child with more triangles than its parent. | NEW `lib/globe/overlayFetchPriority.ts` · NEW `lib/globe/virtualSplitGuard.ts` · `lib/globe/quality.ts` `groundQueueCapsFor` · `scene/imageryGround.ts` (installs, the cache-full kick, the reveal hold cap) · `tuning.ts` `GROUND.overlayFetchPriority · fullCacheKickMs 1500 · virtualSplitGuard/MaxDepth 6/MaxMs 80/GrowthMinTri 512 · revealMaxHoldMs 3000`, `LOADING.groundDesktopCaps {32, 12}`, `QUALITY.ultraDesktop.groundLruBytesMB 1200` · DBG `terrain.stream.*` + `terrain.split.*` · tests `overlayFetchPriority.test.ts`, `virtualSplitGuard.test.ts`, `quality.test.ts` | **FIXED 2026-09-10b** (four levers + a guard): (1) every imagery GET carries a LIVE priority that follows its tile — parse-slot tiles first, downloaded tiles next, preloads last, FIFO (the traversal order) within a band — an error-ordered first cut sat coarse on a cold cache and the sweep caught it (both shells; order only); (2) while the cache is full and the queues idle the ground re-traverses every 1.5 s, and ULTRA's ground cap is 1200 MB (≈ 900 after the mip re-billing); (3) desktop `high` ground runs 32 download / 12 parse jobs (phones keep the tier rule); (4) the split guard refuses a child with more triangles than its parent (above 512), anything under a slow ancestor, or deeper than 6 levels — 48 refusals at the Everest zoom, worst split 15 ms, no gap over 0.35 s; Dnipro FPV keeps composite z19 (an earlier floorless guard cost z19 → z17, caught by A/B). Also (5) the reveal's readiness gate now lifts 3 s after the first rendered tile (a continuous stream held the layer at 0.85 — a 15 % screen-door veil). Measured after: Everest zoom converges in ~10 s warm, tiles land continuously cold; the T77 desktop hitch table and the phones are NOT re-measured this session (the queue caps are desktop-only; everything else is order/guards). **Owner ruling 2026-09-10f — SETTLED:** ULTRA **1400 MB** (`ultraDesktop.groundLruBytesMB`), the REGULAR desktop **600 MB** (`QUALITY.desktopGroundLruBytesMB` through NEW `quality.groundLruCapForDesktop` — `high` off a coarse pointer with the chip off, the ground only; the `high` tier table stays 400), phones untouched (the `/m` pose reads 112/84 as before). Browser-read: regular `…/600 (min 450)`, ULTRA `…/1054 (min 791)`. |
| T135 | 2026-09-10 | **Observations parked from the interlude, not fixed:** (a) a cold-cache FPV boot in high terrain can seat the eye at the ellipsoid (312 m) and never re-seat — the frustum sees no tile from underground so `heightAt` never samples (`interlude-everest-zoom-fix-ultra-030s.jpeg`, `Network.clearBrowserCache` before boot); (b) a wide translucent white diagonal band crossed the Everest zoom frame once at 74 s in a PRE-fix run (`interlude-everest-zoom-ultra-074s.jpeg`) — not reproduced after the fixes; candidates: an aim cone / day arc at a bad depth, a tile skirt edge-on; (c) the bright crest patches on the far Fuji-side ridge (luma 225 vs sky 200) are the imagery's own albedo through the haze, not bloom. | the screenshots named · `scene/aimCones.ts` · `scene/dayArcs.ts` · the FPV seat in `stepFpvPose` | OPEN — re-check (b) on the next Everest sweep; (a) needs a "no tile under the eye for N s → widen the seat probe" rule. **— re-verified 2026-09-11 (audit #4, live): (a) NOT REPRODUCED again** — cold cache (`Network.clearBrowserCache` → reload), Everest FPV, house :9333: t+60 s `camera.altM` 8601 m with `eyeAboveGroundM` 312.4 (≈8289 m ground) — seated on terrain, not the ellipsoid (shots `verify-shots/audit4/t135-cold-*.jpeg`, DBG `t135-cold-dbg.json`). Stays OPEN-parked: no re-seat rule exists in `stepFpvPose` (code-read, audit H5); (b) still not reproduced (clean p4 everest shots). |
| T136 | 2026-09-11 | **The ONE library-bump fence audit #4 (A1+A2, Track I) calls for:** `package.json` pins `"3d-tiles-renderer"` to EXACT `0.4.28` (today `^0.4.28` — any lockfile-regenerating install can bump inside the range silently against nine source-verified patch seams), plus a boot smoke that reads every `installed` flag (`overlayFetchPriority`, `virtualSplitGuard`, `esriPlaceholder`, `compositeCanvasRelease`) and `console.warn`s on any `false` — today ALL drift degrades are silent in production (zero `console.*` in the three lib files; the DBG HUD surfaces only two flags, opt-in). Includes the T33-class machine check for the `_onTileVisibilityChange` guarded swap and a plugin-priority chain pin (I2-2). | audits/audit-full-2026-09-11.md A1/A2/I2-1/I2-2 · `package.json:35` · `lib/globe/{overlayFetchPriority,virtualSplitGuard,esriPlaceholder}.ts` | OPEN — sized S; the version-pin test pattern exists (`quality.test.ts:396` pins queue caps the same way). |
| T137 | 2026-09-11 | **The horizon-bloom golden gate is stale against the T96 LOOK** (audit #4 C1): `skyBudget.test.ts`'s `horizonSky()` twin models only the rotationally-symmetric dome (zenith + haze); since T96 (owner ruling 2026-09-06i) the directional LOOK ships ON the base rig (`uFtwDirK` toward `domeDirK 0.9` + the RC24 `uFtwUltraHaze` tint), none of it in the twin — recomputed shipped worst case 0.8964 vs `BLOOM.threshold` 0.9 (a 0.4 % margin the gate cannot see). Extend the twin with the directional terms + haze; re-pin the margin. | audits/audit-full-2026-09-11.md C1 · `test/components/globe/skyBudget.test.ts:8-50` vs `scene/atmosphere.ts:275-296` · `tuning.ts:957-965,1768` | OPEN — sized S; a golden gate weakened by staleness is MAJOR-class while it lasts. |
| T138 | 2026-09-11 | **The LIVE-SITE GRADUATION batch (audit #4 G1/G2/B5 — owner decisions, some then code):** every accepted-POC risk ratified while prod was dark rode into commercial live 2026-09-10 un-re-ratified: (a) C6's binding "moderation gates publication" is unimplemented — any signed-in member publishes instantly (save store defaults `isPublic: true`; `/api/moderate` parked with Phase 7); (b) T17's Esri/CARTO/OpenFreeMap ToS re-check trigger FIRED (commercial release) and was never recorded; (c) T26's unbounded `/api/upload-url` re-open trigger FIRED the same day. Each needs an explicit owner ruling: accept-as-is (dated), mitigate (a moderation queue / an upload cap), or park the commercial surface. | audits/audit-full-2026-09-11.md G1/G2, B5 · `src/pages/api/photos.ts:117-126` · `src/store/save.ts:82` · `tuning.ts:2200` · ARCHITECTURE.md:138 | OPEN — owner batch; the docs/process halves (T17/T26 rows) update the same day the rulings land. **DATED EDIT 2026-09-11 (the final verify pass): the original item (d) "zero analytics" is REFUTED and discharged** — the analytics live on the Wix headless EDGE (live fetch: every served page carries site-bi, site-analytics, tag-manager-client + `wixAnalytics.trackEvent('PageView')`; checkout lands in the dashboard natively); custom funnel events / error capture remain a dashboard-side Tag-Manager wish, not a defect (audit report's deleted-candidates note). |
| T139 | 2026-09-11 | **`previewUrl` is the one untrusted string on every public pin** (audit #4 G3): `pinRecords.ts:89` validates type+length only (≤2000 chars, ANY content) and `publicPinRecord` copies it verbatim into the world-readable row; every visitor's `PinHoverCard` renders it as `<img src>` and the globe fetches it as a texture — a hostile member points every visitor's browser at an arbitrary URL (tracking pixel / origin probe; `javascript:` is inert in img-src). Fix: scheme+host allowlist at BOTH the write (`parseSavePinBody`) and the render/fetch edge, or store only the Wix media file id and derive the URL server-side. | audits/audit-full-2026-09-11.md G3 · `src/lib/wix/pinRecords.ts:89,126,167` · `panels/PinHoverCard.tsx:24` · `scene/PhotoFrustum.ts:176` | OPEN — sized S–M; MAJOR while un-ruled on a live site with open membership. |
| T140 | 2026-09-11 | **Guide currency batch (audit #4 D3/F1/F2 + harness):** the guide teaches the RETIRED two-finger-drag-rotation (`guideContent.ts:1652` — T129 made it a PAN on 2026-09-09), says "BEST SPOT is desktop only" (`:1321` — false since the /m ◎ SPOT tab, 2026-09-07h), has ZERO coverage for T126's UNDO/DROP-SESSION pills and the AR chip, and `verify-guide.mjs`'s pinned counts went stale (11 chapters/14 goals asserted vs 12/86-topics/16-goals shipped — 3 red checks on clean HEAD, no known-red banner). Re-pin the harness, fix the four content drifts, add the two missing topics. | audits/audit-full-2026-09-11.md D3/F1/F2/L1 · `src/lib/guide/guideContent.ts` · `scripts/verify-guide.mjs:211,357` | OPEN — sized M; the delegated-affordance rule (docs.md item 13) makes the retired-gesture topic load-bearing. |
| T141 | 2026-09-11 | **Docs/memory drift batch (audit #4 D1/D2/D4–D8):** `mem:core` regrew to 46,772 B = 3.9× its 12 KB cap (five days after the hygiene session compacted it to 12,259); DECISIONS §Recent is AT the ~135 KB round-7 trigger (135,178 B measured); `globe-tuning.md` documents none of the ~19 families shipped since 2026-08-27 (the T40 pattern again); `contracts.md` un-re-diffed since 2026-09-06 (scaleX/scaleZ columns, the edit journal, the 20-seam list vs ~31 live); IMPLEMENTATION_PLAN's T77 row frozen at 2026-09-06; README test counts 1,902/130 vs 3,098/205; the e-session count conflict (mem:core 3,093/205 vs DECISIONS 3,094/206); NSP keeps T50 in PARKED while saying it has a live host. One hygiene session, the 2026-09-06g shape. | audits/audit-full-2026-09-11.md D-track · `.serena/memories/core.md` · `.claude/claude-docs/**` | OPEN — sized M; the compaction (round 7) rides it by construction. |
| T142 | 2026-09-11 | **Interlude tails (audit #4 H5-2/J1/J2/H5-1/C2):** the T131 far-plane pin covers sun+moon ONLY — `skyGhosts.ts:61-70` (depthTest TRUE, its own comment demands terrain clips it) and `skyTarget.ts` still anchor mid-band → the same drew-in-front-of-far-terrain class T131 fixed for the discs; the T132 fence pins `uDome` but not `uFtwFarM`; the buildings' night look was never re-judged against the T133 ground retune (J2, needs-eyes); two shipped doc surfaces still describe T134's replaced first cut (`imageryGround.ts:475-476`, `tuning.ts:3021+`); the T133 comments say 0.2 where the shipped floor is 0.15. | audits/audit-full-2026-09-11.md H5-2/J1/J2/H5-1/C2 · `scene/skyGhosts.ts` · `scene/skyTarget.ts` · `test/components/globe/fences.test.ts:1266-1287` | OPEN — sized M; the ghosts/target half is the visible half (verify with the Fuji-moon pose + a ghost visible). |
| T143 | 2026-09-11 | **Platform hardening batch (audit #4 B1/B2/B3/B4/B7/B6):** the ship hook's push-failure path is log-only (no SHIP_ATTENTION write — the exact anomaly class the file exists for); `release.sh`'s POST canary is single-shot against its own sharded-edge advice; the quota is count-then-insert (parallel POSTs overshoot; no transactions on Wix Data — compensate or accept, dated); the bake-bbox coupling has no machine fence (`regions.test.ts` never reads `scripts/bake/cities/*.json`); `.gitignore` misses `.env.development`/`.env.production` (Astro loads both; none exist today); the provision header still says 10-pin quota. | audits/audit-full-2026-09-11.md B-track · `.claude/hooks/session-end-ship.sh` · `scripts/release.sh:82` · `src/pages/api/photos.ts:89` · `.gitignore:16-18` | OPEN — sized S per item; B1 first (it guards every future ship). |
| T144 | 2026-09-11 | **Mobile UX / a11y batch (audit #4 F3/F4/F5/F6 + L3):** every /m sheet is `role='dialog'` with no focus move/restore and no Escape (the desktop Guide has both — extract `useSheetInputFocus` to the sheets); NO sign-out exists anywhere on /m (a phone member must desktop-chip out); data-panel fetches (`/api/market`, `/api/photos`, `/api/places`, `/api/listings`) carry no timeout/abort — LOADING… forever on a stalled connection; the mid-placement Explore click silently no-ops; the upload REVIEW copy says "Three fields are missing" while six rows show MISSING (and the word array `["","","Two","Three"]` reads `undefined` at ≥4). | audits/audit-full-2026-09-11.md F-track + L3 · `mobile/Sheet.tsx:48-75` · `panels/Marketplace.tsx:36-47` · `panels/UploadFlow.tsx:273` | OPEN — sized M; gate = the mobile-batch twin extended (a sheet-focus leg), still 100 %. |
