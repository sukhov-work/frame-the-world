# Audit — full #2 (whole-project expansion-readiness) — 2026-08-18 — baseline: audit-full-2026-08-13.md + DECISIONS 2026-08-13

Owner order 2026-08-18j: verify the project is prepared — documentally, organizationally,
architecturally, code-quality-wise — for further expansion, before the UPLIFT ladder resumes.
Engine: `/frame` type=Audit, tier=Deep, READ-ONLY. Tracks A (code/engine) · B (platform) ·
C (tests/math) · D (docs/memory) ran as parallel finder agents per `references/review-agent.md`;
Track E (mechanical) + NEW Track F (harness/process, charter dimension 4) ran on the main agent.
Verification pass: 29 track candidates + 5 harness findings → **34 confirmed, 0 deleted, 1
reclassified** (B-4 → status check on audit-1 B7's partial fix). FP ratchet 0% — no checklist
tightening triggered. Step-2 re-mine applied dated BEFORE tracks launched: code.md items 20–22
(mirror-never-seats · raw-focusHit pan · 0.4.28 internals) + tests.md item 9 (CDP 9222/evaluate
discipline).

## Verdict

The worst findings are **A1 — the U4 aim-cone terrain seat is the one `heightAt` consumer
without the `[0,9000]` clamp** (the exact invariant class audit #1 caught in Pins; first finite
sample SNAPS, so one coarse-LOD negative seats the circle underground) and **B1 — `checkOrigin:
false` has disabled Astro's CSRF origin check on every cookie-authed write route since the
Phase-1 scaffold with zero documented sanction** (exploitability UNVERIFIED — hinges on Wix
session-cookie SameSite semantics; the defect that is certain is the undocumented ruling).
Beyond those two, the project is in strong expansion shape: all six hard constraints hold
structurally (C6 traced hop-by-hop clean again, incl. the new U3–U5 surfaces), gates are green,
math is literature-locked, the orchestrator is holding (with a rising marginal cost and a named
extraction ladder), and the tail is 22 MINOR items that are overwhelmingly *doc/memory currency
debt from a 31-entry / 5-day ship burst* — the DECISIONS hot file (206 KB) now defeats its own
boot-read instruction, which turns compaction round 3 from hygiene into a harness fix.
**Scope-creep line (Zawinski guard): none detected** — out-list terms appear only inside the
out-lists themselves (probe validated).

## Gates baseline (Track E — all green, classified before any code read)

- `npm test`: **989/989 green**, 86 files, 2.27 s (suite-speed guard holds; no test ≥ 1 s, slowest 270 ms).
- `npx astro check`: **0 errors / 0 warnings / 5 hints** = the NEXT_SESSION dated baseline (5; audit #1 had 6 — shrank, never grew). Hints live in bake dev-scripts (unused imports).
- `wix build`: **Complete, 27 routes**. `dist/client` = **30 MB** (22 MB textures / 7.4 MB `_astro` / 664 K data / 272 K guide) vs 33 MB baseline — the −3 MB is exactly audit-1 slice 7 (B10 enriched-sample move). Vite >500 kB chunk warning pre-existing (index 822 kB).
- `npm audit`: 29 total (1 critical / 19 high) · `--omit=dev` **9 (3 low / 6 high) — identical to baseline**. Track B classified every package: ALL dev/build-toolchain except astro's own runtime code — T24 re-verified (below). The critical (`tar`) is @wix/cli install toolchain, never ships.
- Dead-code probes (all four ran; knip 6.31.0 via registry, depcheck 1.4.7, ts-prune, jscpd 5.0.14 local): knip "31 unused files" = ALL standalone scripts/ entries (false positives); real signal = 7 unused exported types + unused dep `@wix/dashboard` (A4). depcheck "missing sharp" for `build-milkyway-2k.mjs` (dev script, rides B3's stamp slice). ts-prune clean after island/route triage. jscpd src/ **72 clones / 1.89%** (was 28 / 0.87%) — top pair MobileTimeDock↔TimeScrubber 236 lines (sanctioned two-shell twin per MOBILE_PLAN M1 "accepted two-shell cost"; not a finding, watch the trend). jscpd test/ **0 clones**.
- `public/` sweep: **0 unreferenced** (probe validated both directions; 12 new guide webp all warm-list-coupled by test).
- Charter seed-number corrections (D11): stores = **12 files / 11 zustand** (charter said 13; skyAim is a helper, not a store) · memories = **96 files (76 project/)** (charter said 100/75) · StylizedTiles = **3,833 lines / 49 step invocations** (charter said 41; the in-file comment says 38 and 36 — A2).

## Findings (verified; severity-first; tier = evidence tier)

| ID | Track | Severity | Conf | Anchor | Finding | Specific fix | Violated ruling | Tier |
|----|-------|----------|------|--------|---------|--------------|-----------------|------|
| A1 | A | **MAJOR** | 90% | `scene/aimCones.ts:293–298` (wired raw at `StylizedTiles.ts:294`) | The U4 terrain seat eases toward the RAW `terrainHeightAt` probe — the ONLY `heightAt` consumer repo-wide without `clampGroundM` (`lib/geo/terrain.ts:1–12` mandates it for EVERY consumer). First finite sample SNAPS the seat (comment :289–292), so one coarse-LOD negative seats the aim circle underground and the ease *retains* it. Visual magnitude browser-UNVERIFIED. | `import { clampGroundM } from "../../../lib/geo/terrain"`; wrap the probe at :293 (`probe === null ? null : clampGroundM(probe)`), + a regression test on the seat fn if extracted. | DECISIONS §Traps "Negative heightAt garbage" (S2); code.md item 4; audit-1 A1 precedent | local-tested |
| B1 | B | **MAJOR** | 55% | `astro.config.mjs:39` | `security: { checkOrigin: false }` disables Astro's built-in CSRF origin check on ALL cookie-authed state-changing routes (photos POST/PATCH/DELETE, places, listings, upload-url). In repo since the Phase-1 scaffold (1bdedba); **zero hits** for checkOrigin/CSRF in DECISIONS/conventions/memories (probe validated); @wix/astro's dist does not reference it (not forced by the integration). Exploitability hinges on Wix session-cookie SameSite on the deployed host — UNVERIFIED, unreachable this session. Impact ceiling: state-changing writes in a victim's account; no cross-origin read. | Trial `checkOrigin: true` in `wix dev` + one release canary pass (login/checkout callbacks + `#p=` round-trip); if the platform genuinely requires `false`, record the dated ruling in DECISIONS + wix-headless.md — the certain defect is the undocumented default. | platform.md item 12 (strict boundaries); CLAUDE.md Authority (undocumented decision) | UNVERIFIED (exploit) / local-tested (config fact) |
| A2 | A | MINOR | 95% | `StylizedTiles.ts:3672,3705` vs `:3707–3755` | The load-bearing ORDER-contract comment is internally stale ×3: "38 ordered step-closures" (:3672), "all 36 steps" (:3705), actual **49** invocations; the numbered roster omits 6 steps and mislabels positions from 5 on. | Rewrite the header to STRUCTURE only (producer→consumer bands + load-bearing constraints), or regenerate to the 49 actual calls; stop counting (the D6 lesson, half-applied). | globe-tuning.md "ORDER IS THE CONTRACT"; audit-1 D6 resolution | local-tested |
| A3 | A | MINOR | 90% | `store/find.ts:52–56` + `FindPanel.tsx:277`/`FindSheet.tsx:92,156` vs `architecture-and-patterns.md:103` | `_syncGhosts` is `_`-prefixed (= orchestrator-only per the convention) but panel-written BY DESIGN (FIND v2's sanctioned seam inversion: panel = producer, globe = reader). Convention and code contradict; code.md item 9's mechanical probe now false-positives on it. | Rename to un-prefixed `publishGhosts` OR add the panel-producer exception to architecture-and-patterns.md:103 + code.md item 9; one dated DECISIONS line either way. | architecture-and-patterns.md `_`-prefix rule | local-tested |
| A4 | A+E | MINOR | 95% | `azSector.ts:26` · `loadPriority.ts:138` · `guideContent.ts:19,30` · `inline.ts:9,14` · `store/camera.ts:38` · `package.json:23` | 7 exported types with zero external importers (all locally consumed — drop `export`, don't delete; spot-verified SkyMenuInfo) + `@wix/dashboard` referenced nowhere outside package.json (knip + depcheck + grep agree). | De-export the 7; for `@wix/dashboard` check the Wix CLI template isn't tooling-load-bearing before `npm uninstall` (audit-1 A4's hoisting lesson). | code.md item 16 | local-tested |
| A5 | A | MINOR | 85% | `StylizedTiles.ts:838–855, 2779–2797, 2926–2940` vs helper `:888–903` | dir→az/alt-against-ENU-basis math inlined 3× beside the extracted `dirToAzAltAtCamera` (camera-basis-only). | Add `dirAzAltDeg(dir, basis)` to `lib/geo/projection.ts` beside `enuBasis`; fold the 4 sites; pure-math test. | 2026-07-10 dedup precedent; code.md 16 | local-tested |
| A6 | A | MINOR | 90% | `conventions/globe-tuning.md:54–57` | The audit-1-D6-refreshed orchestrator note re-staled in 3 days: "~36 step-closures … 3263 lines as of 2026-08-15" vs 49/3,833 (the line count is dated = honest; the step count rode along undated). | Replace counts with the structural statement ("one sanctioned orchestrator; decomposition = audit-slice backlog only"). | The D6 resolution itself | local-tested |
| B2 | B | MINOR | 90% | `guide.astro:95,114,118,122` · tracked-backlog T24 | T24's re-check trigger FIRED (5 `.astro` files now, +593 lines incl. new guide.astro) and the recorded probe formula is incomplete: a `set:html` sink class now exists (SAFE today: static `guideContent` input, `escapeHtml` on every text run, link targets regex-locked `[a-z0-9-]+` — all verified) and the prod manifest registers `/_server-islands/[name]` (unreachable: zero island components). | Dated T24 state edit extending the formula with `set:html` (+ its static-content rationale) and the manifest-route note, so the next re-check greps for them. | tracked-backlog dated-state rules; Pesticide step 2 | local-tested |
| B3 | B | MINOR | 70% | `sky/constellations.ts:1–2` · `ephemeris/stars.ts:4–5` · `sky/openngc.ts:2,17` · `build-ne-labels.mjs` · `build-earth-landmask.mjs` | Audit-1 B4's bake-date stamps landed on the time-sensitive catalogs (comets/asteroids ✓; new showers.ts is the house-best pattern) but the static catalogs still name sources without dates. Ages-in-years mitigation. Rider: depcheck's "missing sharp" for `build-milkyway-2k.mjs`. | Extend the `// Baked: <ISO> from <snapshot>` stamp to the 4 remaining generators on next regen; devDep sharp or a header note for the milkyway script. | platform.md item 10 (Lehman) | local-tested |
| C1 | C | MINOR | 95% | `testing-standards.md:5,18,30` · `architecture-and-patterns.md:64` | Test conventions no longer describe reality: "tests mirror `src/lib/**`" + "the globe is not unit-testable" ×2, vs 86 files incl. `test/components/globe/` (14), `test/store` (6), `test/bake` (4), static fences. The house pure-twin pattern (azSector/loadPriority/hoverNames) is recorded nowhere durable. | Dated testing-standards edit: real dir map + codify the twin rule ("pure islands exported+tested or extracted to lib/; attach/GL structure = browser tier"); cross-link from globe-tuning.md. | Conventions-describe-reality; tests.md 2 | local-tested |
| C2 | C | MINOR | 85% | DECISIONS §Traps + probes in Track C coverage | Pesticide deliverable — testable-cheaply trap classes NOT encoded: (a) mirror-never-seats static fence (scene/* importing store/plan outside planFeed — clean today, probe validated); (b) `boundingSphere = null` THREE regression (Pins comment, no test); (c) heightAt CONSUMER fence (fn tested, consumers unfenced — would have caught A1 at write time); (d) `setClearColor` static fence (0 call sites today); (e) dayArcs material contract assert; (f) `[hidden]` CSS-pair lint (pinchHardening is the idiom to copy). | One session: ~4 cheap tests (a, d, b, f) + backlog rows for (c)/(e) + the two 18h/18i pure-fn extraction candidates. | tests.md 3; audit-mode step 2 | local-tested |
| C3 | C | MINOR | 90% | `scene/dayArcs.ts:85,93` · `scene/glsl.ts:9,15` | Pure-twin stragglers: `dayArcs.horizonFade/pointDirs` exported, ZERO tests (0 test files vs sibling sky.ts's 3 — probe validated); `glsl.glf/glf3` untested though every tuning value injected into every shader passes through it (`glf(1e-7)` exponent formatting is the bug class a 5-line test pins). | `test/components/globe/dayArcs.test.ts` (~15 lines: band edges, orthonormality) + glf edge cases. | U4/U5 twin precedent; tests.md 1 | local-tested |
| D1 | D | MINOR | 95% | `ARCHITECTURE.md:74–79, 109–154` | §7 "as built 2026-08-15" + §4 engine roster miss ALL 9 files added since (git-listed): tilePriority/loadPriority (U5) · aimCones/azSector (U4) · MapWindow/slippy/map-window.css (U3) · MeteorsCard/showers (P7) · guide.astro; mobile ¶ lacks U1 2D-first. Grep 0 hits for any of them (probe validated). | Refresh §4/§7 dated 2026-08-18: 9 files + showers/azSector in the roster + one U1 sentence + guide.astro. | session_workflow doc-sync; the doc's "as built" contract | local-tested |
| D2 | D | MINOR | 90% | DECISIONS.md byte map (206,252 B / 676 lines) | **Compaction round 3 DUE**: 71% (≈147 KB, 32 entries) is verbatim text of CLOSED eras (astro-engine 08-02→10 ≈28.5 KB · 08-11 trio ≈6.1 KB · mobile/QoL/FIND/guide 08-13→15e ≈112.7 KB with five 4–8 KB mega-lines). Round-2 byte-identical mechanism PROVEN by Track D's probe (162/163 moved lines verbatim). Uplift era stays hot (ladder parked mid-run). Interlocks with F1 — the density now defeats the boot read. | Round-3 slice: move lines 621–676 byte-identical → ARCHIVE "§Moved — round 3"; ~6 era digests mirroring mem:core's index rows. Expected: hot ≈65 KB / ~245 lines. | DECISIONS structure contract (round-2 precedent) | local-tested |
| D3 | D | MINOR | 95% | `README.md:163,192` | Two bare undated "886 tests / 80 files" copies (today 989/86); status ¶ ends at M0–M3, missing P7 + U1–U5 (2D-first mobile is user-visible). "0 errors/0 warnings" technically still true (hints are a third class). | Refresh both counts dated + one status clause. | docs.md item 4 (volatile-counts) | local-tested |
| D4 | D | MINOR | 90% | `naming-conventions.md:10–11,16–17,19` | Three stale samples: "the real six" stores (12 files/11 zustand) · EXIF field list contradicting the schema it says to "mirror exactly" (`focalMm/captureTime/forSale/price` vs `focalLengthMm/capturedAt/priceAmount`, contracts.md §4) · phantom `Listings` collection (falsified by audit #1, never propagated here). | Rewrite the three bullets against contracts.md §4 + `ls src/store`; `Listings` → `SavedPlaces`. | docs.md item 3 | local-tested |
| D5 | D | MINOR | 85% | `testing-standards.md:20,42` | (a) still instructs asserting the `beforeInsert` hook rejecting "insert #11" — mechanism superseded 2026-07-10, numbers 2026-07-17; (b) 7-item undated DEV-seam sublist duplicating contracts.md §3. | (a) → "elevated POST /api/photos returns 402 at quota (100/1000)"; (b) → "see contracts.md §3". Same slice as C1 (same file). | D8 supersession; volatile-counts rule | local-tested |
| D6 | D | MINOR | 90% | `contracts.md:36–42` vs `StylizedTiles.ts:1633,1674` · `quality.ts:151–153` | §3 inventories only the 15 top-level globals; the verify-recipe-consumed SUB-seams (`__globe.u2/u5/u5Mark/map2d/fpv`, `__quality.governor.emaMs/hitchCount`) are uninventoried — their only list is the gitignored NEXT_SESSION_PROMPT (Hyrum-consumed, undocumented). | Dated sub-seam table in §3 (name · owner file:line · consumer). | docs.md item 11 ("later audits diff it") | local-tested |
| D7 | D | MINOR | 85% | DECISIONS §Traps (last origin = Phase-5 era) | Conventions-promotion gap: the 4 U-era trap classes were folded into audit checklists today but are absent from the homes implementers read — mirror-never-seats belongs in architecture-and-patterns §mirror; raw-focusHit + 0.4.28-internals in globe-tuning §Traps; 9222-ownership in testing-standards §harness. The durable DECISIONS §Traps block gained NOTHING all era (fresh traps live only in gitignored NSP + verbatim entries). | Promotion slice: 1 sentence each into the 3 conventions + 4 dated lines into DECISIONS §Traps (GL ×2, Verification ×2). Include Track A's note: ghost impostors' absolute-ECEF far-shell pattern is safe THERE but becomes the S4 trap if copied to ground scale — one globe-tuning sentence. | Traps header "durable" contract; docs.md item 2 | local-tested |
| D8 | D | MINOR | 95% | `UPLIFT_PLAN.md:22` | Dangling memory pointer: `mem:project/wip-2026-08-17-uplift-plan` — actual file is `wip-2026-08-17-p7-meteors-uplift-plan` (verified). | Fix the one pointer. | docs.md item 10 | local-tested |
| D9 | D | MINOR | 90% | `.serena/memories/architecture/system-overview.md:30` · `core.md:178` | Stale ALWAYS-OFFERED memories, no supersession notes: system-overview asserts the phantom `Listings` collection + the 4-endpoint plan-era API list (real: 8 routes); core.md:178 sells sky-bodies-terrain (frozen 2026-07-10) as "current ground pipeline" (rebuilt 2026-08-18b/c). A fresh session reads falsified facts. | Correct or stamp "superseded by ARCHITECTURE §5–7 / contracts.md"; reword core:178 dated. House pattern = globe-rendering's in-place SUPERSEDED header. | memory item 6; the 08-11 gap precedent | local-tested |
| D10 | D | MINOR | 85% | tracked-backlog.md (0 rows since seeding) vs gitignored NSP:71–77 | U-era durable tails live ONLY in the gitignored prompt: aim-cones v2 tails (GL edge labels + skyline sub-bands) · U2 cold-load settle glide · P7 card copy · U5 first-guess knobs; T1's scope grew (U1–U5 device items) without a dated row edit. | T28+ rows (or dated T1 state edit absorbing the device items) pointing at UPLIFT_PLAN §5. + NEW row: the U5 lru/caps idiom extraction seam (Track A OF-2: `captureTileDefaults/applyTileTier` in scene/tilePriority.ts, triggered at 4th renderer or next touch of all three). | backlog header "lands here the same session" | local-tested |
| D12 | D | MINOR | 85% | `PLANNING_QOL_PLAN.md:3` · `GUIDE_PLAN.md` status | Era-close: PLANNING_QOL_PLAN is era-CLOSED ("ALL SHIPPED" 2026-08-15; 329 lines) → archive per the 2026-08-15 ASTRO_ENGINE precedent (2 live refs to repoint). GUIDE_PLAN nearly closed (G3 = owner-taste + T1) → fold G3 into a backlog row, archive. MOBILE (M4–M6), UPLIFT (parked), IMPLEMENTATION stay LIVE; no overlap conflicts found. | Archive moves + repoints in the docs slice. | archive precedent; docs.md item 10 | local-tested |
| F1 | F | MINOR | 95% | `hooks/activate-serena.sh:10` × DECISIONS 206 KB | Harness: the boot hook's "Read DECISIONS.md (recent state, top entries first)" is DEFEATED by the file's density — a naive Read truncates at ~152 lines (~25 K tokens), which covers ONLY ADRs+Traps, never Recent sessions (reproduced on this session's own boot). Compensated today by NSP + mem:core, but the instruction actively misleads. | Hook text: point at NSP + mem:core first, instruct paged/grepped DECISIONS reads; the real fix is D2's compaction (interlock). | Boot-recall-path integrity (session_workflow) | local-tested |
| F2 | F | MINOR | 90% | scripts/ (grep: 0 launch-recipe hits) × NSP:87–88, wip memories ×3, UPLIFT_PLAN | Harness: the browser-verify recipe (occlusion-flag launch + 9222-ownership check + tab-select + in-page probes) exists NOWHERE as a runnable artifact — prose scattered across DECISIONS traps, 3 wip memories, UPLIFT_PLAN, and the gitignored NSP; contracts.md §3 covers only seams. The stale-9222 trap cost ~20 min in U5. | Propose (build in a slice): `scripts/verify-chrome.mjs` (lsof-check who owns 9222 → error/kill on stale owner → launch with the 3 occlusion flags + profile dir → print CDP attach) + a 1-page `conventions/verify.md` (recipe + evaluate-post-load/tab-select/bringToFront traps) referenced from the /frame skill Phase 3. | tests.md 9 (new); charter dim 4 | local-tested |
| F3 | F | MINOR | 85% | ship log 15:32:57 WARN + branches at boot + SHIP_ATTENTION race | Harness: the ship hook's "checkout moved… leaving branches as-is" path leaks stale local ship branches (2 found from 08-13: one deleted this session after tree-identity proof; one M1-era fails strict tree AND patch proof though content is demonstrably landed — sits unpruned forever, unreported by SHIP_ATTENTION). Separately, SHIP_ATTENTION deletion at boot RACED the detached watcher's final write — the file reappeared mid-session with an already-resolved entry (observed twice today). | session-end-ship.sh: prune branches on tree-containment proof; list proof-failing ones ≥N days old in SHIP_ATTENTION with evidence; watcher appends atomically/only-if-unresolved. Boot text: re-check SHIP_ATTENTION once late in session. | session-end-autoship contract | local-tested |
| B4 | B | NIT | 60% | `api/photos.ts:73,240` | STATUS CHECK on audit-1 B7 (not a discovery): the fix threaded `knownLimit` on POST only; GET/DELETE still hardcode `limit: PIN_QUOTA_FREE` (and GET's `.limit(50)` caps `used`). Premium member sees 100. | If MyPins ever renders "N/limit": reuse POST's knownLimit or drop `limit` from GET/DELETE. | audit-1 B7 (partial-fix) | local-tested |
| C4 | C | NIT | 95% | `lazyContract.test.ts:9–10` | Gate doc-comment stale: "searchIndex.ts alone is boot-safe" — `hoverNames` is a second sanctioned boot-safe module (statically imported by scene/skyNames). Gate LOGIC unaffected. | One-line comment naming both. | Gate comments stay true | local-tested |
| C5 | C | NIT | 80% | `azSector.test.ts:155–158` | The one real-ephemeris vector (Dnipro solstice az 51°/309°) names no external source — every other vectored lib does. | One-line citation (NOAA/timeanddate). | tests.md 1 | local-tested |
| C6 | C | NIT | 95% | `checklists/tests.md:51–53` | Item 7's dated baseline stale (~2 s @704). Measured: 1.93–2.73 s @989, slowest single 270 ms. | Dated baseline edit (checklist edits are audit-sanctioned — apply in this session's checklist pass or the next). | Checklist self-maintenance | local-tested |
| C7 | C | NIT | 90% | `scene/streetNames.ts:20` | Comment cites `STREETS.minTextPx`, removed 18d (now `textPxTarget`). | Update the comment. | Doc/code drift | local-tested |
| F4 | F | NIT | 90% | `.claude/CLAUDE.md` (Build/Test header + intro ¶) | (a) "> Commands below become live **after Phase 1 scaffolds**" — Phase 1 shipped 2026-07-09; (b) the intro still sells "premium AI shot-analysis" while Phase 7 (AI) is OUT of all plans (owner standing ruling) — the boot surface contradicts the scope fence. | Drop the conditional; annotate the AI clause "(Phase 7 — parked indefinitely, owner ruling)". | Zawinski guard; boot-surface currency | local-tested |
| D11 | D | NIT | 80% | NSP:34–35 · DECISIONS:264–268 | Charter seed-inventory drift (13 stores → 12/11; 100 memories → 96/76) — corrected in this report's Gates block; feeds no other doc. | Corrected here; carry right numbers forward in NSP refresh. | Evidence discipline | local-tested |

## Verified clean (named probes)

- **C6 privacy (BLOCKER class): PASS — second consecutive clean hop-trace.** Only builder of public location fields = `publicPinRecord` → `reduceLocation` (cell-centre; `parseSavePinBody` accepts only lat/lon/precision — client-supplied "reduced" fields structurally impossible); `/api/market` projects `latReduced/lonReduced` only; hover card carries zero coordinates; PATCH re-reduces; bake exclusion masks untouched since baseline (git-proven). NEW surfaces clean: MapWindow/minimap/aimCones are client-local with ZERO network egress (fetch/sendBeacon/XHR grep rc=1, positive control on uploadMedia); SavedPlaces (exact FPV pose) is ADMIN-read + owner-filtered, never published.
- **C1 thin endpoints: PASS** — all 8 routes validate→auth→gate→SDK; per-call `elevate()`; every elevated insert sets `ownerMemberId`; backend churn since baseline = 3 files (places quota-drop + 2-line planUpgrade), no new endpoints/SDK surfaces.
- **Quota wall: PASS** — 100/1000; places quota-free is a dated owner ruling (2026-08-15c); dev-seed's DEV gate is compile-time eliminated (prod bundle = unconditional 404, structural proof from dist).
- **TUS/media, checkout/listing, R2/bake sync, secrets, auth mechanics, postel boundaries, schema source of truth: PASS** (Track B coverage rows 4–13; audit-1 B2 credential fix HOLDS — all verify scripts env-source; bboxes byte-equal; provision script covers every field incl. SavedPlaces.timeMs).
- **Engine invariants: PASS on 19 of 22 code.md items** (fails = A1 + the two dead-code/naming rows). Highlights: the three fresh re-mine items PASS on current code — mirror-never-seats (stepAimCones resolves LIVE per frame, citing the 18h bug), raw-focusHit (centerOnly pan correct with trap comment), 0.4.28 internals (comparator total over non-tile items + tested, `loadAncestors=false` always paired with the explicit comparator). Ghost impostors' absolute-ECEF far-shell placement REFUTED as a finding (angular error ≪ 1 px at shell distance) — sanctioned-divergence note rides D7.
- **Math integrity: PASS** — new libs all vectored: showers (IMO cal2026 + λ☉ anchor + Jenniskens profile), loadPriority (installed-0.4.28 source parity, branch-by-branch), npf (D850 16.3 s), sunEventFrame (documented derivation idiom); golden gates UNCHANGED since baseline (git-diffed; quality.test additions purely additive; high-tier INVARIANT stands); +N delta chain 704→989 arithmetically EXACT (+285); suite 0 clones.
- **Docs/ledger: PASS on append-only** (per-commit deleted-line probe: compaction r2 byte-identical 162/163, sentinel move dated) · plan checkboxes match reality · datelines match commits · scope guard clean · cross-references resolve (1 dangling mem pointer = D8) · contracts.md top-level rows exact (5/5 ftw: keys, 15/15 seams, 8/8 routes).
- **Audit purity: PASS** — working-tree diff = the two sanctioned checklist files + this report only.
- **T24 astro-XSS re-check: core claim HOLDS** — define:vars 0 · server:defer 0 · spread-in-.astro 0 (positive controls ran); the 3 `transition:` hits are CSS properties, refuted; new `set:html` class is safe today (B2 extends the row's formula).

## Pre-existing / out-of-scope

- npm-audit dev-toolchain trees (tar critical, wrangler/miniflare/undici/ws, sharp — deployed image service is noop, verified from dist manifest): pre-existing, not shipped; ride toolchain updates.
- Vite >500 kB chunk warning: pre-existing sanctioned lazy singletons.
- Browser/device/wix-cloud tiers unreachable: A1 visual magnitude · B1 cookie semantics · live /_image · T1/T5/T13/T18/T2/T3 — all stay on their registry rows.
- MobileTimeDock↔TimeScrubber 236-line clone: sanctioned two-shell cost (MOBILE_PLAN M1); trend-watch only.

## Backlog status changes (to apply in the fix slices — audit is read-only on the registry too)

- **T24** — dated formula extension (B2). **T1** — dated state edit absorbing the U1–U5 real-device items (D10). **T26/T27/T4/T17** — confirmed in recorded states. **T2/T3** — confirmed OPEN (no release since baseline). **T8** — seam alive, fold unbuilt. **T10** — wired, default-off. **T13** — WATCH unchanged.
- **Proposed NEW rows:** T28 U-era taste tails (edge labels + sub-bands v2 · U2 settle glide · P7 copy · U5 knobs — from gitignored NSP) · T29 U5 tile-tier idiom extraction seam (Track A OF-2, trigger = 4th renderer or next touch) · T30 audit-1 B7 remainder (GET/DELETE quota display, B4) · T31 trap→test tail (C2's (c)/(e) + 18h/18i pure-fn extractions) if not taken in the fences slice.

## Fitness scorecard (expansion-readiness dimensions → best proxy → value)

| Dimension | Proxy | Value |
|---|---|---|
| Code growth (charter 1) | orchestrator inventory + seams | HOLDING-MARGINAL: 3,833 ln / 49 steps (+570 ln, +~13 steps in 3 days); FPV cluster ≈950 ln = 25% and absorbed all U2–U5 churn; extraction ladder named (DEV-introspection ~200 ln + quality fan-out ~90 ln = LOW risk → zoom-bank MEDIUM → FocusFrame MEDIUM-HIGH → FPV controller HIGH, strangler = the attach-module idiom itself, one family per session, A/B via __globe.u2/u5 probes). U5 idiom dup = 33 glue lines ×3, restraint-now (T29). tuning.ts 48 groups, contract holds. Stores 11 — healthy, one naming drift (A3) |
| Docs currency (charter 2) | drift count + byte map | 22 findings, ZERO blockers: DECISIONS 206 KB (71% closed-era → r3 slice fully specified) · ARCH §7 9 files behind · 2 plans era-closed · conventions stale ×4 files · contracts §3 sub-seam gap. Ledger integrity PROVEN |
| Memory graph (charter 3) | recall-path measurement | HEALTHY SIZE (780 K total, mandatory boot read ≈42 KB), 3 stale always-offered memories (D9) + 1 dangling pointer (D8) + era-index gap (uplift row). Policy proposed: era-index row per compaction · core Status re-dated at each r-round · size caps core ≤12 K/wip ≤10 K/patterns ≤15 K · keep wip-* leaves (no pressure) · enforce in-place SUPERSEDED headers |
| Harness (charter 4) | gap inventory | 3 MINOR gaps (F1 boot-read defeated · F2 recipe not runnable · F3 branch-prune + race) + 1 NIT (F4) + lint-guard candidates (C2 fences; DEV-seam gate test; contracts-seam-count coupling test) |
| Geo-accuracy / privacy / perf (standing) | gates + probes | 989/989 vectored-green · C6 hop-trace PASS ×2 audits running · bundle 30 MB dated · real-device still unmeasured (T1) |

## Proposed convention / checklist amendments (Pesticide harvest)

1. APPLIED this session (pre-tracks, dated): code.md 20–22 + tests.md 9 (the 6-trap re-mine).
2. Ride the fix slices: architecture-and-patterns §mirror exception (A3) + mirror-never-seats sentence (D7) · globe-tuning §Traps additions (raw-focusHit, 0.4.28, far-shell-ECEF note) · testing-standards harness-trap (9222) + twin-rule codification (C1) · tests.md item 7 re-date (C6) · code.md item 9 probe exception for the FIND seam (A3).
3. Next authoring pass: the C2 static-fence family (mirror-seat grep, setClearColor, heightAt consumers, [hidden] pairs) — (c) would have caught A1 at write time.

## Fix-session slicing (ordered; severity-first; each with its gate)

| # | Size | Contents | Gate | Frozen-chrome? |
|---|------|----------|------|----------------|
| 1 | S | **A1** clampGroundM in aimCones (+ extract-seat-fn + regression test if cheap) · C3's dayArcs/glf tests ride along · C7 comment | vitest (+N) · astro check · one browser shot of the cone seat at street level | no (scene internal) |
| 2 | S | **B1** checkOrigin:true trial in `wix dev` (login, save, checkout-redirect, `#p=`) → if clean, flip + release-canary; either way a dated DECISIONS ruling + wix-headless note | wix dev pass · release canary (pairs T2/T3) · DECISIONS line | no |
| 3 | M | **D2 compaction round 3** (byte-identical move + 6 era digests) + **F1** hook-text repoint + **D3** README + **D1** ARCH §4/§7 + **D12** plan archives/repoints + **D8/D9** memory fixes + core.md uplift era-row + policy (a)/(b) | wc -c targets (~65 KB hot) · checksum move proof (r2 idiom) · grep repoints | no (docs) |
| 4 | S | Conventions batch: **D4** naming ×3 · **D5**+**C1** testing-standards rewrite · **D6** contracts §3 sub-seam table · **D7** trap promotions (3 conventions + 4 DECISIONS §Traps lines) · **A6** globe-tuning structural note · **A3** seam ruling (rename or exception, one DECISIONS line) · **F4** CLAUDE.md staleness | grep re-checks · astro check (if rename lands, vitest too) | no |
| 5 | S | Dead-code: **A4** 7 de-exports + @wix/dashboard removal (template-check first) · **A5** dirAzAltDeg helper + fold ×4 · **A2** ORDER header rewrite (structure-not-counts) | vitest · astro check · knip re-run | no |
| 6 | S | **C2** fences: mirror-seat grep fence · setClearColor fence · boundingSphere THREE test · [hidden] CSS lint · **C4/C5** comment+citation nits · backlog registry edits (T24/T1 + new rows T28–T31) | vitest (+~4) | no |
| 7 | M | **F2** scripts/verify-chrome.mjs + conventions/verify.md · **F3** ship-hook branch-prune + SHIP_ATTENTION append-only race guard · **B3** catalog stamps + sharp devDep | node --check · manual hook dry-run · lsof probe demo | no |
| 8 | — | OWNER: B1 ruling if trial inconclusive · plan-archive approval (D12) · T28/T29 acceptance · B4/T30 display call · T25/T1 real-device pass unchanged | — | — |

Slices 1–2 are the substance (one MAJOR each); 3–4 are the expansion-readiness core the owner ordered;
5–7 are cheap hygiene. The UPLIFT ladder resumes (U6 foveation) when the owner calls this phase done.
