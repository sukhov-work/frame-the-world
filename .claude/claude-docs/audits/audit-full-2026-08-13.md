# Audit — full (whole repo + docs) — 2026-08-13 — baseline: DECISIONS 2026-08-13 (planning-core restructure + M0)

First full-repo audit (owner-ordered hygiene gate before Phase 8a/M1). Engine: `/frame` type=Audit,
tier=Deep, READ-ONLY. Tracks A (code/engine) · B (Wix platform) · C (tests/math) · D (docs/memory) ran as
parallel finder agents per `references/review-agent.md`; every candidate finding re-verified by the main
agent against the cited code/doc (verification pass: 30 candidates → 29 confirmed, 0 deleted, 2 merged;
FP ratchet 0% — no checklist tightening required, but the pass itself surfaced one wrong checklist
number, fixed dated: tests.md K&S quarter 10.3%→9.1%).

## Verdict

The worst finding is **D1 — a working-tree-only corruption of the DECISIONS append-only ledger** (the
audit-machinery entry prepend ate the same-day "PLANNING LADDER PROMOTED TO CORE + M0" entry header;
recoverable BEFORE the next commit, permanent after). Beyond that the codebase is in strong shape for the
Phase-8a/M1 ladder: all six hard constraints hold structurally (C6 traced hop-by-hop clean), gates are
green at baseline, math is literature-locked, and the findings are one MAJOR engine-invariant break
(A1 — the single unclamped `heightAt` consumer) plus a tail of MINOR hygiene/doc-drift items.
**Scope-creep line (Zawinski guard):** none detected — nothing schedules the AI panel, Gaia-depth, GOTO,
tides/rainbow, Skyfire, or mobile upload/marketplace/pins; all Phase-8/M additions are owner-ruled in.

## Gates baseline (Track E, all pre-existing — no my-change/flaky/infra reds)

- `npm test` (vitest): **704/704 green** — 3.02 s cold; **1.54 s / 1.69 s warm** (suite-speed baseline ~2 s HOLDS; the 3.02 s is cold-cache noise).
- `npx astro check`: **0 errors / 0 warnings / 6 hints** = the dated baseline (6, 2026-08-13). Not growing.
- `wix build`: **Complete, 26 routes** (incl. `/m`). Vite chunk warning >500 kB (GlobeCanvas 604 kB, index 822 kB) — pre-existing.
- `npm audit`: 30 total / prod-only (`--omit=dev`) **9 (3 low, 6 high)**. The 1 critical (`tar` ≤7.5.20) is dev-only inside `@wix/cli` — never ships. Prod highs triaged by Track B: **astro@5.18.2 five XSS advisories, fixes only in 7.x (C4 pins Astro 5) — ZERO reachable sinks** (4 `.astro` files; no `define:vars`, no server islands, no element spread props, no `transition:*` directives; all dynamic UI is client:only React) → dated accepted risk, re-check on any `.astro` growth (new backlog row T24). js-yaml/nanoid/postcss/sharp/svgo: build/dev-time only, not in the deployed server runtime.
- Bundle: **`dist/client` = 33 MB (22 MB textures / 8 MB `_astro` JS / 3 MB enriched-sample)**. The "~24 MB" note in audit-mode.md is a stale 2026-07-14 figure (D3); the delta decomposes into dated additions (milky-way 8k 6.4 MB · FAQ webp · astro catalog bins · milkyway-2k 0.57 MB). Re-anchored dated baseline = 33 MB as of 2026-08-13.
- Dead-code probes (knip / depcheck / ts-prune / jscpd — all probed, all ran): knip's 16 "unused export" values are used-in-module (export-keyword-only, A5); phantom dep `@wix/redirects` (A4/B1); jscpd src/ 28 clones / **0.87 %** (A6), test/ **0 clones**.
- `public/` sweep: all assets referenced (enriched-sample glbs via `tileset.json`) — but the 3 MB sample is never fetched at runtime (B10).

## Findings (verified; severity-first; tier = evidence tier)

| ID | Track | Severity | Conf | Anchor | Finding | Specific fix | Violated ruling | Tier |
|----|-------|----------|------|--------|---------|--------------|-----------------|------|
| D1 | D | **BLOCKER** | 95% | DECISIONS.md:163 | The two same-day 2026-08-13 entries are FUSED into one line: the audit-machinery prepend destroyed the "PLANNING LADDER PROMOTED TO CORE… + MOBILE M0 SHIPPED" entry's `- **` header (line carries 4× "2026-08-13", one bullet, an orphaned `).**` after "…§audit-mode extension."). Working-tree-only (HEAD shows all three newest entries uncommitted) — recoverable now, permanent ledger corruption if committed. | Split line 163 after "…§audit-mode extension." and reconstruct the second header from NSP:13 + `mem:project/wip-2026-08-13-planning-core-restructure`, preserving every surviving character. Do this BEFORE any commit. | DECISIONS header contract (append-only, never edit/delete); audit-mode severity ladder | local-tested |
| A1 | A | **MAJOR** | 95% | Pins.ts:295-299 (wired raw at StylizedTiles.ts:536) | The ONLY `heightAt` consumer without the `[0,9000]` clamp: `groundHeight()` returns the raw sample when finite; a negative loading-phase/coarse-LOD elevation roots a public pin underground. Aggravator: `grounded[i]` latches the FIRST finite answer and `resnap()` skips grounded pins (:506-511) — garbage kept permanently. Falsification failed: `imageryGround.heightAt` (:349-358) returns raw `getPositionElevation`, no internal clamp. Visual/pick wrongness only (pins don't drive the camera). | In `groundHeight()`: clamp via `clampGroundM` AND treat an out-of-range raw sample as `real:false` so `resnap()` keeps refining it (latching a clamped 0 would freeze the wrong answer). + regression test. | DECISIONS §Traps "Negative heightAt garbage" (S2); lib/geo/terrain.ts:5-8; code.md item 4 | local-tested |
| A4/B1 | A+B | MINOR | 95% | lib/wix/planUpgrade.ts:46 · store/market.ts:103 · package.json | Phantom dependency: `@wix/redirects` dynamically imported at two load-bearing points (plan upgrade + BUY checkout redirect) but absent from package.json — resolves only as a hoisted transitive (1.0.119 via @wix/headless-ecom). A parent bump breaks checkout/upgrade at RUNTIME (dynamic imports — no build error). Interlock: acting on knip's "unused @wix/sdk" suggestion would unhoist it. | `npm install @wix/redirects@^1.0.119 --legacy-peer-deps` (dependencies); only then evaluate dropping `@wix/dashboard`. | wix-headless.md §5 dependency discipline | local-tested |
| B2 | B | MINOR | 85% | 5 scripts (verify-listing/places/plan-upgrade/phase69-member.mjs, seed-demo-pins.mjs) | Live-site member credential committed: `frame-p5-tester@example.com / FrameP5!test1`. Documented fixture (mem:patterns/members-pins) but no dated accepted-RISK ruling; checklist says no credentials in scripts. Low privilege (free member) keeps it off BLOCKER. | Move to `TEST_MEMBER_EMAIL/PASSWORD` in `.env.local` (same readFileSync idiom as WIX_CLIENT_ID); owner rotates the password after. | platform.md item 9 | local-tested |
| A6 | A | MINOR | 85% | lib/wix/pinRecords.ts:211-223 ↔ pages/api/market.ts:29-44 ↔ store/pins.ts:108-128 | The PublicPins-shape field mapper (~15 fields incl. lat/lon/precision) exists in THREE places; semantic drift already visible (market.ts precision fallback literal `"1km"` vs store's `DEFAULT_PRECISION_TIER`). Drift here is the Photos↔PublicPins-divergence BLOCKER class waiting. Lesser dups: ENU az/alt (comet.ts:518 ↔ targets.ts:547), TTL-cache pair (sbdb ↔ simbad), dayArcs↔skyTrail scaffolding, panel-CSS clones. | ONE record mapper in lib/wix consumed by all three sites; `topoAzAlt()` helper; TTL-cache helper. CSS clones lowest priority (Claude-Design artifacts). | 2026-07-10 dedup precedent; audit-mode MINOR | local-tested |
| B5 | B | MINOR | 80% | store/market.ts:108-112 | `buy()` proceeds when `listing.variantId` is null, omitting `options.variantId` — the exact configuration the repo verified yields a SILENTLY EMPTY checkout (2026-07-16 trap). Unreachable in current data (all live listings post-fix) → latent silent degrade, not live break. | Guard: null variantId → error phase "listing is missing its variant — relist", never create the checkout. | audit-mode silent-degrade class (downgraded: unreachable) | local-tested |
| B9 | B | MINOR | 60% | pages/api/upload-url.ts:10-43 | Any signed-in member can mint unlimited upload URLs of unlimited `sizeBytes` (no cap/rate limit/quota linkage) — media storage is the one unmetered write surface. OWNER JUDGMENT flag (auth required; no C-violation). | Cap sizeBytes (e.g. 200 MB original / 5 MB preview); optionally rate-limit mints. | — (closest: quota-bypass class, weak fit) | local-tested |
| B10 | B | MINOR | 85% | public/enriched-sample/** (~3 MB) | Ships in every build, never fetched at runtime (runtime URL = `PUBLIC_ENRICHED_TILES_URL`: R2 in prod, `bakes/` middleware in dev; the sample is referenced only by comments + its build script). | Move out of `public/` (e.g. git-ignored `bakes/`), update `build-sample-dnipro-tiles.mjs` serve-note; verify with a build. | Track E unused-assets check | local-tested |
| B3 | B | MINOR | 90% | conventions/wix-headless.md §4 | The release ritual (ping canary → `warm-prod-assets.mjs` → `verify-prod-globe.mjs` → sharded-edge-cache "reload until clean") is absent from the one doc that owns release steps (zero grep hits); it survives only in mem:core + session lines. | Append the dated post-release block to §4. | platform.md item 7 | local-tested |
| B4 | B | MINOR | 90% | lib/sky/comets.ts:1-6 · asteroids.ts:1-4 | Generated catalog headers name source + refresh path but NO bake date (MPC updates daily; elements age — repo's own drift measurement 2.1′@+3yr). TEMPEL2 shows the correct dated pattern. | Generators stamp `// Baked: <ISO> from <snapshot>`; headers regenerate on next bake. | platform.md item 10 (Lehman) | local-tested |
| B6 | B | MINOR | 90% | scripts/build-earth-landmask.mjs:83 | Committed bake script writes output to a hardcoded machine/session `/private/tmp/claude-501/...` scratchpad path — refresh path broken for any rerun. | Repo-relative output (the `build-milkyway-2k.mjs` URL idiom). | platform.md item 10 refresh-path integrity | local-tested |
| C1 | C | MINOR | 95% | test/lib/ephemeris/golden.test.ts:6-11 | golden.test's CURVE claims "the shipped numbers" but carries the pre-2026-07-13 curve (−0.139/0.122/0.276); tuning.GOLDEN was widened (−0.21/0.17/0.36); planner.test.ts:29 has the current mirror — the duplicated mirror already drifted once, and the shipped bell shape is never exercised there. | Update the mirror + add a parity assertion against tuning.GOLDEN (or import it — quality.test already imports tuning). | tests.md item 6; skyBudget keep-in-sync convention | local-tested |
| C3 | C | MINOR | 65% | DECISIONS L166 (2026-08-11 FPV fix) | Feature/fix line with no test delta and no stated no-test rationale (pure pose math, regression-testable if extracted). Mitigated: browser-VERIFIED + shot recorded. Process finding. | Next `stepFpvPose` touch: extract walk-anchor integration to a pure helper + regression test; fix lines always carry "+N" or an explicit no-test reason. | tests.md item 4 | local-tested |
| C2/D8 | C+D | MINOR | 80% | ASTRO_ENGINE_PLAN.md:166 | The Phase-A "point/ellipse treatments + auto-widened ring visually UNVERIFIED" tag is PARTIALLY stale and unregistered: the ring widening + tracked-DSO flows WERE browser-verified (M31 hit ≈2.5° 08-03; NGC7000 tracked across reload 08-10) but no session records eyeballing the point/ellipse impostor RENDER (no shot; 08-10 shots cover Saturn disc/ring, Orion figure, star tint). Absence-of-record with positive grep controls. | Dated annotation narrowing the tag to what remains (point/ellipse impostor visuals) + new backlog row T25; eyeball rides the next visible-window session. | tests.md item 5 + docs.md items 5/12 | local-tested |
| D2 | D | MINOR | 90% | DECISIONS.md:171 | "New work appends a dated line here." marker stranded between two 2026-08-03 entries (drifted at commit 70467e2; every entry since goes above it) — falsely marks the append point. Not an append-only violation (no dated line touched). | Move the marker directly under `## Recent sessions` with a dated note (a move is not an edit — compaction precedent). | DECISIONS structure note (:8-13) | local-tested |
| D3 | D | MINOR | 90% | skills/frame/references/audit-mode.md:22 | Track-E line's "~24 MB baseline" is the 2026-07-14 figure imported undated into a doc authored 2026-08-13 — this audit measured 33 MB, fully explained by dated additions. The anti-drift rule's own encoding violated it. | Re-anchor: "33 MB (2026-08-13: 22 textures / 8 _astro / 3 enriched-sample; was 24 MB 2026-07-14)". | docs.md item 4 | local-tested |
| D4 | D | MINOR | 95% | README.md:57,60 | README claims "10-pin quota", "free 10", and "app not yet installed" — quota re-ruled 100/1000 (2026-07-17) and Pricing Plans installed same day. Owner-facing doc contradicts the ledger ×3. | Update both rows (quota 100/1000; Pricing Plans installed, premium plan live). | docs.md item 4; DECISIONS:181/185 | local-tested |
| D5 | D | MINOR | 90% | conventions/wix-headless.md:89 | Convention heading teaches "Quota (10 free / unlimited paid)" — numbers superseded to 100/1000, never updated (the supersession note beside it covers only the mechanism). | Heading → "Quota (100 free / 1000 premium — owner 2026-07-17; was 10/unlimited)". | docs.md item 3 | local-tested |
| D6 | D | MINOR | 85% | conventions/globe-tuning.md:54-57 | Note says StylizedTiles ≈1600 lines with "getting back toward ~200 lines" as a tracked S7 goal — file is 2665 lines and B19 was completed 2026-07-11 via step-closures (structure, not line count). Both facts stale. | Dated note update: B19 done, current size 2665 (2026-08-13), decomposition debt lives in the backlog if raised. | docs.md item 3 | local-tested |
| D7 | D | MINOR | 85% | ARCHITECTURE.md:75-89 | §5 data model diverged from as-built: field names from the pre-Phase-5 inference (mediaFileIdOriginal/focal35/sensorWidthMm/forSale/captureTime-datetime), PublicPins missing gh4/gh6/precision/product fields, and a **Listings collection that does not exist** (listing fields live on Photos/PublicPins). Tagged [INFERRED] but misleading since Phase 5/6. C6 posture of actual fields clean. | Rewrite §5 from `scripts/provision-collections.mjs` (or point at it + contracts.md, keeping only the C6 invariant line). | docs.md item 2; ARCHITECTURE = execution source of truth | local-tested |
| D9 | D | MINOR | 80% | .serena/memories/core.md:343-352 | Graph-root §Source layout frozen at 2026-07-10 ("Sky, Pins TBD", "ephemeris + SDK clients TBD", "`src/backend/` TBD") — all shipped weeks ago; `components/mobile/` absent. Actively misleads Phase-0 reads. | Refresh §Source layout to as-built (mirror ARCHITECTURE §7), dated. | docs.md item 6; mem:memory_maintenance | local-tested |
| D10 | D | MINOR | 70% | .serena/memories/core.md (no 2026-08-11 FPV block) | The documented 08-11 core-gap backfill covered only the mobile-design sessions; the same-day FPV-walk-fix ruling still has no core Status block (lives only in mem:bugs/fpv-walk-orbit + MOBILE_PLAN §4.1). | One-line dated backfill block pointing at mem:bugs/fpv-walk-orbit. | docs.md item 6 (the 08-11 gap precedent) | local-tested |
| D11 | D | MINOR | 85% | tracked-backlog.md T4 · IMPLEMENTATION_PLAN.md:164 | Both still claim Pricing Plans uninstalled (`APP_NOT_INSTALLED`) — installed 2026-07-17 per DECISIONS:181 (premium plan id `5874dba8-…`). T4's real remainder: manual purchase loop + plan rename. | Dated T4 state edit; dated correction note on §6.9's owner-tail. | docs.md items 7/12 | local-tested |
| A2 | A | MINOR | 95% | scene/streetNames.ts:93 | `group.renderOrder = 3` on a THREE.Group — does not propagate (the trap); inert only because meshes set their own (:154). Invites the bug class back. | Delete line 93. | §Traps Group.renderOrder; code.md item 5 | local-tested |
| A3 | A | MINOR | 75% | GlobeCanvas.tsx:93 | Residual `renderer.setClearColor(tokens.bg)` ten lines above the S5 comment mandating scene.background — inert today (scene.background force-clears) but re-arms the navy-floor bug if scene.background is ever removed. | Delete or annotate as deliberately inert. | §Traps navy night-sky floor; code.md item 6 | local-tested |
| A5 | A | MINOR | 85% | 16 value exports + 27 type exports (knip list) | Export keywords on module-internal symbols (all used-in-module — verified sample: cometShortDesignation, skyAim consts, simbadTarget); widens the public surface, defeats dead-code tooling. | De-export per symbol; delete anything then truly orphaned. | code.md item 16 | local-tested |
| A7 | A | NIT | 95% | store/pins.ts:160 · StylizedTiles.ts:534 comment | The viewport-query throttle is correctly a throttle but the variable is `debounceTimer` and the orchestrator comment says "the store debounces" — contradicts the §Traps THROTTLE ruling in name only. | Rename to `throttleTimer`; fix both comments. | §Traps viewport THROTTLE (wording) | local-tested |
| B7 | B | NIT | 90% | api/photos.ts:72,127,237 | GET/POST/DELETE always report `quota.limit = PIN_QUOTA_FREE` (100) — premium members see "used 250 / limit 100". 402 body is tier-correct. | Thread the premium flag when known, or omit limit on happy path. | — display drift | local-tested |
| B8 | B | NIT | 95% | api/photos.ts:3-4 | Header comment "both are ADMIN-only" — PublicPins read is ANYONE (true for writes only). | Reword to "both are ADMIN-write". | — comment drift | local-tested |
| C4 | C | NIT | 85% | test/lib/sky/lazyContract.test.ts:43 | Gate-hardening hole (pre-existing, not weakened): offender regex requires `from` — a bare side-effect import would slip; `import type` would false-positive. | Extend regex: match `import "…mod"`, skip `import type`. | tests.md item 2 (hardening) | local-tested |
| C6 | C | NIT | 85% | test/lib/ephemeris/bodies.test.ts:54-59 | Dec-21 subsolar −23.44° (named in the checklist) exists only as a browser-tier claim; unit test pins June +23.43 only (mirror-symmetric — low risk). | Add one Dec-solstice assertion (3 lines). | tests.md item 1 | local-tested |
| D12 | D | NIT | 75% | README.md:43 · ASTRO_ENGINE_PLAN.md:28 | Two undated bare copies of the 9,096 star count (canonical dated homes exist). Low volatility (bin frozen 2026-07-10). | Date them, or rule catalog-bake counts non-volatile in contracts.md. | docs.md item 4 | local-tested |
| D13 | D | NIT | 80% | skills/frame/SKILL.md:19 · conventions/wix-headless.md:155 | Two machine-local absolute paths (`/Users/yevhens/.../ecom/...`) with no machine-locality note — silent dead pointers on any other box. | Append "(machine-local; unavailable off this box)". | docs.md item 10 | local-tested |

## Verified clean (named probes)

- **C6 privacy (BLOCKER class): PASS** — full hop trace: exact GPS only in Photos (ADMIN read) → `publicPinRecord` lat/lon exclusively from `reduceLocation` (cell-centre, 1 km default, exact opt-in) → `/api/market` reads `latReduced/lonReduced` only → client store → hover card shows NO coordinates → pin click seeds the camera with already-reduced coords (share hashes inherit reduction). All 4 PublicPins writers use the same builders; location edits re-reduce; bake exclusion masks intact; geolocation API unused repo-wide (positive controls run).
- **C1 thin endpoints: PASS** — all 8 routes validate→auth→gate→SDK; zero decode/projection server-side; per-call `elevate()`; every elevated insert sets `ownerMemberId`.
- **Quota wall: PASS** — 100/1000 per the 2026-07-17 ruling, two-tier check, dev-seed DEV-gated (sanctioned).
- **TUS media contract: PASS** — resumable+private originals, ≤1280 px public previews, upload failure degrades to warning with visibility.
- **Checkout/listing contract: PASS** — fixed Stores appId, `options.variantId` on buy, `createProductWithInventory` inStock untracked, teardown on unlist/delete/private, SITE_CURRENCY stamped (one latent edge → B5).
- **R2/bake sync: PASS** — both city bboxes byte-equal tuning↔bake configs; dev never hits R2; built bundle carries the R2 URL; Worker CORS/Range correct in source (live behavior UNVERIFIED — cloud tier).
- **Secrets: PASS except B2** — no committed env files (`git log --all --diff-filter=A -- "*.env*"` empty, probe validated); R2/ion/OAuth creds env-sourced.
- **Engine invariants: PASS** — client:only both mounts (C4); worker decode exact-pinned libraw 1.0.5, buffers transferred, worker terminated; float32 camera-anchoring (Pins/PhotoFrustum patterns intact); color-space rules (one inert residual → A3); ONE shared building material, chained onBeforeCompile documented; store seams (`_sync` zero hits in panels/mobile); per-frame mirrors cadence-gated + deadbanded; lazy contract green and walks new mobile dirs; contract strings tolerant (new `ftw:m-banner-dismissed` follows the family); degrade paths warn (silent catches are commented cache-optimization guards); `[hidden]` CSS pair present on the new banner; no new frame-loop work since baseline; InstancedMesh boundingSphere resets present; fixed panels outside filtered ancestors; FPV adjustCamera + world-space walk offset intact (2026-08-11 fix invariants hold).
- **Math integrity: PASS** — literature vectors: Horizons ±0.05° (query cited), Hale-Bopp ≤2′ / Encke ≤2′ / Ceres ≤30″ / Vesta ≤30″, K&S-1991 moonlight (≈9.1% quarter, derivation inline), textbook FOV, canonical geohash cells, NGP/GC anchors, GMST J2000; golden gates run green this session (lazyContract, skyBudget, quality-tier locks incl. the M0 coarsePointer pins); self-consistency tests honestly labeled; suite warm 1.54–1.69 s, no single test >1 s.
- **Docs/memory: PASS** — committed DECISIONS history append-only (all historical deletions sanctioned); doc-sync twins exist for every entry since 2026-08-10; memory graph has a wip twin per session; datelines match commit reality; cross-references resolve; backlog registry is the ONE list and NSP mirrors it; scope guard clean.
- **Astro XSS exposure: NONE today** — sinks absent from all 4 `.astro` files (probes named in Gates).

## Pre-existing / out-of-scope

- `tar` critical + js-yaml/nanoid/postcss/sharp/svgo highs: dev/build-time trees (npm ls paths verified) — pre-existing, not shipped; ride toolchain updates.
- Vite chunk-size warning (GlobeCanvas 604 kB / index 822 kB): pre-existing; the heavy chunks are the sanctioned lazy singletons (catalog 432 kB behind the lazy contract).
- Browser/device/wix-cloud tiers unreachable this session: T1 (real-device numbers), T5 (checkerboard), T13 (FPV glide WATCH), live R2 Worker headers, prod `/m` (T2), `#p=` smoke (T3), T18 (R2 orphan keys — no bucket access). All stay as their registry rows.

## Backlog status changes (applied to tracked-backlog.md this session)

- **T4** — dated state edit: Pricing Plans install DONE 2026-07-17 (DECISIONS:181, plan `5874dba8-…`); remainder = one real purchase loop + plan rename. (D11)
- **T23** — CLOSED: `conventions/contracts.md` authored by this audit (the Hyrum inventory, from Track D's code-derived enumeration).
- **T16** — corroborated: mobile 2k tier landed in working tree; desktop 4k fallback still parked.
- **T1/T2/T3/T5/T6/T10/T11/T13/T17/T21/T22** — confirmed in their recorded states (evidence in track reports).
- **NEW T24** — astro@5.18.2 XSS advisories (5, fixes in 7.x vs C4 Astro-5 pin): accepted risk while zero sinks exist; re-check on any `.astro` file growth or advisory affecting current usage.
- **NEW T25** — ASTRO_ENGINE_PLAN:166 tail: point/ellipse impostor RENDER never eyeballed (ring widening + tracked-DSO flows verified 08-03/08-10); eyeball + shot rides the next visible-window session. (C2/D8)
- **NEW T26** — upload-url unbounded `sizeBytes`/mint rate (B9): owner judgment on caps.

## Fitness scorecard (seed promises → best proxy → value)

| Promise | Proxy | Value |
|---|---|---|
| Geo-accuracy | literature/Horizons test vectors | Horizons ±0.05° (3 instants) · Hale-Bopp ≤2′ @ Δ=50.6 au · Ceres/Vesta ≤30″ · subsolar +23.43° · 704/704 green |
| Beauty (C2) | dated shots riding numbers | astroB/C shot sets (08-03/08-10) · fpv-walk shot (08-11) · M0 402×874 + 1400×900 (08-13) · milkyway-2k patch means 0.998/0.984/0.949 vs 8k — owner-eyeballed; no regression claims this session (browser tier not run) |
| Perf | measured numbers on record | decode 26 MP ≈ 4.8 s (07-10) · FPV 53 fps (08-11) · suite 1.54–1.69 s warm · bundle 33 MB dated breakdown · **real-device: unmeasured (T1)** |
| Privacy C6 | structural probe results | PASS — full-path trace + writer sweep + geolocation-API absence (probes above) |
| Docs currency | stale-tag / drift count | 6 astro-check hints (= baseline) · stale clusters found: quota-10 ×3 (README/convention/plan) · APP_NOT_INSTALLED ×2 · 2 stale convention notes · ARCHITECTURE §5 · core.md §layout — all sliced for fix |

## Proposed convention / checklist amendments (Pesticide harvest — applied dated where owned by the skill)

1. APPLIED: code.md items 17–19 (InstancedMesh lifecycle · CSS containing-block · FPV disabled-controls/world-space-walk) + platform.md item 13 (provision script = schema source of truth) — the re-mine.
2. APPLIED: tests.md item 1 K&S quarter corrected 10.3% → ≈9.1% (the test was right; the checklist was wrong).
3. Fix-slice: audit-mode.md bundle baseline re-anchor (D3).
4. Proposed for next authoring pass: a static "consumer-clamp" test walking `heightAt` call sites (C7 list, item 1) — would have caught A1 at write time; plus the C7 tail ([hidden] CSS pair walk, TUS threshold chooser test).

## Fix-session slicing (ordered; severity-first; each with its gate)

| # | Size | Contents | Gate | Frozen-chrome? |
|---|------|----------|------|----------------|
| 0 | S | **D1 ledger repair** (split line 163, reconstruct header) + D2 marker move — BEFORE any commit | visual diff review; both entries intact byte-preserving | no (docs) |
| 1 | S | **A1** clamp + no-latch-on-clamped + regression test | vitest (+N) · astro check | no (engine internal) |
| 2 | S | **A4/B1** add @wix/redirects · A2 delete Group renderOrder line · A3 remove/annotate setClearColor · A7 rename+comments | vitest · astro check · wix build | no |
| 3 | M | **A6** extract ONE PublicPins mapper (lib/wix) + topoAzAlt + TTL-cache helper + tests | vitest (+N) · astro check | no (libs/server) |
| 4 | S | **B2** env-ify test credential (owner rotates after) · **B6** repo-relative landmask path · **B4** bake-date stamps in both generators | node --check on scripts · grep | no |
| 5 | S | **B5** variantId guard · **B7** quota display · **B8** comment · **C4** regex hardening · **C6** Dec-solstice vector · **C1** GOLDEN mirror + parity assertion | vitest (+N) · astro check | B7 changes a response field consumed by MyPins — verify display |
| 6 | M | Docs batch: **D3** audit-mode baseline · **D4** README · **D5/D6** conventions · **D7** ARCHITECTURE §5 rewrite from provision script · **D9/D10** core.md refresh+backfill · **D11** T4+PLAN notes · **D12/D13** nits · C2/D8 tag annotation | grep re-checks; no gates (docs) | no |
| 7 | M | **B10** move enriched-sample out of public/ (build + size re-measure) · **A5** de-export sweep (then knip re-run) | wix build · vitest · knip | no |
| 8 | — | OWNER: B9/T26 caps decision · B2 password rotation · T25 eyeball (browser session) · release canaries T2/T3 ride next release | — | — |

Slices 0–6 are safe for one combined fix session (this one, owner-ordered); 7 wants its own build verification; 8 is owner/browser-tier.
