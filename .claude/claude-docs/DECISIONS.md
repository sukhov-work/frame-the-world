# Frame the World — Decisions Log

One line per meaningful change: what was decided, files touched, and any number measured. **Append-only,
absolute-dated.** Verification status is explicit — local-tested, wix-VERIFIED (confirmed against the live
Wix platform), or UNVERIFIED. Supersede a past line with a newer dated line; never edit or delete old ones.
Durable design rulings also live as `mem:decisions/*`. Maintained per `mem:decisions/session_workflow`.

**Structure (compacted 2026-07-11).** Top-to-bottom: binding **ADR-000** decisions · a durable
**Traps & Gotchas** section (the load-bearing gotchas that keep resurfacing) · **Per-phase digests** ·
**Recent sessions** verbatim (newest first — new work appends here). Older verbatim session logs
(2026-07-09 → 2026-07-10) were **moved byte-identical** into [`DECISIONS_ARCHIVE.md`](./DECISIONS_ARCHIVE.md)
— the append-only invariant holds (a move is not an edit; superseded rulings are annotated only in the new
digest/ADR/Traps sections, never in the archived text). **Compaction round 2 (2026-08-15):** verbatim
sessions **2026-07-11 → 2026-08-01** were likewise moved byte-identical into the archive (its
§Moved 2026-08-15 divider) with new per-era digests appended below; the astro-engine era
(2026-08-02 →) stays verbatim in Recent sessions.

---

## Binding decisions — ADR-000 (D1–D15)

From `PROJECT_SEED.md §4`, research-VERIFIED unless noted. **Binding** — change only by an explicit,
dated supersession line under Recent sessions.

> **D8 superseded 2026-07-10:** the `beforeInsert` quota hook was replaced by **endpoint-enforced quota**
> in the elevated `POST /api/photos` — the headless CLI provisions no Wix Data hooks (a member-session
> insert is platform-refused). See the Phase 5 digest. The 10-free / unlimited-paid intent is unchanged.
> **Quota numbers re-ruled 2026-07-17 (owner):** free **100** · premium **1000** (was 10/unlimited);
> enforcement mechanism unchanged. See the 2026-07-17 owner-rulings line; ships in Phase 6.9.

- **D1 — Globe engine:** three.js + `3d-tiles-renderer@^0.4` + Cesium OSM Buildings (ion 96188) + `GlobeControls`.
  Only combo giving real global 3D buildings + geo-accuracy + unrestricted per-tile material override + custom
  cinematic camera. VERIFIED.
- **D2 — Precision:** re-center tiles group near origin (ReorientationPlugin / CESIUM_RTC) + GlobeControls
  dynamic near/far. Solves float32 jitter without a float64 fork. VERIFIED.
- **D3 — Decode:** `exifr` embedded-JPEG preview → `libraw-wasm` Worker demosaic; single-threaded SIMD default;
  HEIC Safari-native detect + `libheif-js` fallback. VERIFIED (pipeline), UNVERIFIED (threads / COOP-COEP).
- **D4 — Orientation UX:** nudge-to-align is core; `FOV = 2·atan(sensorWidth/(2·focal))` + sensor DB +
  `FocalLengthIn35mmFormat` fallback. ILCs rarely write heading; GPS 3–15m, altitude junk → terrain-snap. VERIFIED.
- **D5 — Projection:** textured plane at frustum far face (v1); projective texturing (v2 stretch). VERIFIED.
- **D6 — Ephemeris:** `astronomy-engine` 2.1.19 (±1 arcmin) + procedural sky + Yale BSC5 stars, one source
  drives sliders + lighting. VERIFIED.
- **D7 — Data:** Wix Data Collections + geohash-prefix `hasSome` + client refine; denormalized `PublicPins`.
  VERIFIED (no geo ops), INFERRED (pattern).
- **D8 — Quota:** Pricing Plans check + `beforeInsert` hook rejecting insert #11 for free members (server-side). INFERRED.
- **D9 — Media:** originals private, derived previews public; resumable TUS upload for >10MB; 30-day download
  links. VERIFIED.
- **D10 — AI:** runtime Claude via Wix AI APIs (~1 credit/call; Opus 4.6 shown); vision gets downsized JPEG;
  premium-gated; doubles as the moderation pass. VERIFIED.
- **D11 — Scheduling:** none in v1; if needed, external cron → token-secured HTTP endpoint. VERIFIED.
- **D12 — Rendering:** WebGL2 primary, WebGPU progressive via `three/webgpu`. VERIFIED.
- **D13 — Cesium ion:** Community (free) for PoC; Commercial ($149/mo) at first sale / >$50K entity; manual
  attribution in UI. VERIFIED (terms), INFERRED (burn rate).
- **D14 — Design workflow:** Claude Design as token/motion factory → tokens.css (source of truth) → GL bridge
  `tokens.ts`; fence the globe; skip Claude Design's Wix connector (we scaffold via CLI for island/worker
  control). VERIFIED (workflow), UNVERIFIED (connector details).
- **D15 — Working title:** "Frame the World". ASSUMPTION (provisional).

---

## Traps & Gotchas (durable)

The hard-won gotchas that keep resurfacing. Violating one is a bug. Referenced by
`NEXT_SESSION_PROMPT.md`; the originating session (date/phase) is named for the full story in the
Recent sessions block or `DECISIONS_ARCHIVE.md`.

### GL / rendering
- **Navy night-sky floor.** `renderer.setClearColor` encodes the colour to the renderer's OUTPUT space
  (sRGB) when no render target is bound, and EffectComposer runs autoClear-off → sRGB-encoded values land
  in the LINEAR HalfFloat buffer and OutputPass re-encodes them to (8,26,45) navy on every empty sky pixel.
  Fix = `scene.background = Color(bg)` (three converts per-target + force-clears). (S5)
- **Negative `heightAt` garbage.** Quantized-mesh terrain tiles return NEGATIVE elevations while loading /
  on coarse LOD → **clamp every consumer to `[0, 9000]`** (clamp-only-upward). Unclamped, an arrival sinks
  to its lookAt floor and an underground camera unloads the whole tileset. (S2)
- **Additive overlays vanish on a bright sky.** Additive blending adds only ~10–30/255 against the day sky.
  Planning overlays (day-arcs) are alpha-blended solid strokes, `depthTest` off (analytic horizon owns
  occlusion). (S6)
- **`GlobeControls.update()` skips the near/far fit when `controls.enabled === false`** → a disabled-controls
  mode (FPV) must call `controls.adjustCamera(camera)` every frame or the frozen planes black-screen at
  street level. (S2)
- **Stale `InstancedMesh.boundingSphere`.** three caches the EMPTY sphere from count=0 and GlobeControls
  raycasts before pins load → every later pick early-outs forever. Set `mesh.boundingSphere = null` on
  every instance change. (Phase 5.1)
- **ECEF float32 cancellation.** ECEF ~6.4e6 m in float32 GPU matrices quantises at ~0.5 m and CRAWLS as the
  camera moves. Render instanced / large-coordinate meshes **camera-anchored**: `mesh.position =
  camera.position`, camera-relative instance translations, modelViewMatrix-only shaders. TRAP: `hoverAnchor`
  must add `mesh.position` back. (S4; same lesson as PhotoFrustum.)
- **`backdrop-filter` creates a containing block** → `position:fixed` descendants become panel-relative.
  Render fixed popups OUTSIDE any backdrop-filtered ancestor. (S3)
- **Dynamic far-plane clips the far hemisphere.** GlobeControls' far plane hides the starfield + atmosphere
  far shell → render near-hemisphere, clamp camera-anchored impostor distance ≥ 1.2·near. (Phase 1)
- **Descending zoom outruns tile load.** The manual zoom targets ellipsoid altitude, so a street request
  dives under a high city; once underground the ground tileset fully unloads (0 meshes) and live `heightAt`
  never recovers. Sticky `lastGroundM` sampled at the VIEW FOCUS + a street-floor clamp BEFORE the crossing.
  Tiles exist only inside the frustum — sample the focus, not the camera footprint. (S2)
- **Typed-array texture uploads ignore `UNPACK_FLIP_Y`** → flip in the DATA. No single-channel sRGB format
  exists in WebGL2 → linearise a data texture with a `uNightGamma` uniform. (S5)
- **`Group.renderOrder` does NOT propagate to children** — set renderOrder per object. **Sub-pixel points
  render nothing** — point sizes ≥ 2 px. **`getPivotPoint` returns null on horizon views** and leaves the
  out-arg STALE — fall back to the view focus. (Phase 4 / S6)

### Wix platform / backend (gate Phase 6 — never archive silently)
- **App-defined POST routes** were feared to 403 through the cloud adapter → the released-URL `/api/ping`
  canary confirmed 200; the risk is dead but the canary STAYS as the pre-release gate. (Phase 5 release)
- **Login builds an `http://` callback** repaired from the Referer header; `http` is tolerated only for
  localhost → referer-less (curl) auth fails "Invalid redirect URI". (Phase 5 release)
- **Elevated inserts run as the APP identity** → set `ownerMemberId` explicitly or ownership is wrong. (Phase 5)
- **`extensions.dataCollections` does NOT provision from `wix dev`** → the REST
  `scripts/provision-collections.mjs` is the schema source of truth. (Phase 5)
- **A stale member cookie is silently replaced by a visitor cookie** on the next HTML response → re-mint
  tokens before verifying member flows. (Phase 5)
- **OAuth allowlist is PORT-EXACT** — 4321 and 4322 must both be registered. (Phase 5)
- **Viewport pin query must THROTTLE, not debounce** — the perpetual LEO idle drift starves a debounce
  timer so it never fires. (Phase 5)
- **Media >10 MB must use `generateFileResumableUploadUrl` (TUS)** → async `onFileDescriptorFileReady`. (CLAUDE.md)

### Verification / tooling
- Occluded Chrome throttles rAF to ~1 frame/several-s → `page.bringToFront()` before timing-sensitive
  browser verification.
- Hiding the tiles groups crashes the rAF tick → the canvas presents the last good frame and every "hidden"
  measurement reads stale (guard with a `renderer.info.render.frame` advance check).
- `document.elementsFromPoint` SKIPS `pointer-events:none` nodes; `stars.update()` re-sets `points.visible`
  every frame (hide via `material.visible`); synthetic dblclicks need a preceding pointerdown/up pair.
- Verify tiers, in order: Playwright MCP → Chrome-extension bridge → scripted headless-Chrome CDP
  (`scripts/verify-s5-night.mjs` idiom). DEV seams: `window.__globe / __composer / __cameraStore /
  __timeStore / __uploadStore / __pinsStore / __memberStore`. Screenshots → `verify-shots/` only.

---

## Per-phase digests

One line per phase; full mechanics in the linked memory, verbatim session logs in `DECISIONS_ARCHIVE.md`.

- **Bootstrap (2026-07-09)** — `.claude/` operating environment + Serena memory graph + the persistence
  loop (DECISIONS + NEXT_SESSION). `mem:decisions/session_workflow`.
- **Phase 1 — scaffold + globe (2026-07-09/10)** — Wix headless Astro scaffolded onto the existing repo;
  the globe rebuilt into the LEO "signature scene": NASA Blue Marble July graded into the palette + VIIRS
  night lights + relief, ray-based atmosphere, a second TilesRenderer draping palette-graded Esri imagery
  that dissolves in by altitude, dark edge-stroked OSM buildings. `mem:patterns/globe-rendering`. RELEASED
  to the live URL 2026-07-10.
- **Design system import (2026-07-10)** — chrome tokens + fonts → `tokens.css` + the GL bridge
  `lib/theme/tokens.ts` (ADR D14). `mem:patterns/design-system`.
- **Phase 2 — decode (2026-07-10)** — `exifr` metadata + embedded-thumb preview + **`libraw-wasm@1.0.5`
  EXACT-pinned** (1.1.2+ needs pthreads/SAB = COOP/COEP, unusable on unverified Wix hosting) in a disposable
  per-file Worker + `libheif-js` HEIC fallback. `mem:patterns/upload-flow`.
- **Phase 3 — projection (2026-07-10)** — camera frustum + image plane from EXIF; reactive zustand
  re-projection; click-to-place for missing GPS; 2.2 s bezier flight (reduced-motion cut).
  `mem:patterns/photo-frustum`.
- **Phase 4 — ephemeris (2026-07-10)** — `astronomy-engine` drives sun/moon/terminator/shadows/atmosphere
  from scene time; TimeScrubber ±12 h + multiday; golden-hour bell grade; real BSC5 star field (−GAST); real
  Cesium World Terrain (90 m building sink removed). `mem:patterns/sky-bodies-terrain`.
- **Phase 5 — members + pins (2026-07-10)** — managed member auth (`@wix/astro`); Photos/PublicPins
  provisioned via REST; **endpoint-enforced 10-pin quota** (supersedes D8's hook); C6 reduced-precision
  public pins (cell-centre published); viewport geohash query + click→fly. First Wix-load-bearing phase.
  `mem:patterns/members-pins`.
- **Phase 5.5 S1–S6 — pre-marketplace UX (2026-07-11)** — location finder, flight/FPV core, pin lifecycle,
  pin visuals + Explore journey + Welcome landing, night-sky physics, FPV planning overlays. Canonical
  design `archive/PHASE_5_5_UX_BATCH.md`; full mechanics in the Recent sessions block below + `mem:project/wip-2026-07-11-*`.
- **Pre-S7 architecture review + refactor (2026-07-11)** — this compaction, doc-drift fixes, new
  conventions, and the safe cleanup tier. See `archive/ARCHITECTURE_REVIEW.md` + the Recent sessions block.
- **Phase 5.5 S7 tail + interlude (2026-07-11→12)** — S7 street names went GL: one canvas-textured quad
  per name LYING ON the terrain (DOM labels structurally lag a 60 Hz mesh), sourced from OpenFreeMap
  planet MVT (keyless; **maxzoom 14 — z15 returns empty 200s, probe first**; `pbf@5` exports `PbfReader`,
  no default export); vector road/water web with tile-exact clipping (Sutherland–Hodgman rings ·
  Liang–Barsky lines with re-entry splitting) killed the river-tile z-fight flicker; camera pose became
  shareable/reload-safe via the `#p=` URL hash. High-altitude pin selection landed SHIFTED — root cause:
  `ground.heightAt` returns null/NEGATIVE garbage on unloaded tiles (live: −2047 m at 697 km) so the
  committed arrival pose kept a stale low lookAt; fix = live re-frame + `clampGroundM`
  (`mem:bugs/pin-arrival-reframe`). Pre-S7 safe-refactor tier finished (B6–B15/B26: `lib/geo/{terrain,
  screen,heading}` extracted, the one lib→store violation killed, net −68 LOC); README rewritten for the
  internal Wix contest (headless-stress-test framing, 100% agent-built stated openly).
  Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15. `mem:project/wip-2026-07-11-phase5.5-s7` ·
  `wip-2026-07-11-s7-feedback-batch` · `wip-2026-07-11-pre-s7-refactor{,-s2}` ·
  `wip-2026-07-11-b19-split` · `wip-2026-07-12-readme-rewrite`.
- **Rendering-quality passes + Dnipro enrichment slices 0–3 + illumination (2026-07-12→14)** — Pass 1:
  NEW pure `lib/globe/quality.ts` (`detectDeviceTier` + `makeGovernor` EMA frame governor); **HARD
  INVARIANT: `QUALITY.tiers.high` == the pre-pass constants**; shadows later DECOUPLED from the governor
  (they follow the DEVICE tier; capable machines floor at `mid`) after the M3 Pro was degraded to `low`
  and lost the entire shadow pass. Pass 2: per-building tonal variation keyed on `_batchid`/`_feature_id_0`
  (GLTFLoader lowercases non-standard attributes); **ROOT LESSON: no FLAT per-building emissive ever reads
  as lit windows** — R3 night emissive OFF (`nightWindowGain 0`), window grid removed on owner order.
  Illumination pass owner-VERIFIED: `DRAPE.shadowOpacity 0.80` · `minSunElevSin 0.008` (~0.46°, keeps the
  golden-hour raking shadows) · altitude-adaptive shadow bounds. Enrichment: bake-and-mask REPLACES Cesium
  OSM Buildings in-bbox, self-hosted on R2 (extends D1/D13/C6); **vertical datum = bake relative geometry
  + runtime clamp-to-CWT** (EGM2008 undulation over Dnipro +20.42 m — a naive absolute-Z bake sinks ~20 m);
  Slice 2 = per-cell terrain re-seat (offsets match terrain within ~0.2 m; spread −20.6 m riverbank→hill)
  + a pixel-exact 4-plane ECEF `clipIntersection` mask hole; Slice 3 = 24,714 hash-seeded trees, ONE
  `EXT_mesh_gpu_instancing` node per cell (re-bakes byte-identical). Blank gallery preview fixed = SSR
  boot poster + OG meta — the Wix `site-snapshotter` captures SSR HTML only, never `client:only` islands
  (`mem:bugs/gallery-thumbnail-stale` RESOLVED). Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15.
  `mem:project/wip-2026-07-12-rendering-{quality-pass,pass1-tiling-fluidity,pass2-dnipro-identity}` ·
  `wip-2026-07-13-illumination-pass` · `wip-2026-07-13-dnipro-slice{0-spike,1-bake,2,3-trees}` ·
  `wip-2026-07-13-terrain-reseat` · `wip-2026-07-13-dnipro-enrichment-research`.
- **OSM2World variant + R2 hosting + obstruction moat + owner seating/UI batches (2026-07-14)** —
  `bake-osm2world.mjs` shipped as a PARALLEL VARIANT (`cities/dnipro-o2w.json` `extends` dnipro; the
  `?enriched=` A/B seam; the classic pipeline stayed byte-identical); the 1-cell spike settled the o2w
  axis: `(east,up,−north)` == our `gv(e,u,−n)` (1362/1362 buildings in-bbox). R2 hosting LIVE: a
  Cloudflare **Worker over a private bucket** (free-tier CORS/Range control; r2.dev serves no CORS) at
  `frame-the-world.ievgen-sukhov.workers.dev`; dev NEVER reads R2 — tiles moved out of `public/` into
  git-ignored `bakes/` + a serve-only Vite middleware `ftwLocalTiles` (client bundle 372 MB dead weight →
  24 MB). Both bakes expanded to Greater Dnipro ~20×20 km (grid 20 ≈ 1 km cells; 127,890 bldgs · 161,823
  trees) + the `BLD` chip CLASSIC⇄OSM2WORLD toggle. PER-BUILDING/PER-TREE terrain re-seat done on the
  CPU (position attribute / instanceMatrix) **deliberately** so planFeed occlusion sweeps, shadow maps and
  picks read the same arrays — a shader displacement would desync the planner. Pass 3 = the astro moat:
  pure `lib/ephemeris/planner.ts` (rise/set with eye-height dip; golden windows DERIVED from tuning.GOLDEN)
  + `lib/geo/horizonProfile.ts` + `skylineState` clear/block crossings (12-min scan + bisection ±0.5 s).
  FPV solidity became the screen-door Bayer dissolve — opaque + depth-writing at every k (the binary
  `depthWrite` flip was the 55→56 solid jump); `#f=` shareable FPV URLs; UI QoL batch (uniform
  hover-reveal DragGrip, time playback, shareable custom time in the URL, PLAN top-left, ☀/☾ edge chips).
  Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15. `mem:project/wip-2026-07-14-osm2world-adapter` ·
  `wip-2026-07-14-osm2world-slice1.5-spike` · `wip-2026-07-14-r2-hosting-osm2world-prep` ·
  `wip-2026-07-14-pass3-obstruction-moat` · `wip-2026-07-14-owner-batch-seating-ui` ·
  `wip-2026-07-14-uiux-qol-batch`.
- **Docs reorg → Phase 6 marketplace → 6.9 + release week + St Albans (2026-07-15→18)** — claude-docs
  reorganized into `provenance/ · dnipro-enrichment/ · rendering/ · archive/` subfolders (37 files
  repointed); Milky Way rebaked at 8K (**SVS star maps are flux-per-pixel** — 8k needs ×4 linear gain;
  gaussian pre-blur σ=1 texel in LINEAR space, else mip-less point sampling skips the sub-texel star
  speckle and the band goes near-black). Phase 6 marketplace-light: **Catalog V1 is IMPOSSIBLE on this
  site** (gateway 428 `CATALOG_V3_CALLING_CATALOG_V1_API`) → built on V3 — `POST /stores/v3/products
  {productType:"DIGITAL", …digitalProperties.digitalFile._id}` wix-VERIFIED; the preinstalled Stores
  automation delivers the 30-day link. PROD OUTAGE root-caused: the Wix asset origin cold-serves each
  chunk in 15–30 s and 500s at the ~30 s gateway cutoff → dead islands/black globe after every release
  resets hashes; recovery = SERIAL `warm-prod-assets.mjs` retry-until-200, and **the asset edge cache is
  SHARDED** (a warmed shard ≠ the browser's — reload until clean). Owner rulings: quota free **100** /
  premium **1000** (supersedes D8's numbers) · demo pins owned by yevhens@wix.com and ALL for sale ·
  MARKETPLACE as its own topnav button. 6.9 batch: two-tier quota · EUR fix (`SITE_CURRENCY` — no SDK
  exposes site currency) · SALES tab · `redirects.createRedirectSession` REJECTS relative postFlowUrl
  (absolutize) · Catalog-V3 `createProduct` defaults variants to tracked-quantity-0 (blocks checkout) →
  `createProductWithInventory {inStock:true}`. Demo seed ×27 listed + FAQ panel + orbital-grade pass
  RELEASED live; ground checkerboard parked (`mem:bugs/ground-checkerboard-flicker`). City #2 St Albans
  baked + live on R2 — **the o2w work-cache was sub-grid-INDEX-keyed and collides across cities** →
  per-city `.cache/o2w/<city>/`. Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15.
  `mem:project/wip-2026-07-15-docs-reorg-phase6-prep` · `wip-2026-07-16-phase6-marketplace-research` ·
  `wip-2026-07-16-prod-asset-outage` ·
  `wip-2026-07-17-{phase69-marketplace-batch,demo-seed-curation,seed-orbital-faq-batch}` ·
  `wip-2026-07-18-st-albans-city2`.
- **View-prefs persistence + default flips (2026-07-21)** — defaults flipped: SAT ground on, FPV
  buildings fully solid (slider double-click resets to 100). NEW seam `lib/prefs.ts` (`ftw:view-prefs:v1`
  localStorage; `sanitizeViewPrefs` clamps/drops junk; SSR/node-safe no-throw): SAT/☀☾/BUILDINGS persist
  inside their store setters; **PIN persists at the CHIP only** — the orchestrator's FPV declutter shares
  `setPinsVisible` and must never write the pref (a mid-FPV reload would freeze pins-off as if chosen);
  BLD variant reload-survives via pure `applyStoredVariant` (an explicit `?enriched=` ALWAYS wins
  verbatim). Detail-panel close ask included. Everything from the 2026-08-02 comet-10P tracer onward (the
  astro-engine era) stays verbatim in Recent sessions below.
  Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15. `mem:project/wip-2026-07-21-viewprefs-uiux`.

---

## Recent sessions (verbatim, newest first)

New work appends a dated line here. *(Marker moved to the top 2026-08-13 — it had drifted mid-list
since 2026-08-03; a move is not an edit (compaction precedent). Audit finding D2.)*

- **2026-08-17b-u1-2d-mobile (UPLIFT U1 SHIPPED — /m 2D-first navigation + pinch hardening,
  owner points 1+5. Gates vitest 922/922 (+14) · astro 0 err/5 hints; phone-viewport
  browser-VERIFIED (wix dev + Playwright, shots `verify-shots/u1-01..04`); REAL-DEVICE pass =
  the exit gate, OPEN — rides the next owner session.)**
  **(a) `camera.mapMode` seam** — `"2d" | "3d"` in store/camera (default `"3d"`, NOT persisted —
  owner rule "/m always boots the 2D map"; desktop never writes it → every U1 seam desktop-inert;
  survives `clearAllTargets`, a mode is a place not a glide). **(b) Building detach** —
  `setActive(on)` on BOTH tileset handles (buildings.ts + enrichedBuildings.ts): scene-graph
  remove (render + GlobeControls' whole-scene raycast — three's Raycaster does NOT skip
  invisible objects, so `visible=false` alone was wrong) + frozen `update()` (no traversal/
  download/parse); LRU stays warm → instant re-attach. Orchestrator gate `stepMobileBuildingsGate`
  = `fpvActive || mapMode==="3d"` per frame (handle identity-guards make it free). **(c) 2D boot**
  — body.m class (server-rendered, race-free) detects the shell; no-hash boot = EXACT nadir
  north-up over MOBILE2D.bootLat/Lon@1100km (constructed directly — `arrivalPose` clamps tilt
  ≥5°); `#p=` with tilt≥`twoDMaxTiltDeg` boots 3D (respect an oblique share), nadir `#p=`
  re-lands 2D (the /m mirror writes tilt≈0 hashes — a reload must not silently flip to 3D);
  `#f=` enters FPV, exit lands 2D. **(d) 2D locks** (`stepMobile2dLocks`, after the manual
  glides they defer to) — tilt→nadir + heading→north re-lock, τ=MOBILE2D.lockEaseTauMs;
  heading measured off SCREEN-UP (`mapUpHeadingDeg` — forward-heading is DEGENERATE at nadir:
  it printed 180.5° on a north-up chart; the pose mirror + `#p=` hash use it too while 2D).
  **(e) Two-finger tilt = the library's own touch-ROTATE** (EnvironmentControls.js:562-585
  pinch/parallel classifier — no custom gesture layer): while it is live the tilt lock stands
  down, the heading lock keeps running (gesture reads as pure tilt), crossing
  MOBILE2D.enter3dTiltDeg=15° flips to 3D mid-gesture (buildings attach). Verified synthetic:
  tilt 0.06°→80°, heading pinned 0, ROTATE state 2 throughout; pinch stays ZOOM (1100 km→25 km,
  tilt 0.03° — chart behaviour). **(f) Idle drift OFF in 2D** — the LEO drift slid the chart
  ~3° lon in the first browser pass; a drifting map reads as broken, not cinematic. **(g) FPV
  exit on /m** — sets 2D + flies `mapArrivalPose` (south approach ⇒ north-up, tilt 0→5° clamp,
  MOBILE2D.exitAltAboveGroundM=600 — 200 was a frame view, too tight for a map; measured 602 m,
  heading 359.87°); skips `beginFraming` (it would re-fly the OBLIQUE photo arrival). **(h) /m
  chrome** — SceneActions `▼ 2D MAP / ▲ 3D VIEW` chip (writes mode + reuses the tilt/heading
  glides); ▦ 3D DETAIL hidden in 2D (dead reload chip). **(i) Pinch hardening (point 5)** —
  MobileLayout `maximum-scale=1, user-scalable=no` + iOS `gesturestart` preventDefault guard
  (iOS ≥10 ignores user-scalable; pointer events unaffected); `touch-action` closed on the
  audited §1.2 leak list (desktop `.globe-canvas` none — touch laptops; `.m-bottom`/`.m-actions`/
  `.m-scrim`/`.mm`/`.skymenu`/status chips none; `.m-sheet__body` pan-y); desktop + /guide stay
  browser-zoomable (a11y). `test/styles/pinchHardening.test.ts` pins the leak list file-by-file
  (the declarations are silently droppable — guide-image-cross-check style). DEV seam
  `__globe.map2d()` (mode + group-membership truth). Files: tuning.ts (MOBILE2D block),
  store/camera.ts, scene/buildings.ts, scene/enrichedBuildings.ts, StylizedTiles.ts,
  SceneActions.tsx, MobileLayout.astro, global/chrome/mobile/mini-map/sky-menu css,
  test/store/camera.test.ts, test/styles/pinchHardening.test.ts. UNVERIFIED (real device only):
  actual browser-pinch suppression on iPhone 17 Pro/Pixel 6 Pro (synthetic pointers can't prove
  viewport/touch-action), gesture feel (enter3dTiltDeg/lockEaseTauMs tuning), 3D chip's 55°
  glide landing at ~48° (released early — cosmetic, taste-tune). Next: U2 FPV stability.

- **2026-08-17-p7-meteors+uplift-plan (Phase 8c P7 METEOR SHOWERS SHIPPED desktop + R12 rail layer; UPLIFT_PLAN.md AUTHORED — the owner's 10-point mobile/desktop uplift ladder U1–U8. Gates vitest 908/908 (+22) · astro 0 err/5 hints; desktop surfaces browser-VERIFIED (wix dev + Playwright, shots verify-shots/p7-01/02).)** **(a) `lib/ephemeris/showers.ts` (NEW)** — 21-row IMO-working-list bake (hand-curated 2026-08-17; IMO cal2026 Table 5/6 + IAU MDC streamfulldata + Jenniskens 1994 B slopes — NOT GPL showers.json; provenance in header): λ☉ activity windows/peaks, radiant + deg/day drift, ZHR (null for Var outburst rows JBO/AMO), r, Vg, parent, per-side B slopes (GEM 0.39/0.72 asym, QUA 1.4/2.2; default 0.19 — research corrected the plan's assumed 0.9, no such general value). Model: **λ☉ referred to EQUINOX J2000** (IMO/MDC standard; `SunPosition()` is of-date and runs ~0.37° ≈ 9 h early in 2026 — computed via `Rotation_EQJ_ECL` on the EQJ sun vector, equinox-2000 anchor 0.001°, research-validated 2026-01-03 anchor 283.048°); `lambdaToMs` Newton peak solver; `zhrAt` = Jenniskens 10^(−B·|Δλ|) clipped to the window; `visibleRateAt` = ZHR×sin(radiant alt) γ=1 (IMO practice); `showerNights` = mwSeason-convention moon scoring at the night peak, **score = peak RATE × (1 − interference)** (rate-shaped, deliberately ≠ mwSeason's minutes-shaped score — convention firewall in both headers); `upcomingShowerPeaks` (120 d card feed). **(b) Radiant = tracked target:** `showerTarget` factory in targets.ts (id `shower:PER`, kind "shower" — the type layer was pre-seeded; per-call drifted stateAt, magnitude null by the documented contract) + TargetFacts `shower` member + `targetShortName` → code; catalog three-touch (showerEntry ☄ rows boost 0.9, skyIndex spread → 1,971 sync entries, `shower:` resolver branch) — search "perseids" → track → reticle/trail/windows/edge-chips ALL inherited, prefs persistence free (id carries ":"). **(c) Surfaces:** TargetPanel `ShowerFacts` (ZHR/r/Vg/parent · peak + apparition dates solved from λ☉ · drift + off-centre framing hint); PlanPanel `MeteorsCard` after MoonCal (MwCard grammar, upcoming peaks w/ score bars + ☾%, row = setTime(best-night peak) + setTarget(radiant) — the FindPanel jump idiom); TimeScrubber **meteor intensity layer (R12)** — star-coloured filled area from the rail floor (y 39→22, normalized to the shower's own ZHR; `--color-star`: meteors ARE shooting stars), only when the tracked target IS a shower, Var rows honestly flat; CSS `path.`-qualified over the `.ts-curves path` fill:none. Tests `test/lib/ephemeris/showers.test.ts` (22): equinox/precession anchors, PER-2026 = Aug 12–13, GEM asymmetry re-derived from REAL λ☉ (December runs ~3% over the mean rate — mean-rate test shortcuts were the bug, lib was right), night-scan invariants re-derived from primitives, two-path targetAzAlt agreement, 21-row coherence sweep. Browser-verified chain: SKY search ☄ row → track → THE SHOWER facts (drifted RA 06h21m ORI) → METEORS card 8 autumn rows (AUR ☾31% … LEO Nov 18) → row click pins 2026-10-22 06:00 + tracks shower:ORI → rail area peaks at the cursor. **(d) UPLIFT_PLAN.md (NEW)** — owner 10-point order (2D-first mobile · fullscreen map · direction cones · minimap FOV cone · pinch hardening · FPV stability · closest-first loading · foveation · terrain precision · per-building height overrides) → slices U1–U8 with file:line evidence from 3 parallel scouts. Load-bearing findings recorded there: **the FPV "full re-render + jerk" bug has named mechanisms** (governor tier-down hits the library LRU `minBytesSize` 0.3 GB floor above our mid/low caps → mass evict/re-stream + enriched re-seat; banked `zoomDelta` discharges on FPV exit; per-frame unsmoothed `heightAt` for temp-pin eye + enriched group seat; one-shot `setResolutionFromRenderer` stale after resize) — all source-cited, browser-UNVERIFIED; foveation has REAL library hooks (`LoadRegionPlugin` RayRegion/SphereRegion, `loadAncestors=false` = distance-first); pinch-zoom leak audit (viewport metas unrestricted + bare touch-action list); per-building identity exists in the enriched bake but featureId is NOT re-bake-stable (OSM id dropped by the extractor — baker fix queued in U8). P8 conjunctions + P9 lunar eclipses REMAIN in 8-events; the uplift ladder takes the next sessions per owner order. Files: `lib/ephemeris/{showers.ts(NEW),targets.ts}` · `lib/sky/catalog.ts` · `panels/{MeteorsCard.tsx(NEW),PlanPanel.tsx,TargetPanel.tsx,TimeScrubber.tsx}` · `styles/time-scrubber.css` · `test/lib/ephemeris/showers.test.ts(NEW)` · `claude-docs/UPLIFT_PLAN.md(NEW)`. Open tails: owner taste (METEORS card copy/row cap · meteor-layer colour/height · an "ACTIVE NOW" chip for in-window showers whose peak passed — PER tracked today shows no card row) · /m twin rides M4 · real-device pass unchanged (T1). Memory: `mem:project/wip-2026-08-17-p7-meteors-uplift-plan`.**

- **2026-08-15e-guide-polish (OWNER BATCH ×5 post-guide SHIPPED: /m strip-tap FIX (real bug) · /m ▦ 3D DETAIL chip · ghost depth-occlusion · centred fixed-size GUIDE window · /guide standalone page. Gates vitest 886/886 · astro 0 err/5 hints; all five browser-VERIFIED.)** **(a) /m status strip was DEAD to real taps on `<button>` chips — real bug, owner catch:** `.m-status{pointer-events:none}` re-enabled only `a` (`.m-status a`) — the GUIDE chip AND MobileAccount's SIGN-IN/places button never received a real tap (all prior verification used synthetic `.click()`; Playwright's "canvas intercepts pointer events" actionability failure WAS the honest signal, misfiled as tooling strictness in 15d). Fix: `.m-status a, .m-status button { pointer-events:auto }` (mobile.css). VERIFIED with a full-actionability Playwright click → GUIDE sheet opens; `elementFromPoint` returns the button. **(b) /m ▦ 3D DETAIL chip** (`SceneActions.BuildingsChip`) — the desktop deck BLD twin: classic bake ↔ OSM2World detailed bake, OFF (classic) by default (pref `enrichedVariant` undefined → off; shared across shells), reload-based with the pose hash riding (the desktop effective-state rule: flip URL+pref, not the raw param); env-gated on `PUBLIC_ENRICHED_TILES_URL`; hidden in FPV; quiet when off / accent + "ON" suffix when active. **(c) skyGhosts depth-occlusion (owner screenshot: chain floated over a tower that clipped the real moon):** material `depthTest:false → true` (depthWrite stays off) + the impostor shell pushed ×1.02 deeper than the trail/marker shell — screen position unchanged (discs sit on their direction rays), but buildings/terrain now clip the chain exactly like the real disc (which depth-tests since round 3), and below-skyline ghosts vanish behind the horizon. renderOrder 10 under the body's 11 unchanged. FIND ghosts deliberately UNTOUCHED (planning overlay — rings must read through the skyline; the verdict column carries occlusion). VERIFIED: the 20:35 embankment chain now stops at the skyline (was: discs to the frame corner). **(d) GUIDE window = centred fixed-size reading layout:** NOT resizable (usePanelResize/ResizeGrip removed — the size is design), `min(56rem,92vw) × min(78vh,46rem)` dead-centre (`top/left:50%` + centring translate COMPOSED with the drag vars in one transform — the gd-enter keyframes carry the same composition), rail 10rem, still draggable; measured centred 0/0 px offset at 1440×900. **(e) /guide standalone page** (`pages/guide.astro`) — the THIRD renderer of guideContent: zero-JS server-rendered document (own minimal shell — Layout.astro locks body scroll for the canvas, so the page owns `<html>`), goal router + chapter TOC up top, crosslinks compiled to in-page `#anchors` (all resolve — probe-verified), tokens+fonts shared, print/link-friendly; desktop panel header gains a ↗ OPEN-AS-PAGE anchor (new tab — never kills the globe), /m GUIDE sheet index gains an OPEN AS A PAGE row. Guide copy corrected where the batch changed reality: move-deck BLD = SOURCE switch (classic↔detailed, reloads keeping the view; my 15d copy had implied a visibility toggle), mobile-chips gains ▦ 3D DETAIL; `shell-m.webp` re-shot with the new chip stack (default-off state). **Verify-env lesson (supersedes the 15d "Playwright strictness" trap):** a stolen-foreground `about:blank` tab freezes rAF AND swallows ALL real input in the target tab — `page.bringToFront()` fixes both; `Page.setWebLifecycleState` alone does NOT; and when an actionability check names a pointer-events interceptor, believe it before blaming the tool. Files: `styles/mobile/mobile.css` · `mobile/{SceneActions,GuideSheet}.tsx` · `globe/scene/skyGhosts.ts` · `panels/Guide.tsx` · `styles/guide.css` · `pages/guide.astro(NEW)` · `lib/guide/guideContent.ts` (copy) · `public/guide/shell-m.webp` (re-shot). Open tails: real-device tap pass (the strip fix makes T1 urgent-er) · owner taste on the ▦ chip label/placement + the centred window size. Memory: `mem:project/wip-2026-08-15-guide-g1` §15e addendum.**

- **2026-08-15d-guide-g1 (THE IN-APP USER GUIDE — G1 + G2-content SHIPPED both shells in one full-crunch session (owner order: unified two-shell guide, mixed-skill audience, screenshots + crosslinks, no-slop copy); FAQ ABSORBED; docs+memories REORGANIZED same session. Gates vitest 886/886 (+11) · astro 0 err/5 hints; both shells CDP-VERIFIED 1440×900 + 402×874 with imagery.)** **(a) One content source, fence-clean:** `src/lib/guide/guideContent.ts` (11 chapters · ~40 topics · GUIDE_GOALS "I want to…" router · typed GuideChapter/GuideTopic/GuideMedia with per-shell `where` lines) + `lib/guide/inline.ts` (`[[id|label]]` crosslink grammar, the ONLY markup) — lives in `lib/` because mobileFence bans `components/panels|ui` imports from mobile (faqContent's location was un-shareable). **(b) Desktop `panels/Guide.tsx`** — nav **Guide** entry replaces FAQ; PLAN/FIND-class resizable window (`usePanelDrag("guide")` + `usePanelResize`, default 31rem×76vh cap), left chapter rail + inner-scroll content, crosslink nav with topic scroll+flash, Esc-close; delegated **`data-open-guide`** opener (`data-guide-target` optional) — the data-open-upload idiom, no store. **(c) /m `mobile/GuideSheet.tsx`** — status-strip **GUIDE** chip → full Sheet, index (goals + chapters) → chapter drill with ‹ ALL CHAPTERS, same data, `.m-g*` primitives appended to chrome.css; SheetId union +"guide". **(d) FAQ retired:** Faq.tsx + faqContent.ts + faq.css + public/faq/*.webp DELETED; privacy (C6) / marketplace / upload copy ported into the trust/photo chapters. **(e) 12 fresh screenshots** `public/guide/*.webp` (720px desktop / 360px portrait /m, 6–68 KB, cwebp -q82 from live CDP captures at Dnipro golden hour + Aug-27 full-moon night; ALL added to warm-prod-assets.mjs — the cold-edge lesson; existence + warm-list cross-checked by test). **(f) Copy discipline enforced by test:** `test/lib/guide/guideContent.test.ts` (11) — unique ids, resolving crosslinks + router targets, image existence + warm coupling, steps 1–6, **banned-phrase slop lint** (simply/easily/seamlessly/leverage/delve/"note that"/…) + body ≤5 sentences; stop-slop rule-set distilled from owner-given refs. Copy audited against the LIVE UI: **double-click = pin + FPV in one motion** (deck tip), search bar = top nav, sky-menu labels state-flip (⌖ TRACK↔TARGET PANEL · ⊕ TRACKING↔TRACKING OFF), "My spot" nav label, ◎ SAVE PLACE, MY PINS·PLACES tabs, `&sky=` never shipped (excluded). **(g) Welcome gains HOW IT WORKS** (`data-open-guide`; clears explore so the guide opens on a still globe — the EXPLORE-arming default was wrong for reading). **(h) Docs/memories reorg (audit-driven, 3 background agents):** DECISIONS round-2 compaction — sessions 2026-07-11→08-01 moved byte-identical to DECISIONS_ARCHIVE (checksum-proven 363,628 = 151,433 + 212,195 B; 5 new era digests; hot file ~160 KB); README rewritten to 2026-08-15 reality (48 commits, 886 tests, planning-first section); ARCHITECTURE §7 regenerated; contracts.md + architecture-and-patterns.md + globe-tuning.md deltas; THIRD_PARTY astro-engine assets; ASTRO_ENGINE_PLAN + dnipro research trio → archive/ (refs fixed); 13 stale wip-* memories ARCHIVED-marked; task_completion/tech_stack lint-claim fix; `mem:core` compacted to era index. **Verify traps (NEW):** Playwright MCP saves screenshots relative to the repo ROOT — pass `verify-shots/...` filenames; /m strip chips fail Playwright actionability (canvas "intercepts pointer events") — dispatch `.click()` in evaluate; FPV `requestSkyLook` from script may consume very late (~2 min) — prefer `#f=` hash reload or feedback-loop drags for aiming; daytime 11%-crescent moon is below the pick alpha floor (click-transparent BY DESIGN) — right-click tests need a bright body. Files: `lib/guide/{guideContent,inline}.ts(NEW)` · `panels/{Guide.tsx(NEW),Welcome.tsx}` · `mobile/{GuideSheet.tsx(NEW),MobileShell.tsx}` · `styles/{guide.css(NEW),mobile/chrome.css}` · `pages/index.astro` · `scripts/warm-prod-assets.mjs` · `test/lib/guide/(NEW)` · deletions per (d). Open tails: owner taste pass (chapter set, copy voice, GUIDE chip placement, shot crops) · coach-mark journeys = G3 owner call · real-device pass · SIMBAD/SBDB ToS wording in THIRD_PARTY. Memory: `mem:project/wip-2026-08-15-guide-g1`.**

- **2026-08-15c-uxbatch2 (OWNER UX BATCH ×9 pre-guide SHIPPED both shells — context-menu quick toggles + camera-keeping rise/set + moon % · TRACKING camera lock · /m FIND 4th tab + STICKY in-frame standings (the Pixel bug) · mobile-default entry + landing buttons · /m SAVED PLACES + place quota dropped · altitude-column lift · collapsible /m mini-map. Gates vitest 875/875 (+5) · astro 0 err/5 hints; desktop CDP-VERIFIED 1440×900 + /m 402×874 (shots `verify-shots/uxbatch2-01..05`).)** **(a) FIND scan config lifted to store/find** — NEW `bodies`/`setBody` + `rangeDays`/`setRangeDays`; defaults **moon-only · 1M (30d — owner re-rule, was 6M)**; FindPanel + the /m FIND surface consume the store (chips survive remounts); NEW `test/store/find.test.ts` (5). **(b) SkyContextMenu grown:** NEW **⊕ TRACKING · ◌ MARK · ∿ TRAIL · ⌖ FIND IN FRAME** entries (ghosts idiom — ensureTracked first, state-flipped labels; FIND per body: sun/moon always, target only when `dso:gc`; ON opens the FIND surface, desktop-only closes PLAN first via the `body.m` probe — the one sanctioned shell check, never a mobile import); **rise/set jumps now AIM the camera** (`aimBodyAt` recomputes `targetAzAlt` at the NEW instant → `aimAtSky(az, max(alt,0))`; VERIFIED SET → heading 245→265 = the setting moon, inFrame at alt −0.9° — the §3.5 refracted-label/airless-geometry convention; the FIND "camera never moves" rule stays scoped to ghost/row clicks); moon header gains **illuminated %** (`245° · +16° · 11%`). **(c) TRACKING camera lock (net-new):** `sky.track` (session-only, deliberately NOT persisted — a reload must never grab the camera) + orchestrator step **stepSkyTrack** (8.5, before FpvPose): per-frame `targetAzAlt` from the camera geodetic into closure-local `skyTrackAim` (never a 60fps store write); the existing sky-look glide consumes `skyTrackAim ?? skyLook` with `FPV.skyTrackEaseTauMs 450`, arrival never clears while tracking. Releases: below `FPV.skyTrackReleaseAltDeg −1` · a real look-drag past clickDragPx (taps + FOV zoom deliberately don't) · the toggle; FPV exit suspends only. 5th toggle **TRACK** in TargetPanel + TargetSheet (both toggle rows now flex-wrap; not `visible`-gated — follows the ephemeris, not the render). VERIFIED: engage → scrub +40 min → heading/pitch 238.0/20.8 vs moon 238.1/20.9 (glued); drag releases; past-moonset self-releases. **(d) /m FIND = dedicated 4th tab + sticky standings (Pixel bug root-caused):** the old PlanSheet FindSection cleared the ghost mirror on sheet unmount — collapsing the config KILLED the in-frame standings by design. NEW `mobile/FindSheet.tsx` mounted UNCONDITIONALLY (hooks alive, `if (!open) return null` after them — the shared-window precedent), the single `_syncGhosts` writer on /m; **find.open STICKY on /m** (first visit or menu toggle turns it on; collapse leaves projections in frame; teardown-only cleanup); TabBar → SCENE·PLAN·FIND·SEARCH. VERIFIED: sheet scrim-closed → ghosts alive + in-sky `16.08 · 18%` label rendering (shot uxbatch2-04). TRAP: the sheet covers the tab bar — Playwright closes via scrim tap. **(e) Mobile-default entry:** FIRST index.astro body script client-replaces to `/m`+hash when mobile-UA ∧ coarse+no-hover ∧ multitouch (the M0 no-`Vary`/no-server-UA ruling STANDS — this is the sanctioned client seam); any query string skips; `/?d=1` persists `ftw:prefer-desktop`; the /m DESKTOP chip → `/?d=1` (bounce-loop guard); Welcome gains a **MOBILE VERSION** ghost anchor beside EXPLORE (`.wl-btn` made anchor-safe); topnav Mobile moved BEFORE Upload. VERIFIED via CDP device emulation: `/`→`/m` · `/?d=1` stays+persists · pref honoured · desktop predicate false. **(f) /m SAVED PLACES + quota dropped:** member-gated `▤ SAVED PLACES` chip above 🧭 MY LOCATION (opens the idle-SEARCH MY PLACES list; anonymous absent — verified; member path UNVERIFIED live this pass); `PLACE_QUOTA 50` → `PLACE_PAGE 1000` (Wix Data query max), the POST 402 gate DELETED, GET limit 1000, `quota` dropped from responses (no client read it — grep-verified). **(g) /m layout:** `.m-altcol` bottom 14.4→**16.4rem** (SAVE VIEW joined the stack — ⤓ stole its taps; rect-verified 612 vs 626 clear) · MiniMap NEW `body.m`-only collapse (`.mm--collapsed` 46px puck, right edge EXACTLY the altitude column's 390px, open by default, session-only; fence-legal — panels component + CSS). **Verify-env traps (NEW):** a hidden/minimized CDP Chrome window rAF-starves the globe and window-bounds changes do NOT fix it — **`Page.setWebLifecycleState {state:'active'}`** un-throttles (re-send per CDP session); positive mobile-redirect testing = UA override + `setDeviceMetricsOverride {mobile:true}` + touch emulation. Files: `store/{find,sky}.ts` · `panels/{SkyContextMenu,FindPanel,TargetPanel,Welcome,MiniMap}.tsx` · `mobile/{FindSheet(NEW),PlanSheet,MobileShell,TabBar,TargetSheet,SceneActions}.tsx` · `globe/{StylizedTiles,tuning}.ts` · `lib/wix/placeRecords.ts` · `pages/{index.astro,api/places.ts}` · styles `{welcome,mini-map,target-panel,mobile/{chrome,fpv}}.css` · tests `{store/find(NEW),lib/wix/placeRecords}`. Open tails: real-device Pixel pass (T1) · SAVED PLACES member-path live check · owner taste (TRACK label, 450ms ease, 9-item menu length, MOBILE VERSION copy, puck glyph). Memory: `mem:project/wip-2026-08-15-uxbatch2`.**

- **2026-08-15c (GUIDE TRACK OPENED — owner order: a dedicated multi-session effort for an in-app, mobile-friendly user guide to ALL planning features, reflecting capabilities PRECISELY.)** NEW plan doc **`GUIDE_PLAN.md`**: ground rules (accuracy-first — every claim browser-verified before copy; design fence holds; ONE content data module `guideContent.ts` rendered by a desktop GUIDE panel + a /m Sheet twin; build on/absorb the FAQ island, never two help systems), the full feature-inventory checklist (time/scrubber · PLAN+FIND window · TARGET/ghosts · sky interactions · FPV · save-share-places · search · photo-secondary · /m shell), recommended hybrid UX (chaptered GUIDE panel + 3 coach-mark journeys + Welcome entry), sessions G1 (skeleton + 2–3 chapters + FAQ decision) / G2 (chapters + journeys) / G3 (taste buffer). NEXT_SESSION_PROMPT re-aimed at G1; Phase 8c / M4 / §3.5-V2 queue behind the guide. Manual ship this session (owner "commit and push everything") via the session-end-ship path.**

- **2026-08-15b (OWNER UX BATCH ×5 SHIPPED both shells — PLAN/FIND one toggle window · ghost first-gap root-caused + 15/side · daytime nav scrim · /m login + MY PLACES + SAVE VIEW · scrubber past/future colours. Gates vitest 870/870 (+4) · astro 0 err/5 hints; desktop CDP-VERIFIED (shots `verify-shots/uxbatch-01..04`) + phone-viewport 402×874 (`uxbatch-05/06`) incl. a REAL hosted-login round trip on /m.)** **(a) PLAN/FIND → ONE topnav toggle + shared window:** the two pills leave the floating column for a segmented `PlanFindToggle` island beside the Sidera wordmark (`.topnav-left` cluster; FIND renamed **FIND IN FRAME**, `.pft-ext` collapses <1100px; welcome-hidden like the pills were); PlanPanel/FindPanel keep owning content but mount at the SAME left-column spot under ONE `"planfind"` drag/resize session key — to the user it is one window whose content switches, and it KEEPS its dragged spot + user size across the switch (browser-measured (86,94) 366×403 on both faces). NEW resize primitive `usePanelResize`/`ResizeGrip` in ui/DragGrip.tsx (◢ corner handle, border-box `--win-w/--win-h` vars, double-click reset, same session-Map memory) — cards went `box-sizing: border-box` with the default cap re-expressed (`max(11.9rem, min(27.9rem, 100vh−39.8rem))` = the old content budget + the ~1.9rem padding that used to ride on top; a user resize lifts the cap, `var(--win-h)` wins both slots); in-window `.pp-x` close; width unified 17.25rem. **TRAP found live: shared-key session state.** Both panels stay mounted (null-render keeps hooks + ghost-mirror cleanup alive), so `useState(() => sessionMap.get(key))` initializers go stale when the SIBLING writes the key — fixed by render-time adoption in both hooks (legal derived-state setState; same-key siblings never render simultaneously). **(b) Ghost stepping (owner "weird first gap + wrong count"):** root cause = `GHOSTS.nowGapDiscs 1.2` — a 1.2×disc-DIAMETER exclusion cone around the live body ate k=±1…3 at 1–2 min steps (diurnal ≈0.25°/min vs 0.53° disc), reading as BOTH symptoms; the loop was already symmetric N-per-side with exact `sceneMs+k·step` instants. Retuned **1.2 → 0.25** (only a near-concentric ghost drops — celestial-pole movers; the first step ALWAYS survives) + `maxPerSide` **8 → 15** (30 discs; clamps in sky.ts/prefs.ts/tuning + both ± UIs). NEW `test/components/globe/skyGhosts.test.ts` (4): drives the real InstancedMesh headlessly — 2N semantics, k=±1 survival at 1 min, 30 ceiling, and the bug's arithmetic pinned (cone < 1 min of solar drift). FINDGHOSTS.nowGapDiscs (day-spaced rings) deliberately untouched. Shots: the 1-min chain hugging the disc; 15/side · 5 min full corridor. **(c) Daytime nav legibility:** `.topnav` had NO scrim over the sky canvas — secondary-grey links ~1.3:1 on `--color-sky-day`. Fix in global.css (NOT page-scoped — the nav's React islands render their own buttons): top gradient scrim `color-mix(bg 58%→26%→transparent)` + dark text halo on `.topnav a, .topnav button`; night cost invisible. **(d) /m login + MY PLACES + SAVE VIEW (owner: save/access FPV views incl. time on mobile):** NEW `MobileAccount` strip chip (anonymous → SIGN IN via `loginUrl(returnHereUrl())` resolved at CLICK time — the MemberBadge hash-rides-the-hop discipline, VERIFIED live: the `#p=` pose survived the real dev OAuth round trip; member → `◎ <label>` opens the SEARCH sheet) · NEW `MobilePlaces` — the idle SEARCH sheet doubles as the saved-views list (typing swaps to results; anonymous sees a sign-in row), tap = time-FIRST restore then `requestFpvJump` (VERIFIED: exact `timeMs` 1786789225595 back + tempFpv on + sheet closed) · `SavePlaceChip` in SceneActions — one-tap ◎ SAVE VIEW (auto title `View · <local stamp>`, `⏱` suffix when pinned; anonymous → ◎ SIGN IN TO SAVE; same `/api/places` body as desktop SavePlaceControl incl. `timeMs: live ? null : sceneTimeMs()`); delete/rename stay desktop. All fence-legal (stores only; mobileFence + lazyContract green). MOBILE_PLAN exclusion block clarified: places ≠ pins-browsing. **(e) Scrubber past/future:** NEW chrome token `--color-time-future #7cb0f5` (the cool twin of `--color-warn`; danger/warn precedent → NOT in the GL bridge) — cursor + offset readout + /m dock cursor + /m time chip all split pinned into **amber = behind the wall clock / blue = ahead** (`ts-cursor--future`, `md-cursor--future`, `m-chip--future`; ff-danger precedence kept by exclusive class pick in TSX). Browser-measured: past rgb(232,162,104) / future rgb(124,176,245) / LIVE teal on both shells. **Env note:** the member-store "anonymous while /api/places 200s" scare = the Vite dep-cache 504 on `@wix_members.js` (the documented trap; `touch astro.config.mjs` fixed it live). Files: `panels/{PlanFindToggle(NEW),PlanPanel,FindPanel,TimeScrubber,TargetPanel}.tsx` · `ui/DragGrip.tsx` · `mobile/{MobileAccount(NEW),MobilePlaces(NEW),SceneActions,MobileShell,MobileSearch,MobileTimeDock,TargetSheet}.tsx` · `globe/{tuning,scene/skyGhosts}.ts` · `store/sky.ts` · `lib/prefs.ts` · `pages/index.astro` · styles `{plan-find-toggle(NEW),plan-panel,find-panel,drag-grip,global,tokens,time-scrubber,mobile/{chrome,dock,mobile}}.css` · `MOBILE_PLAN.md`. Owner taste knobs NEW: `--color-time-future` hue · `.pft` segment styling · window default 17.25rem/cap · `nowGapDiscs .25`. **(f) Same-session owner follow-up (browser-VERIFIED, shot `uxbatch-07-moon-ghosts-behind.jpeg`):** ghosts now render BEHIND the real body — `sunMesh`/`moonMesh` renderOrder → **11**, above the depth-free overlay tier (ghosts/trail/arcs/find rings all at 10; safe vs the atmosphere dome — the day contribution is additive and addition commutes; the moon's night-arm opaque disc now also replaces ghosts behind it, which IS the ask) — the k=±1 ghosts slide behind the crescent limb instead of washing the disc · FIND IN FRAME default bodies → **moon-only** (sun/GC chips off — owner: "all three create clutter"; one tap re-enables; §3.5 SUNSETS chips independent) in FindPanel + the PlanSheet twin. Memory: `mem:project/wip-2026-08-15-ux-batch`.**

- **2026-08-15 (§3.5 SUNSETS-IN-FRAME SHIPPED both shells — the event-anchored FIND twin. Gates vitest 866/866 (+10) · astro 0 err/5 hints; desktop + phone-viewport browser-VERIFIED (shots `verify-shots/sunsets-01-desktop-panel-jump.jpeg` (payoff: post-jump sun disc AT the skyline in-frame), `sunsets-02/03-mobile`).)** **(a) NEW pure lib `lib/ephemeris/sunEventFrame.ts`** (10 tests `test/lib/ephemeris/sunEventFrame.test.ts`): the event-time-anchored day loop the spec called for — per `localDayWindow` local day, ≤2 root-finds/kind (`SearchRiseSet` rise/set with eye-dip · `SearchAltitude` at the `goldenElevationsDeg` render-bell edges, planner-verbatim); **the §3.5 refraction pairing implemented and PINNED**: refracted LABEL instants + AIRLESS frame/skyline geometry, and deliberately **NO `altDeg > 0` floor** (the frameFinder predicate would drop every sunset — its airless centre sits at ≈ −0.83° at the refracted instant; test pins `altDeg < 0` while in-frame). Frame test folds the **solar DISC extent** in (`angularRadiusRad` → per-axis normalized pads `tan(r)/tanHalfV(/aspect)` — the limb catches frames the centre-only test misses; frameFinder gap #6 closed for this surface). Rise/set skyline verdict asks the RIGHT question — "is the true horizon visible at that azimuth" (`profile ≤ SUNEVENT.horizonClearMaxDeg .5°`), never centre-vs-profile (which would read every sea-horizon sunset "blocked"); golden kinds sweep their band at `goldenStepMin 5` and report the FIRST in-frame instant, centre-vs-profile verbatim. Per-kind **az drift °/day** seeded from day −1 (first row real; wrap-safe). House two-face split: `sunEventDays` pose-FREE (root-finds survive look-drags) / `sunEventFrameHits` pose-cheap / `sunEventInFrame` one-call compose. `GoldenCurve` stays a parameter (planner discipline — lib never imports tuning). **(b) Desktop surface (owner de-burying order — INSIDE FindPanel, superseding the spec's PLAN-card placement):** SUNSETS · IN FRAME section under the standings — SET/RISE/GOLD chips (SET default), shared range chips, `NEXT · <date> <time> <kind>` headline, rows `date · hh:mm · kind · CLEAR/✕ · ±°/d` with light dots + per-row `.pp-ics` (`icsForSunEvent`), day-KEYED stage-1 memo (`dayKey`, never `minuteKey` — sunsets don't depend on the scrub hour) + pose-key gate; jump = `setTime` + track sun, camera unmoved. VERIFIED: 54 sunset hits in a 68° west frame /6M, Aug times 7:56p→7:43p walking −0.5°/day, row jump landed the EXACT almanac instant with the dimmed disc on the skyline; the blanket ✕ honest (measured skyline 13.9° at 293°). **(c) Mobile twin `PlanSheet SunsetSection`** (sibling section after FIND — the accepted two-shell dup; no ics, compact m-rows): VERIFIED 11 events in the narrower phone frame (the frame IS the query), row jump exact + camera unmoved, GOLD toggle live (33 events; ±2 min wobble = the 5-min sweep cadence, documented). No ghost projection for event rows this slice (the `_syncGhosts` single-writer would clobber; the sunset CORRIDOR arc-pair stays the spec's V2 stretch — T8's natural vehicle). Files: `lib/ephemeris/sunEventFrame.ts` (NEW) · `panels/FindPanel.tsx` · `mobile/PlanSheet.tsx` · tests. Memory: `mem:project/wip-2026-08-15-sunsets-in-frame`.**

- **2026-08-14 night-6 (FIND standings: sky-aware pick floor + label lift (owner bug) + MOBILE M3c — TARGET-sheet GHOSTS · long-press sky menu · tap-to-reveal. Gates vitest 856/856 (+5) · astro 0 err/5 hints; desktop CDP-VERIFIED (twilight moon 6/6 hover incl. the reported 12.01·21% + day sun corridor 24 standings, shots `verify-shots/find-hover-fix-01/02`) + phone-viewport 402×874 on /m (shots `mobile-m3c-01..04`); REAL-DEVICE tier stays open (T1).)** **(a) The owner's "random standings ignore hover" bug — root-caused, fixed:** `findGhosts.pick()` gated on a FLAT `pickMinAlpha 0.08` while at civil twilight (sun −5.8°) EVERY moon standing's effective alpha ran 0.026–0.079 (date-ramp × engine visibility × melt) — rings plainly visible (ringAlphaGain 2.2 widens the drawn band on a dark sky) yet mouse-dead; "random" = the twilight visibility score drifting across the flat threshold minute-by-minute (browser-measured; model matched the rendered label DOM to 4 dp). Fix = NEW pure `findPickFloor(sunAltDeg)` (exported; 5 tests `test/components/globe/findPickFloor.test.ts` incl. the −5.8° repro pin): smoothstep `pickMinAlphaNight .015` ("if it draws, it responds"; shader discard ≈.012) → `pickMinAlphaDay .08` (a washed-invisible day ghost must not steal a sky click) across sun-alt `−8°→+2°`; update() recomputes per frame from `sunDirW·geocentric-up`; pick() + the label gate ride it. **Label gate UNIFIED with interactivity** (hoverable ⇔ labeled; `labelMinAlpha` knob deleted) + **display lift** `labelLiftA .32` (opacity = lift + labelA·(0.95−lift)): twilight labels 0.02–0.07 → 0.33–0.36, day 0.42 → ~0.57 (owner: "date texts get lost in day/twilight"). Files: `scene/findGhosts.ts` · `tuning.ts` (FINDGHOSTS.pick* ×4 + labelLiftA). **(b) M3c (MOBILE_PLAN):** GHOSTS controls → mobile TARGET sheet (`mobile/TargetSheet.tsx` 4th toggle + `.m-ghostrow` −/+ steppers + native MIN select — the tp-ghostrow twin at touch scale in chrome.css; pure skyStore writes, prefs round-trip desktop⇄mobile verified 8→7 / 1→10 MIN) · **long-press sky menu on /m** (`SkyContextMenu` island now mounted from `m.astro` (the MiniMap fence-exemption precedent); the M1 long-press timer arbitrates at FIRE time — sky-body hit ≥ `skyMenuMinAltDeg` → `setSkyMenu` (works in FPV too, the only long-press FPV honours; flag SURVIVES orbit onPointerUp for onFpvPointerEnd's swallow), else the orbit pin-drop as before; menu card gained a viewport clamp (useLayoutEffect margins — also fixes desktop edge overflow); VERIFIED: fires mid-press on the moon, survives lift, no FPV pin drop, next tap dismisses) · **tap-to-reveal (tap = the mobile hover):** a TOUCH tap on open sky that hits nothing interactive parks a synthetic hover latch (`tapRevealX/Y/Until`, `ORCH.tapRevealMs 2000`) — `stepSkyHover` runs its WHOLE cascade off it (body glow, FIND-ghost pulse + row highlight, night star names; name label lifts `tapRevealLiftPx 26` out from under the finger); VERIFIED: Arcturus·Boötes revealed @0.85 then auto-fades. **Touch hit pads:** `ORCH.touchHitPadK 1.7` on coarse pointers multiplies the sky angular picks (pickSkyBody + `findGhosts.pick(rayDir, padK)`) — desktop stays ×1. **Find-in-frame organic on /m** (owner order): PlanSheet FIND writer + the lifted labels + widened tap pads verified end-to-end — tap on the 16.08 standing (the exact ghost that was label-hidden AND tap-dead pre-fix) jumped scene time EXACTLY to its instant, tracked the moon, camera unmoved. Desktop mouse-hover regression re-verified post-M3c (6/6 + labels; the "0 labels" scare = the rAF tab-throttle trap, not code). Memories: `mem:project/wip-2026-08-14-night6-hover-floor` · `mem:project/wip-2026-08-14-mobile-m3c`.**

- **2026-08-14 night-5b (MOBILE M3a + M3b — dock v2 conveyor + the PLAN-sheet twins (THIS FRAME · TODAY · FIND v2 · MOON · SPOT STARS). Gates vitest 851/851 · astro 0 err/5 hints; phone-viewport 402×874 browser-VERIFIED on /m (CDP :9222), shots `verify-shots/mobile-m3a-01-dock-v2.jpeg` + `mobile-m3b-01/02`; REAL-DEVICE tier stays open (backlog T1). M3c NOT started — next: GHOSTS controls into the TARGET sheet, long-press sky-menu trigger, tap-to-reveal hover-names.)** **(a) MobileTimeDock v2 (M3a):** the old clamped-±12 h knob model REPLACED by the desktop conveyor at phone scale — scene time rides a FIXED CENTRE CURSOR (accent live / warn pinned, pennant handle), dragging slides the timeline (drag left = forward; measured EXACT: −200 px = +6.43 h predicted +6.43 h), full `lightSegments` photographic bands (desktop hues verbatim — day/golden/blue/nautical/astro/night), compact sun/moon altitude curves + the tracked-target trace with FPV in-frame emphasis (strokes one step lighter: 4/2/1.4 vs desktop 5/2.4/1.6), REAL local hour ticks + labels every `SCRUB.hourLabelEvery` (midnight = full-strip boundary), outer-zone taps = next/prev almanac event (closed-panel on-demand almanac fallback), dblclick-middle = NOW, keyboard slider parity; span memo = 2× window on the scene HOUR + 0.05° eye (never per pointermove); knob/`md-twilight`/recentre effects deleted. Rail 40 px = 2/28/10 geometry (the .ts-rail 48/3/32/13 twin — comment pins the lockstep). Files: `mobile/MobileTimeDock.tsx` (rewrite) · `styles/mobile/dock.css` (rail internals rewrite; md-light/md-curves/md-ticks/md-cursor). SVG path builders = accepted two-shell duplication (byte-compact twins of TimeScrubber's, same 0..100×0..40 contract). **(b) PlanSheet v2 (M3b):** five compact self-computing twins in the desktop deck order, all on the same pure libs + stores (no desktop-panel imports): THIS FRAME (36 h crossings + ◎ CENTRE chip; FPV-gated) · TODAY (self-computing chronology with light dots + the R5 sun-at-moonrise signal — supersedes the M1 raw "JUMP SCENE TIME" chips) · **FIND IN FRAME (the v2 frame-as-query twin — the sheet IS the store/find ghost-mirror WRITER on /m: mount sets find.open so the projections draw on the mobile globe, unmount clears; same two-stage memo + the `active` FPV gate (the boot-flight-freeze lesson), identity colours via `findStandingColorIdx`, verified: 38 standings @60° FOV, 24 projected, row tap = exact jump (20:05 wall-clock preserved) + camera UNMOVED + body tracked, sheet close → ghosts 0)** · MOON (8 rows + supermoon chip) · SPOT STARS (NPF with frame-auto declination). NEW m-* row grammar in chrome.css (m-rows/m-row/m-dot--phase/m-sw/m-npf — the pp-day__row twins at touch size). Also: `MobileShell` brand string "Frame the World" → **"Sidera"** (the night-4b rename missed it — `&nbsp;` entities defeated the grep; lesson: brand sweeps must grep `Frame&nbsp;` too). **Verify traps (NEW):** a background/unselected tab THROTTLES setTimeout to ~1/min — a 60 s poll loop becomes a 2-min hang (the rAF-suspension trap's timer sibling; keep the verify tab window-selected for the WHOLE session) · the `/m` `#f=` FPV hash entry engages only after tiles+terrain stream around the pin (~1–3 min on the mobile tier vs ~20 s desktop — poll `__globe.fpv().active`, don't fix a timeout budget). Memory: `mem:project/wip-2026-08-14-mobile-m3ab`.**

- **2026-08-14 night-5 (OWNER ORDER — FIND accuracy audit at long focal ("predictions seem to jump unnaturally") → VERDICT: MATH ACCURATE, one real churn bug fixed (identity colours) · in-sky dd.mm labels + moon phase % · sun horizon extinction. Gates vitest 851/851 (+9) · astro 0 err/5 hints; desktop browser-VERIFIED CDP :9222 wix dev :4321, shots `verify-shots/{find-labels-01-corridor,find-labels-02-moon-phase,sun-extinct-A-high,sun-extinct-B-horizon}.jpeg`; owner-device taste tier open.)** **(a) Accuracy audit (owner pose `#f=48.465542,35.069851,1.7,297.5,2.1,7.4`, 185 mm/7.4°):** scratch-vitest ladder dump over 365 d — consecutive in-frame days drift smoothly 0.13–0.22°/day, monotonic per branch, NO discontinuities; the "jump" = a 269-day GAP (05.08→01.05): at that wall-clock hour the sun leaves the frame for autumn/winter and re-enters in spring, and the TWO seasonal branches CRISSCROSS in the same frame corner (Aug branch descends fx −0.53→−0.99, the May branch climbs back through it) — the caterpillar corridor is real physics, not error. Ghost-vs-render agreement measured **0.0024°** for the sun (sub-pixel at 185 mm; geocentric impostor vs topocentric-at-anchor ghost), moon topocentric-vs-geocentric 0.976° CONFIRMS both pipelines correctly avoid the geocentric moon dir (ghost = topocentric `horizontal()`, render = true-position per-camera — the night-3 parallax class). **Near-horizon refraction: the whole scene is AIRLESS by convention** (`horizontal()` no-refraction = almanac airless rows; the impostor renders the geometric direction) — self-consistent in-app, but vs the REAL sky a horizon standing appears ~0.46° HIGHER (measured at alt 0.157° → apparent 0.618°) and real sunset touch is ~3–4 min later than the render; §3.5's "refracted-labels / airless-geometry" convention already codifies this — surfaced to the owner, NOT changed. **(b) The one real churn fixed — identity colours now keyed to the DATE, not the row:** `colorIdx` was the row index, so ANY hit entering/leaving (minute scrub past a boundary, ~1° pose quantization step at the frame edge) recoloured EVERY ring downstream — the ladder "jumped" without moving. NEW `findStandingColorIdx(body, utcMs)` in findPalette.ts = `floor(utcMs/day) + bodyOffset{sun 0, moon 3, gc 6}` — glued to the standing's UTC day (adjacent days keep adjacent wheel slots → warm/cool interleave preserved; same-day bodies differ). Browser-measured: 24/24 ghosts keep colours across a ±2 min scrub (before: all 24 recoloured on any set change). **(c) In-sky standing labels (owner: "subtle date dd.mm near each standing; moon adds phase %"):** NEW `formatStandingLabel(utcMs, illum?)` (readout.ts, local dd.mm, moon `14.08 · 62%`; `FindGhost.illum` NEW field) + a pooled DOM label layer in scene/findGhosts (the skyNames/geoLabels discipline — design fonts, transform-only per-frame writes, `dataset.k` guards text/colour); label = identity colour + bg text-shadow, opacity = ghost's OWN effective alpha × `labelAlphaK .85` (melts/fades/pulses with its ring), offset right of the ring by its screen radius + `labelPadPx 6`; screen-space de-clutter `labelMinSepPx 34` in date order (nearest dates win; the HOVERED ghost's label always shows), NDC 1.02 cull, `labelMinAlpha .06` floor; hidden with the overlay fade + on panel close (browser-verified 0 visible after close; corridor showed 7 of 24 labels ≈ every-other-day at 185 mm — right density). Moon tags verified waxing day-by-day ("19.07 · 33%" → "22.07 · 62%") at 80° FOV south. **(d) Sun horizon extinction (owner cosmetic order):** NEW pure `sunExtinctionK(sunAltDeg, skyK)` in scene/sky.ts (moonDiscArms twin discipline, 4 tests) — smoothstep 1 → `SKY.sunExtinctFloor .4` as sun altitude falls `SKY.sunExtinctAltHiDeg 10°` → 0°, relaxed to 1 by the atmosphere-presence ramp (skyK from ATMOSPHERE.skyFullAlt/GoneAlt — no air in orbit, no extinction; deliberately NOT the physical airmass exponential which is ~10⁻³ at the horizon and would erase the disc, C2); NEW `uExtinct` uniform multiplies the WHOLE impostor colour (core + halo → the bloom driver dims with it), separate from uIntensity so the hover lift never fights it; sinSun/skyK derivation HOISTED above the sun anchor and shared with the moon's uDaySky (one derivation, two consumers). A/B shots: blazing disc at alt 24° vs visibly softened at 1.2°. **Verify env notes:** `setFovRate(+)` ZOOMS IN (fov shrinks) — negative widens; sun A/B aiming via `__cameraStore.requestSkyLook`; two 3d-tiles-renderer fade-plugin TypeErrors ("reading 'range'") appear during big look swings — library-internal tile-visibility race, pre-existing, not ours. Files: `lib/theme/findPalette.ts` · `lib/format/readout.ts` · `store/find.ts` · `panels/FindPanel.tsx` · `scene/{findGhosts,sky}.ts` · `globe/tuning.ts` (FINDGHOSTS.label* ×4 · SKY.sunExtinct* ×2) · tests `findPalette`(+3) `readout`(+2, in 1 block) `sunExtinction`*(+4). Owner taste knobs NEW: `labelAlphaK .85 / labelMinSepPx 34 / labelPadPx 6` (label font hardcoded 0.52rem in findGhosts) · `sunExtinctFloor .4 / sunExtinctAltHiDeg 10`. Memory: `mem:project/wip-2026-08-14-find-accuracy-labels`.**

- **2026-08-14 night-4b (OWNER FOLLOW-UP — FIND markers restyled ("circles too fat") · phase-accurate body pictures + per-hit sky paths · scrubber in-frame band emphasis · app working title → SIDERA. Gates vitest 842/842 · astro 0 err/5 hints; desktop browser-VERIFIED CDP :9222, shots `verify-shots/find-rework2-01..04`; owner-device taste tier open. Env: wix dev port flipped 4322→4321 mid-session (the known Vite trap).)** **(a) Ghost markers (scene/findGhosts rework):** the fat filled ring band is GONE — each sun/moon hit is now (1) a HAIRLINE broken identity ring (the SKY.hoverRing quad-gap grammar; `ringWidthN .07` + NEW `ringAlphaGain 2.2` clamped in-shader — round-2 lesson: a hairline needs alpha compensation or the identity colour vanishes against the day sky) + (2) inside it, a semi-transparent ACCURATE body picture at true disc scale — sun = limb-darkened warm disc (sunCore→sunGlow), moon = the LROC `SKY.moonTexture` phase-lit for THAT hit's instant (per-instance world sun-dir from `bodyStatesAt(hit.utcMs)` banked at rebuild, rotated into billboard space per frame by the inverse camera quaternion, sphere-normal dot lighting + `earthshine .08` floor — the real impostor's recipe on a quad; `bodyAlpha .6`); GC keeps a slim diamond + core dot. Texture sampled OUTSIDE non-uniform branches (GLSL derivative hygiene); ring-over-picture composited un-premultiplied. **(b) Per-hit SKY PATHS:** each sun/moon hit's full day-arc for ITS local day (`sampleDayArc`, `pathStepMin 20`) drawn as ONE LineSegments in the hit's identity colour — the dayArcs unit-dir + group-position/scale anchoring trick (far-plane changes never rebuild); per-vertex colour/alpha/hit-index, horizon melt per vertex; alpha = ghost ramp×visibility × `pathAlphaK .3` (round-2 retune 0.5→0.3: near-coincident sun arcs STACK 1−(1−α)^N — a soft seasonal corridor, not a scratch bundle) with `uHotIdx` hover boost ×3 (row OR canvas hover singles one path out); day arcs CACHED per (body, utc-day) `pathCacheMax 128`, cleared on anchor move — scrubbing minutes never resamples. GC: no path (it barely moves day-to-day). **(c) Scrubber (styles/time-scrubber.css):** semantics now explicit in the comment — base ribbon = ABSOLUTE visibility above the horizon, THICK band = visible IN THIS FRAME; `.ts-curves__trace-frame` 3.6px@65% → **5px@78%** + glow 2px@45% → 3px@55% (clear 2.4@52% and blocked dashes untouched). **(d) Working title → SIDERA (owner order; UI-facing ONLY — repo + all technical identifiers stay "frame the world"):** Layout/MobileLayout default titles + og:image:alt, index.astro `<Layout title>` + topnav logo, m.astro, UploadFlow brand chip, all four .ics summary suffixes "(Frame the World)" → "(Sidera)" (`ics.ts` PRODID kept — technical). **(e) Cover slogan re-aimed at planning/photography/exploration (owner: uploading = "very last priority, a side gimmick"):** eyebrow "EVERY PHOTOGRAPH HAS COORDINATES" → "EVERY FRAME HAS ITS MOMENT", title "See your photographs where the world took them." → **"Plan the shot where the world will take it."** (the deliberate past→future mirror), sub = stand-anywhere/scrub-time/frame-the-sky copy with the upload sentence DROPPED; byte-mirrored in the boot poster (index.astro .bp-*) AND Welcome.tsx (.wl-* — the no-text-jump handoff contract); CTA emphasis FLIPPED — `.wl-btn--primary` = EXPLORE THE GLOBE →, `.wl-btn--ghost` = UPLOAD A PHOTO (behaviours unchanged, classes swapped; **verify-recipe break: `.wl-btn--ghost` now OPENS THE UPLOAD OVERLAY — dismiss via `.wl-btn--primary`**, NEXT_SESSION pointer updated); Layout.astro meta/og description rewritten planning-first (upload demoted to a trailing "photos can join the globe too"). Cover browser-verified, shot `verify-shots/find-rework2-05-cover-slogan.jpeg`. **Traps recorded:** React row-hover from probes needs `mouseover` dispatch (React delegates; synthetic `mouseenter` doesn't bubble → no-op) · a hash-only `goto` on the same origin is a SAME-DOCUMENT navigation — the app never reloads and rewrites its own `#f=` (verify recipe: about:blank between loads, ALWAYS). Files: `scene/findGhosts.ts` · `globe/tuning.ts` (FINDGHOSTS: ringWidthN/ringAlphaGain/ringGapFrac/bodyAlpha/earthshine/pathStepMin/pathAlphaK/pathHotBoost/pathCacheMax; ringWidthFrac retired) · `styles/time-scrubber.css` · `layouts/{Layout,MobileLayout}.astro` · `pages/{index,m}.astro` · `panels/{UploadFlow,FindPanel,TodayCard,FrameCard,MoonCalCard}.tsx`. Owner taste knobs: `ringAlphaGain 2.2` · `bodyAlpha .6` · `pathAlphaK .3` · `pathHotBoost 3` · `ringWidthN .07`. Memory: `mem:project/wip-2026-08-14-find-rework` §rework-2. UNVERIFIED fine-grain: crescent ORIENTATION on the phase picture (shader follows the impostor recipe; too small to confirm in shots), GC diamond in-frame visuals, night-sky look.**

- **2026-08-14 night-4 (FIND v2 OWNER REWORK — dedicated FIND panel, "the frame IS the query" (NO az/el inputs) · per-day scan at the EXACT scrubber time-of-day · ALL standings projected into the frame as per-hit-coloured ghost rings (alpha = date × visibility) · click a ghost/row = jump to that day + track the body, camera unmoved · FindCard REMOVED from the PLAN deck. Gates vitest 842/842 (+14) · astro 0 err/5 hints; desktop browser-VERIFIED headed Chrome CDP :9222 on wix dev, shots `verify-shots/find-rework-01..03`; owner-device taste tier open. §3.5 sunsets-in-frame deferred to next session.)** **(a) Owner semantics questions ANSWERED + fixed:** the shipped QoL-3 FIND did NOT depend on frame size/orientation (hardcoded `azTolDeg 3/elTolDeg 0.5` literals, zero `fov` reads in the card) and did NOT scan a fixed hour (azElHits root-finds an azimuth CROSSING at any time of day; the ±0.5° el gate is why it found so few options). v2: the window IS the live frustum (`azAltFrameMarker` over heading/pitch/fovDeg/aspect — zoom AND orientation shape results; browser-measured 45 standings at 60° FOV → 0 at 3° telephoto, live), the instant is the scrubber's LOCAL wall-clock time on every following day, bodies = ☀/☾/✦ GC. `azElHits` engine + tests stay (unreferenced by UI). **(b) Engine (`lib/ephemeris/frameFinder.ts` FIND-v2 section):** two-face split so the panel memoises them separately — `bodyDayPositions(sampler, instants)` (pose-FREE ~30 µs/sample ephemeris pass) + `frameStandingsFromPositions(body, positions, pose, profileFn)` (cheap frustum filter + annotations on survivors only: skyline verdict, lightPhaseAt, moonIllum/moonGlare) + one-call `frameStandings`; `FrameStanding.visibility` 0..1 = the ghost-alpha model `FIND_VIS` (moon `0.25 + 0.75·illum` · GC smoothstep sun-alt −8°→−18° × (1 − 0.65·moonGlare) · skyline-blocked ×0.35 · sun 1); `FrameMarker` gained `fx/fy` normalized frame coords (offscreen.ts — position hints, behind-camera → 0); NEW `store/time.sameLocalTimeInstants(fromMs, days)` (Date#setDate walk preserves the LOCAL hour:minute across DST — a flat +24 h step drifts an hour; day 0 excluded, the scene shows it). 14 new vitest (frameStandings 9 incl. zoom-dependence + orientation zero-result + visibility monotonicity; palette 3; offscreen fx/fy; instants local-time-preservation over 400 days). **(c) Ghost projections (NEW `scene/findGhosts.ts` + `FINDGHOSTS` tuning group):** ONE InstancedMesh of camera-anchored billboards on the SKY_TARGET impostor shell — HOLLOW ring (sun/moon, `ringRadFrac 1.6` × apparent radius, floor `minDiscDeg 0.55°`) or diamond (GC, L1 metric in the same shader via per-instance aShape) + small core dot; per-instance colour from NEW `lib/theme/findPalette.ts` (10 sanctioned tokens, warm/cool interleaved, sunCore/sunGlow/moonlight EXCLUDED — a ghost must never wear the real body's colour; hollow + alpha-blended + depth-free = never confusable with the filled/additive real discs); alpha = `alphaNear .5→alphaFar .12` date ramp × engine visibility × horizon melt × whole-overlay fade; same-body now-gap (`nowGapDiscs 1.2` — tomorrow's sun sits ~0.3° from today's and would bite the live disc; uses the TOPOCENTRIC moon dir `moonPosW − camera.position`, the night-3 parallax lesson); `pick(rayDir)` angular hit test (pickSkyBody discipline, `pickMinAlpha 0.08` — a faded ghost is click-transparent). Data flows PANEL → `store/find` mirror → scene (single writer; ghosts capped `FIND_GHOST_CAP 24` = tuning `maxGhosts`, kept in sync by note). **(d) Wiring (StylizedTiles):** `stepFindGhosts` between stepSkyTarget and stepSkyHover (manifest updated); `tryFindGhostClick` in BOTH click paths (orbit onPointerUp after trySkyMarkerClick; FPV onFpvPointerEnd) → `setTime(hit.utcMs)` + `setTarget(bodyTarget|galacticCentreTarget)` + SHOW on, camera DELIBERATELY unmoved (the real body arrives where its ghost stood — browser-verified: heading 264.96 before AND after, timeMs === ghost.utcMs exact, `&t=` hash rebased, real moon landed az 243/alt 17.4 for a ghost at 242.6/17.7); ghost hover in stepSkyHover's cadence tick (reuses the just-seated `_pickRay`; bodies > ghosts > star-names priority; pointer cursor; identity-guarded `sceneHoverKey` write → panel row highlights — verified both directions). **(e) Dedicated panel (NEW `panels/FindPanel.tsx` + `styles/find-panel.css`, island in index.astro):** ⌖ FIND pill top-left NEXT TO ☀ PLAN (`.fnd-root left 7.3rem`; the two planning surfaces are MUTUALLY EXCLUSIVE open — verified both directions), 15 rem card reusing the `.pp-*` grammar; friendly context block "AT 18:52 EVERY DAY · NEXT 6M / LOOKING W 265° · FRAME 94°×60°" (cardinal + live h-FOV from `tan(fov/2)·aspect`); ☀/☾/✦ chips + 1W/1M/6M(default)/1Y presets; rows = colour swatch (matches the ghost; hollow beyond the ghost cap) + light dot + date + glyph + ◀▶▲▼ position hint (fx/fy) + CLEAR/✕ + visibility % + per-row .ics; row hover pulses its ghost (hoverKey), row/ghost click share the jump; non-FPV state explains the contract ("YOUR FRAME IS THE QUERY — THE SCRUBBER SETS THE HOUR"). FindCard.tsx DELETED; PLAN deck order now FRAME · TODAY · MOON · SPOT STARS · MW. Skyline bins need NO planFeed change — photo/FPV builds keep mirrors warm with PLAN closed (planFeed gate re-read, `:361-368`). **(f) TRAP caught live — panel self-compute must be GATED:** the stage-1 memo keyed on the camera-focus lat/lon churns EVERY FRAME during a flight → the ~550-sample pass re-ran per frame and froze the boot flight solid (rAF starved, evaluate hung 120 s). Fix: `active = open && poseKey !== null` gates stage 1 (compute only while the panel is open AND FPV stands). Corollary trap for verify: a Playwright-"current" tab is NOT the window-selected tab — rAF stays suspended until `browser_tabs select`; the NEXT_SESSION "keep the verify TAB selected" note is load-bearing. Files: `lib/ephemeris/frameFinder.ts` · `lib/geo/offscreen.ts` · `lib/theme/findPalette.ts*` · `store/{time,find*}.ts` · `scene/findGhosts.ts*` · `globe/{StylizedTiles,tuning}.ts` · `panels/{FindPanel*,PlanPanel}.tsx` · `styles/find-panel.css*` · `index.astro` · `global.d.ts` · FindCard.tsx deleted · tests `frameStandings*`(9) `findPalette*`(3) `offscreen`(+1) (+14). Owner taste knobs: `FINDGHOSTS.{ringRadFrac 1.6, minDiscDeg .55, alphaNear .5, alphaFar .12, hoverBoost 1.9, hoverScale 1.18, maxGhosts 24, gcMarkDeg .9}` (rings read small at wide FOV — first knob to try) · `FIND_VIS` constants · panel width/left offset. Memory: `mem:project/wip-2026-08-14-find-rework`.**

- **2026-08-14 night-3b (OWNER FOLLOW-UP — night moon blow-out + scrubber taller/trace ribbon. Gates 828/828 · astro 0 err; browser-VERIFIED, shots `verify-shots/qol4b-01..02`.)** **(a) "Moon got too bright" was a night-3 REGRESSION INTERACTION:** the tracked-body marker draws a star-like additive point glow whose magnitude gain CLAMPS AT MAX for the mag −12.7 moon (`pointAmp` → 1.5); before night-3 that glow sat 0.88° OFF the disc (parallax bug) and its on-disc fragments were DEPTH-REJECTED by the disc's depth wall — the two round-3 fixes centred it and un-rejected it, stacking a blown core+halo onto the night disc (the owner's screenshot). Fix `scene/skyTarget.ts`: **the point-body treatment never draws for kind sun/moon** (`uBodyFade = 0`, mesh hidden unless the reticle needs it) — the REAL rendered disc IS the body; the reticle keeps working. Plus the ordered gentle trim: `SKY.moonBrightness 3.2 → 2.9` (SHARED by both arms — if the day moon now reads dim, `moonDayAddGain 0.55 → ~0.61` restores the old day product; noted at the knob). Measured (89% gibbous, tracked+ghosts+highlight, the owner's setup): halo at 2 disc-radii → 8.8/255 (was a fully blown reticle interior), disc avg 167 / max 251 with crater texture back. **(b) Scrubber (owner "+20% taller, charts clumped"):** `.ts-rail 40 → 48 px` (curve strip 24 → 32 px — `.ts-curves` height + `.ts-tick--midnight` in lockstep; label row/ticks/cursor bottom-anchored, no other geometry touched; SVG stretches via preserveAspectRatio=none + non-scaling strokes). Target trace restyled as a WIDE TRANSLUCENT RIBBON vs the thin solid sun/moon hairlines: trace-clear 1.3 px @80% → **2.4 px @52%** · trace-frame 2.4 px solid → **3.6 px @65%** (shadow 70→45%) · trace-blocked 1 px @60% → 1.6 px @42%. Files: `scene/skyTarget.ts` · `globe/tuning.ts` · `styles/time-scrubber.css`. Memory: `mem:project/wip-2026-08-14-qol4-batch` §follow-up.**

- **2026-08-14 night-3 (QoL-4 OWNER BATCH — day-moon dark-chunk ROUND 3 (the REAL enabler this time, empirically pinned) · reticle 0.88°→0 (lunar parallax) · ghost disc-exclusion · hover ring + gain · sky hover NAMES · QoL-3 SHIPPED desktop (P4 FIND / P6 MOON / P5 NPF / R9 size→dist) · §3.5 sunset-in-frame spec. Gates vitest 828/828 (+39) · astro 0 err/5 hints; desktop browser-VERIFIED headed Chrome CDP :9222 on wix dev, shots `verify-shots/qol4-01..04`; owner-device taste tier open.)** **(a) Day-moon dark chunk ROUND 3 — SUPERSEDES night-2 (a)'s "impossible by construction" claim:** the additive day arm was correct but NOT sufficient — the disc's `depthWrite` was still punching a depth wall into the sky, and under FPV look-drags the additive dome intermittently lost the depth test against it → no sky painted behind the disc → `dayRgb` over the near-black `scene.background` = the owner's dark-textured chunks (bad renders persist several presented frames — hence screenshotable). Empirical chain: rAF `gl.readPixels` probe around the projected disc through scripted pointer-drag oscillations → 20 dark frames/485 at min 0.11–0.13× sky luminance WITH `uDaySky ≡ 1` (night arm off — night-2's fix held) and same-frame-consistent far/dome/moon geometry; ghosts EXONERATED by A/B (`setGhosts(false)` → dark frames persist; dark pixel RGB [19,19,14] = warm albedo-over-black, not ghost blue); `onBeforeRender` draw-order capture identical in good and bad frames (moon→dome→sun→ghosts); **`moonMat.depthWrite = false` live-toggle → 0 dark frames in 469 (baseline 20)**. Fix `scene/sky.ts`: moon material `depthWrite: false` — its depth serves NOTHING (terrain occlusion = depthTest, star occlusion = the night arm's alpha ≈ 1 replacing the stars drawn before the disc; noted cosmetic edge: a total solar eclipse would wash the additive sun through the disc). Discard kept as hygiene. Night regression argued + verified (89% gibbous over the starfield). **(b) Off-target reticle = LUNAR DIURNAL PARALLAX:** `targetDirW` was the GEOCENTRIC `TargetState.dir` (the code comment even asserted "every trackable target is far enough" — false since qol2 made the MOON trackable: topocentric offset up to ~0.95°, ≈2 disc diameters, measured 0.878° in-page; the reticle-above-moon direction in the owner's shots is exactly parallax's altitude depression). Fix `StylizedTiles.ts`: `sampleEphemeris` banks `targetPosW = dir·distanceAu` when finite; `stepSkyTarget` re-derives `targetDirW = normalize(targetPosW − camera.position)` EVERY FRAME (the sky.ts moon-disc move) → marker, click/hover hit tests and edge chip all land on the rendered body (measured 0.000°); stars/DSOs (distanceAu null) stay geocentric. `targetAzAlt` (ghosts/trail/panel) already subtracted the observer — this closed the one remaining geocentric seam. **(c) Ghost now-exclusion:** at the 1-min default step the k=±1 moon ghosts sit ON the disc and their NormalBlending alpha 0.62 dims a visible bite — `skyGhosts.resample` now skips ghosts within `GHOSTS.nowGapDiscs 1.2` disc-DIAMETERS of the now direction (nearest ghost measured 0.654° ≥ the 0.636° floor). **(d) Hover more distinct (owner):** `ORCH.skyHoverGain 0.25→0.32` + a NEW faint hairline broken ring that breathes in around the hovered sun/moon (`scene/sky.ts` hoverRing billboard, the reticle quad-gap grammar at hover scale; knobs `SKY.hoverRing{RadFrac 1.45, WidthN 0.016, GapFrac 0.22, Gain 2.2}`; positioned by `setHoverGlow` — call stays right after `sky.update`, which banks the camera). Verified: ring visible + uFade 0.32 eased, glow 3.2→4.22. **(e) Sky hover NAMES (owner: "reveal (very gently) their names"):** NEW pure `lib/sky/hoverNames.ts` (raDecUnit / arcDistRad great-circle-segment distance / buildHoverIndex / hitTestNames — tier priority star → asterism figure → constellation figure → constellation anchor; star pads scale by magnitude `SKYNAMES.starHit{Bright 1.1, Faint 0.5}°`, vmag gate 3.6 keeps every naked-eye Milky-Way-band landmark, so the "MW band stars" ask rides the star tier) + NEW `scene/skyNames.ts` (geoLabels-discipline DOM layer — pooled fixed div, pointer-events none, design fonts; HEAVY catalogs `starNames`/`constellations` dynamic-imported + the two figure JSONs fetched ON FIRST ELIGIBLE HOVER — lazyContract intact) + `stepSkyHover` extension (same cadence/ease; night-gated by the STARS nightVis ramp so no names on a day sky; bodies win ties; asterism tier only while the FPV tracery is visible; ECEF ray → J2000 via +GAST rotation; label follows the live cursor, eased alpha at cadence, outgoing text melts via skyNameLast). Verified in-browser: VEGA·Lyra on the star, SUMMER TRIANGLE·asterism mid-edge (guides on), label absent by day/off-sky. 12 new vitest (hoverNames). **(f) QoL-3 SHIPPED desktop:** P4 **FindCard** (az±3°/el±0.5° — R8 constants; presets 1W/1M/6M/1Y; ☀/☾/tracked-target chips; skyline-verdicted rows via the bins mirror; DATE↔LIGHT sort; ⌖ seeds az/el from the live frame; engine = NEW `frameFinder.azElHits[InDay]` — per-day azimuth ROOT-FIND (hourly Δaz sign flips, wrap-seam guarded, bisected ≤1 s) + el-tol check + joint-box edge bisection, ~1 ms/day → 1 Y in one memo pass; END-TO-END validated: row tap landed the sun at az 249.99/el 15.08 for an az 250/el 15 query) · P6 **MoonCalCard** (NEW `lib/ephemeris/moonCalendar.ts`: SearchMoonQuarter/NextMoonQuarter + SearchLunarApsis/NextLunarApsis walks, disc-arcmin + distance annotations from the ONE ephemeris, `SUPERMOON_MAX_KM 360_000`, ★ rows + NEXT SUPERMOON chip, TodayCard row grammar + .ics) · P5 **SpotStarsCard** (NEW `lib/photo/npf.ts`: npfFullSec (k 1–3, cos δ floored 0.05) / npfSimpleSec (D850 vector 16.3 s pinned) / rule500Sec / decAtAzAlt / `maxCosDecInFrame` — 9-point frustum sample, cos δ pinned to 1 when the sampled declinations straddle the celestial equator; FPV-only card, aperture+pixel-pitch inputs (the honest two the pose can't know), ×1/×2/×3 k chips) · R9 **size→distance** (NEW `lib/geo/sizeDistance.ts` + TargetPanel SIZE→DIST row on discs > 300″: person/tower stand-back at the TRUE current disc — verified "1.8 M PERSON @ 209 M" live). PLAN panel order: FRAME · TODAY · FIND · MOON · SPOT STARS · MW; new CSS in the plan-panel grammar (`.pp-in`, `.pp-chip--on`, `.pp-find*`, `.pp-npf*` — spinners stripped, 12.5 rem width kept). **(g) §3.5 SUNSETS/SUNRISES IN FRAME spec'd** (PLANNING_QOL_PLAN): event-time-anchored day loop (SearchRiseSet/SearchAltitude labels ~4 finds/day) + airless frame/skyline geometry test with the disc extent folded in; refraction pairing convention resolved (refracted LABELS, airless GEOMETRY); SUNSETS card + sunset-corridor v2; mobile M3c. Files: `scene/{sky,skyGhosts,skyNames*}.ts` · `lib/sky/hoverNames.ts*` · `lib/ephemeris/{frameFinder,moonCalendar*}.ts` · `lib/photo/npf.ts*` · `lib/geo/sizeDistance.ts*` · `panels/{FindCard*,MoonCalCard*,SpotStarsCard*,PlanPanel,TargetPanel}.tsx` · `globe/{StylizedTiles,tuning}.ts` · `styles/plan-panel.css` · 5 new test files (+39). Memory: `mem:project/wip-2026-08-14-qol4-batch`.

- **2026-08-14 night-2 (QoL-3 OWNER 3-ASK BATCH — day-moon ADDITIVE fix (dark-crescent round 2) · distinct true-size ghosts · panel de-occlusion + deck compaction v2 · sky-body hover affordance. Gates vitest 789/789 (+5) · astro 0 err/5 hints = baseline · desktop browser-VERIFIED headed Chrome CDP :9222 on wix dev, shots `verify-shots/qol3-01..04` + probes `qol3-probe-*`; owner-device taste tier open.)** **(a) Day-moon dark-crescent FIX — SUPERSEDES the 2026-08-14-evening (f) mechanism note:** live probes DISPROVED the recorded "dome depth race" story for the camera-anchored regime (dome surface 0.45·far is ALWAYS nearer than the moon ≥0.5·far, same-frame far, deterministic reversePainterSort — the dome can never lose there; per-frame samplers held `uDaySky = 1`, near/far static through walk + look-drag repros). The REAL defect was the day arm's blend arithmetic: NormalBlending lerped the DARK albedo colour (`albedo·(0.12+lit·3.2)` ≈ 0.3) over a BRIGHTER sky at `alpha = lit·1.35` — every mid-lit pixel DARKENED the sky behind it (~20% at lit 0.3), i.e. the owner's abrupt dark-crescent telephoto frames whenever haze/bloom stopped masking it. Fix in `scene/sky.ts`: **premultiplied CustomBlending (ONE / ONE_MINUS_SRC_ALPHA) with two per-fragment arms** — day arm ADDITIVE-ONLY (`rgb = albedo·lit·moonBrightness·SKY.moonDayAddGain`, blend-alpha 0 → the disc can only ADD light; no pose/ghost overlap/bloom state can ever render it darker than the sky) · night arm byte-identical opaque star-occluding disc (`alpha = fade·(1−uDaySky)`); discard when BOTH contribution and alpha < `moonAlphaDiscard` (depth safety kept). NEW tested CPU twin `moonDiscArms` (the horizonTerms precedent) + `test/components/globe/moonDayArm.test.ts` (5) pinning day-alpha ≡ 0 ∀ lit/fade, lit=0 → zero contribution, night arm byte-identity. `SKY.moonDayAlphaGain` RETIRED → `SKY.moonDayAddGain 0.55` (owner knob). Verified: 275 mm telephoto crescent = bright additive arc on blue, before/after probe shots. **(b) Ghosts distinct + true size (owner: "blurry, smaller than main"):** geometry PROVEN already true-size (state diam 0.5316° vs moon mesh 0.5378° in-page; ghost plane = 2·tan(diam/2)·d ≡ the moon sphere's screen diameter) — the shrink was PERCEPTUAL: `GHOSTS.edgeSoftness 0.45` fed alpha falloff from r=0.55 (a 55% solid core) at peak alpha 0.5 with no bloom. Retuned: edgeSoftness 0.45→**0.18** · alphaNear 0.5→**0.62** · alphaFar 0.14→**0.2** (ramp shape + pastDim 0.6 unchanged — still fainter the further). Verified: crisp disc chain at 4×30 min. **(c) De-occlusion + deck v2 (owner screens: planner over minimap, target card over deck):** minimap `bottom 22.6→20.6rem` (measured .fh is ~16.3 rem, not the comment's ~20 — 2 rem of real room existed); PLAN cap `100vh−39rem → 100vh−41.7rem` and TARGET cap `100vh−10rem → 100vh−31.3rem` — both re-derived from MEASURED column geometry **and the max-height-caps-the-CONTENT-box trap (padding+border ~1.9 rem ride on top; the first cut left a 24 px overlap the browser caught)**; verified PLAN clears the minimap by 5.6 px and TARGET clears the deck by 6.8 px at 934 px vh (linear in vh → holds until the caps' floors). Deck: `.ct .uf-slider` border-bottom 0 (dividers gone; UploadFlow's hairlines untouched — scope override, base rule intact) · chips FLEX-SHARE one nowrap row (`.ct-mode` flex:1 1 0/min-width:0/0.62 rem; per-chip narrow-cut rules + wrap fallback retired; compass flex:none) · **CAM TILT + ROTATE hidden in FPV** (JSX `{!fpvMode && …}` — tilt was already a dead control there (orchestrator skips the glide while fpvActive); look/turn = drag + WASD; both verified back in orbit). **(d) Sky-body hover affordance (owner: "not obvious I can right-click"):** contextmenu candidate enumeration extracted to `pickSkyBody(ndc)` (hover highlights EXACTLY what right-click hits, same `skyMenuMinAltDeg` horizon floor) + new `stepSkyHover` (after SkyBodies/SkyTarget — ORDER note updated): banked hoverX/Y + `ORCH.skyHoverEveryFrames 4` cadence + house exp ease (`skyHoverEaseTauMs 140`) → `sky.setHoverGlow(sunK, moonK)` (ABSOLUTE re-derivation of uIntensity/uBrightness from SKY constants — write-once uniforms, idempotent) + `skyTarget.hoverBoost(k)` (post-update multiply — its gains are per-frame writes); `ORCH.skyHoverGain 0.25`; stands down while placing / pointer-down (new `anyPointerDown` flag) / off-canvas; cursor "pointer" with pin-hover arbitration (pins win by running later; both clear sites now respect `skyHoverKind`). Verified: uBrightness 3.2→4.0 eased on-body, back off-body, cursor pointer↔"", sun untouched. Files: `scene/{sky,skyTarget}.ts` · `globe/{StylizedTiles,tuning}.ts` · `panels/CameraTiltPanel.tsx` · `styles/{camera-tilt,mini-map,plan-panel,target-panel}.css` · NEW `test/components/globe/moonDayArm.test.ts`. Memory: `mem:project/wip-2026-08-14-qol3-batch`.

- **2026-08-14 evening (QoL-2 SHIPPED + OWNER 7-ASK BATCH — ghost-trail seeing feature · sun/moon first-class targets · right-click sky menu · WASD/⇧␣ · daytime-moon dark-disc fix · HUD/minimap width match · compact FPV deck. Gates vitest 784/784 (+21) · astro 0 err/5 hints = baseline · desktop browser-VERIFIED headed Chrome CDP :9222 on wix dev, shots `verify-shots/qol2-01..08`; owner-device taste tier open.)** **(a) QoL-2 core (PLANNING_QOL_PLAN §3.2):** NEW pure `lib/ephemeris/frameFinder.ts` — `frameCrossings(sampler, pose, fromMs, days, {profileFn, stepMin, maxWindows})` (injected `(utcMs)→AzAlt` sampler = the planner idiom; coarse 5-min scan + 22-iter bisection ≤1 s; windows annotated skyline clear/blocked/mixed via `sampleBins` over `plan.profileBins`, `lightPhaseAt` (NEW twilight.ts point query), moonUp/moonIllum/moonGlare per the mwSeason recipe) + `nearestFrameCentre` (global min of angular separation — the P4 seed) + `angularSepDeg`. NEW `lib/export/ics.ts` — pure RFC 5545 builder (CRLF, folding, escaping, deterministic content-keyed UIDs) + client Blob download; no cron, no endpoint (C1). UI: **FrameCard** (PlanPanel section, FPV-gated via the quantized-pose-key idiom, memo on poseKey+5-min quantum+bins identity+target) — ☀/☾/target next-crossing rows "8:37a–10:33a · DAY · CLEAR ⤓" with NOW→ state, jump chips, per-row .ics, ◎ CENTRE chip (tracked sun/moon dedupe: no duplicate target row); **TodayCard** REPLACES the flat `.pp-chips` — midnight→midnight chronology, light-phase dots (the scrubber band colours), ☀-elevation-at-moonrise quality tag, dated full/new-moon chips, per-row .ics; both cards SELF-COMPUTE (MwCard precedent — immune to the planFeed panel-open mirror trap). Browser-verified: bins 120 @ FPV anchor, sun window bisected to the frame edge, moon "NOW → 6:54p · 35%", .ics click produced a real 530-byte text/calendar VEVENT (probe-intercepted). **(b) GHOST-TRAIL (owner ask 1):** NEW scene module `globe/scene/skyGhosts.ts` — ONE InstancedMesh of soft alpha-blended disc billboards (NOT additive — the S6 day-sky lesson; depthTest off, renderOrder 10 = the dayArcs grammar) at scene time ± k·step for the TRACKED target, symmetric count (store/sky `ghosts/ghostCount(4)/ghostStepMin(10)`, persisted prefs), alpha ramps alphaNear→alphaFar with past side dimmed ×0.6, per-ghost horizon melt, true apparent size for sun/moon (floor `GHOSTS.minDiscDeg 0.4°`), colours sunGlow/moonlight/accent by kind; resamples on a 1-s scene-time quantum / knob / anchor change — scrubbing slides the chain along the trajectory (the owner's exact "path against the surroundings" ask). TargetPanel: GHOSTS toggle + "± N EVERY ⟨min⟩" stepper row. Verified: 8-ghost chain on the Aug-18 crescent, knobs 6×30 min re-spread live. **(c) Sun/moon first-class (ask 3):** NEW `bodyTarget("sun"|"moon")` provider (targets.ts; kind "sun" added to TargetKind; planet-facts reuse; sun = mag −26.74 catalog/elongation 0/phase null, moon = engine Illumination; dir agrees with `bodyStatesAt` ≤1″ — tested), `body:sun`/`body:moon` boost-2 search rows + `targetById` branch (catalog.ts), ☀ glyph (searchIndex). SKY search "moon" → tracks; prefs-restore of `body:moon` works. **(d) Right-click sky menu (ask 7):** orchestrator `contextmenu` angular hit test (sun/moon ≥1.2° pad via `ORCH.skyMenuMinHitDeg`, tracked target via `hitRadiusDeg`; ground right-click keeps the native menu) → `camera.skyMenu` mirror → NEW top-level island `SkyContextMenu` (.ct-pinpop discipline): header glyph+name+az/alt · ⌖ TRACK (sun/moon one-click track) / TARGET PANEL · ◎ AIM HERE · ✧ GHOSTS (tracks first if needed) · ↗RISE/↘SET jump chips (dayEvents today+tomorrow). Dismiss: canvas press, click-away, action, Escape — **Escape consume ordered BEFORE the FPV unwind stack** (caught live: closing the menu exited FPV). **(e) WASD + ⇧␣ descend (ask 2):** WASD aliases the arrow walk (e.code, layout-independent) gated on typing surfaces; arrows additionally yield to explicit-tabindex owners (the rail scrubs with ◀▶ — latent conflict fixed); SHIFT+SPACE = descend through the same quadratic ramp (sign rides the live shift mirror — mid-hold reverse keeps the ramp). Verified: W walked SW ~12 m, ␣ 1.7→5.5 m/2 s, ⇧␣ 5.5→3.8 m/1.5 s. **(f) Daytime-moon dark-disc FIX (ask 4, owner screenshots):** root cause (agent-proven from the pixels): the moon shader was day-blind — opaque `vec4(albedo·(earthshine+lit·brightness), 1.0)` NormalBlending + depthWrite, its daytime "pale" look 100% owed to the additive sky dome drawing after it at 0.45·far vs 0.5·far — a fragile depth/sort race camera motion breaks (stale dome frame → depth-rejected ONLY on the disc → the black-disc frames), PLUS a double tone-map (`tonemapping_fragment` + OutputPass) crushing the limb to (7,6,2). Fix in `scene/sky.ts`: per-fragment **day-sky alpha** `vis = mix(1, clamp(lit·moonDayAlphaGain), uDaySky)` (uDaySky = the atmosphere's own dawn ramp × sky-altitude fade, CPU per frame), discard < `SKY.moonAlphaDiscard 0.03` (an invisible limb writes NO depth — the dome can never lose), tonemapping include removed; night keeps the opaque star-occluding disc. Verified: new-moon noon = correctly invisible pale point, Aug-18 35% crescent = clean pale crescent on blue sky, `uDaySky 1.000`. **(g) Widths + compaction (asks 5/6):** minimap 176→200 px canvas (resolution-independent draw) + FPV HUD pinned to the SAME **210 px** border-box (measured 210/210), two-line POSITION row (label+COPY / full coords), two-line walk hint incl. WASD/␣/⇧␣; `.ct--fpv` compact deck variant (tighter slider rows/chips/compass, all controls kept). Files: NEW `lib/ephemeris/frameFinder.ts` · `lib/export/ics.ts` · `globe/scene/skyGhosts.ts` · `panels/{FrameCard,TodayCard,SkyContextMenu}.tsx` · `styles/sky-menu.css` · tests `frameFinder`(12) `ics`(9); MOD `twilight/targets/catalog/searchIndex/simbad/prefs` · `store/{sky,camera}` · `globe/{StylizedTiles,tuning}` · `scene/sky.ts` · `panels/{PlanPanel,TargetPanel,CameraTiltPanel,FpvHud,MiniMap}` · `styles/{plan-panel,target-panel,camera-tilt,fpv-hud,mini-map}` · `index.astro` · `targets.test`(+2). Env note: the wix-dev Vite dep cache 504-wedged mid-session (Outdated Optimize Dep, cache-proof); `touch astro.config.mjs` self-restarts the server (it rebound 4322→4321). Memory: `mem:project/wip-2026-08-14-qol2-batch`.**

- **2026-08-14 late-2 (SHIP-PIPELINE FULL AUDIT + HOOK v2 — owner order "reverify all branches/commits, never block, never lose data, keep master consistent + mirror fresh").** **(a) FULL BRANCH AUDIT — ZERO CONTENT LOSS, all 35 branches proven contained in master:** every local branch tip + `origin/claude/ship-20260814-003854` tree-diffed against its squash commit on master — 33× TREE-VERIFIED (tip tree ≡ master commit, incl. `codeowners`≡`086c63c` and `refactoring-2`≡`afa6922` after fixing two script artifacts: a rev/path name collision and a "refactor II"⊂"refactor III" title-prefix false match) · 1× SHA-ancestor (`marketplace-finalize`) · M1 `03c97ed`/M2 `22ce6bb` (the "missing commits" on the PR-#35 branch the owner spotted) proven by the replay chain: `diff 03c97ed f95e613`=∅, `diff 22ce6bb b576e52`=∅, both replays ancestors of `ece48ef`, `diff ece48ef 4447ea2(master)`=∅, and `diff 22ce6bb master` ≡ `diff 22ce6bb a7388a8` (master adds EXACTLY the QoL-1 session diff on top of M2 — nothing else, nothing lost). SHA-wise absence is EXPECTED under squash merges; content containment is the invariant, and it holds everywhere. **(b) TRUE ROOT CAUSE found in the hook:** v1's automerge-wait used `merge-base --is-ancestor <sha> origin/master` — under SQUASH merges the shipped SHA is NEVER an ancestor → the wait ALWAYS timed out → local checkout stayed on the ship branch → next session built pre-squash history → permanent CONFLICTING PRs (the #35/#36 jam). **(c) HOOK v2** (`session-end-ship.sh` rewritten; `bash -n` + DRY_RUN verified — gates green, preflight clean; reseat finder validated against the REAL incident: from stale tip `a7388a8` it finds `692a5ab`): squash-aware `ship_landed` (SHA-ancestor OR tree-equality OR `gh pr --state merged`) · **divergence-preflight self-heal** (nearest first-parent ancestor with tree ≡ origin/master → `rebase --onto`, conflict-free by construction; stale pre-squash commits replay empty and drop; no tree-equal ancestor → push AS-IS + flag, never a guessed merge) · **no-loss guarantees** (`refs/backups/ship-<ts>` snapshot before anything moves; branch deletion only after `git diff --quiet <sha> origin/master` proof) · superseded-ship-PR auto-close (the #35 trap) · mirror force-sync on EVERY path (landed/nothing-to-ship/timeout) · anomalies → gitignored **`.claude/SHIP_ATTENTION.md`**, which the SessionStart hook (`activate-serena.sh` step 5, new) now orders the next session to read-resolve-delete. **(d) MIRROR SYNCED:** `private/master` was 1 behind (pre-#36), 0 ahead → fast-forwarded `47f081e..4447ea2`; verified `private/master ≡ origin/master`. Docs/memories: CLAUDE.md §auto-ship rewritten · `mem:decisions/session-end-autoship` rewritten (incident + v2 flow + boot rule) · NEXT_SESSION + `mem:project/wip-2026-08-14-qol1-tail-trace` updated. Files: `.claude/hooks/{session-end-ship,activate-serena}.sh` · `.gitignore`.

- **2026-08-14 late (QoL-1 TAIL — §3.1.D TRACKED-TARGET TRACE ON THE RAIL + PLAYHEAD CURSOR — SHIPPED. Gates vitest 763/763 (+11) · astro 0/0 · hints 5 = baseline · desktop browser-VERIFIED headed Chrome CDP :9222 on wix dev (shots `verify-shots/qol1t-01..03`); owner-device taste tier still open.)** **(a) Trace (§3.1.D), with one recorded DESIGN DEVIATION from the spec letter:** planFeed does NOT mirror a trace polyline — it mirrors the ready profile's **skyline bin array** (`store/plan.profileBins`, 120 bins, copied ONCE per completed build via a per-build `binsMirror` cache; nulled in `startBuild`, the focus-anchor fallback branch, and `dispose`) and the RAIL computes the trace itself with the same span-memo idiom its sun/moon curves already use. Rationale: one curve-computation idiom on the rail (all three series memo on scene-HOUR span + 0.05° eye + target id), planFeed stays lean, and the mirrored bins ARE the QoL-2 `frameFinder` profileFn seam (T8's rail-context half rides the same mirror). NEW pure faces: `targetElevationSeries` + `traceStates` (dayArc.ts — `down | blocked | clear` against an injectable skyline sampler; no sampler → honest horizon-only "clear") · `sampleBins` (horizonProfile.ts — the interpolation core of `sampleProfile`, refactored to delegate, callable on the mirrored plain array) · `azAltFrameMarker` (offscreen.ts — az/alt vs a roll-free FPV pose {heading, pitch, vFOV, aspect}; builds the ENU camera basis and delegates to `frameMarker`). Rail render: per-class SVG paths over the band strip (below-horizon = pen up · blocked = dim dashed · clear = accent · **FPV in-frame = 2.4-px emphasized accent** — "it crosses MY frame"), class transitions bridge from the previous point so segments meet without gaps; trace rides the TARGET panel's SHOW gate like the marker/trail. FPV pose reaches React via NEW `FpvHud.aspect` (orchestrator writes `camera.aspect`; single producer) and the rail subscribes to a **QUANTIZED pose key** (1° heading/pitch, 0.5° FOV) so low-cadence HUD writes only re-render on meaningful frame moves. Browser-verified end-to-end: orbit/no-anchor → clear-only trace, bins null (honest fallback); FPV Dnipro `#f=` → bins 120 @ coverage 1.00, blocked+clear segments both render; `requestSkyLook` at the tracked target (Perseus az 61.4°/alt 37.5°) → ONE contiguous emphasized frame segment appears, and after a +3 h pin it correctly stays anchored to its clock window instead of following the cursor. `SCRUB.traceStepMin 8` (~90 samples/12 h; the target sampler is Kepler-grade for comets, heavier than `horizontal` — own knob). **(b) Playhead cursor (owner ask, screenshot-ruled):** the centre knob dot (`.ts-knob`, 12 px circle mid-rail) is superseded by a **playhead** — 2 px full-strip cursor bar + down-pointing pennant handle on top (`.ts-cursor`, clip-path tag; `background: inherit` keeps the pseudo-element in sync), stopping above the hour-label row; same state semantics (accent = LIVE, warn-amber = pinned; verified both). **Bug caught in-session:** the first cut leaked stale `binsMirror` into a focus anchor when FPV exits with the PLAN panel open — binsMirror now resets in the focus branch alongside the scan caches (the frozen-mirror-while-panel-closed behaviour is pre-existing and consistent: frozen eye + frozen skyline). Files: `lib/ephemeris/dayArc.ts` · `lib/geo/{horizonProfile,offscreen}.ts` · `store/{plan,camera}.ts` · `globe/scene/planFeed.ts` · `globe/{StylizedTiles,tuning}.ts` · `panels/TimeScrubber.tsx` · `styles/time-scrubber.css` · tests `dayArc/offscreen/horizonProfile` (+11). **(c) SHIP-PIPELINE UNBLOCK (new trap):** PRs #35/#36 (M1+M2, QoL-1) sat OPEN-CONFLICTING — **squash-merge divergence**: #34 landed as squash `47f081e` while the local session branches kept building on the pre-squash `692a5ab`, so every later auto-ship PR conflicted forever and automerge never fired. Fixed by proving the twin trees byte-identical (`git diff 692a5ab 47f081e` empty) → `rebase --onto origin/master 692a5ab` (conflict-free by construction) → force-push → **#36 automerged instantly** (M1/M2/QoL-1 all on master, `4447ea2`); #35 closed as superseded (its branch = ancestor of #36); local checkout re-seated onto master with the session work stash-carried; stale ship branches deleted. **RULE: if a session boots and the previous ship PR hasn't landed, re-seat onto origin/master FIRST (tree-identity check → rebase --onto) — never build a new session on a pre-squash ship branch.** Memory: `mem:project/wip-2026-08-14-qol1-tail-trace`.

- **2026-08-14 night (PHOTOPILLS DEEP REVIEW + QoL RE-PRIORITIZATION + QoL-1 SHIPPED. Gates vitest 752/752 (+14) · astro 0/0 · hints 5 (was 6 — one stale-symbol hint dissolved) · desktop browser-VERIFIED headed Chrome CDP :9222 on wix dev, shots `verify-shots/qol1-01..08`; real-geolocation prompt + rail feel on glass = owner-device tier.)** **(a) RESEARCH:** photopills.com is Cloudflare-403 to every fetcher → the user guide was pulled from Wayback 2026-07-07 snapshots (Part I planner + Part II pills, guide "Updated to iOS v2.12"), extracted to text, and deep-read by two parallel agents (100% coverage) + one FTW current-system mapper; adoption record = **`PLANNING_QOL_PLAN.md` §1 (R1–R20)**. Headline finds we lacked: elevation curves ON the time bar · event-step tap zones · the Sun/Moon Info "daily surface" chronology (midnight→midnight, cross-body, light-coded glyphs, sun-elevation-at-moonrise quality signal) · Find craft constants (az+el ONLY — their azimuth-only modes proved dead weight; tol 3°/0.5°; 1y sun/2y+ moon) · size→distance telephoto tool · supermoon/perigee/apogee year list · ICS alerts. Their entire AR-calibration + horizon-pin + altitude-offset + real-azimuth-lines apparatus = flat-map workarounds our 3D engine deletes natively (positioning list §1.2). **(b) RE-RULING folded:** IMPLEMENTATION_PLAN §Phase 8 ladder REWRITTEN under the owner's sun/moon/MW-first order → **8-QoL-1** (scrubber v2 + instant wins) · **8-QoL-2** (FPV shoot-this-frame `frameFinder` + TODAY daily surface + ICS) · **8-QoL-3** (P4 Find skyline-filtered + P6 moon calendar/distances + P5 NPF + size→distance) · 8-events · 8-tools/ambience (P10 DSO explicitly slid behind); spec source = PLANNING_QOL_PLAN §2/§3. **(c) QoL-1 SHIPPED:** **TimeScrubber v2** — the fixed ±12 h clamped window is now an **INFINITE CONVEYOR**: scene time rides the fixed centre cursor, dragging slides the timeline (drag left = future), unbounded both ways (verified +25.2 h / −42 h exact across strokes); **full light bands** via NEW pure `lightSegments` (twilight.ts; `LIGHT_DEG` +6/−4/−6/−12/−18 photographic partition; multi-boundary coarse steps refine every crossed edge so the 2°-tall blue sliver never skips; equinox/pole/blue-width tests) painted in DISTINCT hues (new tokens `--color-blue-hour` `--color-night-band`; night genuinely darkens — the P1 "reads night-only" moonlight ramp complaint); **sun/moon elevation curves on the rail** (NEW `elevationSeries` in dayArc.ts, SVG over the band strip, horizon midline) — the PhotoPills Time-Bar composition beaten with **REAL browser-local hour labels** (NEW `hourTicksBetween` in store/time.ts, DST-fold-guarded; midnight = full-height day tick); **event-step tap zones** (outer 12%: next/prev almanac chip; planFeed mirror when warm else pure `dayEvents`+`moonPhaseEvents` computed AT TAP — the mirror only runs while the PLAN panel is open, planFeed.ts:358–363); dblclick-NOW restricted to the middle zone; transport/date/time/steppers/PLAY/EXIF-seeding intact; perf discipline: bands+curves memo on a 24 h span keyed to the scene HOUR + 0.05° eye — the window slides over the cache with plain math. **KEPT-BACK: the tracked-target visibility trace (§3.1.D) — needs the planFeed mirror extension, next session.** **Space = FPV ascend with hold-acceleration** — `Space` joins `fpvKeysDown` (orchestrator-internal, clearAllTargets-IMMUNE by construction — the M2 fpvWalkInput lesson), gain = `min(1, held/2.5 s)²` × the vertical-encoder proportional step, same eye/lift clamps; guards: interactive-element focus skip (incl. any tabIndex≥0 — the rail), preventDefault (page scroll), keyup + NEW window-blur release (stuck-key safety; also releases stuck arrows). Verified: 1.71→16.70 m over a 3 s hold (quadratic visible), release freezes dead, a 150 ms tap = **1 cm** — precision-controlled as ordered. **My-location → FPV on BOTH shells** — desktop NEW `panels/MyLocation.tsx` nav island ("My spot"; geolocation → `requestFpvJump(lat, lon, 1.7 m, N, 0°, tempFovDeg)` — the proven share-link path) + mobile `SceneActions` 🧭 UPGRADED from pin+fly to the same jump; C6 client-side-only carried in both; verified on both shells with a stubbed fix → standing FPV at the exact coordinates. Mobile dock deliberately INHERITS the 12 h `SCRUB.windowHours` (product consistency; its full v2 = the M-twin). **NEW TRAPS:** (1) **a page-context `await import("/src/store/X.ts")` can resolve a SECOND zustand instance in the Vite dev graph** — writes land nowhere, reads lie (observed live on store/time; the M2 note said "same instance" — true then, NOT guaranteed) → store/time + store/camera now publish `window.__timeStore`/`__cameraStore` DEV seams (global.d.ts had them declared all along); ALWAYS probe the `window.__*` registry. (2) planFeed's focus-anchored chips are PLAN-panel-open-gated — any rail/chip consumer outside the panel must compute its own almanac fallback. (3) `goldenElevationsDeg(GOLDEN)` measures **−6.5°…+15.4°** — the render bell is a WIDENED scene-grading device; never paint it as a photographic band (bands use LIGHT_DEG; the small chip↔band divergence is deliberate + documented). Files: `lib/ephemeris/{twilight,dayArc}.ts` · `store/{time,camera}.ts` · `panels/{TimeScrubber,MyLocation(new)}.tsx` · `mobile/SceneActions.tsx` · `globe/{StylizedTiles,tuning}.ts` · `styles/{time-scrubber,my-location(new),tokens}.css` · `pages/index.astro` · tests `twilight/dayArc/time` (+14) · docs `PLANNING_QOL_PLAN.md`(new) + IMPLEMENTATION_PLAN §Phase 8 rewrite. Memory: `mem:project/wip-2026-08-14-qol-batch`.

- **2026-08-14 (OWNER ORDERS, end of the M2 session — recorded for the NEXT session; no code this turn.)** (a) **PhotoPills deep review** — a fresh evidence-cited pass over `https://www.photopills.com/user-guide` (the 2026-08-11 gap analysis was competitor-level; this one is feature-level): find any amazing features we missed that would translate nicely into the app **without overburdening/complicating it** — intuitive shot-planning value only; every adoption **beats, never blindly copies** PhotoPills (standing rule for ALL features). (b) **PRIORITY RE-RULING: sun/moon/Milky Way planning features FIRST — all astro/DSO work comes only after mobile support is fully solved AND the sun/moon/MW quality-of-life pass is done.** This re-scopes the "8b next" ordering (2026-08-13/14 lines): P4 Find + P6 moon calendar are sun/moon/MW-aligned and stay early; P5 NPF serves MW exposure but placement is decided by the research; DSO-leaning items (sensor-frame overlays on DSO targets, catalog depth, etc.) slide behind the QoL pass. Next session must fold the research + the existing §Phase 8/MOBILE_PLAN ladder into one re-prioritized plan. (c) **Owner feature batch** (design with the research, UI/UX to be worked out): **1** FPV "shoot-this-frame" suggestions — for the CURRENT frame show when and what to shoot (sun/moon/MW), plus a search for **when a given sky body will be nearest a specified point/area** (ties into P4 Find; pick the best UX form). **2** **Time-scrubber v2** — ALL band colours (golden hour, blue hour, the twilights, day, night — the P1 dock/rail bands exist for day/civil/nautical/astro/night but the owner reads them as night-only → contrast/palette is part of the fix; referenced screenshot did not come through), PhotoPills-style charts on/around the rail (researched, then bettered — the scrubber must stay convenient and intuitive), a **visibility trace for the selected object in the context of the current frame** (FPV: the real framing; orbit: current screen position; FPV may carry extra info), and the rail becomes a **12 h visible window with CONTINUOUS INFINITE drag into past and future** (replaces the fixed ±12 h) **annotated with actual hour ticks** — while keeping fast-forward, calendar, time-pick, NOW, and the rest of the existing UX intact. **3** Both shells get a **jump-straight-into-FPV-at-my-current-location** button (geolocation via the most idiomatic browser API; mobile's 🧭 MY LOCATION currently only pins + flies — this goes all the way into FPV). **4** **Space bar = ascend** with hold-acceleration (the longer the press the faster the gain, but precision-controlled, never too fast; scope — FPV eye vs orbit zoom — decided at design time). Twin: `mem:project/owner-orders-2026-08-14-qol-batch`.

- **2026-08-14 (MOBILE M2 — FPV TOUCH — SHIPPED. Gates vitest 738/738 (+2) · astro 0/0 · hints 6 = baseline · phone-viewport browser-VERIFIED (headed Chrome CDP :9222 on wix dev, 402×874; shots `verify-shots/mobile-m2-01..02`); REAL-DEVICE tier UNVERIFIED — pairs T1, which now also covers stick/pinch feel + the wake lock's physical dim.)** MOBILE_PLAN §6 M2 + §4.1/4.2: **`store/camera` walk-stick seam** — `fpvWalkInput {fwd,right} −1..1 | null` + `setFpvWalkInput`, **deliberately NOT cleared by `clearAllTargets`** (noteInteract fires it on EVERY canvas pointerdown — in FPV that pointerdown IS the look-drag; walking-while-looking is the joystick's whole point; divergence pinned by a `camera.test.ts` case, backstop clear in the orchestrator's FPV-exit branch) · **orchestrator walk integration** beside `fpvKeysDown` into the world-space `fpvWalkOffset` — the 2026-08-11 pivot invariant holds (only INPUT mutates the offset); analog curve `speed = walkSpeedMps · walkStickMaxMult · d²` (rim = Shift-sprint, ~58 % = arrow speed, centre = Option-creep; direction normalized by raw magnitude), tunables `FPV.walkStickMaxMult 3` / `walkStickDeadband 0.06`; **measured 65.92 m/s at full rail (expect 66), release stops dead, 0° yaw disturbance; pivot invariant RE-VERIFIED ON TOUCH — 0.0000 m drift (mid-drag max AND final) across a 45° look-drag + reverse after a 99 m walk; two-thumb walk CONTINUES through a simultaneous look-drag** (39.6 m + 65.6 m through −24.3°) · **pinch-FOV in `onFpvPointer*`** — second non-primary pointer beside the look finger drives `fovTargetDeg = startFov·startDist/dist` inside the wheel clamps [2.75, 80]; the look FREEZES while the pinch lives, `fpvLastX/Y` stay fresh so lifting the pinch finger resumes the drag seamlessly, either lift ends it, gap <8 px re-seeds, `fpvPinchedDuringDrag` suppresses the sky-marker lift-click; desktop mouse path byte-equivalent (always isPrimary); measured 55°→27.7° on 2× spread (expect 27.5), clamps at 80° · **`mobile/FpvControls.tsx` + `styles/mobile/fpv.css`** — WALK joystick (unit-disc clamp, KNOB_TRAVEL 0.72, null on release/unmount), ⤒/⤓ hold-to-fly on the EXISTING encoder seam `setZoomRate(±CONTROLS.zoomRateMaxPerS)` (1.7→16.88 m in 1.4 s, floors at exactly 1.7), compact HUD row FOCAL·HDG·PITCH·EYE off the `fpvHud` mirror, **Screen Wake Lock** while mounted (1 request→granted on entry→released on exit; visibilitychange re-acquire); mount gate `tempFpv || fpvHud !== null`; `setPointerCapture` in try/catch (a synthetic pointer throws InvalidPointerId and would abort the handler before the store write) · **mini-map on `/m`** — the DESKTOP `panels/MiniMap` island mounted by `m.astro` (fence rule 2 exempts the page; rule 1 untouched), repositioned/shrunk (124 px, top-right) via `body.m .mm` — `MobileLayout` grew `<body class="m">` so the override wins by SPECIFICITY, not island-chunk CSS order (order between mini-map.css and mobile css is NOT guaranteed) · desktop smoke: zero mobile DOM/chunks on `/`, mouse-drag yaw −26° at 0.0000 m drift, wheel −20.2°, arrows ≈22 m/s. **NEW TRAP: the occlusion Chrome flags do NOT cover tab-backgrounding** — a background TAB has `document.hidden = true` and rAF fully parked regardless of `--disable-backgrounding-occluded-windows` (mid-verification the owner browsed in the verify Chrome and every rAF-dependent probe read dead — encoder rates "inert", `flight.update` 0 calls/s — while `setTimeout` kept running); re-select the tab and guard probes with `if (document.hidden) abort`. Twin: `mem:project/wip-2026-08-13-m2-fpv-touch`.

- **2026-08-13 night (MOBILE M1 — THE PLANNING LOOP ON `/m` — SHIPPED. Gates vitest 736/736 (+3) · astro 0/0 · hints 6 = baseline · phone-viewport browser-VERIFIED end-to-end (headed Chrome CDP :9222 on wix dev, 402×874; shots `verify-shots/mobile-m1-01..06`); REAL-DEVICE tier still UNVERIFIED — pairs backlog T1.)** MOBILE_PLAN §6 M1 built as thin store/lib consumers under `components/mobile/**` (the M0 charter): **MobileShell grown** (status strip + NEW pinned/live `TimeChip` + bottom chrome column peek·dock·tabs + sheet switchboard; sheets = `useState`, no new store) · **MobileTimeDock** = the compact ±12 h rail twin — same twilight discipline verbatim (eye = plan anchor ?? focus, memo `[anchorMs, lat·20, lon·20]` NEVER scene time, moonlight color-mix 12/24/38/55% in `styles/mobile/dock.css`), major-ticks-only, date jump + PLAY (armed default ×600) + NOW, edge-recenter carried · **Sheet** (pull-down-dismiss handle ≥90 px, scrim tap closes, 400 ms `cubic-bezier(.65,0,.35,1)` + reduced-motion off, backdrop-filter on the SURFACE only — S2 rule) + **TabBar** (SCENE/PLAN/SEARCH, the only `safe-area-inset-bottom` consumer) · **MobileSearch** EARTH (Photon debounced 320 ms biased to focus; Enter→Nominatim; ODbL credit) | SKY (lazy catalog via `await import` — searchSkyCatalog instant + searchSkyEnriched; **SIMBAD//api/sbdb long-tail NOT wired — deliberate M1 cut, rides a later M-phase**; track = setTarget + setVisible + auto-aim if alt>0 → shell swaps to the TARGET sheet) · **PlanSheet** (owns `plan.setOpen(true)` on mount / false on unmount — planFeed computes focus-anchored chips ONLY while open; verdict BodyLines + CLEARS/HIDES cross chips + chip grid; CHIP_LABEL copy deliberately duplicated from PlanPanel — accepted two-shell cost) · **TargetSheet** (badges + ALT/AZ/MAG/ELONG + `targetWindows(days:8, stepMin:15, limit:5)` keyed `[hourKey, lat·20, lon·20, target]`, mount = the open gate; SHOW/MARK/TRAIL; per-kind fact blocks deliberately dropped) + **TargetPeek** row (per-minute az/alt; verdict badge id-matched) · **SceneActions** (🧭 MY LOCATION = geolocation → temp pin + requestFly(altDefaultM) — client-side only, C6; ◎ LOOK FROM HERE / ✕ CLEAR PIN / ✕ EXIT VIEW — phones have no Escape). **ENGINE SEAM (MOBILE_PLAN §4.3):** long-press pin drop in StylizedTiles — `dropTempPinAt` extracted from onDblClick, `pointerdown` timer NEW `ORCH.longPressMs` 500 ms, ≤ clickDragPx slop, second-finger/lift/drag cancels; **gated `pointerType === "touch"` — STRICTER than the plan wording so the frozen desktop stays behavior-identical** (verified: mouse hold 800 ms → NO pin; dblclick → pin; ✕ clears). **NEW TRAP — long-press fires BEFORE pointerup:** the empty-map-click clear path in onPointerUp would instantly clear the just-dropped pin — a `longPressFired` flag suppresses that one release (dblclick never hits this: its pin lands after the final pointerup). **NEW TRAP — occluded/locked-display Chrome suspends rAF ENTIRELY** (flights never consume, evaluate-on-rAF hangs); `/json/activate` + AppleScript don't wake it — launch the verify Chrome with `--disable-backgrounding-occluded-windows --disable-renderer-backgrounding --disable-background-timer-throttling` (recipe upgraded; the 2026-08-13 bringToFront trap generalizes). Minor: Playwright actionability stalls under the astro-dev-toolbar overlay → dispatch DOM clicks. NEW `test/components/mobileFence.test.ts` promotes the two-shell drift guard from comment to fence (mobile ↛ panels|ui · desktop ↛ mobile · zero-result probe). VERIFIED exit gate on 402×874: SEARCH→Dnipro flight (`#p=48.468,35.042,40099`) → SKY "galactic"→❍ GALACTIC CENTRE tracked, auto-aim heading 164.8°, windows list (Aug 13 21:45–23:30 ★, no moon) → dblclick pin → LOOK FROM HERE (`#f=…,1.7,…`) → PLAN: ANCHOR·FPV, SKYLINE 100% MAPPED · 3 KM TRUST, ☀ CLEAR HIDES 7:54p · ☾ CLEAR · ❍ GC CLEAR HIDES 12:21a + full chip grid → DUSK chip pins 20:34 → rail scrub → Aug 14 01:50 night FPV (bands under the knob, `&t=` rides the hash) → NOW live; synthetic-touch long-press drops + survives release; desktop `/` loads ZERO mobile chunks. Files: `components/mobile/{MobileShell,MobileTimeDock,TabBar,Sheet,MobileSearch,PlanSheet,TargetSheet,TargetPeek,SceneActions}.tsx` · `styles/mobile/{dock,chrome}.css`(new)+`mobile.css` · `globe/{StylizedTiles,tuning}.ts` · `test/components/mobileFence.test.ts`(new). Memory: `mem:project/wip-2026-08-13-m1-mobile-planning`. MOBILE_PLAN §6 M1 annotated ☑; next = M2 (FPV touch) or 8b desktop-first per NEXT_SESSION.
- **2026-08-13 late (SESSION-END AUTO-SHIP HOOK — owner order).** NEW `.claude/hooks/session-end-ship.sh` wired to **SessionEnd** in `.claude/settings.json` (self-daemonizes via nohup so it survives CLI exit): commits everything on `claude/ship-<stamp>` with **`#pr #skipreview #automerge`** (title from gitignored `.claude/.ship-title`) → waits for automerge via `git merge-base --is-ancestor <sha> origin/master` polls (no gh dependency; 45-min cap) → **force-pushes origin/master to the `private` mirror** (`git@github.com:sukhov-work/frame-the-world.git` — leg validated live) → restores the local checkout onto master (only if HEAD/tree untouched meanwhile). ABORT gates: `.claude/BLOCKING_QUESTIONS.md` present (standing owner questions — Claude maintains the marker, convention added to `.claude/CLAUDE.md`) · clean tree (mirror-sync only) · vitest / astro check red (never automerge red). Log `~/.claude/logs/ftw-session-ship.log` · lock `~/.claude/locks/ftw-ship.lock` · `DRY_RUN=1` tested (blocking gate + full dirty-tree plan). Files: `.claude/hooks/session-end-ship.sh`(new) · `.claude/settings.json` · `.claude/CLAUDE.md` · `.gitignore`. Memory: `mem:decisions/session-end-autoship`. NOTE: fires from the NEXT session end; if the settings watcher hasn't reloaded, open `/hooks` once.
- **2026-08-13 evening (AUDIT SLICE 7 + OWNER RULINGS + PHASE 8a "DARKNESS & THE GALAXY" SHIPPED desktop-first. Gates vitest 733/733 (+23) · astro 0/0 · hints 6 = baseline · wix build Complete (26 routes) · dist 32.2 MB (was 33) · desktop surfaces browser-VERIFIED headed Chrome CDP :9222 on wix dev, shots `verify-shots/phase8a-01..15`.)** OWNER RULINGS recorded in `tracked-backlog.md`: **T26** upload-url caps → ACCEPTED RISK, stays unbounded (auth suffices; re-open on abuse evidence/commercial release) · **NEW T27** = the B2 tail (frame-p5-tester password in git history) → WON'T-FIX (test-only member) · **T4** purchase loop + plan rename → DEFERRED long past M6. **SLICE 7:** B10 — `public/enriched-sample/` (2.8 MB) MOVED to `bakes/enriched/sample/{dnipro,dnipro-o2w}`, served at `/enriched/sample/*` by the EXISTING dev middleware (zero middleware code; generator OUT_DIR + serve-note updated; the old `.gitignore` block dropped — `/bakes/` covers it; dist verified 0 sample files, 33→32.2 MB) · A5 — **42 de-exports** (17 src values + WGS84_A/B in bake-lib + 23 types) + true orphans DELETED (`QualityTierSettings`, `ThemeTokens`, `SENSOR_FORMAT_WIDTH_MM` — its only "usage" was a comment; the fresh ts(6133) hint caught it) + `store/upload.ts:23` re-export narrowed to AdjustableParams/AdjustableKey; knip now clean but for 2 documented FPs (DEFAULT_EXCLUDE_TAGS test-imported · fetchOsmXml imported by a bake entry script knip mislabels "unused file" — KEEP both exported). **PHASE 8a (per IMPLEMENTATION_PLAN §Phase 8; mobile twins M1/M3 now UNBLOCKED): P1** NEW `lib/ephemeris/twilight.ts` (TWILIGHT_DEG 0/−6/−12/−18 · `twilightSegments` = 5-min coarse scan + ≤1 s bisection, segments tile the window exactly · `darkWindows`; 10 tests on ANALYTIC vectors — lower-culmination closed-form: 70°N solstice = single day segment, 60°N solstice civil-but-never-nautical (minAlt −6.56°), equator-equinox civil band 20–29 min) → TimeScrubber `.ts-twilight` decoration (FIRST child of `.ts-rail`, absolute + pointer-events:none + aria-hidden = zero layout/behavior delta on the frozen rail; eye = plan anchor ?? camera focus; memo keys `[anchorMs, lat·20, lon·20]` NEVER scene time — the `pinnedMs` subscription re-renders per scrub move; moonlight color-mix steps 12/24/38/55%). **P2** GC = first-class fixed target: `SGR_A_STAR_J2000` 266.41684°/−29.00781° + `GALACTIC_CENTRE_ID = "dso:gc"` (**`targetShortName` slices "dso:" → chip GC**) + `galacticCentreTarget()` in targets.ts (kind "galaxy" ❍, dso facts variant, vmag null → POINT reticle; the TARGET constant is deliberately ≈4.3′ from the frame-defining `GALACTIC_CENTER` — a "close but NOT merged" test locks 0.04°<sep<0.1°); catalog.ts `gcEntry()` (keys: milky way/galactic centre/sgr a/gc, boost 0.95) + resolver branch; band = NEW `milkyWayBandSegments(stepDeg, latitudesDeg)` in lib/ephemeris/stars.ts (asterismSegments pair-layout contract; tests: %6/unit/⊥-pole per-latitude/closure) → two star-sphere children in scene/stars.ts (b=0 core + ±12° edges, `tokens.milkyWay` additive hairlines, `MILKYWAY.bandStepDeg 3 / bandEdgeDeg 12 / bandAlpha 0.5 / bandEdgeAlpha 0.2`) gated by `update({mwBand})` ← StylizedTiles `visible && id === GALACTIC_CENTRE_ID` (the tracked-constellation precedent; star sphere stays UNPRECESSED — ≤0.3° vs the precessed reticle, invisible at band width, commented). **P3** NEW `lib/ephemeris/mwSeason.ts`: `mwSeason` per LOCAL night (noon→noon scan) = longest `targetWindows(days:1, darkSunDeg −18, minAlt 5)` window + `moonInterference` = moon-up ? K&S `moonPhaseIntensity(phaseAngle)` : 0 (NO altitude scaling, documented; **deliberately ≠ planner `finishWindow`'s 0.8·illum·√sin convention — one definition for the MW surface, the two scores never mix**) + `score = usableMinutes × (1 − interference)`; `mwArcTiltDeg` = galactic-equator great-circle tangent vs horizon via galacticToEquatorial→ecefFrameAt→topoAzAlt→azAltToEnu (**literature-anchor test: at lat 89.9° the tilt at the ascending node l≈32.93° = the 62.87° galactic inclination — passed first run**); → PlanPanel `MwCard` after `.pp-chips` (8 night rows: compact "9:15p–1:15a · 240m · ☾12%", accent score bar, click = setTime(window.peak); ARC TILT NOW readout; memo `[dayKey, lat·20, lon·20]`; layout fix: compact hhmm + `minmax(0,1fr)` column — nowrap meta had dragged the bars past the card edge; `.pp-scroll` scrollWidth == clientWidth verified, no phantom h-scroll). **BROWSER-VERIFIED:** twilight ladder on the rail (02) · GC search→track→panel (RA 17h45.7m/DEC −29°00′, ❍ GC chip, windows vs focus-lat geometry exact) (03/04) · band core+edges running through the REAL MW haze bulge, trail-off isolation (06) · FPV SKYLINE CLEAR verdict for GC (08) · **T25 CLOSED: POINT impostor eyeballed on GC — faint point + broken-hairline ring + the band line THROUGH the reticle (09-crop) · ELLIPSE eyeballed on M31 at real 178′×70′ + widened ring (10-crop)** · MW card rows with the waxing moon ☾2%→12% night-over-night after the Aug-12 new moon (15) · band gating proven both ways (M31 tracked → band LineSegments visible:false, GC → true). **NEW TRAPS:** orbit view tilt-clamps ≈74° — sky targets above ~25° alt CANNOT be framed from orbit; the FPV path (temp pin → LOOK FROM HERE) frames them and its auto-aim centres dead-on (that's why asterisms are FPV-gated) · zsh: `set -- $pair` doesn't word-split and a bare `===`/`====` in a command globs — use '--8<--' separators · a tracked target with dso facts MUST have a "dso:"-prefixed id or `targetShortName`'s `id.slice(4)` chips garbage. UNVERIFIED tails: real-device mobile (T1) · release canaries (T2/T3) ride the next `wix release` · MILKYWAY.band* alphas + twilight mix percentages + MwCard styling = owner taste pass. Files: lib/ephemeris/{twilight,mwSeason}.ts(new)+targets/stars.ts · lib/sky/catalog.ts · scene/stars.ts · StylizedTiles.ts · tuning.ts · panels/{TimeScrubber,PlanPanel}.tsx · styles/{time-scrubber,plan-panel}.css · tests twilight/mwSeason(new)+targets/stars · scripts/build-sample-dnipro-tiles.mjs · .gitignore · +28 slice-7 files. Memory: `mem:project/wip-2026-08-13-slice7-phase8a`. IMPLEMENTATION_PLAN 8a ticked ✅ · ASTRO_ENGINE_PLAN Phase-A note annotated (T25 closed) · backlog T25 CLOSED/T26 ACCEPTED/T27 NEW/T4 DEFERRED.
- **2026-08-13 latest (FULL AUDIT #1 EXECUTED + fix slices 0–6 applied — the owner-ordered pre-Phase-8a hygiene gate. Gates after fixes: vitest 710/710 (+6) · astro 0/0 · hints 6 = baseline · wix build re-run post-fixes.)** AUDIT (Deep, READ-ONLY phase; engine `.claude/skills/frame/references/audit-mode.md`): Track E first (704/704 · 0/0/6 · npm audit prod-only 9 — the 1 critical `tar` is dev-only @wix/cli; knip/depcheck/ts-prune/jscpd all probed OK; dist/client 33 MB re-anchored dated) → re-mine appended code.md 17–19 (InstancedMesh boundingSphere · CSS containing-block · FPV disabled-controls/world-space-walk) + platform.md 13 (provision script = schema source of truth) + tests.md K&S quarter corrected 10.3%→9.1% → tracks A–D as 4 parallel finder agents → MANDATORY verification pass: 30 candidates → **29 CONFIRMED, 0 deleted (FP ratchet 0%), 2 merged** (A4≡B1 phantom dep; C2≈D8 narrowed). Report: **`audits/audit-full-2026-08-13.md`**. Worst: **D1 BLOCKER — this file's same-day prepend had SPLICED the two 2026-08-13 entries into one line, eating the PLANNING-LADDER entry's header** (working-tree-only, recovered — see the reconstruction note below); **A1 MAJOR — the ONE unclamped heightAt consumer** (Pins.groundHeight latched loading-phase garbage permanently). C6 traced hop-by-hop CLEAN; astro@5.18.2 XSS advisories = ZERO reachable sinks (→ T24 accepted-risk row); scope-creep: none. Deliverables: **`conventions/contracts.md` authored (T23 CLOSED — Hyrum inventory: hash grammars, ftw:* keys, 14 DEV seams, collection schemas incl. NO-Listings-collection, R2 layout, checkout linkage)** · backlog T4 corrected (Pricing Plans WAS installed 2026-07-17) + new rows T24 (astro XSS) T25 (point/ellipse eyeball) T26 (upload-url caps). FIXES (slices 0–6 of the report's slicing; 7–8 deferred): **0** D1 ledger repair + D2 marker move · **1** A1 `sampleGroundM` clamp-no-latch helper in lib/geo/terrain.ts consumed by Pins.groundHeight + 4 regression tests · **2** A4/B1 `@wix/redirects@^1.0.125` added to package.json (was phantom-transitive under two dynamic imports — checkout/upgrade runtime break risk) + A2 Group.renderOrder line deleted (streetNames) + A3 residual `setClearColor` deleted (GlobeCanvas — the S5 navy-floor re-arm) + A7 throttle rename/comments (store/pins + StylizedTiles ×2) · **3** A6 dedup: NEW `lib/pins/fields.ts` mappers (`publicPinCore`/`pinOpticsFields`/`pinProductFields` — ONE PublicPins reader; store/pins + api/market + lib/wix/pinRecords rewired; the market "1km" literal → DEFAULT_PRECISION_TIER drift healed) + NEW `lib/ephemeris/topo.ts` (`topoAzAlt` — comet.ts/targets.ts ENU twins deduped, Horizons tests lock equivalence) + NEW `lib/sky/ttlCache.ts` (simbad/sbdb caches unified; old `o`/`body` blob keys read-tolerated per contracts.md) · **4** B2 test credential moved to gitignored .env.local (5 scripts read TEST_MEMBER_EMAIL/PASSWORD; ROTATION = owner action) + B6 landmask script repo-relative output + B4 bake-date stamps (both catalog generators + hand-stamped current comets/asteroids headers) · **5** B5 buy() variantId guard (silent-empty-checkout latch closed) + B7 POST returns the premium limit when known + B8 comment + C4 lazyContract regex hardened (bare side-effect imports; import-type excluded) + C6 Dec-solstice −23.44° unit vector + C1 golden.test now imports tuning.GOLDEN (stale mirror healed) + planner.test parity guard · **6** docs: D3 audit-mode 33 MB baseline · D4 README quota/9096/PricingPlans ×4 · D5 wix-headless quota heading · D6 globe-tuning note (B19 done, 2665 lines) · D7 ARCHITECTURE §5 rewritten as-built (NO Listings collection) · D9 core.md §Source-layout refreshed + D10 FPV-walk core backfill · D11 IMPLEMENTATION_PLAN §6.9 correction note · D12/D13 dating + machine-locality nits · C2/D8 ASTRO_ENGINE_PLAN tag narrowed → T25. **DEFERRED:** slice 7 (B10 move 3 MB public/enriched-sample out of the build + A5 de-export sweep — wants its own build verify) · slice 8 owner/browser (B9/T26 caps ruling · B2 rotation · T25 eyeball · T2/T3 release canaries). Files: see the report §Findings anchors + this line. Memory: `mem:project/wip-2026-08-13-full-audit-1`. TO /frame + FULL COMPREHENSIVE AUDIT SCHEDULED as the next session — owner order: hygiene gate BEFORE Phase 8a/M1; docs/skill-only session, gates untouched vitest 704/704 · astro 0/0).** Owner check-in: preliminary bare `/m` view LOOKS FINE — proceed (deep real-device numbers → backlog T1). NEW audit machinery under `.claude/skills/frame/references/` adapted from the epistemic-filter dev skill's audit mode: `audit-mode.md` (READ-ONLY engine — tracks A code/engine · B Wix platform/C1-C6/media/release · C tests/math-vectors · D docs/memory/conventions · E mechanical (test/check/npm-audit/knip-depcheck-ts-prune-jscpd PROBED not assumed/bundle-size); severity ladder calibrated to THIS repo (BLOCKER = C1–C6 violation/wrong load-bearing math/append-only break); FP ratchet >20% tightens checklists; cadence = burst/boundary-triggered, no idle timers) · four checklists with `— check:`/`— anchor:` per item MINED from DECISIONS §Traps + conventions (code 16 items: client:only, worker decode, f32 anchoring, heightAt clamp, renderOrder/points/additive, color-space traps, tuning contract, ONE-shared-material, store seams, per-frame writes, lazy contract, Hyrum contract strings, degrade visibility, [hidden] CSS, frame budgets, dead code; platform 12: thin endpoints, C6 structural, quota, TUS, no-fabricated-signatures, checkout variantId trap, release canaries, R2 bbox sync, secrets, Lehman data-currency, OAuth port-exact, Postel boundaries; tests 8: literature vectors, golden gates, trap→test mining, tests-alongside, tier honesty, staleness/dup, suite-speed ~2s baseline, Goodhart number+eyes; docs 12: append-only, doc-sync twins, convention samples, volatile-counts-once, stale-tag sweep, memory-graph health, plan-state truth, Zawinski scope guard, datelines, cross-refs, contracts.md inventory, backlog integrity) · `review-agent.md` (finder contract: walk-in-order, refute-before-report, zero-result validation) · `audit-report-template.md` (reports → NEW `.claude/claude-docs/audits/`; fitness scorecard; fix-session slicing S/M/L) · **`laws.md`** — the Laws-of-Software-Engineering mapping adapted from the epistemic plan §9 (Hyrum→contracts inventory · Gall→read-only+strangler-seam (enrichedVariant precedent) · Leaky-Abstractions→the GL trap class · Tesler→C1 · Zawinski→permanently-out scope line · Bus-factor-1→the persistence loop + inlined engine · Knuth/Goodhart/Gilb→measured-numbers discipline · Parkinson→burst-only cadence · Postel→liberal-EXIF/strict-C6 · Broken-Windows→Track-E-first + dated hint baseline · Pesticide→re-mine-traps step · Lehman→external-data currency · skips documented: Conway/Brooks-class team laws, CAP/distributed (edge-cache lesson already a Trap), scale laws, Second-System watch-flag) · **`tracked-backlog.md` = the ONE debt registry (T1–T23, dated, git-tracked — carried tails MOVED here; NEXT_SESSION_PROMPT is gitignored so the prompt now mirrors the registry, not vice versa)**. SKILL.md: +Audit type row (comprehensive-review order ⇒ Audit; READ-ONLY, fixes = sliced sessions) + 4 anti-pattern rows (audit-edits-src, re-raising tracked rows, zero-result validation, rendered-geometry-not-properties) + "audit"/"comprehensive review" triggers. NEXT_SESSION_PROMPT rewritten: next session = the FULL AUDIT (Deep, baseline anchor = the 2026-08-13 restructure+M0 line; audit owns authoring `conventions/contracts.md` = T23) → fix slices → release canaries (T2/T3) → Phase 8a. `mem:project/wip-2026-08-13-planning-core-restructure` §audit-mode extension. [Note 2026-08-13 audit #1: the next entry's header was reconstructed after this entry's working-tree prepend spliced into it and ate the header — finding D1, `audits/audit-full-2026-08-13.md`; every surviving character preserved.]
- **2026-08-13 (PLANNING LADDER PROMOTED TO CORE — desktop-first, then mobile — + MOBILE M0 SHIPPED. Gates vitest 704/704 (+3) · astro 0/0 · wix build Complete (26 routes incl. `/m`) · browser-VERIFIED via Playwright-over-CDP on wix dev; the REAL-DEVICE render/touch check = the M0 exit gate, still OPEN).** OWNER RE-RULING: the `MOBILE_PLAN.md §5` feature ladder (P1–P10 + the scheduled backlog + ALL future planning-app features) is **CORE scope** — NEW `IMPLEMENTATION_PLAN.md §Phase 8` (8a darkness/galaxy · 8b search/exposure · 8c events · 8d session tools · 8e ambience/flagship) owns the schedule; build rule = pure lib+vitest → **desktop surface** → mobile surface (a mobile surface never precedes its desktop twin); MOBILE_PLAN M0–M2 stay mobile infrastructure, M3–M6 re-scoped to the mobile SURFACES of 8a–8e (**SUPERSEDES** the 2026-08-11 mobile-only scheduling of those features); the desktop freeze is AMENDED additively (shipped chrome/behavior stays frozen, new planning surfaces allowed); the AI panel stays OUT. Docs: IMPLEMENTATION_PLAN (+Phase 8 + active-track callout) · MOBILE_PLAN (header/§5/§6 annotations + log line) · ARCHITECTURE (§4 ladder bullet + §7 `components/mobile/`). **M0 BUILD:** `pages/m.astro` + `layouts/MobileLayout.astro` (viewport-fit=cover) + `components/mobile/MobileShell.tsx` (status strip, store-free) + `styles/mobile/mobile.css` (100dvh + `touch-action:none` — global.css says 100vh); **mobile texture tier** = NEW `DeviceCaps.coarsePointer` (matchMedia pointer:coarse) caps `detectDeviceTier` AND the governor ceiling at `mid` (an iPhone otherwise detects HIGH — "Apple GPU" passes STRONG_GPU and mem/cores default to 8) + NEW `allow8k` opt threaded GlobeCanvas→StylizedTiles→{baseEarth skips the 3×8k swaps (the `maxTextureSize≥8192` gate alone can't — phones report ≥8192), stars loads the NEW 2k milky-way}; `milkyway-2020-2k.jpg` (572 KB vs 6.4 MB; ~8 MB vs ~134 MB VRAM) baked by `scripts/build-milkyway-2k.mjs` — **LINEAR-light Lanczos resize** (the SVS flux-per-pixel trap, DECISIONS 2026-07-15), landmark patch means 0.998 bulge / 0.984 LMC / 0.949 dark vs the 8k (PASS gate ≤12% drift); desktop deltas (owner-ratified 2026-08-11): topnav `Mobile` link + dismissible coarse-pointer banner (localStorage `ftw:m-banner-dismissed`). VERIFIED (Playwright/CDP, 402×874 + 1400×900): `/m` renders the LEO signature scene + idle drift + `#p=` writer; coarse-shim boot → tier `mid`, ONLY the 2k haze fetched (zero 8k requests); fine-pointer desktop **byte-identical** (tier high, all 8k swaps, banner zero-box); banner show → dismiss → stays-hidden proven by RENDERED GEOMETRY, not the `hidden` property. **NEW TRAPS:** an author `display:flex` BEATS the `hidden` attribute's UA display:none — pair any styled `[hidden]` element with an explicit `.x[hidden]{display:none}` (a property-level check read "hidden" while the banner RENDERED; the screenshot caught it) · Playwright `addInitScript` on a CDP-attached page takes effect only from the NEXT navigation (hop `about:blank`; a hash-only goto is SAME-document — no new document, no init script) · background-tab CLICKS starve on rAF-based actionability — `page.bringToFront()` before clicking (the occluded-Chrome trap generalizes beyond timing). UNVERIFIED: real-device iPhone/Pixel render + touch-orbit (owner hardware — the M0 exit criterion) · `/m` + `/_wix/pages.json` registration on Wix cloud (release canary). `mem:project/wip-2026-08-13-planning-core-restructure`.
- **2026-08-11 (MOBILE PLAN RATIFIED — owner closed all 3 [OPEN] items; schedule extended M0→M6; AI PANEL OUT OF ALL PLANS).** Rulings, same day as the design: **(1)** `/m` gets a permanent desktop-topnav link in addition to the `pointer:coarse` banner (both = M0 scope; still the only desktop deltas). **(2)** Mobile = **planning-only PERMANENTLY** — upload/marketplace/pins-browsing never ship on `/m`, but EVERY current + future planning feature does → the researched backlog is now scheduled: **M5** (P10 sensor-frame/mosaic · star-trail simulator + gap/stacking math · long-exposure/ND + timelapse calcs · What's-Up-Tonight ranking) + **M6** (light-pollution drape + Bortle · ISS passes via satellite.js + cached `/api/tle` CelesTrak proxy · Web-Push alerts — NO-CRON caveat: external scheduler or on-open recompute · solar-eclipse umbra path on the globe). Permanently out: Gaia-depth catalogs · GOTO · tides/rainbow · Skyfire. **(3) Phase 7 (AI panel) is out of scope for ALL plans** — IMPLEMENTATION_PLAN Phase 7 annotated ⛔ PARKED, NEXT_SESSION_PROMPT retitled to "Mobile M0"; **M0 starts next session**, nothing schedules after M6. Files: `MOBILE_PLAN.md` (§1/§5/§6/§7 + decision log) · `NEXT_SESSION_PROMPT.md` · `IMPLEMENTATION_PLAN.md`. Memory: `mem:project/wip-2026-08-11-mobile-design`.
- **2026-08-11 (MOBILE SUPPORT DESIGNED — `MOBILE_PLAN.md` shipped; no code. Design mode, Deep tier, 3 parallel evidence-cited research tracks).** Owner ask: true mobile (iPhone 17 Pro / Pixel 6 Pro) for PLANNING (Stellarium × PhotoPills) — globe + FPV touch nav + compact scrubber + search; desktop FROZEN. **DECISION: separate `src/pages/m.astro` + thin shell** (`src/components/mobile/**`, `MobileLayout.astro`) over responsive retrofit — platform-proven (`output:"server"`, `/api/ping` prod canary, `@wix/astro-pages` route registry) with the only desktop delta a dismissible `pointer:coarse` banner; **NO middleware/UA redirect** (CDN `Vary: User-Agent` unverifiable + the sharded-edge-cache precedent). Research: orbit touch ALREADY works (3d-tiles-renderer EnvironmentControls pinch/touch, source-verified, device-UNVERIFIED); FPV look-drag works but walk=keys-only, FOV=wheel-only, pin drop=dblclick-only → **4 additive engine seams**: analog walk via store/camera into the world-space `fpvWalkOffset` (same invariant as the pivot fix below) · pinch-FOV in `onFpvPointer*` · long-press pin drop · `&sky=<id>` hash. All planning logic confirmed store-mediated (planFeed→store/plan, request*/aimAtSky seams; mobile reuses LIBS never desktop panels; lazyContract test already fences the whole of src/). Sensors verified: iOS = gesture-gated `requestPermission()` + `webkitCompassHeading` (no absolute event); Android = `deviceorientationabsolute`; Wake Lock iOS 16.4+. Feature gap analysis (PhotoPills/Stellarium): P1 twilight bands · P2 MW band + GC first-class target (IAU galactic rotation — pure math) · P3 MW season calendar + moon-interference score · **P4 Find az/el date-search FILTERED BY REAL SKYLINE (category-first — no competitor has it)** · P5 NPF/500 spot-stars (D850 test vector ≈16.3 s) · P6 moon calendar · P7 meteors (bake IAU MDC, avoid GPL showers.json) · P8 conjunctions · P9 lunar eclipses · P10 sensor-frame overlay; ISS needs `/api/tle` proxy (CelesTrak CORS). Phases M0 (route + REAL-device render proof — perf risk gated first) → M1 planning loop → M2 FPV touch → M3 features → M4 AR aim + events + PWA. Open for owner: /m in desktop nav? · marketplace on mobile ever? · M-phases vs Phase 7 priority. Files: `.claude/claude-docs/MOBILE_PLAN.md` (new). Memory: `mem:project/wip-2026-08-11-mobile-design`.
- **2026-08-11 (FPV WALK PIVOT-ELLIPSE BUG FIXED + Shift/Option speed modifiers. Gates vitest 701/701 · astro 0/0 · browser-VERIFIED in wix dev via Playwright).** Owner report: standing FPV look-around pivots correctly, but after WALKING with arrows the look swings the camera in growing ellipses, "as if part of the pivot was left where I initially stood." **ROOT CAUSE (source-confirmed):** `stepFpvPose` stored the walk as two SCALARS (`fpvWalkFwd`/`fpvWalkRight`) re-applied EVERY FRAME along the CURRENT look basis — position = anchor + fwd(yaw)·a + right(yaw)·b, so any head-turn re-aimed the ACCUMULATED displacement, orbiting the eye around the anchor at walk radius (ellipse = circle × the horizontal pitch-projection wobble). **FIX:** the walk is now a persistent WORLD-SPACE `fpvWalkOffset` (THREE.Vector3, ECEF m) integrated incrementally at key-hold time along THAT frame's horizontal look + right; `camera.position.add(fpvWalkOffset)`; only key-holds mutate it — a look never does (true-FPV invariant). Reset `.set(0,0,0)` at both FPV entry sites. Curvature error of the straight-line offset ≈0.009°/km up-drift — irrelevant at walk scale. **MODIFIERS:** Shift+arrows = ×3 sprint, Option/Alt+arrows = ×0.5 creep (`FPV.walkFastMult` 3 / `walkSlowMult` 0.5 in tuning.ts); the keydown guard previously ATE `altKey` (Option+arrow never walked) — now only meta/ctrl are excluded, and `fpvKeysDown.shift/.alt` mirror `e.shiftKey/.altKey` on EVERY keydown+keyup so pressing/releasing a modifier mid-stride retunes the speed live (no arrow re-press). **Browser-VERIFIED** (temp-pin FPV via `#f=` boot hash, Dnipro, `__globe.camera` probe, 53 fps): plain 1.5 s walk = 33.07 m (22 m/s ✓); after walking, a 60.6° look-drag + a reverse drag both moved the camera **0.000 m** (the bug would have shown ~33 m orbit); Shift ratio **3.009** · Option ratio **0.49997**. Hash mirror kept tracking the walked ground point. Shot `verify-shots/fpv-walk-pivot-fix-01.jpeg`. TRAP (session ops): piping backgrounded `wix dev` through `head` SIGPIPE-kills it once the spinner passes the line budget — run it bare; it serves on :4322 when :4321 is taken. Files: `globe/StylizedTiles.ts` (state decl · onFpvKey/onFpvKeyUp · both FPV entry resets · stepFpvPose walk block) · `globe/tuning.ts` (FPV.walkFastMult/walkSlowMult). Memory: `mem:bugs/fpv-walk-orbit`.
- **2026-08-10 (ASTRO ENGINE PHASES B+D+E SHIPPED + constellation search — "search and track ANY object" is now data-complete. Gates vitest 701/701 (+27) · astro 0/0 (hints at baseline 6) · wix build Complete · browser-VERIFIED in a VISIBLE window via CDP :9222).** **B · DATA — every source baked behind ONE lazy chunk** (424 KB `catalog.*.js`, boot chunk untouched — verified in `dist/` (only dynamic `import()` from LocationFinder + store/sky reference it) AND guarded by new `test/lib/sky/lazyContract.test.ts` (walks `src/`, fails on any static import of a heavy sky module from outside `lib/sky/`)): **(1) full OpenNGC** — `scripts/build-openngc-catalog.mjs` → `public/data/openngc.bin` (13,263 records × 20 B LE + "ONGC" header; Dup/NonEx/Messier-covered dropped) + `ngcNames.ts` (type/const/suffix tables, 61 non-NGC/IC extras, 142 common names); runtime `lib/sky/openngc.ts` (pure parser — vitest feeds the committed bin from disk); search philosophy: common names ride the fuzzy index, the anonymous 13k are reachable ONLY via the catalog-id pattern branch (`searchNgcById` — "ngc 891"/"ic434"/"b33"), so queries never drown in field galaxies. **(2) IAU star names** — `build-star-names.mjs` → `starNames.ts` (451 WGSN names WITH their own RA/Dec — no bsc5.bin re-bake needed; 333 carry HR); index keys expand the CSN greek bayer ids (**"alf" not "alp" — browser-caught**; `GREEK_ABBR` covers the 24 forms actually baked) + constellation name/genitive → "alpha lyrae"→Vega. **(3) 88 constellations** — `build-constellations.mjs` → `constellations.ts` (name/genitive/abbr/centre/rank; Serpens merged) + `public/data/constellation-lines.json` (all 88 figures; the ambient asterisms keep the curated 26). NEW first-class `TargetKind "constellation"`: fixed target at the figure centre, facts arm + panel card, glyph ✶. **(4) 952 MPC comets** — `build-comet-catalog.mjs` (CometEls JSON, daily) → `comets.ts`; 10P skipped (the hand-baked lightcurve profile stays canonical); **`cometTarget` id derivation FIXED for C/-designations** (`cometShortDesignation`: "C/1995 O1 (Hale-Bopp)" → "C/1995 O1", not "C"); no-H rows map magnitude → null (never NaN). **(5) 337 bright asteroids** — `build-asteroid-catalog.mjs` (SBDB query, numbered, H<9, a<6 au, **`full-prec=1` — the default ~4-digit elements cost 10–15″**, G defaulted 0.15 where SBDB has none (246/337)) → `asteroids.ts`. **PROPAGATOR REWRITTEN UNIVERSAL-VARIABLE** (`comet.ts perifocalAt`: Stumpff c2/c3, perihelion-anchored, ONE code path for every conic; elliptic wraps Δt to nearest perihelion; **TRAP caught by the 10P Horizons fixtures: Vallado's α is 1/a — folding μ into it corrupts ψ** and drifts 0.23°@2mo); `CometElements` split into brightness-free **`KeplerElements`** + m1/k1 on top; **IAU (H,G) Bowell-1989 law** (`hgMagnitude`, phase angle from the r/Δ/R triangle, coefficients 3.33/0.63/1.87/1.22) behind new `magnitudeModel:"hg"` + `asteroidTarget` provider + panel card. Horizons-locked (2026-08-10 00:00 UT rows): Ceres ≤30″ + APmag ±0.2 · Vesta ≤30″ · **Hale-Bopp (e=0.9949, Δ=50.6 au) ≤2′ — the near-parabolic proof** · Encke ≤2′ (`test/lib/sky/phaseB.test.ts`). **(6) long tail** — `lib/sky/simbad.ts` (CDS sync TAP, ADQL ident⋈basic⋈allfluxes, CORS `*`, 8 s timeout, in-flight dedupe, localStorage cache hits 30 d/misses 1 d; live-verified "sh2-155"→LBN 110.11+02.44) + `lib/sky/sbdb.ts` behind `/api/sbdb` (**the phase-A zero-callers tail wired**; **SBDB TRAP: no `phys_par` unless `phys-par=1` — relay allowlist widened**, browser-caught when 2024 YR4 parsed H-less); fallback fires DEBOUNCED (`SEARCH.skyLongTailDebounceMs` 600) when local rows are empty **OR the query is designation-shaped without an exact local key match** (2nd browser catch: "2024 YR4" drowned under the C/2024 R4 typo family; `looksLikeSmallBody` moved to boot-safe `searchIndex.ts`); resolved objects lead the rows, persist via their caches, and `targetById` falls through to them. **(7) persistence** — `skyTargetId` pref; store/sky restores the last-tracked id AFTER boot (requestIdleCallback, keeps the lazy contract; guarded against a user pick during the wait; browser-verified `ngc:NGC7000` across reload). Index: 1,947 entries + 13k pattern-reachable. **D · RENDER** — `scene/skyTarget.ts` uMode 3 planet treatment: phase-lit disc (moon lambert on the billboard; `uSunLocal` in billboard space, +Y = projected celestial north ⇒ TRUE terminator orientation; radius floored `SKY_TARGET.planetDiscMinDeg` 0.4°, true size wins at long focal) + **Saturn ring band at the REAL pole** (`saturnRingPoleDir` — IAU pole through the same `ecefFrameAt` path; projected opening = signed sin B ≈ −0.16 in 2026, near edge-on; the pole-side arm masks BEHIND the disc, near arm crosses in front; Cassini-division dip baked; `SKY_TARGET.ringGain` 0.55); **star colour** — `bvToRgb` in `lib/ephemeris/stars.ts` (Ballesteros-2012 temperature + blackbody sRGB fit, max-normalized so tint never double-dims) → per-star `aTint` attribute at catalog swap, blended `STARS.bvTintAmount` 0.6 (geometry-verified live: 9,096 stars, 7,609 clearly tinted); **constellation figure highlight** — tracked constellation's figure renders accent via `stars.update({constellation})` + lazy 88-figure asset (`figureSegmentsByAbbr` in asterisms.ts), gated by TARGET SHOW, any camera mode. **E · PLANNER** — `skylineState` core generalised over an injected az/alt sampler (`sampledSkylineState` — the same move `scanWindows` made; sun/moon output test-locked unchanged); `targetSkylineState(target,…)` through the SAME `targetAzAlt` face; planFeed computes a third row (gated TARGET SHOW; target-swap invalidates the scan), mirrored as **`store/plan.target` (`PlanTargetState` = body state + id/label/glyph)** → PlanPanel row + TargetPanel **SKYLINE CLEAR / BEHIND SKYLINE badge**; browser-verified BOTH branches (Saturn CLEAR + HIDES 02:54 · Orion BEHIND + CLEARS 04:32). **VERIFY (visible-window, wix dev + own headed Chrome :9222):** search battery (vega · alpha lyrae · ngc 7000 · ceres · hale-bopp · orion · andromeda→constellation-first with M31 #2 · jupitre) all correct rows; SIMBAD + SBDB live; Saturn 500 mm FPV flagship shot (disc + edge-on ring + reticle + trail); Orion figure + PLAN row; star-tint geometry probe; reload restore. Shots `verify-shots/astroB-01…04*.jpeg`. WATCH: one non-converged FPV auto-aim glide during rapid consecutive re-tracks (direct `requestSkyLook` lands exactly; not reproduced in isolation). TAILS: `/api/sbdb` on Wix CLOUD still UNVERIFIED (dev-verified incl. phys-par; first-egress question rides the next release) · Galilean-moon points (render-table wish) unbuilt · owner taste-pass on ringGain/highlightAlpha/bvTintAmount pending. Files: `lib/ephemeris/comet.ts`·`targets.ts`·`stars.ts`·`asterisms.ts`·`planner.ts` · `lib/sky/{catalog,searchIndex,openngc,simbad,sbdb,starNames,constellations,comets,asteroids,ngcNames}.ts` · `lib/prefs.ts` · `store/{sky,plan}.ts` · `scene/{skyTarget,stars,planFeed}.ts` · `StylizedTiles.ts` · `tuning.ts` · `panels/{LocationFinder,TargetPanel,PlanPanel}.tsx` · `styles/{plan-panel,target-panel}.css` · `pages/api/sbdb.ts` · scripts `build-{openngc-catalog,star-names,constellations,comet-catalog,asteroid-catalog}.mjs` · `public/data/{openngc.bin,constellation-lines.json}` · tests `phaseB`/`lazyContract`(new) + comet/planner/stars/searchIndex extended. Memory: `mem:project/wip-2026-08-10-astro-engine-phase-bde`.

- **2026-08-03 (ASTRO ENGINE PHASE C SHIPPED — the owner's 5-item feedback batch: sky TRAIL + SHOW rename · marker click-to-aim · post-search auto-aim · N-target edge chips · comet-era hardcode cleanup. Gates vitest 674/674 (+5) · astro 0/0 · browser-VERIFIED end to end in a VISIBLE window).** All five owner rulings from the phase-A test, in order: **(1) TRAJECTORY — `scene/skyTrail.ts` (new):** the tracked target's az/alt path across the observer's local solar day, the dayArcs grammar generalised (shared `makeArcMaterial`/`horizonFade`/`pointDirs` now EXPORTED from `scene/dayArcs.ts`; alpha-blended + depth-free so it reads at noon; hour ticks; past/future split; accent colour so it reads as the marker's own line). Sampler = new `sampleTargetArc(target,…)` in `lib/ephemeris/dayArc.ts` via `targetAzAlt` — one target, one ephemeris, three faces (marker/trail/panel can never disagree); `SkyArc` = the body-agnostic shape, `DayArc extends SkyArc`. Anchored at the SAME eye the panel prints (plan anchor ?? view focus); rebuild deadband `SKY_TARGET.trailRebuildMinDeg` 0.05° (an orbit pan must not re-run the ~170-call arc per frame); same impostor distance as the marker ⇒ the marker sits ON its trail (browser shot). Panel toggles now **SHOW · MARK · TRAIL** (SKY→SHOW rename; TRAIL new, persisted, ON by default). **(2) MARKER CLICK:** the billboard keeps `raycast=()=>{}` (it is far bigger than the visible mark) — the hit test is ANGULAR: `trySkyMarkerClick` in the orchestrator compares the click ray to `targetDirW` against the LIVE ring radius (`SkyTargetHandle.hitRadiusDeg()` = current ring × 1.25 slack — it widens for extended objects, M31 hit ≈2.5°); wired into BOTH pointer paths — orbit `onPointerUp` (placing > marker > pins priority) and FPV `onFpvPointerEnd` (new `fpvDownX/Y` click-vs-look-drag separation, `pointerup` only). Hit → aim + `setOpen(true)`. **(3) AUTO-AIM from `LocationFinder.track()`:** after a SKY pick, if the target is above the horizon at the panel's eye → aim immediately; below → camera stays put (NEXT SESSIONS is the tool there). **Shared aim idiom extracted to `store/skyAim.ts`** (`aimAtSky(az,alt)`: FPV → `requestSkyLook`; orbit → `setTargetHeading` + raise-only tilt capped 88° — `FpvHud.bringIntoView` deduped onto it). Browser-verified both branches: orbit pick → th/tt set + consumed, heading lands on the azimuth; FPV pick → `skyLook` set, look glide centres M31 to NDC (−0.001,−0.004). PLATFORM LIMIT (expected, chip-doc'd): at altitude GlobeControls caps tilt (~74° at 246 km) — a high target stays best-effort above the frame top; at 4 km tilt 88 reaches it. **(4) EDGE CHIP for the tracked target:** `camera.ts` `skyMarkers` widened from hard `{sun,moon}` to `SkyMarkers{sun|null, moon|null, target: SkyTargetMarker|null}` (marker + kind glyph + `targetShortName` label); `stepFpvHudAndSkyMarkers` computes the bearings reference when guides OR the target want it (sun/moon slots ride the SKY-guides chip, target rides the TARGET SHOW toggle — verified independently gated in-browser); FpvHud renders per-slot chips, target chip = glyph + label + arrow (`fh-chip--target`, accent) and its click ALSO fronts the panel. **(5) CLEANUP:** tuning split **`SKY_TARGET`** (reticle/night-gate/impostor/pointCore + trail deadband + click slack — everything generic) out of **`COMET`** (coma/tail look ONLY); `scene/skyTarget.ts` re-pointed (~20 refs); prefs renamed `cometVisible/cometHighlight` → **`skyTargetVisible/skyTargetHighlight`** (+ new `skyTargetTrail`) with a read-old-keys fallback in `sanitizeViewPrefs` (new key wins; old blobs keep their chips — browser-verified: `{cometVisible:false}` → reload → `visible:false`, first save rewrites under new names); `pillLabel` deduped onto new `targetShortName()` (targets.ts); `ecefFrameAt` doc now names all three provider layers. **VERIFY NOTE (supersedes part of the phase-A trap):** the Playwright MCP here rides a HEADED Chrome via CDP (:9222) — `/json/activate/<id>` makes the tab truly visible (`document.hidden:false`, real 60 fps rAF) and everything lands in seconds; the rAF-shim + 1 s-frame dance is only needed while hidden, and a MessageChannel "unthrottled rAF" pump is a TRAP (floods the task queue, starves CDP evaluate — the tab needs force-closing via `/json/close`). Files: `scene/skyTrail.ts`(new) · `store/skyAim.ts`(new) · `scene/dayArcs.ts` · `scene/skyTarget.ts` · `lib/ephemeris/dayArc.ts`·`targets.ts`·`bodies.ts` · `globe/tuning.ts` · `StylizedTiles.ts` · `store/camera.ts`·`store/sky.ts` · `lib/prefs.ts` · `panels/TargetPanel.tsx`·`FpvHud.tsx`·`LocationFinder.tsx` · `styles/fpv-hud.css` · tests: `dayArc.test.ts`(+3) · `prefs.test.ts`(+2) · `store/camera.test.ts`(shape). Shots `verify-shots/astroC-01-trail-on/02-trail-off/03-fpv-trail-marker.jpeg`. Memory: `mem:project/wip-2026-08-03-astro-engine-phase-c`.

- **2026-08-03 (OWNER TEST OF PHASE A — "generally works but"; 5 rulings queued as the next batch, detailed in `ASTRO_ENGINE_PLAN.md` phase C + `NEXT_SESSION_PROMPT.md`).** (1) sky TRAJECTORY for the tracked target: build it (`scene/skyTrail.ts`), **ON by default**, toggle beside the panel toggles, and **rename the `SKY` toggle → `SHOW`**; (2) clicking the sky marker centres the target in view (FPV) + opens the panel if closed; (3) after a SKY-search track, auto-aim at the target when reasonable (position/orientation-wise); (4) the tracked target gets the sun/moon-style dynamic EDGE CHIP (pill + direction arrow — widen `skyMarkers` from hard `{sun,moon}` to N); (5) generalize the comet-era hardcode — split a `SKY_TARGET` tuning group out of `COMET` (20 generic refs in `scene/skyTarget.ts`), rename prefs `cometVisible/cometHighlight` WITH a read-old-keys migration. The owner's test resolves the "treatments visually UNVERIFIED" tail from the phase-A line below.
- **2026-08-03 (ASTRO ENGINE PHASE A SHIPPED — SkyTarget seam · EARTH⇄SKY search · planets + 110 Messier + 10P through ONE interface · /api/sbdb proxy. Gates vitest 669/669 (+30) · astro 0/0 · browser: state+UI VERIFIED on wix dev, new marker treatments visually UNVERIFIED).** Phase A of `ASTRO_ENGINE_PLAN.md`, end to end: **(1) `lib/ephemeris/targets.ts` (new)** — the load-bearing `SkyTarget` interface (`stateAt(utcMs) → {dir, raDeg/decDeg, distanceAu, magnitude+model+uncertainty, elongation, phase, angularDiam, tailDir}` + typed per-kind `facts`) with THREE providers: **fixed** (J2000 RA/Dec → precession/nutation + sidereal spin via the shared `ecefFrameAt` — the one frame conversion), **engine** (astronomy-engine `GeoVector`+`Illumination`, 8 planets + Pluto, runtime-probed: Pluto works, Saturn's mag includes ring tilt, NO built-in angular diameter ⇒ own radii table), **kepler** (the 10P propagator, already profile-parameterized — **10P is now `cometTarget(TEMPEL2)`, not a special case**). `targetAzAlt()` = the shared topocentric face (parallax only when `distanceAu` is real). Tests cross-check az/alt against the library's OWN `Equator`/`Horizon` pipeline (independent path, ≤0.05°, three instants) + `DefineStar` for the fixed provider. **(2) planner:** `cometWindows` scan generalised to `scanWindows(sampler)`; new `targetWindows(from, o, target, opts)`; `cometWindows` kept verbatim-compatible (`CometWindow = TargetWindow` alias) — test-locked equal output. **(3) Data:** `scripts/build-messier-catalog.mjs` (house bake pattern: fetch OpenNGC NGC.csv+addendum.csv → pack → spot-check fails loud) → generated `lib/sky/messier.ts`, **all 110 Messier, 24 KB raw, CC-BY-SA-4.0 attribution in the header + panel source line + search credit**. TRAPS: the addendum's M102 row carries `M=101` (the NED duplicate cross-ref) — the `Name` column must win or M101 gets silently overwritten; OpenNGC centers Pleiades on Alcyone, 0.25° from SIMBAD's centroid ⇒ wide-cluster spot checks need 0.5° tolerance. M102 ships as OpenNGC's `Dup` stance and the panel SAYS the id is contested. **(4) Search:** `lib/sky/searchIndex.ts` (pure: normalize incl. greek α→alpha + tier ladder exact/prefix/token/substring/OSA-edit-distance≤2 + fame/brightness boost) + `lib/sky/catalog.ts` (index + `targetById` resolver) — **LAZY by contract**: only reached via `await import()` from the first SKY interaction; boot chunk unchanged. Grammar test-locked: `m31`/`M 31`/`messier 31`/`ngc224`/`andromeda`/`andromedia`/`saturm`/`jupitre`/`10p`/`orion nebula` all resolve; `m1` exact beats the m1x prefix family. **(5) UI:** LocationFinder grew the EARTH|SKY segmented toggle (one transform on `.lf` — DragGrip contract kept); SKY swaps placeholder/rows (kind glyph + type + constellation + mag)/action (**track** = `setTarget`+`setVisible`+panel open, instead of fly-to) + credit line. `CometPanel`→**`TargetPanel`** (`styles/target-panel.css`, cp-→tp-, look unchanged): generic live block (ALT/AZ · MAG±model · ELONG · RA/Dec · Δ/r) + per-kind cards (comet: perihelion/closest chips + object card + staleness warning; planet: blurb/phase/true disc″; DSO: type/constellation/real extents/names/OPENNGC) + per-model magnitude honesty note + **NEXT SESSIONS now works for ANY target** via `targetWindows`. `store/comet`→**`store/sky`** (`target` field; prefs keep the ORIGINAL `cometVisible/cometHighlight` key names — renaming drops saved chips; `__cometStore`→`__skyStore`). **(6) Scene:** `scene/comet.ts`→**`scene/skyTarget.ts`** — same camera-anchored impostor/night-gate/horizon-fade machinery, THREE treatments switched by uniform: comet (coma+tail, verbatim 10P look), point (star-like, gain from magnitude — a LINEAR overlay ramp, NOT flux: mag −4 Venus at true ratio would be 10⁷× Pluto), ellipse (galaxies/nebulae/clusters at REAL major/minor arcmin + position angle vs projected celestial north; ring auto-widens to frame the object). Orchestrator: `stepComet`→`stepSkyTarget` (+ immediate re-sample on target-id change — the 1 s cadence would show the old body's direction under the new treatment for a beat). **(7) `/api/sbdb` (new, first outbound-fetch route):** GET-only pass-through to `ssd-api.jpl.nasa.gov/sbdb.api` (JPL sends no CORS — 2026-08-03 finding), allowlisted params, 8 s timeout, relays SBDB's 200/300/404 as payloads — dev-VERIFIED: 400 guard · 10P orbit JSON · `tempel` ambiguity list. **VERIFY TRAPS (cost ~40 min):** a hidden Playwright window **fully SUSPENDS rAF** (not throttle — zero frames; `document.hidden` stays true even after `bringToFront` + CDP window-state) ⇒ every `__globe.bodies()` read shows the BOOT sample and a target swap "never lands" — the false lead even produced stale-`?t=` dual-module-instance theory (StylizedTiles served `store/sky.ts?t=…`, panels unstamped) before the rAF shim (`addInitScript` rerouting rAF→setTimeout when hidden) proved the UI flow works end to end (orchestrator picked up Saturn: real mag 0.50, mesh visible). Hidden-tab timers then clamp to ~1/min (Chrome intensive throttling) ⇒ long scene sequences need a VISIBLE window. UNVERIFIED tails: point/ellipse treatments + widened ring never SEEN rendered (only mesh-visible + uniforms asserted) — first visible-window session should eyeball Saturn point + M31 ellipse + ring radius; `/api/sbdb` on Wix cloud. Files: `lib/ephemeris/targets.ts`(new)·`planner.ts` · `lib/sky/{searchIndex,catalog,messier}.ts`(new) · `scripts/build-messier-catalog.mjs`(new) · `store/sky.ts`(new, replaces `store/comet.ts`) · `scene/skyTarget.ts`(new, replaces `scene/comet.ts`) · `panels/TargetPanel.tsx`(new, replaces `CometPanel.tsx`) · `panels/LocationFinder.tsx` · `styles/target-panel.css`(replaces comet-panel)·`styles/location-finder.css` · `pages/api/sbdb.ts`(new) · `StylizedTiles.ts`·`prefs.ts`·`global.d.ts`·`index.astro` · `test/lib/ephemeris/targets.test.ts`(new)·`test/lib/sky/searchIndex.test.ts`(new). Shots `verify-shots/astroA-01/02`. Memory: `mem:project/wip-2026-08-03-astro-engine-phase-a`.
- **2026-08-03 (COMET MAGNITUDE CORRECTED — owner catch; + hairline marker style; + ASTRO ENGINE plan. Gates vitest 639/639 (+9) · astro 0/0 · browser-VERIFIED).** **(1) THE BUG:** the shipped panel said 10P was **mag 12.8 / "TELESCOPIC ≈150 mm"**; owner said ~8 and binocular. **He was right — the positions were never wrong (≤3″ vs Horizons), the BRIGHTNESS MODEL was.** JPL's `M1/k1` are an automated fit to the magnitudes attached to ASTROMETRIC submissions — CCD frames through a TIGHT photometric aperture that misses most of an extended coma. Ground truth from **COBS** (cobs.si), same nights late July: **8.2/8.6/8.9/9.0 from 5–11 cm wide-field** vs **10.3–11.5 from 28–50 cm**; median of the last 14 d = **8.4**. The aperture effect IS the story. **FIX:** two labelled models, never one silent number — new `CometLightCurve` (`m = h + 5·log10Δ + 2.5·n·log10 r`) fitted to the COBS wide-field/visual subset (aperture ≤15 cm or ICQ method `T`): **h −0.76 · n 29.3 · rms 0.80 · N 215 · valid 2026-05-01…2026-10-31** (monthly residuals May −0.1 · Jun +0.2 · Jul +0.0 · Aug −0.6); `jplMagnitude()` kept, labelled, used OUTSIDE the window with a ±2 band; `cometBrightness()` → `{magnitude, model, uncertaintyMag}`; `visibilityClass()` → "BINOCULARS (10×50)". Panel now reads **MAG 8.4 ±0.8 · BINOCULARS (10×50)** + a note naming the model in force. **WHERE THE WINDOW STARTS IS PHYSICS, NOT FIT-SHOPPING:** one power law over the full approach missed March by 2.4 mag (the new script's guard fired and blocked the bake); the models SWAP as the coma inflates — April: COBS 16.0, **JPL 15.9 good**, observed-curve 17.0 bad; June: COBS 10.8, JPL 13.8 (3 mag faint), curve 11.0 good — so pre-May deliberately falls through to JPL, and both behaviours are test-locked. New `scripts/fit-comet-lightcurve.mjs` re-bakes from COBS + prints monthly residuals + **fails loud >1.5 mag**. **(2) MARKER STYLE (owner: "crude and obscures the view — much thinner, subtler"):** the reticle is now a **broken hairline ring** — stroke 0.035→**0.012** of radius (~1 px at 38° FOV/1600 px), radius 1.15→**1.5°** so it frames rather than crowds, gain 0.8→**0.45**, ring BROKEN into four arcs with the gaps on the axes and hairline ticks in the gaps. TRAP: the first tick attempt used an ANGULAR wedge — constant-arc, so at that radius it drew stubby blobs; ticks must be masked by PERPENDICULAR distance to the nearest axis. This is now the house style for every future body type. **(3) NEXT-SESSION PLAN — `ASTRO_ENGINE_PLAN.md`** (owner ask: search any celestial body, track it like the comet, trajectory + off-screen hint + info panel, realistic per-category treatment). Two findings that shape it, both verified with `Origin:` requests: **JPL's SBDB and Horizons APIs send NO `Access-Control-Allow-Origin`** (OPTIONS → 405) ⇒ NOT browser-callable, small bodies need a thin `GET /api/sbdb` pass-through; **SIMBAD TAP and VizieR DO send `*`** ⇒ they are the client-side fallback. Honest inventory for the owner's "some data may already be there": 9,096 BSC5 stars carry **no names**, there are **zero** deep-sky objects, one comet, zero asteroids — everything past the planets is new data (bake list + `SkyTarget` interface + phases A–E in the doc). Carried gap named there: the comet has **no projected sky trajectory** yet (`scene/dayArcs.ts` is the renderer to generalise). Files: `lib/ephemeris/comet.ts` · `panels/CometPanel.tsx` · `styles/comet-panel.css` · `globe/tuning.ts` · `globe/scene/comet.ts` · `scripts/fit-comet-lightcurve.mjs`(new) · `test/lib/ephemeris/comet.test.ts` · `ASTRO_ENGINE_PLAN.md`(new) · `NEXT_SESSION_PROMPT.md`. Shots `verify-shots/comet-13/14`. Memory: `mem:bugs/comet-magnitude-model`.
- **2026-08-02 (COMET 10P/TEMPEL 2 TRACER — temporal, LOCAL-ONLY owner ask; gates vitest 630/630 (+21) · astro 0/0 · browser-VERIFIED on wix dev via Playwright-MCP-over-CDP).** Owner: "enable 10P/Tempel visibility in the sky, add its main info, highlight it, render it with the same mechanisms as sun/moon so I can trace it and predict the next astro-gazing session — keep changes local." **Why now:** 10P hit **perihelion 2026-08-02 02:44:45 UTC** and passes **0.4145 au (62.0 M km) on 2026-08-03 ≈21:00 UTC**, near opposition (elong 164°) — its best apparition until the 2030s, at **mag ≈12.8 (telescopic)**, culminating only ~16.6° due south from Dnipro at ≈22:40 UTC. **(1) Ephemeris `lib/ephemeris/comet.ts` (new, pure):** astronomy-engine has NO small-body support ⇒ own two-body propagator over JPL-Horizons-baked osculating elements (`DES=10P; CAP`, ecliptic J2000, epoch 2461253.5 = 2026-08-01.0 TDB, soln JPL#K265/43) — but NOT a second ephemeris: Earth comes from `HelioVector(Body.Earth)` and the EQJ→ECEF landing reuses `bodies.ts`, whose recipe was extracted to the new shared **`ecefFrameAt(time)`** (sun/moon/comet now share ONE frame conversion). Kepler→perifocal→ecliptic→equatorial→minus-Earth→**3 light-time iterations** ⇒ astrometric RA/Dec; `cometAzAlt()` subtracts the observer's ECEF (21″ parallax, free). Magnitude law is **`M1 + 5·log10Δ + k1·log10 r`** (13.7/6.5) — the `2.5·k1` form was 1.5 mag wrong. **Measured vs Horizons: RA/Dec ≤3.04″ across 2026-06→11 · az/alt 0.004°/0.003° at Dnipro · T-mag 0.000 · two-body drift 0.35′ @±1.5 yr, 2.12′ @+3 yr** (`ELEMENTS_TRUST_DAYS` 550; the panel prints an EXTRAPOLATED warning past it). `scripts/build-comet-elements.mjs` re-bakes AND re-runs the residual check (fails >1′). **Horizons API traps:** every param value must be QUOTED (`QUANTITIES='1,9,20,23'`, `START_TIME='2026-08-01'`) or it errors "Too many constants"; row stamps `2026-Aug-02 00:00` parse only as `Date.parse(when+" UTC")`. **(2) Planner `cometWindows()` (planner.ts):** forward scan (sun < −15° AND comet > 5°) → start/end/peak/az/mag + moon alt&illum, `score = alt × moonFactor × duration`; sun sampled every step, comet only while dark ⇒ 8-day/15-min scan ≈10–20 ms. **(3) Render `globe/scene/comet.ts` + `COMET` tuning + tokens `cometComa #9FF3C8`/`cometTail #8FD8FF`:** ONE additive billboard (coma + anti-sunward tail + accent highlight reticle) camera-anchored at `far×0.5` clamped `[near·1.2, far·0.95]`, rolled so local +X = the anti-solar direction projected on the view plane (tail orientation is REAL, length stylized), riding the now-exported `HORIZON_FADE_GLSL` + the star-field night gate. Retuned in-browser: comaAngRad 0.32→**0.16**°, comaIntensity 1.5→**0.85** (1.5 sat over `BLOOM.threshold` 0.9 → UnrealBloom smeared a ~2° green ball that ate the tail — a fake great comet for a mag-12.8 object), tailIntensity→0.75. **HONESTY CONTRACT (in code + panel):** at mag 12.8 the true disc is sub-pixel — the marker is an INSTRUMENT OVERLAY at stylized size; direction, horizon slice and all numbers are the real ephemeris. **NEW TRAP (cost 4 shots + a red-shader probe):** a low tracer that "won't render" at street level is the **REAL SKYLINE occluding it** — buildings/trees write depth in the opaque pass and the impostor is depth-tested against them (honest planner behaviour, not a bug); proven by swapping in a solid-red fragment shader (quad present, sliced by a rooftop). A wrong "the sky dome overpaints it" hypothesis + its `renderOrder` fix were REVERTED — the dome is AdditiveBlending/depthWrite:false and can never paint over anything (note left in `COMET.impostorFarFrac`). **(4) UI `panels/CometPanel.tsx` + `styles/comet-panel.css` + `store/comet.ts`** (17th island, top-right ☄ 10P pill): live ALT/AZ/MAG/ELONG + **RA/Dec J2000 push-to** + Δ/r, PERIHELION & CLOSEST chips, **NEXT SESSIONS** windows that pin scene time to each window's peak, the object card (period 5.37 yr · q 1.418/Q 4.71 au · nucleus 10.6 km · rot 8.93 h · discovered 1873-07-03 Wilhelm Tempel, Milan · next perihelion 2031-12-14), SKY/MARK toggles persisted via `lib/prefs` (`cometVisible`/`cometHighlight`, default ON). Card uses DENSE glass `rgba(11,15,20,.9)`+blur on the CARD (never the drag-transformed root) — the 72% wash interleaved its text with the camera deck it overlaps. Files: `lib/ephemeris/comet.ts`·`planner.ts`·`bodies.ts` · `globe/scene/comet.ts`·`scene/sky.ts`·`tuning.ts`·`StylizedTiles.ts` · `panels/CometPanel.tsx`·`styles/comet-panel.css`·`store/comet.ts`·`lib/prefs.ts`·`global.d.ts`·`index.astro`·`tokens.css`/`tokens.ts` · `scripts/build-comet-elements.mjs` · `test/lib/ephemeris/comet.test.ts` (21 Horizons-anchored tests). Shots `verify-shots/comet-01…12`. Memory: `mem:project/wip-2026-08-02-comet-10p-tracer`. UNVERIFIED: orbit-altitude framing not isolated in a shot; the daylight reticle floor (0.35) is unverifiable at this site (near opposition ⇒ 10P is never both above the skyline and in a lit sky).
