# Frame the World — Decisions Log

One line per meaningful change: what was decided, files touched, and any number measured. **Append-only,
absolute-dated.** Verification status is explicit — local-tested, wix-VERIFIED (confirmed against the live
Wix platform), or UNVERIFIED. Supersede a past line with a newer dated line; never edit or delete old ones.
Durable design rulings also live as `mem:decisions/*`. Maintained per `mem:decisions/session_workflow`.

**Structure (compacted 2026-07-11).** Top-to-bottom: binding **ADR-000** decisions · a durable
**Traps & Gotchas** section (the load-bearing gotchas that keep resurfacing) · **Per-phase digests** ·
**Recent sessions** verbatim (newest first — new work appends here). Older verbatim session logs
(2026-07-09 → 2026-07-10) were **moved byte-identical** into [`DECISIONS_ARCHIVE.md`](./DECISIONS_ARCHIVE.md)
— the append-only invariant holds (a move is not an edit; superseded rulings are annotated only in the new
digest/ADR/Traps sections, never in the archived text). **Compaction round 2 (2026-08-15):** verbatim
sessions **2026-07-11 → 2026-08-01** were likewise moved byte-identical into the archive (its
§Moved 2026-08-15 divider) with new per-era digests appended below. **Compaction round 3
(2026-08-18, audit-2 D2):** verbatim sessions **2026-08-02 → 2026-08-15e** moved byte-identical
(archive §Moved 2026-08-18; checksum-proven 147,274 B) with 7 era digests below; the UPLIFT era
(2026-08-17 →, ladder parked mid-run) stayed verbatim. **Compaction round 4 (2026-08-22, audit-3
D16):** that carve-out EXPIRED when U8 completed the ladder (2026-08-19) and §Recent had reached
135 KB, so verbatim sessions **2026-08-17 → 2026-08-19d** moved byte-identical (archive §Moved
2026-08-22; **md5 `5ed47c51b9d44a754964771ffe418330`, 556 lines / 79,306 B**) with 3 era digests
below; the OWNER-BATCH era (2026-08-21 →) stays verbatim because it all rides the un-shipped
release gate. §Recent went 141.7 KB → 62.6 KB.

---

## Binding decisions — ADR-000 (D1–D15)

From `PROJECT_SEED.md §4`, research-VERIFIED unless noted. **Binding** — change only by an explicit,
dated supersession line under Recent sessions.

> **D8 superseded 2026-07-10:** the `beforeInsert` quota hook was replaced by **endpoint-enforced quota**
> in the elevated `POST /api/photos` — the headless CLI provisions no Wix Data hooks (a member-session
> insert is platform-refused). See the Phase 5 digest. The 10-free / unlimited-paid intent is unchanged.
> **Quota numbers re-ruled 2026-07-17 (owner):** free **100** · premium **1000** (was 10/unlimited);
> enforcement mechanism unchanged. See the 2026-07-17 owner-rulings line; ships in Phase 6.9.

- **D1 — Globe engine:** three.js + `3d-tiles-renderer@^0.4` + Cesium OSM Buildings (ion 96188) + `GlobeControls`.
  Only combo giving real global 3D buildings + geo-accuracy + unrestricted per-tile material override + custom
  cinematic camera. VERIFIED.
- **D2 — Precision:** re-center tiles group near origin (ReorientationPlugin / CESIUM_RTC) + GlobeControls
  dynamic near/far. Solves float32 jitter without a float64 fork. VERIFIED.
- **D3 — Decode:** `exifr` embedded-JPEG preview → `libraw-wasm` Worker demosaic; single-threaded SIMD default;
  HEIC Safari-native detect + `libheif-js` fallback. VERIFIED (pipeline), UNVERIFIED (threads / COOP-COEP).
- **D4 — Orientation UX:** nudge-to-align is core; `FOV = 2·atan(sensorWidth/(2·focal))` + sensor DB +
  `FocalLengthIn35mmFormat` fallback. ILCs rarely write heading; GPS 3–15m, altitude junk → terrain-snap. VERIFIED.
- **D5 — Projection:** textured plane at frustum far face (v1); projective texturing (v2 stretch). VERIFIED.
- **D6 — Ephemeris:** `astronomy-engine` 2.1.19 (±1 arcmin) + procedural sky + Yale BSC5 stars, one source
  drives sliders + lighting. VERIFIED.
- **D7 — Data:** Wix Data Collections + geohash-prefix `hasSome` + client refine; denormalized `PublicPins`.
  VERIFIED (no geo ops), INFERRED (pattern).
- **D8 — Quota:** Pricing Plans check + `beforeInsert` hook rejecting insert #11 for free members (server-side). INFERRED.
- **D9 — Media:** originals private, derived previews public; resumable TUS upload for >10MB; 30-day download
  links. VERIFIED.
- **D10 — AI:** runtime Claude via Wix AI APIs (~1 credit/call; Opus 4.6 shown); vision gets downsized JPEG;
  premium-gated; doubles as the moderation pass. VERIFIED.
- **D11 — Scheduling:** none in v1; if needed, external cron → token-secured HTTP endpoint. VERIFIED.
- **D12 — Rendering:** WebGL2 primary, WebGPU progressive via `three/webgpu`. VERIFIED.
- **D13 — Cesium ion:** Community (free) for PoC; Commercial ($149/mo) at first sale / >$50K entity; manual
  attribution in UI. VERIFIED (terms), INFERRED (burn rate).
- **D14 — Design workflow:** Claude Design as token/motion factory → tokens.css (source of truth) → GL bridge
  `tokens.ts`; fence the globe; skip Claude Design's Wix connector (we scaffold via CLI for island/worker
  control). VERIFIED (workflow), UNVERIFIED (connector details).
- **D15 — Working title:** "Frame the World". ASSUMPTION (provisional).

---

## Traps & Gotchas (durable)

The hard-won gotchas that keep resurfacing. Violating one is a bug. Referenced by
`NEXT_SESSION_PROMPT.md`; the originating session (date/phase) is named for the full story in the
Recent sessions block or `DECISIONS_ARCHIVE.md`.

### GL / rendering
- **Navy night-sky floor.** `renderer.setClearColor` encodes the colour to the renderer's OUTPUT space
  (sRGB) when no render target is bound, and EffectComposer runs autoClear-off → sRGB-encoded values land
  in the LINEAR HalfFloat buffer and OutputPass re-encodes them to (8,26,45) navy on every empty sky pixel.
  Fix = `scene.background = Color(bg)` (three converts per-target + force-clears). (S5)
- **Negative `heightAt` garbage.** Quantized-mesh terrain tiles return NEGATIVE elevations while loading /
  on coarse LOD → **clamp every consumer to `[0, 9000]`** (clamp-only-upward). Unclamped, an arrival sinks
  to its lookAt floor and an underground camera unloads the whole tileset. (S2)
- **Additive overlays vanish on a bright sky.** Additive blending adds only ~10–30/255 against the day sky.
  Planning overlays (day-arcs) are alpha-blended solid strokes, `depthTest` off (analytic horizon owns
  occlusion). (S6)
- **Mirrors never SEAT geometry.** A deadband-quantized store mirror is for readouts; anything that
  positions scene geometry (seat/anchor/cone centre) resolves LIVE in the orchestrator frame — a 0.02°
  stale mirror seat is ~2 km of visible offset. Pan/centring likewise consumes the RAW per-frame
  `focusHit`, never the deadband copy. (2026-08-18h U4 owner round; promoted audit-2 D7)
- **3d-tiles-renderer 0.4.28 internals are load-bearing but undocumented.** Comparator = sort-then-POP
  (return 1 ⇒ first); fields at `tile.traversal.*`/`tile.internal.*` (`__dunder` fields GONE); custom
  `priorityCallback` stays total on non-tile items; `loadAncestors=false` always pairs with an explicit
  comparator. Re-verify ALL of these on any version bump. (2026-08-18g U5; promoted audit-2 D7)
- **`GlobeControls.update()` skips the near/far fit when `controls.enabled === false`** → a disabled-controls
  mode (FPV) must call `controls.adjustCamera(camera)` every frame or the frozen planes black-screen at
  street level. (S2)
- **Stale `InstancedMesh.boundingSphere`.** three caches the EMPTY sphere from count=0 and GlobeControls
  raycasts before pins load → every later pick early-outs forever. Set `mesh.boundingSphere = null` on
  every instance change. (Phase 5.1)
- **ECEF float32 cancellation.** ECEF ~6.4e6 m in float32 GPU matrices quantises at ~0.5 m and CRAWLS as the
  camera moves. Render instanced / large-coordinate meshes **camera-anchored**: `mesh.position =
  camera.position`, camera-relative instance translations, modelViewMatrix-only shaders. TRAP: `hoverAnchor`
  must add `mesh.position` back. (S4; same lesson as PhotoFrustum.)
- **`backdrop-filter` creates a containing block** → `position:fixed` descendants become panel-relative.
  Render fixed popups OUTSIDE any backdrop-filtered ancestor. (S3)
- **Dynamic far-plane clips the far hemisphere.** GlobeControls' far plane hides the starfield + atmosphere
  far shell → render near-hemisphere, clamp camera-anchored impostor distance ≥ 1.2·near. (Phase 1)
- **Descending zoom outruns tile load.** The manual zoom targets ellipsoid altitude, so a street request
  dives under a high city; once underground the ground tileset fully unloads (0 meshes) and live `heightAt`
  never recovers. Sticky `lastGroundM` sampled at the VIEW FOCUS + a street-floor clamp BEFORE the crossing.
  Tiles exist only inside the frustum — sample the focus, not the camera footprint. (S2)
- **Typed-array texture uploads ignore `UNPACK_FLIP_Y`** → flip in the DATA. No single-channel sRGB format
  exists in WebGL2 → linearise a data texture with a `uNightGamma` uniform. (S5)
- **`Group.renderOrder` does NOT propagate to children** — set renderOrder per object. **Sub-pixel points
  render nothing** — point sizes ≥ 2 px. **`getPivotPoint` returns null on horizon views** and leaves the
  out-arg STALE — fall back to the view focus. (Phase 4 / S6)
- **A `let` declared BELOW its first use in `StylizedTiles.ts` is a TDZ ReferenceError that reads
  as "the globe is gone".** `sampleEphemeris` runs at MODULE INIT, so any orchestrator state it
  writes must be declared ABOVE it — not with the look state ~1,300 lines down. When it is not,
  `attachStylizedTiles` throws `Cannot access 'x' before initialization`, the dynamic import's own
  `.catch()` swallows it as a one-line `[globe] tiles disabled` warning, and the ENTIRE real-Earth
  globe silently falls back to the procedural placeholder. **Invisible to vitest AND `astro
  check`** — only a browser load catches it. Twice now: `ultraOn` (2026-08-22j) and `lunarEcl`
  (2026-08-22k).

### Wix platform / backend (gate Phase 6 — never archive silently)
- **App-defined POST routes** were feared to 403 through the cloud adapter → the released-URL `/api/ping`
  canary confirmed 200; the risk is dead but the canary STAYS as the pre-release gate. (Phase 5 release)
- **Login builds an `http://` callback** repaired from the Referer header; `http` is tolerated only for
  localhost → referer-less (curl) auth fails "Invalid redirect URI". (Phase 5 release)
- **Elevated inserts run as the APP identity** → set `ownerMemberId` explicitly or ownership is wrong. (Phase 5)
- **`extensions.dataCollections` does NOT provision from `wix dev`** → the REST
  `scripts/provision-collections.mjs` is the schema source of truth. (Phase 5)
- **A stale member cookie is silently replaced by a visitor cookie** on the next HTML response → re-mint
  tokens before verifying member flows. (Phase 5)
- **OAuth allowlist is PORT-EXACT** — 4321 and 4322 must both be registered. (Phase 5)
- **Viewport pin query must THROTTLE, not debounce** — the perpetual LEO idle drift starves a debounce
  timer so it never fires. (Phase 5)
- **Media >10 MB must use `generateFileResumableUploadUrl` (TUS)** → async `onFileDescriptorFileReady`. (CLAUDE.md)

### Verification / tooling
- Occluded Chrome throttles rAF to ~1 frame/several-s → `page.bringToFront()` before timing-sensitive
  browser verification.
- Hiding the tiles groups crashes the rAF tick → the canvas presents the last good frame and every "hidden"
  measurement reads stale (guard with a `renderer.info.render.frame` advance check).
- `document.elementsFromPoint` SKIPS `pointer-events:none` nodes; `stars.update()` re-sets `points.visible`
  every frame (hide via `material.visible`); synthetic dblclicks need a preceding pointerdown/up pair.
- Verify tiers, in order: Playwright MCP → Chrome-extension bridge → scripted headless-Chrome CDP
  (`scripts/verify-s5-night.mjs` idiom). DEV seams: canonical inventory = `contracts.md §3`
  (top-level + sub-seams). Screenshots → `verify-shots/` only.
- **Check who owns the CDP port FIRST** (`lsof -nP -iTCP:9222 -sTCP:LISTEN`). A stale verify Chrome
  without the occlusion flags keeps the port; the fresh flagged launch silently fails to bind and the
  client attaches to the buried stale window where rAF is frozen (~20 min lost in U5). Managed launch:
  `scripts/verify-chrome.mjs`. (2026-08-18g; promoted audit-2 D7)
- **CDP `Runtime.evaluate` attaches only POST-load** — the dev-local initial tile stream (~2 s) is over
  before any sampler attaches; construction-relative metrics must come from IN-PAGE probes
  (`__globe.u5Mark()` idiom), never from evaluate-side timing. (2026-08-18g; promoted audit-2 D7)

---

## Per-phase digests

One line per phase; full mechanics in the linked memory, verbatim session logs in `DECISIONS_ARCHIVE.md`.

- **Bootstrap (2026-07-09)** — `.claude/` operating environment + Serena memory graph + the persistence
  loop (DECISIONS + NEXT_SESSION). `mem:decisions/session_workflow`.
- **Phase 1 — scaffold + globe (2026-07-09/10)** — Wix headless Astro scaffolded onto the existing repo;
  the globe rebuilt into the LEO "signature scene": NASA Blue Marble July graded into the palette + VIIRS
  night lights + relief, ray-based atmosphere, a second TilesRenderer draping palette-graded Esri imagery
  that dissolves in by altitude, dark edge-stroked OSM buildings. `mem:patterns/globe-rendering`. RELEASED
  to the live URL 2026-07-10.
- **Design system import (2026-07-10)** — chrome tokens + fonts → `tokens.css` + the GL bridge
  `lib/theme/tokens.ts` (ADR D14). `mem:patterns/design-system`.
- **Phase 2 — decode (2026-07-10)** — `exifr` metadata + embedded-thumb preview + **`libraw-wasm@1.0.5`
  EXACT-pinned** (1.1.2+ needs pthreads/SAB = COOP/COEP, unusable on unverified Wix hosting) in a disposable
  per-file Worker + `libheif-js` HEIC fallback. `mem:patterns/upload-flow`.
- **Phase 3 — projection (2026-07-10)** — camera frustum + image plane from EXIF; reactive zustand
  re-projection; click-to-place for missing GPS; 2.2 s bezier flight (reduced-motion cut).
  `mem:patterns/photo-frustum`.
- **Phase 4 — ephemeris (2026-07-10)** — `astronomy-engine` drives sun/moon/terminator/shadows/atmosphere
  from scene time; TimeScrubber ±12 h + multiday; golden-hour bell grade; real BSC5 star field (−GAST); real
  Cesium World Terrain (90 m building sink removed). `mem:patterns/sky-bodies-terrain`.
- **Phase 5 — members + pins (2026-07-10)** — managed member auth (`@wix/astro`); Photos/PublicPins
  provisioned via REST; **endpoint-enforced 10-pin quota** (supersedes D8's hook); C6 reduced-precision
  public pins (cell-centre published); viewport geohash query + click→fly. First Wix-load-bearing phase.
  `mem:patterns/members-pins`.
- **Phase 5.5 S1–S6 — pre-marketplace UX (2026-07-11)** — location finder, flight/FPV core, pin lifecycle,
  pin visuals + Explore journey + Welcome landing, night-sky physics, FPV planning overlays. Canonical
  design `archive/PHASE_5_5_UX_BATCH.md`; full mechanics in the Recent sessions block below + `mem:project/wip-2026-07-11-*`.
- **Pre-S7 architecture review + refactor (2026-07-11)** — this compaction, doc-drift fixes, new
  conventions, and the safe cleanup tier. See `archive/ARCHITECTURE_REVIEW.md` + the Recent sessions block.
- **Phase 5.5 S7 tail + interlude (2026-07-11→12)** — S7 street names went GL: one canvas-textured quad
  per name LYING ON the terrain (DOM labels structurally lag a 60 Hz mesh), sourced from OpenFreeMap
  planet MVT (keyless; **maxzoom 14 — z15 returns empty 200s, probe first**; `pbf@5` exports `PbfReader`,
  no default export); vector road/water web with tile-exact clipping (Sutherland–Hodgman rings ·
  Liang–Barsky lines with re-entry splitting) killed the river-tile z-fight flicker; camera pose became
  shareable/reload-safe via the `#p=` URL hash. High-altitude pin selection landed SHIFTED — root cause:
  `ground.heightAt` returns null/NEGATIVE garbage on unloaded tiles (live: −2047 m at 697 km) so the
  committed arrival pose kept a stale low lookAt; fix = live re-frame + `clampGroundM`
  (`mem:bugs/pin-arrival-reframe`). Pre-S7 safe-refactor tier finished (B6–B15/B26: `lib/geo/{terrain,
  screen,heading}` extracted, the one lib→store violation killed, net −68 LOC); README rewritten for the
  internal Wix contest (headless-stress-test framing, 100% agent-built stated openly).
  Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15. `mem:project/wip-2026-07-11-phase5.5-s7` ·
  `wip-2026-07-11-s7-feedback-batch` · `wip-2026-07-11-pre-s7-refactor{,-s2}` ·
  `wip-2026-07-11-b19-split` · `wip-2026-07-12-readme-rewrite`.
- **Rendering-quality passes + Dnipro enrichment slices 0–3 + illumination (2026-07-12→14)** — Pass 1:
  NEW pure `lib/globe/quality.ts` (`detectDeviceTier` + `makeGovernor` EMA frame governor); **HARD
  INVARIANT: `QUALITY.tiers.high` == the pre-pass constants**; shadows later DECOUPLED from the governor
  (they follow the DEVICE tier; capable machines floor at `mid`) after the M3 Pro was degraded to `low`
  and lost the entire shadow pass. Pass 2: per-building tonal variation keyed on `_batchid`/`_feature_id_0`
  (GLTFLoader lowercases non-standard attributes); **ROOT LESSON: no FLAT per-building emissive ever reads
  as lit windows** — R3 night emissive OFF (`nightWindowGain 0`), window grid removed on owner order.
  Illumination pass owner-VERIFIED: `DRAPE.shadowOpacity 0.80` · `minSunElevSin 0.008` (~0.46°, keeps the
  golden-hour raking shadows) · altitude-adaptive shadow bounds. Enrichment: bake-and-mask REPLACES Cesium
  OSM Buildings in-bbox, self-hosted on R2 (extends D1/D13/C6); **vertical datum = bake relative geometry
  + runtime clamp-to-CWT** (EGM2008 undulation over Dnipro +20.42 m — a naive absolute-Z bake sinks ~20 m);
  Slice 2 = per-cell terrain re-seat (offsets match terrain within ~0.2 m; spread −20.6 m riverbank→hill)
  + a pixel-exact 4-plane ECEF `clipIntersection` mask hole; Slice 3 = 24,714 hash-seeded trees, ONE
  `EXT_mesh_gpu_instancing` node per cell (re-bakes byte-identical). Blank gallery preview fixed = SSR
  boot poster + OG meta — the Wix `site-snapshotter` captures SSR HTML only, never `client:only` islands
  (`mem:bugs/gallery-thumbnail-stale` RESOLVED). Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15.
  `mem:project/wip-2026-07-12-rendering-{quality-pass,pass1-tiling-fluidity,pass2-dnipro-identity}` ·
  `wip-2026-07-13-illumination-pass` · `wip-2026-07-13-dnipro-slice{0-spike,1-bake,2,3-trees}` ·
  `wip-2026-07-13-terrain-reseat` · `wip-2026-07-13-dnipro-enrichment-research`.
- **OSM2World variant + R2 hosting + obstruction moat + owner seating/UI batches (2026-07-14)** —
  `bake-osm2world.mjs` shipped as a PARALLEL VARIANT (`cities/dnipro-o2w.json` `extends` dnipro; the
  `?enriched=` A/B seam; the classic pipeline stayed byte-identical); the 1-cell spike settled the o2w
  axis: `(east,up,−north)` == our `gv(e,u,−n)` (1362/1362 buildings in-bbox). R2 hosting LIVE: a
  Cloudflare **Worker over a private bucket** (free-tier CORS/Range control; r2.dev serves no CORS) at
  `frame-the-world.ievgen-sukhov.workers.dev`; dev NEVER reads R2 — tiles moved out of `public/` into
  git-ignored `bakes/` + a serve-only Vite middleware `ftwLocalTiles` (client bundle 372 MB dead weight →
  24 MB). Both bakes expanded to Greater Dnipro ~20×20 km (grid 20 ≈ 1 km cells; 127,890 bldgs · 161,823
  trees) + the `BLD` chip CLASSIC⇄OSM2WORLD toggle. PER-BUILDING/PER-TREE terrain re-seat done on the
  CPU (position attribute / instanceMatrix) **deliberately** so planFeed occlusion sweeps, shadow maps and
  picks read the same arrays — a shader displacement would desync the planner. Pass 3 = the astro moat:
  pure `lib/ephemeris/planner.ts` (rise/set with eye-height dip; golden windows DERIVED from tuning.GOLDEN)
  + `lib/geo/horizonProfile.ts` + `skylineState` clear/block crossings (12-min scan + bisection ±0.5 s).
  FPV solidity became the screen-door Bayer dissolve — opaque + depth-writing at every k (the binary
  `depthWrite` flip was the 55→56 solid jump); `#f=` shareable FPV URLs; UI QoL batch (uniform
  hover-reveal DragGrip, time playback, shareable custom time in the URL, PLAN top-left, ☀/☾ edge chips).
  Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15. `mem:project/wip-2026-07-14-osm2world-adapter` ·
  `wip-2026-07-14-osm2world-slice1.5-spike` · `wip-2026-07-14-r2-hosting-osm2world-prep` ·
  `wip-2026-07-14-pass3-obstruction-moat` · `wip-2026-07-14-owner-batch-seating-ui` ·
  `wip-2026-07-14-uiux-qol-batch`.
- **Docs reorg → Phase 6 marketplace → 6.9 + release week + St Albans (2026-07-15→18)** — claude-docs
  reorganized into `provenance/ · dnipro-enrichment/ · rendering/ · archive/` subfolders (37 files
  repointed); Milky Way rebaked at 8K (**SVS star maps are flux-per-pixel** — 8k needs ×4 linear gain;
  gaussian pre-blur σ=1 texel in LINEAR space, else mip-less point sampling skips the sub-texel star
  speckle and the band goes near-black). Phase 6 marketplace-light: **Catalog V1 is IMPOSSIBLE on this
  site** (gateway 428 `CATALOG_V3_CALLING_CATALOG_V1_API`) → built on V3 — `POST /stores/v3/products
  {productType:"DIGITAL", …digitalProperties.digitalFile._id}` wix-VERIFIED; the preinstalled Stores
  automation delivers the 30-day link. PROD OUTAGE root-caused: the Wix asset origin cold-serves each
  chunk in 15–30 s and 500s at the ~30 s gateway cutoff → dead islands/black globe after every release
  resets hashes; recovery = SERIAL `warm-prod-assets.mjs` retry-until-200, and **the asset edge cache is
  SHARDED** (a warmed shard ≠ the browser's — reload until clean). Owner rulings: quota free **100** /
  premium **1000** (supersedes D8's numbers) · demo pins owned by yevhens@wix.com and ALL for sale ·
  MARKETPLACE as its own topnav button. 6.9 batch: two-tier quota · EUR fix (`SITE_CURRENCY` — no SDK
  exposes site currency) · SALES tab · `redirects.createRedirectSession` REJECTS relative postFlowUrl
  (absolutize) · Catalog-V3 `createProduct` defaults variants to tracked-quantity-0 (blocks checkout) →
  `createProductWithInventory {inStock:true}`. Demo seed ×27 listed + FAQ panel + orbital-grade pass
  RELEASED live; ground checkerboard parked (`mem:bugs/ground-checkerboard-flicker`). City #2 St Albans
  baked + live on R2 — **the o2w work-cache was sub-grid-INDEX-keyed and collides across cities** →
  per-city `.cache/o2w/<city>/`. Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15.
  `mem:project/wip-2026-07-15-docs-reorg-phase6-prep` · `wip-2026-07-16-phase6-marketplace-research` ·
  `wip-2026-07-16-prod-asset-outage` ·
  `wip-2026-07-17-{phase69-marketplace-batch,demo-seed-curation,seed-orbital-faq-batch}` ·
  `wip-2026-07-18-st-albans-city2`.
- **View-prefs persistence + default flips (2026-07-21)** — defaults flipped: SAT ground on, FPV
  buildings fully solid (slider double-click resets to 100). NEW seam `lib/prefs.ts` (`ftw:view-prefs:v1`
  localStorage; `sanitizeViewPrefs` clamps/drops junk; SSR/node-safe no-throw): SAT/☀☾/BUILDINGS persist
  inside their store setters; **PIN persists at the CHIP only** — the orchestrator's FPV declutter shares
  `setPinsVisible` and must never write the pref (a mid-FPV reload would freeze pins-off as if chosen);
  BLD variant reload-survives via pure `applyStoredVariant` (an explicit `?enriched=` ALWAYS wins
  verbatim). Detail-panel close ask included.
  Verbatim: `DECISIONS_ARCHIVE.md` §Moved 2026-08-15. `mem:project/wip-2026-07-21-viewprefs-uiux`.
- **Astro engine A–E + comet 10P (2026-08-02→10)** — search/track ANY body: 1,947-entry fuzzy
  index (stars/constellations/comets/asteroids/full OpenNGC), universal-variable kepler +
  SIMBAD/SBDB long-tail with TTL caches, target trail/markers/rise-set windows, planet phase
  discs; comet 10P tracer + magnitude-model fix. Verbatim: archive §Moved 2026-08-18.
  `mem:project/wip-2026-08-03-astro-engine-phase-a` · `-c` · `wip-2026-08-10-astro-engine-phase-bde` ·
  `wip-2026-08-02-comet-10p-tracer` · `mem:bugs/comet-magnitude-model`.
- **Mobile design trio (2026-08-11)** — the /m planning-shell design memo + M-ladder plan +
  device matrix; mobile = planning-only ruled PERMANENT. Verbatim: archive §Moved 2026-08-18.
  `mem:project/wip-2026-08-11-mobile-design` · `MOBILE_PLAN.md`.
- **Full audit #1 + fix slices 0–7 + Phase 8a + planning-core restructure (2026-08-13)** —
  first whole-repo audit (report `audits/audit-full-2026-08-13.md`), 8 fix slices same-day,
  twilight/GC/MW planning core, FPV walk-orbit bug. Verbatim: archive §Moved 2026-08-18.
  `mem:project/wip-2026-08-13-full-audit-1` · `wip-2026-08-13-planning-core-restructure` ·
  `wip-2026-08-13-slice7-phase8a` · `mem:bugs/fpv-walk-orbit`.
- **Mobile M0–M3 (2026-08-13→14)** — /m shell (sheets/tab bar/dock conveyor), FPV touch
  (joystick walk, pinch-FOV, wake lock, minimap), PlanSheet twins, TARGET GHOSTS + long-press
  sky menu, mobile-default entry (`?d=1` escape). Verbatim: archive §Moved 2026-08-18.
  `mem:project/wip-2026-08-13-m1-mobile-planning` · `-m2-fpv-touch` ·
  `wip-2026-08-14-mobile-m3ab` · `-m3c` · `MOBILE_PLAN.md`.
- **Planning QoL 1–4 + FIND v2/v3 + §3.5 sunsets (2026-08-14→15)** — scrubber v2 + tail trace,
  frameFinder cards, GHOSTS chain, NPF/moon-calendar/size-dist tools, FindPanel frame-as-query
  per-day scan + in-frame ghosts + standings, sunEventFrame (refracted labels / airless
  geometry PINNED). Verbatim: archive §Moved 2026-08-18.
  `mem:project/wip-2026-08-14-qol-batch`…`-qol4-batch` · `-find-rework` ·
  `-find-accuracy-labels` · `-night6-hover-floor` · `wip-2026-08-15-sunsets-in-frame` ·
  `mem:project/owner-orders-2026-08-14-qol-batch`.
- **Owner UX batches ×5 + ×9 (2026-08-15b/c)** — PLAN/FIND one shared resizable window, grown
  sky context menu (TRACKING/MARK/TRAIL/FIND-IN-FRAME + camera-aiming rise/set), TRACKING
  camera lock, /m FIND 4th tab + login + MY PLACES + SAVE VIEW, collapsible mini-map.
  Verbatim: archive §Moved 2026-08-18. `mem:project/wip-2026-08-15-ux-batch` · `-uxbatch2`.
- **Guide G1 + polish (2026-08-15d/e)** — ONE content module `lib/guide/guideContent.ts`
  (11 chapters · ~40 topics · goals router · `[[id|label]]` crosslinks) → desktop Guide panel +
  /m GuideSheet; FAQ ABSORBED (island deleted); 12 warm-list-coupled screenshots; slop-lint
  tests; DECISIONS compaction round 2 same session. Verbatim: archive §Moved 2026-08-18.
  `mem:project/wip-2026-08-15-guide-g1` · `GUIDE_PLAN.md`.
- **UPLIFT ladder U1–U8 + audit #2 + fix slices (2026-08-17 → 2026-08-19).** P7 meteor showers
  (21-row IMO bake, radiant-as-target, R12 rail layer) + `UPLIFT_PLAN.md` authored → U1 /m
  2D-first · U2 FPV stability ×8 · U3 fullscreen MapWindow + 2D-map batch + desktop flat map ·
  U4 direction lines + aim cones · U5 closest-first loading → PARKED for AUDIT #2 (report
  `audits/audit-full-2026-08-18.md`) + its fix slices → UN-PARKED: U6 foveation · U7 terrain
  audit · U7b GLO-30 terrain patch + best-variant buildings rule · **U8 per-building height
  override (2026-08-19) — the 10-point ladder COMPLETE**, backend prepared but dormant for the
  batch-sync phase. Verbatim: archive §Moved 2026-08-22.
  `mem:project/wip-2026-08-17-p7-meteors-uplift-plan` · `wip-2026-08-17-u1-2d-mobile` ·
  `wip-2026-08-17-u2-fpv-stability` · `wip-2026-08-18-u3-2dmap-batch` · `wip-2026-08-18-u4-aim-cones` ·
  `wip-2026-08-18-u5-loading` · `wip-2026-08-18-audit2` · `wip-2026-08-18-audit2-fixslices` ·
  `wip-2026-08-18-u6-foveation` · `wip-2026-08-18-u7b-glo30-terrain-buildings-rule` ·
  `wip-2026-08-18-u8-height-override` · `UPLIFT_PLAN.md` · `BAKED_ASSETS.md`.
- **Owner UX batches #2 + #3 (2026-08-19b/c).** #2 (11 items): override cap 1000 · /m map glyph
  + day steppers + joystick-over-fullscreen-map · Esc-closes-map-first · SKY search default ·
  DISABLE menu labels + find-in-frame composite-state fix · the UNFOLLOW verb
  (`sky.stopFollowing`) + peek hint + target-section reorder both shells · FIND generalised
  `gc`→`target` (ANY tracked target) · /m ⊞ LAYERS chip + MY-PLACES-ON-MAP (`store/places`) +
  `aimVisible` RADAR master + `pinsVisible` /m-default-off. #3 (9 items + 2 tails): desktop 2×4
  toggle grid · desktop radar <10 km band · my-places-on-map desktop FIXED (missing lit CSS +
  new GL `scene/placeMarkers.ts`) · UNFOLLOW also disables its FIND body · /m LAYERS expands
  LEFT · radar-bearings regression fixed (session-only dismissal + body-named DIRECTION labels
  + one-time `prefsRev` re-arm) · places nearest-first (`lib/geo/proximity.ts`) · /m SAVE VIEW
  optional-name Sheet · GOTO tracked-target chips both shells (`panels/SkyGotoChips.tsx`).
  Verbatim: archive §Moved 2026-08-22. `mem:project/wip-2026-08-19-owner-uxbatch2` ·
  `wip-2026-08-19-owner-uxbatch3`.
- **PLUX launch grooming — brand + domain + guide G2-refresh + guide search (2026-08-19d).**
  Brand Sidera→**PLUX** everywhere (wordmark hero, nav/strip/upload marks, favicon set,
  favicon.svg deleted) · domain assessed and the repo flipped to `https://www.plux.today`
  (`SITE_URL` + 7 script defaults + `FTW_SITE_URL` override) — **prod DARK until the owner's
  GoDaddy nameserver replacement → Wix www TLS → OAuth allowlist**, the release gate every
  later batch still rides · guide G2-refresh (16 topics corrected + 7 new + 3 goals;
  `shell-m.webp` re-shot) · guide BM25+fuzzy search both shells (`lib/guide/search.ts`).
  Verbatim: archive §Moved 2026-08-22. `mem:project/wip-2026-08-19-plux-launch-grooming`.

---

## Recent sessions (verbatim, newest first)

### 2026-08-26b — REGION #4 Chernobyl / Pripyat: two buildings bakes + a GLO-30 patch, LIVE on R2 — and the bake found three pre-existing defects, the loudest of which was rendering the New Safe Confinement at double its height

**Owner ask (explicitly scoped away from the remaining RENDERING CHARTER work — Group D and RC21 are untouched):** a high-fidelity OSM buildings bake plus a Copernicus height bake over a ~10×10 km patch capturing *both* Pripyat city and the Chernobyl reactor, full pipeline, uploaded to R2. **Gates: vitest 2,050/2,050 (139 files, +5) · `astro check` 0 err / 0 warn / 6 hints · `npx knip` exit-0 · `verify-chernobyl.mjs` 8/8 in headless CDP Chrome.** Dnipro / St Albans / Everest bakes, caches, prefixes and the default runtime path untouched throughout.

**The box.** `[30.006, 51.352, 30.15, 51.442]` = 10.01 × 10.02 km on 51.3973/30.0780, the midpoint of Pripyat centre and ChNPP unit 4 — they are only 3.45 km apart, so one box holds both easily. grid 10 → ~1 km cells. Deliberately outside: the *town* of Chernobyl (~15 km SSE) and the Duga-1 radar (~10 km S), both of which need a taller box than 10×10. **Runtime cost of the whole region: ONE entry in `src/lib/globe/regions.ts`** — no env edit, no `tuning.ts` edit, because `resolveEnrichedSelection` already routes a `#p=` pose into its containing region and segment-swaps the env URL, and `StylizedTiles` already reads `r.terrain`.

**Shipped:** `cities/chernobyl.json` + `cities/chernobyl-o2w.json` (subGrid 2) · the `chernobyl` EGM2008 grid in `terrain/geoid.mjs` (`[30,51,31,52]` @ 0.25°; bilinear reproduces `GeoidEval` at the probe point to 3.9 mm) · the `regions.ts` entry · `scripts/verify-chernobyl.mjs` (8 checks; the worked template for region #5). **Numbers:** classic 1,212 buildings / 72 tiles / 9.20 MB · o2w 1,688 features (1,172 Building, 379 pylons) / 74 tiles / 358k verts / 16.94 MB · **166,599 trees** · terrain 1,488 files / 3.6 MB, probes **bias 0.3 / 0.0 m, spread 2.3 / 2.0 m, rim seam 0.5 m** — all on the DEFAULT tolerances, which is why the config deliberately omits `probeBiasTolM`/`probeSpreadTolM`. Config values were tuned against a *measurement* of the box rather than copied from Dnipro: 721/1,275 footprints (56.5 %) already carry `height` or `building:levels` — the best-mapped box in the repo, hand-traced by the Pripyat mapping community — and of the 554 that don't, 457 are `building=yes` with a **median footprint of 200 m²**, hence `defaultHeightM 6` against Dnipro's 8.

**The C6 call, made narrowly and flagged rather than buried.** The built-in blocklist matches 63 footprints here. `exclusion.tags` replaces it with the same list minus **exactly one rule, `["power","generator"]`**: 54 stay excluded (45 `power=substation` — all `substation=minor_distribution` neighbourhood ТП kiosks — and 9 abandoned `military=*` checkpoints/barracks), and the 9 returned are reactor blocks 1, 2, 3, 6 plus one diesel house. C6 protects *live* Ukrainian military and critical-energy infrastructure; every object returned is a permanently shut-down RBMK block inside a licensed-tourism exclusion zone, and OSM itself tags three of the four `disused:power=generator` **with their shutdown dates** (1996-11-30 / 1991-10-11 / 2000-12-15). The structures that actually read as "Chernobyl" from the air needed no override at all — the New Safe Confinement, the sarcophagus, reactor 4's ruins and the 800 m turbine hall carry no `power=*` tag and were never excluded. What made the override worth making rather than shrugging at is that **reactor 3 shares a wall with reactor 4**, so leaving it out punched a visible hole beside the arch. **Reverting is deleting the `tags` key.** The override reaches the o2w bake too (same merged config at `bake-osm2world.mjs:126`) — all 8 landmark OSM ids verified present in both bakes' sidecars.

**Three pre-existing defects, all found by baking, all fixed.** (1) **`roof:height` was ADDED to a tagged `height` instead of subtracted from it** (`lib/buildings.mjs`). OSM's `height` is the TOTAL including the roof; `building:levels` counts storeys BELOW it — the extruder treated both as the eave, so every co-tagged building rendered taller by exactly `roof:height`. The degenerate case is the NSC (`height=110 roof:height=110 roof:shape=round→gabled`), which came out **a 220 m ridge**. The fix subtracts only when `heightSource === "height"`, caps `roof:height` at `total − 1` so the tagged total is preserved *exactly*, and **moves the `min_height` clamp below the roof block** — clamping the base against the pre-roof total could seat it above its own wall top and invert every quad. +5 regression tests; blast radius measured over every cached Overpass response (164/128,649 Dnipro — all small — - 0/25,510 St Albans, 1/1,275 here) → **T55**, Dnipro not re-baked. (2) **The rim blend died on `ENFILE`** — the *system-wide* file table, squeezed by the IDE's own `lake serve`/`lean --server` holding ~16k fds. The blend is ~1,000 serial CWT fetches and is **not idempotent**, so a mid-way crash means a full re-bake: added `withFdRetry` and, more importantly, **narrowed both `.catch(() => null)`s to ENOENT**, because an ENFILE swallowed there silently left a rim tile UNBLENDED — a height step at the seam nothing downstream would flag. (3) **`upload-r2.mjs --terrain` gated on `layer.json`, which mago writes BEFORE the blend, prune, availability post and probe** — so the interrupted tree from (2) would have uploaded clean-looking, unblended and unverified. It now also requires **`patch-info.json`**, the last thing `bake-terrain.mjs` writes and the only honest completion marker (verified by pointing it at the broken tree first). Plus a fourth, smaller: `bake-terrain.mjs`'s *extent* probe assumed `extentMaxDepth ≤ bakedMax` while the city probe already clamped, so it printed its "topped out at L12" note, passed containment, then died on a raw ENOENT for a level never written.

**L12, not L13 — and the ceiling is the tool's heuristic, not the data.** Copernicus GLO-30 coarsens LONGITUDE sampling above 50° N to hold ~30 m on the ground as meridians converge, so `N51_E030` is **2400×3600 px (1.5″ lon × 1.0″ lat)** where dnipro's `N48_E035` and everest's `N27_E086` are both 3600×3600 — measured off the cached COGs, not inferred. **mago clamps `-max` on pixels-per-degree, not ground metres**, so the same ~30 m source lands one level shallower: asked 13, got 12. In ground terms this box is 29.2 × 30.9 m (nearer square than Dnipro's 20.7 × 30.9) while L12 posts ~48 × 54 m, so about a third of the source's detail goes unused. Not worth fighting here — 29 m of relief across the probe tile, 2.3 m measured spread — and the only route to L13 is resampling the COG in Node, which the 2026-08-18 ruling forbids outright. → **T56**.

**Browser-verified 8/8** (headless :9333; the owner's persistent :9222 Chrome was down): region resolves to `chernobyl-o2w` and *not* the dnipro registry head · 14 o2w cell glbs, 0 errors · 32 terrain tiles at levels 9-12, max L12 as configured · heights 131.2..174.3 m over 5 probes · NSC cell classic **110 m (was 220)** / o2w 125 m · `?enriched=chernobyl` → 12 classic glbs and ZERO o2w · `?enriched=off` → nothing · Pripyat centre → 25 o2w glbs. The shots show the NSC as a real barrel dome (the o2w tier's payoff — the extruder's `round`→OBB-ridge cannot do it) and Pripyat's slabs with forest closed through the city. **Two probe traps cost most of the verify time, both false negatives on a working page:** `performance.getEntriesByType("resource")` **caps at 250 entries** while one settled globe frame issues ~1,700 requests (1,517 ArcGIS alone), so it reported an empty resource list — use the CDP `Network.responseReceived` log; and the terrain patch is claimed by the ground renderer's `fetchData` hook only *after* the camera settles over the region, so a fixed settle read zero tiles on a page that went on to stream 32 — poll, don't sample once. (Also `Network.setCacheDisabled`: a memory-cache hit fires `requestServedFromCache`, not `responseReceived`.) And one of mine: traversing `__globe.enriched.group` comparing `geometry.boundingBox.max.y` mixes LOCAL spaces under different parent transforms — the 152.7 m it reported was not the height of anything; replaced with the baked-glb accessor max, which is the shared ENU frame the bug actually moved.

**R2 LIVE, curl-verified through the Worker:** all three prefixes 200 + `access-control-allow-origin: *` + correct content types (`.glb` → `model/gltf-binary`, `glTF` magic confirmed by ranged GET; `.terrain` → `application/vnd.quantized-mesh`); `enriched/dnipro/` and `terrain/dnipro/` still 200. 1,785 files / 28.6 MB across `enriched/chernobyl/`, `enriched/chernobyl-o2w/`, `terrain/chernobyl/`; bucket ≈ 430 MB of the 10 GB free tier. **Dev streams the LOCAL bakes by design**, so R2 is curl-verified rather than browser-verified — an intentional split, not a gap. Incidentally **this is the first bake to ship `.meta.json` sidecars** (72 + 74, uploaded): the writers already existed in both bakers and the other three bakes merely predate them, so RC17's writer half arrived for free — its runtime-consumption half is still open. New backlog: **T55** (Dnipro re-bake owed to the roof:height fix), **T56** (the L12 ceiling), **T57** (Pripyat's unmapped self-seeded street trees — the canopy-raster tier, whose recorded blocker "needs a GeoTIFF reader" is now stale, since `geotiff` is already a dependency of the terrain bake).

### 2026-08-26 — RENDERING CHARTER Group E + RC25 + RC30: four slices shipped, and the two that mattered most were both fixed by a MEASUREMENT that contradicted the design note it came from

**Shipped: RC18, RC19, RC20, RC25, RC30.** Gates: vitest **2,045/2,045 (139 files, +2)** · `astro check` 0 err / 0 warn / 6 hints · `npx knip` exit-0 · **`verify-rendering-charter.mjs` 85/85 ALL PASS**.

**RC18 — the governor lever split.** `applyTier` became `applyTierRenderer` (DPR → composer realloc, bloom, the AO gate, `tierOverlayPx`, the two DPR mirrors) + `applyTierTiles` (error targets, LRU cap/floor, queue caps, foveation, street/lattice budgets), with a pure `planTierApply(next, appliedTiles, fpvActive, livePromote)` in `lib/globe/quality.ts` deciding which half lands. A governor **PROMOTE inside FPV now lands its tile half immediately**; the renderer half still parks. A DEMOTE never splits — shrinking a cap mid-FPV evicts everything outside the visible set and then discards each fresh parse against the full cache, which is the U2/A9 loop. The safety argument is not prose: `quality.test.ts` asserts the tier TABLE is monotone (a promote only ever lowers an error target and raises a cap), so if a future tier edit breaks that, the split fails a unit test rather than becoming an eviction storm. **Browser-proven at Dnipro FPV: tileTier=high landed while tier stayed low, pending=high, DPR unchanged, ZERO overlay rebuilds.** The one hazard the split opens — promote-then-demote inside one FPV leg — is closed structurally: `planTierApply` returns BOTH halves for every non-FPV call including equal tiers, so FPV exit always re-converges. Rollback knob `QUALITY.leverSplit.livePromoteInFpv`. Also: the lean-shell flat-chart flip now re-applies the RENDERER half only, instead of re-writing every tile lever on every 2D↔FPV leg.

**RC20 — the T34 ground-LRU rest-trim, and the measurement that re-scoped it.** The defect is one line of `LRUCache.js` 0.4.28: `hasBytesToUnload = unused && cachedBytes > minBytesSize || …`. A single tile the traversal did not visit, plus a cache above the **FLOOR**, starts an eviction — the cap is never consulted. A mode flip marks the whole previous working set unused in one frame. Fix = a bounded **flip bank** (`lruBankFloorBytes` + `bankWindowMsLeft`, `QUALITY.lruBank`): hold the floor near the cap for `holdMs` after each FPV boundary crossing. Keyed on `fpvActive` and NOT `flatGround` — the flat latch has an un-hysteresed altitude term that would re-arm the bank every frame for a camera parked at 120 km, silently converting a bounded bank into a permanent raised floor. **M13 IS ANSWERED, and it says the desktop half of T34 does not exist:** on `high` the ground cache rests at **109.8 MB against a 322.1 MB floor** — the trim condition never arises. The bank ships enabled for mid/low only; the desktop leg proves the off-state (the library's non-integer 0.3/0.4 GiB pair, literally untouched). A sweep over every reachable cap also found that the shipped `lruFloorBytesForCap` INVERTS at cap 1–2 (`round(1.5) === 2`); unreachable in production, pinned in a test rather than "fixed", because its exact arithmetic is the byte-identical restore path.

**RC19 — the /m PiP, and a banked design note that was WRONG.** The second full scene render is now cached into a `WebGLRenderTarget` and blitted with a `FullScreenQuad`; the blit runs every frame (that is the anti-flicker half — `composer.render()` overwrites the rect, so a skipped paint alternates between the miniature and the full-scale view), and only the scene render is cached, on a camera/projection/sun delta with a `maxStaleMs` heartbeat. **The banked note said the blit material needs `toneMapped: false`. Source says the opposite:** three forces `NoToneMapping` on anything rendered into a render target (`WebGLPrograms.js:178-186`) and encodes into a target in the working colour space, so the target holds raw linear HDR and the blit is the ONLY place `NeutralToneMapping` can still be applied — `toneMapped` must stay TRUE. Shipping the note as written would have shipped an un-tone-mapped, clipping miniature. Also: `pipQuad.dispose()` is never called — `FullScreenQuad.dispose()` disposes three's MODULE-LEVEL shared triangle that bloom, output and GTAO all draw with. The sun is compared as a DIRECTION so ULTRA's `lightDistM` swap is not read as a view change. `PIP.maxStaleMs: 0` is the rollback and is unit-locked as byte-identical to the pre-RC19 pass.

**RC25 — the capped mip chain, and the 89× measurement that rewrote it.** Four charter premises died. (1) The crux — "an incomplete chain renders black unless `TEXTURE_MAX_LEVEL` is set" — **does not apply**: three never sets that parameter anywhere, and with `generateMipmaps` false it allocates IMMUTABLE storage sized to `mipmaps.length`, which is complete over exactly its allocated levels. The black-chain failure belongs to the mutable WebGL1 path this renderer never takes. (2) The artefact is not a colour bleed but a **double alpha multiply** (the composite is non-premultiplied, the drape shader premultiplies at sample time). (3) The dominant seam is neither — adjacent composites share NO border texels, so coarse levels average disjoint sets across a shared edge; that is a C0 discontinuity no filter removes and only the CAP bounds. (4) "3–4 levels" and "≤ +33 %" cannot both hold: 4 total levels are +32.81 %, five are +33.20 %. **The implementation was then rewritten by a measurement.** The first version read each composite back with `getImageData` and filtered in JS: **5.36 ms per composite on the main thread** (4.10 readback + 1.26 filter), with ~450 composites per flight — it stalled the page so hard that CDP `Runtime.evaluate` stopped returning and the harness sat silent for fifty minutes. Halving with canvas `drawImage` instead: **0.06 ms, 89× cheaper** — and it is the CORRECT filter, because canvas 2D composites in premultiplied alpha, which is the exact inverse of the shader's multiply. **Browser-proven: 357/357 composites chained at exactly 4 levels, VRAM ×1.328125 = 85/64 to the digit, and the off-state proven by RETURNING to it (mipMax 0, bytes === level-0 bytes).** The LRU is re-billed: `MemoryUtils.getTextureByteLength` scales by 4/3 only for AUTO mipmaps, so an unbilled manual chain would park ~200 MB past ULTRA's 600 MB ground cap — the U2/A9 loop reached from the accounting side. All three LRU writers now go through one `applyLruBand`.

**A user-visible consequence, measured and now documented:** creation-time levers (anisotropy, the mip chain) reach almost NOTHING on a mid-session flip. The ground cache never turns over — RC9 already measured that the whole Dnipro drape fits inside the desktop cap, and squeezing the cap does not help because the tiles under the camera are in the renderer's `usedSet`. Flipping the chip mid-session left **2 of 321** composites chained; with the chip on at BOOT, **452 of 452**. These levers are delivered by a RELOAD — the same state RC26 already surfaces on the ULT chip.

**Harness lessons, both paid for in this session.** A probe that reads a field which does not exist **fails OPEN**: the RC18 leg read `u2().fpv?.active` (it is `__globe.fpv()`, a function), got `undefined`, and reported "not in FPV" while the engine was demonstrably in FPV with the split already landed. And every CDP call now carries a 90 s timeout — an unbounded `Runtime.evaluate` turned a page stall into a fifty-minute silent hang with zero output.

**RC30 shipped**: `rendering/RENDERING_ARCHITECTURE.md` — the as-built doc, absorbing the audit §2 tables and ULTRA §8/§10, with the five refutations (M5 curvature 4.0 %, M7 zero parent wins over 47k samples, RC28 depth, audit S10 refinement, and now M13/T34 desktop) stated as measurements.

**NOT done, and why.** **RC21 (on-demand render) was not built.** Read-only recon mapped 40+ independent per-frame visual-change sources across 20 files, ~14 of them asymptotic eases with no snap, and its verdict was that the live default is not safely shippable — a false negative is a frozen globe. That judgement is on record with the full inventory; the heartbeat-bounded pattern RC19 just shipped is the shape RC21 should take. **Group D (RC13/RC15/RC16/RC17) was not started** — bake infrastructure was confirmed available this session (R2 credentials, Java 21, the OSM2World jar, 1.7 GB of Overpass caches, the existing bakes), so it is unblocked, not blocked.


New work appends a dated line here — immediately below this note, above every dated entry.
*(Moved to the top 2026-08-13 after drifting mid-list since 2026-08-03; it drifted AGAIN and
was re-seated here 2026-08-22 during compaction round 4 — audit #3 D13, which also records the
root cause: appends go to the top and nobody re-seats the marker. A move is not an edit.
THIRD DRIFT re-seated 2026-08-24d, from below four dated entries: the marker cannot survive a
top-append convention by sitting in the list. It now lives directly under the heading, where an
append physically cannot get above it — backlog T40.)*

- **2026-08-25e · RENDERING CHARTER, GROUP F + RC29 + RC22 — the two ULTRA seams closed, the chip's hidden state surfaced, and the memo's own counter caught an undersized constant.** Gates: **vitest 1,996/1,996 (137 files)** · astro check 0 err / 0 warn / 5 hints · `npx knip` exit-0 · `verify-ultra.mjs` 29/29 · `verify-eclipse.mjs` 38/38 · **`verify-rendering-charter.mjs` 70/70 ALL PASS** (+14 checks; shots `charter-09..10`). Tier: LOCAL + BROWSER. · **RC23 — the ULTRA × ECLIPSE seam.** Every ULTRA look term is driven by SOLAR ELEVATION, and an eclipse does not move the sun: at totality the band curve still said "day", so the aerial perspective went on painting a day-tinted veil over a world the eclipse had just darkened and the hemisphere fill stayed warm. The fix is three multiplies rather than a new curve, and the principle is the reason why — **under an eclipse ULTRA's day-driven additions fade toward BASELINE**, because the darkening already lives in the shaders baseline carries (`uFtwEclipse` on the ground, `uEclipse` on the dome, `× eclipseK` on the key). Haze target and the hemisphere TINT MIX both scale by `eclipseK`; exposure deliberately does not (it is a taste lever, owner A/B AB5). Browser-measured at the owner's Burgos totality: ground haze **0.0253** at `eclipseK` 0.040, against a golden-hour value of 0.1138. · **RC24 — the golden-hour dome seam, the one visible SEAM the ULTRA track shipped with** (`ULTRA_ARCHITECTURE` §12). The dome tints its horizon haze from GOLDEN's bell over solar elevation; ULTRA's ground tints its aerial perspective from a four-stop band curve spanning 36° — two different curves meeting at the terrain/sky junction, which is exactly where the eye is at dusk. The coupling is deliberately NOT a second curve in `atmosphere.ts`: the orchestrator pushes the ground's **own effective, already-gated, already-eased** haze value and the band tint it is using, so the dome moves when the ground moves, through the ground's altitude / flat-chart / dark-drape gates, and cannot drift onto its own schedule — the same structural trick `FTW_AERIAL_GLSL` uses to keep ground and buildings together (share the emitted value, not the intent). Only the HAZE band is pulled, not the zenith: the haze is the band that meets the terrain, and pulling the zenith would fight the `skyBudget` guard for no visible gain. `ULTRA.domeTintK 0.45`, below `hemiTintK` on purpose — the dome is the background the golden band is read against. Browser-proven: dome pull **exactly** `groundHaze × 0.45` and the dome and ground carrying the IDENTICAL band tint hex. · **BOTH OFF-STATES ARE EXACT, and that is the load-bearing half**: `mix(x, y, 0.0)` is `x`, and the run asserts `=== 0` (not `< ε`) with the chip off at totality, then flips the chip on, then off again and re-asserts EXACTLY 0 after the snap (>6.2τ on `exposureTauMs` 950 — a wait, not a round number). · **RC26 — the chip's hidden state, surfaced.** Three of ULTRA's shadow levers are CONSTRUCTION-TIME (three latches a shadow map's size on first render and recompiles every material on a live `shadowMap.enabled` flip), so a mid-session toggle moves every eased lever and leaves the shadow rig on the boot profile. Nothing said so — the chip lit up and the rig quietly did not, which reads as a weak feature rather than as a pending reload. NEW `ultraBootSnapshot()` freezes the boot answer (memoized, because the pref moves the instant the user clicks and a later read would answer "what is it now", not "what did we build with"; the ordering is safe by construction since the chip lives in the panel whose first render necessarily precedes any toggle of it). `pref !== snapshot` drives a hairline warn dot on the chip plus a tooltip that says what to do. GlobeCanvas now reads the same snapshot, so UI and rig cannot disagree. The ULTRA flag fence (`fences.test.ts:212-308`) still passes — the new function names neither the flag nor a new owner file. · **RC27 — two design notes landed where they will actually be read.** The `ULTRA.shadowMapSize` 8192² rollback criteria (three named triggers, and the measurement that decides it — `metresPerTexel` against the live `boundsM`, which RC4 made variable). And the one that would have bitten silently: **if `AO.enabled` is ever flipped on, AO becomes an ULTRA lever by accident** — `updateAoEnabled()` gates on `tier === "high"` and the ULTRA chip PINS the tier to `high`, so on a machine the governor had stepped down to `mid`, turning ULTRA on would also turn AO on. A coupling nobody chose, in no tunable, inside a track whose whole contract is "the chip off changes no pixel". Two honest options recorded; the instruction is not to leave it implicit. · **RC29 — the two convention lines the audit scheduled and nobody wrote.** The `GlobeControls` radians-into-a-degrees-function bug (`Ellipsoid.js:361` emits radians, `:523` reads degrees ⇒ the far plane is 169.4 m / 0.094 % short at Dnipro), latent only because `MIN_ELEVATION = 2550` overshoots the FPV horizon ~39× — **with the reason not to fix it locally: eight modules scale their impostor anchors off `camera.far`.** And the vertical-datum asymmetry: the BAKE is geoid-corrected (`geoid.mjs`'s checked-in EGM2008 grids), EXIF deliberately is not (D4 binding; the `Math.max(sliderAlt, terrainH + 1.7)` clamp is the sanctioned mitigation), **do not add a runtime geoid** — plus the trap that paid for the line, the clamping lookup that answered Everest with Dnipro's corner and turned a correct bake into a 48 m phantom fault. · **RC22 — A/B-PREP only, exactly as chartered: three mobile knob proposals recorded in `tuning.ts` with their rationale and ZERO behaviour change**, because each is a real-device judgement under T1 (lean `overlayResolution2dPx` 512→384, a NEW lean-only `shadowMaxAltM` 8 km against the shared 30 km, and `queueCaps.low.download` 8→6 with parse left alone since it is main-thread glb decode). · **THE COUNTER EARNED ITS KEEP: `GROUND.heightMemoCapacity` 20_000 → 100_000.** RC11's memo overflowed, and the `overflows` counter added alongside it is the only reason that surfaced rather than reading as unexplained frame cost — the constant was guessed from "a few thousand buildings" and the browser measured the real working set at **39,302 buildings + 60,527 tree instances across 101 loaded cells at Dnipro alone**, with the memo at 18,457 live entries. Cost at the new cap ~15 MB against a ground LRU in the hundreds. · **HARNESS TRAP, RE-PAID:** the first three runs of this session died with every island failing to hydrate. Not the code — `504 Outdated Optimize Dep` on the vite dep graph (`curl` on the dep URL is the diagnostic; the page itself answers 200 the whole time). Moving `node_modules/.vite` aside is not enough on its own: the server must be fully down first, then restarted, then warmed with ONE real page load before any browser attaches. · **DEFERRED with reasons, not silently:** RC25 (capped mip chain) — the transparent-border bleed needs hand-built levels judged in a browser A/B at a 4-tile junction, and getting it wrong ships the seam grid the charter warns about. RC18/RC19/RC20/RC21 — with one design finding banked for RC19: **half-rate is NOT viable**, because `composer.render()` overwrites the PiP rect every frame, so the second pass needs a render target plus a textured-quad blit (RT linear, blit material `toneMapped: false`, map `colorSpace` LinearSRGB — that path reproduces the current look exactly). Files: `src/components/globe/StylizedTiles.ts` · `src/components/globe/scene/atmosphere.ts` · `src/components/globe/tuning.ts` · `src/lib/globe/ultraBoot.ts` · `src/components/globe/GlobeCanvas.tsx` · `src/components/panels/CameraTiltPanel.tsx` · `src/styles/camera-tilt.css` · `.claude/conventions/globe-tuning.md` · `scripts/verify-rendering-charter.mjs`.

- **2026-08-25d · RENDERING CHARTER, GROUP C SHIPPED (RC6–RC11) — the seat/height core, and M5 came back a REFUTATION.** Gates: **vitest 1,996/1,996 (137 files, +85 over the session)** · astro check 0 err / 0 warn / 5 hints · `npx knip` exit-0 · `verify-ultra.mjs` 29/29 · `verify-eclipse.mjs` 38/38 · `verify-rendering-charter.mjs` **56/56 ALL PASS**. Tier: LOCAL + BROWSER. · **THE HEADLINE IS M5, THE MEASUREMENT THAT WAS SUPPOSED TO ORDER GROUP D — AND IT KILLED ONE OF ITS SLICES.** The fidelity audit's §1.3 could not tell a QUADRATIC seat error (F1: both bakers project onto a tangent plane and never subtract the `d²/2R` curvature rise) from a FLAT BIAS (F2: the DSM includes the buildings it seats) or a TIME-DECAYING one (F4: streaming), and asked for "applied seat delta vs distance from the bake origin". **The first implementation of that histogram measured the wrong quantity twice before it measured the right one, and each wrong version looked completely plausible.** Binning the per-FEATURE applied delta gives a flat ~15 m rms at every distance — but that is WITHIN-CELL RELIEF, not bake error, because the per-cell seat absorbs the bake's error before any feature is placed. Binning `terrainSeat − bakedHeight` gives a clean +109 → +149 m growth that reads exactly like a quadratic — but the enriched bake is deliberately laid at h≈0 (`ENRICHED.reseatToTerrain`), so that number is the terrain height, not an error. **The quantity that actually answers the question is the curvature residual the per-cell re-seat CANNOT absorb: `d · cellHalfSpan / R`, because every cell is independently re-seated onto terrain at its own centre, so the tangent-plane rise survives only as its VARIATION ACROSS ONE CELL — bounded by the CELL radius, never the bake radius.** Browser-measured over the shipped Dnipro bake: at the 3,500–4,000 m ring, **0.568 m of curvature residual against 14.20 m rms of within-cell relief — 4.0 %.** **F1 IS REFUTED AS THE DOMINANT FLOATING MECHANISM, and RC12 (tangent-plane curvature subtraction, a re-bake of THREE regions) is not worth its cost.** Group D's value is RC13/RC15/RC16/RC17. The derivation is in `debugSeats()` next to the numbers so it can be re-derived rather than remembered (`ENRICHED.cellHalfSpanM`). · **RC6 (audit gap #2) — `lib/globe/terrainPick.ts`: the terrain samplers pick the FINEST hit, not the nearest.** `TilesFadePlugin` keeps a coarse parent in the scene and RAYCASTABLE for the whole crossfade after its children land, and over relief it can sit above the fine mesh — so `intersectObjects(...)[0]` seated buildings on the LOD error until the fade ended, then silently corrected. Tile depth is stamped at `load-model` from `tile.internal.depth`; `heightAt` and `pickGround` both select on it. **Measurement M7's answer is a NEGATIVE, and it is worth recording: over 37,742 samples at rest AND 9,874 more taken through an active refine flown in from 30 km, `hitsPerSample` was exactly 1.00 and the parent won ZERO times.** The mechanism is real in principle and does not occur at Dnipro; RC6 is cheap correctness insurance, not the fix for anything observed. · **RC11 (audit S16) — `lib/globe/heightMemo.ts`: an EXACT (epoch, lat, lon) memo in front of `heightAt`.** Not a spatial approximation — the seat sweep is a round-robin over a fixed set of footprints, so after one wrap it re-asks identical coordinates forever, and the correct cache introduces no new error term anywhere near the one-vertical-authority contract. Invalidation rides `terrainEpoch` (the BEST SPOT tile-load counter already living next to `heightAt`), so a hit is exactly as fresh as a raycast; `null` is deliberately never cached. Browser-measured: **84.0 % hit rate, 18,457 entries, 0 overflows** — which is what funded raising `reseatFeatureSamplesPerFrame` 16 → 64 and the tree budget 10 → 40. · **RC7 (audit S4) — look-biased seat priority + a never-sampled-first drain.** The sweep spent half its budget on the single nearest cell by pure distance, which in FPV is the cell under your feet, not the ones down the street you are looking at; cells are now ranked with the SAME bias law the download queue uses (`lookBiasedDistance`, one definition, so the seating front follows the streaming front) and features with no seat at all drain before any refresh. **A defect introduced and caught by the browser run in the same session: the drain popped an index and re-pushed it on failure, and since `pop()` takes from the same end, one unanswerable footprint retried immediately and forever, eating the whole budget.** Deferred indices now go to the BACK of the queue. Measured: look-cone convergence 50.3 % vs 33.9 % city-wide — the prioritisation is real, and the audit's S4 bar (>0.9 in the cone within 5 s) is **NOT met and recorded as an open tail**: the drain is deferring and the next step is instrumenting why (unloaded terrain under the footprint vs the gate — 4,570 gate rejections are counted, null-terrain deferrals are not). · **RC8 (audit gap #5) — the plausibility gate is relief-scaled and, for the first time, VISIBLE.** The flat 45 m bound was sized for Dnipro's ±20 m cells; in steep terrain every real sample exceeds it, and a gate rejecting 100 % of a cell's samples is indistinguishable on screen from a cell nobody has swept. The bound now widens from the relief a cell has ACTUALLY SHOWN (accepted samples only, so a garbage raycast can never widen the gate that would have caught it) and `debugSeats().rejected` publishes the gate's own work. · **RC9 (audit S6) — the seat cache that survives an LRU eviction.** Keyed by the baked content URI (the env-invariant identity U8's persisted overrides already use); feature seats keyed by baked feature id, so a re-bake simply misses. **Distance alone does not evict — the whole 6 km Dnipro bake fits inside the shipped desktop LRU cap, so at tier `high` these cells are never dropped, and the first version of this check reported 0 banked / 0 warm and looked exactly like a pass.** Squeezing the cache proves the banking leg (101 → 95 cells, 6 banked). The WARM-RESTORE leg is unit-covered but **browser-UNVERIFIED** — the evicted cells did not stream back inside the run's wait; it needs a leg that waits on `load-model` rather than on a timer. Open tail. · **RC10 (audit gap #8) — the FPV walk re-seat.** A temp FPV eye is `pinGround + up·eyeM + walkOffset`, and `walkOffset` is a fixed WORLD displacement — so the eye keeps the height of the ground under the PIN however far you walk, and `fpvEyeAboveGroundM` reported the nominal height either way. The ground under the WALKED eye is now sampled on DISTANCE (`FPV.walkReseatDistM` 4 m — standing still costs nothing, a sprint samples at a fixed spatial cadence whatever the frame rate) and eased with the same `seatStep` every other seat uses. Browser-measured over a 302 m walk: the correction tracked to 0.50 m, the largest camera-altitude change between samples was 0.23 m, and `__globe.u2()` recorded **zero >0.5 m single-frame eye jumps outside walk input**. · NEW DEV seams: `__globe.terrainPickStats()` / `resetTerrainPickStats()` / `heightMemoStats()`; `debugSeats()` gained `rejected`, `unseated`, `nearFeatures`/`nearFeaturesSampled`/`priorityCells`, `seatCacheHits`/`Misses`/`Cells` and `m5`; `__globe.fpv()` gained `walkGroundM`/`walkAppliedM`/`walkOffsetM`. Files: `src/components/globe/scene/{imageryGround,enrichedBuildings}.ts` · `src/components/globe/StylizedTiles.ts` · `src/components/globe/tuning.ts` · `src/lib/globe/loadPriority.ts` · NEW `src/lib/globe/{terrainPick,heightMemo}.ts` · NEW tests `test/lib/globe/{terrainPick,heightMemo}.test.ts` + `loadPriority.test.ts` · `scripts/verify-rendering-charter.mjs` (Group C legs).

- **2026-08-25c · RENDERING CHARTER, GROUP B: ALL FOUR OWNER BUGS FIXED AND BROWSER-PROVEN (RC1–RC5), plus RC0's cheap probes.** Gates: **vitest 1,970/1,970 (135 files, +59)** · astro check 0 err / 0 warn / 5 hints · `npx knip` exit-0 · `verify-ultra.mjs` 29/29 · `verify-eclipse.mjs` 38/38 · **NEW `scripts/verify-rendering-charter.mjs` 41/41 ALL PASS** (shots `charter-01..06`). Tier: LOCAL + BROWSER. **THE HEADLINE IS THAT THE HARNESS LIED FIRST.** The first run of the new script reported the sunset scrub as perfectly smooth — 61 samples, `max |d shadowIntensity| = 0.0000` — and it was measuring a FROZEN TAB: the verify Chrome carries no occlusion flags, rAF was throttled, and every scalar the rig writes per frame was the same stale frame's value, 61 times. A "no step" result from a page that never rendered a second frame is the most convincing possible pass and means nothing. Every sample now goes through `ticks()` (bringToFront + a real rAF count), and the script asserts that consecutive samples carry DISTINCT sun elevations — a zero-result validation on its own probe. Two more harness lies died with it: `bodies()` publishes `sunDir`, not `sunAltDeg`, so a bisection on `undefined > 0` walked silently to its own lower bound and "found sunset" at exactly the search start; and `JSON.stringify(<async IIFE>)` stringifies the PROMISE, so the M6 probe reported `{}` rather than failing. · **RC1 (B2, the square at totality) — `scene/glsl.impostorEdgeWindow` + `SKY.sunQuadFade [0.72, 0.98]`.** Two additive terms had no compact support at the sun quad's own boundary (the corona's `0.42·x^-2.6`, ≈0.003 linear there) and `DITHER_GLSL` painted ±1/256 over EVERY pixel of the plane including where the signal was exactly zero — truncating both at a straight edge IS a rectangle, readable only once the background fell to `ECLIPSE.daylightFloor`. Fix: one radial window closing to EXACTLY zero at 0.98 × `sunGlowExtent` = 6.86 of the plane's 7 inscribed disc radii, `discard` past it (the corners reach 9.9), and — the load-bearing detail — the window multiplies the colour **AFTER** the dither, so the noise fades out with the signal instead of stopping at a line; gating the dither would only have moved the straight edge onto the gate. Sibling pass on `skyTarget.ts` (`SKY_TARGET.quadFade [0.92, 0.995]`, chosen to clear the widest reticle tick the code can draw, 0.908 of the half-extent). Browser-measured at the owner's own Burgos totality hash: quad half-extent 210.4 css px, largest single-pixel luminance step across the old boundary **2.66 of 255 vs 32.05 in the control band inside it**, and 23.2 vs 24.33 either side. · **RC2 (B3, the sunset snap) — `lib/globe/keyHandoff.ts` + `SHADOWS.fadeBandSin 0.0523` (≈3°).** Three flips landed on the SAME boolean at sun elevation +0.46°: `castShadow`, the sun→moon key teleport, and the dedicated moonlight standing down — and the golden bell is flat at 1.0 there, so a kilometres-long 0.75-opacity shadow field vanished in one frame at the key's brightest moment of the day (2.025). The enabler is that `shadow.intensity` is a LIVE per-frame light uniform in three 0.185 (`LightShadow.js:41` → `WebGLLights.js:289` → `mix(1.0, shadow, shadowIntensity)`) AND `getShadowMask()` routes it through `ShadowMaterial` too, so ONE write fades the building-received shadows and the ground twins together. Both arms now trough to zero AT the gate and the dedicated moonlight gives up exactly the share the rig takes (`moonKs × (1 − moonRigTakeover)`), so the total moon key is continuous while the source changes underneath it. **The phantom night key is deliberately UNTOUCHED where no moon is waiting** — killing it is AB1, not this slice. Browser-measured across 103 samples from sun +4.21° to −1.28°: shadow field **1.000 → 0.0000 → 0.601**, largest single-step change **0.0270** (the old boolean was a 1.0 step), trough at sun elevation **0.426°** — the gate is asin(0.008) = 0.458° — with the key at 0.0000 there too, and `castShadow` never flips at all. · **RC3 (B4a) — the `!!focusHit` term is GONE from `shadowEligible`.** It survived the ULTRA rewrite verbatim and was the biggest shadow killer in FPV: any look at or above level nulls the ellipsoid intersection and one frame later `castShadow` was false AND every terrain caster was detached. Browser-proven: **4 of 7 sweep samples have no ellipsoid hit at all** (the fit equals `sqrt(h(2R+h))` at that sample's own altitude, 16.4–33.4 km) **and still cast**. · **RC4 (B4b) — `lib/globe/shadowFit.ts` + `SHADOWS.viewFitK 1` / `boundsQuantM 128`, and a rig-only `_shadowFocus`.** The ortho box was centred on the screen-centre ellipsoid hit with a half-extent riding altitude — but the hit distance rides altitude too, so whether your own foreground was inside the box depended on PITCH ALONE: `atan(1/boundsAltK)` = **59.0° base / 42.0° ULTRA, at every altitude**, which is why zooming never helped. The box is now framed on the VIEW (extent `clamp(max(alt·K, d/2 + boundsM/2), boundsM, cap)`, centre pushed `min(d/2, half − boundsM/2)` along the horizontal look) and `_focus` is untouched — that vector is the tilt/heading pivot and the lat/lon source for PLAN/FIND/BEST SPOT, and its documented miss-fallback is load-bearing for them. Browser-measured: the eye is inside the box at every pitch, the extent tracks the view (1,600 → 5,000 m across the sweep, quantized so `updateProjectionMatrix` still runs on change only), and at the owner's −0.2° case the box is 5,000 m with its centre 4,200 m ahead of the eye at 2.44 m per shadow texel. **The crispness trade, stated:** near-level looks now spend texels on 5 km instead of 1.6 km (0.78 → 2.44 m/texel) — but at those pitches there were NO shadows at all before RC3, and steeper looks keep 0.78–0.91. `viewFitK: 0` restores the old extent while keeping the re-centring; goes to the owner with AB4. · **RC5 (B1, "Map data not available") — `lib/globe/esriPlaceholder.ts` + a `public/sw.js` carve-out.** Esri answers **200 OK** outside its coverage with a 2,521-byte JPEG, byte-identical everywhere (re-probed live 2026-08-25 at Everest z19, a random Pacific z19 and a z22 past the service's max level: same bytes, md5 `f27d9de7f80c13501f470595e327aa6d`, ETag `"vvvvvvvvvvvvf"`). Every fallback in the stack is a network-FAILURE path, so none of them ever armed. A construction-time `o.fetch` wrapper now detects the sentinel, fetches the parent, crops this tile's quadrant and upscales it — the placeholder never draws — plus a once-per-tile `cache: "default"` heal (against `force-cache` pinning) and a learned cap table that skips known-sentinel GETs. **THE CHARTER'S PROPOSED DETECTOR WOULD HAVE HALF-FAILED IN THE BROWSER: Esri sends no `Access-Control-Expose-Headers`, so `ETag` is not CORS-readable from a page** (`res.headers.get("etag")` is null) — detection is on the BYTES, length + FNV-1a-32 `0x92d9118f`, pinned against a committed fixture. Browser-proven against the LIVE service through a DEV probe (`__globe.esriProbe`), because whether a camera pose ever asks for an out-of-coverage tile depends on the terrain tileset's own LOD there: three real Everest sentinels came back as **6,790 / 12,618 / 11,441 bytes of real imagery**, the summit tile passed through untouched at 11,556, repeat GETs were all skipped without re-asking the service, and the one case the fallback cannot answer (past Esri's max level, where every ancestor is a sentinel too) fails soft and is then served from a banked body rather than re-GET forever. · **RC0 probes recorded:** M1 FPV `near` **1.0 m** / `far` **180,375 m** (ratio 180,375) · M2 **DEPTH_BITS 24**, webgl2 · M6 `heightAt` **0.018–0.067 ms/sample**, 200/200 answered over a loaded city · M8 Dnipro terrain 68.9–186.0 m. M5 (the §1.3 separator ordering Group C vs D) still open — it needs per-cell seat instrumentation that Group C opens anyway. Files: `src/components/globe/scene/{glsl,sky,skyTarget,imageryGround}.ts` · `src/components/globe/StylizedTiles.ts` · `src/components/globe/tuning.ts` · NEW `src/lib/globe/{shadowFit,keyHandoff,esriPlaceholder}.ts` · `public/sw.js` · NEW tests `test/components/globe/impostorEdge.test.ts` + `test/lib/globe/{shadowFit,keyHandoff,esriPlaceholder}.test.ts` + `test/swTileCache.test.ts` · NEW fixture `test/fixtures/esri-placeholder.jpg` · NEW `scripts/verify-rendering-charter.mjs`.

- **2026-08-25b · RENDERING CHARTER AUTHORED — four owner bugs ROOT-CAUSED and the fidelity audit RECONCILED against HEAD into one execution charter for the coming autonomous graphics session. Zero code written (design/prep by owner order); BEST SPOT tails (T52/T49/T50) PARKED by the same order.** Method: 7 parallel read-only agents (4 bug investigations · 2 audit-reconciliation passes vs HEAD 1a486c9 · 1 mobile/ULTRA inventory), synthesized into **NEW `.claude/claude-docs/rendering/RENDERING_CHARTER_2026-08-25.md`** (ladder RC0–RC30 + owner A/B queue AB1–AB7 + do-not-do list). Registry: **NEW row T54** (the charter) · **T38 CLOSED dated** (all three sub-items verified fixed at HEAD; new remainder — the /m PiP still runs a FULL second scene render per frame — is charter RC19) · **T43 dated edit** (start from the charter, not the audit's §5). **THE FOUR BUGS, each with the mechanism pinned: (B1)** Everest "Map data not available" tiles are **HTTP-200 placeholder JPEGs** (live-probed: 2,521 B byte-identical, ETag `"vvvvvvvvvvvvf"`, max-age 86400; z19 coverage there is a sub-km ISLAND — real imagery on the summit tile, 100% unavailable 256–1024 tiles out, so no static region cap can work). The only fallback machinery is the network-FAILURE path (`imageryGround.ts:332-343`) which a 200 never arms, and `force-cache` + `sw.js` then pin the placeholder (iOS: 7 days). Fix = construction-time `o.fetch` wrapper (the aniso-stamp precedent): detect by byte-length+ETag fail-soft → fetch parent tile, crop quadrant, upscale — the placeholder never draws; + sw.js sentinel carve-out + once-per-session `cache:"default"` revalidation. The `calculateLevel`-override alternative is a TRAP: lock/release recompute the level as a default argument, so a cap table changing between them makes `DataCache.releaseViaFullKey` THROW. **(B2)** the totality square = TWO additive terms nonzero at the sun quad's boundary — the corona outer power law (no compact support; ~0.003 linear truncated at the edge = hard step) + the unconditional ±1/256 `DITHER_GLSL` with **no discard anywhere in the sun fragment**; visible only while `eclipseK` bottoms near `daylightFloor 0.04` AND `tot > 0` (coverage 0.985→1), which is why it condenses near maximum and dissolves both ways. Bloom exonerated by arithmetic. Fix = radial window-to-exact-zero + epsilon discard (the `stars.ts:168-171` idiom; the moon already does it at `sky.ts:399-400`); same defect class flagged in `skyTarget.ts`. **(B3)** the sunset snap = **`castShadow` is a one-frame BOOLEAN at sun elevation +0.46°** (`SHADOWS.minSunElevSin 0.008`, `StylizedTiles.ts:4024/:4068`) fired at the key's daily MAXIMUM (1.5 × golden 1.35 = 2.025) — a km-long 0.75-opacity shadow field vanishes in one frame; plus the sun→moon key switch is a same-frame FLIP on the same threshold (intensity 2.025→0.3·moonKs, azimuth teleports; the model is Krisciunas–Schaefer 1991 — the audit's "Kasten & Young" label was wrong), and with no qualifying moon **the sun key never dies at all** (no elevation term — walls lit all night at 1.5 by a below-horizon sun). ULTRA shares the gates (its exposure ramp slightly AMPLIFIES the step). KEY ENABLER, source-verified in three 0.185: **`shadow.intensity` is a live per-frame uniform** (`WebGLLights.js:289/343` → `mix(1.0, shadow, shadowIntensity)`) reaching building shadows AND the ground twins in one write — so the fix is a smoothstep fade over the last ~3° (charter RC2), and the full "real dusk" baseline package (band curve to baseline + key elevation term + the phantom-night-key decision) is owner A/B AB1 because the frozen night look DEPENDS on the phantom key. **(B4)** the `!!focusHit` shadow-kill gate **survived the ULTRA rewrite verbatim** (`StylizedTiles.ts:4023`; the fallback the fix needs already exists at `:3107-3110` and is discarded) — any near-level look kills `castShadow` AND `setTerrainCast(false)` in one frame; AND the ortho box (centre = ellipsoid hit, half-extent `clamp(alt·K, 1600, cap)`) covers the viewer's own foreground **only at pitch ≥ ~59° base / ~42° ULTRA — independent of altitude**, everything outside rendering fully lit with a hard edge (three r185 returns LIT outside [0,1]). Streaming/stamp theories RULED OUT (terrain-caster stamp-at-load is correctly wired incl. LRU re-loads). Fix = gate kill + surface-projected eye fallback (RC3, XS) + rig-only `_shadowFocus` view-distance framing (RC4). **AUDIT RECONCILIATION HEADLINES:** #1/#9/#16-tracking shipped **ULTRA-gated** via T44/T45 — their baseline halves are A/B items fenced by the ULTRA off-state exactness contract + byte-identical-`high`; **audit S10's premise is REFUTED** (desktop imagery availability-capped, `tuning.ts:584-587` — do not build); S12 rephrased to "generalize the shipped `ultraTileLevers` edge seam"; S17's surface grew to **22 raw-ShaderMaterial instances / 14 modules** (+3 `bestSpotSheet.ts`; module list −skyTrail +bestSpotSheet); seat/bake gaps #2–#8/#11/#13 byte-equivalent with anchors moved ~+200–400 lines (fresh anchors in the charter §1); bake scope is now **×3 regions** (st-albans landed); #25/#26 doc dispositions were never executed (RC29; #26 can now cite `scripts/bake/terrain/geoid.mjs` and its 48 m Everest incident). **NEW findings logged:** ULTRA×eclipse haze seam (haze rides sun elevation, never scaled by `eclipseK` — day-tinted haze paints over totality under ULT; RC23 one-line fix candidate) · the golden-hour dome seam confirmed at code level (`atmosphere.ts` reads no band uniform; RC24) · chip-flip "reload for the full shadow rig" state surfaced nowhere in UI (RC26) · if GTAO (T10) is ever enabled it silently couples to the ULT tier pin (RC27 records the decision point). Verification tier: **research/docs only** — no code, no tests changed; every bug mechanism is source-computed and marked for browser confirmation in its slice's proof-of-done. Files: the charter (new) · `tracked-backlog.md` (T54 + T38/T43 dated edits) · this line · NEXT_SESSION_PROMPT rewritten · `mem:project/wip-2026-08-25-rendering-charter`.

- **2026-08-25 · BRAND: PLUX IS THE PRODUCT, "Frame the World" IS THE REPO — and a real leak had already shipped.** Owner: *"it should be plux everywhere by now (except repo name, have to live with that for now)"*. Gates: **vitest 1,911/1,911 (131 files, +6)** · astro check 0 err / 0 warn / 5 hints · knip exit-0 · `npm run proofs` 25/25. Tier LOCAL. **THE LEAK:** `src/lib/export/ics.ts` shipped `PRODID:-//Frame the World//Shot Planner//EN` and `UID:ftw-…@frame-the-world` **inside every exported .ics calendar file** — a user artifact that leaves the app. Fixed to `PRODID:-//PLUX//…` and `UID:plux-…@plux.today` (RFC 5545 §3.8.4.7 wants a real domain on the right-hand side); safe to change the UID because prod is dark, so no calendar holds a prior UID that a re-import would duplicate instead of update. Also swept: README title + hero alt text, `public/sw.js` and `scripts/bake/r2-worker.mjs` headers, `CLAUDE.md` / `ARCHITECTURE.md` / `contracts.md` / the `/frame` skill titles. **The rest of the app was already correct** — `<title>Plux</title>`, og tags, `SITE_URL = https://www.plux.today`, 35 PLUX mentions in `src/`. **OWNER RULING, ASKED AND ANSWERED: the rename stops at what a user can SEE.** Internal identifiers deliberately keep the repo name, and the reasons are asymmetric: the six `ftw:*` localStorage keys are **persisted user state** (view prefs, building-height overrides, the desktop opt-out, dismissed banners) so renaming them **silently wipes every existing browser** — including the owner's, which is exactly where the `▦ 3D DETAIL`-chip-off constant-field bug came from; the ~20 `uFtw*`/`vFtw*`/`FTW_*` shader identifiers are a large GLSL surface where **a missed rename fails SILENTLY** (the injected-header trap); the `Ftw` Lean namespace is internal. **NEW `test/brandFence.test.ts` fences the split BOTH WAYS** — it fails if a user-visible string says "Frame the World", **and** if an over-eager future sweep renames the persisted keys. Comments are stripped first, because several legitimately EXPLAIN the rule and `tokens.ts`/`tokens.css` cite the Claude Design board, which is genuinely *named* "Frame the World" (rewriting that would make a provenance pointer wrong). Carries zero-result validation on the stripper in both directions, a corpus-size guard, and a POSITIVE CONTROL replaying the two leaked strings verbatim. **Falsified by mutation:** re-introducing the old PRODID turns it red. **The durable fix is the process one** — the ruling now leads `CLAUDE.md` (§The name) and `mem:core`, because the leak and my own prose slips were both a case of the split being recorded somewhere a session does not read first.

- **2026-08-24d · DOCS RECONCILED AFTER THE HEATMAP SHIP, AND THE MATH GOT A MACHINE-CHECKED FLOOR — a Lean 4 + Mathlib proof project that found TWO reachable defects on day one, in code that 1,902 tests, `astro check`, `knip` and a 100-check browser harness all passed.** Owner order: make `BESTSPOT_PLAN.md` + the architecture + memories consistent after the BEST SPOT ship, then evaluate Lean / Wolfram / anything else for making the prediction math verifiable. Gates: **vitest 1,905/1,905 (130 files, +3)** · `astro check` 0 err / 0 warn / 5 hints · `npx knip` exit-0 · **NEW `npm run proofs` — 25 theorems, 0 `sorry`, axiom-audited, 25 PASS / 0 FAIL**. Tier LOCAL (no browser work this session; nothing rendered changed). **THE HEADLINE: the proof was not the point — the HYPOTHESES were.** To state "`S ∈ [0,1]` and `S` is monotone in each preference term" you must write down `0 ≤ w` and `conf ≤ 1`, and neither was enforced. Verified executably, not by inspection: `sanitizeScoringPatch({graze:{conf:{terrain:5}}})` resolved VERBATIM (publishing `CellScore.f = 1.6` against its own documented `0..1`), and `sanitizeScoringPatch({weights:{f:-1}})` resolved to a weight sum of **−0.30000000000000004**, making the score **non-monotone in its own term** — a cell with a better silhouette ranks LOWER. Both reachable from a persisted `ftw:view-prefs:v1` blob, which is precisely what `clampResolved`'s own docstring promises cannot happen (*"can be handed a raw, unsanitized patch and still cannot return an unsafe or dishonest profile"*). `clampResolved` bounded `conf.tree` and nothing else; every test passed a SANE profile, so the clamp's absence was invisible. Fixed with `BESTSPOT_SAFETY.confMax 1` + `.weightMin 0` (both `clampResolved` and the `clampLeaf` single-leaf path, which is what `__globe.bestSpotTuning` reaches), pinned by two regression tests that cite the theorem, with zero movement in any shipped number (every preset re-verified). **A test samples; a specification quantifies.** · **`formal/` — Lean 4.33.1 + Mathlib at the matching `v4.33.1` tag** (`lake exe cache get`, 8,690 prebuilt `.olean`, 7.4 GB, gitignored; ~5 min cold, 2.6 s warm). `Ftw/Hull.lean` proves the ARCHITECTURAL KEYSTONE — `hull_fold` (finding F4's curvature fold: the identity that makes the eye height `L` survive in exactly ONE scalar `q_c`, which is why a lift change is a re-query and not a rebuild), **`below_chord_never_sets`** (a sample on or below the chord joining two others is never the maximiser **at any eye height whatsoever** ⇒ the setter always lies on the upper hull ⇒ the hull is eye-independent — the theorem behind `hullBuilds === 0`), `setter_moves_outward` (once the farther sample wins it keeps winning, which licenses the `break` at `horizonSweep.ts:580` and the binary peak search at `:849`), and `slope_le_iff_cross`. `Ftw/Score.lean` proves `clamp01`, the preference blend (bounded · monotone · **invariant under a uniform rescale of the weight vector**, the algebraic content of a test that pins one scale factor), R7 `M_eff` (exactly 1 for sun kinds FOR EVERY FLOOR), GRAZE `1−exp(−τ/s)` (in `[0,1)`, strictly monotone, zero at zero — the property the replaced `F_sil` lacked), the `cut` factor, and the two COUNTEREXAMPLE theorems that are the defects above stated as mathematics. **THE FRAMING WAS THE WHOLE COST DIFFERENCE:** formalizing the IMPLEMENTATION (define a computable `upperHull`, prove the monotone chain correct) is a 1–3 person-WEEK project because Mathlib has no computational upper hull at all; formalizing the CLAIM the code depends on took under an hour. When a formalization looks like weeks, you are usually formalizing the implementation instead of the claim. · **The gate is `scripts/verify-proofs.mjs` (`npm run proofs`), and the BUILD IS NOT THE GATE** — a proof stubbed with `sorry` still builds. It bans `sorry`/`admit`/`native_decide` at source level (comment-stripped first), builds, then AXIOM-AUDITS every `theorem` against `[propext, Classical.choice, Quot.sound]`. Falsified by mutation: injecting one `sorry` gives `lake build` **PASS** and the audit **FAIL**. Deliberately NOT in `npm test` (vitest stays 22 s and must not need 7.4 GB of Mathlib). · **The new script tripped a REAL fence and the fence was right** — `test/verifyHarness.test.ts` (audit-3 C11) requires every `scripts/verify-*.mjs` to import the CDP cleanup helper and avoid bare `process.exit`. `verify-proofs.mjs` drives no browser, so it is outside C11's scope; the repair was NOT to loosen the rule but to make the exclusion PROVABLE — `NON_CDP` is now guarded by a test asserting each member is CDP-free, with a positive control. **The obvious marker `/json/new` does not work**: both pre-existing exemptions NAME that endpoint without calling it (`verify-chrome.mjs:132` prints the curl recipe as help text; the cleanup helper quotes it in its docblock and only ever calls `/json/close`). The marker that separates them is whether the script SPEAKS the protocol — measured `verify-bestspot.mjs` 7, cleanup 7, `verify-chrome` 1, `verify-proofs` **0**. Falsified by smuggling a real CDP script into the list. · **DOCS**: `BESTSPOT_PLAN.md` gained an `AS BUILT — S3a→S7` appendix (its record stopped DEAD at S1/S2) plus in-place corrections to every NORMATIVE claim a reader would hit before the appendix — `F_sil` (which never shipped; `silTangency` has zero call sites) superseded by GRAZE, the composition corrected to the weight-NORMALISED registry form with `M_eff`, `renderOrder 9 → 4`, the phantom `BESTSPOT.fillAlphaMax` (zero hits in `src/`) replaced by the veil/ink split, the score-texture parenthetical that said "compute it, do not assume" while asserting 201² > 335² backwards (shipped is **601²**), the three-tier residency table marked superseded by six, `worthFloor → worth.effectiveFloor`, and open items 1/2/3/6/7/8 CLOSED in place with their closing anchors. `BESTSPOT_SPEC_V2.md` took 22 corrections in 27 targeted edits, headlined by **the term buffer being 75 B/cell, not 59** — the 59 B layout stored the notch as a finished product, which would have FROZEN five `gap.*` leaves that `CLASS_OF` files as `recompose`, i.e. implementing the spec would have made the shipped invalidation table a lie; also COMPOSE 0.272 → ~2.5 ms (the architectural conclusion survives: still >200× cheaper than the cheapest re-solve), 54 leaves not ~45, the browser ladder (45.4 ms first ink / 523.8 ms refined) beside the forecast, the THREE refusals incl. `no-built-geometry`, the FOUR streaming epochs incl. `builtEpoch`, the built-density prior as an evidence gate, `refuseBelowReachM`, the regenerated 60-key tuning inventory, the shipped Turbo hexes (the 8 interior stops had been reproduced from memory and all 8 were wrong), and §8's three "open questions" converted to ANSWERED with their rulings. · `ARCHITECTURE.md` gained **§7c** and five §7 sub-list repairs (the `14 stores` claim was factually wrong); `contracts.md` 20 → **21** DEV seams + the four BEST SPOT sub-seams; `globe-tuning.md` gained a **§BESTSPOT family** (the doc named ZERO of the 54-leaf scoring profile or the 60-key render block — a REPEAT of audit-3 D5's exact finding) + four traps (the renderOrder ladder, a worker entry being a new Vite optimize-dep root, `postMessage` transfer-lists detaching a by-reference buffer **which vitest structurally cannot express**, and reading a published field's DISTRIBUTION rather than a flag); README gate counts 1,373/113 → 1,905/130 + a BEST SPOT bullet; `mem:core` §Next step rewritten (**it still said "NEXT: S3" and "LOCAL ONLY — nothing has been in a browser yet" about a feature that shipped and was browser-verified** — the highest-severity stale line in the graph root) + six missing era rows. · **NEW `.claude/claude-docs/FORMAL_VERIFICATION.md`** — what is machine-checked, the honest LIMITS (no Lean→JS path worth using, so a proof can silently drift from the code; proofs are over ℝ while the code is float64, and Lean 4.33.0's new `Float.Model` covers arithmetic and `sqrt` but **`Float.Model.sin` does not exist**, so the gap stays open exactly where an ephemeris needs it closed), the WOLFRAM evaluation (free Engine for Developers is alive and a `.wls`→JSON→vitest fixture loop is squarely inside its licence; an OFFICIAL MCP server exists — `WolframResearch/AgentTools`; it beats Lean at `Interval[]` outward rounding and `FindInstance` counterexamples and loses at producing a re-verifiable certificate), and a RANKED ladder. · **Backlog T48–T53 opened** (culverted drains — a defect whose source comment said "Recorded" with no registry row · BEST SPOT taste pass · Wix-cloud unverified · the S8/S9 deferral · **T52 six suspected-false invariants, S2 first** · **T53 the verification ladder**), T40 given a dated edit (compaction round **5** is now due — §Recent measured 146.7 KB against its own ~140 KB trigger, having re-grown 84 KB in two days). · **Six more suspected-false invariants are NOT yet reproduced and are recorded as such** — a finding that does not reproduce is deleted, not downgraded (T52). · **IDE ADDENDUM (same session): the Lake WORKSPACE ROOT moved to the REPO ROOT so the proofs are editable in IntelliJ.** The owner's lean4ij plugin failed with *"Unable to locate lean toolchain … Expected toolchain location: <repoRoot>/lean-toolchain"*. Established from the installed 0.2.8 bytecode: lean4ij computes `Path.of(project.getBasePath(), "lean-toolchain")` with **no upward or downward walk**, exposes **no** project-root / toolchain-path setting (all 34 `Lean4Settings` fields are cosmetic), and then runs `lake serve -- <basePath>` with `cwd = basePath`. **A symlinked `lean-toolchain` is a TRAP, not a fix** — the existence check follows symlinks, so the balloon disappears and Lake then finds no config, falls back to plain `lean --server` with `LAKE_INVALID_CONFIG`, and every `import Mathlib.…` goes unresolved: syntax colouring over a Mathlib-blind editor, strictly worse than an honest error. So `lakefile.toml` + `lean-toolchain` + `lake-manifest.json` moved up, with `srcDir = "formal"` keeping every `.lean` source in `formal/`. **The plan's `buildDir`/`packagesDir` redirect back into `formal/` was tried and MEASURED WRONG**: Lake honours `packagesDir` when computing `LEAN_PATH` but materializes dependencies at the hardcoded `<workspaceDir>/.lake/packages` regardless, leaving a **second real 601 MB git clone of Mathlib** at the root alongside the one it was pointed at. One location — the default `/.lake/` — is the only self-consistent layout and the only one that bootstraps correctly on a fresh clone; `formal/` is now sources ONLY. `/.lake/` was gitignored **before** the first root-level `lake` command ran, because `session-end-ship.sh` does `git add -A` and one build materializes gigabytes in seconds. Verified with the three calls the LSP actually makes: `lake build --no-build` → *All targets up-to-date (2011 jobs)* · `lake setup-file formal/Ftw/Hull.lean` → `Ftw.Hull` / package `ftw` / **1992 importArts** (Mathlib on the path) · `printf '' | lake serve -- $PWD` → only *Cannot read LSP request: Stream was closed*, i.e. **no** config-fallback. Known cost, accepted: `cd formal && lake build` no longer works (Lake does not search parent directories). Fully reversible by moving three text files back. NEW `formal/README.md` carries the bootstrap, the IDE acceptance test, the CLI goal-state workflow (`trace_state` · `by skip` prints the goal · `exact?` searches Mathlib) and the traps; the `/frame` skill gained a **four-condition** gate on when a Lean theorem is even appropriate.

- **2026-08-24c · BEST SPOT SHIPPED END TO END (S3a→S7) AND BROWSER-VERIFIED — and the first real browser run found the feature substantively broken in the one case it exists for, with every unit gate green.** Built the whole remaining feature in six waves: **S3a** the tunable scoring profile (`lib/geo/bestSpotScoring.ts`, 54 leaves, `CLASS_OF` invalidation table, PHYSICS/SAFETY/HONESTY blocks unreachable from a patch, an EVERY-FIELD-IS-LIVE walk that perturbs every leaf) · **S3b** **GRAZE** replaces `silTangency`'s provenance gate (`cut × Q × dwell`; the grazing 8 km ridge goes 0.0000 → **0.9912** grazing / 0.4843 perpendicular, and F's correlation with P falls **r² 0.997 → 0.393**, so the 0.30-weighted framing term finally carries independent ranking signal) · **S3c** `scoreMask` (1.8–1.9×), **`reachM`** + an `openSky` gated on it, the fused pass and the **75 B/cell TERM BUFFER** (never `S`) · **S3d** the long-lived module worker + feed + six residency tiers + the 24/12/6/3 m ladder + refinement debounce · **S4** the GL sheet (veil/ink split, RG8 `LinearFilter`, `fwidth` contours, rim falloff, plumb line + **scale spoke**) · **S5** the desktop panel as the third `planfind` segment · **S6/S7** the residency pins and the honesty layer. **Gates: vitest 1,902/1,902 (130 files, +342 from the 1,560/119 baseline) · `astro check` 0 err / 0 warn / 5 pre-existing hints · `npx knip` exit-0 · `scripts/verify-bestspot.mjs` 100 PASS / 0 FAIL, exit 0, reproduced twice.** **THE HEADLINE — EVERY UNIT GATE WAS GREEN WHILE THE FIELD WAS A CONSTANT.** The first browser run measured the published RG8 over dense central Dnipro at `rMin === rMax === 187` — **one distinct value across all 31,417 scored cells** — beside `heightProvenance {enriched: 0, osm: 0}` and a healthy `builtDensityPerKm2 54.7`. **No building geometry ever reached the worker's DSM**, so every cell saw the same open horizon: the plan's §11 "single most dangerous failure mode" (a warm, confident, uniform disc) firing at the owner's hero location, with a top-8 spread of **0.4 %**. Two independent causes, both measured: (a) the `▦ 3D DETAIL` chip was OFF in that browser's `ftw:view-prefs:v1`, and `buildings.setActive(false)` **removes** `tiles.group` from the scene, so `flattenTin` traversed an empty group; (b) **no epoch watched BUILDING tile arrivals** — the feed's three streaming epochs were the ground tileset, the MVT version and the enriched *re-seat* counter, so a disc solved before the buildings streamed never re-solved. Fixed with a `builtEpoch` on both building tilesets' `load-model`/`dispose-model`, and — because the engine *knew* both facts and said nothing — a disc with dense MVT and **zero** building meshes now **REFUSES** (`"no-built-geometry"`) instead of painting warm. After: **31 distinct score bytes, mean 0 at the 1.7 m eye → 159 at a 56.7 m sheet** (the pre-fix disc measured byte 187 at *both* lifts, because there was no mass to clear), top-8 spread **56 %**. Nine more browser-only defects, each invisible to vitest: **`postMessage` transferred `conformM`, which `composeField` hands out BY REFERENCE from the resident rung** — the first post detached the worker's own copy, so every later post threw `An ArrayBuffer is detached and could not be cloned`, freezing the on-screen `scoringHash` and killing `.ab()` (un-catchable in vitest, where `postMessage` has no transfer semantics) · `postingOf` returned `cellM` for every input **algebraically** (two `known` factors cancelled), so the panel printed `OVER TERRAIN AT ~3 m` — a **C2 violation**; replaced by a TIN-vertex-density measure that now reads **58.5 m in Dnipro vs 35.9 m at Everest** · the refine ran but the store mirror rebuilt `topK` from the last *rung* message and discarded it · `waitRefined` returned on `refinedMs > 0 && !solving`, neither of which carries solve identity, so the S6 checkpoints straddled a phase boundary (the **script** was wrong, not the engine — with only the wait replaced, scrub measures **+0** and radius **+156**) · the mid-reservoir negative was simply **not on the water** (its 300 m disc scored 8,050 cells; hill-climbing the engine's own LandGrid found 48.479450, 35.048099, which measures **0 scored / 31,417 blocked**) · the cross-model check was **vacuous twice over** (asked at the scrubber's instant where the sun sits 7.35° down, and `blockedNow` is *structurally* true at `t0Ms`, the refracted contact, where the airless centre is −0.87°) — re-expressed as a `skylineAltDeg` spread, **66.2° across the disc's own two extremes** · and `window.__globe.scene` was never exposed, so **all seven of S4's "read the LIVE material" done-checks lived only in vitest against constructor arguments**; a `__globe.bestSpotSheet()` probe now asserts them against the shipped objects (max sampled `aVeil` **0.3000**, 601² `NoColorSpace` `LinearFilter`). **PROCESS — a wall-clock perf budget inside a 12-way-parallel vitest is a claim about the RUNNER.** Three pins went red under load with zero regressions (3 m solve 646 ms standalone vs 1,335–1,522 in-suite). `test/lib/geo/_perf.ts` now expresses them in **reference-machine milliseconds**, calibrated **per iteration** — a single up-front calibration was measured reporting `k = 1.04` while the solves it normalised ran 1,335 ms — and the calibration workload had to be grown to **32 M iterations (~27 ms)**, because anything shorter **fits inside one scheduler quantum and reports exactly 1.00 under any load** (4 M: 3.6 ms quiet, 3.6 ms at load average 110). Budgets unmoved; all five pins falsified by mutation; five consecutive full-suite runs green, plus green with 10 extra spinners on 12 cores. **Also carried: R7 `M_eff = 0.35 + 0.65·M`** (exactly 1 for sun kinds, so no sun number moved; the median moon night goes 0.020 → 0.256) and the **absolute azimuth-lattice snapping** that takes a +1-day track from **0 of 40** shared hulls to **36 of 39**. Verification tier: **LOCAL + BROWSER** (`verify-shots/bestspot-01…08`); Wix cloud UNVERIFIED (prod is still dark behind the nameserver gate). Twin: `mem:project/wip-2026-08-24-bestspot-s3-s7`.

- **2026-08-24b · BEST SPOT S6 + S7 SHIPPED — the residency contract became ONE falsifiable number, and it was FAILING; the built-density prior closed the plan's own “single most dangerous failure mode”.** **S6 — THE PIN WAS RED AS BUILT, and no per-slice test could see it.** `bestSpotSolver.test.ts` already pinned the kernel half (“a LIFT change re-queries the resident hulls and rebuilds NONE of them”) and it was green — while the feature rebuilt every hull on every drag frame, because `bestSpotWorker.solveRung` called `buildDsm` UNCONDITIONALLY and the hull cache is keyed on `dsm.ground` IDENTITY (`solveTerms`: `cached.ground === dsm.ground`). The invariant was correct in the kernel and thrown away one layer up: measured **39 `buildHulls` per drag frame against a plan that pins 0**. FIX: a `sourcesEpoch` on the job, bumped by the feed ONLY on a T0 move (centre/radius) or a streaming refinement past `rebuildQuietFrames` — never by the lift, the day or a taste patch — plus a `RungState.sourcesKey` so the DSM and the LandGrid survive T1/T1′ verbatim. R6's lift probes now re-use the sources AND the hulls too (they were `mode:"stream"`, i.e. they opted out of the very invariant they exercise). **MEASURED, real MVT + real ephemeris, K=39: first solve 39 · within-day scrub 0 · 2→400 m lift drag (8 frames) 0 · day step 2/39 · radius change 39 then 0.** NEW `test/lib/geo/bestSpotResidency.test.ts` drives `solveRung` over a real `Resident` (the layer where the contract is kept or lost) and carries a NEGATIVE CONTROL that moves `sourcesKey` per frame and reds at 3·K — so the three zeros are the contract and not a dead counter. Also pinned: **coarse solve p95 9.1 ms standalone at the 24 m drag rung** (the plan re-pinned 12 m → 24 m because 12 m is 54–83 ms and structurally cannot pass) · **full 3 m COLD solve p95 591–616 ms standalone** · **mirror 21 writes / 241 frames = 1 per 11.5** · the published pack's ArrayBuffer AND the `topK` array identity survive a no-op scrub (`planFeed.binsMirror` rule) · the T1 key moves for a sources change and stands still for a lift or a day. **TIMING PINS CARRY THE BUDGET ON THE MEDIAN AND PRINT THE p95, deliberately**: inside 12-way parallel vitest the identical code reports 25–32 ms and ~1,050 ms, i.e. the tail measures the RUNNER; raising the budget to fit it would make the number meaningless, so the median carries the plan's figure, the p95 is logged every run and a hard 2× ceiling catches a non-subtle regression. **S7 — THE BUILT-DENSITY PRIOR, and it is an EVIDENCE gate, never a score penalty.** `parseTile` does `if (!layer) continue`, so “tile fetched, zero buildings” is byte-identical to “OSM never surveyed here”; reproduced here at **S(centre) 0.6612 vs §3.2's measured 0.661**, coverage 1.000, unmapped 0.000, **frac S>0.6 = 1.000** — warm, uniform, confident. `SolveInput.builtEvidence:false` stops crediting an **OPEN-SKY** ray as evidence (that flag means “nothing stands between you and the horizon”, which with no survey behind it is not a measurement): `C` falls and the cell becomes the UNKNOWN RENDER CLASS through machinery that already exists — **never a low score, never a cold colour**. ONE rule, three verdicts: Dnipro (prior off) · flat rural → frac S>0.6 **0.0000**, 76,479 cell-azimuth visits withheld · **Everest-shaped terrain-only + real relief → 0 withheld, coverage 1.000, still scored** (its rays are set by measured ground, not by an open-sky claim). THRESHOLD `BESTSPOT.builtDensityFloorPerKm2 = 1`, derived as the GEOMETRIC MEAN of §3.2's two measured populations — √(26.6 × 0.048) = 1.13, rounded down; measured on the committed hero z14 tile at **7.98/km² (21 buildings) against 0.00/km² for the same tile with only the building layer removed**. §3.2's OTHER number (0.470 over `unknown` landcover) is now UNREACHABLE — S3c's no-landcover refusal catches an all-`unknown` grid first — so the live failure mode was the 0.661 one. Panel renders `⚠ RURAL — TERRAIN ONLY, NO SURVEYED BUILDINGS HERE` with the measured density AND the floor beside it (S5 left it unrendered because no published threshold existed). **`refuseBelowReachM` TURNED ON at `BESTSPOT.refuseBelowReachM = 400` = the collar**, the measured safe ceiling (200/380/400 m → **0** refused · 420 m → 175 · 500 m → 3,027, because a rim cell's up-sun reach is bounded by the collar). The calibration was taken at 3 m and `reachM` is quantised by the cell pitch, so S7 re-measured it at **every rung: 24 m→0 12 m→0 6 m→0 3 m→0**; on the §3.1 truncation fixture the centre goes **0.5522 → 0.0000**, unmapped 0.000 → 0.726. **R8 SPLIT IN TWO, as ruled.** 1 m ACCESSIBILITY on EVERY solve: one resident 601² LandGrid over the SCORED disc (no collar — landcover is a per-cell property), built once per `sourcesKey`, re-judging the top `shortlistCandidates = 256` before the NMS — a candidate whose 1 m cell is inaccessible is DROPPED and the soft rung is re-read, measured **0.7 ms grid + 6.5 ms re-rank** against §8 Q3's +52–59 ms budget. 1 m OBSTRUCTION stays behind `REFINE THIS SPOT` through the solver's `mode:"stream"` path (~1.0–1.6 s, the ONE justified spinner); the feed's TIN flatten radius grew by `REFINE_RADIUS_M = topKMinSepM + 15` because a row 300 m out needs geometry to `radius + refine + collar`, and flattening short would have made the refine report `unknown` for a well-surveyed cell. **Done-check met: rows publish `gridCellM === 1` while the field publishes 3**, and the panel names the two apart — `OBSTRUCTION AT 3 m · SHORTLIST ACCESSIBILITY AT 1 m` — because an unqualified “1 m heatmap” is a **C2 violation**. That line now reads `gridCellM` (the rung that LANDED) and not `cellM` (the tier REQUESTED), which is a STRENGTHENED pin: arming ULTRA no longer makes the panel claim 1 m before the 1 m rung exists. **PROVENANCE BADGES are published, not asserted**: `heightProvenance {enriched, osm}` is counted at `flattenTin` — the one place the two tilesets are still distinguishable — and rides the job, so the panel prints `BUILDING HEIGHTS: n SURVEYED + m OSM-DERIVED (~78% DEFAULTS) · TREES ARE MODELLED, NOT SURVEYED` (§8: 99,590 of 127,890 OSM heights are class defaults; 151,046 of Dnipro's 161,823 canopies are seeded scatter, 628 surveyed). §8's ~1–2 cell landcover RIBBON is now a printed line too. **NON-NEGOTIABLES RE-CHECKED and pinned in `bestSpotHonesty.test.ts`:** `graze.conf.tree` still clamps at 0.6 (raisable to 1 in a patch → sanitized back) · water/building/blocked stay `hard = 0` and a hostile patch carrying `groundHard` is DROPPED by `sanitizeScoringPatch` · ULTRA still throws above 300 m in the SOLVER and not only in the store, including through a hand-built `DiscGeometry` · C6 — a solved row is a grid key + lat/lon with no id, no timestamp and no persistable handle (asserted key-for-key). **DEV SEAM widened** rather than reaching into closures: `__globe.bestSpot()` now publishes `hullBuilds` / `hullBuildEvents` / `frames` / `mirrorWrites` / `keys.sources` and an `honesty {terrainOnly, builtDensityPerKm2, openSkyUncredited, refusedShortReach, shortlistCellM}` block, and `__globe.bestSpotField()` hands out the PUBLISHED pack so a verify script can compute the done-check's own “fraction above 0.6” off the texture the sheet actually samples instead of re-deriving the physics. `scripts/verify-bestspot.mjs` EXTENDED (not forked): an S6 section driving the real 2→400 m slider with **FRAME_PROBE across the drag** (the facility is NOT shared in this repo — `grep -rn FRAME_PROBE` finds exactly one local const in `verify-ultra.mjs`, so it is a documented second copy, the scripts having no shared module but `verify-cdp-cleanup.mjs`), a radius-change positive control, the Dnipro/rural provenance + `terrainOnly` + refine-spinner rows, the rural `S>0.6` fraction read off the pack, and a THIRD SITE (Everest) so the three-verdict done-check is checkable in a browser. **Gates: vitest 1,887/1,887 (130 files, +27 tests / +2 files over the 1,860/128 baseline, zero regressions) · `astro check` 0 err / 0 warn / 5 pre-existing hints · `npx knip` exit-0.** **Verification tier: LOCAL ONLY — nothing in S6 or S7 has been in a browser this session (the owner is driving that half); `verify-bestspot.mjs` was extended but NOT run.** **Two existing assertions were STRENGTHENED, none weakened:** the obstruction status line now pins the landed rung rather than the request, and the panel's status block gained the ribbon + provenance lines. **OPEN / UNVERIFIED:** the built-density prior is a whole-disc verdict, so a disc whose 1–4 overlapping z14 tiles happen to be mostly river could in principle read below the floor in a surveyed city (the hero tile has 8× of headroom, but this is unmeasured on a river-dominated disc) · with the prior ON a genuinely FLAT rural disc goes 100 % UNMAPPED, which is the honest answer for a site with no survey and no relief but has never been looked at on screen · the refine patch sits exactly at the widened flatten radius, so a rim cell of the 1 m re-solve could still report `unknown` if the terrain TIN is short there · the 1 m grid cost (0.7 ms) was measured on the committed single TRIMMED tile, not on four full ones.

- **2026-08-23 · BEST SPOT — the observability heatmap: design LOCKED and the whole pure-lib floor SHIPPED (S1+S2), with two BLOCKERs caught at slice SEAMS that every per-slice suite passed.** Owner order: park the eclipse taste pass / T46 umbra / small fixes; one new planning feature — predict the best places to stand for SUNRISE/SUNSET/MOONRISE/MOONSET inside a radius of the `look from here` pin, as a translucent hi-res heatmap over the map. Plan doc **`.claude/claude-docs/BESTSPOT_PLAN.md`** (662 lines, read its **AS BUILT** appendix before touching this track); log `mem:project/wip-2026-08-23-bestspot-heatmap`. Design by a 13-agent pass (8 research tracks → architect → 3 adversarial lenses → consolidator), then a 7-agent build and a 3-agent fix pass. **THE ADVERSARIAL PASS OVERTURNED THE ARCHITECT'S OWN RECOMMENDATION.** **F1 — the naive generalization is 8–35 HOURS, measured:** one `planFeed` profile is 2,520 raycasts and `ground.heightAt` is a full `Raycaster.intersectObjects(tiles.group.children, true)` at 18–58 µs/ray that bypasses 3d-tiles-renderer's own BVH traversal, plus a mesh sweep whose subdivision is a function of the EYE's closest approach — i.e. structurally non-reusable across eyes; × 282,743 cells. **THE ARCHITECTURE: all-CPU, one long-lived module worker, and THE LOAD-BEARING INVARIANT is that the per-ray UPPER CONVEX HULL of the along-ray surface is independent of BOTH eye height AND scene time** — so the owner's two live controls (time scrub, altitude slider) pay only a cheap max-angle query. Measured M3 Pro at 601²/1 m/K=24: hull build 243 ms · query 108 ms · score 100 ms (2.24:1 — every rejected option re-ran 351 ms when only 108 ms changed). **NO (H,D) SLAB IS EVER MATERIALISED** (1 m × K=42 × 6 B = **139 MB**); the fused form is 1.9 MB at 3 m. **THE GPU PATH WAS REFUTED on three verified breakers**: its "361,201 fragments" is the CELL count, so it needs an offscreen RT (the first in `src/` outside `GlobeCanvas.tsx:279`), a 2nd `render()`, the `shadowMap.autoUpdate` bracket (already a pinned fence) and `readRenderTargetPixelsAsync` (**throws on any non-RGBA target**) = four mechanisms; the in-material reading is 5.94 M fragments EVERY frame, twice on `/m`; and the headroom is **−1.1 ms under ULTRA** (36.1 vs budget 35) where the governor's results are DISCARDED, so there is no backstop. Reusing the shipped directional shadow map: 7 separate breakers (one shadow light, latched `mapSize`, a 2nd `DirectionalLight` forces a full material recompile and darkens every ground `ShadowMaterial` twin, terrain does not cast outside ULTRA, and the rig itself refuses below 0.46° — the hero cases are 0–2°). **OWNER RULINGS 2026-08-24: R1** DRONE semantics above **5 m** (below it the ground mask gates; above it only solid INTERIORS), with a `groundReachable` bit carried alongside because the owner's stated preference was "a place I can climb to" · **R2** FPV is a CENTRE SOURCE but renders NOTHING in the viewfinder (§9 records the exact three changes to un-park it) · **R3** field at **3 m**, **1 m reserved for ULTRA**, shortlist always re-solved at 1 m · **R4** GL overlay only, radial rim falloff, and **the hollow cylinder is REPLACED by a centre PLUMB LINE** + billboarded altitude chip + ground tick. **THE METRIC** (§3): `S = A_hard · A_soft^0.5 · M · G(V) · [0.15V + 0.30L + 0.25P + 0.30F]`, gates multiply and preferences sum, `G(V)` SOFT because the hero case intentionally occults the disc, coverage < 0.5 ⇒ **UNKNOWN, no ink** (never a cold colour). Atom = the disc's visible AREA fraction integrated over azimuth COLUMNS; the track is reparameterised by AZIMUTH (time-sampling aliased `F_sil` 0.51↔1.00 on identical geometry). **SHIPPED: `lib/geo/{bestSpotTypes,bestSpotTrack,bestSpotMetric,localDsm,horizonSweep,landcoverRaster}.ts` + 6 test files ≈ 9,500 lines, plus an ADDITIVE widening of `scene/vectorTiles.ts`** (bridge-deck/pier/plaza POLYGONS — `if (f.type !== 2) continue` was throwing away a **29,054 m² deck** at 48.47831,35.05757, the owner's hero location — plus `subclass`/`surface`/`access`/`foot`/`layer` on roads, the `landuse` layer, `render_height`/`render_min_height`, a `brunnel` string, and `parseTile` hoisted out of the orchestrator closure to an exported pure `parseVectorTile` because the widening was otherwise observable only through a live network fetch). **THE HEADLINE: BOTH BLOCKERS WERE AT SLICE SEAMS AND EVERY SLICE PASSED ITS OWN TESTS.** (1) **The bridge was scored as a PURE LIABILITY** — `RayEvidence.src` was bound to the GROUND setter alone while `localDsm` deliberately keeps floating solids out of `surfaceTop`/`surfaceSrc`, so a genuine deck published `src:"terrain"` and `silTangency`'s `if (!isBuiltSrc(src)) return 0` made the tangency kernel §3.4 wrote FOR the bridge structurally unreachable: measured **0.60799 WITH the deck vs 0.62306 with no bridge at all**. The metric's own PIN 3 passed only because its fixture hand-wrote `src:"deck"` on a ray the producer cannot emit. Fixed by carrying `groundSrc`/`groundDistM` AND `bandSrc`/`bandDistM` and redefining `src` as the NEARER channel → composed on the real ephemeris **withDeck 0.88853 vs bare 0.69606**; the mirror-image bug (gating the ground edge on the headline `src`, which would score the WATER UNDER a bridge as a built silhouette) is separately guarded. (2) **47 % of the track's weight sat BELOW the horizon**, where `f = 0` is guaranteed — the ±3° notch shoulders were swept for `F_notch`/`C` and then integrated into `V`, and the weight `exp(−max(0,alt)/2.5°)` does not decay below 0. Over a PERFECT open horizon at 1.7 m: **V = 0.5138, S = 0.41** — below §6's own ~50 % legibility floor, i.e. **the best possible pedestrian cell rendered invisible**; only a 2 km sheet saturated the gate. Fixed with `windowLo`/`windowHi` markers AND a HORIZON CEILING on the weight anchored at the eye's own dip (never at 0 — which is what finally makes the lower window extension pay for a lifted sheet): **weight-below-dip 0.46879 → 0.00750, V 0.51376 → 0.96849, S → 0.69527**, while a 15° courtyard skyline still reads `v = 0`. **THREE MORE REAL BUGS**, all "two conventions that look alike": `accessAt` compared an ABOVE-GROUND height against ABSOLUTE `solidBase`/`solidTop`, so **R1's only aerial gate was silently off and a drone was free inside every building** (fixed by deleting the second site — the envelope test now exists only in `localDsm.insideSolidInterior` and TypeScript rejects the old call) · the landcover raster was in a DIFFERENT FRAME from the DSM (equirectangular 111_320 vs the true 111_199 m/°lat ⇒ **0.937 m at 500 m, 1.875 m at 1 km**, misregistering the mask by 1–2 cells at the rim in a feature advertising 1 m; `localDsm` now owns one ENU frame, worst error **1.2e-5 m**) · **23 of the 30 canonical (radius, cellM) specs produced an EVEN grid**, putting the disc centre on a cell CORNER while `discGridSpec` forces ODD (one shared `oddSpanCells` now serves both). Longitude wrap solved BY CONSTRUCTION (cos/sin only). **F4 — CURVATURE MUST BE FOLDED IN BEFORE THE HULL**: the drop term is convex, so `Z − drop` is `Z` plus a CONCAVE function, which ADDS hull vertices — a sample interior to `hull(raw Z)` can be the true setter (verified: `d=[1,2,3], Z=[0,0.9,2]` from an eye at −1 gives −2.86° vs −26.57°). **F2 — mixing the APPARENT skyline with `bodies.horizontal()`'s AIRLESS altitude is a 0.6243° = 2.37-SOLAR-RADII error** in a feature that lives inside a 0–2° band; converted once at the track via astronomy-engine's exported `Refraction("normal", alt)` (verified at `astronomy.js:6361`, agrees with `Horizon(...,"normal")` to 1e-12), and the two refractions still never mix. **THE PLAN WAS WRONG IN FIFTEEN PLACES and every one is now recorded in its AS BUILT appendix** — the notch floor read the WALL not the gap (`Hg(az*)` samples the flank BY CONSTRUCTION: depth 0, `F_notch` ≡ 0 in `cellScore` while the standalone kernel returned 0.358; corrected to `min Hg` over the disc's ±ρ footprint) · S1 pin 2 was arithmetically impossible (a 1.5° canyon caps at 0.339; break-even 1.264°) · pin 5's falsification mechanism was backwards (a 0-anchor makes `L` HIGHER; the real damage is loss of DISCRIMINATION — a 2000 m sheet collapses the whole [−1.339°,0] band to `L=1`) · the `3ρ` lower window is too short for a 2000 m sheet (dip = 5.08ρ) · `az*` assumed a northern-hemisphere SET · the 0.1° salience floor is DEAD at solar/lunar ρ · azimuth WRAPS (Tromsø 359.97°→404.60°) and DECREASES in the southern hemisphere · `new Date(ms)` TRUNCATES to whole ms, which silently dropped the outermost shoulder step so the ±3° `F_notch` window quietly became ±2.5° · and **`eventTrack` returned null across the TROPICS, not at the poles** — lat 0 is physics (the setting azimuth is stationary at the horizon) but 2–14° was an avoidable bug where the shoulder march guarded ALTITUDE monotonicity while its progress predicate is an AZIMUTH one (lat 2/5/10/12/14 all recovered). Also measured-and-corrected: **"the deck IS the standable strip over the river" is FALSE** — ZERO of 11,703 deck cells were `water` in a no-deck rebuild, because the carriageway LINES already lift them out under §4's own paint order; what the deck buys is the VERDICT (`A_soft` 0.15 → 1.0), not the hard gate. **Gates: vitest 1,560/1,560 (119 files, +187 from the 1,373/113 baseline, ZERO regressions in old files) · `astro check` 0 err / 0 warn / 5 pre-existing hints · `npx knip` exit-0** — all three re-run and confirmed by the orchestrator, not taken on report. **Verification tier: LOCAL ONLY (vitest + astro check + knip). Nothing here has been in a browser; S3–S7 are unbuilt, so there is no rendered artefact to verify.** Every fix is MUTATION-VERIFIED (revert → red → restore byte-identical): rebinding the headline to the ground channel reds 4, dropping the band-edge walk reds 6, the bare exponential weight reds 4, the wrong datum reds 7, the even grid count reds 26 — and the stacked pre-fix state reconstructs the reviewer's own `V = 0.5137554515047305` exactly, which is the evidence that no threshold was relaxed. **NEW `test/lib/geo/bestSpotComposition.test.ts`** drives the whole chain on the hero case because **a per-slice suite cannot catch a seam**. **Process lesson: the RED tests were UNTRACKED when the fix agents ran, so `git diff` could not prove the assertions were not weakened — commit red tests before handing them to a fix pass.** **OPEN, carried forward (AS BUILT §"Open"):** `F_sil` SATURATES TRIVIALLY (measured 0.846 for BOTH the deck and the wall, so `F ≈ P` in a city and the 0.30-weighted framing term carries almost no ranking signal — an owner-visible decision for S7) · **R1's aerial gate is structurally blind to FLOATING solids** (a drone parked at 12 m inside the Central Bridge deck reads as free air — same bug class as the two blockers, on the same object; pinned with a positive control, NOT fixed, because the producer is S3's feed) · `LandClass` has no AERIAL member so R4's distinct class cannot be expressed (S4 decision) · `rail → blocked` and `intermittent → wetland` are inferences needing an owner ruling · multi-tile MVT seam assembly is untested · **§5's T0 ≈ 105 ms / T1 ≈ 90 ms budget is still a PROJECTION** (the 17 ms `buildLandGrid` measurement is on a 21-building fixture tile, not dense Dnipro, and nothing was measured inside a worker) · worker import is verified by a three-free source fence and a Node ESM import, not by loading in a real Web Worker, and `planElevationsM` drags comet/targets/showers into the chunk. **NEXT: S3 (worker + `BESTSPOT` tuning + store bridge + `stepBestSpotFeed` in the FEEDS-LAST band after `stepPlanFeed` + the `__globe.bestSpot()` DEV seam + `fences.test.ts` sanction) → S4 GL sheet → S5 desktop panel → S6 residency → S7 honesty.** **SEPARATE PRE-EXISTING DEFECT found and NOT fixed (out of scope): the FPV mini-map draws culverted drains as visible watercourses** (`minimapFeed` skips `line.tunnel`, and the waterway kind hard-codes `tunnel:false`).

- **2026-08-22k · ECLIPSES — solar occlusion + corona + world darkness, lunar umbra reddening, and a PREDICTED ECLIPSES section on both shells.** Owner repro `#f=42.354484,-3.698240,17.0,283.5,7.2,8.3&t=1786559722469` = the real 2026-08-12 Spanish totality from Burgos, 6 min past peak: "sun and moon got aligned but sun totally dominated moon." **ROOT CAUSE, measured, worse than `sky.ts`'s own "cosmetic edge" comment claimed: the moon mesh was not washed out, it was DISCARDED.** By day `uDaySky→1` so the disc runs its additive arm at alpha 0, and during a solar eclipse the near side is a NEW MOON (illuminated fraction 8e-5) so `lit≈0` — both under `SKY.moonAlphaDiscard`. No render-order/depth/blend change on the moon could occlude anything that is never drawn; **the sun has to carve itself.** **THE LOAD-BEARING FACT: the geometry must be TOPOCENTRIC.** At the repro instant the GEOCENTRIC sun/moon separation is 1.006° against radii 0.263°/0.272° (the discs do not touch); topocentric is 0.062° = 88% covered. Lunar parallax (~1°) is the same order as the whole phenomenon, so `bodyStatesAt().moonDir` reports NO ECLIPSE during totality. The scene was already right by accident — `sky.ts` derives the moon direction as `moonPos − camera.position` — and the rendered separation (geocentric sun × topocentric moon) sits 2.6″ from truth, 0.28% of a solar radius. **NEW `lib/ephemeris/eclipse.ts`** (pure, three-free): `discCoverage` circle-circle lens area · `solarEclipseFromDiscs`/`solarEclipseAt` · `lunarEclipseFromState`/`lunarEclipseAt` (Meeus ch. 54 shadow cone, `SHADOW_ENLARGEMENT` 1.02) · `nextSolarEclipses`/`nextLunarEclipses` · `eclipseDaylightK`. **Validated against the almanac**: umbral coverage matches astronomy-engine's own obscuration to <0.003 across the 2025-28 lunar series, and the modelled umbral magnitude crosses 1.0 at 06:26Z/07:31Z for 2025-03-14 — the published totality contacts to the minute. **RENDER**: sun fragment carves the moon analytically in the billboard's own plane (`uMoonOff`/`uMoonR` in sun-disc radii — exact, and precise where `acos(dot)` of two near-parallel world rays is mush) + halo falloff + corona (two-term, gated `coverage 0.985→1` so it stays out of every annular eclipse) + chromosphere hairline; moon vertex gains `vDisc = position.yz` so the Earth's umbra draws its real CURVED edge across the disc with an edge-brightening gradient. `moonMesh.renderOrder` 11→**12** (an equal renderOrder falls through to a depth sort and both impostors sit at the SAME anchor distance — the winner flipped with camera aim). **WORLD DARKNESS**: ONE `eclipseK` scalar from `stepEclipse()` (new, between the ephemeris resample and the ULTRA look — it CANNOT ride `sampleEphemeris`'s 1 Hz cadence because the geometry is camera-relative, and it must precede the key-light step) → `sunLight.intensity` · `ground.setShadowStrength` · ground `uFtwEclipse` (ALTITUDE-GATED: being inside the umbra is a street-level truth; from orbit it is a ~100 km spot, so `baseEarth` is deliberately NOT wired) · atmosphere `uEclipse` (inside its low-altitude branch only) · `stars` reveal (folded in BEFORE the `fade > 0.01` hard return, or it is unreachable) · moon `uDaySky`. Lunar dimming multiplies **`moonKs` at the sample** — the ONE write that reaches uMoonGlow, uFtwMoonGlow, the moon-shadow key, ground shadow strength and `moonLight` together. **PANELS**: `EclipseFacts` on `TargetPanel` + a `/m` twin in `TargetSheet`, mounted AFTER unfollow so verify-uxbatch4's FIND-IN-FRAME/UNFOLLOW adjacency stays green; gated on **`target.kind`, never `facts.kind`** (the sun and moon both carry `facts.kind === "planet"`); rows carry the YEAR (the next 5 solar eclipses at one site span a decade) and a dash, not a false 0%, for penumbral. **BROWSER-CAUGHT BUG (invisible to vitest AND astro check): TDZ.** The eclipse state was first declared with the other look state ~1,300 lines below `sampleEphemeris`, which runs at module init — `ReferenceError: Cannot access 'lunarEcl' before initialization`, swallowed by the dynamic import's own `.catch()` as "[globe] tiles disabled", and the ENTIRE real-Earth globe silently fell back to the procedural placeholder. Second time this exact trap has bitten in this file (see the `ultraOn` note). New tokens `eclipseUmbra`/`eclipseChromo` (tokens.css + GL bridge, D14 — no hex literals in tuning.ts). Gates: **vitest 1,373/1,373 (113 files, +43)** · `astro check` 0 err / 5 hints · `npx knip` exit-0 · **NEW `scripts/verify-eclipse.mjs` 37/37 ALL PASS** (shots `eclipse-01-owner-hash-88pct` · `-02-totality-corona` · `-03-partial-crescent` · `-04-off-state-noop` · `-05-lunar-total-copper` · `-06-lunar-partial-bite` · `eclipse-panel-{desktop,mobile}-{sun,moon}`) · `verify-ultra` 28/28 + `verify-uxbatch4` ALL PASS re-run. **Verification tier: BROWSER-VERIFIED** (CDP, both shells, pictures looked at). OFF-STATE proven an EXACT no-op: `eclipseK`, the sky and ground uniforms read exactly 1 and the sun's `uEclipse` exactly 0 an hour before first contact. **Open taste knobs for the owner** (`ECLIPSE` block in tuning.ts): `umbraLight` 0.055 (the eclipsed moon's brightness — the honest physical number is ~1e-4 of full, which would be invisible; this is eye adaptation applied once, in one named place) · `daylightFloor` 0.04 · `coronaGain` 1.15 · `shadowSoftFrac` 0.09 (the umbra edge may want to be softer) · the earthshine-lit silhouette reads mid-grey rather than black at partial phases. **NOT DONE:** the true LEO umbral SPOT (the ~100 km shadow drawn on the globe from orbit) — `baseEarth` already holds `uMoonDir`, so it is a contained follow-up. **DOCS + ONE FENCE REPAIRED IN THE SAME SHIP** (26-gap adversarial doc audit, 3 findings refuted): ARCHITECTURE §4 gains the eclipse bullet + §7 the module and the panel responsibility · IMPLEMENTATION_PLAN P9 ticked SHIPPED with the box left OPEN on P8 alone, and the 8-tools flagship rung NARROWED (the local eclipse shipped, the globe-scale umbra did not) · `conventions/contracts.md` §3 gains the `__globe.eclipse()` sub-seam · `conventions/globe-tuning.md` gains the `ECLIPSE` family table AND its uniform-count invariant restated COUNT-FREE (the literal "16 vs 16" re-staled the moment `uFtwEclipse` landed) · `conventions/verify.md` gains the eased-scalar and 0.5°-disc traps · DECISIONS §Traps gains the TDZ rule · README's two dated head baselines corrected (1,144/102 → 1,373/113) · `architecture-and-patterns.md` repo layout · guide topic `target-card` now documents PREDICTED ECLIPSES (the module's own "every claim must match the LIVE UI" contract) · backlog **T46** (LEO umbral spot) + **T47** (eclipse taste pass) opened. **AND the verify harness's own C11 fence was found VACUOUS on this ship**: `verifyHarness.test.ts`'s comment-stripper ran `//[^\n]*`, which ate everything after `http://` — so `/json/new` vanished and the "every script that opens a target registers it for cleanup" check never fired for any script inlining its CDP URL. Measured: 22 of 24 scripts open a target, 21 were fenced, and the ONE blind spot was the newest script — i.e. the fence was decaying exactly as new scripts were added, making it an instance of the very "checks that could not fail" class it was written to kill. Fixed with a `(?<![:\\])` lookbehind (0 blind of 22, real comments still stripped), plus a zero-result-validation positive control and a NEW test fencing the third half of verify.md's class-(a) claim (every opener EXITS through `finishVerify`).

- **2026-08-22j · THE ULTRA FIDELITY TRACK SHIPPED — T44 (textures) + T45 (light + shadows), nine levers behind ONE chip.** Gates: **vitest 1,330/1,330 (111 files, +25)** · `astro check` 0 err / 5 hints · `npx knip` exit-0 · **NEW `scripts/verify-ultra.mjs` 28/28 ALL PASS** (shots `ultra-{off,on}-city-{day,golden,sunset,civil,nautical,night}` + `ultra-{off,on}-everest-{day,lowsun,golden}`). Executed against `ULTRA_PLAN.md`; the owner's plan question ("re-instate an HQ chip, or fold both halves under ULT?") resolved as **fold under `ULT`** — their own words treat it as one mode. **THE RULING THAT UNBLOCKED IT (owner, verbatim): *"regarding low fps ceiling in manual ULTRA mode, even if it is sub 15FPS but graphics fidelity improves and gives nicer richer picture — worth it, user enables it in it's own volition anyways."* That SUPERSEDES `ULTRA_PLAN.md` §2's "a 12 fps ULTRA is a broken feature"** and is what made the construction-time shadow levers shippable at all. Measured, dev build, 1600×950 @ DPR 2: city OFF 30.7 ms → **city ON 36.1 ms (33 → 28 fps, +18% frame time)**, Everest ON 29.3 ms — far cheaper than feared; frame time is now REPORTED, never a veto. **SHIPPED, in the plan's own order (light before shadows, per the owner's transition steer): §1a** the photographic de-grade reaches 3D on its OWN uniform `uFtwPhoto3d` (`max(uFtwFlat2d*uFtwPhotoK, uFtwPhoto3d)`; `dayK` is deliberately NOT forced — that half stays a C2 breach) · **§1b** anisotropy 16 on the drape composites · **S9** the day factor now comes from a TWILIGHT-BAND curve anchored on the almanac's own thresholds (+6/−4 golden, −6 civil, −12 nautical, −18 astro — asserted against `lib/ephemeris/twilight.ts` in a test), spanning **36° of solar elevation instead of `EARTH.termBand`'s 9.2°** (stated in DEGREES because the wall-clock duration is a function of latitude and date — at the Dnipro verify pose, 48.5°N, it works out at ≈4.4 h against ≈62 min), so day→dusk→night acquires STRUCTURE instead of being one blend · **S11** `toneMappingExposure` ramps 1.00 → 1.46 across the sweep (eased, τ 950 ms) · **S4** AERIAL PERSPECTIVE on ground + buildings + edges from ONE shared `FTW_AERIAL_GLSL` (extinction + Mie forward scattering; measured band tints `#d8e6f2 → #fdba70 → #4c93d4 → #366d9f → #0c1822`) · **S10** the HemisphereLight finally sits on LOCAL UP and tracks the ephemeris (**audit gap #16 killed** — it had always pointed along ECEF +Y, i.e. correct on exactly one meridian) · **S2** soft shadows · **S5** an 8192² map · **S3 TERRAIN CASTS SHADOWS** (the owner's named killer feature — browser-proven at Everest, the Khumbu in shadow under sunlit peaks). NEW: `lib/globe/lightBands.ts` (pure, band curves + the EMITTED GLSL twin) · `lib/globe/ultraBoot.ts` (the boot gate) · `tuning.ULTRA` · DEV seam `__globe.ultraLook()`. **FIVE FINDINGS THAT CHANGED THE PLAN, all source-verified against `node_modules`: (1) `PCFSoftShadowMap` IS DEAD CODE in three 0.185** — `WebGLShadowMap.js:99-104` intercepts it, warns "deprecated" and rewrites it to `PCFShadowMap`; there is no `SHADOWMAP_TYPE_PCF_SOFT` define left. **The plan's S2 as written would have shipped a no-op.** The real r185 lever is `shadow.radius` on a 5-tap **Vogel disk rotated per pixel by interleaved gradient noise** — a LIVE uniform, so soft shadows are edge-applied, and a big radius degrades to noise, not banding. **(2) `shadow.bias`'s unit is a FRACTION of the shadow camera's near→far range, not a length** — the base rig's −2e-4 over 7,000 m is −1.4 m, and over ULTRA's ~96 km the same constant would be **−19 m**, detaching every shadow from its caster. The rig now derives `bias = −ULTRA.shadowBiasM/(far−near)` and the tunable is in METRES; **changing `depthMarginM` silently rescales the shipped bias**. **(3) terrain casting fails SILENTLY without `shadowSide = FrontSide`** — `getDepthMaterial` inverts FrontSide→BackSide, which culls a single-sided ground sheet entirely: no error, no warning, no shadows. The verify script counts `frontSideShadow` off the LIVE scene graph because our own flag would have passed. **(4) `mapSize` is LATCHED** — three reallocates the depth target only when the map is null or the type changed, so a runtime `mapSize.set()` is a no-op; hence the boot-read, which is `ULTRA_PLAN` §2's second sanctioned path. **(5) a directional shadow target costs 2× a depth-only reading** — an RGBA8 colour attachment nothing ever samples, plus D24: 4096² ≈ 128 MiB, 8192² ≈ **512 MiB**. `SHADOWS.mapSize`'s "~67 MB" comment was wrong by 2× and is corrected in place. **REJECTED WITH REASONS: CSM** (present, but `setupMaterial()` ASSIGNS `onBeforeCompile` and would clobber the buildings' 12-uniform injection and the ground's explicitly-CHAINED one; `ShadowMaterial` has no cascade dispatch — `getShadowMask()` multiplies ALL cascades; 3 extra lights ⇒ full recompile + 3 depth passes; zero reach into the ~19 raw ShaderMaterials. **The altitude-adaptive ortho already does a cascade's job for free**: street level clamps to 1.6 km ⇒ 0.39 m/texel at 8192², a mountain view spends the same texels on 11 km of relief) · **PCSS** (not shipped in the npm package at all; a hand port needs a raw depth read but the PCF sampler is a `sampler2DShadow` with hardware compare) · **VSM** (makes every `receiveShadow` mesh an implicit caster — i.e. all the ground twins) · **full mip chains** (each composite is an independent ClampToEdge canvas cleared TRANSPARENT; a chain bleeds that border inward and clamps into a tile-seam grid — and `minFilter` is ALREADY `LinearMipmapLinearFilter`, the gate three needs, so anisotropy alone is the legal win) · **GI stays rejected, owner-accepted, and the timelapse says its condition was met.** **THE OFF STATE IS EXACT, browser-proven, not asymptotic**: with the chip off `photo3d/dayMix/haze` are literally 0, exposure 1, `hemiPos [0,1,0]`, `radius` 2, `normalBias` 0.75, terrain casting 0, composite anisotropy 1 — the eased terms SNAP to zero under an epsilon and `stepUltraLook` then early-returns. **THE MOBILE FENCE HOLDS UNDER THE REAL LEAK CONDITION**: with `ultraQuality: true` written into the shared `ftw:view-prefs:v1` blob, `/m` reports `{allowed:false, coarsePointer:true, pref:true, on:false}` with every lever at baseline and `shadowMapPx 1024`. `fences.test.ts` gained `lib/globe/ultraBoot.ts` as a fifth owner plus TWO new rules (its flag read must name `ultraShellAllowed` on the same line; the boot gate and the engine gate must test the same two terms). **TRAPS: backticks inside a GLSL template literal terminate it** (writing `` `uFtwFlat2d` `` in an injected-shader comment produced 17 phantom TS parse errors — prose only in there) · **the Vite dep-cache trap bit exactly as documented** (two new globe-bundle imports ⇒ EVERY module 504'd across all ~24 islands; restarting `wix dev` was NOT enough, `node_modules/.vite` had to be moved aside) · **a passing check over a worthless picture** — the first Everest pose put the camera UNDER the terrain (alt 6400 over 6.6 km ground), the street-floor guard rescued it and reset tilt to 20°, and every assertion passed while the shot showed a wall of snow; the pose check is now DERIVED from the live shadow extent so it is falsifiable. **OPEN (owner taste, not defects):** the `dayCurve` civil anchor 0.30 keeps the ground notably bright at civil twilight (physically intended — a real civil twilight is navigable; first knob if it reads too lit) · the sky DOME was not touched, so at golden hour a warm ground haze meets the old blue-grey dome above the horizon (a mild seam; fixing it means reaching into `scene/atmosphere.ts`, outside this slice) · the capped mip chain · `ULTRA.shadowMapSize` is the rollback knob if 512 MiB proves too much. Log: `mem:project/wip-2026-08-22-ultra-track`.

- **2026-08-22i · OWNER 2-ITEM ADDENDUM: FPV ceiling 400 → 2000 m · ULTRA HQ SHIPPED · HQ 3D MAP BUILT, MEASURED INERT, AND REMOVED.** Gates: **vitest 1,305/1,305 (110 files)** · `astro check` 0 err / 5 hints · `npx knip` exit-0 · both items browser-verified on the owner's CDP Chrome. **(1) FPV ALTITUDE CEILING 400 → 2,000 m** (`FPV.tempEyeMaxM`). ONE constant, five clamp sites (two encoder branches, two Space-lift branches, the `#f=` share parser at `StylizedTiles.ts:2326`), so a shared FPV link stays inside it by construction. No rate change needed — the encoders step by `rate · dt · max(h, vertEncoderBaseM)`, i.e. exponentially, so at `spaceLiftRatePerS 1.1` the climb reaches 2,000 m in ≈5.0 s against ≈3.6 s for 400 m; **browser-measured: held at exactly 2000.0**. The one real consequence is view, not control: the geometric horizon moves 71 → 160 km, so a maxed eye pulls a visibly larger working set — the same distance-SSE far field T43 measures, reached further into, not a new failure mode. **(2) ULTRA HQ SHIPPED** — desktop-only, off by default, `ULT` chip. **NOT a fourth tier**: `QualityTier` / `TIER_ORDER` / `detectDeviceTier` / `makeGovernor` / `QUALITY.tiers` are all untouched (a fourth rung is both a type error at `queueCapsForTier` and a `toEqual` failure on the TIER_ORDER lock). It is `QUALITY.ultraDesktop`, an override PROFILE layered on the running tier — the structural mirror of `leanMobile`. Two halves: the TILE levers land immediately on the chip edge via `stepUltraGate` re-running `applyQualityTier(activeQualityTier)` (waiting for the governor would never fire — on a `high` machine it is a documented no-op), and the TIER PIN rides GlobeCanvas's existing FPV-deferred `pendingTier`. **`governor.force()` alone is not enough** — it only moves the index and resets hysteresis, so on a machine that actually hurts the next over-budget streak walks the tier straight back down; the governor therefore keeps STEPPING (its EMA stays honest) and only its results are dropped, with an EXPLICIT re-seat to `deviceTier` on the OFF edge. **Browser-measured round trip: OFF `SSE 16 / LRU 410 / floor 307` → ON `SSE 12 / LRU 600 / floor 450` → OFF byte-identical to the first reading, `__overlayRebuilds` 0 across every flip.** The LRU raise is the one lever tier `high` can never pull (its `null` means "restore the library default"), and the floor travels with the cap (U2/A9 — a max-only raise is the parse → cache-full → discard → re-download loop). EXCLUDED with reasons, pinned by test: `dprCap` (inert behind `min(devicePixelRatio, …)`) · `overlayResolutionPx` (one-way ratchet ⇒ un-undoable + the QA-7b storm) · shadows/AO/8k (construction-time; a live flip recompiles every material) · `foveation` (a cfg on `high` SOFTENS the periphery) · queue caps (hitches, not quality). **(3) HQ 3D MAP WAS BUILT IN FULL, THEN MEASURED AND REMOVED — the premise is false on desktop.** It shipped as designed (bounded `SphereRegion` on its own `LoadRegionPlugin`, deliberately NOT widening `mapFlatNow()`, verified attached at the view focus 18 km out at the right radius, `mapFlat` still false). It produced **ZERO extra tiles at 895 m AND at 5,969 m tilted 72°**. The killer control: forcing the GLOBAL `errorTarget` to **0.05** also produced zero — the ground is **availability-capped** (Esri z19, patch L13), and `GROUND.errorNearAlt` is **60 km**, so below that the 3D target is already pinned at its finest (2). **There is no unspent imagery-refinement headroom on desktop, so the 2D chart holds no detail the tilted 3D view lacks.** Independently corroborates T43 §1.1 from the opposite direction: what the owner sees when tilting is **grazing-angle minification** (composites built `generateMipmaps=false`, `anisotropy=1`; `maxAnisotropy` measured **16** available), not resolution. A chip that is measured to do nothing is a decoration, so it was removed rather than shipped — the anisotropy lever that WOULD deliver it is T43 slice S1. **(4) `.ct-vec.is-on` FIXED — a pre-existing bug found in passing**: `ct-vec` appeared nowhere under `src/styles/**`, so the VEC chip has had NO lit state since it shipped 2026-08-21. Second time this by-hand lit group has bitten (PLC was the first); the group now carries a comment saying so. **(5) THE BUG ONLY THE BROWSER COULD FIND**: `let ultraOn` declared beside the shell gate put it in the TDZ for `applyQualityTier`, which is called at ATTACH time hundreds of lines earlier — the whole tileset failed with `ReferenceError: Cannot access 'ultraOn' before initialization`, swallowed by GlobeCanvas's `.catch` as a console.**WARN**, app silently rendering the procedural placeholder. `astro check` cannot see TDZ across a closure. **(6) OWNER ORDER, end of session — the NEXT session is SINGLE-TRACK on ULTRA fidelity.** *"Schedule whatever it takes (T44, anisotropy etc … the visual difference between 2d and tilted is huge, it is less resolution, and very grayish) for the next session to bring that crisp 3d high fidelity textures look in 3d"*, plus a NEW ask: **ULTRA-mode shadows pushed to the limit** — crisper, more physically accurate building shadows, better sunrise/sunset AND moonlight, and **terrain casting shadows** ("amazing looks in high mountain regions"). Explicitly NO ray tracing ("even macbook m3 may die"); research what game/3D engines do that a browser can hold. Same standing rules, restated a third time: desktop only, experimental, opt-in, off by default, zero disruption to prod defaults or mobile. Charter written to **`ULTRA_PLAN.md`**; backlog **T44** (textures) + **T45** (shadows). **The owner's two words split the texture problem for us:** *"very grayish"* is the PHOTOGRAPHIC GRADE (`photo` at `imageryGround.ts:377`) and it is **SEPARABLE** from the `dayK` force at `:372` — drive `photo` from a new uniform and the terminator/golden hour/moonlight all survive, which means the "bright" half this session refused wholesale is deliverable after all (refusing `dayK` was right; bundling the two was not); *"less resolution"* is ANISOTROPY, already proven not to be tile depth. Log: `mem:project/wip-2026-08-22-owner-3slice`.
- **2026-08-22h · OWNER 3-ITEM SLICE: scrubber card RESTORED (a regression, not a decision) · EVEREST true-heights bake SHIPPED · FPV accuracy investigation (read-only).** Gates: **vitest 1,296/1,296 (110 files, +4)** · `astro check` 0 err / 5 hints · both changes browser-verified on the owner's CDP Chrome (shots `scrubber-01/02`, `everest-00/01/02`). **(1) THE SCRUBBER WAS A BUG WITH A NAME AND A DATE.** The owner asked which decision restyled it: none did. Commit `086ff37` (2026-08-21f, owner QA batch item 4 — "the expanded map's bottom line is the REAL TimeScrubber") needed a `body.mw-open .ts` seat carrying a z-index and a lift. Instead of ADDING a rule holding only the delta, it **moved the entire declaration list** out of `.ts` into the state selector — so `background` / `border` / `backdrop-filter` / `border-radius` / `padding` / `width` existed *only while the expanded map was open*. Everywhere else the rail rendered as bare text, unreadable over a bright chart or a snow region, for a day. Restored verbatim to the base rule; `body.mw-open .ts` now carries only `z-index: 43` + the `--mw-credit-h` lift. **Fenced**: `mapWindowChrome.test.ts` asserts the six chrome properties on `.ts` AND their ABSENCE from the state rule — the shape of the mistake, not just its symptom. (`stripComments` is applied first: `decl()` anchors on `^`/`;`/`{`, so a docblock above a declaration hides it.) A/B on one live frame proves it: with the chrome suppressed, "TIME SCRUB", "−4 d 5 h" and every tick label vanish into the ground. **(2) EVEREST GLO-30 PATCH LIVE — the second true-heights bake, terrain-ONLY.** 20 km radius on the summit (86.925278, 27.988056): `cityBbox [86.72, 27.805, 87.13, 28.17]` from proper degree lengths at φ=27.988 (110.819 / 98.376 km per degree — the 11 % axis difference is real and pinned by a test), `extentBbox [86,27,88,29]` = the four 1°×1° COGs it straddles. **13,487 files · 210 MB · L0–13**, rim-blended 271,382 verts / 683 tiles, uploaded to `terrain/everest/` and confirmed through the Worker. **Runtime-verified: 612 ground tiles carrying `/terrain/everest/` URIs and ZERO CWT inside the extent; Kala Patthar reads 5,550 m against a published 5,545 m.** NEW registry shape — a region may be **terrain-only** (`variants: []`); `resolveEnrichedSelection` now skips a variant-less boot region, because without that guard standing on Everest resolved `variants[0]` to `undefined` and requested `…/enriched/undefined/tileset.json`. Terrain-only entries go at the registry TAIL (`BAKED_REGIONS[0]` is the last-resort fallback and its `variants[0]` is read unguarded — both pinned). **(3) THREE MEASURED FINDINGS, all recorded in BAKED_ASSETS §2/§3/§6.** **(a) THE VERIFICATION REFERENCE WAS WRONG, NOT THE BAKE.** The first Everest bake failed its own probe with a −39.3 m "datum fault". `geoid.mjs` held ONE grid (lons 34–36 / lats 48–49) and **CLAMPED** out-of-range lookups to its nearest corner, so the Everest probe was answered with Dnipro's 36 E/48 N value, **+20.025 m where the truth is −28.341 m**. A correct bake was indicted by a reference extrapolating 5,000 km. The clamp is now a **throw**, the bake **pre-flights grid coverage before meshing**, and sampling a grid is step 2 of the add-a-city runbook. *A reference that extrapolates silently turns "no data" into evidence — and the evidence indicts the wrong component.* **(b) THE PROBE ONLY WORKED ON A PLAIN.** It compared ONE tile-centre sample against the source COG: on a river plain that is accuracy, in the Khumbu it is one point's slope error. Rewritten to a 9×9 interior grid splitting **bias** (median signed — the datum gate, which cancels sign-symmetric slope error however steep the ground) from **spread** (median absolute — the resolution gate, the only one that must grow with relief), plus a positive control that all 81 samples land inside a source COG so a mosaic at −180/90 cannot pass with a median of nothing. Tolerances are region-scoped with the Dnipro numbers as defaults. NEW `--probe-only` re-runs verification against an on-disk tree writing nothing — **the only safe way to re-touch an existing bake, because the rim blend is not idempotent** — and it re-verified the SHIPPED Dnipro artifact unchanged: bias +0.4 m / spread 2.4 m. Everest: bias +8.8 m / spread 10.2 m on a probe tile carrying **1,944 m of relief**, rim Δ **−0.0 m**. **(c) MESH INTENSITY IS NOT THE LEVER — measured and REJECTED.** mago's `--intensity` (default 4, never set by the bake) at 12 tripled vertices (1,096 → 3,500) and bytes (21 → 68 KB) and moved the summit sample by **0.1 m**. The shipped default is already at the source's information limit; raising it would take 210 MB to ~640 MB for nothing. **What the height map can actually do, end to end:** GLO-30 reads the summit pixel at **8,732 m** — the 30 m DSM is **111 m below the published 8,848.86 m apex by construction** — our mesh carries that peak to within 3 m (tile max 8,734.9 m vs the source's 8,737.8 m window max), and a point sample at the apex coordinate reads lower still because a 34 m posting cannot resolve a pyramid. **The bake is faithful to its source; the source is not faithful to the apex.** Also REJECTED with its cost: dropping `extentMaxDepth` below `maxDepth` would cut 210 MB → ~41 MB but moves the patch↔CWT boundary off the rim-blended extent edge onto an un-blended `cityBbox` edge — 170 MB bought with a height cliff ringing the region. **(4) FPV far-field accuracy investigation ran READ-ONLY** (owner: "do not implement anything in this session") — six-dimension inventory + adversarial refutation; report and slice ladder recorded separately. Log: `mem:project/wip-2026-08-22-owner-3slice`.
- **2026-08-22g · THE GUIDE IS FINALIZED — all ten charter slices G-A…G-J shipped in one session.** Gates: **vitest 1,292/1,292 (110 files, +75 this session)** · `astro check` 0 err / 5 hints · `npx knip` exit-0 · NEW `scripts/verify-guide.mjs` **ALL PASS across all three surfaces** (30 checks, shots `guide-01..05`) · `verify-audit3.mjs` 16/16 regression PASS. Executes `GUIDE_FINALIZATION_PLAN.md` (the 732-line charter authored 22f by a 10-agent fan-out). **THE HEADLINE IS THAT THE CHARTER WAS NOT ENOUGH: a 12-agent adversarial re-audit of the copy against live source raised 44 findings and 38 SURVIVED refutation — including eight the charter's own 58-finding pass had missed.** The most consequential: **`move-pin` claimed "PLAN reads the light at the pin" and PLAN does no such thing** — `scene/planFeed.ts:342-351` is a three-rung ladder (`photoApex → fpvEye → focus`) with **no temp-pin rung at all**, and `dropTempPinAt` (`StylizedTiles.ts:1616-1628`) never moves the camera focus either, so the escape hatch "the focus IS the pin" does not exist; the pin seats the **aim** (`lib/geo/aimAnchor.ts:56-57` does carry a tempPin rung), and a bare pin yields NO skyline verdict until you stand on it (`plan-verdicts.tip` corrected too). Also caught: **`fpv-hud.where` named the wrong corner on BOTH shells** (desktop `.fh` is `bottom: 2.4rem` — "True bottom-left corner", `fpv-hud.css:10-16`; `/m` `.m-fpvhud` is `top: 3.1rem`, under the status strip) and claimed "the same rows ride /m" when `FpvControls.tsx:119-138` renders **four** of the eight; **`trust-airless` had the refraction sign BACKWARDS** — an airless sky reaches the horizon *before* the refracted almanac time, not after; the `fpv-height` ghost previews the **new** height over the solid original, not "a ghost of the original"; `fpv-cone`'s "shows from the moment the app boots" is false at a 1,100 km boot pose (it is altitude-gated like the radar); `find-mobile` called FIND "the fourth tab" (it is third); `photo-upload` named the wrong gesture (SET ON GLOBE arms it, *then* you click); `target-card` credited it with rise/set jumps that live in the sky menu; `photo-align` called the heading/pitch **encoders** "sliders"; and the ▲/▼ glyphs on `move-deck` are the **/m chip's** — the desktop deck renders bare `3D`/`2D`. **G-A gates first (test-only):** NEW `guideSearchGolden.test.ts` — a 31-row CHARACTERIZATION fixture where an unfixed row pins its MEASURED wrong answer, so it can neither drift silently nor be "fixed" without deleting the pin (27 rows started pending; **all 27 now reach their target and `KNOWN_MISSES` is EMPTY**) · NEW `test/pages/guideAstroShape.test.ts` machine-checks T24's three `set:html` properties over a GLOBBED `.astro` sweep · the voice lint's corpus went **144 → 294 strings** (titles, both `where` lines, the goal phrases and the new alias field had ZERO enforcement; the six existing BANNED regexes passed clean on all 150). **G-B content model:** `GuideTopic.list` (an `<ul>` for the 13 topics that rendered an enumeration as one 80-word sentence), `GuideMedia.w/h` (13 lazy images reserved no box; `guide.astro` hard-coded `width="720"` for nine shots and it was **wrong for every /m shot at 360×783**) with a gate that parses the real **WebP header** (VP8/VP8L/VP8X) so a re-shoot cannot drift, `GuideTopic.keys` and `GuideGoal.route`. **G-C/G-D copy:** the charter's 18 drifts + the 38 audit corrections + THREE new topics — `start-explore` (Explore is a permanent nav entry the guide mentioned **zero** times), `keys` (12 keyboard binding sites, 4 documented), `fpv-focal-axes` (T41, owner-ruled ACCEPTED AS-IS → **CLOSED**, documented in both registers; the engineering half is a dated `globe-tuning.md` trap row naming both constants, the full six-vs-one consumer split, and the `Joystick.tsx:115-117` proof that ONE live FOV feeds both readouts). **ONE CHARTER CLAIM REFUTED BY MEASUREMENT: `find-sunsets` is NOT desktop-only** — `/m` ships SUNSETS IN FRAME inside the PLAN sheet (`PlanSheet.tsx:284-426`, an accepted two-shell dup); scoping it desktop-only would have introduced a fresh error, so it got a two-shell `where` naming the different homes. **G-E search:** `keys` aliases + glyph/shorthand QUERY expansion (`◉ ∠ ⌖ ▲ ▼ ◎ ▤ ⊞ ▦ ✕ ☀ ☾ 🧭 …`, `/m`, `36h`), caption indexing at both topic AND chapter level (GOTO lives only in a chapter caption), fuzzy floor 4→**5** chars (`exif` reached `fpv-exit` through "exit"), prefix `minLen` **3** (`re` exploded to re/red/real/read/reset), per-tier expansion caps ranked by **`w × idf`** (the old `.slice(0,8)` by weight rewarded SHORT terms and dropped `compass`/`constellation` for `cone`/`copy`), and a six-tier identity ladder — title-exact ×3.0 · **id-exact ×2.5** · title-phrase ×2.0 · title-lead ×2.0 · title-word ×1.5 · verbatim-phrase ×1.4 · title-prefix ×1.35. **Two of those tiers were forced by measurement, not the charter:** resolving a bare `[[fpv]]` to its rendered TITLE (the parity fix) **deleted the literal token "fpv" from the corpus** and `searchGuide("FPV")` returned `[]` — so node **ids** are now indexed at 2.5, which is where `fpv`/`mobile`/`plan` actually live; and `foc` lost to `move-search` because the rare, incidental "focus" out-IDF'd "focal" **in the title and the id**, which document frequency cannot see. Result: **every curated query, all 70 topic titles, all 11 chapter titles and all 14 goal phrases now rank their own node first, with all four negatives still `[]`.** Declined per the charter's numbers: K1/B retuning (inert at N=78), stemming, a search library, click-learning. The charter's proposed LEXICAL alias fence was **rejected on measurement** — it would have permitted only aliases for words that were already findable; the shipped fence is BEHAVIOURAL (an alias must reach its own topic, no alias on >2 topics, no alias may answer a negative) and it immediately caught **7 real over-claims** (`start-shells:"mobile"`, `fpv-enter:"walk"`, `mobile-map:"map"`…). **G-F parity + THE LIVE BUG:** `nav()` wrote an identical `chapterId` for a same-chapter target, React bailed, and the scroll effect keyed on `[chapterId, open]` never re-ran — **two shipped crosslinks did not scroll on either island**; fixed with a monotonic `navSeq` in the deps and browser-proven with a TRIGGER GUARD (the target starts 1,742 px below the fold). Plus: goal → **topic** anchors on `/guide` (it linked the chapter), `/m` search **hoisted above** the index/chapter split (it was unreachable from inside a chapter) with the dropped snippet restored, sticky chapter header, `↺ GOALS`, 62/66ch measure caps matching the page, the page's missing `h1`, a topic tier in all three TOCs (67→**70** topics now reachable without opening a chapter), and `/guide` linked from the shell at last (`grep '"/guide"'` had returned exactly two sites, **both inside the guide itself**). **SURF-1 shipped and it reverses a written invariant** — `guide.astro`'s "zero client JS" charter is superseded by the owner's explicit "all places … support advanced search"; the amend is in the docblock, the form is `hidden` until the bundle unhides it, and the no-JS reader gets the full chapter→topic outline as real anchors. **G-G:** Escape now owns the guide via a **CAPTURE-phase** listener (`stopImmediatePropagation`), which beats the globe's bubble-phase Escape chain regardless of registration order and therefore needed **zero globe edits** — browser-proven that Escape closes the panel and leaves FPV standing; `?guide=<id>` deep links on both shells (a QUERY param, never a hash — the globe rewrites the whole hash every ~1.6 s and both pose parsers are anchored; `/m` reads it in `MobileShell` because `GuideSheet` only mounts once its sheet is open) that strip themselves and preserve `#p=`; goal ROUTES with a `STEP n OF m` footer; `/`-to-search, ↓/↑ through hits, Enter to take one; zero-JS image anchors. **G-H anti-slop, judiciously:** four new BANNED groups at **0 current hits** — a REGRESSION GUARD against the next "enrich the guide" pass, not a cleanup, because **97 of ~120 stop-slop patterns already scored zero** — plus i-have-adhd's one-bounded-action-per-step rule, landed AFTER the splits (12 of 43 steps violated it). **REJECTED with measured counts and recorded in the lint's own comment:** the em-dash ban (652 in this file, **4,647 across `src/**`** — and the skill breaks it inside its own example), three-item lists (every third item is a real on-screen thing), the blanket `-ly` ban (`exactly`/`automatically`/`hourly` are the precision), `never` (the **C6** wartime-privacy guarantee), and "cut quotables" (which is the `tip` field's entire job). **Three defects were caught by eyeballing the shots, not by any test:** the `/guide` search dropdown rendered as raw underlined links because **Astro scopes `<style>` and the runtime-built rows carry no `data-astro-cid`** (→ `:global()`); the header did not wrap; and `.g-goals a` matched DESCENDANT anchors, dressing all 14 reading routes as full goal cards five screens tall (→ `> li > a`). NEW `test/components/guideParity.test.ts` fences 15 content fields × 3 renderers so a field cannot become two-thirds shipped — **and its own comment stripper had to be fixed first**: block-comments-first let a `//` line containing `components/mobile/**` open a phantom comment that ate ~100 lines of live code, so `stripComments` (now shared in `test/styles/_css.ts`, line comments FIRST) is the fix and the trap. **T24 re-verified and dated in the same commit as the `guide.astro` change**, per the row's own trigger. **T42 stays OPEN** — the eight stale crops are owner-taste by the charter's own ruling — but is annotated with three things this session settled: `orbit.webp` needs no re-shoot (the copy was wrong, not the image), `upload.webp` shows the DROP ZONE so its caption was corrected to match the shipped frame, and any re-shoot must now update the declared `w`/`h` or the suite fails. Files: `lib/guide/{guideContent,search}.ts` · `panels/Guide.tsx` · `mobile/{GuideSheet,MobileShell}.tsx` · `pages/{guide.astro,index.astro}` · `styles/{guide.css,mobile/chrome.css}` · `conventions/globe-tuning.md` · 4 test files + `scripts/verify-guide.mjs`. Log: `mem:project/wip-2026-08-22-guide-final`.
- **2026-08-22f · OWNER ORDERS: T41 RULED + the GUIDE FINALIZATION CHARTER authored (planning entry — no product code this session).** **(1) T41 RULED — ACCEPTED AS-IS, no code change.** Owner: *"i am ok for now with this and understand the reasoning, for next session - plan to document this focal dichotomy."* The row converts from a code item to a DOCS item, and its MECHANISM was corrected in the same pass by reading `Joystick.tsx:115-117`: **inside FPV both readouts derive from the SAME live vertical FOV** (`hFovNow = s.fpvHud ? horizontalFovDeg(s.fpvHud.fovDeg, s.fpvHud.aspect) : plannedView.hFovDeg`), so the divergence is **purely the sensor AXIS** — `focalFromVerticalFov` measures the 35 mm-equivalent across the frame's HEIGHT (24 mm, six consumers: `FpvHud.tsx:105` · `FpvControls.tsx:124` · `CameraTiltPanel.tsx:250` · `SpotStarsCard.tsx:43` · `PlanSheet.tsx:493` · `MyPins.tsx:190`) and `focalMmFromHFov` across its WIDTH (36 mm, sole consumer `Joystick.tsx:168`). There is no second source of truth; the earlier "plannedView vs hud" framing was incomplete. **(2) The owner's next-session order is single-track: FINALIZE THE GUIDE** — *"fully updated, enriched and interactive"*, all three surfaces *"referencing same resources"* with *"a nice layout"* and *"advanced search… more precise and fluent in terms of data matching"*, cleaned of AI slop via `ayghri/i-have-adhd` + `hardikpandya/stop-slop`, *"judiciously"*. Charter authored by a **10-agent fan-out** (2 skill-distillation · 4 mapping [surfaces/search/coverage/interactivity] · 4 adversarial verification, each told to REFUTE) → **58 findings raised, 0 refuted**, synthesized into NEW **`.claude/claude-docs/GUIDE_FINALIZATION_PLAN.md`** (732 lines, 10 dependency-ordered slices G-A…G-J, each with the command that proves it done). `NEXT_SESSION_PROMPT.md` rewritten as a short entry point to it (the `*_PLAN.md` convention — UPLIFT_PLAN/MOBILE_PLAN/archived UXBATCH4_PLAN). **The headline of the research is a REJECTION, which is what "judiciously" earns:** 97 of ~120 stop-slop patterns score **ZERO hits** in the shipped copy — the guide already reads the way the skill is trying to produce — while adopting it wholesale would destroy **em dashes** (measured **4,647 in `src/**`**, 652 in `guideContent.ts` alone; the house separator — and the skill **violates this rule inside its own recommended example** at `examples.md:45`), **three-item lists** (19 hits, every third item a real on-screen thing — truncating them would make the doc factually WRONG, losing to `guideContent.ts:7`), the blanket **-ly ban** (`exactly`/`automatically`/`hourly` ARE the precision in a measurement instrument, and it contradicts the same skill's own "Be specific"), **lazy extremes** (`trust-privacy`'s "never" is the **C6 wartime-geo guarantee** from PROJECT_SEED §3 — banning it forces the hedging the skill's own "Trust readers" forbids), **"cut quotables"** (that is precisely what the `tip` field is defined to be), Wh- openers, and "vary rhythm" (unmeasurable under a 5-sentence cap). What IS adopted: **one new BANNED regex group** — the *essayist* tells the current *marketing* lint never reached (throat-clearing openers, emphasis crutches, meta-commentary, narrowed business jargon, 15 NAMED adverbs, chatbot openers/closers), all at **0 current hits, i.e. a REGRESSION GUARD against a future "enrich the guide" session** — plus i-have-adhd's one-bounded-action-per-step rule, where the guide is genuinely non-compliant (**12 of 43 shipped steps**), and a cross-skill warning that i-have-adhd explicitly PROTECTS the hedges a naive slop pass would strip ("accurate to about one arcminute" is a factual claim, and its own rubric marks stripping it `blocker: true`). **LIVE BUG FOUND AND VERIFIED BY THE MAIN AGENT, not yet fixed (G-F owns it):** `nav(id)` sets `pendingTopic` then `setChapterId(ref.chapterId)` while the scroll effect's deps are `[chapterId, open]` — a crosslink to a topic in the **CURRENT** chapter sets an identical value, React bails, the effect never re-runs, **nothing scrolls**; identical shape on both islands (`Guide.tsx:110-116` vs `:157`; `GuideSheet.tsx:95-100` vs `:117`), and two shipped crosslinks hit it today (`move-minimap → move-aimstick`, `target-toggles → target-ghosts`). Also surfaced for the plan: **150 user-visible strings the voice lint does not walk** (chapter/topic titles, `where.*`, the 10 goal phrases); the standalone `/guide` page has **no search at all** and its goal router lands on CHAPTERS where the two islands land on TOPICS; `guide.astro:99` hard-codes `width="720"` on nine chapter shots and it is WRONG for the 360×783 `/m` one; 18 factual drifts against the live UI; and three undocumented surfaces (Explore, the keyboard map, T41). Gates unchanged and re-confirmed: vitest 1,217/1,217 · astro 0 err/5 hints · knip exit-0. Release still GATED on the owner's domain fix. Plan: `GUIDE_FINALIZATION_PLAN.md`; log: `mem:project/wip-2026-08-22-audit3-fixslices`.
- **2026-08-22e · AUDIT #3 FIX SLICES F1–F10 SHIPPED — ALL TEN, the whole queued table.** Gates: **vitest 1,217/1,217 (107 files)** · astro 0 err/5 hints · **`npx knip` exit-0 clean** (was 45 false "unused files" at audit time, then 2 unlisted deps + 2 unused exports; `sharp` is now a declared devDependency, java/sips are declared external binaries, and `cssInk`'s invalidation seam is exercised by a test rather than ignored) · all SEVEN regression suites PASS + NEW `scripts/verify-audit3.mjs` ALL PASS both shells (shots `a3-01..04`). **(F1 one-liners)** T32 `planner.ts` — the observer-elevation CEILING, and the test found a SECOND throw site inspection had missed: `SearchRiseSet(..., metersAboveGround)` builds a second observer at `observer.height − metersAboveGround` and runs THAT through `Atmosphere()`, so a clamped 10 km observer against an un-clamped 6,000 km eye still threw `Invalid elevation: -5990000`; NEW `planElevationsM()` is the ONE writer of both, `obsM − eyeM` in band by construction, with a positive control pinning that astronomy-engine really throws un-clamped · A1-4/T37 chart-tile `onerror` — **the audit's literal fix would have been a regression**: `draw()` runs at ~20 Hz in FPV, so a bare `cache.delete(url)` turns one 404 into a 20 req/s storm; shipped shape is `failedAtMs` + a 30 s `TILE_RETRY_MS` cooldown + a warn capped at 8 · A1-5 the terrain-seat reset gated on `moved` only (day-cross/target-swap/skyline-arrival rebuild geometry at the SAME point; re-seating there pops the radar to ellipsoid 0 whenever `terrainHeightAt` answers null) · A2-6 the stale `emphTauMs` comment (emphasis gates the FILL WASH ONLY — the rim carries no `emphEased` term) · A2-5 `__globeQuality` REGISTERED in `global.d.ts`, the `as unknown as` cast dropped, rebuilt as **live getters** (the old snapshot was written inside `applyTier`, so on desktop — where a flat flip does not re-apply the tier — every field went stale at the last governor step), `flat2d`→**`leanFlat2d`** (it is the coarse-pointer-only DPR latch, permanently false on desktop — the name now says so) plus a NEW honest `mapFlat`; `__memberStore` moved out of a local `declare global` into the registry, which is why `contracts.md §3` under-counted by five. **(F2 range contract)** A2-3/T39 `clampPlannedView` applied ONCE at `store/camera.setPlannedView` (4 of 7 writers bypassed the band); the placed-photo seed's inline clamp deleted as redundant; tests reproduce both measured breaches from the production expression itself (1.27° at aspect 0.46, 122.4° at 2.17) and the browser verifies 0.4/1.27/122.4/5000/−3 all land in [3,120]. A2-4/A1-12 dead branches **KEPT as dated ENGINE-ABSENT GUARDS, not deleted**: `setPlannedView(null)` has no call site, but a build without `PUBLIC_CESIUM_ION_TOKEN` never attaches the orchestrator (the chrome still mounts) and a `#f=` FPV boot defers the seed to the first exit. **(F3 per-frame waste, T38)** focalCone allocates its two BufferGeometries ONCE and rewrites in place (`HFOV_EPS_DEG` 0.1° sits below the stick's rate, so "per rebuild" meant per frame while held) — browser-verified by **identical geometry uuids across a 79.6°→90.0° sweep** · NEW `lib/theme/cssInk.ts` memoises the `:root` tokens both canvas radars paint from (**0 `getComputedStyle` calls left in either paint**, source-fenced; ~320 forced style reads/s at 20 Hz before) · the /m PiP's second `renderer.render` brackets `shadowMap.autoUpdate` and restores it. **ms cost still UNMEASURED — device half rides T1.** **(F4 the anchor ladder, T36)** NEW `lib/geo/aimAnchor.aimAnchorFor()` — FPV eye → placed photo → temp pin → view focus — on all three surfaces. Browser-measured: the camera NADIR sat **4,341 m** from the view focus on a tilted desktop orbit, which is the size of the bug. **THE AUDIT'S OWN FINDING THEN BIT THE HARNESS, LIVE:** `verify-qaslice-cab.mjs` hand-transcribed the old ladder to check where ◉ centres — the exact C8 anti-pattern the same audit flagged — and failed by **81.8 m against a corrected app**. Fixed the honest way: the chart PUBLISHES its resolved anchor (`__mapWindowView.anchorLatDeg/anchorLonDeg`) and the script READS it. **RULE: a verify script never re-derives a shipped decision.** **(F5 extraction seams, T35)** all four: `lib/geo/radarBands.ts` (`bandFor` ×3 + `bandFutureInk` ×2, now cross-file fenced — the fence strips comments first, because the docblocks legitimately name the tunables) · `scene/tangentOverlay.ts` (flat material + tangent-plane root/seat + presence ramp + fade — ≈47 lines incl. a byte-identical factory) · `panels/radarCanvas.ts` (the ≈95-line canvas painter, contract-tested through a recording 2D context rather than against a transcription) · C8's `chartTransform`/`rotFwd`/`rotInv` in `slippy.ts`, which makes `chartWalkAzRad`'s round trip run through SHIPPED code. jscpd **35→33 clones, 1.14→1.06 %**. **(F6 harness hardening)** C11 NEW `scripts/verify-cdp-cleanup.mjs` wired into ALL 20 verify scripts — `trackTarget` after `/json/new`, `finishVerify` at every exit, closing over plain HTTP and from the `uncaughtException`/`unhandledRejection` handlers (the only `finally` a top-level-await module has); `fail()` helpers THROW `VerifyFailure` rather than `process.exit`, because an `await finishVerify()` there would let a script run PAST its own failure (every call site is `if (x) fail(...)` with no await) · C14 the fences de-brittled (sibling relation proved by JSX DEPTH not by a trailing comment; function bodies lifted by BRACE MATCHING not indentation; value pins → presence pins; the PiP's "true miniature" contract expressed as the vw==dvh EQUALITY it actually is, so a taste retune of 32 does not go red) · C16 the vacuous `|| "absent"` arm dropped — the check now pins `visibility: hidden` on a MOUNTED card, which is the invariant · C17 the `qsl-04` rename annotated · C18 NEW `test/styles/_css.ts` · A1-16 NEW `PLAN.minCoverageForGaps 0.5` + `horizonProfile.skylineBinsFor` — ONE gap gate on three surfaces, now including the EVIDENCE floor (`profileCoverage` reached the store and both PLAN panels and was consulted by no radar: a 15 %-covered profile fractured its bands like a complete one — the gaps are true, but the clear sky between them was ignorance drawn as knowledge). **(F10 iOS focus)** NEW `useSheetInputFocus()` extracted from the documented QA fix and applied at both siblings the audit found still armed (`SceneActions` React `autoFocus`, `MobileSearch` bare re-`focus()`), plus **mobileFence rule 4**: no `autoFocus` and no bare `.focus()` anywhere in the /m shell. **(F7 docs, T40)** D5 `globe-tuning.md` gains the whole batch-#4→#7 tunable vocabulary (AIMCONES bands/mobile variants/`mapRadiusHK`/`skylineGuardM`/`northOffsetK`/`fillAlphaRest`, FOCALCONE wholesale, GROUND/TILESETS chart knobs, `QUALITY.leanMobile`, `PLAN.minCoverageForGaps`) **plus the two TRAP lines it lacked** — the sticky overlay-px ONE-writer rule and the injected-GLSL fragment-header rule · D9 `verify.md` gains all six harness environment classes as a symptom/counter-move table, the synthetic twist + long-press recipes, and the four newer DEV probes · D7 `contracts.md` §2 `ftw:bldg-overrides:v1`, §3 15→**20** seams + `__globe.aim()`, §7 8→**9** routes + `public/sw.js` re-homed · D8 `UXBATCH4_PLAN.md` self-contradiction fixed, archive banner added, `git mv`'d to `archive/` · D3 ARCHITECTURE now names every structure that was in no doc · D14 the bare "GOTO" disambiguated on both permanently-out lists (the shipped camera-aim pill is not telescope mount control) · D15 knip CLOSED. **(F9 guide, D2 remainder + half of D10)** 4 NEW topics — `fpv-cone` (the focal cone had ZERO topics), `move-aimstick` (all five `joystick` hits described the WALK stick), `fpv-map-controls`, `fpv-map-gestures` (`grep -c twist` was 0) — plus 6 extensions (`move-minimap`, `mobile-map`, `time-inputs`, `move-search`, `start-shells`, `target-radar`'s mobile compaction). NEW **reproducible** `scripts/shoot-guide.mjs` (recipe-driven, warm two-pass, CDP + sharp) re-shot `orbit`/`fpv`/`fpv-m`/`target` on a warm cache and took the NEW `fpv-map` shot; all four stale captions rewritten to match what the frames now show. **TRAP for the record: `Page.navigate` to a URL differing only in its HASH does not reload the document, and /m re-mirrors the camera into `location.hash` ~1.6 s after boot — bounce through `about:blank` or pass 2 silently re-photographs pass 1's state.** **NEW DEBT, both from this session's own work: T41** — the FPV HUD prints the focal from the VERTICAL fov (24 mm frame height) while the AIM stick prints it from the HORIZONTAL fov (36 mm width); both correct, they agree at 3:2, but a portrait phone reads **23 MM and 75 MM for the same view**, visible side by side in the new `fpv-m.webp`. Not a maths bug and not in the audit's set — a readout-consistency ruling left to the owner, nothing changed unilaterally. **T42** — 8 of 12 guide shots still date from 2026-08-15; the harness to re-take them now exists, the crops are owner taste. Backlog: **T32/T35/T36/T37/T38/T39/T40 CLOSED**; T41/T42 opened; T1 grew by the T38 device numbers. Release still GATED on the owner's domain fix. Log: `mem:project/wip-2026-08-22-audit3-fixslices`.
- **2026-08-22d · POST-SHIP PREP — audit harvest LANDED + knip fenced + DECISIONS COMPACTION ROUND 4 (owner: "prepare to cover maximum scope for next session").** Shipped as PR #69 first (the title cap worked — PR created **and merged** within 45 s, where the 1,221-char subject had produced no PR at all), then the preparation work whose whole purpose is to remove next session's *discovery* cost. **(1) The six checklist amendments from the audit's Pesticide-Paradox harvest are LANDED, not just proposed** — `tests.md` gains **11 "Can this check FAIL?"** (the three shapes to refuse, each drawn from a real one this session: the already-true absolute · the unfalsifiable capture · the no-op that reads as success) and **12 "a counter needs a positive control AND its precondition pinned"**; `code.md` gains **26** screen-edge ownership on BOTH shells before adding a surface (the shells are not symmetric — this cost two collisions in one session), **27 a z-index cannot lift a child out of its parent's stacking context** (A1-2's fix had to be geometric), **28** when a shared offset token is introduced, sweep EVERY surface anchored to that edge; `docs.md` gains **13** delegated affordances make their guide topic load-bearing. **(2) `knip.json` LANDED AND VERIFIED** (D15): the 45 "unused files" are **gone** — declaring `scripts/**/*.mjs`, `public/sw.js` and the test glob as ENTRY points rather than ignoring them keeps knip analysing them for genuinely dead code instead of blinding it. Config then tightened against knip's own hints until it emits none; the only remaining output is **2 real unlisted dependencies** (`vite` in `astro.config.mjs`, `sharp` in a bake script — both dev/build-only, both transitively resolved; registered, not blind-fixed, since adding them to `package.json` could shift resolution). **(3) DECISIONS COMPACTION ROUND 4 EXECUTED** (D16 — it was DUE, not merely proposed): round 3's carve-out ("the UPLIFT era stays verbatim — ladder parked mid-run") **expired** when U8 completed the ladder on 2026-08-19, and §Recent had reached 135 KB against the 147 KB that triggered round 3. Verbatim sessions **2026-08-17 → 2026-08-19d** (34 dated rows, `:320`–`:875`) moved **byte-identical** into `DECISIONS_ARCHIVE.md` §Moved 2026-08-22 — **md5 `5ed47c51b9d44a754964771ffe418330`, 556 lines / 79,306 B, checksummed BEFORE and AFTER the append with an automatic restore-on-mismatch guard**. Containment re-verified per row with **fixed-string** probes plus a positive control (2026-08-22c must be absent from the archive and present in DECISIONS) — the first containment sweep reported all rows "MISSING" because the shell's `grep` is `ugrep` and parsed the leading `**` as an empty regex subexpression; that was a **probe failure, not a finding**, and is exactly the zero-result-validation rule the audit had just re-mined. Byte accounting closes: 587,733 → 588,328 B total, the +595 being the new divider and sentinel text. **THREE era digests appended** (UPLIFT ladder U1–U8 + audit #2 + fix slices · owner UX batches #2/#3 · PLUX launch grooming), the OWNER-BATCH era (2026-08-21 →) deliberately kept verbatim because it all rides the un-shipped release gate. **D13 closed in the same pass**: the "New work appends a dated line here" marker had drifted mid-list AGAIN (10 newer rows above it, its own annotation falsifying itself) — re-seated directly under the §Recent heading with the root cause recorded (appends go to the top; nobody re-seats the marker). **Result: DECISIONS 167,396 → 91,522 B and §Recent 141.7 → 62.6 KB — every future session's Phase-0 read is ~79 KB cheaper**, which is the concrete form the owner's "maximum scope" order takes. Gates unchanged: vitest 1,147/1,147 · astro 0 err / 5 hints.
- **2026-08-22c · AUDIT #3 COMPLETE (4 tracks, TWO WAVES) → report + 5 unfalsifiable checks fixed + docs reorg part 1.** Report: **`audits/audit-batchseams-2026-08-22.md`**. Ran as owner-ordered: micro-slice FIRST (22a/b), then the audit; Track D verified no `src/` diff is attributable to the audit itself. **Two waves (A1+A2, then C+D)** — the 2026-08-21h run fanned out all four at once and an API session limit killed every one before any reported. All four returned this time. **46 candidate findings, 0 deleted in verification** (two were self-demoted by their own tracks before reporting — A2 refuted a suspected desktop zero-area PiP render via the JSX gate; C refuted an Astro-scoping concern by running `@astrojs/compiler` on a minimal reproduction). **THE HEADLINE RESULT IS VERIFICATION INTEGRITY: five checks that could not fail — four written THIS session — were found and fixed.** **C4** `expect(...).not.toMatch(/display:\s*none/)` was *structurally* unfalsifiable (the capturing regex terminated at `visibility: hidden`, so the string could never contain it) → capture the full rule body. **C2** "the REAL scrubber is LIFTED clear of the bar" held with the lift **deleted entirely** (`.ts` base `bottom: 2.2rem` = 35.2 px already exceeds the 13.6 px bar) — it proved the bar existed, never the lift → measure the **delta** across a close/reopen (now: lift 13.6 px == bar 13.6 px). **C1** the whole QA-slice-C leg could run with **zero** Esri traffic and pass: the counter only fires in satellite ground mode, `/tmp/ftw-cdp` persists that pref across sessions, and the settle check printed the count without asserting it > 0 — the reported "≈0 new GETs" and "residual ~600 GETs/leg" would have been fiction → precondition assert + `esriGets > 0` as the counter's own positive control. **C3** "following RESUMED after ◉" passed if the walk moved nothing (◉ had just centred the chart *on* the eye) → trigger guard, verified 118.9 m walked. **C5** the A1-2 viewport loop captured `altPresent` and never asserted it; `.m-altcol` mounts only inside FPV, so a reordering would have made all three sizes pass **vacuously** on the one occlusion they exist to disprove. Also **C6** the mid-stack bleed sweep skipped `position:absolute/sticky` and `z-index:auto` and had no positive control → widened, scoped out `.mw`'s own subtree (the first widening over-reached and flagged the window's own children), and now reports `scanned` (11 candidates). **C7 REFUTES A CLAIM RECORDED IN DECISIONS 2026-08-21h** — "the FOV inverse pair is transitively pinned" was **false** (it came from a killed agent's pre-verification output): `horizontalFovDeg`/`verticalFovDeg` are a PRODUCTION round trip but no test imported both halves. Now pinned for real across 5 aspects × 5 focal values in both directions, plus **C9**'s cross-pin of the duplicated `focalMmFromHFov` ↔ `focalFromHorizontalFov` — the math was correct; only the pin was missing. **C12** `/m` had no attribution wrap fallback (the A1-3 media block is page-scoped to `/`) → mirrored branch at ≤22rem. **C13/C15** the new fence over-claimed "the ONLY path back" (the same session's own DECISIONS §D says otherwise) and its discovery guard scanned a hardcoded 4-file list → retitled; now `readdirSync`s the scene directory. **VERIFIED CLEAN (probes named in the report):** the sticky overlay-px invariant end to end (ONE `setOverlayResolution` caller, no bypass, `Math.max` monotone, additionally FPV-deferred) · the `skylineBins` array-identity key with **no** null↔array flap path, probed specifically at the `skylineGuardM` boundary · **checklist-23 injected-GLSL sweep re-run mechanically rather than trusted** (imageryGround **16 JS uniforms vs 16 header declarations, 0 JS-only**; `uFtwTileSeed`'s apparent mismatch is a correct vertex-header declaration) · C6 privacy on every new debug surface · shadow-map correctness under the PiP scissor · the bottom-strip inventory on BOTH shells (the sweep that surfaced A1-15) · supersession discipline · 12/12 new-tunable coverage · golden gates intact. **DOCS REORG PART 1 SHIPPED** from Track D's doc-line→`src:line` pairs: **D2** the guide taught gestures the code does not perform — worst, "Long-press the full MAP — VIEW FROM HERE" while `/m` long-press **places a point and stays**, and `MapWindow.tsx` *deliberately delegates* the `/m` affordance to the guide while hiding the on-screen hint, so the delegation pointed at wrong copy; 3 wrong claims fixed (fpv-map, mobile-gestures, target-radar's "blue" future halves) plus the stale `MapWindow.tsx` header docblock that seeded the error. **D1** ARCHITECTURE §7 had **3 factually wrong** claims (8 routes → 9; 12 stores → 14; "all shared logic lives in `lib/**`+`store/**`" false since 2026-08-21b) → re-dated, corrected, `components/controls/` tier + `public/sw.js` + the bands/sticky-overlay/three-surface rules documented. **D4** README gate counts 4 sessions stale → 1,144→1,147 / 102 files / 2026-08-22. **D6** `components/controls/` was enforced only by a test no convention advertised and was absent from the design-fence allow-list → `CLAUDE.md` + `architecture-and-patterns.md` now name the three fence rules and three shared tiers. **D12** the debt registry had not moved in 4 shipping sessions → **T34–T40** added (LRU rest-trim churn ~600 GETs/leg surfaced to the owner as ordered · 3 named extraction seams · the anchor-ladder divergence · the tile-`onerror` one-liner · per-frame waste · the `plannedView` clamp · the docs remainder). **Notable finding both A-tracks reached independently:** the chart's radar anchor ladder diverges from the GL fan's (missing placed-photo rung; the bare `camGeo` rung anchors on the camera **nadir** outside FPV) — **the refutation attempt FAILED**: the only `setMapWindowOpen(false)` in the orchestrator is the Escape handler, so exiting FPV any other way leaves the map open and the divergence live. **Six checklist amendments proposed** (the Pesticide-Paradox harvest), including "name the mutation that would make this check red", "a counter needs a positive control", "a z-index cannot lift a child out of its parent's stacking context", and "if code DELEGATES an affordance to the guide, the guide topic is load-bearing". **Gates: vitest 1,147/1,147 (102 files) · `astro check` 0 err / 5 hints · `verify-qaslice-cab.mjs` 64/64 both shells · all seven regression suites PASS.** Remaining audit output is unfixed BY DESIGN (audits are read-only; fixes are sliced sessions) and carries a 10-slice plan F1–F10 in the report.
- **2026-08-22b · OWNER FOLLOW-UP (/m PiP re-seat) + FOUR AUDIT-#3 DEFECTS IN 22a FIXED BEFORE SHIP + the auto-ship title trap CLOSED.** **(A) Owner order: the /m PiP moves up so its TOP aligns with the MAP/+/− pills.** Done structurally rather than by matching literals — NEW `--mw-top-y 0.6rem` + `--mw-pip-h 32dvh` tokens (`map-window.css`) that `.mw-top`, `body.m .mw-pip` and `body.m .mw-recenter` all derive from, so the whole `[+ −] · [PiP] · [◉]` right rail moves as one if the rung ever changes. Browser-measured: PiP top **9.59 px** == row top == pill top; pills end at 140 px, the PiP starts at 253 px (no horizontal overlap); `.mw-top` stays `pointer-events:none` so its full-width invisible box passes taps through. **(B) REGRESSION the move exposed, fixed:** the PiP is a *hole* (draw() clearRects it, `body.m .mw` has no background), so anything fixed between the GL canvas (z 0) and the map window (z 20) paints INSIDE it — the batch-#5 "minimap inside minimap" class. The new rung put the hole over `.m-status` (Plux · account · GUIDE · DESKTOP, z 10) and the chips showed through. `.m-status` joins the existing `body.m.mw-open { visibility: hidden }` group — nothing is lost, the opaque map canvas already covered the strip. The **z-2 DOM label layers** (`.sky-names` ×2 modules, `.geo-labels`, `.bldg-edit-label`) joined too: that bleed is **PRE-EXISTING** (the hole overlapped the upper screen at 3.4rem as well) and was surfaced, not caused, by the new **intersection sweep** the verify suite now runs — it enumerates every fixed 1 ≤ z < 20 surface overlapping the PiP rect instead of naming today's offenders, so the next reseat cannot silently reintroduce the class. Those layers are positioned for the FULL-SCREEN view while the hole shows a scaled miniature, so they could only ever paint in the wrong place there. **(C) AUDIT #3 wave 1 (Track A1) found three MAJORs in the 22a slice itself — all fixed before ship, not deferred:** **A1-1** — `panBy()` latched on EVERY pointer move, so the ~2 px drift of a 500 ms long-press (the primary /m chart gesture — it places a point) armed the now-permanent override, with ◉ the only way out. The latch moved into `onPointerMove` behind the SAME `DRAG_CANCEL_PX 6` threshold that already decides drag-from-press; sub-threshold moves still pan (the follow tidies them) but never latch. Browser-verified: a 3 px jitter leaves the button unlit. **A1-2** — the ◉ button, the sole exit from the permanent override, **slid under the FPV altitude column on short viewports**: `.m-altcol` is lifted to z 24 by `body.mw-open`, ABOVE the z-20 window, so the tap would have nudged the eye's altitude instead. (Raising the button's z cannot fix this — `.mw` is itself a z-20 stacking context.) `.m-altcol`'s geometry is now published as `--m-altcol-bottom` + `--m-altcol-h` and `body.m .mw-recenter`'s top is a `min()` whose second arm is the last rung clearing that box. Verified at three viewports: 390×844 keeps the PiP rung (287.7 px, alt at 485.6), 360×640 → 222.4 vs 281.6, **360×560 → the floor engages at 160 px vs alt at 201.6** (before the fix it was 86% occluded). **A1-3** — the desktop attribution truncated below ≈900 px: the `clamp()` FLOOR (6.4 px) stops the shrink while the 265-char list keeps its width, and `nowrap` + the bar's `overflow:hidden` clipped BOTH ends — "© Esri" itself — against the rule 22a had just written down and the T17 ToS posture. A `@media (max-width: 60rem)` branch wraps the line and redefines `--mw-credit-h 1.8rem`, so `time-scrubber.css`'s lift tracks it with no second rule. Verified unclipped at 1100 / 820 / 700 px with the scrubber lifting 48.8 → 64.0 px. **A1-15 (MINOR)** — `.tr` (TimeReadout) is deliberately co-axial with the scrub rail but was not lifted, splitting the two instruments by exactly `--mw-credit-h`; `body.mw-open .tr` now takes the same lift (separation measured **10.4 px**, the designed 0.65 rem). **(D) SUPERSEDES a 22a claim (audit A1-13):** "◉ is the one and only path back" is not literally true — `manualPan` and `setPanned` both live in the `[open]` effect, so closing and reopening the window also clears the latch (on /m the PiP tap = close, so an accidental pinch-latch is one tap from clearing). The code comments and the guide copy say so now. **(E) AUTO-SHIP TITLE TRAP CLOSED (owner report):** the 2026-08-22 00:20 ship pushed `claude/ship-20260822-002057` fine but **no PR was ever created** — GitHub rejects an over-long PR title and the automation derives it from the commit subject, which was **1,221 chars**. `.claude/hooks/session-end-ship.sh` now caps the SUBJECT at `SUBJECT_MAX=200` bytes, rewinds to the last word boundary (which also discards any byte-split multibyte char), appends `…`, logs the cap, and puts the **full untruncated title in the commit body** so nothing is lost. Exercised against the 1,184-char title: 198 chars / 202 bytes out, valid UTF-8, 225 with the ` #pr #skipreview #automerge` tags — under GitHub's 256 limit. The unlanded QA-slice commit `0ba44f7` is intact on the local branch (origin/master is its ancestor), so the next ship carries both. Files: `src/components/panels/MapWindow.tsx` · `src/styles/map-window.css` · `src/styles/mobile/fpv.css` · `src/styles/time-readout.css` · `src/pages/index.astro` · `.claude/hooks/session-end-ship.sh` · `scripts/verify-qaslice-cab.mjs` · `test/styles/mapWindowChrome.test.ts`. **Gates: vitest 1,144/1,144 (102 files) · `astro check` 0 err / 5 hints.** Browser-VERIFIED both shells: `verify-qaslice-cab.mjs` **61/61 ALL PASS** (now covering the jitter latch, the PiP top-alignment, the mid-stack bleed sweep, three /m viewport sizes and three desktop widths) + **all seven** regression suites PASS, Chrome restarted between suites. The remaining A1/A2 findings are audit output for the report + fix slices, NOT shipped here.
- **2026-08-22a · OWNER MICRO-SLICE SHIPPED 3/3 (the addendum below, implemented) — expanded-map manual-pan override made PERMANENT + ◉ RE-CENTRE button + bottom-edge attribution.** **(1) The manual-pan override is now permanent (SUPERSEDES the 2026-08-21g eye-motion re-arm).** Owner ruling after device-testing the slice: *"I do not want to auto-latch back after any manual panning starts, at all."* `MapWindow.tsx` loses `FOLLOW_REARM_M`, the `manualAnchor` ref, the anchor capture in `latchManualPan()` and the whole re-arm block at the top of `draw()`; `manualPan` is still armed by `panBy()` (drag + pinch midpoint) and by the 2-pointer branch of `onPointerDown` (zoom/twist-only pinches), wheel/chip zoom still never latches, and the `&& !manualPan` guard on the FPV-follow block stands. Walking now leaves the chart exactly where it was dragged **even once the eye leaves the visible bounds** — browser-measured: eye **302 m** from the chart centre vs a **184 m** half-diagonal, chart moved **0.0 m**. The radar/cone/eye-dot rule needed NO code change (they are already placed at `toPx(anchor)` → `xformNow().fwd`, so they scroll off with the world) — VERIFIED rather than edited, as ordered. **(2) NEW round ◉ RE-CENTRE button** (`.mw-btn.mw-recenter`, U+25C9, `aria-label="Centre the map on me"`), right edge under the top row at the rung item 3 freed (`right .75rem / top 3.1rem`; on **/m** it drops BELOW the 32vw×32dvh PiP that owns that rung — `top: calc(3.4rem + 32dvh + .5rem + safe-top)`, a deviation from the spec's literal seat because the PiP would have collided). Action = `aimAnchorNow(...)` (the SAME anchor the radar uses) → write `view.current.lat/lonDeg` → clear the latch → `requestRedraw()`; wired through the existing `zoomButtons.current` bridge idiom as `recenterRef.current`. The latch is mirrored into React **on transitions only** (`if (manualPan) return` in `latchManualPan`; a fence test asserts `draw()` contains no `setPanned`) — never per pan event, never per frame. Owner-flagged taste call, done: always visible, muted while following, **accent-lit** while overridden. Measured: ◉ centres to **0.0 m** from the eye, following resumes and re-tracks to the **18.5 m** deadband edge. **(3) ALL map attribution → ONE very thin full-bleed line on the SCREEN's bottom edge, under the time strip.** New `--mw-credit-h 0.85rem`; the two bottom-anchored time surfaces are **lifted by it, never z-bumped** — `body.mw-open .ts { bottom: calc(2.2rem + var(--mw-credit-h)) }` and `body.m.mw-open .m-bottom { bottom: calc(var(--mw-credit-h) + env(safe-area-inset-bottom)) }` (the dock's mw-open `padding-bottom` drops its safe-area term — the credit bar owns the inset now, `box-sizing: content-box` so the inset ADDS to the line height). `.mw-creditbar` is a **SIBLING of `.mw`**, not a child: `.mw` carries the centring/drag transform, which would be the containing block for any `position:fixed` descendant and trap the bar inside the desktop window. `pointer-events:none` on the bar, `auto` on the anchor only; `white-space: nowrap` with viewport-scaled type (`clamp`) so the contractual source list never truncates. **Shell split discovered in browser verification (a real collision the spec did not anticipate):** on desktop `index.astro`'s page-level `.map-credit` ALREADY pins an attribution line to the bottom edge whose source list is a strict **superset** of the map window's (Cesium ion · OpenMapTiles · Copernicus · NASA · Gaia on top of Esri·CARTO·OSM) — the new bar drew straight over it (two overlapping lines, first desktop shot). Resolution: `.mw-creditbar` is **/m-only** (the `.mw-pip` precedent — /m has no page credit at all) and desktop instead **promotes the page line** to the same full-bleed bottom bar while `body.mw-open`, via a new inert `.map-creditbar` wrapper (`pointer-events:none`; the anchor re-enables itself on its BASE rule, so the link stays clickable in BOTH states — the closed-state check caught that regression). Result on both shells: exactly ONE bottom line, unclipped, with the scrubber/dock riding above it. **Verify + supersede (house rule):** `scripts/verify-qaslice-cab.mjs`'s *"A: walking re-armed the follow"* check is **annotated as superseded and inverted**, plus new checks for the off-bounds hold, the ◉ button (present · lit · centres · unlights · following resumed) and the attribution bar; a **DESKTOP leg** was added to that script (it was /m-only) covering the ◉ seat, the mouse-drag latch, the promoted page bar and the closed-state restore. `scripts/verify-uxbatch7.mjs`'s *"credit re-seated to the top band"* check is likewise annotated + inverted to bottom-anchored. NEW `test/styles/mapWindowChrome.test.ts` (11 tests, the swTileCache/hiddenPairs source-fence idiom) locks the cross-file contract: no re-arm survives, `manualPan = false` has exactly two sites (declaration + ◉), the transition-only mirror, the sibling-not-descendant bar, the `--mw-credit-h` lift in all three files, the no-double-inset rule, the round-button geometry and the /m PiP clearance. Files: `src/components/panels/MapWindow.tsx` · `src/styles/map-window.css` · `src/styles/time-scrubber.css` · `src/styles/mobile/fpv.css` · `src/pages/index.astro` · `scripts/verify-qaslice-cab.mjs` · `scripts/verify-uxbatch7.mjs` · `test/styles/mapWindowChrome.test.ts`. **Gates: vitest 1,139/1,139 (102 files) · `astro check` 0 err / 5 hints.** Browser-VERIFIED both shells: `verify-qaslice-cab.mjs` **39/39 ALL PASS** (shots `qsl-01..05`, `qsl-04b`, `qsl-04c`, `qsl-06`, `qsl-07`) + **all seven** regression suites (uxbatch4 / s2 / s3 / uxbatch5 / uxbatch6 / uxbatch7 / qa7ab) ALL PASS, Chrome restarted between suites. T1 device-pass riders added: the ◉ seat under the /m PiP, the 0.85 rem line's real-device legibility, and the permanent-override feel. Log: `mem:project/wip-2026-08-22-owner-microslice`.
- **2026-08-22 · OWNER ADDENDUM after device-testing the 21h slice — QUEUED, no code this session** (the owner ordered the audit/docs/guide work deferred, so this is a scheduling entry; the spec lands in `NEXT_SESSION_PROMPT.md` §0 as the FIRST next-session task, ahead of the audit). Three items, all on the expanded minimap: (1) **the manual-pan override becomes PERMANENT** — the 21h eye-motion re-arm (`FOLLOW_REARM_M 0.5`) is REVERSED by owner ruling after testing: once the user pans, the chart NEVER auto-recentres, not even when the walking eye leaves the visible chart bounds; default follow (me + radar centred) is unchanged until that first pan. Delete `FOLLOW_REARM_M`, `manualAnchor` and the whole re-arm block; keep `manualPan` armed by `panBy` + the 2-pointer pinch branch, and the `&& !manualPan` follow guard. The radar/focal-cone rule is EXPLICITLY unchanged (anchor-drawn, so they scroll off with the eye — verify, don't edit). (2) **NEW round RE-CENTRE button** on the map's right edge UNDER the +/− zoom buttons (`.mw-recenter` on the `.mw-btn` base, equal w/h so the pill reads round; seat = the `3.1rem` rung `.mw-credit` vacates in item 3, incl. its safe-area-inset-top /m variant): glyph **◉** (alternates ⦿/⌾ — ⌖ and ◎ are taken by FIND IN FRAME and SAVE), `aria-label="Centre the map on me"`; action = centre on `aimAnchorNow()` (the SAME ladder the radar uses), clear `manualPan`, redraw ⇒ following resumes until the next pan; wired through the existing effect→JSX bridge idiom (`zoomButtons.current = zoomBy` → add `recenterRef`), with the latch mirrored into React state ONLY on transitions for a lit/idle look (taste call, flagged to the owner). (3) **ALL map attribution → one very thin single line at the BOTTOM EDGE of the screen, directly below the time strip**, replacing the current under-the-top-row seat (`.mw-credit`, map-window.css:152-166 · MapWindow.tsx:968-975). Needs real bottom-band coordination, not a z-bump: a `--mw-credit-h` (~0.85rem) that offsets the /m dock (z-24 under `body.m.mw-open`) and the desktop TimeScrubber (z-43) upward while the map is open; one line, no wrap, ~0.45-0.5rem, `pointer-events:none` except the anchor (never steal an edge drag), `padding-bottom: env(safe-area-inset-bottom)`; the FULL Esri/CARTO/OSM source list stays LEGIBLE (contractual attribution — thin is fine, truncated is not). RIDER: `scripts/verify-qaslice-cab.mjs`'s check "A: walking re-armed the follow" is SUPERSEDED by item 1 — invert it (walking leaves the chart put, even past the bounds) + add a ◉-button check, and ANNOTATE the superseded check rather than deleting it (the discipline the audit's Track C is auditing for).
- **2026-08-21h · PRE-AUDIT QA SLICE C→A→B SHIPPED (owner order 2026-08-21g-end) + AUDIT #3 STARTED, results handling RESCHEDULED (owner mid-session order: wrap immediately, everything remaining → next session).** (C) **CRITICAL regression KILLED — overlay-composite rebuild storm**: QA-7b's per-frame resolver was two-way (512 on the flat chart / tier base off it) ⇒ `setOverlayResolution` fresh-instance rebuild on EVERY 2D↔FPV/3D flip. Fix = NEW pure `stickyOverlayPx()` (lib/globe/quality) — the effective px only RATCHETS UP (first flat visit or a governor promote; ≤1 post-boot rebuild per rung), never lowers on a mode flip OR a governor demote (the S3-era rebuild-on-demote folds under the same rule); `overlayPxEff` seeded 0 in StylizedTiles (frame-1 ratchet == constructor px). Root cause 2 CONFIRMED from UpdateOnChangePlugin.js source (update() gated on camera-MVP + needsUpdate; preprocessNode self re-arms as nodes stream) ⇒ ONE-frame refinement kicks added in imageryGround `setOverlayResolution` (post-rebuild) AND `setQualityTier` (error-target change on a parked camera = same stall class). NEW DEV probe `window.__overlayRebuilds` — THE storm assert (raw Esri GET counts CANNOT isolate the storm: the pre-existing LRU rest-trim churn re-fetches at the same magnitude). Verified: 1 rebuild at boot settle, **0 rebuilds across two full 2D→FPV→2D cycles**, uFtwFade holds ≈1. **RESIDUAL QUANTIFIED (pre-existing, NOT the regression — owner should know):** ~600 Esri GETs per flip leg from ground-LRU rest-trim churn — lruCache probe rests at exactly minBytesSize (145/144 MB, cap 192 on the headless `low` tier): the cache can't hold both the chart set and the FPV street set (disk-cache hits on desktop; network on iOS's small cache — exactly the #15 SW-cache motivation; a real phone runs `mid` → 320 MB). Rollback knob unchanged: `overlayResolution2dPx→256`. (A) **expanded-minimap follow YIELDS to manual exploration** — `manualPan` latch in MapWindow (armed by any pan/pinch incl. zoom/twist-only pinches; wheel/chip zoom deliberately does NOT latch), cleared ONLY by the eye-motion detector: distance-from-LATCH-ANCHOR > NEW `FOLLOW_REARM_M 0.5` m (anchor-distance, never per-frame delta — a 20 Hz stroll moves ~7 cm/frame and standing jitter must never accumulate; null anchor ⇒ first live camGeo clears = FPV-entry teleport; the place-point re-seat teleports camGeo ⇒ correctly re-arms, owner QA-1 rule; heading/focal edits move nothing ⇒ structurally never re-arm). Verified: drag 142 m away → holds 0.0 m over 3 s standing; walk 119 m → recentres to 18.5 m (= the 0.12 deadband edge at z18/390×844, exact). (B) **screen-relative walk on the expanded chart, BOTH shells, one code path** — NEW pure `chartWalkAzRad(x,y,rot)=atan2(x,y)−rot` (lib/geo/slippy; unit tests round-trip every direction THROUGH the chart transform), published live via NEW `store/minimap.mapWindowRotRad` (seeded 0 on open BEFORE first draw, deadbanded 1e-4 per paint, nulled on close; pipRect idiom) + NEW DEV probe `window.__mapWindowView`; the orchestrator walk basis flips to ENU chart-up/chart-right while rotRad ≠ null (east = polarZ × up, the _skyEast idiom — the _sky temps are free there), camera-relative basis untouched everywhere else (desktop arrows/WASD ride the same block). Verified on a twisted chart (rot ≈ −270°): stick-up world track 270.0° vs chart-up 270.0° — **Δ 0.0°** with camera heading 0°. Files: lib/globe/quality.ts · lib/geo/slippy.ts · StylizedTiles.ts · scene/imageryGround.ts · panels/MapWindow.tsx · store/minimap.ts · global.d.ts · tuning.ts (docs) · tests quality(+8)/slippy(+4) · NEW `scripts/verify-qaslice-cab.mjs` (17 checks, shots qsl-01..05). Gates: **vitest 1,128/1,128** · astro 0 err/5 hints · qaslice-cab 17/17 + regressions uxb4/s2/s3/uxb5/uxb6/uxb7/qa7ab ALL PASS (verify-chrome restarted between suites). **AUDIT #3 (Deep, owner-scoped: batch #4/#5/#6 + QA-batch + this slice's seams) STARTED same session:** checklists RE-MINED first (code.md +23 injected-GLSL uniform declaration · +24 per-frame writers of construction-time values must be sticky/monotone · +25 event-swallow/shared-CSS lifecycles; tests.md +10 the six verify-harness environment classes) · **Track E COMPLETE** (numbers in NEXT_SESSION_PROMPT §0 — bundle 33 MB accounted · npm audit --omit=dev 9 = baseline-identical · jscpd anchors MapWindow↔MiniMap 29-line clone + aimCones↔focalCone 14) · 4 parallel finder agents (A1 radar/MapWindow/chrome · A2 orchestrator/render/debris · C tests · D docs/guide) were LAUNCHED; the owner then ordered an immediate wrap, and ALL FOUR agents independently terminated on an API **session limit** before returning reports (owner also re-logged-in + switched model mid-session — no work on disk was affected; gates re-confirmed after). Their transcripts were HARVESTED before wrap and the recovered verdicts are carried verbatim into NEXT_SESSION_PROMPT (the "RECOVERED partial findings" block) as PRE-VERIFICATION leads (the mandatory verification pass never ran on them): A2 — checklist-23 GLSL header-decl sweep CLEAN on all four chained-shader sites (imageryGround all 16 uniforms declared · buildingMaterial vertex+fragment+edge · enrichedBuildings treeMat), `src/pages/api` untouched by the batches (building-overrides.ts is U8's own session), and an independent confirmation that imageryGround's constructor arg == the tier px so the sticky frame-1 write is a true no-op · C — CANDIDATE: `verify-uxbatch5.mjs`'s FPV-entry check keys on `.m-joy`, which mounts UNCONDITIONALLY at /m boot (MobileShell.tsx:85) ⇒ passes for the WRONG reason, flagged as a recurring pattern across scripts; CANDIDATE: nothing fences the checklist-23 trap; PASSES: suite 1,128/1,128 in 4.52 s, no script reads `performance.getEntriesByType` (zero-result validated by positive control), all 12 new tunables/functions have test anchors, FOV inverse pair transitively pinned, and **`chartWalkAzRad`'s test convention matches MapWindow's real `fwd` (R(rot), tile-north = −y) exactly** — independent confirmation of this session's QA-slice B math; T31/T32 confirmed OPEN · D — ARCHITECTURE.md current only to 2026-08-18, `conventions/globe-tuning.md` has ZERO hits for ALL new tunables (positive control passed), controls/ tier + focalCone.ts + plannedView.ts + sw.js + api/building-overrides.ts + 2 stores undocumented, UXBATCH4_PLAN fully shipped ⇒ archive confirmed, no knip config, README.md:166+195 still claim "1,004 tests (2026-08-18)" vs 1,128, guide shots all 08-15 except shell-m 08-20 · A1 — died in its first read pass, NOTHING recovered, full re-run. Finder pass RE-LAUNCHES fresh next session IN TWO WAVES (A1+A2, then C+D — a 4-way fan-out is what exhausted the limit; prompts + scope in NEXT_SESSION_PROMPT §1). **NEXT SESSION: the owner's 2026-08-22 micro-slice FIRST (NEXT_SESSION_PROMPT §0 — permanent manual-pan override + ◉ re-centre button + bottom-edge attribution line), THEN audit results handling (re-launch finders in two waves → verification pass → report → audits/) + docs reorg + guide work — full brief NEXT_SESSION_PROMPT.md.** Log: `mem:project/wip-2026-08-21-qaslice-cab`.
- **2026-08-21g · QA-7 a+b SHIPPED (same session follow-up — owner: "lets try both a + b, i will judge final performance issues in real devices").** (a) **PHOTOGRAPHIC 2D chart** — NEW `GROUND.flat2dPhotoK 1` → `uFtwPhotoK`; the grade shader lerps the WHOLE stylized treatment out by `photo = uFtwFlat2d × uFtwPhotoK × (1 − uFtwDark)` (shade→1, desat→0, gain→1, cast→neutral, water-darken/golden/moonlit/ambient→off) — the /m 2D chart (and the desktop nadir flat-map, same latch — one-flat-treatment doctrine, taste item) now shows RAW Esri colorimetry like the MapWindow canvas; dark-CARTO mode untouched; headed-Chrome A/B shots qa7-08/-09 are night-and-day · (b) **crispness** — `TILESETS.esriMaxLevelCoarse 17→18` + NEW `QUALITY.leanMobile.dprCap2d 1.5` applied ONLY while `TilesHandle.mapFlat()` (GlobeCanvas tick flips and re-applies the tier; FPV/3D keep the 1.25 heat cap) + the REQUIRED third lever NEW `GROUND.overlayResolution2dPx 512` (the ImageOverlay level chooser derives source zoom from resolution/rangeWidth — the 256 lean composite alone pinned the chart one level shallow even with the cap at 18; `stepGroundUpdate` is now the ONE writer of the effective composite px). z18 fetches CDP-verified (35 tiles at 220 m); DPR verified tier-consistent via NEW DEV probe `window.__globeQuality` (headless governs to `low` so 1.25 binds there; a real mid-tier phone reaches 1.5). EXPENSIVE TRAP RECORDED: adding a uniform to imageryGround's JS `uniforms` object is NOT enough — the fragment-header injection must DECLARE it (`uniform float uFtwPhotoK;`); missing it makes the new program fail compile while tiles keep rendering the PREVIOUS program, so every live uniform poke silently no-ops (gain/fade/photo all read "inert" for ~40 min). Also: count tile fetches via CDP Network (resource-timing buffer caps at 250); /tmp/ftw-cdp persists prefs across verify sessions. Files: tuning.ts (dprCap2d · esriMaxLevelCoarse 18 · flat2dPhotoK · overlayResolution2dPx) · imageryGround.ts (photo blend + header decl + uFtwPhotoK) · GlobeCanvas.tsx (leanDprCapNow + flat-flip re-apply + __globeQuality) · StylizedTiles.ts (tierOverlayPx + stepGroundUpdate raise + GROUND import) · quality.test locks (+4 expects) · NEW scripts/verify-qa7ab.mjs (6 checks) · verify-uxbatch4-s3.mjs (DPR check superseded → ≤1.5 on the chart). Gates: vitest 1,116/1,116 · astro 0 err/5 hints · qa7ab ALL PASS + uxbatch7 ALL PASS + uxbatch4-s3 ALL PASS. ON-DEVICE JUDGMENT (T1, owner): chart crispness/brightness vs heat + the 2D↔FPV composite-rebuild feel; rollback knobs: esriMaxLevelCoarse→17, dprCap2d→1.25, overlayResolution2dPx→256, flat2dPhotoK→0 (each independent).
- **2026-08-21f · OWNER QA BATCH — 6 fixes + 1 answered question (post-batch-#6 device QA; the owner's two item-7 screenshots arrived as byte-identical placeholder icons AGAIN — the batch-#5 corruption — item 7 was answered from pipeline code).** (1) **expanded-minimap radar follows the viewer** — MapWindow `aimAnchorNow` is camGeo-first WHILE FPV IS LIVE (`(fpvHud ? camGeo : null) ?? tempPin ?? camGeo ?? focus`; outside FPV batch-#6 pin-first stands), the per-frame temp re-pose gained a `fpvPinKey` change re-seat (fresh ENU basis at the NEW pin, heading/elevation carried, `fpvWalkOffset` zeroed — the stale-basis+stale-offset detach root cause; verified eye 0.0 m from pin, map stays open), the chart rubber-band FOLLOWS the walking eye (`FPV_FOLLOW_FRAC 0.12`, never during a gesture; supersedes the batch-#5 no-recentre note for the FPV-live case), and the radar SIZE is unified via NEW `AIMCONES.mapRadiusHK 0.5` — rBase = canvas h × 0.5 (× mobileRadiusK on /m), the GL fan's fraction-of-height equivalent, replacing 0.3×min(w,h) (≈3.7× too small on a phone; draw + tap-promote hit test both edited) · (2) **FPV entry preserves the focal cone** — the no-share temp entry (LOOK FROM HERE / placed point) now builds its basis from `plannedView.headingDeg` (the old camera-forward projection DEGENERATED TO NORTH at the /m 2D nadir) and its FOV from `verticalFovDeg(plan.hFovDeg, camera.aspect)` (sensors.ts — the inverse of horizontalFovDeg, newly wired into the orchestrator); SceneActions long-press ▲3D and desktop MapWindow viewFromHere are plan-first too (old lastFpvFovDeg/north as fallback; the plan re-seeds on FPV exit so batch-#4 "last focal" survives inside it) — verified entry heading 137.0 exact, fov 76.5 = computed · (3) **radar occlusion gaps** — research verdict: gaps NEVER existed on any radar surface at any commit (7 git -S probes + U4-birth blobs + S2 diff; the owner remembered the time-rail trace + horizon rise/set gaps), so this shipped as a NEW capability: pure `fractureRunsBySkyline` in azSector.ts (altDeg ≥ skyline(azDeg) keeps a sample; <2-sample sub-runs dropped; null sampler ⇒ untouched), band fills + rim arcs fracture on ALL THREE surfaces (rise/set spokes + direction lines stay whole), fed by store/plan `profileBins`+`sampleBins` (canvases) and an orchestrator-resolved `skylineBins` ctx field (GL; array identity = rebuild key) behind NEW `AIMCONES.skylineGuardM 60` (bins apply only when the swept eye sits ≤60 m from the radar anchor — the traceStates honesty rule); verified: profile ready 13.9 m from eye, arcs visibly fracture (uxb7-06); GL-fan visual = T1 rider (needs a placed photo) · (4) **time scrubber on the expanded map, NO new component** — /m lifts the REAL dock (`body.m.mw-open .m-bottom` z 24; `.m-peek/.m-tabs` display:none so the dock sits flush, mounts stay warm; dock inherits the safe-area padding), desktop raises the REAL TimeScrubber (`body.mw-open .ts` z 43 over the z-42 window); the bottom `.mw-hint` is RETIRED (desktop wording → `.mw-tophint` in the top row) and `.mw-credit` re-seats top-left (Esri attribution kept; /m max-width clears the PiP) · (5) **/m chips under controls** — `.fh-chip` and the joysticks were both fixed z-10 in one stacking context and the chips island mounts last in m.astro (paint-over + touch-steal); `body.m .fh-chip` z 9 · (6) **search** — placeholders "TYPE TO SEARCH — e.g. m31 · moon · orion · ngc 7000" (desktop) / "Type to search — e.g. m31 · moon · orion" (/m) + earth twins; the iOS dark-screen was React `autoFocus` firing while the sheet sat at translateY(100%) (Safari scrolls the LAYOUT viewport to the off-screen input; Android resizes the visual viewport only) — now a mount useLayoutEffect `focus({preventScroll:true})` in the same discrete-event commit (keyboard still opens) + scrollTo(0,0) pins via rAF/550 ms/visualViewport-resize; REAL-iOS feel UNVERIFIED (T1) · (7) **map-quality answer**: /m 2D map = GL pipeline vs minimap = raw MapWindow canvas, same Esri tiles — the gap is `esriMaxLevelCoarse 17` vs z19 (4× linear texels, dominant), DPR 1.25 vs 2 (1.6×), and the stylized ground grade (gain 0.60/desat 0.52/waterDarken 0.35 ⇒ the whole brightness delta) — all deliberate iOS-jetsam levers except the grade; zero-cost candidate = lerp the grade out by the existing `uFtwFlat2d` uniform, owner to rule. NEW TRAPS: headless verify-Chrome EXHAUSTS WebGL contexts across suites (tabs never close — "BindToCurrentSequence failed"; restart verify-chrome between suites — the s3/uxb5/uxb6 "failures" this session were all this) · Vite dep-optimizer 504 "Outdated Optimize Dep" after new imports into the globe bundle — restart wix dev before browser work. Files: tuning.ts · azSector.ts · scene/aimCones.ts · StylizedTiles.ts · MapWindow.tsx · MiniMap.tsx · SceneActions.tsx · MobileSearch.tsx · LocationFinder.tsx · fpv-hud.css · map-window.css · mobile/fpv.css · time-scrubber.css · tests azSector(+5)/aimCones(+2) · NEW scripts/verify-uxbatch7.mjs. Gates: vitest 1,116/1,116 · astro 0 err/5 hints · regressions uxb4 23/23 + s2 16/16 + s3 18/18 + uxb5 17/17 + uxb6 12/12 + NEW uxb7 22/22 (shots uxb7-01..06). Log: `mem:project/wip-2026-08-21-owner-qabatch7`. NEXT: the standing owner order 2026-08-21e — AUDIT PASS + docs/guide reconcile (now incl. this batch's seams) · release still gated on the domain fix (rides it) · T1 grown (iOS search focus, gaps-on-GL-fan, dock ergonomics, QA-7 knob ruling).
- **2026-08-21e · OWNER BATCH #6 — four fixes on batch #5 (same session; owner screenshot arrived intact this time).** (1) **placed point OWNS the map radar** — MapWindow `aimAnchorNow` reordered to `tempPin ?? camGeo ?? focus` (the walking-FPV camGeo used to outrank the pin, so after a place the radar kept a "buggy offset" glued to the viewer; GL orchestrator was already pin-first). Side discovery, judged GOOD and kept: a STANDING temp FPV re-derives its camera from `tempPinPoint()` every frame, so placing a point also relocates the under-map FPV → the PiP previews the NEW standpoint → tap = you are there (fills the no-jump-affordance gap flagged in batch #5; a walked FPV keeps its walk track). (2) **band stack REORDERED + COMPACTED** (supersedes the batch-#4 "sun inner, moon outer" sketch): moon INNERMOST, sun directly above, target a small gap above the sun at 3× the band width — desktop `bandMoon [0.3,0.38] · bandSun [0.42,0.5] · bandTarget [0.55,0.79]`, mobile `[0.24,0.32] · [0.34,0.42] · NEW bandTargetMobile [0.46,0.7]` (the rim-clipped [0.55,1] zone read "too far out and huge"); `bandFor(key,mobile)` returns the target variant now; the N marker rides `bandTarget[1]×northOffsetK` on GL + MapWindow (a fixed unit-1.09 seat would float off the compacted ink; minimap keeps DOM `.mm-n`). The "lost moon band" = the old silver ring reading invisibly on bright ground (+ possibly a session-dismissed MOON direction) — it draws innermost now, brightness taste rides T1. (3) **focal cone FROM BOOT** — `stepPlannedView` seeds a null plan eagerly (live view heading + `horizontalFovDeg(FPV.tempFovDeg, camera.aspect)` — the stick's first-touch seed done at boot; later photo/jump/FPV-exit/stick seeds overwrite); **aim stick mm readout** — NEW pure `focalMmFromHFov()` (lib/geo/plannedView, 36 mm full-frame width, unit-tested ×2) + `Joystick` grew a `footer` prop; AimJoystick subscribes `fpvHud → horizontalFovDeg(live)` else `plannedView.hFovDeg` and renders `formatFocal(mm)` on the pad's lower body (`.m-joy__footer`, focal-cone ink). (4) **/m aim stick re-seated** — OFF the minimap corner (`.m-joy--aim-minimap` is desktop-only now; MiniMap gates on `!mobileShell`), ONE /m instance in MobileShell: `variant={fpvOn ? "fpv" : "map"}` — `.m-joy--aim-fpv` sits just above the WALK stick (same left rail, 108 px pad + 12 px gap) and rides the existing `body.mw-open` z-24 rung, which un-loses the stick in the /m fullscreen-map view (batch #5 had hidden the `.mm` card that carried it). Gates **vitest 1,109/1,109** (+2 focalMm; band tests rewritten to the new order incl. 3×-width + off-rim invariants) · astro 0 err/5 hints · verify: S1 23/23 + s2 **16/16** (2 SUPERSEDED checks: plannedView now SEEDS at boot; /m stick above walk, corner retired) + s3 18/18 + uxb5 17/17 (press point moved off the new stick rail — it landed on the z-24 aim pad) + NEW `scripts/verify-uxbatch6.mjs` **12/12** (boot plan desktop hFov 79.6°/m 27.1° → "75 MM" footer; aim y+h ≤ walk y same rail; corner gone; stick visible over the map; pin moved w/ map open; shots `uxb6-01..05` incl. a dedicated low-alt /m band-stack close-up — moon innermost CONFIRMED drawn). **TRAPS (new):** the /m left rail is now TWO stacked z-24 pads — synthetic map long-presses below x≈126 land on the sticks, not the chart; `wix dev` on :4321 DIED mid-session (curl 000) — restarted, the "engine booted" waitFor guard is now in uxb6 for cold rebundles. **UNVERIFIED (rides T1):** moon-silver band readability on bright ground (bump candidate: moon rim/wash alpha), 108 px aim-pad size taste (owner's "keep current size" read as the standard pad, not the 72 px corner), place-point-relocates-standing-FPV feel. Memory: `mem:project/wip-2026-08-21-owner-uxbatch6`.

- **2026-08-21d · OWNER BATCH #5 — six post-batch-#4 fixes (/frame 4-scout cited fan-out; owner's screenshots arrived as broken placeholder icons — worked from the written descriptions + own browser repro).** (1) **radar band RESTING fills** — root cause: fill alpha was ×`emphEased` on ALL THREE surfaces, so only the focused body (default target) ever filled — sun/moon strips read EMPTY; NEW `AIMCONES.fillAlphaRest 0.05` (above the 0.003 shader discard gate, below fillAlpha 0.08): GL `aimCones.ts` uAlpha = `(rest + (fill−rest)·emphEased)·overlayA`, MapWindow + minimap twins always fill (`emphasized ? fillAlpha : fillAlphaRest`, minimap keeps its ×2 patch scale) — body-ink future / grey past now always visible, emphasis just breathes. **Focal-cone edge de-fattened** — the GL half-width lives in RAY-EXTENDED units (×rayLenK 6): old `edgeHalfWidthK 0.0015` ⇒ 3.0× the radar direction line; now **0.000625** ⇒ 1.25× ("a bit more distinct"), `edgeAlpha 0.55→0.7` (brightness carries the reading); canvas twins stroke 1.5·dpr; minimap cone stroked the whole CLOSED wedge (far arc incl.) — now boundary LEGS only like GL/MapWindow. (2) **/m radar shrink** — NEW `AIMCONES.mobileRadiusK 0.8` (GL radius + focalCone reach + MapWindow `rBase`, orchestrator-pushed `mobile: isMobileShell` into both scene updates — the fence forbids scene store/DOM reads; MapWindow hit-test rBase mirrored or tap-promote desyncs) + NEW `bandSunMobile [0.24,0.32]` / `bandMoonMobile [0.34,0.42]` (sun/moon ~20% closer to centre, same 0.08 widths, non-overlap invariants hold; `bandFor(key, mobile)` + both panel twins; minimap gets bands but NOT the radius shrink — its card is already CSS-shrunk to 124px on /m). (3) **/m PiP = true miniature** — the S3 punched hole showed a 1:1 screen CROP and the z-10 MiniMap card (fully inside the hole, between GL z-0 and map z-20) bled through ("minimap inside minimap"). Now: `.mw-pip` sized **32vw × 32dvh** (EQUAL viewport fractions ⇒ box aspect ≡ screen aspect — the trick that lets the engine reuse the live camera untouched), MapWindow publishes the measured box (deadbanded) to NEW `minimap.pipRect`/`setPipRect` (cleared in the open-effect cleanup), `TilesHandle.pipRect()` mirrors it (orchestrator owns store facts — GlobeCanvas stays store-free), GlobeCanvas ticks ONE scissored `renderer.render(scene, camera)` into the rect after `composer.render()` (setViewport/setScissor take CSS px — three applies DPR itself; Y-flip vs innerHeight; viewport restored after — the composer reads it next frame; tone map + sRGB apply natively on the backbuffer and bloom is already off on /m [leanMobile], so the look matches); mid-stack chrome hidden while the /m map is up (`body.m.mw-open .mm/.m-fpvhud/.fh-chip { visibility: hidden }` — mounted, subscriptions warm). (4) **/m place-point stays on the map** — `viewFromHere` was `requestFpvJump` (= `setTempPin` + `setTempFpv(true)`) + close; on /m it now ONLY `setTempPin` and returns (window stays; `wantKind === fpvKind` so the live FPV under the PiP never re-enters; the document-capture click swallow now guards the trailing click from radar tap-promote instead of the unmount retarget); hint → "LONG-PRESS — PLACE POINT"; desktop unchanged. (5) **/m dock date+time = desktop twins** — root cause found in the S1 #12 diff: deleting the `.md-rate` CSS block took `.md-date`'s entire rule body, leaving `.md-date,` dangling onto `::-webkit-calendar-picker-indicator { filter: invert(0.7) }` — the WHOLE input was inverted, unstyled, light-scheme; rule rebuilt as the `.ts-date` twin (font inherit / 11px / border / radius 6 / `color-scheme: dark` / indicator-scoped invert + `:focus-visible` accent) AND the read-only `.md-clock` span replaced by a native `<input type="time" class="md-date md-time">` with `onTimeChange` = the desktop `withLocalTime` null-guarded handler (time PICKING existed on desktop only; tint classes dropped — desktop's time input is untinted, offset chip + cursor carry the signal); `verify-uxbatch4.mjs` check updated `.md-clock`→`input.md-time`. (6) **shell-switch pose carry** — /m always honored `#p=`/`#f=` at boot but every desktop→/m link was a bare `href="/m"` (hash dropped ⇒ `MOBILE2D.boot*` 1100 km default), and a carried oblique tilt ≥`twoDMaxTiltDeg 10` would land 3D; NEW pure `mobileShellHash()` (urlPose.ts, unit-tested): `#p=` re-formats with **tilt 0** (2D door; alt/coords/heading/`&t=` preserved), `#f=` passes EXACT, garbage → ""; wired at click time via an index.astro PROCESSED module script (both `a[href="/m"]`: topnav + coarse banner) + Welcome CTA onClick (mirror is welcome-suppressed → usually plain /m, correct) + the /m DESKTOP chip carries `location.hash` raw onto `/?d=1` (desktop honors both forms; the MobileAccount click-time returnTo idiom). Gates **vitest 1,107/1,107** (+6: mobileShellHash ×4, mobile bands, fillAlphaRest) · astro 0 err/5 hints · regressions S1 23/23 + S2 15/15 + S3 18/18 ALL PASS + NEW `scripts/verify-uxbatch5.mjs` **17/17** (shell-switch probe landed `mapMode 2d` lat 48.464 lon 35.046 alt 2494 m from a 2495 m desktop pose — no 1100 km; PiP box 124.8×270.1 @390×844 = exact 32vw/32dvh; hole alpha 0/chart 255; `.mm` visibility hidden; place-point pin moved with map still open; DESKTOP chip carried `#f=` verbatim; shots `verify-shots/uxb5-01..05`). **TRAP (new):** /m re-mirrors the LIVE camera into `location.hash` within ~1.6 s of boot (`urlPoseEveryFrames`) — a verify script can NOT assert the transformed link hash post-boot, assert the boot RESULT (store probe) instead. **UNVERIFIED (rides T1):** real-device PiP scaled-pass perf/feel, band-wash taste (fillAlphaRest 0.05, ×2 minimap), mobile radar 0.8 taste, iOS native time-input popover look. Memory: `mem:project/wip-2026-08-21-owner-uxbatch5`.

- **2026-08-21c · OWNER BATCH #4 S3 — BATCH COMPLETE (addendum-#2 items 17+18 + #15 network + #5 iOS resilience + #1 /m PiP; /frame 4-scout cited fan-out).** (17) **radar sun/moon body-tinted future** — NEW `bandFutureInk()` exported from `scene/aimCones.ts` (sun→sunGlow, moon→moonDial, target keeps timeFuture; unit-locked); `makeSectorMaterial(futureHex)` sets per-body `uFuture` at creation (fill+rim; rise/set spokes untouched; the `step(uNow01,vT)` split unchanged); MapWindow twin + minimap radar pick future ink per body via their existing `b.color` (minimap reads the tokens bridge — NO `--color-moon-dial` CSS var exists, deliberate). (18) **TargetPanel GOTO before SHOW** — the chip's aim handler EXTRACTED to `store/skyAim.gotoSkyBody(kind)` (skyMarkers mirror when present, else live `targetAzAlt` fallback at `camGeo ?? focus` so the button works with SHOW off; below-horizon → `nextRiseAzimuth` at the horizon; pure decision twin `gotoAimSolution` tested in NEW `test/store/skyAim.test.ts`); SkyGotoChips delegates to it; the pill is NOT gated on `visible` (the TRACK precedent). (#5) **iOS resilience** — GlobeCanvas: `webglcontextlost/restored` handlers (three already preventDefault()s + re-inits GL — the app half is a `ctxLost` render gate + composer realloc on restore) and the tick now SKIPS hidden pages (iOS delivers throttled hidden rAF = heat) while re-seating the governor clock (no giant-frame tier shed on return); StylizedTiles: `visibilitychange`/`pagehide`/`pageshow` freeze ALL NINE tile queues (3 renderers × download/parse/processNode via `PriorityQueue.autoUpdate` — the library's one scheduling gate, orthogonal to the tier `maxJobs` caps) with `scheduleJobRun()` re-kick on return; NEW **`QUALITY.leanMobile {dprCap 1.25, bloom false, shadowMapSize 1024}`** applied as coarse-pointer renderer-lever OVERRIDES on top of whatever tier the governor runs (tile knobs stay per-tier; shadows stay ON at 1024 — the mid look survives, unlike tier `low`; `high` byte-identical untouched, test-locked). (#15) **network** — (a) NEW **`public/sw.js`** iOS-DIRECTED tile cache (registered from BOTH layouts ONLY for iOS-like devices [iPhone/iPod/iPad UA, or Macintosh UA + maxTouchPoints>1 = iPadOS] and NEVER on localhost): cache-first Cache-Storage over Esri/CARTO/assets.ion.cesium.com/tiles.openfreemap.org/*.workers.dev, FIFO cap 6000 entries ≈300 MB + 7-day per-entry max-age (**Esri ToS posture: a performance cache, never an offline extract** — the TILESETS UNVERIFIED-for-prod note stands, flagged for the owner), NEVER caches tileset.json/layer.json/the `/planet` TileJSON/api.cesium.com (token endpoint — not allowlisted); policy fenced by NEW `test/swTileCache.test.ts` against the tuning source of truth. (b) **demand shrink**: per-tier `overlayResolutionPx` (512 high / 256 mid+low — the REAL network lever: calculateLevel picks the Esri source zoom from resolution, ≈4× fewer GETs) — `imageryGround` gains the **`setOverlayResolution` rebuild path** (plugin.resolution + FRESH overlay instances via the plugin's own delete→add idiom — re-adding the SAME instance NESTS the download-queue fetch wrapper; the U6 visibility-guard patch + load-error retry closure ride the plugin instance and survive), construction now takes the tier resolution so the attach-time fan-out no-ops; NEW `TILESETS.esriMaxLevelCoarse 17` (coarse-pointer Esri depth cap; desktop keeps z19); ground-ONLY LRU raise `groundLruBytesMB` 320 mid / 192 low (high 400 = lruBytesMB, byte-identical; buildings/enriched stay at the shared cap — blanket raise worsens jetsam). (c) **per-URL force-cache** (immutable binaries only): Esri/CARTO overlay images (`overlay.fetchOptions` — an overlay fetches only tile images, kills the measured ~60/reload revalidations), terrainPatch `.terrain`, NEW `FTW_ENRICHED_FORCE_CACHE` `.glb` claimer on the enriched renderer (tileset.json declines → keeps revalidating), openfreemap `.pbf` (dated build path; the TileJSON keeps the default mode). (#1) **/m PiP** — NEW `.mw-pip`: a transparent 200 px button top-right of the fullscreen map (draw() `clearRect`s the canvas under its exact DOM box — CSS owns placement so ring+hole can't drift; `body.m .mw` drops its panel background so cleared pixels reach the GL canvas rendering the LIVE FPV view beneath — chrome included, a true PiP); REPLACES ✕ MINI-MAP on /m only (desktop MapWindow untouched); tap → close → back to FPV (FPV persists under the window the whole time). **TRAPS (new):** the dev bundle renames three's pass classes (`_UnrealBloomPass`) — CDP pass probes must match by SUBSTRING; a hash-only `Page.navigate` does NOT reload (the `#p=` pose applies at load) — go `about:blank` first; the city chart pose sits under the mapFlat bloom gate (`mapFlatMaxAltM` 120 km) — assert bloom at a LEO pose, off-on-the-chart is BY DESIGN (2026-08-18e). Gates **vitest 1,101/1,101** (+13: bandFutureInk, gotoAimSolution, leanMobile + #15 tier invariants, SW policy fence) · astro 0 err/5 hints · S1 **23/23** + S2 **15/15** regression ALL PASS + NEW `scripts/verify-uxbatch4-s3.mjs` **18/18** (GOTO steers targetHeadingDeg null→143.06°; /m lean DPR 1.25 + bloom pass off; PiP hole insideAlpha 0 / chart outsideAlpha 255; shots `verify-shots/uxb4-s3-01..04`). **UNVERIFIED (rides T1 + the first prod release):** /sw.js actually served by Wix hosting (Content-Type/scope unprobed — the one #15a risk), real-iOS jetsam/contextlost/heat behaviour, Esri-z17 + 256-composite look on a phone, governor-flip overlay rebuild flash. **Batch #4 CLOSED — 18/18 items** (T1 real-device rider open). Memory: `mem:project/wip-2026-08-21-owner-uxbatch4`.
- **2026-08-21b · OWNER BATCH #4 S2 — radar unify + focal cone everywhere + aim joystick + MapWindow twist + item 16 (/frame + investigate-design-v3 design mode; 3-scout cited fan-out; design locked in `UXBATCH4_PLAN.md` §S2 DESIGN).** (16) **street labels ×0.5** — `STREETS.textPxTarget [15,13,11]→[8,7,6]` AND `textHeightM [22,15,11]→[11,7.5,5.5]` (tuning.ts:1439/1449): the owner's giant riverfront label WAS the world-size floor branch (screen size = max(world-px, pxTarget) via `labelScaleFor` floor-at-1 — halving only the px targets changes nothing at street level). (9) **radar → concentric annular bands** — NEW `AIMCONES.bandSun [0.3,0.38] / bandMoon [0.42,0.5] / bandTarget [0.55,1]` (unit [inner,outer]) consumed by all three surfaces from the SAME tunables: GL fan rebuilt as annular quad strips + inner→outer spokes (`bandFor()` exported+tested), MapWindow twin as outer-arc+reversed-inner paths, and the **minimap GAINS the radar** (in-component `sampleAimDay` memo — the MapWindow idiom; pose channel untouched). **compactK + lineLenK RETIRED** (bands non-overlapping by construction; `radiusTauMs`→`emphTauMs`, emphasis gates FILL only); sun/moon dials cap at their OWN band outer radius; target ray keeps rayLenK 6. **`N` rim marker all surfaces** (GL 64px canvas-raster quad at `northOffsetK 1.09`; MapWindow fillText riding the rotation; minimap keeps DOM `.mm-n`). (state) **planned view** — camera store `plannedView {headingDeg, hFovDeg}` + `plannedRates` (hFov stored HORIZONTAL — no surface ever needs an aspect), session-only; seeds last-writer-wins: photo placement (param-key watch), long-press-jump consume, **FPV exit** (both hud-null branches — the framed shot survives to the planning surfaces), joystick first touch; pure math in NEW `lib/geo/plannedView.ts` (stickRate expo γ / integratePlanned low-pass+wrap+log-clamp / plannedAtRest / `horizontalFovDeg` moved here as canonical — minimapFeed re-exports) + orchestrator `stepPlannedView` (zero store churn at rest). (cone) **focal cone everywhere** — NEW token `--color-focal-cone` **#E08FC6 orchid-rose** (tokens.css + GL bridge; timeFuture/pinIce/cometTail sit too close to the radar's future-blue, lavender = places) + `FOCALCONE` tunables (fill 0.05 / edge 0.55 / hFov clamp 3–120° / rate ceilings 45°/s + 0.9/s log): NEW GL `scene/focalCone.ts` (unit ENU wedge, rebuild only on hFov Δ>0.1°, heading=rotation.z, reach = radar radius × rayLenK, rides the aimCones anchor/band/RADAR-master, hidden in FPV), MapWindow's hardcoded-0.22 block REPLACED (fpvHud live at the eye > plannedView at the radar anchor), minimap cone re-inked. (11) **AimJoystick** — `Joystick` parameterized (raw unit-disc `onVector`); NEW SHARED TIER **`src/components/controls/`** (the mobile fence forbids panels↔mobile; instruments whose FEEL must not fork live in a leaf both shells import — **fence rule 3 added** to `mobileFence.test.ts`: controls/ may import ONLY react+store+lib+globe/tuning+styles). In FPV writes the real `setHeadingRate/setFovRate`; outside seeds+steers the planned view. Mounts: /m 2D/3D map bottom-left (MobileShell) + minimap card bottom-right both shells (72px, knob wears the cone ink). (4b) **MapWindow twist** — `view.rot` + ONE rotation-aware transform `xformNow()` (fwd/inv) replacing the FOUR duplicated zDraw stacks; tiles blit under ctx.rotate with half-diagonal AABB range (+1px overdraw seam guard; texel-snap round kept at rot 0); tap-promote de-rotates; pinch composes twist (1:1) + midpoint pan; north-up reset per open; desktop stays rot 0. **S1 BUG found+fixed:** the long-press trailing click RETARGETS after the pressed chip unmounts (tempFpv flips the chrome within a frame) — it landed on member-gated SAVE VIEW and navigated to the LOGIN page mid-jump (read as a white "crash" in shots); fix = one-shot document-capture click swallow, 900 ms fuse (`SceneActions.jumpHere` + `MapWindow.viewFromHere`). **TRAP: an element-level click-swallow dies with its element.** **Side quest (owner ask): rendering-pipeline audit + cache measurement** — scout-verified ALL UPLIFT optimizations IN-PLACE-WIRED (U5 closest-first incl. queue caps/fpvBias gating, U6 foveation all 3 renderers, mobile tier coarse→mid + DPR 1.5 + 3×256MB LRU + 2k textures; confirmed negatives w/ positive controls: zero contextlost/pagehide handlers). **S3 note: `GROUND.overlayResolution` is a construction-time ImageOverlayPlugin arg — the planned 256-on-mid/low shrink needs a plugin rebuild path.** NEW `scripts/measure-tile-cache.mjs` (cache ENABLED, LOAD→WANDER→RELOAD, both views): Chrome disk cache **HOLDS** (reload ≈95% fromDiskCache, only ~60 ~0-byte metadata revalidations repeat; zero in-session re-fetches over a 6-pan wander) → **request-level cache-busting REFUTED; the owner's desktop-Chrome storm observation ≈ the DevTools disable-cache checkbox; the iOS-small-cache ranking STANDS and the SW mitigation stays iOS-directed** (gap: mobile-view WANDER gesture didn't take on the emulated tab — device re-measure rides T1). Dev-env trap: a `wix dev` predating new module files serves **504 "Outdated Optimize Dep"** → black canvas; restart after adding modules. Gates **vitest 1,088/1,088** (+14: plannedView math, band contract, fence rule 3) · astro 0 err/5 hints · S1 regression suite ALL PASS · NEW `scripts/verify-uxbatch4-s2.mjs` **15/15** both shells (Δheading 16° via minimap stick; FPV-exit seed Δ0.00°; twist pixel-diff; /m stick seeds+zooms plan 27.1°→22.9°; shots `verify-shots/uxb4-s2-01..07`). **UNVERIFIED:** real-device twist/stick feel + band-radii taste (rides T1). **Remaining: S3 = #15 SW tile cache (iOS-directed) + demand shrink (needs the plugin-rebuild lever) + #5 iOS contextlost/pagehide/lean profile + #1 minimap PiP.** Memory: `mem:project/wip-2026-08-21-owner-uxbatch4`.

- **2026-08-21 · OWNER BATCH #4 S1 — 10/15 items (touch correctness + overlay/chrome quick wins; /frame + investigate-design-v3 implement/Deep, 4-scout cited fan-out; plan `UXBATCH4_PLAN.md`).** Owner's post-real-device list (iPhone 17 Pro Safari) organized into 6 tracks / 3 sessions; S1 shipped: (2) **selection tint killed** — global `user-select/-webkit-user-select: none` + `-webkit-touch-callout: none` + `-webkit-tap-highlight-color: transparent` on body (repo had ZERO tap-highlight rules), inputs/textarea/select/contenteditable opt back in (`global.css`, both layouts). (3) **2D-map gesture rework** — tilt-into-3D door REMOVED (`MOBILE2D.enter3dTiltDeg` retired; ▲ 3D chip = only door), two-finger parallel drag now ROTATES the chart: tilt re-locks nadir EVERY frame (kk=1 mid-gesture — same-frame kill, no ease wobble), heading lock stands down during touch-ROTATE and a `mobile2dFreeHeading` latch keeps the user's heading until 2D re-entry / heading glide re-arms north (`stepMobile2dLocks`). (4) **MapWindow pinch CONTINUOUS** — fractional z (tiles at `Math.round(z)+boost`, canvas scaled `2^(z−zDraw)`; the old `Math.round(log2)` integer snap was the owner's "chaotic steps"), damping `PINCH_SENS 0.8`, wheel/chips re-seat integer levels, FPV open z 17→**18**. (6) **target tracking RAY** — `AIMCONES.rayLenK 6` (GL dial, emphasis-independent) + window-edge length on the canvas twin + full-ray tap-promote reach (aimCones.ts / MapWindow.tsx). (7) **vector ink** — fillOpacity 0.5→**0.25** · lineOpacity 0.85→**0.55** · flatLineK 0.55→**0.32** + NEW pref `vectorsVisible` (camera store, rev-2 blob) → desktop **VEC** chip (grid row 3) + /m **▤ VECTOR** in LAYERS; gates `vectorFeatures.update enabled` only (street names stay — content, not wash). (8) **⌖ FIND IN FRAME toggle** seated above UNFOLLOW both shells (`.tp-findframe`/`.m-findframe`; sky-menu body mapping + composite `find.open && bodies[b]` read; desktop closes PLAN first). (10) **long-press ▲ 3D** → `requestFpvJump` at the map centre, current heading, last-FPV focal (session `lastFpvFovDeg` tracker; ORCH 500 ms/6 px shape; click-swallow guard). (12) **/m time dock rework** — PLAY + speed selector REMOVED on /m (desktop scrubber untouched), status-strip TimeChip removed, dock gains `.md-clock` time-only readout (amber past / blue future). (13) **MapWindow desktop** — `usePanelDrag("map-window")` + grip, −10% (`min(57.6rem,84.6vw) × min(72vh,43.2rem)`); **`.mw` overflow:hidden REMOVED** (it clipped the DragGrip tab — the documented inner-wrapper trap; corner clip moved to `.mw-canvas` border-radius). (14) **Guide resizable** — `usePanelResize("guide")` + ResizeGrip, `--win-w/--win-h` reads (supersedes the 2026-08-15e "not resizable" ruling); guide search was ALREADY shipped 2026-08-19d — owner tested a pre-ship build. Guide topics updated (mobile-map/layout/chips/gestures). Gates **vitest 1,074/1,074** (+1 prefs) · astro 0 err/5 hints · browser-verified BOTH shells via NEW `scripts/verify-uxbatch4.mjs` (raw-CDP; **23/23 PASS** — incl. compass −69° after synthetic two-finger drag, no 3D flip; long-press → FPV; VEC pixel-diff 53k px; shots `verify-shots/uxb4-01..11`). **TRAP for the record:** synthetic CDP two-finger gestures need ≤3 px steps — the library classifies ROTATE-vs-ZOOM on the FIRST move past ~6 px and CDP delivers the two pointers in separate tasks, so a coarse step reads as a pinch (state 3) mid-frame. **UNVERIFIED:** real-device gesture feel + iOS Safari behaviour (rides T1). **Remaining: S2 = radar rework (9) + focal cone everywhere + focal joystick (11) + MapWindow twist rotation (4b); S3 = tile-request storm (15: SW cache — headers PROBED FINE on every host, the storm is LRU-eviction re-fetch vs iOS's small HTTP cache; Esri is HTTP/1.1-only) + iOS reload/heat (5: zero contextlost/pagehide handling today) + minimap PiP (1).** Memory: `mem:project/wip-2026-08-21-owner-uxbatch4`.

*(Sentinel — older sessions moved byte-identical to `DECISIONS_ARCHIVE.md`: 2026-08-02 →
2026-08-15e in §Moved 2026-08-18 (round 3), and 2026-08-17 → 2026-08-19d in §Moved 2026-08-22
(round 4, md5 5ed47c51b9d44a754964771ffe418330, 556 lines / 79,306 B). Their era digests live
in §Per-phase digests above. Entries above this line are the hot OWNER-BATCH era, 2026-08-21 →,
which rides the un-shipped release gate.)*
