# Frame the World — Decisions Log

One line per meaningful change: what was decided, files touched, and any number measured. **Append-only,
absolute-dated.** Verification status is explicit — local-tested, wix-VERIFIED (confirmed against the live
Wix platform), or UNVERIFIED. Supersede a past line with a newer dated line; never edit or delete old ones.
Durable design rulings also live as `mem:decisions/*`. Maintained per `mem:decisions/session_workflow`.

**Structure (compacted 2026-07-11).** Top-to-bottom: binding **ADR-000** decisions · a durable
**Traps & Gotchas** section (the load-bearing gotchas that keep resurfacing) · **Per-phase digests** ·
**Recent sessions** verbatim (newest first — new work appends here). Older verbatim session logs
(2026-07-09 → 2026-07-10) were **moved byte-identical** into [`DECISIONS_ARCHIVE.md`](./DECISIONS_ARCHIVE.md)
— the append-only invariant holds (a move is not an edit; superseded rulings are annotated only in the new
digest/ADR/Traps sections, never in the archived text).

---

## Binding decisions — ADR-000 (D1–D15)

From `PROJECT_SEED.md §4`, research-VERIFIED unless noted. **Binding** — change only by an explicit,
dated supersession line under Recent sessions.

> **D8 superseded 2026-07-10:** the `beforeInsert` quota hook was replaced by **endpoint-enforced quota**
> in the elevated `POST /api/photos` — the headless CLI provisions no Wix Data hooks (a member-session
> insert is platform-refused). See the Phase 5 digest. The 10-free / unlimited-paid intent is unchanged.

- **D1 — Globe engine:** three.js + `3d-tiles-renderer@^0.4` + Cesium OSM Buildings (ion 96188) + `GlobeControls`.
  Only combo giving real global 3D buildings + geo-accuracy + unrestricted per-tile material override + custom
  cinematic camera. VERIFIED.
- **D2 — Precision:** re-center tiles group near origin (ReorientationPlugin / CESIUM_RTC) + GlobeControls
  dynamic near/far. Solves float32 jitter without a float64 fork. VERIFIED.
- **D3 — Decode:** `exifr` embedded-JPEG preview → `libraw-wasm` Worker demosaic; single-threaded SIMD default;
  HEIC Safari-native detect + `libheif-js` fallback. VERIFIED (pipeline), UNVERIFIED (threads / COOP-COEP).
- **D4 — Orientation UX:** nudge-to-align is core; `FOV = 2·atan(sensorWidth/(2·focal))` + sensor DB +
  `FocalLengthIn35mmFormat` fallback. ILCs rarely write heading; GPS 3–15m, altitude junk → terrain-snap. VERIFIED.
- **D5 — Projection:** textured plane at frustum far face (v1); projective texturing (v2 stretch). VERIFIED.
- **D6 — Ephemeris:** `astronomy-engine` 2.1.19 (±1 arcmin) + procedural sky + Yale BSC5 stars, one source
  drives sliders + lighting. VERIFIED.
- **D7 — Data:** Wix Data Collections + geohash-prefix `hasSome` + client refine; denormalized `PublicPins`.
  VERIFIED (no geo ops), INFERRED (pattern).
- **D8 — Quota:** Pricing Plans check + `beforeInsert` hook rejecting insert #11 for free members (server-side). INFERRED.
- **D9 — Media:** originals private, derived previews public; resumable TUS upload for >10MB; 30-day download
  links. VERIFIED.
- **D10 — AI:** runtime Claude via Wix AI APIs (~1 credit/call; Opus 4.6 shown); vision gets downsized JPEG;
  premium-gated; doubles as the moderation pass. VERIFIED.
- **D11 — Scheduling:** none in v1; if needed, external cron → token-secured HTTP endpoint. VERIFIED.
- **D12 — Rendering:** WebGL2 primary, WebGPU progressive via `three/webgpu`. VERIFIED.
- **D13 — Cesium ion:** Community (free) for PoC; Commercial ($149/mo) at first sale / >$50K entity; manual
  attribution in UI. VERIFIED (terms), INFERRED (burn rate).
- **D14 — Design workflow:** Claude Design as token/motion factory → tokens.css (source of truth) → GL bridge
  `tokens.ts`; fence the globe; skip Claude Design's Wix connector (we scaffold via CLI for island/worker
  control). VERIFIED (workflow), UNVERIFIED (connector details).
- **D15 — Working title:** "Frame the World". ASSUMPTION (provisional).

---

## Traps & Gotchas (durable)

The hard-won gotchas that keep resurfacing. Violating one is a bug. Referenced by
`NEXT_SESSION_PROMPT.md`; the originating session (date/phase) is named for the full story in the
Recent sessions block or `DECISIONS_ARCHIVE.md`.

### GL / rendering
- **Navy night-sky floor.** `renderer.setClearColor` encodes the colour to the renderer's OUTPUT space
  (sRGB) when no render target is bound, and EffectComposer runs autoClear-off → sRGB-encoded values land
  in the LINEAR HalfFloat buffer and OutputPass re-encodes them to (8,26,45) navy on every empty sky pixel.
  Fix = `scene.background = Color(bg)` (three converts per-target + force-clears). (S5)
- **Negative `heightAt` garbage.** Quantized-mesh terrain tiles return NEGATIVE elevations while loading /
  on coarse LOD → **clamp every consumer to `[0, 9000]`** (clamp-only-upward). Unclamped, an arrival sinks
  to its lookAt floor and an underground camera unloads the whole tileset. (S2)
- **Additive overlays vanish on a bright sky.** Additive blending adds only ~10–30/255 against the day sky.
  Planning overlays (day-arcs) are alpha-blended solid strokes, `depthTest` off (analytic horizon owns
  occlusion). (S6)
- **`GlobeControls.update()` skips the near/far fit when `controls.enabled === false`** → a disabled-controls
  mode (FPV) must call `controls.adjustCamera(camera)` every frame or the frozen planes black-screen at
  street level. (S2)
- **Stale `InstancedMesh.boundingSphere`.** three caches the EMPTY sphere from count=0 and GlobeControls
  raycasts before pins load → every later pick early-outs forever. Set `mesh.boundingSphere = null` on
  every instance change. (Phase 5.1)
- **ECEF float32 cancellation.** ECEF ~6.4e6 m in float32 GPU matrices quantises at ~0.5 m and CRAWLS as the
  camera moves. Render instanced / large-coordinate meshes **camera-anchored**: `mesh.position =
  camera.position`, camera-relative instance translations, modelViewMatrix-only shaders. TRAP: `hoverAnchor`
  must add `mesh.position` back. (S4; same lesson as PhotoFrustum.)
- **`backdrop-filter` creates a containing block** → `position:fixed` descendants become panel-relative.
  Render fixed popups OUTSIDE any backdrop-filtered ancestor. (S3)
- **Dynamic far-plane clips the far hemisphere.** GlobeControls' far plane hides the starfield + atmosphere
  far shell → render near-hemisphere, clamp camera-anchored impostor distance ≥ 1.2·near. (Phase 1)
- **Descending zoom outruns tile load.** The manual zoom targets ellipsoid altitude, so a street request
  dives under a high city; once underground the ground tileset fully unloads (0 meshes) and live `heightAt`
  never recovers. Sticky `lastGroundM` sampled at the VIEW FOCUS + a street-floor clamp BEFORE the crossing.
  Tiles exist only inside the frustum — sample the focus, not the camera footprint. (S2)
- **Typed-array texture uploads ignore `UNPACK_FLIP_Y`** → flip in the DATA. No single-channel sRGB format
  exists in WebGL2 → linearise a data texture with a `uNightGamma` uniform. (S5)
- **`Group.renderOrder` does NOT propagate to children** — set renderOrder per object. **Sub-pixel points
  render nothing** — point sizes ≥ 2 px. **`getPivotPoint` returns null on horizon views** and leaves the
  out-arg STALE — fall back to the view focus. (Phase 4 / S6)

### Wix platform / backend (gate Phase 6 — never archive silently)
- **App-defined POST routes** were feared to 403 through the cloud adapter → the released-URL `/api/ping`
  canary confirmed 200; the risk is dead but the canary STAYS as the pre-release gate. (Phase 5 release)
- **Login builds an `http://` callback** repaired from the Referer header; `http` is tolerated only for
  localhost → referer-less (curl) auth fails "Invalid redirect URI". (Phase 5 release)
- **Elevated inserts run as the APP identity** → set `ownerMemberId` explicitly or ownership is wrong. (Phase 5)
- **`extensions.dataCollections` does NOT provision from `wix dev`** → the REST
  `scripts/provision-collections.mjs` is the schema source of truth. (Phase 5)
- **A stale member cookie is silently replaced by a visitor cookie** on the next HTML response → re-mint
  tokens before verifying member flows. (Phase 5)
- **OAuth allowlist is PORT-EXACT** — 4321 and 4322 must both be registered. (Phase 5)
- **Viewport pin query must THROTTLE, not debounce** — the perpetual LEO idle drift starves a debounce
  timer so it never fires. (Phase 5)
- **Media >10 MB must use `generateFileResumableUploadUrl` (TUS)** → async `onFileDescriptorFileReady`. (CLAUDE.md)

### Verification / tooling
- Occluded Chrome throttles rAF to ~1 frame/several-s → `page.bringToFront()` before timing-sensitive
  browser verification.
- Hiding the tiles groups crashes the rAF tick → the canvas presents the last good frame and every "hidden"
  measurement reads stale (guard with a `renderer.info.render.frame` advance check).
- `document.elementsFromPoint` SKIPS `pointer-events:none` nodes; `stars.update()` re-sets `points.visible`
  every frame (hide via `material.visible`); synthetic dblclicks need a preceding pointerdown/up pair.
- Verify tiers, in order: Playwright MCP → Chrome-extension bridge → scripted headless-Chrome CDP
  (`scripts/verify-s5-night.mjs` idiom). DEV seams: `window.__globe / __composer / __cameraStore /
  __timeStore / __uploadStore / __pinsStore / __memberStore`. Screenshots → `verify-shots/` only.

---

## Per-phase digests

One line per phase; full mechanics in the linked memory, verbatim session logs in `DECISIONS_ARCHIVE.md`.

- **Bootstrap (2026-07-09)** — `.claude/` operating environment + Serena memory graph + the persistence
  loop (DECISIONS + NEXT_SESSION). `mem:decisions/session_workflow`.
- **Phase 1 — scaffold + globe (2026-07-09/10)** — Wix headless Astro scaffolded onto the existing repo;
  the globe rebuilt into the LEO "signature scene": NASA Blue Marble July graded into the palette + VIIRS
  night lights + relief, ray-based atmosphere, a second TilesRenderer draping palette-graded Esri imagery
  that dissolves in by altitude, dark edge-stroked OSM buildings. `mem:patterns/globe-rendering`. RELEASED
  to the live URL 2026-07-10.
- **Design system import (2026-07-10)** — chrome tokens + fonts → `tokens.css` + the GL bridge
  `lib/theme/tokens.ts` (ADR D14). `mem:patterns/design-system`.
- **Phase 2 — decode (2026-07-10)** — `exifr` metadata + embedded-thumb preview + **`libraw-wasm@1.0.5`
  EXACT-pinned** (1.1.2+ needs pthreads/SAB = COOP/COEP, unusable on unverified Wix hosting) in a disposable
  per-file Worker + `libheif-js` HEIC fallback. `mem:patterns/upload-flow`.
- **Phase 3 — projection (2026-07-10)** — camera frustum + image plane from EXIF; reactive zustand
  re-projection; click-to-place for missing GPS; 2.2 s bezier flight (reduced-motion cut).
  `mem:patterns/photo-frustum`.
- **Phase 4 — ephemeris (2026-07-10)** — `astronomy-engine` drives sun/moon/terminator/shadows/atmosphere
  from scene time; TimeScrubber ±12 h + multiday; golden-hour bell grade; real BSC5 star field (−GAST); real
  Cesium World Terrain (90 m building sink removed). `mem:patterns/sky-bodies-terrain`.
- **Phase 5 — members + pins (2026-07-10)** — managed member auth (`@wix/astro`); Photos/PublicPins
  provisioned via REST; **endpoint-enforced 10-pin quota** (supersedes D8's hook); C6 reduced-precision
  public pins (cell-centre published); viewport geohash query + click→fly. First Wix-load-bearing phase.
  `mem:patterns/members-pins`.
- **Phase 5.5 S1–S6 — pre-marketplace UX (2026-07-11)** — location finder, flight/FPV core, pin lifecycle,
  pin visuals + Explore journey + Welcome landing, night-sky physics, FPV planning overlays. Canonical
  design `archive/PHASE_5_5_UX_BATCH.md`; full mechanics in the Recent sessions block below + `mem:project/wip-2026-07-11-*`.
- **Pre-S7 architecture review + refactor (2026-07-11)** — this compaction, doc-drift fixes, new
  conventions, and the safe cleanup tier. See `archive/ARCHITECTURE_REVIEW.md` + the Recent sessions block.

---

## Recent sessions (verbatim, newest first)

New work appends a dated line here.
- **2026-07-15 (MILKY WAY REFINEMENT — owner pass on the same-day batch: 8K + subtler + star punch + sky embed; vitest 573/573 · astro 0/0 · wix build Complete · browser-VERIFIED, re-shot `prephase6-c1..c3`).** 8K bake (8192×4096 q90, 6.4 MB, same `/textures/milkyway-2020.jpg`) — **TWO BAKE TRAPS: (a) SVS star maps are FLUX-PER-PIXEL** (8k per-pixel = ¼ of 4k, patch-measured ratio 4.0 → ×4 linear gain), **(b) flux hides in sub-texel star speckle** — mips are OFF (RA-wrap safety) so wide-FOV minification point-samples PAST the speckle and the band went near-black → **gaussian pre-blur σ=1 texel in LINEAR space before encode** (flux integrates under point sampling; linear patch means now match the 4k bake within 1%; single-pixel checks mislead on speckle — compare patch means). `hazeGain 1.0→0.8` (subtler) · `STARS.sizeGamma 0.35→0.42` + `sizeMax 5→6.5` (first-magnitude stars punch through the band; mag-4+ tail unmoved) · **NEW atmospheric extinction on the diffuse layers** (`MILKYWAY.extinctionBandDeg 16 / extinctionFloor 0.22 / extAlt 20k→150k`; per-frame `uExtFloor` lifts to 1 with altitude — no extinction from orbit; catalog stars exempt): the band now melts into the horizon sky. GPU note: 8k ≈ 134 MB VRAM (no mips) — mobile-tier 4k fallback parked. Files: tuning (MILKYWAY/STARS) · `scene/stars.ts` (uExtFloor + baked ext band) · texture. — 4 owner tasks: Milky Way realism · true-horizon body occlusion · saved places · PIN visibility chip; `/frame` + investigate-design-v3, implement/Deep; gates: vitest 573/573 (+25) · astro check 0/0 · wix build Complete · browser-VERIFIED via scripted headless-Chrome CDP, shots `verify-shots/prephase6-*`).**
  **(1) Milky Way** — NASA/GSFC SVS "Deep Star Maps 2020" Milky-Way-only layer (Gaia DR2 minus the bright Hipparcos/Tycho stars → no BSC5 doubles; svs.gsfc.nasa.gov/4851, credit added to `index.astro`) baked offline EXR→sRGB+dither→`public/textures/milkyway-2020.jpg` (4096×2048 JPEG q92, 2.9 MB; landmark-verified: bulge px (3064,1351) vs Sgr A* (3113,1354), LMC (1140,1815) vs (1128,1817)); rendered as an inward SphereGeometry CHILD of the star Points (inherits −GAST → auto-aligned with BSC5 — Cygnus asterism sits on the Great Rift in the shots), per-fragment dir→RA/Dec UV (`u = 0.5 − RA/2π` RepeatWrapping, no mips → no seam), additive + DITHER + uFade; procedural points retuned to sparkle (`MILKYWAY.alpha` 0.35→0.14, `hazeGain` 1.0); **diffuse layers attenuate at long focal lengths** (`narrowFovLoDeg 8 / HiDeg 35 / floor 0.22` — 15× texture magnification read as blobs at 250–300 mm, and the sparkle points would pose as fake stars). Files: `scene/stars.ts` (haze mesh + fovK) · tuning MILKYWAY · `test/components/globe/milkyWayHaze.test.ts` (UV twin inverts raDecToUnit exactly).
  **(2) True-horizon occlusion (the FPV razor-cut, owner screenshots)** — ROOT CAUSE: `horizonFade` tested closest-approach vs a SPHERE of radius WGS84_A (the real surface at 48°N is ~11.9 km BELOW it → below-horizontal rays pinned fade 0) while the `tc<=0` guard pinned above-horizontal to 1 → binary cut exactly at 0° geocentric elevation, ~0.3° above the true horizon; the atmosphere sky regime had a matching C1 kink at sinEl=0 (haze slope jump + `pow(x,0.55)` infinite derivative) = the full-frame seam. FIX: ANGULAR fade vs the TRUE ellipsoid horizon — CPU float64 per frame (`scene/sky.ts horizonTerms/horizonBandSin`: ellipsoid-scaled space, tangency exact; uniforms `uHorizonUp/uSinHor/uHorizonBandSin`), band `SKY.horizonFadeStreetDeg 0.08` ↔ `OrbitDeg 0.6` blended over 50–600 km (orbit reproduces the old ~0.6° limb melt); same fade added to stars + MW haze (bodies and stars now sink at ONE line); atmosphere regime re-anchored at the true horizon (`sRel`), zenith ramp → `pow(smoothstep(...),k)` (C1), haze crest → softabs blend (`ATMOSPHERE.skyHazeSoft 0.01`, crest stays exactly 1 → skyBudget guard untouched). `horizonFadeBandM` superseded. Verified at 275 mm sunset + 229 mm moonrise: disc slices at the terrain horizon, glow bleeds over, NO line/seam. Files: `scene/{sky,atmosphere,stars}.ts` · tuning · `test/components/globe/horizonFade.test.ts` (dip ≈ √(2h/R) at 95 m eye; LEO limb −32°; gradient exists mid-band — pins the regression). DayArcs already fade by astronomical altitude — untouched.
  **(3) Saved places (photo-less FPV bookmarks)** — NEW `SavedPlaces` collection (provision-collections.mjs, ADMIN-everything like Photos; **provisioned live**) + thin `/api/places` GET/POST/DELETE (photos.ts idiom; cap `PLACE_QUOTA 50`, 402 shape) + pure `lib/wix/placeRecords.ts` (SavePlaceBody = UrlFpvPose + optional pinned `timeMs`; SAME clamp bands as the `#f=` hash — one contract). UI: `MyPins` → tab strip **MY PINS | PLACES** (fetch-per-open per tab; ◎ rows, two-press delete); **SAVE PLACE** control on the camera deck in any FPV (members-only; button → inline name input, Enter saves — key events stopPropagation so FPV walk/Escape never fire while typing; pose source = the same fpvHud/camGeo mirrors the hash writer uses; LIVE time never persisted). **Jump = `camera.fpvJumpRequest` seam** (UrlFpvPose): orchestrator consumes it as a synthetic `pendingFpvShare` + `setTempPin/setTempFpv` → the EXACT share-link entry path, works from orbit AND FPV→FPV (forced re-entry: `fpvKind = null`, no fly-out); saved custom time restored via `setTime`, live via `goLive`. Member-flow browser-VERIFIED end-to-end (minted test-member session per the Phase-5 recipe — NOTE the OAuth allowlist only admits `/api/auth/callback` as redirectUri): save → row listed → jump restores heading/fov/eye EXACT (351.8/55.0/1.7) → FPV→FPV re-jump exact → UI delete confirmed server-side. **Watch: Wix Data threw intermittent `WDE0054: Unknown Error` on the minutes-old collection** (verify script hardened with id-keyed polling; every path also confirmed green at least twice).
  **(4) PIN visibility chip** — `camera.pinsVisible` (+setter) → `PinsHandle.setVisible()` gates render AND pick in ONE place (update() re-sets mesh.visible per frame — the stars trap; hidden = early-return, skips all matrix work; `pick()` returns null → click + hover both covered); PIN chip in CameraTiltPanel between SAT/BLD (SAT idiom, `.ct-pins`); **FPV hides pins by default** — entry captures `pinsVisibleBeforeFpv` + hides, exit restores UNLESS re-lit inside FPV (**TRAP hit live: the entry branch sets `fpvActive = true` BEFORE the guard — must capture `wasFpvActive` before the transition branches**); viewport queries keep running while hidden (instant re-show). Browser-VERIFIED: toggle, auto-hide, restore, override-survives-exit.
  **Ops:** verification = scripted headless-Chrome CDP (`scripts/verify-prephase6-uiux.mjs` + `verify-places-member.mjs`; Chrome-extension bridge was down; needs Node ≥22 — used nvm v24; TRAP: hash-only `Page.navigate` does NOT reload — bounce via about:blank). Vite dep-cache went stale mid-session after heavy edits (all islands "Failed to fetch dynamically imported module") → restart `wix dev` (the known trap, new symptom). `.env.local` `WIX_CLIENT_ID` is QUOTED — strip quotes when parsing.
- **2026-07-15 (LATEST — DOCS REORG + PHASE 6 RUNWAY) — claude-docs reorganized into topic subfolders + the three working docs refreshed; Phase 6 (marketplace-light) is the declared next step (`/frame` + investigate-design-v3, implement/Standard; docs/ops only — gates re-run clean: astro check 0/0 · vitest 548/548).** Root now holds ONLY `ARCHITECTURE.md` · `DECISIONS.md` (+`DECISIONS_ARCHIVE.md`) · `IMPLEMENTATION_PLAN.md` · `NEXT_SESSION_PROMPT.md` · `PROJECT_SEED.md`. Moves (git mv, content byte-identical): **`provenance/`** ← DEEP_RESEARCH.md + CLAUDE_DESIGN_MEMO.md · **`dnipro-enrichment/`** ← DNIPRO_3D_ENRICHMENT_{PLAN,RESEARCH_PROMPT,RESEARCH_RESULTS}.md + DNIPRO_SLICE0_SPIKE.md + OSM2WORLD_EXPERIMENT_PREP.md · **`rendering/`** ← RENDERING_QUALITY_PASS.md · **`archive/`** ← PHASE_5_5_UX_BATCH.md + ARCHITECTURE_REVIEW.md + B19_HANDOFF.md. References updated repo-wide (37 files: code comments in tuning.ts/quality.ts/enrichedMask.ts/etc., bake scripts, conventions, `.claude/CLAUDE.md`, tokens.css, `.serena/memories/*`) — both path-qualified `claude-docs/X` and bare doc-name forms; **the verbatim Recent-sessions text below and `DECISIONS_ARCHIVE.md` were deliberately left untouched (append-only invariant)** — resolve any stale path in an older line via the map in this line. `NEXT_SESSION_PROMPT.md` rewritten from a ~460-line shipped-work stack into a Phase-6 brief (scope/DoD/pre-reads, the catalog-v3-vs-v1 open question, carried tails, docs map). `ARCHITECTURE.md` refreshed to as-built (terrain + enriched-R2-tileset + vector-web + quality-tier additions in §2, planner in §4, `authorName` + C6 cell-centre in §5, full module lists + `scripts/bake/` in §7, risk register). `IMPLEMENTATION_PLAN.md`: Phase 5.5 ticked ☑, NEW **"Interlude 2026-07-12→14"** ledger (10 shipped owner initiatives, one line + pointer each), Phase 6 marked **NEXT** with pre-reads + open questions, empirical-validation statuses trued up. Memory: `mem:project/wip-2026-07-15-docs-reorg-phase6-prep`.
- **2026-07-14 (LATEST — FPV FOLLOW-UP: SOLIDITY DISSOLVE + SHAREABLE FPV URLS) — the BUILDINGS slider now fades gradually + uniformly (the 55→56 full-opacity jump is dead) and any FPV view is perfectly shareable via a `#f=` hash (`/frame`, implement/Standard; gates astro check 0/0 · vitest 548 (+5) · wix build Complete · browser-VERIFIED in wix dev via Playwright MCP; shots `verify-shots/uiux-05..09`).** **(1) Solidity ROOT CAUSE:** the slider ramped opacity but flipped `depthWrite` at a BINARY threshold (`k>0.55` enriched / `>0.6` OSM) — the instant depth-writing engaged, faces occluded each other and every mesh read fully solid between two slider ticks. FIX: solidity renders as the repo's own SCREEN-DOOR Bayer dissolve (the F1 idiom) — **fill + canopy stay OPAQUE and depth-writing at every k** (no transparent-sort, no recompile toggles, no threshold): `buildingMaterial.ts` gains `uFlatAlpha` and the fill chunk discards on `(ftwBayer4 + ftwHash11(vFtwBId+5))/16 > ftwA` where `ftwA = mix(1, mix(ghostA,1,uSolidK), uGhostK) · uFlatAlpha` — the per-building hash jitter inside each Bayer step turns 16 ordered levels into an effectively continuous city-wide response (no banding); `FTW_BAYER_GLSL` exported; `buildings.ts` setGhost/setGhostSolid lose their transparent/depthWrite lines; `enrichedBuildings.setSolidity` drives `uFlatAlpha` (0.28+0.72k) and the trees get their own `uFtwTreeAlpha` Bayer discard. The OSM near-camera ghost melt is now a stipple dissolve (same owner-approved F1 look); shadows unchanged. Live sweep 15/50/60/100 = smooth density ramp, 100 = clean solid. **(2) `#f=<lat6dp>,<lon6dp>,<eyeM>,<headingDeg>,<pitchDeg>,<fovDeg>` (+`&t=`)** — NEW pure `formatFpvHash`/`parseFpvHash` (+`UrlFpvPose`; `parseTimeHash` accepts `[pf]=`); **eye height is GROUND-RELATIVE by design** (reproduces across machines regardless of terrain-tile LOD). WRITE: the hash block lost its `!fpvActive` gate — FPV writes the LIVE camera view (geodetic eye + enuBasis view az/alt + `fpvEyeAboveGroundM` + `camera.fov`) at the same cadence; photo-FPV shares too (restores as temp FPV — the VIEW is shareable, the photo isn't). RESTORE: boot parses `#f=` (the `#p` boot construction refactored into ONE `bootPoseAt()` — re-verified identical), boots `FPV.shareBootAltM` 260 m above the point along the shared bearing, `setTempPin`+`setTempFpv`, and the temp-FPV entry consumes the pending share: exact basis from the shared heading (FRESH scratch vectors — the S7 aliasing trap), pitch/eye/FOV applied, entry flight descends onto the exact eye; Welcome + boot-poster (`/^#(p|f)=/`) skip on `#f`; `&t=` restore covers both hash forms. Live round-trip: heading 352.1/fov 26.8/eye 1.7/lat-lon EXACT, pitch within 0.2° (terrain-settle vs pitch clamp, cosmetic); FPV+time combo restores both; `#p=` links and the plain-URL welcome regression-clean. Tails: solidity deliberately NOT in the hash (display preference — easy 7th field later); pitch ±0.2° settle. Files: `buildingMaterial.ts` · `buildings.ts` · `enrichedBuildings.ts` · `urlPose.ts` · `StylizedTiles.ts` (bootPoseAt · pendingFpvShare entry · #f write) · `tuning.ts` (FPV.shareBootAltM/shareBootTiltDeg) · `Welcome.tsx` · `index.astro` · `test/lib/geo/urlPose.test.ts` (+5). Memory: `mem:project/wip-2026-07-14-uiux-qol-batch` §Follow-up.
- **2026-07-14 (prior — UI/UX QoL BATCH, 6 owner tasks) — uniform hover-reveal drag handles + time playback/fast-forward + precise time & shareable custom time in the URL + PLAN panel relocated top-left + zero horizontal scroll + clickable ☀/☾ edge chips (`/frame` + investigate-design-v3, implement/Standard; gates astro check 0/0 · vitest 543 (+13) · wix build Complete · browser-VERIFIED in wix dev via Playwright MCP; shots `verify-shots/uiux-01..04`; NO globe scene/shader code touched).** **(1) DragGrip rework:** ONE placement for every panel — a tab just OUTSIDE the window above its top-right corner (`bottom: calc(100%+3px); right:0; width:max(30px,10%)`), hidden until the panel is hovered via **`:hover > .drag-grip`** (CSS :hover matches an element when the pointer is over any DOM descendant, even one outside its box) + an invisible `::before` halo; pointer-events:none hosts (.tr/.fh) keep a faint 0.28 resting opacity (their body never hovers). `inset`/`corner` variants DELETED; the overflow panels that needed them (.pd/.mp-panel) moved scrolling to INNER wrappers (`.pd-scroll`/`.mp-scroll`) — RULE: a scrolling panel keeps overflow OFF its root or the outside tab clips. mp-enter keyframes now compose the drag vars (pd-enter idiom). Verified: hidden→1 on hover, drag −120/−90 exact, dblclick resets. **(2) Time playback (the important one):** store/time gains `play(rate)/stopPlay/playRate/playWallMs` — **`sceneTimeMs()` derives `timeMs + (now−playWallMs)·rate` (pure `playbackNowMs`), so every per-frame consumer gets CONTINUOUS fluid time with zero per-frame store writes** (SKY.sampleIntervalMs is scene-ms → the ephemeris resamples every frame at ≥1 min/s; no jarring steps). Semantics: play(1) while LIVE = no-op (owner spec) · stop freezes pinned · goLive clears · **setTime during playback REBASES (scrub/chips adjust the reel, don't stop it)**. TimeScrubber: ±H/±M stepper pairs + PLAY/STOP + rate select (SCRUB.playRates 60/600/3600 + REAL ×1) in a new `.ts-foot` row; steppers recentre the rail only in the edge clamp band; display ticks at SCRUB.playTickMs 150. FF signals: `.ts-offset` ▶▶ + `--color-danger`, new "FAST-FORWARD 1 HR/S" label, TimeReadout `▶▶ ×3600` red chip + red dot (real-speed = `▶ PLAYING`, amber). CSS trap: `.tr-mode.tr-mode--ff` double-class needed to beat the later `.tr-mode` rule. Live: 3 real s at ×3600 = +184 scene min, rolled past midnight fluidly. **(3) Precise time + URL:** `<input type="time">` beside the date (`localTimeStr`/`withLocalTime`, null on garbage); hash extends to `#p=…&t=<utcMs>` via `formatSceneHash(pose, live ? null : sceneTimeMs())` — **LIVE time is never shared (owner spec)**; NEW `parseTimeHash` + `parsePoseHash` tolerates `&t` (old links parse unchanged); boot restores the pinned instant after the pose. Live: +1H → `&t=` appears · NOW → dropped · fresh boot with `&t=` lands PINNED 04:30 · EXIF capture-time seeding now rides the hash after placement. **(4) PLAN panel:** `.pp-root` → `left:1.6rem; top:3.4rem; z-index:31` (above .pd's 30), card unfolds DOWN (`top:2.4rem`), 12.5rem wide, `max-height: max(10rem, min(24rem, calc(100vh−39rem)))` keeps it clear of the FPV mini-map slot; head (with its InfoDot) stays OUTSIDE the new `.pp-scroll` body. **(5) Horizontal scroll ROOT-CAUSED:** `overflow-y:auto` forces computed overflow-x to `auto` (spec), and the tips.css `::after` pills are LAID OUT even while `visibility:hidden` — tooltips poking past a card edge were the phantom h-scrollbar (measured: PlanPanel head InfoDot 237 px, .pd tiers tip +32 px). Fixes: `overflow-x:hidden` on every scroll container (.pp-scroll/.pd-scroll/.mp-scroll/.uf) + genuinely-poking tips re-anchored (`.pd-save__tiers` → `data-tip-pos="left"`). Verified with a full-page audit (every overflow-y:auto element, scrollWidth ≤ clientWidth) across orbit/FPV/placed-photo/upload states → ZERO offenders. **(6) ☀/☾ chip click → bring into view:** BodyChip is now a real button; NEW `camera.skyLook` one-shot request — **FPV:** stepFpvPose eases fpvYaw/fpvPitch against the PRE-look anchor basis (final az = az0+yaw about geodetic up; pitch honours ±pitchClampDeg; `FPV.skyLookEaseTauMs` 320; snap+clear <0.003 rad; cancelled by look-drag/ROTATE-deflection/clearAllTargets/FPV-exit); **orbit:** resolves into the existing glides — `setTargetHeading(az)` + tilt only RAISED to `min(88, 90+alt−18)`. Live: FPV eased 112°→300° exactly onto the sun (pitch 2.9° vs alt 2.8°), orbit tilt 60→75 sun-in-frame, chips vanish on arrival. Files: store/{time,camera}.ts · lib/geo/urlPose.ts · StylizedTiles.ts (boot t-restore · hash t-write · skyLook glide) · tuning.ts (SCRUB.playRates/playTickMs · FPV.skyLookEaseTauMs) · panels TimeScrubber/TimeReadout/FpvHud/PlanPanel/PhotoDetailPanel/MyPins · ui/DragGrip.tsx · 8 CSS files · tests time (+9) / urlPose (+4). UNVERIFIED tails: sub-M3 playback-tick cost · MyPins scroll wrapper live (needs member cookie; same construction as verified .pd/.pp) · time input UX while the native picker is open during FF. Memory: `mem:project/wip-2026-07-14-uiux-qol-batch`.
- **2026-07-14 (prior — LOCAL-DEV TILES, NEVER R2 IN DEV) — baked tilesets moved OUT of `public/` → `bakes/enriched/<city>/` (git-ignored) + a `serve`-only Vite middleware (`astro.config.mjs` `ftwLocalTiles`) serves them same-origin at `/enriched/*` in dev + `.env.development.local` (mode precedence beats `.env.local`) points dev at `/enriched/dnipro/tileset.json` → `wix dev` streams tiles FROM THIS MACHINE with ZERO R2 requests; `wix build`/release run production mode, never read the dev env file, and bake the R2 Worker URL (`/frame`, fix/ops; gates astro check 0/0 · vitest 530 · browser-VERIFIED + build-VERIFIED).** Why the move (not just the env swap): with 372 MB of tiles under `public/`, every `wix build` would copy them into the client bundle as dead weight (prod streams from R2 anyway) — bundle measured **24 MB** after the move. Browser-verified via Playwright: default → `rootURL /enriched/dnipro/tileset.json` · `dnipro-real-3` · 25 tiles · **`performance` resource entries show 0 workers.dev requests**; BLD chip → `/enriched/dnipro-o2w/tileset.json` · `dnipro-o2w-2` · still 0 R2 requests (the `?enriched=` segment-swap resolves against the LOCAL env URL — the whole A/B is offline-capable). Build-verified: `dist/client` contains NO bakes (only the pre-existing 2.8 MB `enriched-sample` + code) and greps show the R2 URL baked in. R2 state re-confirmed by curl first (both tilesets FULLY pushed: `dnipro-real-3` 386 cells + manifest · `dnipro-o2w-2` 389 cells + manifest · glb magic on sampled cells from each). TRAP: `// @ts-check` in astro.config flags an untyped Vite plugin (5 implicit-any errors) — `/** @returns {import("vite").Plugin} */` types `apply:"serve"` + the middleware params contextually. Files: EDITED `astro.config.mjs` (dev middleware) · `.gitignore` (`/bakes/`) · `cities/dnipro.json` + `cities/dnipro-o2w.json` (`output → bakes/enriched/*`) · `bake.mjs` (ONE cosmetic hint-string line — the printed dev URL strips `bakes/` too; pipeline behaviour untouched) · `scripts/bake/README.md` (§Wire-it = dev-local/prod-R2 split) · `.env.local` (comment) ; NEW `.env.development.local` (git-ignored via `.env.*.local`). Fresh-clone note: no local bakes → comment out the dev override to fall back to R2, or re-bake. Memory: `mem:project/wip-2026-07-14-osm2world-adapter` §local-dev-tiles.
- **2026-07-14 (prior — 10 km EXTENT + BLD TOGGLE) — BOTH bakes expanded to GREATER Dnipro ~20×20 km (~10 km radius, owner ask), re-baked + LIVE on R2, plus a CameraTiltPanel `BLD` chip that switches CLASSIC ↔ OSM2WORLD in one click (`/frame`, implement; gates astro check 0/0 · vitest 530 (+5) · browser-VERIFIED via Playwright MCP; shots `verify-shots/o2w-10km-01..02` — the same 48.532/35.131 periphery pose ~8.9 km from centre, chip states visible).** Config: `cities/dnipro.json` bbox → **[34.915, 48.37, 35.185, 48.55]** (centre unchanged 48.46/35.05), **grid 10→20** (~1.0 km cells — streaming granularity + within-cell relief unchanged), `maxTrees` 60k→**200k** (the cap thins HASH-UNIFORMLY over the whole box — a low cap would visibly thin the in-city parks; landed UNDER cap at 161,823), version `dnipro-real-3`; `dnipro-o2w.json` subGrid 2→**4** (16 sub-extracts ~5×5 km), version `dnipro-o2w-2`; `tuning.ts ENRICHED.bbox` matched (the MUST-match mask/extent contract, comment updated with the regen-BOTH rule). **Classic bake: 127,890 bldgs · 386 tiles · 5.05M verts · 161,823 trees · 149.14 MB · maxH 167 m** (one 62 MB Overpass fetch worked). **o2w bake: first pass came out 577 MB/20.3M verts → measured per-class verts on the biggest intermediate glbs: `PoleFence` = 59.3% + `ChainLinkFence` 3.3% of ALL vertices (OSM2World renders every fence post; peripheral garden fencing, sub-pixel beyond ~1 km) → both dropped via `dropClassesRegex` (the knob built for this; converts mtime-cached so only the adapter re-ran) → FINAL 127,097 bldgs + 5,662 constructions (HVPowerTower 1,314 · Wall 1,045 · StreetLamp 2,309 · Powerpole 342 · RetainingWall 171 · Cliff 60 · masts/billboards/…) · 389 tiles · 7.71M verts · 223.76 MB · max cell 4.97 MB.** Uploads: 388 files/142.30 MB → `enriched/dnipro/` + 391 files/213.46 MB → `enriched/dnipro-o2w/` (Worker unchanged; NOTE: grid-10 era cells remain as orphan keys in the bucket — tileset.json no longer references them, harmless, clean when convenient). **UI toggle:** NEW helpers in `enrichedVariant.ts` (`ENRICHED_VARIANT_NAME`/`isVariantActive`/`toggleVariantUrl` — toggles `?enriched=` in a full URL, PRESERVING the `#p=` pose hash; +5 tests) + a `BLD` chip in CameraTiltPanel after SAT (renders only when `PUBLIC_ENRICHED_TILES_URL` is set; `is-on` when the variant is active; CSS `.ct-bld` narrow cut + `.ct-row` flex-wrap for the 5th chip). **RELOAD-BASED BY DESIGN:** a live tileset swap would tear down the enriched renderer's per-building seating/occlusion/edges state mid-frame — the pose-preserving reload is lossless and keeps the globe runtime untouched. Browser-verified: chip click → `?enriched=dnipro-o2w` streams `dnipro-o2w-2` from R2 at the SAME pose (chip pressed) → second click → clean URL, `dnipro-real-3` back (chip idle); periphery pose proves the radius (old bbox ended at 48.50/35.10). KNOWN LIMITS carried: no HLOD — the (now 20×20 km) bbox reads honestly building-empty above ~20 km until a coarse tier exists; o2w untagged-height defaults + draco levers unchanged from the adapter line below. Files: EDITED `cities/dnipro.json` · `cities/dnipro-o2w.json` · `tuning.ts` (ENRICHED.bbox) · `enrichedVariant.ts` · `CameraTiltPanel.tsx` · `camera-tilt.css` · `test/lib/globe/enrichedVariant.test.ts`. Memory: `mem:project/wip-2026-07-14-osm2world-adapter` §10 km-extent.
- **2026-07-14 (prior — OSM2World PRODUCTION ADAPTER) — `bake-osm2world.mjs` SHIPPED as a PARALLEL VARIANT + full-city bake LIVE ON R2 + the `?enriched=` A/B compare seam (`/frame` + investigate-design-v3, implement/Deep; owner constraint honored: the CURRENT pipeline/models/behaviour are UNTOUCHED — bake.mjs, cities/dnipro.json, public/enriched/dnipro and the default runtime path are byte-identical). Gates: astro check 0/0 · vitest 525 (+12) · browser-VERIFIED in wix dev via Playwright MCP; shots `verify-shots/o2w-adapter-01..05` (same-#p-pose pairs).** NEW: `scripts/bake/bake-osm2world.mjs` (extract→convert→re-bin orchestrator) · `lib/osmXml.mjs` (tiled Overpass-XML extract subGrid 2×2, cached; REFERENCE-SAFE C6 — excluded ways/relations removed whole, excluded TAGGED nodes keep geometry but lose tags, so no dangling refs; exercised live −126 elements/13 stripped nodes) · `lib/readGlb.mjs` (promoted spike inverse of encodeGlb, +Buffer input) · `cities/dnipro-o2w.json` (`extends: dnipro` shallow-merge → bbox/grid/trees/mask IDENTICAL; `upload-r2.mjs --city dnipro-o2w` works unchanged) · `src/lib/globe/enrichedVariant.ts` (pure `resolveEnrichedUrl`, ONE call wired at StylizedTiles.ts:172: no param → env URL byte-identical · `off` → no tileset + no mask = stock Cesium OSM · a name → variant path-segment swap · contains "/" → verbatim; the pose lives in the #hash → SAME-POSE A/B by reload). **LOAD-BEARING FIX the spike missed: the constant-ENU-offset re-base is only valid near the origin — with `>;` relation recursion each sub-box's data bounds inflate 100+ km, `scene.extras.origin` lands far away, and Mercator-vs-ENU drift scattered half the city (first run: 11,156/27,920 buildings in-bbox, densest cell (2,1) SW-shifted ~2 km, 44 cells). FIX = invert OSM2World's `MetricMapProjection` EXACTLY per vertex (formula disassembled from the 0.5.0-SNAPSHOT jar via javap: S=40075016.686·cos(lat0); x=((lon+180)/360)·S−x0; z=(ln((1+sinφ)/(1−sinφ))/4π+0.5)·S−y0; glTF POSITION=(x,ele,−z)) → true lat/lon → `projectEN` into OUR frame — after: 26,104/27,920 in-bbox (extruder bake: 26,569 footprints; the remainder = OSM2World's fault-tolerant skips + multipolygon merging), 91 feature cells (extruder 90), densest (6,5) = the city centre.** Bake: 95 tiles · 3.89M verts · 110.22 MB · 29,216 binned features = 26,104 Buildings + 3,112 constructions (kept classes: PoleFence 1,919 · Wall 491 · StreetLamp 279 · HighVoltagePowerTower 155 · RetainingWall 103 · ChainLinkFence 46 · PowerTower 40 · Powerpole 16 · NodeModelInstance 24 · Cliff 11 · BollardRow 9 · Railing 8 · MobilePhoneMast 4 · Flagpole 3 · Chain 2 · Hedge 1 · Billboard 1 — authoritative in `public/enriched/dnipro-o2w/bake-manifest.json` counts.classes) · trees = the SAME 24,714 placements as the default bake (same cache+seed) so an A/B differs ONLY in structures. Uploaded 97 files/105.14 MB → `enriched/dnipro-o2w/` (the Worker is city-agnostic — ZERO infra changes; curl: CORS `*` + glTF magic). Browser-verified: default → R2 `dnipro-real-2` streams (this ALSO closes the carried "globe paints from R2" tail) · `?enriched=dnipro-o2w` → R2 `dnipro-o2w-1` streams (11 cells + trees, no shader/tile errors, seated flush on CWT) · `?enriched=off` → enriched absent + Cesium OSM back in-bbox. **HONEST TRADE for the owner to judge by eye:** o2w delivers real S3DB roof shapes + `building:part` massing + courtyard holes + walls/fences/lamps/pylons; costs 110 MB vs 32.8 MB uncompressed (~3.4×; levers = gltf-transform weld+draco, 23× spike-measured but needs DRACOLoader wiring in the enriched runtime, or `--lod 1`), and UNTAGGED buildings take OSM2World's OWN height defaults which sometimes read LOWER than our class-tuned defaults (Soviet apartment blocks) — a per-place fidelity regression to weigh in the A/B. Convert 15–77 s/sub-box, mtime-cached (`--reconvert` forces; a rewritten safe.osm would invalidate the cache → filter only on fresh raw). Traps: `dropClassesRegex` "Rail" prefix ate "Railing" (fixed → "Railway") · wix dev intermittently 500'd after idle → restart (known class). Files: NEW `scripts/bake/bake-osm2world.mjs` · `scripts/bake/lib/{osmXml,readGlb}.mjs` · `scripts/bake/cities/dnipro-o2w.json` · `src/lib/globe/enrichedVariant.ts` · `test/bake/osm2world.test.ts` · `test/lib/globe/enrichedVariant.test.ts`; EDITED `StylizedTiles.ts` (2-line seam) · `scripts/bake/README.md` (§OSM2World variant) · `OSM2WORLD_EXPERIMENT_PREP.md` (§10). Memory: `mem:project/wip-2026-07-14-osm2world-adapter`. (*first-run numbers before the Railing fix differ slightly.)
- **2026-07-14 (prior — OSM2World "Slice 1.5" spike) — 1-CELL DE-RISK SPIKE DONE → VERDICT VIABLE, green-light the full adapter (`/frame` + investigate-design-v3, implement/Deep). Browser-VERIFIED in `wix dev` via Playwright MCP; NO tracked source touched → gates unchanged astro check 0/0 · vitest 513; shots `verify-shots/o2w-slice15-01..03`.** Executed `OSM2WORLD_EXPERIMENT_PREP.md §8` end-to-end HERE (curl the 478 MB 0.5.0 distro → Overpass-XML ~1 km² central-Dnipro extract → C6 filter → `OSM2World convert` stylized glb → inspect → adapter → browser A/B). **All 4 DoD answered: (1) AXIS+N/S — `(east,up,−north)` == our `gv(e,u,−n)` CONFIRMED to certainty (was ~75%): the adapter re-bin with north=−Z lands 1362/1362 (100%) buildings inside our bbox, north=+Z lands 0; +Y up (POSITION Y∈[−1,70] m); browser river/bridge geography not mirrored. (2) SEATING — ground Y≈0 (min −1.0 m) matches the h=0 bake contract + runtime clamp-to-CWT (R1); browser @646 m & @276 m building bases flush on the street grid, no float/sink. (3) IDENTITY — fully recoverable FREE: `keepOsmElements=true` → one NAMED node per element carrying `extras.osmId` (`"Building w294038853"`) → contiguous `_feature_id_0` per building, no connected-component labeling. (4) MB/CELL — buildings-only re-bin 2.91 MB/21 cells, densest cell (4,5) 1.219 MB raw (vs the current baker's same cell 0.855 MB, ~1.4×) → weld+draco 52 KB (23×); the scary raw 25.66 MB glb was ~89% ROADS (RoadModule on + Overpass `>;` extent-recursion to a 200 km bbox — the fix = filter to `Building*` nodes).** CORRECTIONS to the prep doc (now in §9): the distro is **`0.5.0-SNAPSHOT`, NOT 0.4.0** → modern subcommand CLI `convert -i <in> -o <out.glb> --config <p> --lod 2` (+ MANDATORY `--add-exports java.base/java.lang,java.desktop/sun.awt,java.desktop/sun.java2d =ALL-UNNAMED` on Java 21); origin = data-bounds centre (`scene.extras.origin`) — the adapter re-bases by the constant ENU offset `projectEN(osmOrigin, ourBasis)` = E −7521/N +6443 m; no `_texture0_dir` ⇒ genuinely 0 textures (color rides `COLOR_0`, dropped); `excludeWorldModule` needs Road/Railway/SurfaceArea too (or the node-class filter). The owner's "other constructions" ARE present + node-classified (Wall 23 · PoleFence 71 · RetainingWall 4 · ChainLinkFence 3 · MobilePhoneMast 1 · StreetLamp 26 · BollardRow 7 · AreaFountain 3). The enriched OSM2World tileset is a visual DROP-IN — `buildingMaterial.ts` swap works, 9 cells streamed, NO shader-link/tile errors (the only console errors were the benign anonymous-member 403 + a blocked Wix telemetry beacon). **NEXT = build the full adapter: `scripts/bake/bake-osm2world.mjs` mirroring `bake.mjs`, reusing `encodeGlb`/`buildTileset`/`geo.mjs`/`vegetation.mjs` verbatim; promote `spike-osm2world/readGlb.mjs` → `lib/readGlb.mjs`; production extract = Geofabrik PBF + `osmium extract`+`tags-filter --omit-referenced` C6; decide draco-decode-wiring vs ship-uncompressed + which construction classes to keep.** Files: git-ignored spike scaffolding `scripts/bake/spike-osm2world/{readGlb,01-extract-and-filter,02-inspect-glb,03-adapter}.mjs` + `work/stylized.properties`; EDITED `.gitignore` (spike ignore) + `.claude/claude-docs/OSM2WORLD_EXPERIMENT_PREP.md` (§9 verdict + top pointer). Memory: `mem:project/wip-2026-07-14-osm2world-slice1.5-spike`.
- **2026-07-14 (latest) — OWNER R2 HOSTING LIVE (all stages) + OSM2WORLD EXPERIMENT PREP (`/frame` + investigate-design-v3; implement + research/design). Ops/docs only → local gates unchanged (astro check 0/0 · vitest 513); the tiles are LIVE-verified via curl (CORS/preflight/206/glTF-magic/health), the globe-paints-from-R2 browser check is the one marked tail.** **(A) R2 HOSTING — the Slice-2 (d) owner-gated tail is DONE, live at `https://frame-the-world.ievgen-sukhov.workers.dev`.** The owner's `.env.local` revealed a BETTER setup than the old custom-domain recipe: a Cloudflare **Worker** bound to a **private** bucket `frame-the-world-bucket` (r2.dev serves no CORS + owner has no domain → a Worker over a private bucket gives full CORS/Range control on the free tier while keeping the bucket private). Shipped: **(1)** `upload-r2.mjs` now reads the owner's `CLOUDFLARE_*` env names (fallback `R2_*`); **(2)** uploaded the full-city bake — **96 files · 32.49 MB → `enriched/dnipro/`** (all HTTP 200); **(3)** the starter Worker carried NO `access-control-allow-origin` (every cross-origin tile fetch would be browser-blocked) → replaced with a robust gateway `scripts/bake/r2-worker.mjs` (CORS + OPTIONS preflight + Range/206 + content-type + Cache-Control + `/` health-canary + city-agnostic path serving), deployed via a NEW reproducible `scripts/bake/deploy-worker.mjs` (pure-fetch multipart PUT that **PRESERVES the `R2_BUCKET` binding** — a bare PUT wipes it; current script snapshotted first for rollback); **(4)** `PUBLIC_ENRICHED_TILES_URL` → the Worker URL (same-origin `public/` fallback kept as a comment — baked artifacts are git-ignored so a released/CI build MUST use R2). **VERIFIED live via curl:** tileset.json 200 + `access-control-allow-origin:*` + cache/accept-ranges; OPTIONS→204; Range→206 `bytes 0-99/522048`; full glb→glTF magic; root→`{"service":"frame-the-world-tiles","ok":true}`. **FREE TIER SAFE:** R2 10 GB (33 MB used), egress always free, Class A/B far under; Workers 100k req/day (≈1k globe views/day, cut by browser cache). **EXTENSIBLE:** a new city or wider Dnipro bbox = `bake --city X` → `upload-r2.mjs --city X` (lands at `enriched/X/…`, Worker serves it with ZERO changes; only the client URL moves); a custom domain later = a pure client-URL swap. README §Hosting rewritten (custom-domain → Worker recipe + free-tier math + multi-city). **TRAP: use `node --env-file=.env.local …` (NOT `env $CFENV node`) to load `.env.local` — the shell didn't split a `$CFENV` var into separate `NAME=VALUE` assignments so the API token never reached the process; `--env-file` also parses cleanly past the multi-line PEM.** **(B) OSM2WORLD EXPERIMENT — deep investigation + preparation (owner: "more realistic buildings and other 3d elements and constructions").** 3 parallel cited agents (capabilities 87% · bake-contract 93% · integration/toolchain 88%) → NEW `.claude/claude-docs/OSM2WORLD_EXPERIMENT_PREP.md`. **VERDICT: viable + recommended as fidelity tier 2; drops in cleaner than expected.** De-risks: **the coordinate frame aligns for free** — OSM2World's glTF writer negates Z → POSITION `(east, up, −north)` == our baker's `gv(e,n,u)=(e,u,−n)` (verify N/S on 1 cell); **default vertical = ground Y=0** (`ZeroInterpolator`, no `srtmDir`) matching our runtime clamp-to-CWT; **the jar runs HERE** (Java 21 ✓; distro probed 200/478 MB, Overpass + Geofabrik reachable — 30 bldgs in a 200 m probe box). **What it buys (the headline):** every S3DB roof shape + walls/fences/hedges, bridges, tunnels, power lines/pylons, masts, street furniture, cliffs — real constructions the footprint-extruder can't make (default-on; `excludeWorldModule` toggles). **Pipeline:** OSM extract (Overpass-XML here / Geofabrik+osmium prod) → **C6 pre-filter on the INPUT** (reuse `makeExcluder`) → `OSM2World convert` with a stylized no-texture `.properties` (ground=0, keepOsmElements, exclude Tree/Indoor/Aeroway) → **the load-bearing NEW adapter: `readGlb()` (inverse of `encodeGlb`) + re-bin triangles into our 10×10 grid by centroid + assign `_feature_id_0` per building node + inject OUR instanced trees → REUSE `encodeGlb`/`buildTileset` unchanged** → optional gltf-transform draco → the LIVE `upload-r2.mjs`. **Reuse (cited): `gltf.mjs`/`geo.mjs`/`exclusion.mjs`/`vegetation.mjs` unchanged; only the footprint aggregator → a triangle-binner is new. We do NOT use OSM2World's own tileset exporter (own tile scheme, no batch ids — doesn't match our per-cell contract).** **Owner decision forks (in the doc):** `_feature_id_0` — recover via `keepOsmElements`+adapter labeling (recommended) vs accept per-cell tone/re-seat; textures — strip for v1 (stylized/small) vs realism tier later; modules — keep buildings/barriers/bridges/power, exclude roads/surfaces (we have a vector web); single-tile vs re-grid = **re-grid** (decisive — single loses culling/streaming/per-cell re-seat, not mobile-viable). **NEXT = the "Slice 1.5" spike (exact steps in the doc): 1 km² central-Dnipro Overpass-XML → C6 filter → OSM2World stylized glb → inspect axis/nodes/size → re-bin ONE cell → browser A/B vs the current bake.** Files: NEW `scripts/bake/{r2-worker.mjs,deploy-worker.mjs}` · `.claude/claude-docs/OSM2WORLD_EXPERIMENT_PREP.md`; EDITED `scripts/bake/{upload-r2.mjs,README.md}` · `.env.local`. Memory: `mem:project/wip-2026-07-14-r2-hosting-osm2world-prep`.
- **2026-07-14 (later) — OWNER BATCH: PER-BUILDING TERRAIN SEATING + 4 UI ASKS SHIPPED (`/frame` + investigate-design-v3, implement/Deep), browser-VERIFIED in `wix dev` via Playwright MCP (gates: astro check 0/0 · vitest 513 (+7) · wix build Complete; shots `verify-shots/owner0714-01..03`). R2 hosting deferred again (owner call: fix these first).** **(1) PER-BUILDING/PER-TREE RE-SEAT (critical — owner clipped underground and saw sunk/levitating meshes; the known ±10–20 m within-cell-relief limit of the per-cell plane):** each building (one contiguous `_feature_id_0` vertex run; glb local +Y IS geodetic up — the baker maps ENU (e,n,u)→glTF (e,u,−n)) and each tree instance now ADDITIONALLY lifts by (terrain@its-own-footprint − cell seat), written INTO the position attribute / instanceMatrix[13] **on the CPU — deliberately, so the planFeed occlusion sweeps, shadow maps and controls picks read the same arrays** (a shader displacement would desync the skyline planner — rejected). Edge strokes co-mutate via an exact-position→feature CSR map built from the PRISTINE buffers (EdgesGeometry copies floats verbatim); pure helpers `featureRunsOf/runCentroid/vertexKeyToRun/mapVertsToRuns/csrFromRunIds` in `lib/globe/enrichedMask.ts` (+5 tests). Budgeted sampling (16 bldg + 10 tree heightAt raycasts/frame; HALF always on the camera-nearest cell → the street you stand on seats in ~1 s, half round-robin); footprints located lazily one-shot per cell (gated on the first cell write — TilesGroup matrixWorld trap); snap-first/ease-later (`seatStep`), 1 cm deadband, bounds padded once (`reseatBoundsPadM` 40). **TWO GARBAGE-SAMPLE GUARDS, both browser-caught live:** a streaming-time raycast returns coarse-LOD garbage → clamped 0 → a first-sample snap put a building **−134 m underground**; (a) samples accepted only within `reseatFeatureMaxDeltaM` (45 m) of the cell seat, and (b) the pair can be poisoned the OTHER way (the CELL seat itself garbage-0 when the feature samples, |0−0| passes, then the cell corrects) → an implausible delta at APPLY time nulls the feature seat + eases back to the cell plane + re-samples. planFeed consistency: enriched handle gains `seatState()`/`debugSeats()` (`__globe.enrichedSeats()` DEV seam), planFeed gains `invalidate()`, orchestrator invalidates a READY profile ONCE per settled seating epoch (`PLAN.reseatQuietFrames` 90 — no rebuild thrash while easing). **Measured live: applied deltas settle to −17.0…+18.7 m (real relief); 12,113 features + 7,433 trees across 48 cells ALL sampled; epoch freezes, quiet grows; transients now capped at the ±45 bound.** **(2) ALWAYS-ON VIEWER COORDS (left card, both modes):** new `camera.camGeo` mirror (camera-nadir geodetic + `ground.heightAt`, null-honest, ~0.1 m deadband; pose-mirror cadence in orbit / HUD cadence while FPV walks) → FpvHud card renders whenever camGeo exists: POSITION row in the exact Google-Earth paste shape (new `formatLatLonPaste` — signed decimal 6 dp, ASCII minus, unit-tested) + COPY (clipboard.writeText → "COPIED ✓"; the card stays pointer-events:none — the row re-enables itself) + GROUND row. TRAP: clipboard **readText()** hangs headless Playwright on a permission prompt — verify by UI state. **(3) ALL FLOATING PANELS DRAGGABLE:** new `ui/DragGrip.tsx` (usePanelDrag + ⠿ grip; a dedicated handle, never whole-surface — sliders/encoders own their pointerdowns) + `styles/drag-grip.css`; the hook writes `--drag-x/--drag-y` custom props and EACH PANEL'S CSS composes them into its own transform (`.ts`/`.lf` keep their `translateX(-50%)`); offsets clamped on-screen, session-remembered per key, double-click resets; wired into `.ct-stack` (safe ONLY because .ct-pinpop is a sibling), `.ts`, `.tr`, `.lf`, `.pp-root`, `.fh`, `.pd` (inset grip — overflow clips the overhang; pd-enter keyframes compose the vars), `.mp-panel` (inset, corner-left), `.mm`; UploadFlow is a full-screen page — skipped. Verified: drag −180/−120 moves, dblclick resets. **(4) FPV ALTITUDE INVERTED:** the two FPV `fpvEyeM/fpvLiftM` clamps flipped − → + in StylizedTiles — **drag right = ASCEND** (orbit ZOOM untouched); verified 1.7→7.4 m on +rate. **(5) FPV MINI-MAP (new `MINIMAP` tuning group; `patchM` 200 = THE owner tunable):** `vectorTiles.ts` now parses the OpenMapTiles **`building`** layer (probe-verified over Dnipro: 1,539 footprint rings in the central z14 tile; `vectorFeatures` skips the new kind) → NEW `scene/minimapFeed.ts` (step 40) reuses the SHARED vtiles cache (zero new network usually), clips features to 1.6×patchM around the eye, projects to origin-local metres (equirect; sub-cm at this scale) → NEW `store/minimap.ts` (features on tile-arrival/60 m walk; pose {dx,dy,heading} at ~20 Hz) → NEW `panels/MiniMap.tsx` + `styles/mini-map.css`: 176 px north-up canvas, token colours via getComputedStyle, green/water/building fills + class-tiered road strokes + heading wedge at centre, N + "200 M" labels, FPV-only (unmounts on Esc), welcome-hidden, draggable. Verified: 32 lines + 20 fills incl. buildings; pose tracked a 33.4 m arrow-key walk. Files: NEW `ui/DragGrip.tsx` · `styles/drag-grip.css` · `scene/minimapFeed.ts` · `store/minimap.ts` · `panels/MiniMap.tsx` · `styles/mini-map.css`; EDITED `scene/enrichedBuildings.ts` · `lib/globe/enrichedMask.ts` · `scene/planFeed.ts` · `scene/vectorTiles.ts` · `scene/vectorFeatures.ts` · `StylizedTiles.ts` · `tuning.ts` · `store/camera.ts` · `lib/format/readout.ts` · 8 panels · 8 panel CSS · `global.d.ts` · `index.astro` · 2 test files. UNVERIFIED tails: sub-M3 cost of the apply-pass compares + 20 Hz canvas redraw · photo-FPV coords/minimap (temp-FPV verified; same mirrors) · heading-up minimap mode (north-up shipped). Memory: `mem:project/wip-2026-07-14-owner-batch-seating-ui`.
- **2026-07-14 — PASS 3 (astro/obstruction moat) + SLICE 5 (enrichment feed) SHIPPED (`/frame` + investigate-design-v3, implement/Deep), browser-VERIFIED in `wix dev` via Playwright MCP (gates: astro check 0/0 · vitest 506 (+33) · wix build Complete; shots `verify-shots/pass3-01..02`).** The WS4 quick-win AND structural capability in one pass: **(planner)** pure `lib/ephemeris/planner.ts` — sun/moon rise/set with eye-height dip (`SearchRiseSet`), civil dawn/dusk, **golden windows DERIVED from tuning.GOLDEN** (bell=0.5 at the smoothstep band midpoints → `asin(mid sines)` = −6.53°/+15.37° at current tuning — chips land where the render actually turns golden), culminations (`SearchHourAngle`), next full/new moon; **`skylineState` = the moat**: next clear/block crossings of `bodyAlt(t) − profile(bodyAz(t))` via 12-min scan + bisection ±0.5 s (own root-find — the profile is piecewise-bin, astronomy `Search`'s quadratic interp has nothing to exploit). **(profile)** pure `lib/geo/horizonProfile.ts` — azimuth-bin max-elevation container + terrain march (curvature−refraction drop `d²(1−k)/2R`, k=0.13) + per-bin known flags (**the WS4 LOD-honesty contract**: unloaded tiles ⇒ bin stays "open sky", coverage reported, trust radius 3 km). **(occlusion — the Slice-5 design crux: NO Raycaster)** pure `lib/geo/occlusion.ts` — building TRIANGLE-EDGE sweeps with **azimuth-adaptive subdivision sized by the segment's closest approach** (endpoint sizing skips bins on long near walls — pinned by the street-canyon test) + OSM vertices rejected inside the `bboxClipPrismEcef` mask prism (the OSM mask is FRAGMENT-level clipping — a CPU ray would hit clipped-away mass; either-endpoint-inside drops the edge, enriched owns the interior) + **trees as analytic canopy spheres read straight from the slice-3 `EXT_mesh_gpu_instancing` TRS** (sidesteps their `raycast=()=>{}` noop AND per-instance raycast cost; ~0.5 ms/25k trees vs ~3.7 ms/ray via InstancedMesh.raycast). **(wiring)** `scene/planFeed.ts` + orchestrator **step 40** (LAST — enriched cells re-seat every frame, sweeps need post-update matrices): anchor = photo apex > FPV eye > view focus (chips only); TIME-SLICED build (3 terrain bins + 2 meshes/frame — reseatSamplesPerFrame idiom); `store/plan.ts` mirrors at 12-frame cadence behind a change signature; scan throttled (5-min scene staleness / 900 ms real); focus quantized 0.05° + skipped while the panel is closed (LEO drift would churn finder calls). **(UI)** `panels/PlanPanel.tsx` PLAN pill (left 18rem — the free strip between FpvHud and TimeScrubber) → BEHIND SKYLINE/CLEAR verdict rows with CLEARS/HIDES jump chips + "SKYLINE N% MAPPED · 3 KM TRUST" + almanac chips → `timeStore.setTime`. New `PLAN` tuning group; DEV seams `__planStore` + `__globe.plan()`. **Browser-verified numbers:** city-centre FPV profile coverage 1.0, skyline 0.4–13.3°; **sun clears the REAL skyline +37 min after astronomical sunrise (02:29Z vs 01:52Z) and drops behind the west skyline 55 min before sunset**; crossing precision 0.42° margin at +90 s; blockedNow flips on scrub; chip click jumps scene time; park anchor reads canopy-consistent skyline (max 5.4°, 4.9k tree instances) — **trees ARE occluders (Slice 5 DoD)**; FpvHud and planner agree independently (sun −0.9° vs skyline 1.3° → blocked; moon +2.7° → clear); console clean. **BUG found live + regression-tested: zenith overflow** — a canopy grazing the eye pushed altTop to 92° and its 1/cos azimuth spread painted ALL 120 bins (first park build read "everything blocked ≥2°, max 92.1°"); FIX = clamp altTop ≤ 89.9 + azHalf ≤ 180 (f32 bins round 89.9 up — assert `< 90`). **TRAPS:** `Bash cmd \| head -N` SIGPIPE-kills a background `wix dev` when head exits (the mid-verify 87-error storm was the page losing its server — pipe to a file); the `#p=` restore glide at low altitude can keep steering for minutes and EXITS temp-FPV instantly → grab (pointerdown/move/up clears targets) then arm FPV; `sampleProfile` interpolates between bin CENTRES → march/tests at (i+0.5)·binWidth; b3dm positions may be interleaved/normalized → fall back to getX/getY/getZ; `ctx.fpvEyeAboveGroundM` is STALE while orbiting a placed photo → photo anchors derive eye height from apex−terrain. **UNVERIFIED tails:** photo-apex anchor in-browser (same startBuild construction as the verified FPV path — needs a real placement flow) · landmark-height validation (Slice-5 DoD tail) · dayArcs skyline fold (seam ready: `planFeed.profileSample()`) · post-build streamed cells don't retro-raise the profile (rebuild on >25 m anchor move only). Files: NEW `lib/ephemeris/planner.ts` · `lib/geo/{horizonProfile,occlusion}.ts` · `scene/planFeed.ts` · `store/plan.ts` · `panels/PlanPanel.tsx` · `styles/plan-panel.css` · 3 test files (+33); EDITED `tuning.ts` (PLAN) · `StylizedTiles.ts` (step 40) · `global.d.ts` · `pages/index.astro`. Memory: `mem:project/wip-2026-07-14-pass3-obstruction-moat`.
- **2026-07-13 — SLICE 3 TREES SHIPPED (Dnipro 3D enrichment; `/frame` + investigate-design-v3, implement/Deep), browser-VERIFIED in `wix dev` via Playwright MCP (gates: astro check 0/0 · vitest 473 (+14) · wix build Complete; shots `verify-shots/slice3-01..04`).** ~24.7k instanced trees now ride the enriched tileset. **(DATA)** Dnipro maps only **418 `natural=tree` nodes** (Overpass probe) → the bake leans on **202 `tree_row` ways (sampled every `rowSpacingM` 9 m) + seeded jittered-grid scatter over `natural=wood`/`landuse=forest` (13 m) + `leisure=park` (24 m)** polygons (grass NOT planted; outer rings only); placements rejected inside ANY building footprint (spatial-hash index, 424 rejected), C6-excluded zones, and outside the bbox; capped `maxTrees` 60k via hash-probability thinning; ALL hash-seeded (mulberry32/FNV — re-bakes byte-identical). Result: **417 points + 2,754 row + 18,121 wood + 3,422 park = 24,714 trees · 94 tiles (4 tree-only cells) · 34.05 MB (+1.2 MB ≈ 50 B/tree)**. **(FORMAT — the design crux, source-verified 2-way)** each cell glb gains ONE **`EXT_mesh_gpu_instancing`** node ("ftw-trees": unit tree mesh + TRANSLATION/ROTATION/SCALE f32 accessors; `extensionsUsed` only, never Required) — three **0.185 registers `GLTFMeshGpuInstancing` by DEFAULT** (GLTFLoader.js:249,1682) and builds ONE `InstancedMesh` per cell; `GLTFExtensionsPlugin` only ADDS extensions. So trees inherit cell streaming + LRU + the per-cell terrain re-seat with ZERO new renderer machinery. Unit tree = 8-gon double-cone canopy + prism trunk (72 verts, flat-shaded soup); per-instance scale (radius/0.5, height, radius/0.5) + yaw quat. **(RUNTIME)** `enrichedBuildings.ts`: `isInstancedMesh` branch FIRST in load-model (InstancedMesh passes `isMesh` — else it'd get building material + EdgesGeometry); ONE shared `treeMat` (MeshStandard flatShading `tokens.vecGreen`); `setNight` dims canopy albedo CPU-side (`TREES.nightDim` 0.55 — live-verified `#2e4a3a` day ↔ `#1d3126` night); `setSolidity` fades trees with the FPV BUILDINGS slider (floor `TREES.fpvMinOpacity` 0.15); trees cast+receive shadows (`TREES.castShadow`); new `TREES` tuning group. **TWO SOURCE-VERIFIED TRAPS:** (1) 3d-tiles-renderer disposal never calls `mesh.dispose()` and `instanceMatrix` is an attribute ON THE MESH → **every LRU eviction leaks the instance GL buffer** unless dispose-model calls `c.dispose()` on InstancedMesh (added; three's WebGLObjects frees it on the 'dispose' event); (2) `InstancedMesh.raycast` iterates EVERY instance and GlobeControls raycasts the whole scene per pointer-down → `c.raycast = () => {}` on tree meshes (pivotMesh precedent). **Browser-verified live:** 63–68 InstancedMesh cells / 14.7k–21k instances streaming beside 72 building meshes; trees seated on the park slope + riverbank (per-cell re-seat inherited); full LRU cycle 2.6 km→30 km→2.6 km = 0→68 meshes rebuilt, ZERO new console errors (dispose path clean); night no-glow confirmed. **Terrain decision RECORDED (plan §Slice 3): keep Cesium World Terrain — R1 runtime clamp never forced a co-bake.** Canopy-height rasters (ETH 10 m/Meta 1 m) = documented upgrade tier 4 (`scripts/bake/README.md` — GeoTIFF reader needs deps npm-blocked here; contract-compatible with `packTreeInstances`). UNVERIFIED tails: FPV slider tree-fade interaction (same construction as the verified buildings path, not driven in FPV this session); sub-M3/phone FPS (no device — carried). Files: `scripts/bake/lib/vegetation.mjs` (new) · `lib/gltf.mjs` (multi-node + instancing encoder) · `bake.mjs` · `cities/dnipro.json` (vegetation block) · `README.md` · `scene/enrichedBuildings.ts` · `tuning.ts` (TREES) · `test/bake/vegetation.test.ts` (new, 11) · `test/bake/bake.test.ts` (+3). Memory: `mem:project/wip-2026-07-13-dnipro-slice3-trees`.
- **2026-07-13 — SLICE 2 REST SHIPPED (clip-hole · stylization reconcile · full-city bake · R2 tooling), browser-VERIFIED in `wix dev` via Playwright MCP (gates: astro check 0/0 · vitest 459 (+11) · wix build Complete; shots `verify-shots/slice2-04..08`).** **(b) CLIPPING-PLANE HOLE** — the confirmed coarse-ancestor straddle-leak is fixed pixel-exactly: pure `bboxClipPrismEcef` (`lib/globe/enrichedMask.ts`, +6 tests) builds 4 ECEF planes with NEGATIVE signed distance inside the bbox (E/W = exact meridian planes through the Earth axis, constant 0; N/S = tangent planes at the centre longitude, ~2 m corner error, exact along altitude since north ⊥ up); `buildings.ts` puts them on BOTH shared OSM materials with `clipIntersection` (discard ONLY inside all 4) + `clipShadows` (no phantom shadows in the hole) + `renderer.localClippingEnabled`, all gated on `maskBbox` (no enrichment → byte-identical). Live-verified: OSM fill+edge carry 4 planes/intersection/shadows, enriched carries 0, box core CLEAN of OSM at 28 km, dense OSM just outside survives. **(c) STYLIZATION RECONCILE** — the whole building-material construction (R2 tone via `_batchid`/`_feature_id_0`+tile seed, dormant R3 night hooks, F1 screen-door reveal, FPV ghost curve) extracted to a NEW shared factory `scene/buildingMaterial.ts`; `buildings.ts` consumes it behavior-identically; `enrichedBuildings.ts` swaps its plain MeshStandardMaterial for its OWN factory instance (separate instances are REQUIRED — a literally-shared material would clip the enriched set away inside its own prism) + per-tile birth/seed onBeforeRender writes + uNowMs clock + NEW `setNight` fan-out (StylizedTiles drives both tilesets from ONE ephemeris sample); `ENRICHED.debugDistinctEdges=false` (accent → landHi). Live: enriched glbs expose `_feature_id_0` (global ids across the bake) + the injected shader (uFtwTileSeed probe) — OSM and enriched read as ONE city at the NW boundary. **(e) FULL-CITY BAKE** — `cities/dnipro.json` bbox → [35.00,48.42,35.10,48.50] (~7.4×8.9 km), grid 7→10, tilesetVersion dnipro-real-2, `ENRICHED.bbox` synced: 26,996 footprints → C6 excluded 72 (53 substations/1 plant/5 generators/13 transformers) → **26,569 buildings · 90 tiles · 1,169,172 verts · 32.8 MB · maxH 167 m**; all 90 cells stream, LRU-evict and RE-stream (verified 90→42→0→90 across zooms); group lift 98.06 m ≈ terrain. **(d) R2 TOOLING (hosting = owner-gated)** — NEW pure-Node SigV4 uploader `scripts/bake/upload-r2.mjs` + `lib/s3sign.mjs` (+5 tests; no SDK/wrangler — npm blocked here), dry-run verified (92 files/31.3 MB); bucket + custom domain + CORS remain an owner Cloudflare action (r2.dev serves no CORS), recipe in `scripts/bake/README.md` §Hosting. **TRAP (cost ~10 min): extracting module-level GLSL consts to a new module left the RUNNING dev server serving a STALE HMR transform of `buildings.ts` → runtime `ReferenceError: FTW_BAYER_GLSL is not defined` from `onBeforeCompile` on every frame; the on-disk file was clean — restart `wix dev` (the known Vite optimize-dep desync; a client reload can't clear it).** KNOWN LIMITS: the enriched set has no HLOD → above ~20 km the box renders EMPTY of buildings (enriched SSE-culls, OSM in-box is now correctly clipped — the honest trade until an HLOD tier; at ≤12 km all 90 cells render); sub-M3/phone FPS benchmark still open (no such device here). Files: `scene/buildingMaterial.ts` (new) · `scene/buildings.ts` · `scene/enrichedBuildings.ts` · `StylizedTiles.ts` · `tuning.ts` · `lib/globe/enrichedMask.ts` · `scripts/bake/{cities/dnipro.json,upload-r2.mjs,lib/s3sign.mjs,README.md}` · tests (+11). Memory: `mem:project/wip-2026-07-13-dnipro-slice2`.
- **2026-07-13 — SLICE 2 HEAD: TERRAIN ELEVATION / PER-CELL RE-SEAT SHIPPED (owner #4 "all buildings at water level"), browser-VERIFIED in `wix dev` via Playwright MCP (gates: astro check 0/0 · vitest 448 (+6) · wix build Complete; shots `verify-shots/slice2-01..03`).** The enriched tileset was lifted by ONE `terrainHeightAt` sample (bbox centre) → a flat plane over the ~5.9×6.3 km bake. NOW each grid cell (a separate leaf tile) ADDITIONALLY offsets its scene along ITS OWN geodetic up by (terrain@cell-centre − centre seat): registered on `load-model` from the tile's raw region bounding volume (`regionCenterDeg`), sampled round-robin `ENRICHED.reseatSamplesPerFrame` 6/frame (bounded raycast cost; 46 cells sweep ~8 frames), first sample SNAPS + refinements EASE (`seatStep`, `reseatEaseK` 0.12), sticky-last-good + `clampGroundM` (the S2 terrain discipline), gated on `reseatToTerrain && reseatPerCell` + a real centre sample (unsampled cells stay on the centre plane = old behavior). **Measured (41 loaded cells): per-cell offsets match terrain deltas within ~0.2 m — spread −20.6 m (riverbank) → +82.0 m (SW hills), a 102 m relief that was previously ONE plane; group lift 98.64 m == terrain@centre.** CWT-renders-elevation CONFIRMED over Dnipro (probes: riverbank 75.6 / centre 98.6 / SW hills 175.2 / left bank 82.1 m). The frame-of-ref open question RESOLVED empirically: offsets are applied in the GROUP frame along the cell's geodetic up — the same construction as the verified whole-group lift, no glb-local axis assumptions. TRAPS: **`TilesGroup.updateMatrixWorld` recurses into children ONLY when the group's own matrix changed** → every per-cell `scene.position` write must call `scene.updateMatrixWorld(true)` itself (the library does the same on visibility flips); per-cell content shifts against its STATIC region bounding volume → the baker now pads region heights ±80 m (`RESEAT_PAD_M`; re-baked — identical 46 tiles · 731k verts · 20.51 MB, only tileset.json regions changed). Also: a hash-only URL change does NOT re-boot the pose — force a full reload (about:blank hop) when driving `#p=` poses in verification. KNOWN LIMIT: within-cell relief (~±10 m on steep cells) remains — finer grid (`cities/dnipro.json` grid 7→10+) or per-building seating is the upgrade if wanted. Files: `scene/enrichedBuildings.ts` (cell registry + sample/ease/apply), `lib/globe/enrichedMask.ts` (`regionCenterDeg` + `seatStep`, pure), `tuning.ts` (ENRICHED.reseatPerCell/reseatSamplesPerFrame/reseatEaseK), `scripts/bake/bake.mjs` (region pad), `test/lib/globe/enrichedMask.test.ts` (+6). Memory: `mem:project/wip-2026-07-13-terrain-reseat`.
- **2026-07-13 — OWNER FEEDBACK (Dnipro enrichment + FPV UX): bake EXPANDED + 3 FPV controls, all browser-VERIFIED (astro check 0/0 · vitest 442 · wix build). Shots `verify-shots/slice1-04..06`.** Owner saw only the central 2.2 km enriched + asked for 4 items. **(1) Bake EXPANDED** to ~5.9×6.3 km ({35.005,48.435,35.085,48.492}, grid 7×7) → 46 tiles · 731k verts · 20.5 MB · maxH 167 m, both Dnipro banks + across the bridge; `cities/dnipro.json` bbox + `ENRICHED.bbox` synced. **(2) FPV max focal 500 mm** (`FPV.minFovDeg` 8→2.75; verified camFov clamps 2.75° / HUD 500 mm). **(3) FPV arrow-key WALK** (`FPV.walkSpeedMps` 22): held arrow integrates a ground-plane displacement along the horizontal look dir in `stepFpvPose` (◀▶ strafe; `onFpvKey`+new `onFpvKeyUp`; reset on entry) + a HUD hint; verified ~22 m/s. **(4) FPV BUILDINGS slider** (0=see-through wireframe, 100=solid shaded): `camera.fpvBuildingSolidity` → OSM `setGhostSolid(max(auto,s))` + enriched NEW `setSolidity(s)` (fill 0.28→1 opacity + edge 0.5→0.14 fade); `CameraTiltPanel` Slider (FPV-only); verified both ends. Files: `store/camera.ts`, `StylizedTiles.ts`, `scene/enrichedBuildings.ts`, `panels/{CameraTiltPanel,FpvHud}.tsx`, `styles/fpv-hud.css`, `tuning.ts`, `scripts/bake/cities/dnipro.json`. **DEFERRED (owner #4, next session): TERRAIN ELEVATION / per-cell re-seat** — owner: "all buildings at water level"; the enriched tileset is lifted by ONE terrain height (bbox centre) → a flat plane; fix = per-cell re-seat (each grid tile lifts by its own centre's `terrainHeightAt`; frame-of-ref needs a browser test) + confirm CWT renders elevation. Memory: `mem:project/wip-2026-07-13-dnipro-slice1-bake` §owner-feedback.
- **2026-07-13 — SLICE 0 BROWSER-VERIFIED + SLICE 1 REAL BAKE SHIPPED (Dnipro 3D enrichment; `/frame` + investigate-design-v3, implement/Deep). Gates: astro check 0/0 · vitest 442 (+19 bake) · wix build Complete. Slice-0 gate AND the real bake are BROWSER-VERIFIED in `wix dev` via Playwright MCP (shots `verify-shots/slice0-01..06` + `slice1-01..03`).** **(A) Slice-0 gate PASSED** (unblocks the whole enrichment): flew to the sample bbox; via `window.__globe` seams + a mask audit replicating the plugin's own `bv.getSphere→ellipsoid.getPositionToCartographic→bboxContainsRad` — enriched renders UPRIGHT (up-axis correct, the one thing synthetic data couldn't prove) w/ gable+flat roofs; **R1 clamp-to-CWT seating correct** (`enriched.group.position.length` 100.2 m ≈ `terrainHeightAt` 99.6 m; bases on the street, no float/sink); **mask clean at leaf level — 0 OSM tiles centered in bbox** at 1.2 km AND 520 m (OSM-only A/B shot shows the building-free hole); shadows cast. **KNOWN-LIMIT CONFIRMED real+minor: ~8 coarse OSM tiles STRADDLE the bbox edge → a thin rim leaks → Slice-2 clipping-plane hole is genuinely needed.** Layer A/B is safe (`renderer.info.render.frame` advanced while a group hidden → no stale-frame trap). Tilt: LOW=top-down, ~86°=horizon (inverted). **(B) Density frame-challenge (Overpass, central Dnipro 6698 bldgs): `building:levels` 45.2% · any-height 47.5% · `roof:shape` 9.5%** — the plan's "<10% height" is the CITY-WIDE avg (periphery-dragged); central Dnipro is ~47% tagged → the LIGHT path gives a good result. **(C) Env probe:** Java 21 ✓ · Python 3.10+PyPI ✓ · OSM2World download ✓ · but **Docker daemon NOT running** (heavy 3dfier path = code+recipe only) + **public npm BLOCKED** (`3d-tiles-tools` glb→3D-Tiles unavailable). **(D) SLICE 1 = a NEW reproducible Node DATA-DRIVEN baker** (owner-picked over OSM2World/heavy — reuses the browser-VERIFIED glb+tileset writer + runs+verifies HERE, no Docker/npm): `scripts/bake/` (`bake.mjs` CLI + `lib/{geo,gltf,exclusion,buildings,overpass}.mjs` + `cities/dnipro.json` + `README.md`), `npm run bake -- --city dnipro`. Pipeline: Overpass footprints+tags (cached) → **C6 exclusion BEFORE tiling** (built-in military/critical-infra tag blocklist + per-city polygons; excluded counts reported) → **height inference** (`height`→`building:levels×3m`→per-class→generic) → **roof-shaped extrusion** (flat ear-clipped cap / gabled OBB-ridge / pyramidal centroid-fan; watertight, outward normals) → **spatial-grid 3D-Tiles 1.1** (per-cell glb, ONE shared ENU→ECEF frame so the R1 re-seat seats every cell; `_FEATURE_ID_0` per building for Slice-2 stylization). **REAL Dnipro bake BROWSER-VERIFIED:** 3304 footprints → C6 excluded 21 (13 substations/1 plant/7 transformers) → 54% real heights → **3233 buildings · 16 tiles · 171,957 verts · 4.83 MB · maxH 129 m** → rendered upright+seated (`group.position.length` 101.3 ≈ terrain 101.3) + masked + streaming, 0 tile/shader errors; native-edge recolor (0x687d73 = OSM sage) reads as a seamless extension of the OSM style. Bake logic unit-gated (`test/bake/bake.test.ts`, +19). **Wiring:** `ENRICHED.bbox` expanded to {35.03,48.452,35.06,48.472} to MATCH the bake extent (mask==enriched extent; byte-identical when the layer is OFF) · `.env.local` URL → `/enriched/dnipro/` · `package.json` `bake` script · `.gitignore` `public/enriched/`+`scripts/bake/.cache/`. **KNOWN LIMITS → Slice 2:** single-point re-seat floats a few m near the river over a 2 km bbox (fix = per-cell re-seat — cells are already separate tiles) · boundary OSM leak (clipping planes) · single-LOD grid · roof OBB overhang on non-rectangular plans. **OSM2World + heavy 3dfier LOD2 = documented upgrade tiers (`scripts/bake/README.md`).** Memory: `mem:project/wip-2026-07-13-dnipro-slice1-bake`.
- **2026-07-13 — VERIFIED (owner: "working"): the shadow-governor fix RESTORED building cast shadows** (building→ground AND building→building) on the M3 Pro — owner browser-confirmed. So the whole illumination pass (crisp shadows + sun/moon golden-hour GI + brighter dark-of-moon night; DRAPE.shadowOpacity 0.80, minSunElevSin 0.008, altitude-adaptive shadow bounds, GOLDEN band/strengths + keyBrighten/moonKeyStrength) is now **owner-VERIFIED**. Illumination + Slice-0 enrichment de-risk are both DONE. **Next initiative: the FULL Dnipro 3D enrichment implementation (Slices 1→5 of `DNIPRO_3D_ENRICHMENT_PLAN.md`)** — the real bake (OSM2World light path / Overture+height-inference heavy path) → glb→3D-Tiles → self-host on R2 (custom domain + CORS) → mask & stream over Dnipro → trees → feed the Pass-3 obstruction moat. Handover: `NEXT_SESSION_PROMPT.md` (rewritten this session for the full impl).
- **2026-07-13 — FIX (owner-CONFIRMED root cause via a live `window.__globe` console diagnostic): "not a single cast shadow" = the runtime FRAME GOVERNOR threw the M3 Pro all the way to tier `low`, and `low.shadowsEnabled=false` disables the ENTIRE shadow pass (`renderer.shadowMap.enabled=false` → `hasShadowMap:false`), even though the diagnostic proved `sun.castShadow:true`, `14/14` buildings casting, `32` ground shadow-twins ready. NOT a pipeline bug — a quality-governor over-degradation. Two fixes:** (A) **SHADOWS DECOUPLED from the governor** — shadow enable + map size now follow the DEVICE tier (capability, set once at init on `deviceTier`), and `applyTier` no longer touches `renderer.shadowMap.enabled`/size; a capable machine keeps shadows even when the governor sheds DPR/bloom/tile-detail for frame rate (shadows are a core aesthetic, not a degradable lever). (B) **GOVERNOR FLOOR** — `makeGovernor` gains a `floor` param; GlobeCanvas passes `floor = deviceTier==='high' ? 'mid' : 'low'`, so a confirmed-strong device never collapses below `mid` (keeps bloom + DPR 1.5 + shadows). The governor was over-degrading the reference M3 Pro at retina DPR under the 22 ms/45 fps budget — violating the quality pass's "M3 Pro stays capable" intent. Files: `GlobeCanvas.tsx` (applyTier shadow-decouple + floor wired), `lib/globe/quality.ts` (makeGovernor floor), `test/lib/globe/quality.test.ts` (+1 floor guard). Gates: astro check 0/0 · vitest 423 · wix build. Root cause is CONFIRMED (live diagnostic); the visual is BROWSER-UNVERIFIED this session — Playwright was blocked by a Vite `504 Outdated Optimize Dep` desync (this session's repeated `wix build` runs raced the dev-server optimizer; a server restart clears it — a client reload can't). After a clean reload the tier floors at `mid`, `shadowMapEnabled` true → shadows render. `mem:project/wip-2026-07-13-illumination-pass` §shadow-governor.
- **2026-07-13 — FIX (owner: "still not a single cast shadow (building→ground AND building→building)", screenshot @ 20:46 local / 1% new moon): ROOT-CAUSED = shadows were correctly GATED OFF for that view, two independent reasons; the shadow PIPELINE was never broken (`renderer.shadowMap.enabled` per tier, per-mesh castShadow/receiveShadow, ShadowMaterial ground twins all fine). (1) TIME/LIGHT — the sun was ≈ +0.7° (essentially at sunset) which is BELOW `SHADOWS.minSunElevSin` 0.03 (~1.7°), and the moon was 1% (new) so `moonShadows` needs illum ≥ moonMinIllum 0.6 → NO caster above threshold at that instant. (2) SHADOW REGION — the ortho was a FIXED ±1600 m patch around screen-centre (tuned for street level), so at an oblique CITY zoom almost every building sat OUTSIDE it → no shadow even at midday. FIX: `minSunElevSin` 0.03→0.008 (~0.46°: shadows persist through the golden/dusk hour — the raking long shadows that are the point at that time — while staying above the horizon so no garbage) + ALTITUDE-ADAPTIVE shadow bounds in StylizedTiles.stepKeyLightAndShadow: ortho half-extent = `clamp(alt × SHADOWS.boundsAltK 0.6, boundsM 1600, maxBoundsM 5000)` with the depth near/far extended by it, so the shadow map covers the visible ground at zoom while street level clamps to the crisp 1600. Gates: astro check 0/0 · vitest 422 · wix build. RUNTIME BROWSER-UNVERIFIED — confirm shadows now appear at this dusk view + tune boundsAltK/maxBoundsM vs the visible extent; note a new/low moon still correctly casts nothing (scrub to a fuller moon phase or to daytime for the cleanest shadows). Files: `tuning.ts` (SHADOWS.minSunElevSin/boundsAltK/maxBoundsM), `StylizedTiles.ts` (adaptive bounds block). `mem:project/wip-2026-07-13-illumination-pass` §shadow-gating.
- **2026-07-13 — IMPLEMENT (owner: "remove windows emulation, never asked for this, looks junky… what I need is buildings casting nice shadows on the ground and other building"): procedural window GRID REMOVED + refocused on building shadows. Gates: astro check 0/0 · vitest 422 · wix build.** Reverted `scene/buildings.ts` to the DORMANT R3 flat emissive (gain 0) — buildings carry **no night emissive** by design; their night identity = dark mass + lit edges + cast/received SHADOWS + the golden/moon key light. Removed the window-grid shader (vertex `vFtwLPos`/`vFtwObjUp`, the `cross(dFdx,dFdy)` per-wall grid, `uFtwWindowCool`) + the 6 BUILDINGS window tunables; `BUILDINGS.nightWindowGain` 0.4→0. **Building-shadow focus:** `SHADOWS.normalBias` 1.0→0.75 (anchor building shadows to their bases — a large offset PETER-PANS them into a "floating" look; 0.75 m still covers the ~0.5 m float32@6.4e6 acne; browser-revert to 1.0 if acne returns) + `SHADOWS.radius` 1→2 (revert the untested cyberpunk-crisp to the verified soft edge). Building→GROUND shadows are already much stronger from the prior line's `DRAPE.shadowOpacity` 0.62→0.80 + brighter `DRAPE.gain` 1.6; building→BUILDING shadows work via `receiveShadow` (contrast limited by the dark palette — a browser-tune call). KEPT all crisp-shadow + sun/moon golden-hour GI tuning from the prior line (those WERE requested). RUNTIME BROWSER-UNVERIFIED — building-shadow niceness (peter-panning vs acne, inter-building contrast) is a visual call to confirm in `wix dev`. Files: `scene/buildings.ts`, `tuning.ts`. `mem:project/wip-2026-07-13-illumination-pass` §update.
- **2026-07-13 — IMPLEMENT (illumination pass, owner: "bring back crisp shadows + more sun/moon golden-hour GI, cyberpunk"): CODE-COMPLETE, locally gated (astro check 0/0 · vitest 422 · wix build), RUNTIME BROWSER-UNVERIFIED (the building GLSL compile is the #1 risk — only surfaces over Dnipro tiles). Designed via a 5-agent understand+design workflow.** DIAGNOSIS (cited): nothing was deleted — illumination was SOFTENED across 3 passes + the "yellow faces" rework only killed the flat R3 night emissive. Real causes: `DRAPE.shadowOpacity` 0.9→0.62 (S7; the DEFAULT dark-drape city view blends toward THIS not SHADOWS.groundOpacity 0.75 — StylizedTiles.ts:1677-79 — so shadows read faint); `ATMOSPHERE.skyHorizonGain` 0.35→0.16 halved the dusk-sky golden; `SUN.hemiIntensity` 0.4→0.25 dropped dark-of-moon fill (moon terms are phase-gated by moonKs≈0.05 near new moon). CEILING confirmed: golden + shadow are both MULTIPLIES against a dark grade → need a brighter base too. CHANGES (all additive; every new gain 0 = byte-identical): **shadows crisp** — DRAPE.shadowOpacity 0.62→0.80 (biggest win), DRAPE.gain 1.3→1.6 + GROUND.gain 0.56→0.60 (brighter base = contrast ceiling), SHADOWS.radius 2→1, moon shadow opacities 0.5/0.55→0.62 + moonMinIllum 0.85→0.6; **sun golden** — GOLDEN band widened (−12°→+21°, symmetric by construction), castGain 1.15→1.3, earth/ground/atm/key strengths ↑ (0.7/0.8/0.6/0.85→0.9/1.0/0.8/1.0), ATMOSPHERE.skyGoldStrength 0.55→0.72 (unguarded) + skyHorizonGain 0.16→0.20 (skyBudget guard still green), **NEW GOLDEN.keyBrighten 0.35** (sunLight.intensity ×(1+goldenK·keyBrighten) — buildings BRIGHTEN warm at dusk, StylizedTiles.ts:1673); **moon golden hour** — **NEW GOLDEN.moonKeyStrength 0.4** (lerp moon key toward goldenHour by bell(moon elev), StylizedTiles.ts:1661); **night GI** — hemiIntensity 0.25→0.32, ambientNightK 0.012→0.02, GROUND.nightFloor 0.35→0.40, EARTH.nightFloor 0.19→0.23, moonSceneGlow 0.35→0.5, moonFillK 0.5→0.7; **CYBERPUNK WINDOWS** — `BUILDINGS.nightWindowGain 0→0.4` now drives a PROCEDURAL lit-window GRID in scene/buildings.ts (replaces the twice-rejected flat emissive): object-space floor bands × window columns via new `vFtwLPos`/`vFtwObjUp` varyings + `cross(dFdx,dFdy)` per-wall tangent, AA panes, per-window on-hash, cool-cyan minority (`uFtwWindowCool`=tokens.atmosphere), wall-gated + top-25% building hash, NaN-guarded, rides the ONE shared material; float32-safe IF object positions are RTC-local (the crux browser check — log geometry.attributes.position bbox at load-model). New BUILDINGS knobs: nightFloorH/nightWindowW/nightWindowFill/nightWindowAA/nightCyanFrac/nightWindowOnFrac. Files: `tuning.ts` · `scene/buildings.ts` (window grid + uFtwWindowCool) · `StylizedTiles.ts` (keyBrighten + moonKeyStrength). **Browser-verify checklist + full old→new table: `mem:project/wip-2026-07-13-illumination-pass`; NEXT_SESSION_PROMPT.md.**
- **2026-07-13 — IMPLEMENT (Slice 0 de-risk spike, `/frame` + investigate-design-v3): the ANALYSIS + INTEGRATION half of the Dnipro-enrichment Slice 0 is DONE + locally gated; the data-bake + R2 + browser-confirm half is reduced to a one-command recipe + a flag flip. All code DEFAULT-OFF behind `PUBLIC_ENRICHED_TILES_URL` (absent → byte-identical). Gates: astro check 0/0 · vitest 422 (+6 enrichedMask) · wix build Complete. Runtime = BROWSER-UNVERIFIED (no browser this session).** The three hard unknowns RESOLVED with cited evidence: **(R1 vertical-datum seating, the #1 risk) = runtime clamp-to-CWT** — Cesium World Terrain renders WGS84-**ellipsoidal** heights (matches our `ground.heightAt`=`getPositionElevation`), open DEMs are geoid-orthometric (GLO-30=EGM2008; **EGM2008 undulation over Dnipro = +20.42 m** via GeographicLib GeoidEval), so a naive absolute-Z bake sinks ~20 m (+~2–6 m inter-DEM residual); fix = DON'T bake absolute Z, bake relative geometry + lift the tileset to the rendered CWT at runtime (matches how Cesium OSM Buildings seat). Impl `scene/enrichedBuildings.ts`: sample `terrainHeightAt(bboxCentre)` → translate `tiles.group.position` along the centre geodetic up; knobs `ENRICHED.seatOffsetM`/`reseatToTerrain`. **(masking OSM in-bbox) = stop-traversal `calculateTileViewError` plugin** (3d-tiles-renderer **0.4.28**, source-verified): return truthy + `target.inView=false` → tile not marked-used/queued/traversed (no download, LRU-evicts); test each tile's bounding-sphere CENTRE lat/lon (`tile.engineData.boundingVolume.getSphere` + `tiles.ellipsoid.getPositionToCartographic`, radians, group-local frame → seat-offset-independent) vs `ENRICHED.bbox`. Impl `scene/buildings.ts` `FTW_DNIPRO_OSM_MASK` (registered only when `maskBbox` passed). **KNOWN LIMIT (Slice 2): coarse-ANCESTOR OSM leakage at low zoom — pixel-exact hole needs THREE `clippingPlanes`; leaf-centre test clean at street level.** **(3rd TilesRenderer + streaming) = plain `new TilesRenderer(url)` + `GLTFExtensionsPlugin{dracoLoader,meshoptDecoder}`** (no ion), seat via `group.position` (sanctioned). **Toolchain verdict: OSM2World** for the light/sample bake (only light tool emitting S3DB roof shapes + glb direct, local z=0 frame) — **3dfier is LoD1-FLAT only, NOT for roofs**; neither emits browser-ready 3D Tiles → need `glb→3D-Tiles` (`3d-tiles-tools`/`py3dtiles`). **R2: `r2.dev` returns NO CORS → Custom Domain required** (free tier fine). No bake tools were installed here (no osm2world/3dfier/py3dtiles/pg2b3dm/wrangler; `npx` can't auto-install), so the sample is **pure-Node hand-baked** (`scripts/build-sample-dnipro-tiles.mjs`: 12 central-Dnipro blocks w/ gable roofs → `public/enriched-sample/dnipro/{tileset.json,buildings.glb}`, git-ignored, baked at ellipsoid h=0 so the runtime re-seat is the ground-truth R1 test; glTF Y-up, region bounding volume). Files: `lib/globe/enrichedMask.ts`(+test 6) · `scene/enrichedBuildings.ts`(new) · `scene/buildings.ts`(maskBbox) · `tuning.ts`(ENRICHED group) · `StylizedTiles.ts`(attach+stepEnrichedUpdate+quality+dispose+`__globe.enriched`) · `scripts/build-sample-dnipro-tiles.mjs`(new) · `.gitignore`. **VERIFICATION-BLOCKED (browser tier — next session): `node scripts/build-sample-dnipro-tiles.mjs` → `PUBLIC_ENRICHED_TILES_URL=/enriched-sample/dnipro/tileset.json` → `wix dev` → fly to Dnipro → confirm accent-edged enriched buildings + OSM masked in-bbox + seating (tune seatOffsetM) + streaming + the up-axis (buildings upright — the one thing synthetic data can't prove; fix = the known 90°); then Slice 1 real OSM2World bake + R2.** Full verdict + recipe: `.claude/claude-docs/DNIPRO_SLICE0_SPIKE.md`. Memory: `mem:project/wip-2026-07-13-dnipro-slice0-spike`.
- **2026-07-13 — VERIFIED (production, owner-confirmed "worked, preview updated"): the gallery/social-preview fix (see the IMPLEMENT line further down) WORKS LIVE.** After `wix release` + a snapshot re-capture (re-save submission / @tuvitk), the internal "Headless Day" gallery card for "Frame the World" refreshed from the stale Wix+Astro starter page to the new SSR boot-poster/globe. Confirms the full chain: the `site-snapshotter` DOES re-capture on demand, and what it grabs is the **SSR boot poster** — the `client:only` homepage alone would have stayed blank. `mem:bugs/gallery-thumbnail-stale` = RESOLVED.
- **2026-07-13 — DESIGN (owner): external Dnipro-3D-enrichment research INCORPORATED → plan + next-session brief. Decision = an OFFLINE BAKE of roof-shaped LOD2 buildings, SELF-HOSTED on Cloudflare R2, with Cesium OSM Buildings MASKED inside the Dnipro bbox. No code yet (research-VERIFIED external + cited; design UNVERIFIED — no spike). New docs: `DNIPRO_3D_ENRICHMENT_RESEARCH_RESULTS.md` (cited report) + `DNIPRO_3D_ENRICHMENT_PLAN.md` (execution plan/backlog).** Reframes/retires **R4** (client-side S3DB roof reconstruction): its blocker was reconstructed roofs z-fighting the streamed Cesium LOD1 prism + needing a batch-id→OSM-id cull the one-material swap can't do — **bake-and-mask REPLACES the buildings in-bbox instead of overlaying, sidestepping the blocker.** **Extends (breaks nothing):** D1 (Cesium OSM Buildings stays the GLOBAL fallback; enriched tileset = a 2nd `TilesRenderer` masked to the bbox; engine unchanged) · D13 (self-host OUR derived tiles on **R2, NOT ion Community** — non-commercial + we have a paid marketplace; still stream Cesium's own ion assets #1/#96188) · C6 (three baked-in rules: a military/critical-infra **exclusion mask** applied before tiling, **archival/pre-war imagery only** — fresh UAV capture is illegal under martial law, and reduced precision carried onto the enrichment layer) · C1 (bake is OFFLINE/one-time, tiles static on CDN, runtime is pure client streaming). **Stack "b" ($0 data):** Overture footprints (ODbL) + Microsoft ML footprints (covers UA) fallback → height inference (`levels×3` → MS ML → class default; the height GAP is the real problem — <10% of Dnipro's 85,802 bldgs carry height) → 3dfier/City4CFD straight-skeleton LOD2 roofs → CityJSON → py3dtiles/pg2b3dm 3D Tiles (~0.1–0.3 GB) → R2 (free tier, zero egress); ETH/Meta canopy → instanced trees; hero-landmark Gaussian splats (COLMAP+gsplat→Spark) = DEFERRED (Slice 4, archival photos only). **Verified NEGATIVES (dead for Dnipro):** EUBUCCO/JRC DBSM exclude UA · Google Open Buildings not-Europe · Nearmap no-UA · Blackshark SYNTH3D commercial/geo-typical · Google P3DT still absent. **License TRAPS:** FABDEM = CC-BY-NC-SA → REJECT (use Copernicus GLO-30 / ALOS AW3D30); ion Community = non-commercial → don't host our tiles there. **Owner dismissed the 3-fork question → proceeding with recommended DEFAULTS:** (1) de-risk SPIKE first (hand-bake a few central-Dnipro blocks; prove masking + vertical-datum seating vs rendered Cesium World Terrain + R2/CORS + stylization reuse) → (2) PIVOT to enrichment (the pending Pass 1/2 browser-verify + GTAO folds into the next browser session) → (3) splats DEFERRED. **Biggest technical risk = R1 vertical-datum seating** (baked buildings ref GLO-30 vs we render CWT → float/sink; resolve in the spike). **Synergy:** enriched buildings+trees = the fine obstruction surface for the queued Pass 3 astro/occlusion moat (`lib/geo/occlusion.ts`). Files: `.claude/claude-docs/{DNIPRO_3D_ENRICHMENT_PLAN.md(new),DNIPRO_3D_ENRICHMENT_RESEARCH_RESULTS.md(new),DNIPRO_3D_ENRICHMENT_RESEARCH_PROMPT.md}` · `NEXT_SESSION_PROMPT.md` (headline updated). Memory: `mem:project/wip-2026-07-13-dnipro-enrichment-research`.
- **2026-07-13 — IMPLEMENT (owner: "do it"): fixed the blank gallery/social preview — SSR boot poster + OG meta (browser-VERIFIED in `wix dev`; ships on next `wix release`).** Follows the investigation line below. The page's only server-rendered visual is now `#boot-poster` (`src/pages/index.astro` + `src/styles/boot-poster.css`): a full-bleed splash (chrome-free globe cover + Welcome's eyebrow/title at the same 7.5vw anchor + "BUILDING THE GLOBE" loader) that paints in the SSR HTML → a screenshot/OG crawler captures the brand, not the black `client:only` void that froze the old snapshot. Inline `is:inline` script dismisses it on first interaction / a `globe:ready` window event (future hook) / a 3.8 s safety timer (reduced-motion 1.2 s), skips on a `#p=` hash (matches Welcome); a JS/WebGL-less snapshotter keeps it. z-index 200 (above app max 60; the bottom pill that peeked in verify was `<astro-dev-toolbar>` z-999999 — DEV-ONLY, absent in prod). Cover `public/social-cover.jpg` (1600×800) = a chrome-free LEO limb captured from the live globe via Playwright (NOT `docs/media/globe-leo.jpeg` — that has UI baked in). `src/layouts/Layout.astro` head gained `description`/`og:description`/`og:image`(+w/h/alt)/`twitter:description`/`twitter:image` (absolute via SITE_URL; no og:title — Wix owns it; fixes social cards, NOT the gallery snapshotter). Gates: astro check 0/0 · vitest 416 · SSR curl shows poster+og · poster renders+auto-dismisses in wix dev (verify-shots/poster-02..04). Fence intact (index/Layout/styles/public only). STILL REQUIRED: `wix release` (live still serves stale Phase-5 → `/social-cover.jpg` 404s till then), then force a snapshot RE-CAPTURE (re-save submission / ping @tuvitk — no self-serve refresh) + curl the live head to confirm OG survives Wix's SEO layer. `mem:bugs/gallery-thumbnail-stale` §Fix.
- **2026-07-13 — INVESTIGATION (owner): why the internal "Headless Day" gallery shows no nice preview for "Frame the World" — ROOT-CAUSED with browser + Slack + code evidence (no code change; `mem:bugs/gallery-thumbnail-stale`).** Gallery card thumbnail = Wix **`site-snapshotter-web`** server screenshot, keyed by msid (`wix.com/site-thumbnail/{siteId}?preset=site-list` → stored wixmp JPG) — NOT OG tags, NOT builder-uploaded (the `my-submission` form has no image field). Our stored snapshot is **STALE**: it captured the default Wix+Astro STARTER page ("To get started, open the `src/pages` directory") at provisioning (07-09) BEFORE the first `wix release` (07-10), and never re-captured. Organizer @tuvitk (Slack #headless-day-26): "it's supposed to update automatically if you submitted the correct msid" (ours IS correct) — auto-refresh is effectively one-time for headless redeploys; fallback is a manual organizer-side regen. **Compounding:** the homepage is 100% `client:only` (C4 — globe never SSR'd; `index.astro` SSR HTML = only nav+attribution), so even a fresh server capture would be near-blank (the starter captured only because its hero was static SSR HTML). Also found: deployed `<head>` has Wix `og:title`+`twitter:card=summary_large_image` but **no `og:image`/`og:description`** → empty social/link previews too (separate system; does NOT feed the gallery). Recommended fix (designed, NOT implemented): SSR branded loading poster in `index.astro` (uses existing `docs/media/*.jpeg`; fenced to panels/ui/styles) so any snapshot looks great + improves first-paint, THEN trigger re-capture (re-save submission / ping @tuvitk) + add og:image for social. Evidence shots: `verify-shots/gallery-01..03`. Verification tier: browser-VERIFIED (gallery DOM + stored thumbnail) + Slack-authoritative (mechanism) + code-confirmed (curl head + `src/{layouts/Layout,pages/index}.astro`). Awaiting owner fork on remediation scope.
- **2026-07-13 (R3 OFF) — FIX (2nd owner browser feedback): Pass 2 R3 night facade emissive DISABLED (`nightWindowGain 0`); R2 tone variation stays. astro check 0/0 · vitest 416.** The wall-gate (line below) was ALSO rejected — "sides of random buildings become constant yellow color". **ROOT LESSON: a FLAT per-building emissive fills whole surfaces with a CONSTANT colour → reads as "painted", never as lit windows, regardless of gate (roof→wall) or intensity (0.6→0.4). No flat-emissive tuning looks like windows.** R3 turned OFF by default (wall-gate/hash plumbing stays wired so a real pattern can drop in). The correct "city alive at night" = a procedural WINDOW PATTERN (lit-window grid up the facades via precision-safe RTC-local `position`: height `dot(position, objectUp)` → `fract(h/floorH)` floor bands × a horizontal axis → grid; `objectUp = normalize(transpose(mat3(modelMatrix))·uFtwUp)`) — its own slice, iterative shader art that needs a live browser loop (do NOT ship blind — two misses: roof flood, wall flood). Files: `tuning.ts` (nightWindowGain 0). Memory: `mem:project/wip-2026-07-12-rendering-pass2-dnipro-identity` §2026-07-13(2).
- **2026-07-13 (R3 wall-gate fix) — FIX (owner browser feedback): Pass 2 R3 night emissive "looks broken" → gated to vertical facades + toned down (astro check 0/0 · vitest 416 · same-day follow-up to the line below).** The owner's night-Dnipro screenshot **browser-VERIFIED the building shader COMPILES + links** (R2 tone + R3 emissive rendering — the #1 correctness risk local gates couldn't catch is now PASS), but R3 flooded whole building ROOFS with solid saturated yellow (~45% of buildings, full intensity) — read as "painted yellow", not lit windows. Root causes + fix: (1) **roofs glowed** → gate the emissive to VERTICAL walls: NEW world face normal `vFtwWNormal = mat3(modelMatrix) * objectNormal` (direction-only → float32-safe at ECEF scale; a `dFdx(worldPos)` normal would cancellation-explode since the buildings tileset is NOT re-centered), `wallness = 1 − |normalize(vFtwWNormal)·uFtwUp|` length-guarded (no-normal tile → fall back to 1, never NaN), `uFtwUp` = view-focus geodetic up passed via `setNight(sunElevSin, up)`; (2) **too intense** `nightWindowGain 0.6→0.4` (0.6 pushed lit R over BLOOM.threshold → bloom smear); (3) **too many** `nightWindowLitLo 0.55→0.7` (~45%→~25% of buildings lit). Verified `objectNormal` is defined by `<beginnormal_vertex>` (meshphysical vert :33) before `<begin_vertex>` (:40); `modelMatrix` built-in. Files: `scene/buildings.ts` (wallness + uFtwUp + setNight sig) · `StylizedTiles.ts` (`setNight(…, _focusUp)`) · `tuning.ts` (gain/lit). PENDING owner re-check (wall-only is subtle from steep top-down — roofs dark = correct night city; add a roof floor or height-band windows if building presence from above is wanted). Memory: `mem:project/wip-2026-07-12-rendering-pass2-dnipro-identity` §2026-07-13.
- **2026-07-12 (Pass 2 R2+R3) — IMPLEMENT: rendering Pass 2 (Dnipro identity) R2 per-building tonal variation + R3 night window emissive SHIPPED; R4 roof reconstruction DECIDED = design-and-defer. Local gates green: astro check 0 errors/0 warnings, vitest 416 (+8 buildingNight), wix build complete. Runtime shader-compile + the aesthetic look = BROWSER-UNVERIFIED (owner's Pass-1 "local gates only, no browser this session" call carries; this machine is `high` tier).** The 2/5→4/5 "reads as Dnipro" default-stylized layer, both effects riding the ONE shared fill material's existing chained `onBeforeCompile` (invariant intact — no per-tile material, no new pass, no tier knob needed). **PROBE (crux, self-verified against node_modules, resolves the doc's UNVERIFIED R2 gate):** the b3dm batch id survives GLTF load as `geometry.attributes._batchid` (legacy) or `_feature_id_0` (3D Tiles 1.1) — three's `GLTFLoader` lowercases any non-standard attribute (`GLTFLoader.js:4806`; Draco path `:1941/:1949`), `_BATCHID`/`_FEATURE_ID_0` aren't in its `ATTRIBUTES` map, and `3d-tiles-renderer@0.4.28` never strips attributes (zero `deleteAttribute` hits). **The reframe that de-risked it: readability doesn't GATE the code** — the vertex shader sums `_batchid + _feature_id_0 + uFtwTileSeed`, so it's correct for BOTH encodings AND degrades to per-TILE tone when neither is present (three reads an unbound attribute as 0 — never an error); the browser only grades granularity (per-building vs per-tile). **R2** (`scene/buildings.ts`): vertex carries a stable per-building key to a `flat`-free `varying float vFtwBId`; fragment `<color_fragment>` multiplies `diffuseColor.rgb` by `mix(1−toneVariation, 1+toneVariation, hash(vFtwBId+11))` — `BUILDINGS.toneVariation 0.12` (0 = byte-identical pre-Pass-2 no-op comparator); a golden-ratio per-tile seed (`tileSeedSeq++·φ %1`, no Math.random) written per-tile by the existing `onBeforeRender` (same idiom as the F1 birth stamp) decorrelates tiles so batch ids that restart at 0 per tile don't repeat. **R3** (night facade emissive): new pure tested `lib/globe/buildingNight.ts` (`buildingNightFactor(sunElevSin, EARTH.lightsBand)` = `1−smoothstep01` over the SAME terminator band the earth/ground use — CPU twin, unit-testable unlike the shader) → `buildings.setNight(sunDirW.dot(_focusUp))` from `stepKeyLightAndShadow` (step 26; drives `uFtwNight`); fragment injects after `<emissivemap_fragment>` (verified present in three 0.185 meshphysical; `totalEmissiveRadiance` declared :168, consumed :198) `totalEmissiveRadiance += uFtwWindow × (nightWindowGain × uFtwNight × lit)` where `uFtwWindow = new THREE.Color(tokens.cityLights)` (warm sodium, LINEAR uniform, same as the earth's uCityLights) and `lit = smoothstep(nightWindowLitLo 0.55, Hi 0.95, hash(vFtwBId+71))` gates WHICH buildings glow — "city alive at night", not a light box (`nightWindowGain 0.6`, 0 = off). Free fragment math at all tiers → **degrades with the existing bloom tiering** (bloom-off `low` tier → windows read as flat warm colour, no glow — no new QUALITY field, quality.test.ts still locks the perf invariant; 416 confirms). Edges (edgeMat) stay the uniform lit stroke (R2/R3 touch the fill only). **R4 DECISION = design-and-defer** (its own slice, not this session): bespoke L-effort S3DB geometry (no drop-in three.js roof lib — OSM Buildings/Streets.GL are whole engines, `straight-skeleton` is a single-maintainer WASM CGAL hobby wrapper) + a net-new Overpass→`public/data` bake (85,802 bldgs / 2,568 roof:shape / 1,766 building:part live-measured; ~sub-MB packed, precedent = build-ne-labels.mjs) + the crux blocker: reconstructed roofs z-fight the streamed Cesium LOD1 prism, and cleanly culling the prism per-footprint needs a batch-id→OSM-id map + per-building sub-mesh culling the one-material swap can't do (deeper than R2's probe, browser-gated). Carve-off available on request: ONE hero glb (DRACO decoder wired at `buildings.ts:86` but NO standalone GLTFLoader — needs one + a licensed Dnipro-landmark model, none in-repo). Files: `lib/globe/buildingNight.ts` (new) · `test/lib/globe/buildingNight.test.ts` (new, 8) · `components/globe/tuning.ts` (BUILDINGS +toneVariation/nightWindowGain/nightWindowLitLo/Hi) · `scene/buildings.ts` (R2 vertex+frag, R3 emissive, setNight, per-tile seed) · `StylizedTiles.ts` (setNight in step 26). **DO NEXT (browser, `wix dev`): (1) confirm the building shader COMPILES — the #1 correctness risk local gates can't catch — check the console for a GLSL link error over Dnipro; (2) A/B the look (toneVariation/nightWindowGain/lit are glf-baked → retune + reload; window color = tokens.cityLights); (3) confirm the live tiles carry `_batchid` (per-building) vs only per-tile via `console.log(Object.keys(mesh.geometry.attributes))` at load-model. THEN Pass 3 (moat) / decide R4 build. Phase 6 marketplace still deferred.** Memory: `mem:project/wip-2026-07-12-rendering-pass2-dnipro-identity`.
- **2026-07-12 (latest) — IMPLEMENT: rendering Pass 1 COMPLETED — tile-knob tiering + fluidity F1/F3/F5/F7 + GTAOPass wired (owner calls: GTAO DEFAULT-OFF, land + local gates only). All local gates green: astro check 0 errors/0 warnings, vitest 408 (+7: 4 drift, 3 lruCapBytesForTier), wix build complete. Runtime perf/visual = BROWSER-UNVERIFIED (this machine is `high` tier; mid/low degradation needs a non-M3 box).** Completes the "remaining Pass 1" the keystone deferred. **Tile-knob tiering** (SAFE without a browser — `high` stays byte-identical): NEW pure `lib/globe/quality.ts lruCapBytesForTier(tier,mb)` returns **`null` on high** (each renderer restores its captured library default) / `round(mb*1024*1024)` raw bytes on mid/low (3d-tiles-renderer `LRUCache.maxBytesSize` is RAW BYTES, default `0.4*2**30`≈410 MiB — verified in node_modules; effective on next `tiles.update()`); each `TilesRenderer` captures `lruDefaultBytes` at construction. Module setters (primitives, decoupled): `buildings.setQualityTier(errorTarget,lruCapBytes)` (sets `tiles.errorTarget`+LRU), `imageryGround.setQualityTier(errorNear,lruCapBytes)` (mutable `errorNearOverride` → the update() error-ramp NEAR endpoint), `streetNames.setMaxVisible`, `vectorFeatures.setLatticeBudget`. `StylizedTiles` handle gained `setQualityTier(tier)` (→ `applyQualityTier` fans out `QUALITY.tiers[tier]`) + opts `qualityTier` (applied once at attach); `GlobeCanvas.applyTier` calls `tilesHandle?.setQualityTier(t)` each governor change. **F1 building screen-door reveal** (the #1 street pop): per-tile birth stamped on `load-model`, written to a SHARED-material uniform by each mesh's/edge's `onBeforeRender` (fill+edge = separate render-list items → own birth holders; `uNowMs` advanced in `buildings.update()`); the ONE shared fill material's CHAINED onBeforeCompile + a NEW edgeMat onBeforeCompile inject the SAME 4×4 bayer as imageryGround.ts:280 and `discard` at `<dithering_fragment>` when `age<1` — opaque, O(1)/draw, no per-tile material (`BUILDINGS.fadeInMs` 600; aged tiles render byte-identical via the `if(age<1)` guard; verified `LineBasicMaterial:'basic'` frag HAS `<common>`+`<dithering_fragment>` so the edge `.replace` anchors exist). **F3 street-name opacity ease**: LabelEntry `opacity`+`dying` — new labels fade in from 0, de-selected marked `dying`→ease to 0→drop when <0.01, re-selected REVIVES (`STREETS.labelFadeLerp` 0.15). **F5 dt-normalize idle drift**: NEW pure `lib/globe/drift.ts driftRadiansForDt(degPerSec,dtMs)` (+4 tests); `DRIFT.degPerFrame 0.0011 → degPerSec 0.066` (==0.0011/frame @60 Hz, behavior-identical; correct pace at 30/120 Hz) applied with the CLAMPED `dtMs`. **F7 graticule opacity fade**: `graticule.setPresence(k)` (scales uOpacity, hides at 0); StylizedTiles ramps `GRATICULE.fadeBottom/TopAltM` 150k→250k instead of the hard `visible=alt>decorMinAlt` toggle (GATES became unused → dropped from StylizedTiles' import). **R1 GTAOPass — WIRED but DEFAULT-OFF**: NEW `AO` tuning (`enabled:false` = never constructed, zero cost); when true GlobeCanvas builds `GTAOPass`→`composer.insertPass(_,1)` (after RenderPass, before bloom), gated by tier(high)+altitude (`aoControl.setAltActive`, `alt<AO.maxAltM` 12 km), exposed as `window.__quality.ao`; horizon tint = a GUARDED `GTAOBlendShader` monkey-patch (both `.replace` targets byte-verified in node_modules — compiles clean when enabled, skips if the source drifts). GBuffer prepass = 1 extra full scene render → high+city only; `screenSpaceRadius:false` so `radiusM` is world/view metres (needs in-browser scale tuning). Files: `tuning.ts` (DRIFT.degPerSec · BUILDINGS.fadeInMs · GRATICULE.fade{Top,Bottom}AltM · STREETS.labelFadeLerp · NEW AO) · `lib/globe/quality.ts` (+lruCapBytesForTier) · NEW `lib/globe/drift.ts` · `scene/{buildings,imageryGround,streetNames,vectorFeatures,graticule}.ts` · `StylizedTiles.ts` · `GlobeCanvas.tsx` · NEW `test/lib/globe/drift.test.ts` · `test/lib/globe/quality.test.ts`. **DO NEXT: browser-verify in `wix dev` (ideally non-M3) — `window.__quality.force('low')` smoother, F1 stipple reads intentional, high unchanged; then tune+enable GTAO; then Pass 2 (Dnipro R2/R3/R4) → Pass 3 (moat).** Memory: `mem:project/wip-2026-07-12-rendering-pass1-tiling-fluidity`.
- **2026-07-12 (keystone) — IMPLEMENT: rendering quality Pass 1 KEYSTONE shipped — adaptive quality tier + runtime frame governor (owner picked Pass 1 first, Pass 2 queued; realistic-mode dropped, existing SAT toggle stays opt-in). Local gates green: 401 vitest (+24), astro check 0 errors/0 warnings.** The scene had NO adaptive quality (static `min(DPR,2)`; composer rendered full quality every frame) — the #1 reason it wouldn't run below an M3 Pro. NEW pure `src/lib/globe/quality.ts` (three-free, DOM-free): `detectDeviceTier(caps)` (WEBGL_debug_renderer_info GPU-family + deviceMemory/cores/maxTextureSize → low/mid/high; conservative, unknown→mid) + `makeGovernor(initial, cfg, ceiling)` (EMA frame-time → steps the tier with asymmetric hysteresis + cooldown; a no-op whenever frames stay under budget). NEW `QUALITY` tuning block (governor cfg + 3 tier records: dprCap/bloom/shadowsEnabled/shadowMapSize/lruBytesMB/groundErrorNear/buildingErrorTarget/vectorLatticeBudget/maxStreetNames). **HARD SAFETY INVARIANT: `tiers.high` == the pre-pass constants** (dprCap 2, shadow 4096, groundErrorNear 2, vectorLatticeBudget 8, maxStreetNames 40, bloom+shadows on, library-default 16 SSE / 0.4 GB LRU) — locked by `test/lib/globe/quality.test.ts` against the live RENDERER/SHADOWS/GROUND/VECTOR/STREETS — so a capable machine is byte-identical and ONLY weaker hardware degrades (why it's safe to land without a browser pass). GlobeCanvas wired: `antialias:false` (P1 — composer MSAA owns AA, the base backbuffer AA did nothing), `powerPreference:'high-performance'` (P2), device-tier → initial DPR + shadowMap.enabled + shadow mapSize + bloom.enabled, the governor in the rAF tick (`applyTier` on change: DPR realloc + bloom.enabled + shadowMap.enabled + shadow map realloc), `window.__quality` DEV seam (tier/deviceCaps/governor/force). Governor ceiling = `low`→capped (memory pressure is invisible to frame time), `mid`/`high`→may climb to `high` (a privacy-blocked GPU string on a real M3 Pro won't get stuck at mid). Files: `lib/globe/quality.ts` (new) · `components/globe/tuning.ts` (+QUALITY) · `components/globe/GlobeCanvas.tsx` · `src/global.d.ts` (+__quality) · `test/lib/globe/quality.test.ts` (new, 24). **VERIFICATION TIER: local logic/types only — the runtime perf/visual effect (per-tier FPS/heap, DPR-change feel, governor stability) is BROWSER-tier UNVERIFIED; needs `wix dev` on a NON-M3 machine.** REMAINING Pass 1 (needs the wix-dev loop — deferred deliberately): tile-knob tiering (building errorTarget, LRU cap, ground-error ramp, vector/street budgets driven by the tier via the orchestrator), fluidity fixes (F1 building screen-door fade via the chained onBeforeCompile, F3 street-name ease, F5 dt-normalized drift, F7 graticule fade), and R1 GTAOPass tier-gated (three-0.185 built-in — no dep). Then Pass 2 (Dnipro identity) per the owner's "roadmap immediately after". Plan + backlog: `RENDERING_QUALITY_PASS.md`. Memory: `mem:project/wip-2026-07-12-rendering-quality-pass`.
- **2026-07-12 — DESIGN: rendering quality pass investigated → `.claude/claude-docs/RENDERING_QUALITY_PASS.md` (design-only; NOT yet implemented; crux claims self-verified against source; 377 vitest still green, astro check 0).** Owner ask: hard pass on perf + fluidity + aesthetics of all 3D rendering (esp. street level + FPV), must run below an M3 Pro; Dnipro buildings/landmarks/terrain feel 2/5; the "predict astro events + fit photo vs actual cityscape/obstructions" moat is still unmet. Method: inline scout + 4 parallel cited research tracks (perf/cross-device · fluidity · Dnipro realism · obstruction/astro moat) → self-verified synthesis. **Synthesis thesis: one keystone (an adaptive quality-tier + runtime frame-governor — none exists today: DPR is a static min(DPR,2) `GlobeCanvas.tsx:31`, composer renders full quality every frame `:228`) resolves both the "runs below M3 Pro" AND the C2 "beauty not sacrificed" asks by making every realism add sheddable per device.** Verified findings: (1) `antialias:true` on the base renderer is redundant under the MSAA composer (`GlobeCanvas.tsx:30`) — free win; two 0.4 GB-default LRU tile caches, 4096² PCF shadows + per-tile ShadowMaterial twins, ~11-target bloom, all un-tiered. (2) **Building hard-pop is structural** — `TilesFadePlugin`'s per-material WeakMap + shared-material `needsUpdate` recompiles are incompatible with the ONE-material invariant; fix = a per-tile "age" **screen-door discard** via the existing chained `onBeforeCompile` (opaque, O(1)/draw, reuses the `imageryGround.ts:280` dither idiom). (3) **Google Photorealistic 3D Tiles are VERIFIED absent over Ukraine** (Google's own coverage table) → useless for Dnipro (terrain-only, no buildings) + C5/$6-CPM/EEA-403 — do NOT wire; the 2/5→4/5 is a DEFAULT-stylized problem: **`GTAOPass` (built into three 0.185, NO new dep) tier-gated is the highest-ROI first move**, then per-building variation from OSM batch metadata (probe first), night emissive, and client-side S3DB roof reconstruction for the ~2–4k `roof:shape`/`building:part` Dnipro landmark subset (85,802 bldgs / 2,568 roof:shape live-measured via Overpass). (4) **The moat is highly feasible** — the finder engine already ships (`astronomy-engine` `Search`/`SearchRiseSet` w/ eye-height dip/`SearchAltitude`/`SearchHourAngle`/`Seasons`, verified `astronomy.d.ts:1241-1894`), buildings ARE raycastable (only edges disable it — the one-material invariant does NOT block CPU raycasting), apex = ray origin; QUICK WIN = pure `lib/ephemeris/planner.ts` + terrain-only `horizonProfile.ts` + a jump-to-time PlanPanel (no LOD risk), STRUCTURAL = building-accurate occlusion (`lib/geo/occlusion.ts`, 2–4 km trust radius). Recommended sequence: **Pass 1 Foundation** (QUALITY tier scaffold + governor + safe perf quick-wins P1/P2/P4–P8 + building screen-door fade F1 + eases + GTAOPass tier-gated) → Pass 2 Dnipro identity → Pass 3 moat. Nothing supersedes a locked ADR (all additive; WS1 pulls the Phase-7 KTX2/OPFS/mobile earmarks forward). Awaiting owner fork (which pass first · moat-now-vs-after-Phase-6 · drop realistic-mode). Memory: `mem:project/wip-2026-07-12-rendering-quality-pass`.
- **2026-07-12 (later) — README rewritten for the internal Wix contest (docs-only; no runtime change; facts re-verified this session: 377 vitest green · astro check 0 errors/0 warnings · first→last commit 2026-07-09→07-12, 17 commits).** Owner reframing made canonical in the root `README.md`: the project's PRIMARY goal is a **stress test of the Wix headless ecosystem** ("if it can host this, it can host anything"); the photo/3D-earth/ephemeris layer is the owner's side hobby used to make the test fun and to probe AI-agent limits (repo is 100% agent-built — stated openly, with the committed `.claude/` harness presented as part of the deliverable). Structure: hero + why-this-exists · 90-second feature tour (4-image grid) · **Wix integration surface table** (auth/Data/elevate/Media-TUS/geohash/Pricing-Plans/eCom-Phase-6/AI/CLI with per-row verification status) · **proven-vs-falsified platform findings** (6 proven incl. C6 privacy-by-structure + released-runtime parity; 6 falsified edges incl. CLI-provisioning, no-hooks, port-exact OAuth) · client-heavy architecture split + stack table · AI-agent section · run-it commands · docs map. NEW committed asset dir **`docs/media/`** (5 curated screenshots copied from git-ignored `verify-shots/`: welcome, LEO 8k, photo-FPV HUD, night vector city, pins hover-card — ~1.1 MB; verify-shots stays ignored, this is documentation not verification). Deliberately OMITTED the live URL (it still serves the stale Phase 5 build — add it after the pre-release canary + `wix release`, see carried follow-ups). THIRD_PARTY.md described accurately (decoder licenses only; imagery attribution lives in the app footer — a first-draft README line claiming it covered Esri/CARTO was caught by the falsification gate and fixed). Files: `README.md` (full rewrite) · `docs/media/*` (new). Memory: `mem:project/wip-2026-07-12-readme-rewrite`.
- **2026-07-12 — S7 feedback batch #2: river-tile flicker killed (static vector web) + camera pose in the URL (share links, reload-safe, welcome-skipping) — all tiers green: astro check 0 · 377 vitest (+21) · wix build · browser-VERIFIED via Playwright MCP (shots s7fb-09..14).** Owner: "river blue tiles appear and disappear / flicker — make them more static" + "save current position in url … so reload doesn't go back to welcome; also allows sharing urls". **(1) Flicker had FOUR stacked causes, each fixed structurally:** (a) MVT features carry a BUFFER past the tile edge → neighbor tiles drew the same water twice and the two translucent fills z-fought at every seam — geometry is now CLIPPED to the exact tile square at parse (`clipRingToBounds` Sutherland–Hodgman for rings · `clipLineToBounds` Liang–Barsky with re-entry splitting for roads/waterways — pure, unit-tested; shared-edge lattice knots sample identical lon/lat so neighbors meet without cracks); (b) builds were DROPPED the moment they left the focus ring and rebuilt on re-entry → tiles popped in/out on every pan/focus wobble — builds now PERSIST (eviction only past `VECTOR.maxBuilds 48`, farthest-from-focus first, never inside the active ring); (c) the flat ellipsoid-0 pre-build flashed at the wrong height then jumped when the lattice landed — tiles now build ONLY once their height lattice is complete; (d) **the deep one (two live iterations to root-cause):** a lattice sampled mid-terrain-refinement bakes coarse heights and the fill lands metres UNDER the final ground (patches read as missing/flickering water), and knots OUTSIDE the camera frustum return null FOREVER (terrain tiles exist only in-frustum — the S2 lesson) which stalled whole tiles → the cursor now SKIPS unanswerable knots (NaN → mean-of-answered fill), and a **pre-build stability gate** requires TWO consecutive lattice passes agreeing within `refreshEpsM 3 m` before the first build — a tile appears exactly once, correctly seated, and stays; the post-build refresh cycle (`refreshEveryFrames 240`, one tile per cycle, rebuild only on ≥3 m drift, old geometry up the whole time) converges tiles the camera later descends onto. `liftWaterM 2.4→3.5`. Verified: continuous Dnipro body at 2.8 km after a cold reload (s7fb-12/13 — one foreground tile visibly converging then filled), pan along the river keeps every previously-seen tile (s7fb-14). **(2) URL pose:** new `lib/geo/urlPose.ts` (`#p=<focusLat>,<focusLon>,<camAltM>,<heading>,<tilt>` — 5 dp/1 m/0.1°; `formatPoseHash`/`parsePoseHash`/`wrapLon` pure, malformed → null; 14 tests). Orchestrator writes it inside the step-23 mirror block (`ORCH.urlPoseEveryFrames 96` ≈ 1.6 s — Safari rate-limits history calls; `history.replaceState` only, on-change only, and NEVER while welcome/Explore/FPV/a flight owns the camera); boot: `StylizedTiles` parses the hash and reconstructs the camera through the ONE arrival derivation (`arrivalPose` — approachHoriz = −(sin h·east + cos h·north), groundAltM 0 at boot) INSTEAD of the LEO default, and `Welcome.tsx` initial-states hidden on the same hash (no welcome, no Explore arming over a shared view). Round-trip verified live: flew to the river → hash `#p=48.47200,35.06500,2793,47.3,52.1` → reload → welcome skipped, camera landed focus 48.4720/35.0650 · alt 2786 · heading 47.3 · tilt 52.1 (exact); pan updated the hash. Files: `scene/{vectorTiles,vectorFeatures}.ts` · `tuning.ts` (VECTOR maxBuilds/refresh*/liftWaterM/tileCacheMax 56 + ORCH.urlPoseEveryFrames) · `lib/geo/urlPose.ts` (new) · `StylizedTiles.ts` (boot restore + step-23 hash write) · `panels/Welcome.tsx` · tests `vectorTiles` (+8 clip) · `urlPose` (new, 14). Carried: newly-entered ring tiles appear progressively (~1–3 s per tile, by design — seated-once beats flash-and-jump); URL pose intentionally NOT written during FPV (orbit pose only, v1). Memory: `mem:project/wip-2026-07-11-s7-feedback-batch` §batch-2.
- **2026-07-11 (latest) — S7 owner-feedback batch: GL street names pinned to the mesh + vector road/water web + uniform ground illumination + subtle low-alt haze + UI tooltip system (all tiers green: astro check 0 · 356 vitest (+17) · wix build · browser-VERIFIED via Playwright MCP on wix dev · S5 night golden gate PASS — night sky 2/5.9/11 ≈ baseline, K&S ratio 10.3%, moon-shadow switch intact).** Owner: "S7 street names horrible — laggy, jumping, must be pinned on the mesh; add street lines + basic vector terrain (street vs river vs bridge unreadable); ground jarringly black — uniform sun/moon illumination; day haze too white at low-alt tilt; add tips/info badges everywhere." **(1) Street names v3 = GL (supersedes the S7 v2 DOM labels):** root cause was structural — DOM divs restyled every 3rd frame vs a 60 Hz mesh + full screen-space re-selection per sync (reshuffle) + one-shot terrain reseat snap. `scene/streetNames.ts` rewritten: one canvas-textured quad per street name (44 px raster, `--font-ui` resolved for ctx.font, bg-halo baked in-texture, SRGB + aniso 8) LYING ON the terrain in the anchor's tangent plane, rotated along the street — same composer frame as the mesh ⇒ cannot lag, by construction; float32-safe because the geometry is a unit plane and the ECEF lives in `mesh.matrix` (CPU-float64 modelView — the PhotoFrustum lesson, NOT the Pins instanced-attribute trap); type is WORLD metres (`STREETS.textHeightM [22,15,11]` by class tier — road-paint that optically scales with zoom, replacing fontMinPx/fontMaxPx/fontScalePow); selection = dedupe-by-name → rank → WORLD-space minSep 130 m at a 20-frame cadence (no per-frame reshuffle), maxVisible 40; lazy terrain seat now EASES (reseatEveryFrames 240, ε 2.5 m); per-frame writes are only opacity + a hysteresis upright-flip (`flipHysteresis 0.08`, pure fn). **TRAP (found live): a stored basis vector aliased the module `_v` temp → every matrix corrupted to ~1.1e9 scale once the temp was reused — entry-owned vectors must be FRESH, never module temps.** Verified: 40 quads @1.6 km, 0 DOM labels, names painted along «проспект Дмитра Яворницького»/«Лазаря Глоби» @477 m (shots s7fb-03/06). **(2) Vector feature web:** new shared `scene/vectorTiles.ts` — ONE fetch/parse per OpenFreeMap z14 tile now RETAINS geometry (S7 v2 threw it away; probe: 903 KB decoded/3×3 ring, ~21k line verts — trivial) and feeds both consumers; parses transportation (class+brunnel), waterway, water/landcover/park polygons (ring winding via `ringArea`, MVT y-down: CW=outer). New `scene/vectorFeatures.ts` (step 39): roads/waterways as RIBBONS with real metre widths (`VECTOR.roadWidthM` motorway 22→path 2; miter-averaged strip, pure `ribbonStrip`), water/green as ShapeUtils-triangulated fills, per-tile LOCAL tangent-frame geometry (anchor in mesh.matrix — float32-safe), seated on rendered terrain via a lazy 6×6 `heightAt` lattice (bilinear, amortized 8 raycasts/frame, flat-at-0 until filled then ONE rebuild); bridges +6 m lift + brightest ink, tunnels skipped; presence 15→8 km, ring 2 above 4 km, night-dimmed by solar elevation (`nightDimFor`, unlit ink must not glow); colours = 5 new `vec*` tokens (tokens.css + GL bridge — map ink, not signal). Verified: tiered road web + Dnipro water fill + bright bridges day and night (shots s7fb-01/02/04). **(3) Ground illumination:** grade rework in imageryGround — day/night gate `dayK` now reads GEODETIC up (`sunUpDot` — solar elevation: "is it daytime HERE"), not the surface normal (a noon hillside used to fall to the night floor); slope relief stays via `dayShade` (bounded ≥ dayGradMin); golden + night gates follow `sunUpDot`; NEW additive terms so dark pixels can never multiply to black — `uFtwAmbDay` (skyHorizon × `GROUND.ambientDayK 0.1`) + night moonlight ambient (`ambientNightK 0.012`) + a NON-albedo moon fill (`moonFillK 0.5` × moonGlow × moonUp), all scaled out by `uFtwHiAlt` through the orbital fade band; DRAPE softened: nightFloor 0.45→0.52, shadowOpacity 0.9→0.62, moonShadowOpacity 0.65→0.5 (near-black 0.9 footprints were half the "jarring black"). Verified: uniform slate at noon (s7fb-02/03), navigable full-moon night (s7fb-07), golden gate PASS. **(4) Low-alt haze:** the old horizon budget ran ~1.2 additive over BLOOM.threshold 0.9 → bloom spread the white-out. Atmosphere sky regime now anchors the horizon at `mix(skyDay, skyHorizon, skyHorizonWhiteness 0.55)` (tinted, not white), haze pulls blue (`skyHazeBlue 0.35`), `skyHorizonGain 0.35→0.16`, falloff 0.1→0.075, and a very-low-alt dim ramp (`hazeLowAltK 0.45` below 300 m→full by 6 km); **NEW unit-tested horizon-budget guard `test/…/skyBudget.test.ts` — the JS twin of the shader sum asserts horizon < BLOOM.threshold at every altitude.** Verified: 320 m @ 82° tilt = readable blue sky, horizon + river visible, zero white-out (s7fb-04). **(5) UI tips (sub-agent, design-fenced):** new `styles/tips.css` (CSS-anchored `::after` glass pills on `.tip[data-tip][data-tip-pos]` — absolute, so the backdrop-filter containing-block trap is moot; 0.38 s reveal delay, :focus-visible, reduced-motion, `:active` duck) + `ui/InfoDot.tsx` (13 px "i", role=note, focusable); **32 hover tips + 5 InfoDots** across CameraTiltPanel (8+dot, new `.ct-head`), TimeScrubber (5+dot), LocationFinder, PhotoDetailPanel (10+provenance dot; panel is LEFT-docked → up/down tips), UploadFlow (5+2 dots), MyPins, nav; 4 native titles migrated (no double tooltips); copy corrected against real behavior (no "double-click resets" where none exists). Verified: SAT chip tip renders (s7fb-08). Also: `LABELS.syncEveryFrames 2→1` (city labels now restyle every frame — same lag class, cheap), stale `.street-labels` welcome rule dropped, LABELS doc de-stale'd. Files: `tuning.ts` (STREETS reworked + VECTOR new + GROUND.ambient*/moonFillK + DRAPE + ATMOSPHERE haze knobs + LABELS) · `scene/{vectorTiles(new),vectorFeatures(new),streetNames(rewrite),imageryGround,atmosphere}.ts` · `StylizedTiles.ts` (attach + steps 38/39) · `lib/theme/tokens.ts` + `styles/tokens.css` (+5 vec*) · `styles/{welcome,tips(new),camera-tilt}.css` · `ui/{InfoDot(new),Slider,Encoder}.tsx` · 6 panels · `index.astro` · tests `{streetNames(rewrite),vectorTiles(new),vectorFeatures(new),skyBudget(new)}.test.ts`. Shots `verify-shots/s7fb-01..08`. Carried: STREETS.textHeightM/VECTOR opacities are taste knobs (owner pass pending); vector web also shows in SAT mode (tunable if unwanted); landcover green is near-subliminal by design. Memory: `mem:project/wip-2026-07-11-s7-feedback-batch`.
- **2026-07-11 (later) — S7 owner rework: street names went VECTOR (raster was blurry) + grow-on-zoom REMOVED (all tiers green: astro check 0 · 339 vitest · wix build · browser-VERIFIED · S5 night golden gate PASS).** Supersedes two parts of the S7 line below. **(1) Street names v2** (owner: "really blurry… smaller… appear earlier, ~2–2.5 km… scale with zoom" — draped raster text structurally cannot do any of that): the CARTO `dark_only_labels` overlay is GONE (cartoLabelsUrl removed; LABELS.street* raster gates removed); new `scene/streetNames.ts` = OpenFreeMap planet **MVT** (keyless, CORS *, ODbL/OpenMapTiles, **maxzoom 14** — z15 returns empty 200s, probe first!) fetched as a 3×3 z14 ring around the view focus (TileJSON resolved at attach — the dated tile-path segment rotates with builds, never hardcode), parsed with `@mapbox/vector-tile@3` + `pbf@5` (**pbf v5 exports `PbfReader`, no default**); `transportation_name` line features → one label per street NAME (best class then longest segment), longest-line midpoint anchor + next vertex → screen-space rotation along the street (kept upright ±90°), seated on RENDERED terrain via `ground.heightAt` + `clampGroundM` (ellipsoid-0 fallback until tiles answer); same DOM-pool idiom as geoLabels (direct style writes @3 frames). New `STREETS` tuning: topAltM 2500→fullAltM 2100 reveal (the owner's 2–2.5 km), **type SCALES with zoom** `px = clamp(9·(2500/alt)^0.45, 9, 19)` (browser-measured 9.4 px @2.3 km → 17.1 px @606 m), maxVisible 26/minSep 60 px, classPriority majors-first, cache 48 tiles. Hidden in FPV (verified 0 visible) + welcome (`.street-labels` hide rule); credit line + `© OpenMapTiles`. Deps added: `pbf`, `@mapbox/vector-tile` (tiny, typed). Tests: `streetNames.test.ts` (7 — presence band, font monotone+clamp, Dnipro z14 tile 9786/5663 probe-anchored, local↔lonlat roundtrip). **(2) Grow-on-zoom REMOVED** (owner: unreliable): buildings.ts inner-group wrap/lazy-anchor/apply loop deleted, `update()` signature restored, `growMatrix.ts` + its 5 tests deleted, `BUILDINGS.grow*` tunables gone — buildings render full-height whenever tiles load (the pre-S7 behavior); Overture flag + meshopt wiring KEPT. Browser: 26 crisp rotated Cyrillic names @2.3 km, full-height buildings, Flow-0 console clean (2 known environmental errors only). Files: `tuning.ts` (STREETS in, BUILDINGS.grow*/cartoLabelsUrl out) · `scene/{streetNames(new),imageryGround,buildings}.ts` · `StylizedTiles.ts` (step 38 StreetNames; buildings.update() reverted) · `welcome.css` · `index.astro` · `package.json` · tests ±. Shots `verify-shots/phase55-57/58`. Memory: `mem:project/wip-2026-07-11-phase5.5-s7` (updated). (ground rework — the last 5.5 session; all tiers green: astro check 0 · 337 vitest (12 new) · wix build · browser-VERIFIED via Playwright MCP · S5 night golden gate PASS).** **(a) Dark drape:** CARTO `dark_nolabels` raster (owner-approved; keyless, CORS *, content-probed to z20 NYC+Dnipro → `TILESETS.cartoMaxLevel 20`) as a second `XYZTilesOverlay` — the KEY find: `overlay.opacity` is a LIVE per-frame uniform (`layerInfo.value[i] = overlay` in ImageOverlayPlugin `_updateLayers`; the wrap shader reads `.opacity` at upload), so the dark↔Esri crossfade is one eased write, NO re-composite. New `DRAPE` tuning group (fade band 7→5 km, ease 350 ms, gain 1.3, flat `dayShade` 1.0, nightFloor 0.45, per-mode `shadowOpacity 0.9`/`moonShadowOpacity 0.65`); grade GLSL blends the Esri-specific colorimetry OUT by `uFtwDark` (water detect + desat + hiAlt harmonizer × (1−dark); shade/gain/cast lerp to DRAPE twins; golden+moonlight stay — scene physics); `imageryGround.update(alt, darkGround)` + `darkBlend()`; orchestrator blends shadow strength by the live dark fraction in `stepKeyLightAndShadow`. Satellite = OPT-IN: `camera.groundMode 'dark'|'satellite'` + SAT chip in CameraTiltPanel (browser-verified: uFtwDark 1↔0 eased, mid-band 0.514 @5,972 m). **(b) Geo labels:** `scripts/build-ne-labels.mjs` bakes Natural Earth 50m GeoJSON → `public/data/ne-boundaries.bin` (393 parts/19,859 verts/157 KB packed Float32) + `ne-places.json` (1,251 rank-sorted); new `scene/geoLabels.ts` = boundaries ONE LineSegments (graticule token, lift 9 km) + place labels as a module-owned DOM layer (`position:fixed` pool, direct style writes @2-frame cadence — no zustand churn, design `--font-ui` type; rank gate lerps in LOG altitude; screen-space min-sep 76 px in rank order; hemisphere + view-z + NDC culls) — step 37 `stepGeoLabels`; **window default 100–900 km: the DoD's "gone at LEO" (default pose 1,100 km) beat the design's 2,000 km top** (tunable); `.geo-labels` joined the welcome hide list; street names = third overlay CARTO `dark_only_labels` (dark mode ≤5 km; Cyrillic legible at street zoom, big city-name text smears oblique at ~1.3 km — the accepted v1 raster risk, MVT billboards stay the v2). **(c) Grow-on-zoom:** per-tile inner Group scaled along local up about the tile's min-bbox-elevation ground anchor — `scene/growMatrix.ts` pure `L = ms⁻¹·T(A)·S(u,k)·T(−A)·ms` (5 unit tests: anchor fixed, height ×k exact, footprint untouched at ECEF magnitude); anchors computed LAZILY on first parented update (matrixWorld untrustworthy at load-model); k = altitude band 2 km→600 m (BUILDINGS.grow*, ease 260 ms, `growMinK 0.004` — k=0 is singular), ONE-shared-material invariant intact; browser-verified flat @2.2 km / half @1.3 km / full @553 m, bases seated. **(d) Overture trial NEGATIVE:** wiring OK (`TILESETS.overtureBuildings` flag routes `TilesRenderer(overtureTilesetUrl)` + `meshoptDecoder` in GLTFExtensionsPlugin; tileset.json 1.1 parses, per-subtree "limited support" warns) but **every glb content tile (`/v5-add/{z}/{x}/{y}.glb`) returned HTTP 500 over Dnipro — the no-SLA risk realized; flag ships OFF**, fallback Cesium OSM, last resort self-hosted Overture extract. Also: `glf3()` vec3 helper in scene/glsl.ts; attribution line gained `© CARTO` + `Natural Earth`. Files: `tuning.ts` (TILESETS/DRAPE/LABELS/BUILDINGS.grow) · `scene/{imageryGround,buildings,geoLabels,growMatrix,glsl}.ts` · `StylizedTiles.ts` (steps 5/24/26 + 37) · `store/camera.ts` · `CameraTiltPanel.tsx` · `camera-tilt.css` · `welcome.css` · `index.astro` · `scripts/build-ne-labels.mjs` · `test/components/globe/{growMatrix,geoLabels}.test.ts`. Shots `verify-shots/phase55-49..56`. Memory: `mem:project/wip-2026-07-11-phase5.5-s7`. **Phase 5.5 is COMPLETE → Phase 6 (marketplace) is next.** StylizedTiles `update()` split into 36 named step-fns — provably behavior-identical, all tiers green (astro check 0 · 325 vitest · wix build · browser Flow-0 CLEAN · night-shadow golden gate PASS).** The last pre-S7 refactor item. The ~950-line per-frame `try{}` body is now a flat ordered dispatch of 36 step-closures (`stepFrameTiming … stepDayArcs`) defined at factory scope; `stepArrivalReframing` is step 29 (the pin-reframe bugfix, after `stepFrustumResnapAndTick`). Frame-locals hoisted to a factory "per-frame hub" of bare `let`s with BYTE-IDENTICAL names (the B19_HANDOFF FrameContext realized as bare lets — the optional ctx-object phase-4 skipped, lower risk); `zc`→factory const; the 14 producers changed `const/let X=`→`X=` (values unchanged). File 1704→1883 lines (pure relocation). Only `StylizedTiles.ts` touched. Executed by a constrained sub-agent (single file, gate loop, no-reorder guardrails); VERIFIED independently — order regressions typecheck clean, so the proof is the safety: (1) **line-for-line + order-exact partition** — extracted step bodies in def order == pre-split body (771==771), call order == def order; (2) whole-file code-line MULTISET diff → 0 unexplained added/removed; (3) trap-site CODE counts unchanged (`updateMatrixWorld`8·`updateProjectionMatrix`2·`++frameCount`1·`adjustCamera`1·`performance.now`8); (4) astro check 0 · 325 vitest; (5) browser (`scripts/verify-pin-reframe.mjs` w/ Flow-0 console capture → HIGH-alt select centered + reframe + **0 [globe]/exception errors**; `scripts/verify-s5-night.mjs` golden gate PASS — night/shadow values identical to S5 baseline). Cosmetic follow-up: step bodies kept 8-space indent (byte-identical for the proof; no prettier config/lint script → left as-is; S7 can dedent). Memory `mem:project/wip-2026-07-11-b19-split`; `B19_HANDOFF.md` marked DONE. **Pre-S7 refactor is now COMPLETE → S7 (ground rework) is unblocked; B20–B25 fold into S7.**
- **2026-07-11 — BUGFIX (owner): high-altitude pin-selection jump landed SHIFTED — arrival re-framing + terrain clamp (all tiers green: 325 vitest · astro check 0 · wix build · browser-VERIFIED via scripted CDP).** Root cause (adversarial 3-agent Workflow CONFIRMED 85–92%, then browser-reproduced): a public pin has no altitude (C6) so the frustum rides on TERRAIN; from HIGH/oblique selection the pin's tiles are unloaded/coarse → `ground.heightAt` returns null or **NEGATIVE garbage** (live: −2047 m at 697 km) → `PhotoFrustum` seats the apex at `terrainH≈0`; `onPlaced` commits a STATIC arrival pose and `flight.finalPose` lands EXACTLY on that low `lookAt`; as the camera descends `frustum.resnap()` lifts the frustum to the true height (~90–150 m) but the committed target stayed low → the photo lands off-centre. FPV/orbit focus-lock were always correct because they re-read `frustum.current()` LIVE — only the initial jump used a snapshot (the altitude/tilt correlation is a proxy for tile-load state at selection, no geometric tilt term). **Fix:** (a) `PhotoFrustum.ts` — `clampGroundM` on `terrainH` at all 3 sites (the negative-garbage seat every other terrain consumer already guards); (b) `flight.ts` — optional `FlightStartOpts.durationMs` (default `FLIGHT.durationMs`; `let durationMs: number` — `as const` narrows to `2200`); (c) `StylizedTiles.ts` — orchestrator-local arrival re-framing: `beginFraming(pose)` armed by `onPlaced` + placed-photo FPV-exit; a per-frame step after the resnap line resnaps EVERY frame while framing and, once the live plane-centre SETTLES (`reframeSettleFrames`) AND diverged > `reframeMinMoveM`, re-flies a short `reframeDurationMs` glide to `frameArrivalPose(live)` (bounded by `reframeMaxCount`/`reframeDeadlineMs`), never mid-cinematic-flight (`!flight.active()`). **Disarm gates** (never fight the user): `noteInteract`, `fpvActive`, phase≠placed, any manual glide/rate target, `exploreActive`, and — the fix-critic's catch — a **photo-param edit** (`upNow.params !== framingParams`; the PhotoDetailPanel encoders `setParam` directly, firing neither `noteInteract` nor a camera target). New FLIGHT tunables `reframe{DurationMs 800,MinMoveM 10,SettleEpsM 1,SettleFrames 12,MaxCount 3,DeadlineMs 6500}`. **Browser (scripted CDP `scripts/verify-pin-reframe.mjs`, Node ≥22 for global WebSocket — node20 lacks it, used v24.10.0):** HIGH 697 km → plane-centre NDC (0,0), 1 corrective re-frame, apex rose 54–69 m, −2047 clamped→94 m, stale target would sit NDC y −0.134; CITY 3.5 km → NDC (0,−0.012) seamless (correction merges into the landing frame); PARAM-GATE probe: `setParam(heading)` after landing → camera moved 0.00 m, no chase. Shots `verify-shots/pin-reframe-01/02`. Files: `PhotoFrustum.ts` · `flight.ts` · `tuning.ts` · `StylizedTiles.ts` · `test/components/globe/flight.test.ts` (+2) · `scripts/verify-pin-reframe.mjs`. Memory: `mem:bugs/pin-arrival-reframe`. (Landed BEFORE B19 so the orchestrator split starts from the corrected base — the framing step is one self-contained block.)
- **2026-07-11 — Pre-S7 refactor session 2: the safe/local-verified dedup tier finished (local-VERIFIED + workflow-adversarial-verified; 323 vitest · astro check 0 · net −68 LOC).** Shipped the remaining safe backlog from `ARCHITECTURE_REVIEW.md` — **B6, B8, B9, B10, B11, B12, B13, B14(index), B15, B26** (only **B19** orchestrator split + **B20–B25** remain, both correctly deferred to the browser/S7 pass). All behavior-preserving extractions/renames/constant-promotions, no runtime-path logic changed. **B6** shared geo/math → lib: `lib/geo/terrain.ts` `clampGroundM`+`MAX_TERRAIN_M` (the 7 inline `min(max(x,0),9000)` S2-clamp sites), `lib/geo/screen.ts` `clientToNdc`/`ndcToClient` (6 inline NDC↔px formulas), `lib/geo/heading.ts` `wrapHeadingDeg`/`headingDeltaDeg` **moved OUT of store/camera** (importers repointed; heading tests → `test/lib/geo/heading.test.ts`), `sensors.focalFromHorizontalFov` (consumed by upload.openSavedPin). **B8** the ONE lib→store violation killed — pure param layer moved to `lib/decode/params.ts` (derivedFov + Placement/AdjustableParams/etc.), `store/upload` re-exports for back-compat, `lib/save/pinBody` now imports from lib (`grep store/ src/lib` = comments only). **B9** `lib/geo/coerce.ts` (numOrNull/strOrNull, dedup'd from pinRecords + pins) + `lib/pins/fields.ts` `CameraPoseOptics` (11 pose/optics fields `extends`-ed by SavePinBody/PhotoListItem/PublicPin + `Partial` by SavedPinView) + pins fallback → `DEFAULT_PRECISION_TIER`. **B10** `lib/api/http.ts` `json()`+`requireMember({full?})` — killed the 3 duplicated json helpers + getCurrentMember try/catch in api/{photos,upload-url,ping} (POST/PATCH full fieldsets, GET/DELETE id-only). **B11** `src/global.d.ts` typed Window for all 8 `__*` DEV seams — every `(window as any)`/`as unknown as` cast removed. **B12** `GlobeControlsInternal` shim replaces the `controls as any` (zoomDelta/up/getUpDirection/_applyRotation); **the generic-requestJson half DECLINED** (falsification gate — uploadMedia's requestJson has no true 2nd consumer; geocode's error contract differs). **B13** ~20 orchestrator magic numbers → new **ORCH** tuning group + additions to CONTROLS/FPV/FRUSTUM/TEMPPIN/SUN (pure math-degeneracy guards left inline). **B14** tuning.ts section index. **B15** save.ts `BUSY_PHASES`/`isBusy` (update/delete now also blocked during a concurrent save's upload — correct invariant) + `toApiError`. **B26** update() catch throttled (log-once-then-count, ORCH.errorLogThrottleMs — no 60 fps flood). **Verification:** 323 vitest (314→323, +9 lib tests) · astro check 0 · a 3-reviewer adversarial Workflow (const-value fidelity · extraction equivalence · layering/completeness) with real git-diff investigation → **0 findings**. **NEXT:** **B19** (owner's explicit pre-S7 ask, browser-verified) — split StylizedTiles `update()` into named step-fns, EXACT order preserved; the B6/B12/B13/B26 scaffolding it needs is now in place. A full **execution playbook was prepared this session** — `.claude/claude-docs/B19_HANDOFF.md` (36-step ordered map + line ranges · a concrete `FrameContext` TS type + the closure-state-that-stays list · **13 load-bearing traps with guards** — the `++frameCount` pre/post-gate split, the `camNow`-vs-`camStore` dual snapshots, `adjustCamera`-every-FPV-frame, frozen `_focus`/`alt`, the `updateMatrixWorld` flush sites · a mechanical migration recipe · a 14-flow browser verification checklist), synthesized from a 5-agent (4 analysts + synthesizer) workflow reading the current file. Then **B20–B25 fold into S7**. Memory: `mem:project/wip-2026-07-11-pre-s7-refactor-s2`.
- **2026-07-11 — Pre-S7 architecture review + safe-refactor tier (local-VERIFIED; 314 vitest · astro check 0).** Reflective review via a 7-track parallel workflow → risk-classified 26-item backlog (`ARCHITECTURE_REVIEW.md`). Executed the safe/local-verified subset: **DECISIONS.md compacted** 709→333 lines (ADRs hoisted + durable Traps & Gotchas section + per-phase digests; the 2026-07-09/10 logs moved BYTE-IDENTICAL to `DECISIONS_ARCHIVE.md`, append-only preserved — 35 entries + 15 ADRs conserved, integrity-checked); **doc-drift fixed** across `.claude/CLAUDE.md`, `ARCHITECTURE.md`, `IMPLEMENTATION_PLAN.md`, `wix-headless.md` + 4 conventions (killed the phantom `npm run lint` gate — no lint script exists; regenerated the repo-layout block to the actual tree; removed the unused-Tailwind claims; recorded the D8 hook→endpoint-quota supersession; resolved the COOP/COEP + 90 m-sink + wasm TODO staleness); **4 conventions codified** (store seam+mirror, ECEF camera-anchoring, ONE-shared-building-material, the browser-verify 3-tier harness + DEV-seam registry + scene-attach naming); **dead code removed** (`components/Welcome.astro` + 3 orphaned SVGs; dead `frustumPose`/`FrustumPose` + its test; dead `TERRAIN` tuning const) — but **KEPT `api/ping.ts`** (falsification-gate catch: it is the documented pre-release POST-403 canary, not dead — only its stale header refreshed); **format dedup** (`formatAltM`/`formatEyeM`/`cardinal`/`formatSigned` → `lib/format/readout.ts`, both panels rewired) + **added `test/lib/decode/extract.test.ts`** (the decode-path classifiers). **DEFERRED (next):** the remaining safe dedup (B6/B8–B13/B15 — a clean local-verified follow-up batch); **B19 orchestrator readability** (split `StylizedTiles.ts`'s 917-line `update()` into named step-functions over a frame-context — browser-verified; the owner's explicit pre-S7 ask); **B20–B25 fold into S7** (browser/shader/precision — S7 edits that same terminator GLSL + camera math). Full backlog + risk classes: `ARCHITECTURE_REVIEW.md`. Memory: `mem:project/wip-2026-07-11-pre-s7-refactor`.
- **2026-07-11 — S6 follow-up (owner): sky guides gated to FPV + SKY toggle — day-arcs & asterisms show ONLY in FPV/camera-view; every other mode gets just ☀/☾ direction chips; one right-panel toggle enables/disables it all (browser-VERIFIED via Playwright MCP on wix dev; 304 vitest · astro check 0 · wix build green).**
  New `camera.skyGuides` (default ON) + `setSkyGuides` + `skyMarkers` mirror (`{sun, moon} FpvBodyMarker | null`, %3-frame cadence like fpvHud): the orchestrator's marker block now runs in EVERY mode — bearings reference = the FPV anchor in FPV, else the camera's own `ecefToGeodetic` position (the `up` gate then means "not behind the planet" from orbit — matches the impostor's horizon fade). Chips moved from `fpvHud` to `skyMarkers` in FpvHud.tsx (card stays FPV-only — it's an instrument readout, not sky decoration; toggle OFF in FPV keeps the card, kills arcs/asterisms/chips). Asterisms: `stars.update` gained `asterisms?: boolean` — `visible = assetLoaded && flag` (flag = `fpvActive && skyGuides`; the night/altitude star fade still applies on top) — the LEO ambient night tracery is GONE by default (S6 behavior superseded). Day-arcs: anchor passes only when `fpvActive && skyGuides` (existing 250 ms ease-out covers the toggle; hidden needs ~4.6τ ≈ 1.2 s — verify scripts must wait). Toggle UI = `☀☾` chip in the CameraTiltPanel ct-row (`.ct-sky.is-on` accent-lit, aria-pressed). Chips + HUD hidden under `body.welcome-active` (they'd float over the landing otherwise). Verified live: welcome clean · orbit default = 2 chips/no arcs/no asterisms (shot phase55-49) · toggle OFF orbit = 0 chips, mirror null · temp FPV ON = arcs+HUD+chips, night asterisms 0.15 · toggle OFF in FPV = arcs hidden (fade 1.2 s), asterisms off, card stays · re-arm clean. Files: `store/camera.ts` · `globe/StylizedTiles.ts` · `globe/scene/stars.ts` · `panels/{FpvHud,CameraTiltPanel}.tsx` · `styles/{camera-tilt,fpv-hud}.css` · test `store/camera.test.ts` (303 → 304). Memory: `mem:project/wip-2026-07-11-phase5.5-s6` §SKY-toggle follow-up.
- **2026-07-11 — Phase 5.5 S6 SHIPPED: FPV planning overlays (sun/moon day-arcs + asterisms) + owner FPV batch — HUD · ALTITUDE/FOCAL ZOOM encoders · building opacity curve · brighter moon (browser-VERIFIED via Playwright MCP on wix dev; 303 vitest · astro check 0 · wix build green).**
  **(1) Day arcs** (§Item 4): `lib/ephemeris/dayArc.ts` (pure, 10 tests) — `sampleDayArc(body, sceneMs, lat, lon)` samples `horizontal()` every 10 min across the LOCAL SOLAR day (`round(lon/15)` h — the captureTime convention, NOT browser TZ; window helpers `localDayWindow`/`dayFraction`; peak asserted 64.97° at the Dnipro-solstice Horizons anchor) + `azAltToEnu`. Renderer `globe/scene/dayArcs.ts`: camera-anchored group at THE SAME impostor distance function as sky.ts — the discs sit ON their arcs by construction (verified: live sun + 2026-07-29 full moon each pinned to their line, shots phase55-43/46); per-vertex `aT01`+`aFade` attrs → past/future split is a `step(uNow01, vT)` shader compare (scrub +3 h moved uNow01 by exactly 0.125, sun slid 216°→266° along the arc — NO geometry rebuild; rebuild only on anchor move / day-cross, both verified) and horizon melt over altDeg −6°→−1° (rise/set dive visibly). Anchor = photo placement | temp pin; overlay eases in/out (DAYARC.fadeTauMs 250) with FPV. **TRAP (browser-bisected): additive-blended arcs VANISH against the bright day sky** — the "arc" first seen was actually pin stems; diff-shot proved the additive lines added only ~10–30/255. Arcs are alpha-blended (PhotoPills-style solid stroke), sun = tokens.sunGlow / moon = tokens.moonlight, depthTest OFF (analytic horizon owns occlusion — the fake camera distance would make depth lie), renderOrder 10 **set per-object (a Group's renderOrder does NOT propagate to children)**.
  **(2) Asterisms** (§Item 4): `scripts/build-asterisms.mjs` bakes d3-celestial (BSD-3, credit kept) p≤3 → `public/data/asterisms.json` (26 figures, 165 verts, ~8 KB; **RA stored in DEGREES — `raDecToUnit` takes HOURS, ÷15 lives in `lib/ephemeris/asterisms.ts` ONLY**, Dubhe-anchored test); LineSegments CHILD of the BSC5 star sphere (inherits −GAST + camera-follow + scale + visibility gate; own `opacity = fade × ASTERISMS.alpha 0.15` since LineBasicMaterial can't share the points shader). Live: 134 segments at opacity 0.15 over the night star field (shot phase55-45), gone by day/street.
  **(3) Moon brighter (owner, "organically")**: SKY.moonBrightness 2.4 → 3.2 + moonEarthshine 0.1 → 0.12 — rides the existing albedo·(N·sun)^0.8 curve so maria/phase survive; extra light lands as bloom glow, not clipping (full-moon disc shot phase55-46).
  **(4) FPV HUD** (owner): LEFT-side `panels/FpvHud.tsx` + `styles/fpv-hud.css` (mono instrument card: FOCAL mm-equiv + FOV° · HEADING+cardinal · PITCH · EYE · SUN/MOON az/alt or BELOW HORIZON) + off-frame EDGE CHIPS (☀ warm/☾ cool, clamped to a 64 px margin box, arrow toward the body; hidden when in-frame or below FPV.bodyMarkerMinAltDeg −6°). Mirrors: `camera.fpvHud` written by the orchestrator every FPV.hudSyncEveryFrames 3 (bearings from `enuBasis` at the anchor; camera-space classification via pure `lib/geo/offscreen.ts frameMarker` (5 tests); moon direction CAMERA-relative like the impostor — disc/arc/chip agree). Focal equiv = new `focalFromVerticalFov` (sensors.ts, 24 mm FF height; 35 mm round-trips exactly, 3 tests). Verified live: 23 MM·55.0° at temp entry; chips flip exactly on inFrame; both read BELOW HORIZON at astronomical night. **Layout fix (browser-caught): PhotoDetailPanel overlapped the HUD → .fh docks at bottom 2.4rem (clear 16 px gap at 900px).**
  **(5) FPV buildings** (owner): ghost fill 0.2 → 0.3 + edges 0.12 → 0.18, and the fill gained a PER-FRAGMENT camera-distance falloff via chained `onBeforeCompile` on the ONE shared MeshStandardMaterial (invariant holds — distance/altitude are global uniforms): alpha = uGhostAlpha × smoothstep(60 m, 260 m, |vViewPosition|), lifted to 1.0 by uSolidK = smoothstep(40 m, 180 m, eyeAboveGround) (owner: altitude → full opaqueness); `setGhostSolid(k)` per-frame + depthWrite re-engages >0.6 (near-solid ghosts must not show their own backfaces); edge opacity blends ghost→normal by the same k (live: 0.1847 at 52 m eye = the curve to 4 decimals; depthWrite flip verified at 400 m).
  **(6) FPV controls** (owner): ZOOM encoder relabels **ALTITUDE** in ANY FPV (readout = eyeAboveGroundM) and a **FOCAL ZOOM** encoder appears (readout = live focal equiv) → new `camera.fovRatePerS` seam applied onto the SAME eased `fovTargetDeg` as the wheel (FPV.fovRateMaxPerS 0.9: 55°→19.3° on a 1.2 s stick = exp math exact). `rateAllowed` widened to !flight — photo FPV unlocked: ROTATE = look yaw (44→215° verified), ALTITUDE = **vertical LIFT off the apex** (`fpvLiftM` 0–400 m, 0 = exact photo alignment reset at entry; lift 114.5 m → HUD eye 116.1 = 1.7+114.5 exact); temp FPV keeps fpvEyeM. Escape unwind + HUD unmount + arc fade-out verified clean.
  Files: `lib/ephemeris/{dayArc,asterisms}.ts` · `lib/geo/offscreen.ts` · `lib/decode/sensors.ts` · `globe/scene/{dayArcs,stars,buildings}.ts` · `globe/{StylizedTiles,tuning}.ts` (DAYARC + ASTERISMS groups; FPV grew 9 knobs) · `store/camera.ts` (fovRatePerS · fpvHud + FpvBodyMarker) · `panels/{FpvHud,CameraTiltPanel}.tsx` · `styles/fpv-hud.css` · `pages/index.astro` · `scripts/build-asterisms.mjs` · `public/data/asterisms.json` · tests dayArc/asterisms/offscreen/sensors (280 → 303). Shots `verify-shots/phase55-43..48`. Memory: `mem:project/wip-2026-07-11-phase5.5-s6`. UNVERIFIED: mobile/reduced-motion pass · asterism readability at LEO daytime (same gate as stars — accepted) · wix release.
- **2026-07-11 — S5 follow-up (owner): global continental/ocean textures upgraded to 8k — the blocky ~20 km coast "hexels" at mid zoom are gone (browser-VERIFIED at the owner's Sea-of-Azov pose; 280 vitest · astro check 0 · wix build green).**
  Root cause of the owner's screenshot: `earth-landmask.png` is 2048×1024 ≈ **19.6 km/texel** — at ~1700 km (Blue Marble owns >750 km) each mask texel spans ~20 screen px, and coastBand smoothstep over bilinear texels renders the blocky water boundary; the 5400² colour was also ~7 km/texel soft. Upgrades (both async, BSC5/night idiom, gated maxTextureSize ≥ 8192; boot textures stay as first-paint/mobile fallback): **(a) colour** `earth-color-8k.jpg` 8192×4096 (7.1 MB q88, sips-downscaled from NASA BMNG July 21600×10800 record 73751 — HEAD-verified single-file) swapped via TextureLoader/sRGB; **(b) land/water mask** `earth-landmask-8k.png` (2.9 MB) **DERIVED FROM THE SHIPPED 8k COLOUR ITSELF** in headless-Chrome canvas (`scripts/build-earth-landmask.mjs`, one-time bake) — registration-PERFECT with the rendered coastlines, unlike any external mask; classifier = BMNG-bathy blue-dominance `clamp((b−max(r,g)−2)/10)` + **near-black-lake rule** (BMNG paints inland lakes ~BLACK with JPEG-destroyed hue — Baikal (0,8,11), Superior (9,20,3), Tanganyika (3,3,5) — while the darkest land (Congo 37, taiga 47) stays above: `clamp((28−maxc)/12)` OR'd in) + ice/gray guards (v>0.82·desat<0.12 → land); 18-point known-coordinate probe suite all correct (oceans/seas/lakes water · deserts/forests/ice-sheets land; July pack-ice fringes classify "land" → read as ice shelf under the organic white — accepted). Ships as a single-channel **R8 DataTexture** via the S5 `extractRedChannel` pipeline (mask is linear data — no gamma). baseEarth upgrade block refactored: shared `rasterize`/`makeR8`/`swapUniformTex` helpers drive night+mask+colour. NASA NEO's BMNG landmask archive is 404-dead (probed) — derivation was also the better option. Verified live: all three upgrades land (mask/colour 8192) · Azov/Crimea coastlines crisp at the owner's pinned instant (Arabat spit + Sivash resolve; shot phase55-41) · LEO day look unchanged in tone, crisper in line (shot 42). GPU ledger note for the mobile pass: base-earth textures now ≈ 280 MB (colour 8k RGBA+mips 179 · night+mask R8 45×2 · 2k normal/elevation) — upgrades already skip <8192 GPUs, consider a deviceMemory gate later. Files: `globe/tuning.ts` (EARTH.textures.color8k/landMask8k) · `globe/scene/baseEarth.ts` · `scripts/build-earth-landmask.mjs` · `public/textures/earth-{color,landmask}-8k.*`. Memory: `mem:project/wip-2026-07-11-phase5.5-s5` §8k-earth follow-up. UNVERIFIED: mobile memory · elevation/normal remain 2048² (relief + land-ramp colour zones are the next softness ceiling if the owner asks).
- **2026-07-11 — Phase 5.5 S5 SHIPPED: night-sky physics — K&S-1991 moonlight · moon-shadow rig switch · brighter stars/MW · Black Marble 8k R8 city lights · near-black night sky with the navy floor ROOT-CAUSED (browser-VERIFIED via scripted headless-Chrome CDP on wix dev; 280 vitest · astro check 0 · wix build green).**
  **(1) Moonlight physics** (§Item 7): `lib/ephemeris/moonlight.ts` — pure K&S-1991 `I(α) ∝ 10^(−0.4(0.026α + 4e−9α⁴))` normalised to full; `bodies.ts` exposes `moonPhaseAngleDeg` (phase_fraction = (1+cos α)/2, test-asserted); the orchestrator scales the moon key + `uMoonGlow` by the K&S intensity (SKY.moonKeyIntensity/moonSceneGlow stay the FULL-MOON anchors — calibration preserved by construction). Live-verified quarter/full = **10.3%** (moonLight 0.0282 vs 0.2729; instants precomputed w/ astronomy-engine: full 2026-07-29T22:10Z illum 0.999 α 3.9°, quarter 2026-08-06T00:50Z illum 0.508 α 89°). Moon disc `SKY.moonBrightness` 1.8 → 2.4.
  **(2) Moon shadows** (§Item 7): ONE-rig source switch in StylizedTiles — sun below `minSunElevSin` AND moon above it AND illum ≥ `SHADOWS.moonMinIllum 0.85` → the sun key IMPERSONATES the moon (tokens.moonlight colour · `SKY.moonKeyIntensity × ks` · position from moonDir) while the dedicated moonLight stands down (`sky.update` gets moonIntensity 0 — the night key is never doubled); ground twins get `setShadowStrength(SHADOWS.moonGroundOpacity 0.55 × ks)` (new ImageryGroundHandle method — ONE shared ShadowMaterial). Live: castShadow TRUE at the full-moon Dnipro city night (key bfd0e8 @ 0.273 = 0.3×0.9098) · OFF at quarter (illum gate) · sun rig untouched by day (1.5, white→golden, castShadow).
  **(3) Stars/MW brighter** (§Item 7): STARS.brightMin 0.55→0.65, alpha 0.8→0.9; MILKYWAY.alpha 0.25→0.35, sizeBase 2.2→2.6.
  **(4) Black Marble 8k** (§Item 8): `public/textures/earth-night-8k.jpg` (8192×4096 gray, 1.77 MB — sips-downscaled from NASA record 144897 13500×6750); baseEarth upgrades ASYNC (BSC5-catalog idiom): fetch → canvas → `lib/textures/redChannel.ts extractRedChannel` (pure, tested; **flipY must happen IN THE DATA — WebGL UNPACK_FLIP_Y ignores typed-array uploads**) → RedFormat R8 DataTexture (~34 MB+mips vs ~134 RGBA; GPU mips fine, R8 is colour-renderable) + `uNightGamma` 2.2 shader linearisation (**no single-channel sRGB format exists in WebGL2**; the boot texture stays hardware-decoded at γ1); shader now samples `.r` (luma dot dropped); 3600² VIIRS stays as boot/mobile fallback (upgrade skipped when MAX_TEXTURE_SIZE < 8192); NASA credit appended to the DOM attribution. Verified live: gamma 2.2 + isDataTexture 8192×4096; LEO night lights crisp (shot 36).
  **(5) Darker night sky + NAVY FLOOR ROOT CAUSE** (§Item 15 — found by live bisection; NOT any suspect on the carried list): `renderer.setClearColor` converts the colour to the renderer's OUTPUT space (sRGB) because **no render target is bound at setup** (three `getUnlitUniformColorSpace`), and EffectComposer runs autoClear-off, so RenderPass's raw `renderer.clear()` dumped those sRGB-ENCODED values into the LINEAR HalfFloat buffer every frame; OutputPass then treated them as linear — PBR-Neutral's black offset ate the red channel and the sRGB encode boosted G/B: **#05070B rendered as (8,26,45) navy on every empty sky pixel** (RT readback showed literally (5,7,11)/255 as linear floats; the pipeline math reproduces the measured pixel to the unit). FIX: `scene.background = Color(tokens.bg)` — three converts it PER-RENDER-TARGET inside renderer.render (linear into the composer's buffer, sRGB when direct) and force-clears over the stale GL state. Night sky now measures **(2, 5.9, 10.7)** mean (was (10.8, 28.3, 46.2)) — near-black, stars own it (shot 37). Companions: **atmosphere Chapman obliquity** (`ATMOSPHERE.obliquityK`, sinLimit = √(2H/(π·Re)) per scale height — up-looking rays traverse ~H/sin(el) of air, not the grazing chord; they previously rendered as bright as the limb at 20–350 km; limb-passing rays keep pk=1, the LEO look is untouched) + night floors EARTH 0.22→0.19 / GROUND 0.38→0.35.
  **Verification:** `scripts/verify-s5-night.mjs` (reusable CDP harness; asserts 8k swap · K&S ratio · moon-shadow engage/stand-down/day-intact · night-sky mean) — **PASS**; shots `verify-shots/phase55-36..40`. DEV add: `window.__composer` (pass introspection, __renderer/__globe idiom). **DEBUG TRAPS recorded:** `document.elementsFromPoint` SKIPS pointer-events:none nodes (it "proved" no DOM overlay while being blind to one class of them) · hiding the tiles groups CRASHES the rAF tick → the canvas keeps presenting the last good frame and every "hidden" measurement reads unchanged (guard bisections with a renderer.info.render.frame advance check) · `stars.update()` re-sets points.visible every frame — hide via `material.visible`.
  Files: `lib/ephemeris/{moonlight,bodies}.ts` · `lib/textures/redChannel.ts` · `globe/{tuning,StylizedTiles}.ts` · `globe/scene/{sky,baseEarth,atmosphere,imageryGround}.ts` · `GlobeCanvas.tsx` · `pages/index.astro` · `public/textures/earth-night-8k.jpg` · `scripts/verify-s5-night.mjs` · tests `test/lib/ephemeris/moonlight.test.ts` + `test/lib/textures/redChannel.test.ts` (270 → 280). Memory: `mem:project/wip-2026-07-11-phase5.5-s5`. UNVERIFIED: mobile 8k decode (134 MB transient RGBA during extraction) · moon-shadow READ on other cities (rig verified; contrast has the known dark-palette ceiling) · 13.5k texture tier (not shipped — 8k only).
- **2026-07-11 — Welcome/Explore start fix: the journey now OPENS CRUISING — no entry flight, no pin lunge on welcome load — and cruises 2× faster (owner bug report; browser-VERIFIED via scripted headless-Chrome CDP; 270 vitest · astro check 0).**
  `globe/explore.ts`: `"entering"` state REMOVED — `setActive(true)` → `"arming"`, and the first `update()` runs `begin(nowMs)` which NN-orders and starts the first leg **from the current camera pose**; the <2-pins fallback no longer flies to the explore pose either (idle drift owns until the viewport query lands ≥2 pins — on welcome load that is ~0.5 s of drift, then straight to cruising). `poseFor` + the cinematic flight survive only as reduced-motion leg cuts. The entry pose is absorbed by the existing altitude low-pass plus a NEW `easeLookToward` view-ray low-pass (τ `poseEaseTauMs` 1200 ms, ceiling `lookMaxRateDegPerS` 12°/s) eased in **tilt/heading space at the camera** — two measured traps forced that design: pure exponential err/τ on a ~120° entry error whips at >90°/s, and easing the look POINT along the surface (even rate-capped — a geocentric cap is amplified ~R/d ≈ 4–6× into camera swing) drags the gaze through a nadir stare mid-pan because the surface great-circle between the look points passes near the sub-camera point; angle-space keeps tilt oblique (measured 51.1→49.9° monotone) while the heading pans ≤12°/s. Cruise speed ×2: `legTargetS` 28→14, ω band 0.06–0.55 → 0.12–1.1 °/s (resolves S4's "legs ~45 s" pacing note). Verified by ~100 ms CDP pose-sampling (Playwright MCP wedged AND the Chrome-extension bridge disconnected — scripted headless Chrome is the third fallback tier): flight never activates on welcome load · states fallback(0.5 s)→cruising→dwelling · max camera-ray rate 10.6 °/s · min tilt 49.9° · max position rate 0.54 °/s · altitude eases 1100→900 km · canvas pointerdown still dismisses welcome AND exits the journey. Shots `verify-shots/welcome-cruise-{4s,22s}.jpeg`. Files: `globe/{explore,tuning}.ts`. Memory: `mem:project/wip-2026-07-11-phase5.5-s4` §Welcome-start fix. UNVERIFIED: reduced-motion cuts (path unchanged) · pointer-feel on a live display.
- **2026-07-11 — Phase 5.5 S4 SHIPPED: pin visual rework (stems/heads/hues/hover) + Explore ambient journey + Welcome landing (browser-VERIFIED via Playwright MCP on wix dev; 267 vitest · astro check 0 · wix build green).**
  **(1) Pin look** (`globe/Pins.ts` rework): THREE instanced draws sharing one angular-constant head radius — stems (unit cylinder, vertex-alpha quadratic fade to base), heads (ShaderMaterial: semi-transparent core `headCoreAlpha 0.38`, fresnel rim pow 2.6/gain 1.6, per-instance shimmer phase+rate from hash(id)), additive billboard cross-FLARES gated to shimmer peaks (`flareThreshold 0.86` — calm twinkle, not a blink). Heads = the raycast pick target (stems/flares raycast-noop); `boundingSphere = null` discipline kept on every instance change; highlight pulse survives (verified post-refactor: scale 6.15→8.7 swing).
  **(2) Per-author hues:** `lib/pins/appearance.ts` (pure, 11 tests) — FNV-1a `hash01`, weighted `pinHueIndex(authorName, weights, salt)` into NEW pin tokens (pinTeal/pinIce/pinMint/pinLavender/pinWarm in tokens.css + GL bridge; teal-weighted 3.5/2/2/2/0.5, warm rare). **`hueSalt "pin:"` is the palette seed — tuned so the 3 live authors resolve distinct** (Svitlana=mint · Yevhen=lavender · tester=teal; live-verified). Legend swatch row in PhotoDetailPanel save section previews the member's hue (client `memberLabel` = server `authorLabel` twin) — UNVERIFIED with a live member cookie (anon gate blocks the row).
  **(3) De-cluster** (owner same-day): `clusterLayout` de-levels same-gh6 pins to evenly spaced stem-height slots AND scatters them on a `scatterRadiusM 140` ring (cell-hashed rotation; C6-honest — reduced coords are cell centres). Live-verified: the 3 colocated Dnipro pins render separated and individually hoverable. Stems raised ×1.5 (base 3.6/spread 2.8/max 3600 — owner "stems higher").
  **(4) FLICKER ROOT-CAUSED + fixed** (owner: "sphere texture flickery/patchy at close zooms"): ECEF ~6.4e6 m × float32 GPU matrices = large×large cancellation quantising at ~0.5 m that CRAWLS as the camera moves (the PhotoFrustum lesson, now on instances). Fix = every matrix rebuild anchors all three meshes AT THE CAMERA (`mesh.position = camera.position`, instance translations camera-relative) + shaders use ONLY `modelViewMatrix` (the cancellation happens CPU-side in f64; fresnel moved to view space; flare billboards in view axes). Verified: consecutive close-zoom frames pixel-stable. TRAP: `hoverAnchor` must add `mesh.position` back (instance matrices are now relative).
  **(5) Explore ambient journey** (`globe/explore.ts` + EXPLORE tuning + `camera.exploreActive` seam + nav `[data-explore]` + `ExploreMode` chip): nearest-neighbour order over loaded pins from the view focus (pure, tested) → cinematic entry flight → constant-ω great-circle legs (`ω = clamp(arc/28 s, 0.06–0.55°/s)`, edge ramps 0.18/floor 0.12) at 900 km/50° tilt (look-ahead γ = `asin((R+h)/R·sinα)−α` ≈ 10.9° — pure, tested), 6 s dwells with a slow 0.12°/s orbit around the pin (journey never freezes on one-city data), fallback <2 pins = explore pose + idle drift (re-begins when pins land). Exits: ANY canvas pointer/wheel (noteInteract), Escape (tops the unwind chain), encoder/slider steering, search fly-to. Verified live: entry→dwell→2 legs (Dnipro→Kyiv→Odesa synthetics)→clean pointerdown exit (chip + nav accent clear). NOTE: legs run ~45 s with ramp tails (design band 20–40 s) — tune `legTargetS`/ramps if it feels slow.
  **(6) Hover** (throttled every 4 frames, head raycast): hovered pin eases ×1.55 (per-pin eased amounts — breathes out too), cursor=pointer, `pins._syncHover` mirrors pin + projected head point → `PinHoverCard` (fixed, `.ct-pinpop` discipline; thumb/title/authorName/date). Verified live incl. post-refactor anchor.
  **(7) Welcome landing** (owner mockup, replaces the hero): `panels/Welcome.tsx` + `welcome.css` — eyebrow/headline/copy + UPLOAD A PHOTO (data-open-upload) + EXPLORE THE GLOBE, real pin-count footer, `body.welcome-active` hides ts/tr/ct-stack/lf/explore-chip, **auto-arms the Explore journey as the living backdrop**; any globe click dismisses into the full UI (one gesture also exits the journey); UPLOAD dismisses into the flow (verified), EXPLORE dismisses keeping the cruise. "+ ADD PHOTO" pill RETIRED (`AddPhotoPill`/probe/CSS removed). Time scrub now properly centred (`left 50%/translateX`, width backs off the right column); TimeReadout raised to the scrub's axis (bottom 2.85rem).
  **VERIFY-TRAP (tooling):** an occluded Chrome window throttles rAF to ~1 frame/several s — flights/cruise appear "broken" (862 km readings mid-descent). `page.bringToFront()` before timing-sensitive Playwright verification.
  Files: `globe/{Pins,explore,StylizedTiles,tuning}.ts` · `lib/pins/appearance.ts` · `lib/theme/tokens.ts` · `store/{pins,camera}.ts` · `panels/{PinHoverCard,ExploreMode,Welcome,PhotoDetailPanel,UploadFlow}.tsx` · `styles/{tokens,pin-hover,explore-mode,welcome,photo-detail,time-scrubber,time-readout,upload-flow}.css` · `pages/index.astro` · tests `test/lib/pins/appearance.test.ts` + `test/components/globe/explore.test.ts` (+ store seams). Shots `verify-shots/phase55-18..27`. Memory: `mem:project/wip-2026-07-11-phase5.5-s4`. UNVERIFIED: legend swatch with a member cookie · welcome/journey on mobile · reduced-motion explore cuts.
  **(d) ADAPTIVE de-cluster — SUPERSEDES (c)'s head-lean (owner: "do not skew, this is ugly"; browser-VERIFIED end-to-end):** pins stay vertical; colocated pins adapt by camera→cluster DISTANCE (≈ zoom altitude). **FAR (≥`clusterSingleDistM` 300 km):** a cluster renders as ONE marker (only the rank-0 representative visible; live: 5 pins → 2 markers); hover card says "N PHOTOS HERE · CLICK TO ZOOM IN" (`pins store hoverCount` + card variant); CLICK dives to `clusterDiveAltM` 60 km instead of opening (live: 4-cluster hover card ✓, click → 60 km, no panel). **MID:** members spread on a min-separation ring (`ringRadiusM(n, sepFrac 2.4 × head radius)` — neighbor chord ≥ one head diameter; screen-constant, world radius shrinks with zoom: live 1.9–2.7 km apart at 60 km → 96–143 m at 3 km) blending to TRUE coordinates via `trueBlend(clusterTrueMinSep / requiredSep)`. **NEAR (≤`clusterMergeDistM` 1 km):** the ring folds onto exact coordinates (live at 0.5 km: identical trio coincident, the 152 m-distinct member at ITS true spot — truth wins); stem-height stagger keeps merged pins selectable — **the flat `stemMinM` floor levelled low-slot stems at street zoom (browser-caught coincident heads) → the MIN clamp is now stagger-scaled `stemMinM×(1+s)` (live: 7.5 m min head separation)**. **SELECTED pin walks home at any range** (`selectEaseTauMs` 350; orchestrator mirrors viewingPinId → `pins.setSelected`; live: ring member 479 m off-truth → 0.5 m after open). Anchors stay TRUTHFUL (the (c) invariant: frustum apex = stored coordinates — displayed positions are per-frame copies). Pure math in appearance.ts (`ringRadiusM`/`proximityFactor`/`trueBlend` + cluster membership in `clusterLayout`), all unit-tested. 270 vitest · astro check 0 · wix build green. Shots phase55-33(collapsed hover)/34(mid ring)/35(merged). Files: `lib/pins/appearance.ts` · `globe/{Pins,StylizedTiles,tuning}.ts` · `store/pins.ts` · `panels/PinHoverCard.tsx`. Limitation: clustering is per-gh6-cell — near-boundary pins in adjacent cells don't co-cluster.
  **(c) Truthful pin bases — de-cluster leans HEADS, never anchors (owner, same day; browser-VERIFIED; the lean SUPERSEDED by (d), the truthful-anchor invariant KEPT):** the anchor-ring scatter (a) shipped a contradiction — clicking a scattered pin opened its frustum at the TRUE coordinates ~140 m away from the marker. Now `placePin` anchors every stem base at the pin's exact coordinates; de-clustering leans only the HEAD: stem vector = up·stemH + ENU ring offset × `PINS.scatterHeadFrac 1.5`·size (replaces scatterRadiusM — size-proportional → constant screen separation at every zoom), stems rendered base→head as leaning needles, head floats along the stem direction. Colocated pins read as a bouquet of needles rising from the one truthful spot, converging as you zoom. Verified live: clicked a fanned head → placement === pin lat/lon to 1e-9, cone apex at the stem-base convergence (shots phase55-31/32). 267 vitest · astro check 0. Files: `globe/{Pins,tuning}.ts`.
  **(b) Photo PITCH = encoder (owner, same day; browser-VERIFIED):** the ±90° positional slider became the same spring-centred rate control as HEADING — the S2 heading rAF loop generalised to `onParamRate("heading"|"pitch")` (one loop per param; heading wraps 0–360°, pitch clamps ±90°); new `CONTROLS.pitchRateMaxDegPerS 25` (half the heading rate — half the range, comparable full-stick sweep time), same expo/reset idiom. Verified live: hold-right 24.5°→44.9°, hold-left →29.8°, double-click reset → EXIF 24.5 exact (shot phase55-30). 267 vitest · astro check 0. Files: `panels/PhotoDetailPanel.tsx` · `globe/tuning.ts`.
  **(a) Hover-bounce fix + rest transparency (owner, same day; browser-VERIFIED):** hover used to multiply the ONE `size` that also derives stem height + head centre → the head lifted out from under the cursor → hover dropped → it fell back → the pin bounced. Now LAYOUT (stem height, head centre) always uses the BASE size; hover/pulse multiply only the head/flare INSTANCE SCALE — the head grows in place (highlight pulse made position-stable too). Hover also brightens (`hoverBrighten 0.45` via new per-instance `aHover` attr riding the same ease) and SOLIDIFIES: rest core alpha `headCoreAlpha` 0.38 → **0.22** (airier by default, owner ask), hover restores `headCoreAlphaHover 0.38`. Verified: cursor parked on a head → hoverPin stable for 3 s straight (20/20 samples, no flicker); rest-vs-hover shots `phase55-28/29`. 267 vitest · astro check 0. Files: `globe/{Pins,tuning}.ts`.
- **2026-07-11 — Phase 5.5 S3 SHIPPED: pin lifecycle (PATCH/DELETE) · authorName · custom pin name · placement-flow completion · upload CTA · dblclick memo · temp-pin "UPLOAD HERE" (wix-cloud-VERIFIED in wix dev with a member cookie; 243 vitest · astro check 0 · wix build green).**
  **(1) Backend:** `api/photos.ts` gains PATCH {photoId,…SavePinBody} + DELETE ?id= — elevated, owner-gated (`items.get` → `ownerMemberId === member._id`), thin (C1). Pure core in `pinRecords.ts`: `parseUpdatePinBody` + `applyPinUpdate` (null media/file fields in a patch KEEP stored values — media continuity live-verified) + `authorLabel` (nickname → email user-part → "Member"). C6 stays structural on edits: PATCH re-derives PublicPins via `publicPinRecord` — a location edit re-reduces to the NEW cell centre (live: exact 57.43621/49.67614 → published 57.43378/49.67468); isPublic toggle removes/recreates the public row (both branches live-verified); DELETE removes PublicPins (linked id, else photoRef query) + Photos + media best-effort (`files.bulkDeleteFiles`) — **quota slot freed live (2/10 → 1/10)**. POST/PATCH now use `getCurrentMember({fieldsets:['FULL']})` (PUBLIC omits loginEmail).
  **(2) authorName** denormalized on PublicPins at save/update; provision script gained an incremental **create-field diff pass** (`POST /wix-data/v2/collections/create-field` — collection-exists no longer means skip); `scripts/backfill-author-names.mjs` back-filled **5/5 live pins keyed on photoRef** (both label paths seen: nicknames + "frame-p5-tester"). `PublicPin.authorName` now on the pins store (null on pre-S3 rows).
  **(3) Custom pin name:** `save.title` + PIN NAME input (defaults to file-name title; `buildSavePinBody` opts.title override).
  **(4) Client edit/delete:** MY PINS passes `ownPhotoId` (a globe click on a foreign pin never sets it) → PhotoDetailPanel swaps SAVE for **UPDATE PIN / ⌖ RE-PLACE / DELETE (two-step "SURE?")**; edit section seeded from `ownPinMeta`; `upload.rePlace()` rides the placing machine (Escape/cancel returns to the pin via `cancelPlacing`), MyPins rows gained armed ✕ delete; `save.updatePin/deletePin` → `pins.refresh()`.
  **(5) Placement-flow completion:** live accent marker hugs the rendered ground under the pointer while `placing` (PLACING tuning; pickGround per 2 frames — the crosshair alone hid the drop pixel); PLACED ✓ → TUNED → SAVED stepper in the panel; save success = "PINNED ✓" beat (1.6 s) → auto-close → `requestFly` fly-out to `PINS.savedFlyOutAltM 3800` (landed 3,899 m live) → **pulse-highlight** the new pin (pins store `highlightId` → `Pins.setHighlight`, 8 s breathing scale; private pins skip — no public row to pulse).
  **(6) Upload CTA (item 12):** nav Upload = accent-outlined chip + one-time glow ease-in (no loops; reduced-motion off) + "+ ADD PHOTO" pill over the idle globe for anonymous/zero-pin members (one own-pins probe per load).
  **(7) OWNER ADDS:** `.ct-stack` memo above the camera controls — "◎ double-click anywhere to drop a pin & look from there" (retires while a temp pin exists / upload active); temp-pin popup gained **"↑ UPLOAD HERE"** → `upload.uploadAt(lat,lon)` seeds `pendingPlacement`, applied at ingest ONLY when the file has no GPS (EXIF wins — real capture location), consumes the temp pin, review GPS field shows the seed with a **FROM PIN** badge (live: ARW ingested placed at the exact dblclick point).
  Files: `api/photos.ts` · `lib/wix/pinRecords.ts` · `lib/save/{pinBody,uploadMedia}.ts` · `store/{save,upload,pins}.ts` · `globe/{Pins,StylizedTiles,tuning}.ts` · `panels/{PhotoDetailPanel,UploadFlow,MyPins,CameraTiltPanel}.tsx` · `styles/{photo-detail,my-pins,camera-tilt,upload-flow}.css` · `pages/index.astro` · `scripts/{provision-collections,backfill-author-names}.mjs`. Shots `verify-shots/phase55-08..17` (new numbering: 08-idle-cta-memo … 17-mypins-delete). Spectator LOOK-FROM-HERE regression green. Memory: `mem:project/wip-2026-07-11-phase5.5-s3`. UNVERIFIED: media bulkDeleteFiles effect on the Media Manager (best-effort by design) · quota-11 refusal after S3 (unchanged code path).
  **(a) Placement accuracy ROOT-CAUSED + fixed:** click-to-place AND the temp-pin dblclick rays hit the bare ELLIPSOID — 100–190 m
  BELOW the rendered terrain — so oblique rays landed 100 m+ PAST the visible ground (drift grows off-centre and with tilt, exactly the
  reported "gravitates outward / away from viewer"). New `pickGround(ndc)` raycasts the rendered terrain tiles first (shadow twins are
  already raycast-noop), ellipsoid only past-the-limb; used by photo placement + temp pin. Verified: dblclick 320 px off-centre at 52°
  tilt → the pin re-projects **1 px** from the click. **(b) Popup placement bug:** the pill was rendered INSIDE `.ct` whose
  `backdrop-filter` creates a CONTAINING BLOCK — `position:fixed` descendants become panel-relative (that's why it sat on the ROTATE/ZOOM
  encoders). TRAP recorded. Now: `camera.tempPinScreen` mirror (%6 frames, projected marker, hidden off-screen/behind) → `.ct-pinpop`
  floats just above the pin (10 px house corners, instrument-card chrome — no more rounded pill); "EXIT LOOK · ESC" renders INSIDE the
  controls card above the sliders (`.ct-exitlook`). **(c) Temp-FPV control retargeting:** ROTATE encoder turns the look itself
  (fpvYaw; verified 42° for a 1.5 s stick) and ZOOM elevates the viewpoint STRICTLY vertically (fpvEyeM, proportional speed with an
  8 m floor base — pure exponential from a 1.7 m eye barely got airborne; clamp eye 1.7–`FPV.tempEyeMaxM 400`; verified climb with
  **0.00 m horizontal drift**); photo FPV stays locked at the apex. Files: `globe/StylizedTiles.ts` · `store/camera.ts`
  (tempPinScreen) · `panels/CameraTiltPanel.tsx` · `styles/camera-tilt.css` · `globe/tuning.ts` (FPV.tempEyeMaxM). Shots
  `verify-shots/phase55-09/10`. Memory: `mem:project/wip-2026-07-11-phase5.5-s2` §Follow-ups.
- **2026-07-11 — Phase 5.5 S2 follow-ups SHIPPED (owner, same day): photo-HEADING encoder · FPV building ghosting · pin pivot lock · temp virtual pin + look-around FPV (browser-VERIFIED via Playwright MCP; 223 vitest · astro check 0 · wix build green).**
  **(1) Photo HEADING = encoder** (PhotoDetailPanel): the 0–360° positional slider became a spring-centred rate control (`ui/Encoder.tsx`
  gained optional `badge`/`onReset`); a rAF loop nudges `params.headingDeg` per frame while deflected (same CONTROLS.headingRateMax/expo);
  double-click still resets to EXIF (verified 214→239 on a 1.2 s hold→214 on reset). **(2) FPV building ghosting**: `buildings.setGhost()`
  (two shared-material writes — per-tile obstruction testing would break the one-material invariant) fades fill to `FPV.buildingGhostOpacity
  0.2` + edges 0.12 + depthWrite off while ANY FPV is active; restored on exit (verified 0.2 in / 1.0 out). **(3) Pin pivot lock**: while a
  photo is placed OR a temp pin is set, the heading/zoom glides + encoder rates pivot on the PIN (the orchestrator overrides the view-focus
  frame; `hasFocus` keeps the dolly path). Verified: camera–apex distance 481 m → 481 m through a rotation burst. Empty-map click / Escape
  deselects a VIEWED saved pin (`clear()`); an own unsaved upload is never discarded by a stray click (deliberate deviation — losing an
  untweaked upload to a misclick is hostile). **(4) Temp virtual pin** (`camera.tempPin`/`tempFpv` seams + `TEMPPIN` tuning + accent marker):
  double-click the ground drops it (deselecting any viewed pin first — the gesture means "focus here"; ignored while placing/editing an own
  upload), it becomes the rotate/zoom pivot, single click elsewhere or Escape clears it; a centre pill (CameraTiltPanel) offers "◎ LOOK FROM
  HERE" → FPV generalised to two anchors (`fpvKind 'photo'|'temp'`): temp FPV stands at ground+eye 1.7 m (verified 184.2 = 182.5 ground +
  1.7) at `FPV.tempFovDeg 55`, basis captured at entry (camera azimuth), exits fly to the standard arrival pose around the pin. Escape
  unwinds one level: photo FPV → temp FPV → temp pin → viewed pin. Note: the dblclick drag-guard compares against the last pointerdown —
  synthetic dblclicks without a preceding down are rejected (test scripts must send down/up pairs first). Files: `ui/Encoder.tsx` ·
  `panels/PhotoDetailPanel.tsx` · `panels/CameraTiltPanel.tsx` · `scene/buildings.ts` · `globe/StylizedTiles.ts` · `globe/tuning.ts`
  (FPV ghost/tempFov + TEMPPIN) · `store/camera.ts` · `styles/camera-tilt.css` · tests → 223. Shot `verify-shots/phase55-08`.
  Memory: `mem:project/wip-2026-07-11-phase5.5-s2` §Follow-ups.
- **2026-07-11 — Phase 5.5 S2 SHIPPED: flight fixes + street-level camera + shared arrival pose + FPV photographer mode + compass/2D-3D + encoder controls (browser-VERIFIED via Playwright MCP on wix dev; 221 vitest · astro check 0 · wix build green).**
  **Flight** (`globe/flight.ts` rework): terrain path floor — `pathAltitude()` clamps the ellipsoid-only blend from below by
  max(terrain at both endpoints)+`FLIGHT.floorClearM 250`, ramped over `floorRampFrac 0.2` so endpoints stay EXACT; orientation now
  follows the path tangent + radial up through long flights (`pathFrameWeight` in 0.3/out 0.25 of eased progress; `pathFollowWeight`
  gates by ground distance 100→600 km so short hops keep the plain q0→q1 slerp), killing the rotated-start spin (measured: smooth
  single-hump rotation profile decaying to ~1°/s at landing; orientInFrac 0.15→0.3 browser-tuned, 334→257°/s peak). **Shared arrival
  pose** `arrivalPose()` (ONE derivation for pin flights, FPV exits AND search fly-tos): camera altAboveGround over the RENDERED ground,
  horizontal back-off drop·tan(tilt); pins land `FLIGHT.arrivalAltAboveGroundM 200`/`arrivalTiltDeg 80` (verified 203.2 m / 79.9° at the
  Dnipro fixture; backFactor/liftFactor + orphaned PINS.fly* tunables REMOVED); search keeps S1's 52° but is now terrain-aware (La
  Paz-class plateau arrivals fixed). **TRAP found live: `ground.heightAt` can return NEGATIVE garbage on unloaded/coarse quantized-mesh
  tiles — every consumer must clamp [0, 9000] (clamp-only-upward)**; unclamped, the Dnipro arrival sank to its lookAt safety floor.
  **Street camera:** `CONTROLS.zoomMinAltM 120→2`, `cameraRadius 8→2.5`; **TRAP: a descending zoom glide targets ELLIPSOID altitude and
  outruns tile loading — once under the surface the ENTIRE ground tileset unloads (0 meshes, verified) and neither adjustHeight
  (down-ray from the camera) nor live heightAt can recover.** Fix = sticky `lastGroundM` sampled every frame at the VIEW FOCUS (tiles
  only exist inside the frustum — the camera-footprint sample stayed null through a whole dive, also verified) + a street-floor guard
  clamping the camera to lastGround+2 m below 50 km + glide stall-release (`zoomStallFrames 6`). Verified: floor dive rests 2.5 m above
  the Kyiv street, 144 tile meshes alive, glide releases. **FPV** (`upload.viewMode 'orbit'|'fpv'` + orchestrator controller + `FPV`
  tuning group): entry flight to the frustum apex, camera FOV eases to the PHOTO's vFov (verified 50.23°, plane corners at NDC y=±1.0 —
  the photographer's exact frame; exit restores POSE 38°); GlobeControls disabled + `adjustHeight` OFF (cameraRadius would push off the
  apex) while `controls.adjustCamera()` is called manually each FPV frame (**GlobeControls.update() skips the near/far fit entirely when
  disabled** — frozen planes would black-screen at street level); drag = grab-world look (yaw/pitch on the frustum basis, ±80° elevation
  clamp, sensitivity scales with FOV), wheel = FOV zoom 8–80°, Escape/panel button exits → flight back to the arrival pose; pin-picks,
  placing clicks, glides and rates all gated during FPV. Verified: apex distance 0.00 m, drag → yaw 31.7°/pitch 12.7°, wheel 50.2→24.7°,
  Escape → orbit/38°/controls re-enabled. **Compass + 2D/3D** (CameraTiltPanel rework): needle = −headingDeg live mirror, click →
  `setTargetHeading(0)` fluid north glide (verified 214→359.9°, tilt preserved); 2D/3D = `setTargetTilt(0 ↔ toggle3dTiltDeg 55)`
  (verified 0.1°/55.0°, labels flip). **Encoders** (`ui/Encoder.tsx` replaces the absolute ROTATE/ZOOM sliders): spring-centred RATE
  controls → `camera.headingRateDegPerS`/`zoomRatePerS` seams (null on release; `clearAllTargets` clears them — grabbing the globe wins),
  applied per-frame through the SAME rotation/dolly paths with a `rateEaseTauMs 140` low-pass (ease-in + coast-out), expo
  `rateExpoGamma 2.2` (verified: 80 % deflection = 27.5°/s = 45·0.8^2.2 exactly; zoom −1.0/s → ×3.4 altitude in 1.2 s); readouts stay
  live mirrors; TILT stays absolute; absolute target seams kept for compass/2D-3D/FPV. Items 13+14 landed in-session — **no S2b needed**.
  Files: `globe/flight.ts` · `globe/StylizedTiles.ts` · `globe/tuning.ts` (FLIGHT/CONTROLS/FPV) · `store/camera.ts` · `store/upload.ts` ·
  `panels/CameraTiltPanel.tsx` · `panels/PhotoDetailPanel.tsx` · `ui/Encoder.tsx` · `styles/camera-tilt|photo-detail.css` · tests
  `test/components/globe/flight.test.ts` (new) + store tests → 221 total. Shots `verify-shots/phase55-03..07`. Mechanics:
  `mem:project/wip-2026-07-11-phase5.5-s2`. NOT released to live. Next: **S3 pin lifecycle** (NEXT_SESSION_PROMPT).
- **2026-07-11 — Phase 5.5 amended (owner): CARTO dark_nolabels APPROVED for S7a + five items added (11–15), folded into existing sessions — no new session count.**
  Owner accepted the CARTO license risk ("absolutely ok" — Esri-class POC posture, © OSM © CARTO attribution; Stadia/MapTiler stay as
  documented fallbacks) → S7a source LOCKED. New items: **11** Explore = ambient pin journey (900 km alt / 50° tilt, slow nearest-neighbour
  great-circle cruise through public pins, interaction exits, reduced-motion respected; EXPLORE tuning group) → S4; **12** more salient
  (still subtle) upload CTA → S3; **13** compass (headingDeg needle, click→north) + 2D/3D toggle (targetTilt 0↔55, existing glide seams)
  → S2; **14** encoder-style ROTATE/ZOOM — spring-centred RATE controls (deflection = speed, expo curve, release springs back; new
  headingRateDegPerS/zoomRatePerS store seams through the SAME rotation/dolly code paths; heading infinite, zoom hard-clamped; absolute
  target seams kept for compass/2D-3D/FPV) → S2 (S2b if heavy — flight fixes keep priority); **15** darker night sky (lower night floors +
  root-cause the carried navy-floor mystery) → S5. Docs updated: PHASE_5_5_UX_BATCH.md (items 11–15 sections + decision log),
  IMPLEMENTATION_PLAN §5.5 bullets, NEXT_SESSION_PROMPT S2 steps 6–7. Design-only (no code).
- **2026-07-11 — Phase 5.5 S1 SHIPPED: location finder (free geocoding → cinematic fly-to) + day ◀ ▶ steppers (browser-VERIFIED via Playwright MCP on wix dev).**
  **LocationFinder** (`panels/LocationFinder.tsx` + `styles/location-finder.css`, top-centre instrument card): Photon autocomplete
  (320 ms debounce, ≥3 chars, camera-focus bias — verified live: "dnipro" ranks Dnipro city #1 over Chernihiv/Kyiv namesakes) with
  Nominatim on explicit Enter (its policy forbids autocomplete; zips/global ranking better — verified "10001 new york usa" → Manhattan
  centroid); both keyless, session-cached (Nominatim policy REQUIRES caching), "© OpenStreetMap contributors" in the dropdown; provider
  adapter `lib/geo/geocode.ts` (pure parsers exported; LocationIQ = documented keyed fallback). **Fly-to seam:** `store/camera.ts` gains
  one-shot `flyRequest` + `requestFly`/`_consumeFlyRequest` + `focusLatDeg/focusLonDeg` mirrors (synced in the existing %12-frame block);
  orchestrator consumes → arrival pose along the CURRENT approach azimuth (no corkscrew) at `arrivalAltM(extent)` (extent-sized:
  Dnipro bbox → 40.1 km verified; floor 3 km because flight is TERRAIN-BLIND until S2; cap 1200 km) with SEARCH.arrivalTiltDeg 52°
  (verified 51.5° on arrival) → existing `flight.start`. New `tuning.SEARCH` group. **Day steppers:** ◀ ▶ flanking the TimeScrubber date
  input — `setTime(sceneTimeMs() ± 86 400 000)` + re-anchor (verified: LIVE Jul-11 → PINNED Jul-12 same time-of-day). Transatlantic
  Dnipro→NYC flight clean; consoles carry only known-benign errors (frog telemetry blocked, visitor members/my 403). Files: geocode.ts,
  LocationFinder.tsx, location-finder.css, camera.ts, StylizedTiles.ts (fly consume + focus mirror), tuning.ts (SEARCH), TimeScrubber.tsx,
  time-scrubber.css, index.astro, ARCHITECTURE §7. **206 vitest (+16: geocode parsers/arrivalAltM/fly seam) · astro check 0 · wix build
  green**; shots verify-shots/phase55-01 (Dnipro 40 km/52°) + phase55-02 (Manhattan 11.1 km). Next: S2 flight/camera core (NEXT_SESSION_PROMPT).
- **2026-07-11 — Phase 5.5 DESIGNED: pre-marketplace UX/quality batch (10 owner items → 7 ordered sessions; blocks Phase 6).**
  4-track parallel research (codebase map w/ file:line · geocoding policies · basemap/buildings ToS · astronomy datasets) consolidated into
  `.claude/claude-docs/PHASE_5_5_UX_BATCH.md` (canonical) + IMPLEMENTATION_PLAN §Phase 5.5. Locked: geocoding = Photon autocomplete +
  Nominatim-on-Enter (keyless, CORS-verified; Nominatim bans autocomplete; OSM attribution); flight bugs root-caused in code (terrain-blind
  altitude blend flight.ts:124 + decoupled orientation slerp :126); felt 100 m floor = CONTROLS.zoomMinAltM 120 (slider clamp; lib floors are
  10/8 m); moonlight = Krisciunas&Schaefer-1991 phase curve on the EXISTING rig + rig-source switch for full-moon shadows (no 2nd shadow map;
  moon:sun ≈ 1:400,000 — relative scaling only); night lights = Black Marble 2016 gray 13.5k (URL HEAD-verified) shipped as 8k single-channel;
  asterisms = d3-celestial asterisms.json (BSD-3 coordinate polylines, no HIP mapping); labels = Natural Earth 50m places+boundaries (public
  domain); Ukraine buildings: Mapbox REJECTED (Product Terms 2026-06-17 — custom renderer per-request billed, no caching/deriving §1.9/§1.6),
  trial = Re:Earth hosted Overture 3D Tiles (endpoint 200-verified, in-renderer UNVERIFIED); dark ground = CARTO dark_nolabels PENDING owner
  license-risk call (enterprise-only LICENSE.md vs keyless de-facto; Esri-class POC risk; Stadia/MapTiler fallbacks). Schema deltas queued for
  S3: authorName on PublicPins + PATCH/DELETE /api/photos. Order: S1 location finder+day buttons · S2 flight/FPV core · S3 pin lifecycle+
  placement UX · S4 pin visuals · S5 night-sky physics · S6 FPV arcs+asterisms · S7 ground rework. Verification: design-only (no code yet).
  New `upload.openSavedPin(SavedPinView)`: synthesizes the EXIF baseline from a stored record (stored hFov reproduced EXACTLY via
  focal35 = 18/tan(hFov/2) through the derivedFov shortcut) and lands the store atomically in "placed" — the whole existing pipeline
  (PhotoFrustum rebuild + onPlaced flight + PhotoDetailPanel) fires from the one transition. `viewingPinId` marks the re-opened state:
  panel HIDES the SAVE section (re-saving would duplicate); loadFile/ingest/clear reset it. **PublicPins gained camera-POSE fields**
  (altitudeM/heading/pitch/roll/focal/hFov/textureWH/make/model/lens — orientation & optics, NOT location; C6 governs coordinates):
  publicPinRecord + provision script + live create-field ×11 + back-fill of all 3 existing pins from their photoRef (first back-fill
  attempt patched dataItems[0] — the WRONG pin, the owner's live PXL save; fixed by keying on photoRef). GET /api/photos rows carry
  pose too (owner re-opens at EXACT coords; globe pins open at REDUCED coords). My-pins rows are now buttons → openSavedPin + close.
  **SUPERSEDES the "transient post-flight pick miss" story: the real bug was a STALE InstancedMesh.boundingSphere** — GlobeControls
  raycasts the scene before pins load (this is why decorations null their raycast), three caches the EMPTY sphere from count=0, and
  every later pick fails its early-out forever. Fix: `mesh.boundingSphere = null` whenever instances change (Pins.setPins/update).
  A small genuine arrival-window miss remains (camera creeps ~2 s after a flight) — cosmetic. Verified: globe click on the owner's
  live PXL pin → camera view heading 46°/31 mm/pitch 16.5° with dusk photo texture (wixstatic CORS fine, no errors); My-pins click →
  gps-heading at exact coords heading 214°. Files: store/upload.ts (SavedPinView/openSavedPin/viewingPinId), store/pins.ts +
  globe/Pins.ts (pose fields + sphere fix), StylizedTiles (click → openSavedPin; flyToPin removed), lib/wix/pinRecords.ts,
  api/photos.ts, MyPins.tsx (+CSS), PhotoDetailPanel.tsx, provision script. **193 vitest (+1 net) · astro check 0 · wix build green**;
  shots verify-shots/phase5-10/11. Live-site note: the owner saved a real PXL RAW twice on the released URL (duplicate pin left as-is).

---

_Older sessions (2026-07-09 → 2026-07-10, verbatim, newest-first) live in
[`DECISIONS_ARCHIVE.md`](./DECISIONS_ARCHIVE.md) — moved byte-identical during the 2026-07-11 compaction._
