# wip 2026-09-10e — THE RENDERING INTERLUDE: four desktop quirks (T131–T134), five records

**Mode:** implement (fix), Deep — `/frame` under investigate-design-v3; four parallel research agents (moon, horizon, moonlight, tile stream), every claim cited; the house headless :9333 + scratchpad probes (`probe-pose/-stream/-stall/-who/-reset/-cpu/-split.mjs`), screenshots `verify-shots/interlude-*`. Owner batch 2026-09-10b: desktop first, mobile must not regress. DECISIONS 2026-09-10e is the full record; backlog T131–T135.

## What was wrong, what was done (one line each — DECISIONS has the numbers)
- **T131 moon in front of Fuji** — the impostor ANCHOR at 0.5·far (far pinned ~180 km below 2 550 m by GlobeControls `MIN_ELEVATION`) put the disc at 90 km, in front of a 107 km mountain; the analytic horizon fade is ellipsoid-only. FIX: `gl_Position.z = gl_Position.w` in both sky vertex shaders (`scene/sky.ts`). Fence: `fences.test.ts` §2026-09-10b.
- **T132 horizon white band** — the sky dome (camera-anchored, ADDITIVE, depth-tested, radius 0.45·far = 81 km) added the whole horizon-sky colour onto every terrain pixel beyond 81 km → bloom → white. FIX: `uDome` + the same far-plane depth pin in sky-dome mode (`scene/atmosphere.ts`); plus the far-plane fog in `ftwAerial` (`scene/glsl.ts`, `uFtwFarM` pushed by the ground only; `ULTRA.farFogStartFrac/EndFrac`) so the horizon-fitted far plane's clip lands on sky-coloured pixels.
- **T133 milky moonlight** — terrain is UNLIT; under the LOOK the night floor was 0.4×skyLevel(0.03)=0.012, the daylight photo de-grade (`photo3dK`) ran all night, the flat moon fill (0.7) outweighed every albedo term, ×1.46 exposure. FIX (imageryGround.ts + tuning): `night` hoisted, `photo3d × (1 − night)`; `GROUND.nightFloorSkyMin 0.15`; `moonFillK 0.05` leaning 0.6 on the surface normal (`moonFillNormalK`); `moonSheenK 0.45` (ground-only sheen gain); `ambientNightK 0.012`; `SHADOWS.moonGroundOpacity 0.75`. Twin: `test/components/globe/moonlightTerrain.test.ts` (facing rock 15–40 % of day, aspect 1.4/1.7/1.8×, snow/rock > 1.6). Taste levers: those five.
- **T134 tile-stream stalls** — four measured mechanisms: (1) overlay imagery preloads at FIFO priority below every terrain download while parse slots wait on them (38 s composites flat) → NEW `lib/globe/overlayFetchPriority.ts` (a live priority getter per image fetch following its tile; `GROUND.overlayFetchPriority`); (2) cache-full discard without needs-update + no re-traversal at rest → `GROUND.fullCacheKickMs 1500`, ULTRA `groundLruBytesMB 1200`; (3) 5 network-bound parse slots = 6 tiles/s cold → `LOADING.groundDesktopCaps {32,12}` via `quality.groundQueueCapsFor` (desktop `high` ground only); (4) the VIRTUAL SPLIT RUNAWAY (cost doubling per level, 0.7 → 95 s gaps; 50 s of GeometryClipper in a 59 s profile) → NEW `lib/globe/virtualSplitGuard.ts` (growth above 512 tris / slow ancestor > 80 ms / depth > 6 refused; a floorless guard cost Dnipro z19 → z17 — the floor keeps it); (5) the reveal readiness gate held 0.85 under a continuous stream → `GROUND.revealMaxHoldMs 3000`. DBG rows `terrain.stream.*`, `terrain.split.*`.

## The sweep's lesson (FIFO within bands)
The first cut ordered the imagery bands by traversal ERROR; the sweep showed the regular rig sitting coarse at the 8 s cap on a cold cache (finest tiles first → parents cannot hide until all four children land). Fixed: FIFO within a band (the tag sequence = traversal order); the SLOT band alone removes the stall. Cold A/B fpv-south: fix 29 visible / 130 cached at 16 s vs baseline 17 / 106; warm 296 at 6 s. A sweep's absolute visible counts at the 8 s cap are CACHE STATE (fresh mkdtemp profile = cold; the disk cache thrashes across a long probe session) — the gate is calls/tris at convergence (identical on descent + cityscape).

## Traps paid for
- A backtick inside a GLSL template-literal comment kills the whole module (esbuild "Expected )") — the page boots the placeholder globe with "Failed to fetch dynamically imported module".
- `Network.clearBrowserCache` before an FPV boot in high terrain seats the eye at the ellipsoid and it never re-seats (T135a) — cold-cache probes must expect it.
- Editing `src/` during a probe run HMR-reloads the page mid-run (the pin lost, the terrain not re-seated) — the documented trap, hit once.
- The library's `errorPriorityCallback` reads `a.priority || 0`: tiles compare as 0, so any POSITIVE image priority beats terrain downloads and any negative one waits behind them — the whole ordering lever is that one line.
- `expandVirtualChildren` runs synchronously inside `processTileModel`; the plugin's `resetVirtualChildren` (on overlay add/delete/order) re-clips EVERY resident split tile.

## Not measured this session
The T77 desktop hitch table with 12 ground parse slots (a 12-tile landing frame is possible), the phones (order/guards only there), the sweep's per-pose pixel diffs are expected on far horizons (T132) and night poses (T133).

## Owner rulings (2026-09-10f)
- **The ground cache ladder SETTLED:** ULTRA **1400 MB** (`ultraDesktop.groundLruBytesMB`; ≈ 1,054 after the mip re-billing, floor 791), the REGULAR desktop **600 MB** (`QUALITY.desktopGroundLruBytesMB` through NEW `lib/globe/quality.groundLruCapForDesktop` — `high` off a coarse pointer with the chip off, the GROUND only; buildings/enriched keep the tier rule; the `high` tier TABLE stays 400 so the byte-identical-tier fence holds), phones untouched (the `/m` pose reads 112/84 as before). Browser-read: regular `…/600 (min 450)`, ULTRA `…/1054 (min 791)`. 4 tests pin the ladder.
- The night taste pass stays open (the five levers).
- **Next session = the owner's FULL AUDIT with ANOTHER AI model** (code base, rendering pipeline, all recent changes; technical/architectural/UX/business/emergent defects). Machinery: `/frame` Audit mode (`references/audit-mode.md`, read-only on `src/`, report under `audits/`), the backlog rows are what an audit VERIFIES.
