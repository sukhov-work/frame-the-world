# 2026-07-11 — B19: StylizedTiles.update() split into 36 named step-fns — DONE (all tiers verified)

**Status: DONE. The last pre-S7 refactor item. astro check 0 · 325 vitest · wix build green · browser
Flow-0 CLEAN (0 [globe] errors) · night-shadow golden gate PASS. Provably behavior-identical. Next = S7.**

## What shipped
`src/components/globe/StylizedTiles.ts` only. The ~950-line per-frame `update()` `try{}` body is now a
flat, ordered dispatch of **36 named step-closures** (`stepFrameTiming … stepDayArcs`) defined at
factory-closure scope just before `return {`; the `try{}` just calls them 1..36 in the identical order,
inside the ONE existing throttled try/catch. Readability win: `update()` reads as a 36-line table of
contents. File 1704 → 1883 lines (pure relocation + wrapping).

### Shape (the low-risk pattern — reuse it)
- **Per-frame hub** = frame-locals hoisted to factory-scope bare `let`s with BYTE-IDENTICAL names (so every
  READ stayed unchanged): `now,dtMs,zoomStep,alt,kRate,tMs` (=0), `rateAllowed,focusLocked,hasFocus,
  moonShadows` (=false), `focusHit` (`ReturnType<typeof rayEllipsoidIntersect>`), `upNow,camNow,camStore`
  (=…getState()). `zc` became a factory `const` (same cast every frame). The 14 producers changed
  `const/let X = …` → `X = …` (value unchanged). This is the B19_HANDOFF "FrameContext" realized as bare
  factory lets (its optional phase-4 ctx-object wrapping was skipped — lower risk, identical behavior).
- Steps are closures (defined ONCE, no per-frame alloc) reading/writing the hub + all persistent closure
  state directly by name. `stepArrivalReframing` (the pin-reframe bugfix, [[bugs/pin-arrival-reframe]]) is
  step 29, right after `stepFrustumResnapAndTick` (28) — added to the handoff's 35-step map as the drift note said.

### Execution
Delegated the mechanical extraction to one constrained sub-agent (single file, gate loop, "never reorder,
names unchanged, one try/catch, ++frameCount stays put, preserve every matrix flush + getState re-read +
fresh performance.now()"). Then I VERIFIED independently (below) — the verification, not the typing, is
where the safety came from (order regressions typecheck clean).

## Verification (the reusable proof technique — order regressions are invisible to astro check/tests)
1. **Provable line-equivalence (strongest).** Snapshot the pre-split try-body (`sed -n 704,1656p`); after the
   split, extract each step body by OPENER-delimited slicing (`const stepX = () => {` … next opener; drop
   the trailing `};`; strip comments/blanks) and concatenate in DEF order; normalize the OLD body for the
   14 const→assign conversions + the zc hoist → **new step-bodies == old body, line-for-line (771==771)**,
   and **call order == def order**. A pure, order-preserving partition. (Naive brace-counting FAILS on
   template literals / `${}` / inner `return {` object literals — use opener-delimiting + match the FACTORY
   `return {` by its exact 2-space indent.)
2. **Whole-file code-line MULTISET diff** (order-independent, no brace parsing): the only adds are 36
   openers/closers/calls + 12 hub lets + 14 producer-assigns; the only removes are the 14 producer consts;
   **0 unexplained added, 0 unexplained removed** — no statement lost/added/altered.
3. **Trap-site CODE counts unchanged** vs backup: `updateMatrixWorld()`=8, `updateProjectionMatrix()`=2,
   `++frameCount`=1 (at its site), `controls.adjustCamera(camera)`=1, `performance.now()`=8. (getState 30→33
   = the 3 one-time hub inits at factory scope, harmless.) The 9-vs-8 / 3-vs-2 raw counts were header-comment prose.
4. **Local:** astro check 0 · 325 vitest.
5. **Browser (scripted CDP, Node ≥22):** `scripts/verify-pin-reframe.mjs` (added Flow-0 console capture +
   `Runtime.exceptionThrown`): HIGH 697 km select → centered + 1 reframe + **FLOW-0 CONSOLE CLEAN (0 [globe]/
   exception errors)** over a long multi-step drive (fly-to·flight·reframe·pins·ephemeris) + PARAM-GATE no-chase.
   `scripts/verify-s5-night.mjs` GOLDEN GATE → PASS (quarter/full 10.3%, city full-moon key `bfd0e8`/
   castShadow true/moonLight 0, night sky (2,5.9,10.8) — identical to the S5 baseline).

## Follow-up (cosmetic, optional)
The extracted step BODIES kept the original 8-space indentation inside the 2-space-scoped closures (chosen
to guarantee byte-identical bodies for the equivalence proof) → they read one level over-indented. Repo has
**no prettier config + no lint script**, so a formatter pass would reformat the whole file (noise on untouched
code) — LEFT AS-IS. S7 (which edits this file's terminator GLSL + camera math anyway) can dedent the step
bodies by 4 spaces if wanted. Also B20–B25 still fold into S7.

Backup of the pre-split file: `/tmp/StylizedTiles.b19bak.ts` (session-local; gone on reboot).
Handoff: `.claude/claude-docs/archive/B19_HANDOFF.md` (now marked DONE). Related: [[bugs/pin-arrival-reframe]] ·
[[patterns/globe-rendering]] · [[project/wip-2026-07-11-pre-s7-refactor-s2]] · DECISIONS 2026-07-11 B19 line.