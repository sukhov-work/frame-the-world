# wip 2026-09-06l (the brief called it 2026-09-07) — T77: E1 / E2 (both A-rest levers CLOSED), T80-h the fused bloom SHIPPED, T99 closed, the GPU-timer finding (T104)

Mode: implement, Deep (`/frame` under the investigate-design-v3 spine). Owner order: "proceed with
plan from NEXT_SESSION_PROMPT.md". DONE 2026-09-06 ≈19:35–20:45 local (UTC+3; the sweep stamps are UTC).

## Boot
master == origin/master (e5f2781), clean · VPN on (FI), ion 401 · budget 0/1 · 0/1 · 87 % free · the
owner's headed :9222 appeared mid-session (timed cells attached to it; never touched otherwise).
Pre sweep `pre-2026-09-07`: 13/14 byte-identical; `legacy-m` 490 px (T103).

## E1 / E2 — `verify-temporal-stability --shimmer --step 200 --arms …` (MEASUREMENTS §16.1–16.2)
- E1 lever 2 (`ladderOff` = cascades false): arm took, mask unchanged; churn city scrub +24 %, scrub4x
  +2 %, Everest −1 / −5 % → **CLOSED** (rule: ≥ 50 % down builds; ±20 % closes). No ShaderChunk work.
- E2 A1 (`--rig 0,0`, 0.5 / 0.3 texel): churn UP everywhere (city scrub 0.0197 → 0.0417 / 0.0291) and
  the shadow MASK collapses (city 0.308 → 0.093 / 0.206; Everest 0.721 → 0.380 / 0.578) → **A1 stays 0,
  CLOSED**. With E3 (k2, lever 12) slice A is complete.
- The harness's 12 "structural failures" at step 200 are the same on every arm (resamples 100/599; the
  streaming ULTRA control churn max 0.003–0.006) — they do not void an A/B.

## T80-h — the fused bloom (MEASUREMENTS §16.3; patch `~/.claude/ftw-wt-patches-2026-09-06j/t80h-final.patch`)
- The brief's premise ("~8 ms = five blur levels at full res") was FALSE: `UnrealBloomPass.js:102-103`
  the bright target is `round(w/2)×round(h/2)`; the whole 12-draw chain ≈ 2 ms by the `bloomCheap` delta.
  What was full-res after T80-g: the resolve COPY + the additive BLEND. The fused path removes both.
- Built: `ScaledBloomPass.deferBlend` (the library's 12 chain draws copied from
  `UnrealBloomPass.js:306-349`, the body pinned by sha-256 in `test/components/globe/fusedBloom.test.ts`;
  the 13th draw skipped; `bloomTexture` = `renderTargetsHorizontal[0].texture`) ·
  `scene/fusedOutput.ts` `FusedOutputPass extends OutputPass` (two materials: the base's own for the
  off-state, `OutputShader` + `tBloom` added BEFORE the tone map for the on-state; the base's define
  cache is nulled on a switch; both disposed) · `tuning.ts` `BloomPath` + `BLOOM.path: "fused"` ·
  `GlobeCanvas.tsx` `setBloomEnabled` = the ONE writer of `bloomPass.enabled` / `resolvePass.enabled` /
  `bloomPass.deferBlend` / `outputPass.setBloomTexture` (C2'), `__quality.bloomPath("msaa"|"resolved"|"fused")`
  reports `deferBlend` / `outputMaterial` / `outputBloomBound` live · `resolvedComposer.ts` C2' + C7 ·
  the `setClearColor` fence in `fences.test.ts` gained its ONE allowed file (the library-copy pair).
- Why a copied render body: a `visible=false` blend material still issues `renderer.render()` into the
  MSAA read buffer and three resolves it in the epilogue — the exact second resolve the lever removes.
- T99 closed in the same edit: no `composer.setPixelRatio()` at boot (the constructor already took the
  renderer's ratio; the canvas is 300×150 then, so the doubled-DPR alloc was 1200×600 — a few MB).
- Timed (owner's headed :9222, `high`, DPR 2, `--post-ab`, one boot): base rig `frame.gpu` msaa 22.4 →
  resolved 19.8 → **fused 18.3** (off 10.0); **dt p50 19.2 → 15.4 → 14.2** (off 12.1). ULTRA gpu 23.7 →
  21.4 → 19.3; dt 21.0 → 18.0 → 15.9. Bloom = 2.1 ms of frame time now.
- **T104 — `frame.gpu` over-counts pass-heavy chains:** `--bloom-mips 2` (six tiny passes dropped, 3.5 %
  of chain pixels) read gpu −6.6 ms, dt ±0 (15.2 vs 15.0, fps 66 vs 67). `EXT_disjoint_timer_query`
  on ANGLE/Metal brackets command-buffer queueing. Read the T77 GPU gates on dt p50: ≤ 15 is MET
  (14.2 base / 15.9 ULTRA); unmet on `frame.gpu`.
- Pixels (`probe-bloom-path --paths msaa,resolved,fused`, T94-frozen, 3200×1900, 2 runs): cityscape fused
  vs others 8,004 px (0.13 %) at Δ ≤ 2 / 48,133 px (0.79 %) at Δ1, **0 px > 3**; msaa vs resolved 1,378
  px Δ1. West-sunset 0 px on every pair AND on the positive control (mip ×0.5) → no visible bloom
  there, uninformative. Path-independent transients on random rungs: a recurring 32,828-px Δ1 step
  (an ease the freeze does not hold) and one 33k-px Δ143 tile landing (T102's shape).
- Gates on master: vitest **2,716/2,716 (173)** (the first run had ONE un-reproduced failure, not
  captured; two reruns green) · astro 0/0/9 · knip 0 · charter **84/85** (RC2 fade 0.0508 = T100) ·
  uxbatch4-s3 16/16 · shimmer base `fpv.u0` control p50/mean 0.0000, max 0.00075 (one pixel once;
  k2 0.00000) · post sweep `post-2026-09-06l` 11/14 self-check (legacy-fpv-eye Δ1 transient,
  legacy-everest tile landing, legacy-m T103); the `--compare` vs the pre golden is dominated by the
  PRE run's mid-reveal captures (quiet cap `8.1s!` at everest-orbit-52 / everest-fpv-sunset / zoom).

## Harness additions
- `verify-perf-baseline.mjs`: `bloomMsaa` / `bloomResolved` / `bloomFused` cells (each pinned + read
  back), `--bloom-mips N` pass-count cell (timing only).
- `probe-bloom-path.mjs`: `--paths`, T94 two-phase freeze (noise floor 0), positive-control rung
  (`bloomScale 0.5`), VERDICT lines, exit 1 on a non-zero frozen noise floor (PROBE BLIND).
- Traps learned: macOS has no `setsid` (use `nohup … &`); a `run_in_background` tool call is capped at
  10 min; `git stash -u` in a worktree stashes the `node_modules`/`bakes` symlinks.

## Owner rulings 2026-09-06m (same session, after the report)
"for OWNER CALLS - i am ok with all your reccomended options" → T80 (a) CLOSE as a frame-time lever
+ T104 gates on dt · T100 (b) band start ~+0.2°, width kept, ULTRA-only · T101 (a) per-cell
`deepPending` · T92 (a) fade the focal cone's FILL, keep the boundary rays (driver chosen from the
sheets: tilt vs altitude vs coverage) · T93 (a) limb tinted to the horizon haze. Recorded: DECISIONS
2026-09-06m · backlog rows · NEXT_SESSION_PROMPT §OWNER RULINGS (order + per-slice gates).

## Records
DECISIONS 2026-09-06l · MEASUREMENTS §16 · T77 plan pointer · backlog T77/T80/T99 amended, T103–T104
added · NEXT_SESSION_PROMPT rewritten (the five OWNER CALLS: T100 · T101 · T92 · T93 · T80 pass count) ·
`mem:core` · `.claude/.ship-title`. Worktree `../ftw-wt-t80g` left on `claude/t80h` (master's tip +
a disposable stash of the old t80g diff); the other five stale worktrees untouched (tail).
