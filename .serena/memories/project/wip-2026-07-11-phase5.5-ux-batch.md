# 2026-07-11 — Phase 5.5 pre-marketplace UX batch: DESIGNED (all 10 items) + S1 SHIPPED

**Status: DESIGN COMPLETE + S1 browser-VERIFIED. Next session = S2 (flight/camera core + FPV).**

## What happened
investigate-design-v3 Deep session: 4 parallel research tracks (codebase map w/ file:line ·
geocoding policies · basemap/buildings ToS · astronomy datasets) → consolidated + falsification-
gated → canonical design doc **`.claude/claude-docs/archive/PHASE_5_5_UX_BATCH.md`** (per-item current
state, approach, tunables, open questions, S1–S7 order) + IMPLEMENTATION_PLAN §Phase 5.5 +
NEXT_SESSION_PROMPT (S2 brief) + 2 DECISIONS lines. Then implemented S1.

## Locked decisions (evidence in the design doc + DECISIONS 2026-07-11)
- Geocoding = Photon autocomplete (camera-bias MANDATORY) + Nominatim-on-Enter (autocomplete
  policy-forbidden) behind `lib/geo/geocode.ts` adapter; OSM attribution; LocationIQ keyed fallback.
- Flight bugs root-caused: terrain-blind altitude blend (flight.ts:124) + decoupled orientation
  slerp (:126). Felt 100 m floor = CONTROLS.zoomMinAltM 120 (slider clamp; lib floors 10/8 m).
- Moonlight = Krisciunas&Schaefer 1991 phase curve (quarter ≈ 9% of full) on the EXISTING light;
  full-moon shadows = shadow-rig SOURCE SWITCH sun→moon at night (no 2nd rig; moon:sun 1:400k).
- Night lights = Black Marble 2016 gray 13.5k (eoimages…144897/BlackMarble_2016_3km_gray.jpg,
  HEAD-verified) shipped as 8192×4096 single-channel (RedFormat) default.
- Asterisms = d3-celestial asterisms.json (BSD-3, RA/Dec polylines — no HIP/HR mapping needed).
- Labels = Natural Earth 50m places (scalerank culling) + admin-0 boundary lines (public domain).
- Ukraine buildings: Mapbox REJECTED (Product Terms 2026-06-17: per-request billing for custom
  renderers, no caching §1.9, no deriving §1.6); trial = Re:Earth hosted Overture 3D Tiles
  (buildings.reearth.land/tileset.json, 200-verified, needs MeshoptDecoder, in-renderer UNVERIFIED).
- Dark ground = CARTO dark_nolabels raster — **owner APPROVED 2026-07-11** ("absolutely ok";
  Esri-class POC posture, © OSM © CARTO attribution; Stadia/MapTiler documented fallbacks).
- Owner adds (2026-07-11, items 11–15, folded into sessions — see design doc sections):
  11 Explore ambient pin journey (900 km/50°, slow NN cruise, interaction exits) → S4 ·
  12 salient-but-subtle upload CTA → S3 · 13 compass + 2D/3D tilt toggle (0↔55, existing glide
  seams) → S2 · 14 encoder-style ROTATE/ZOOM rate controls (headingRateDegPerS/zoomRatePerS seams,
  absolute targets KEPT for compass/2D-3D/FPV) → S2 (S2b if heavy) · 15 darker night sky + navy-
  floor root cause → S5.
- Order: S1 finder+day-steps ✅ · S2 flight/FPV + compass/2D-3D + encoder controls · S3 pin
  lifecycle+authorName+placement UX + upload CTA · S4 pin visuals + Explore journey · S5 night-sky
  physics + darker sky · S6 FPV arcs+asterisms (needs S2) · S7 ground rework (a dark mode /
  b labels / c building-grow + UA buildings).

## S1 shipped mechanics (for future maintenance)
- `lib/geo/geocode.ts`: pure `parsePhoton`/`parseNominatim`/`arrivalAltM` (16 tests) + abortable
  fetchers + session caches (Nominatim caching is a POLICY requirement). Photon extent
  [minLon,maxLat,maxLon,minLat]; Nominatim boundingbox [latMin,latMax,lonMin,lonMax]; both lon
  spans cos(lat)-corrected → spanDeg → arrivalAltM = clamp(span·111320·1.1, 3 km, 1200 km),
  no-extent default 4 km. Floor is load-bearing until S2's terrain-aware flight.
- `store/camera.ts`: one-shot `flyRequest {latDeg,lonDeg,altM}` + `focusLatDeg/focusLonDeg`
  mirrors (geocode bias; synced with the existing %12-frame focus block in StylizedTiles).
- StylizedTiles consume: arrival along CURRENT approach azimuth (horiz projection of camera−target
  on target horizon plane, north fallback) at tan(SEARCH.arrivalTiltDeg 52°)·alt back-off →
  flight.start; lastInteract = now pauses drift.
- LocationFinder panel: top-centre; ↑↓/Enter/Escape; Enter with dropdown closed (Escape first) →
  Nominatim path; pointerdown-outside closes; OSM credit row in dropdown.
- TimeScrubber `.ts-day` ◀ ▶: setTime(±86_400_000)+setAnchorMs — pins from LIVE, keeps time-of-day.
- Verified: Dnipro autocomplete-bias #1 → 40.1 km/51.5° arrival; zip 10001 via Enter → Manhattan
  11.1 km; day-step Jul-11→Jul-12 pinned. 206 vitest · astro check 0 · wix build green.
  Shots verify-shots/phase55-01/02. NOT released to live (release when a batch is worth it).

Related: `mem:core` · [[patterns/globe-rendering]] (fly-request wiring lives beside the glides) ·
DECISIONS 2026-07-11 ×2 · archive/PHASE_5_5_UX_BATCH.md (canonical).
