# 2026-07-11 — Phase 5.5 S3: pin lifecycle + placement UX + upload CTA — SHIPPED (wix-cloud-VERIFIED)

**Status: DONE. 243 vitest (baseline 223) · astro check 0 · wix build green · verified in wix dev
with a real member cookie (frame-p5-tester). Absorbed 2 owner adds (dblclick memo + UPLOAD HERE).
Next session = S4 (pin visual rework + Explore journey). NOT released to live.**

## Backend (api/photos.ts + lib/wix/pinRecords.ts)
- **PATCH /api/photos** {photoId, …SavePinBody}: owner gate = `elevate(items.get)("Photos", id)` →
  `ownerMemberId === member._id` (404 otherwise). Pure merge `applyPinUpdate(existing, body)`:
  null media/file fields (originalFileId/previewFileId/previewUrl/fileName/fileSizeBytes) KEEP the
  stored values — the client edit flow never re-uploads media (previewUrl survival live-verified).
  PublicPins reconciliation: isPublic → `publicPinRecord(effective, photoId, authorLabel(...))`
  update-in-place (spread + `_id: prevPinId`) or insert; !isPublic → `items.remove` + null link.
  Photos row written LAST with the reconciled publicPinId. C6 live-verified: location edit
  re-reduced exact 57.43621/49.67614 → published cell centre 57.43378/49.67468.
- **DELETE /api/photos?id=**: PublicPins via linked publicPinId, else defensive
  `.eq("photoRef", id)` query; Photos remove; media best-effort `auth.elevate(files.bulkDeleteFiles)(fileIds)`
  (@wix/media; catch → warn, records already gone). Response carries fresh quota count — **live: 2/10 → 1/10**.
- SDK signatures verified from node_modules d.ts: `items.get(coll, id)` / `items.remove(coll, id)`
  → `Promise<Item|null>`; `items.update(coll, itemWithId)`; `bulkDeleteFiles(fileIds: string[], opts?)`.
- POST/PATCH use `members.getCurrentMember({fieldsets:["FULL"]})` — PUBLIC fieldset omits loginEmail.
  TS TRAP: SDK types `_id` optional — helper returns `{...member, _id: member._id}` to narrow.
- `authorLabel(nickname, loginEmail)` in pinRecords (server-side twin of store/member memberLabel —
  deliberate duplication; the endpoint must not import a client store).

## authorName (schema + back-fill)
- `publicPinRecord(body, photoRef, authorName = null)` — third param; PublicPin store type +
  pinFromItem carry `authorName: string | null` (null on pre-S3 rows).
- **provision-collections.mjs now diffs fields on EXISTING collections** and adds missing ones via
  `POST /wix-data/v2/collections/create-field` (recipe: @wix/agent-skills wix-manage
  cms-schema-management.md). Run 2026-07-11: `+ PublicPins.authorName field added`.
- `scripts/backfill-author-names.mjs`: query PublicPins (REST items/query) → rows missing
  authorName → Photos by `filter: {_id: photoRef}` (NEVER positionally — dataItems[0] bug on
  record) → `GET /members/v1/members/{id}?fieldsets=FULL` (member cache) → PUT items/{rowId}
  full data + authorName. Run live: 5/5 filled ("Svitlana Berlova", "Yevhen Sukhov" ×3 nickname
  path, "frame-p5-tester" email-user-part path).

## Client stores
- `store/upload.ts`: `ownPhotoId` + `ownPinMeta {isPublic, precision}` (set by openSavedPin when
  the view carries `ownPhotoId` — MY PINS passes it; globe pin clicks don't) · `rePlace()`
  (placed+viewingPinId → placing, placement kept) · `cancelPlacing()` (viewed pin → placed;
  fresh upload → review overlay) · `pendingPlacement` + `uploadAt(lat,lon)` (UPLOAD HERE seed;
  ingest applies it ONLY when the file has no GPS — EXIF wins; applying clears the temp pin via
  static `useCameraStore` import (no cycle: camera.ts imports zustand only); closePanel/clear drop it).
- `store/save.ts`: phases + `updating|updated|deleting` · `title` (null = file-name default;
  NOT cleared by reset() — the panel effect keyed on `viewingPinId ?? fileName` owns seeding) ·
  `updatePin()` (buildSavePinBody with null media + title fallback `title ?? u.fileName`, PATCH,
  pins.refresh) · `deletePin()` (DELETE, pins.refresh, upload.clear, phase idle + fresh quota).
- `store/pins.ts`: `highlightId` + `highlight(id|null)`. save success arms it with the new
  publicPinId right after refresh().

## Globe
- `globe/Pins.ts` `setHighlight(id)`: pulse = scale × (1 + 0.45·fade·(0.5+0.5 sin(2π t/1100ms)))
  for PINS.highlightMs 8000, fade-out over the last 1.5 s; while pulsing the matrix rebuild runs
  every frame (`dirty = pulsing` keeps one restore pass after expiry); highlightIdx recomputed in
  setPins (the id usually arrives BEFORE the refreshed pins land — findIndex must rerun there).
- Orchestrator: pins-store subscription now split (pins list by reference, highlight always) —
  zustand subscribe fires on every store change, so the old `subscribe(s => setPins(s.pins))`
  would have re-placed 1000 pins per highlight change.
- **Placing live marker**: `placingMarker` mesh (PLACING tuning; raycast noop; depthWrite off);
  `pointermove` mirrors hover client coords; per-frame while `upload.phase === 'placing'` and
  every PLACING.repickEveryFrames 2 → `pickGround(hoverNdc)` → position + angular-constant scale;
  hidden past the limb / on phase exit (cursor subscription also hides it). Live-verified: marker
  visible under pointer at 300 m alt, re-place click landed exactly there.
- Post-save fly-out: PhotoDetailPanel effect (`savedBeat` = saved && !viewingPinId) → 1.6 s
  timeout → guard re-check (still placed + still saved) → `camera.requestFly({...placement,
  altM: PINS.savedFlyOutAltM 3800})` → `upload.clear()`. flyRequest survives clear; landed 3,899 m live.

## Panels / chrome
- PhotoDetailPanel: PLACED ✓ → TUNED (dirty) → SAVED stepper (new saves only) · PIN NAME input
  (value = save.title ?? (viewing ? fileName : titleFromFileName(fileName))) · own-pin actions
  UPDATE PIN / ⌖ RE-PLACE / DELETE (two-step "SURE? DELETE", armed state warn-toned; disarmed on
  identity change) · statusLine handles UPDATING…/DELETING…/UPDATED ✓ · edit seeding effect keyed
  on `viewingPinId ?? fileName` (setTitle(null) + isPublic/precision from ownPinMeta).
  PlacementHint copy switches to "CLICK THE GLOBE TO MOVE THIS PIN" when re-placing; cancel →
  cancelPlacing (UploadFlow Escape too).
- MyPins: rows = `.mp-row` flex (item + armed ✕ `.mp-del`); delete → deletePhotoRecord →
  local list filter + pins.refresh + upload.clear if the deleted pin was in view; openPin passes
  ownPhotoId/isPublic/publicPrecision.
- CameraTiltPanel: wrapped in `.ct-stack` (fixed slot moved OFF `.ct` onto the wrapper — no
  backdrop-filter on the wrapper, the containing-block trap again); `.ct-memo` above the card
  ("◎ DOUBLE-CLICK ANYWHERE TO DROP A PIN & LOOK FROM THERE") shown only while
  !tempPin && !tempFpv && uploadPhase === 'idle' (retires once the gesture is demonstrated);
  temp-pin popup = LOOK FROM HERE · sep · **↑ UPLOAD HERE** (uploadAt) · ✕, extracted as
  TempPinPopup rendered OUTSIDE the stack.
- UploadFlow: `AddPhotoPill` ("+ ADD PHOTO", fixed bottom 7.6rem centre — 6.6 kissed the
  scrubber) for anonymous OR zero-pin members (module-level `ownPinsProbe` = one /api/photos
  fetch per page load); review GPS Field shows a seeded placement with a **FROM PIN** badge
  (Field gained `badgeText` override). Nav Upload = `.nav-upload` accent chip + one-time
  `nav-upload-hello` glow (2.6 s, backwards fill; reduced-motion disables).

## Verification receipt
Baseline 223 → 243 vitest (22 files) · astro check 0 · wix build green. Wix-cloud (wix dev,
member cookie minted via the register-test-member recipe — redirectUri MUST be the exact
allowlisted `http://localhost:4321/api/auth/callback`, bare origin 400s): save with custom name →
quota 2/10 + authorName "frame-p5-tester" on the public row · PATCH: title/precision · location
re-reduction · media continuity · isPublic OFF (row removed) / ON (new row b0effd77…) · DELETE →
quota 1/10, list+globe clean. Browser: idle chrome shot (chip+pill+memo) · pinpop with UPLOAD
HERE · ARW seeded at dblclick point (FROM PIN badge) · placed panel stepper+name (anon gate) ·
fly-out 3,899 m + highlight pulse · own-pin edit panel · re-place live marker 1-px-class accuracy ·
armed delete · spectator FPV regression (kind temp, fov 55). Shots verify-shots/phase55-08..17.
UNVERIFIED: bulkDeleteFiles effect in Media Manager (best-effort) · quota-#11 after S3 (path unchanged).

Files: see DECISIONS 2026-07-11 S3 line. Related: [[patterns/members-pins]] (Phase-5 backend
mechanics this extends) · [[project/wip-2026-07-11-phase5.5-s2]] (temp pin/FPV seams S3 builds on) ·
[[project/wip-2026-07-11-phase5.5-ux-batch]] (design §Item5/10/12) · NEXT_SESSION_PROMPT.md (S4).
