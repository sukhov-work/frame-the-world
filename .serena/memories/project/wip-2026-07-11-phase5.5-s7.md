# 2026-07-11 — Phase 5.5 S7 (ground rework) — SHIPPED + same-day owner rework, all tiers green

**Status: DONE — the LAST Phase-5.5 session, including the owner rework (street names → vector,
grow-on-zoom removed). Final: astro check 0 · 339 vitest · wix build green · browser-VERIFIED via
Playwright MCP (shots `verify-shots/phase55-49..58`) · S5 night golden gate PASS (identical values).
Phase 5.5 COMPLETE → Phase 6 (marketplace) next.**

## (a) Dark drape (CARTO dark_nolabels, owner-approved)
- **THE mechanic:** ImageOverlayPlugin `overlay.opacity` is a **LIVE per-frame uniform** —
  `_updateLayers` assigns `params.layerInfo.value[i] = overlay` (the object itself) and the wrap
  shader multiplies by `layerInfo[i].opacity` at upload time. Crossfade = plain eased write on the
  overlay object. NO re-composite. Reuse for ANY future raster overlay fade.
- Overlay stack (imageryGround.ts): Esri → `cartoDark` ONLY (the dark_only_labels raster was tried
  and DROPPED — below). CARTO keyless + CORS *, real content probed to **z20**; above-max zooms
  return 200 with a blank 103-byte 1-bit PNG — probe with correct tile math.
- `DRAPE` tuning: fade band 7→5 km · easeTauMs 350 · gain 1.3 · flat dayShade 1.0 · nightFloor 0.45
  · shadowOpacity 0.9 / moonShadowOpacity 0.65 (per-mode shadow contrast).
- Grade GLSL: `uFtwDark` blends the Esri-specific colorimetry OUT (water detect + desat × (1−dark);
  shade/gain/cast lerp to glf-baked DRAPE twins — `glf3()` vec3 helper in scene/glsl.ts); golden +
  moonlight STAY (scene physics). Seams: `ground.update(alt, darkGround)` + `ground.darkBlend()`;
  `stepKeyLightAndShadow` lerps shadow opacity by the live dark fraction. `camera.groundMode
  'dark'|'satellite'` + SAT chip (`.ct-sat`). Verified: uFtwDark 1 @3 km / 0.514 @5,972 m / →0 on SAT.

## (b) Geo labels + boundaries (Natural Earth 50m) + VECTOR street names
- Bake: `scripts/build-ne-labels.mjs` → `public/data/ne-boundaries.bin` (Float32
  `[partCount, …counts, …(lat,lon)]`; 393 parts / 19,859 verts / 157 KB) + `ne-places.json`
  (1,251 places RANK-SORTED — the loader early-breaks). Contract unit-tested against the real assets.
- `scene/geoLabels.ts`: boundaries = ONE LineSegments (tokens.graticule, lift 9 km); city labels =
  module-owned DOM pool (`.geo-labels`), direct style writes @2 frames, rank gate lerped in LOG
  altitude, min-sep 76 px. **Window 100–900 km** (DoD "gone at LEO" beat the design's 2000 km; LEO
  pose 1,100 km + Explore 900 km stay clean). Culls: tangent-plane hemisphere · **view-space z<0
  (project() alone mirrors behind-camera points)** · NDC margin. Step 37.
- **Street names v2 = `scene/streetNames.ts` (owner rework — the raster labels were "really blurry",
  appeared too late, couldn't scale):** OpenFreeMap planet **MVT** — keyless, CORS *, OpenMapTiles
  schema, **maxzoom 14 (z15 returns EMPTY 200s — probe before assuming!)**; TileJSON resolved at
  attach (`STREETS.tileJsonUrl` — the dated tile-path segment `20260621_…` rotates with builds,
  NEVER hardcode); 3×3 z14 ring around the focus mirrors (camStore.focusLat/LonDeg). Parse:
  `@mapbox/vector-tile@3` + `pbf@5` — **pbf v5 exports `PbfReader` (named), no default export**.
  `transportation_name` line features → ONE label per street NAME (best classPriority then longest
  segment); anchor = longest line's middle vertex, next vertex gives the screen-space ROTATION
  (kept upright ±90° — CSS rotate, y-down); seated on RENDERED terrain via ground.heightAt +
  clampGroundM (ellipsoid-0 until tiles answer; re-seat keeps the B point's OWN lon/lat — early bug).
  Reveal `topAltM 2500 → fullAltM 2100` (owner's 2–2.5 km), stays to street; **type SCALES with
  zoom** `px = clamp(9·(2500/alt)^0.45, 9, 19)` — measured 9.4 px @2.3 km → 17.1 px @606 m.
  maxVisible 26 · minSep 60 px · cache 48 tiles (insertion-order LRU; failed tiles stay failed).
  Hidden in FPV (`enabled: !fpvActive`, verified 0 visible) + welcome (`.street-labels` hide rule).
  Step 38. Credit + `© OpenMapTiles`. Deps pbf + @mapbox/vector-tile (tiny, ship own d.ts).
  7 unit tests (presence band · font monotone+clamps · Dnipro z14 9786/5663 probe-anchored ·
  tile-local↔lonlat roundtrip).

## (c) Building grow-on-zoom — BUILT then REMOVED same day (owner: unreliable)
- The per-tile inner-Group vertical scale about a lazy min-bbox ground anchor WORKED in browser
  (flat @2.2 km / half @1.3 km / full @553 m) but the owner called it unreliable → fully removed:
  buildings.ts wrap/anchor/apply gone, `update()` restored (no alt param), growMatrix.ts + 5 tests
  deleted, BUILDINGS.grow* gone. Buildings render full-height whenever tiles load (pre-S7 behavior).
  If it ever returns: the removed mechanics are in DECISIONS (S7 line) + git history; the lazy-anchor
  trap (matrixWorld untrustworthy at load-model) and growMinK-singularity note live there.

## (d) Overture trial — NEGATIVE (flag ships OFF)
- Wiring kept: `TILESETS.overtureBuildings` → `new TilesRenderer(overtureTilesetUrl)` +
  `meshoptDecoder` in GLTFExtensionsPlugin. tileset.json parses (3D Tiles 1.1, benign warning flood);
  **every glb content tile `/v5-add/{z}/{x}/{y}.glb` → HTTP 500 over Dnipro**. Fallback Cesium OSM.

## Verify traps
- Welcome dismissal needs a REAL Playwright click on the canvas — synthetic PointerEvent dispatch
  does NOT dismiss; removing the CSS class ≠ dismissing (Explore still owns the camera and eats
  flyRequests — a fly to 1.2 km "arrives" at 900 km = EXPLORE.altM).
- Golden-gate harness: launch Chrome yourself (`--headless=new --remote-debugging-port=9333
  --user-data-dir=/tmp/…`), then `~/.nvm/versions/node/v24.10.0/bin/node scripts/verify-s5-night.mjs`.
- Dev-seam recipe: `__cameraStore.requestFly/setTargetZoom/setTempPin+setTempFpv` +
  `__timeStore.setTime` + `__globe.groundUniforms.uFtwDark` + `.geo-labels`/`.street-labels` census.

## Files (final)
tuning.ts (TILESETS+DRAPE+LABELS+STREETS) · scene/{imageryGround,buildings,geoLabels,streetNames,
glsl}.ts · StylizedTiles.ts (steps 5/24/26 + 37 GeoLabels + 38 StreetNames) · store/camera.ts
(groundMode) · CameraTiltPanel.tsx · camera-tilt.css · welcome.css · index.astro ·
scripts/build-ne-labels.mjs · test/components/globe/{geoLabels,streetNames}.test.ts ·
public/data/{ne-boundaries.bin,ne-places.json} · package.json (+pbf, +@mapbox/vector-tile).

## Carried / follow-ups
- Street-label polish candidates: per-label occlusion vs buildings (currently draws over), density
  tier below ~300 m (z14 has all names already — a maxVisible bump may suffice).
- One-off dark streak at 1.3 km top-of-frame (phase55-53, unreproduced) — watch.
- B16/B17 owner-declined; B20–B25 shader/precision cleanups still open.
- S1–S7 NOT released to the live URL (pre-release gate: /api/ping canary).

Related: [[project/wip-2026-07-11-phase5.5-ux-batch]] · [[patterns/sky-bodies-terrain]] ·
[[patterns/globe-rendering]] · DECISIONS 2026-07-11 S7 + S7-rework lines.
