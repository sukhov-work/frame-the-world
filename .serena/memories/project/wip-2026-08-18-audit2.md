# AUDIT #2 — whole-project expansion-readiness — SHIPPED 2026-08-18k

**Report: `.claude/claude-docs/audits/audit-full-2026-08-18.md`** (the full findings table,
verified-clean probes, fitness scorecard, and the 8-slice fix plan live THERE — this memory is
the session log + the facts a fix session needs fast). Charter:
[[project/audit2-2026-08-18-charter]] (status now DONE). Baseline anchor: audit #1
([[project/wip-2026-08-13-full-audit-1]]).

## How it ran
/frame Audit, Deep, READ-ONLY. Step-2 re-mine FIRST (dated): code.md items 20–22
(mirror-never-seats · raw-focusHit-pan · 0.4.28-internals) + tests.md item 9 (CDP 9222 +
evaluate-post-load) + item-7 suite baseline re-date. Then Track E (main), 4 parallel finder
agents (A/B/C/D per review-agent.md), Track F (harness, main) while they ran, then a 100%
verification pass (every candidate re-read at its anchor): **34 confirmed / 0 deleted / FP
ratchet 0%**. Audit purity PASS (diff = checklists + report + Phase-4 record only).

## The two MAJORS (fix slices 1–2)
- **A1 (90%)**: `scene/aimCones.ts:293–298` — U4 terrain seat eases toward RAW
  `terrainHeightAt`, the ONLY unclamped `heightAt` consumer (terrain.ts:1–12 mandates
  clampGroundM for every consumer; wired raw at StylizedTiles.ts:294). First finite sample
  SNAPS → one coarse-LOD negative seats the circle underground and the ease retains it.
  Fix: clampGroundM around the probe + regression test.
- **B1 (55%)**: `astro.config.mjs:39` `security:{checkOrigin:false}` — CSRF origin check OFF
  on all cookie-authed writes since scaffold commit 1bdedba; ZERO documented sanction
  (grep-proven); @wix/astro does not force it. Exploitability UNVERIFIED (Wix cookie SameSite,
  cloud tier). Fix: trial `checkOrigin:true` (wix dev + release canary: login/checkout/`#p=`),
  then a dated DECISIONS ruling EITHER WAY.

## Key verdicts (details in report)
- **C6 hop-trace CLEAN ×2 audits running** — incl. U3–U5 surfaces (MapWindow/minimap/aimCones
  zero-egress-proven); SavedPlaces ADMIN-read only.
- **Orchestrator HOLDING-MARGINAL**: 3,833 ln / **49** step invocations (in-file comments say
  38 and 36 — A2); FPV cluster ≈950 ln (25%) took all U2–U5 churn. Extraction ladder (Gall,
  strangler = attach-module idiom, one family/session, A/B via `__globe.u2()/u5()`):
  DEV-introspection (~200 ln, LOW) → quality fan-out (~90 ln, LOW) → zoom-bank (MED) →
  FocusFrame anchor/eye rules (MED-HIGH) → FPV controller (HIGH).
- **U5 lru/caps idiom ×3 tile modules = 33 glue lines**: restraint-now; named seam
  `captureTileDefaults/applyTileTier` in scene/tilePriority.ts, trigger = 4th renderer or next
  touch of all three (proposed row T29).
- **Scene-test ruling RE-AFFIRMED narrowed**: "pure islands exported/extracted + tested;
  attach/GL = browser tier" — 14 test files already test scene code; both owner-caught U4 bugs
  (18f/18h) sat in the untested orchestration layer → the guard is C2's STATIC FENCES
  (mirror-seat grep · setClearColor · boundingSphere · [hidden] pairs), not GL unit tests.
- **DECISIONS compaction round 3 DUE**: hot file 206,252 B, 71% (~147 KB, lines 621–676) =
  closed eras (astro-engine 08-02→10 + 08-11 trio + mobile/QoL/FIND/guide 08-13→15e). Target
  ≈65 KB hot. Round-2 byte-identical mechanism PROVEN (162/163 lines verbatim in archive).
  Interlocks harness F1: the boot hook's "Read DECISIONS" truncates at ~152 lines (~25 K
  tokens) — never reaches Recent sessions (reproduced on this session's own boot).
- **Memory graph**: healthy size (780 K disk, boot read ≈42 KB); debt = 3 stale always-offered
  memories (system-overview phantom Listings + 4-endpoint list, no supersession note;
  core.md:178 "current ground pipeline" on the frozen 2026-07-10 sky-bodies-terrain) + dangling
  UPLIFT_PLAN:22 pointer (→ wip-2026-08-17-p7-meteors-uplift-plan) + missing uplift era-index
  row. Policy proposed (report scorecard): era-row per compaction · re-date core Status each
  round · caps core ≤12 K / wip ≤10 K / patterns ≤15 K · KEEP wip leaves · in-place SUPERSEDED
  headers (globe-rendering models it).
- **Harness (Track F)**: F1 boot-read defeated (above) · F2 verify recipe not runnable — 9222
  ownership check + occlusion flags live only in prose (DECISIONS/3 wips/UPLIFT_PLAN/gitignored
  NSP); propose `scripts/verify-chrome.mjs` + `conventions/verify.md` · F3 ship-hook "checkout
  moved" WARN leaks local ship branches (deleted tree-proven 20260813-191945; 20260813-205327
  fails tree AND patch proof though content landed — needs the prune/report mechanism) AND
  SHIP_ATTENTION deletion at boot RACED the detached watcher's final write (file reappeared
  mid-session with an already-resolved entry — observed live).
- **T24 re-verified, formula extended**: core claim holds (0 define:vars/server:defer/spread;
  the 3 `transition:` hits are CSS); NEW `set:html` ×4 in guide.astro is SAFE (static
  guideContent + escapeHtml + `[a-z0-9-]` targets) but must join the row's grep formula;
  `/_server-islands/[name]` registered in the manifest with zero island components.
- **npm-audit classification**: ALL 29 dev/build-toolchain except astro runtime code (sinks
  unreachable, T24); deployed image service is noop (sharp absent from dist); prod-only audit
  9 (3 low/6 high) = baseline byte-identical.
- Track E numbers: vitest 989/989 · astro 0/0/5 hints (=NSP baseline; audit-1 had 6) · bundle
  30 MB (= 33 − enriched-sample) · jscpd src 1.89%/72 clones (top: MobileTimeDock↔TimeScrubber
  236 ln — SANCTIONED two-shell cost, watch trend) · knip/depcheck real signal = 7 dead
  exported types + unused `@wix/dashboard` · public/ 0 unreferenced.
- Seed-number corrections (D11): stores **12 files / 11 zustand** (not 13) · memories **96
  (76 project/)** (not 100/75) · steps **49** (not 41).

## Fix slices (order; full table in report)
1. S A1 clamp + tests (+C3 dayArcs/glf tests, C7) · 2. S B1 checkOrigin trial → ruling ·
3. M compaction r3 + F1 hook text + D3 README + D1 ARCH + D12 archives + D8/D9 memories ·
4. S conventions batch (D4/D5+C1/D6/D7/A6/A3/F4) · 5. S dead-code (A4/A5/A2) ·
6. S test fences (C2) + backlog rows T24/T1 edits + NEW T28–T31 · 7. M harness
(verify-chrome.mjs + verify.md + ship-hook prune/race) + B3 stamps · 8. owner calls.

## Traps (new this session)
- **SHIP_ATTENTION can reappear after boot deletion** — the detached watcher's final write
  races it; re-check late in session before relying on "resolved".
- A finder agent's stale file-existence claim (Track B "saw" SHIP_ATTENTION) can reflect the
  race, not an error — verify liveness yourself before acting.
