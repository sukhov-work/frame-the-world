# T77 step 1 — MEASURE (2026-09-05/06) — DONE. Digest.

Mode: implement (instruments + two DEV read seams) under `/frame` Audit rules (READ-ONLY on `src/` except read seams). Deliverable: `.claude/claude-docs/rendering/MEASUREMENTS_2026-09-05.md` (verdict §0 · matrix §2 · shadow cost §3 · tiers/bloom §4 · models §5 · ULTRA §6 · CPU profile §7 · shimmer §8 · reseat §9 · ESTIMATED→MEASURED §10 · phone §11 · SLICE ORDER SUPERSESSION §12 · traps §13). DECISIONS 2026-09-06. Backlog T77 (dated edit) + NEW T79 (controls raycast) · T80 (bloom) · T81 (seat stall + epoch churn) · T82 (rAF stall, harness env). Raw artefacts gitignored: `verify-shots/perf/baseline-warm-dsf2-2026-09-05T19-56-33.{json,md}` (+ `-m6redo-`), `temporal-warm-2026-09-05T20-50-21.json`, `cpu-{orbit,everest,fpv}-*.cpuprofile`.

## The headline numbers (M3 Pro, headed Chrome 152, warm profile, 1600×950 @ dsf 2, 120 Hz display)
- **Orbit poses are CPU-bound in the CONTROLS**: `frame.cpu` 31–47 ms at every tier at a static `#p=` pose (FPV 2 ms). Profile: 84 % = `GlobeControls.update → adjustCamera → _getPointBelowCamera → raycaster.intersectObject(scene)` (3d-tiles-renderer 0.4.28 `EnvironmentControls.js:995/1059/1461/1736`; controls built with the WHOLE scene, `StylizedTiles.ts:1086`; brute-force `Mesh.raycast` over the 7.7 M-vertex enriched soup). Levers: terrain-only `setScene`, layers mask, memoised `heightAt`, `three-mesh-bvh`. Owner call: `cameraRadius` clears ROOFTOPS today.
- **Bloom = largest GPU consumer**: off −13.3 ms at FPV (25.1→11.8), −21 orbit, −22 Everest, −14 city, −25 ULTRA. FPV frame is GPU-fill-bound (dt≈gpu); high→mid DPR cuts GPU 25→15. GTAO is OFF in the build.
- **Shadows cheap**: depth pass 0.5 ms GPU / 1.7 ms frame at FPV; 6–7 ms under ULTRA; 14 ms GPU only at the 26-km city view (718 calls / 6.7 M tris). GPU-timer `noUpdate` vs `off` asymmetry at city/orbit-with-models UNRESOLVED.
- **Shimmer baseline** (frozen camera, sun stepped 2,000 scene-ms/frame): mask churn p50 0.185 FPV base · 0.116 city ULTRA · 0.077 Everest ULTRA; speckle 0.62–0.75; 4×/1× ratio 1.24–1.64 (rate-independent → the rig's re-fit); control legs EXACTLY 0 (deterministic) except the city ULTRA cascade refresh (18 pop frames/239). Slice A gate proposal: p50 ≤ 0.05, control 0 incl. refresh, ratio ≥ 3, speckle ≤ 0.3.
- **Reseat baseline**: the ease STALLS at 0.01/0.12 = 0.0833 m (write gate vs `reseatEaseK`) — every leg ends at exactly 0.083 m; terrain epoch bumps 130 (1.9/s) during orbit arrival, 166 (4.8/s) at the FPV boot; FPV: seat writes in 98 % of frames for 70 s, 41 features/frame, 1,210 rejections, city p95 30 m at the end (poisoned-pair loop is a steady state); look-cone quiets ~50 frames after the last bump; user models seat in 6 frames.
- **24 realistic models (DamagedHelmet) cost nothing measurable** at FPV (dt 23.1→22.7, +19 calls, +17 textures — one URL's textures shared, +17 MB heap); +5 ms CPU at orbit (controls raycast). MESH_SUITE §12 texture cliff REFUTED at the count cap.
- ULTRA +4–8 ms GPU, +75–290 calls. Memory proxies: heap 400–490 MB FPV, 840–920 MB city, 100 MB `/m`; ground LRU at its 410 MB cap at every Dnipro pose; city streams 3,300 ground tiles, 63–68 s to quiet.
- Slice order SUPERSEDED (MEASUREMENTS §12): NEW slice 0 (orbit frame: T79 then T80) → A shadows (shimmer gate) → B seats (T81) → C → D → E.

## Instruments (all fenced by `test/verifyHarness.test.ts`; Node 24 PATH; HEADED :9222; `wix dev` up; never two at once; never edit src/ while running)
- `scripts/verify-perf-baseline.mjs [9222] [--quick] [--only <re>] [--post-ab] [--models 0,6,24] [--dsf 2] [--settle 90] [--label warm] [--cleanup-seeds <json>]` — boots per pose × ULTRA pref (pre-boot localStorage via `Page.addScriptToEvaluateOnNewDocument`) × device tier (pre-boot `navigator.hardwareConcurrency` override 2→low, 4→mid; governor PINNED in-page right after seams appear) × N resident models (dev-seed rows, N = TOTAL resident, world baseline read at first boot; `finally` removes; journal); samples on/bloomOff/noUpdate/off; HUD-closed rAF sampler + feed-active window; artefacts per cell; boot failure → replace target, continue; rAF watchdog. Full run 41 min; `--quick` ~10.
- `scripts/verify-temporal-stability.mjs [--shimmer] [--reseat] [--frames 480] [--step 2000] [--px 640]` — shimmer = `shadow.intensity` A/B inside one rAF → screen-space mask → XOR churn; reseat = `__globe.seatSettle()` per frame.
- `scripts/probe-cpu-profile.mjs --pose orbit|fpv|city|everest [--ultra]` — V8 profile, self time by file/function, `.cpuprofile`.
- `scripts/t77-model-ramp.mjs status|to N|clear` — the phone kill ramp; imports the app's geohash encoder (Node ≥22.6 strips TS).

## DEV read seams added (src/)
- `window.__debugFeed` (`lib/globe/debugFeed.ts publishDebugFeedSeam`; `snapshot()/read/ids/series/action/actionIds/active/setActive`; DEV always; runtime-gated via `debugHud` pref at boot — `lib/globe/debugBoot.ts` → GlobeCanvas, which also ACTIVATES the feed on panel-less shells). Registry `global.d.ts`; contracts §3 (28 seams). Tests +4 in `test/lib/globe/debugFeed.test.ts` (series-id list source-pinned).
- `__globe.seatSettle()` → `{frameCount, terrainEpoch, enriched:{frame,maxResidualM,movedFeatures,nearMaxResidualM,nearMovedFeatures,nearCells,epoch,quietFrames,deferred,rejected}}` (`enrichedBuildings.ts applyFeatureSeats` accumulators + `prioritySet` mirror).

## Poses (cited verbatim from owning harnesses)
fpv `#f=48.4647,35.0462,1.7,25,8,60&t=1787133600000` · orbit `#p=48.4647,35.0462,700,25,40` (verify-usermodels ORBIT; the model ramp's orbit — `loadRadiusM` is camera-distance) · city `#p=48.464,35.046,900,74,300&t=1787305200000` (verify-ultra DNIPRO = heading 74°, tilt 88° clamped, camera ~26 km back; its struct mislabels) · everest `#p=27.87,86.83,11500,76,35` · m `/m#p=48.4640,35.0460,220,0,0&t=1787313600000` (390×844 @3, touch).

## Traps (see MEASUREMENTS §13)
boot-demotion + late pin = mass composite rebuild · `pgrep -f` self-match (`patter[n]`) · write artefacts per cell · rAF-promise stall 3/27 boots (T82) · 120 Hz vsync floor · `force()` cannot set the shadow profile · bloom/AO enables rewritten per frame (getter traps) · dev-seed takes any https URL · hand-copied geohash was wrong (`ub8gt` is the eye cell) · `Browser.getVersion` over a page ws works.

## The phone flow (BUILT 2026-09-06b; owner: Device Farm project w/ iPhone 17 Pro pool, 1,000 free min; Pixel 6 Pro over adb offered)
- iOS: `tools/devicefarm/ios-baseline.mjs` (own package.json: webdriverio + @aws-sdk/client-device-farm; node_modules gitignored; outside knip's globs). Remote Access session → `endpoints.remoteDriverEndpoint` → WebdriverIO `browserName: "Safari"` (XCUITest; no udid/platformVersion caps; `appium:version 3` on the session) → `executeScript` reads `__debugFeed.snapshot()`; tunnel = `cloudflared tunnel --url http://localhost:4321` + `wix dev --allowed-hosts <host>` (ngrok free interstitial blocks Safari); kill ramp = seed from Mac → reload → boot marker vanished; soak 8 min; `finally` unseeds + StopRemoteAccessSession. Limits (docs): 150 min cap, 5 min idle, 4 min/command, metered per minute, XCUITest only, no BiDi. `--dry-run` resolves project+device. NOTHING run yet — no AWS credentials on the Mac.
- Android: `verify-perf-baseline.mjs 9444 --device` after `adb reverse tcp:4321 tcp:4321` + `adb forward tcp:9444 localabstract:chrome_devtools_remote`; no emulation / tier override; ULTRA pref refused on coarse pointer asserted. Untested (Pixel not attached).
- Owner setup DONE 2026-09-06c: awscli 2.36 + cloudflared 2026.8.3 (brew); AWS profile **`plux`** (us-west-2, json) = the account ROOT key ("be careful": Device Farm List/Get/Create/Stop calls only; never print/store; nothing in the repo reads ~/.aws; recommend an IAM user later). Dry run (read-only): project `…:project:69e9a004-b773-4e76-87d5-259381e752df`; pool "Iphone 17pro" → device `…:device:6200F380A4874FEB9C72EED72B863B67` = iPhone 17 Pro iOS 26.3.1 1206×2622 HIGHLY_AVAILABLE remoteAccess true. Run: `AWS_PROFILE=plux node tools/devicefarm/ios-baseline.mjs --host https://<cloudflare>`. NEXT SESSION = STEP 1 the phone runs (farm, then Pixel over adb), THEN slice 0.

## Owner calls open (block T79/T80 SHIP, not investigation)
(1) rooftop clearance change with a terrain-only controls raycast; (2) bloom pixels at `high`; (3) phone farm vendor (17 Pro [UNVERIFIED]). Phone §B UNKNOWN.
