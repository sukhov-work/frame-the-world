# mem:project/wip-2026-09-11-audit4 — AUDIT #4 EXECUTED (the full breadth-first adversarial pass)

**The charter:** `audits/AUDIT4_CHARTER_2026-09-10.md`. **The report:** `audits/audit-full-2026-09-11.md`
(READ IT FIRST — verdict, findings with anchors, fix-session slicing S1–S10). DECISIONS 2026-09-11 line is
the twin. Read-only held throughout: the session's diff = the report + README row + checklists (code 29–31,
tests 13–16, platform 14) + backlog dated edits + this leaf.

## What ran
- **Track E (main):** vitest 3,098/3,098 · astro 0/0/12 (baseline holds) · knip 0 · npm audit INFRA
  (both registries unreachable via VPN) · wix build 32 MB < 33 MB baseline · ~31 DEV seams (readers OK) ·
  PLUX_MASTER_LOGO/PLUX_FAVICON unreferenced (NIT) · pipeline clean (12 stale remote-tracking refs, NIT).
- **15 finder tracks** (Workflow tool, ultracode): A · B · C · D · F-static · G · H1–H5 · I1/I2 · J · K →
  consolidation → adversarial panels. **Two API usage walls** hit the verifier fleet (1308/1302 429s);
  both resumed; EVERY completed verdict refuted NOTHING (0 refuted across all runs) — the pending tail
  was finishing at press time (13/29 MINOR + 9 NIT batches).
- **Browser tier (main, one Chrome + one dev server, VPN on):** sweep `audit4-2026-09-11` 14/14
  (freeze 14/14 byte-identical on re-run; draw-count 10/14 exact vs `post-2026-09-10b`, the 4 diffs in
  the documented ground-tile settle band); the four interlude poses re-run (T131–T134 HOLD: P2 no band,
  P3 mean 23, P4 stream converging, DBG slotBoosts/growthCapped live); **T135a NOT reproduced again**
  (cold cache, altM 8601 seated); harnesses debughud ALL · **mobile-batch 127/127** · meshedit PASS ·
  usermodels 21 legs; `verify-guide` 3 FAIL = stale pinned counts (11 chapters/14 goals vs 12/86/16 —
  L1); UX live: welcome/upload(REVIEW honest-missing)/m-shell/slow-net graceful; live-site canaries 200.

## The headline findings (all MAJOR verified by panel + main-agent re-read)
1. **The LIVE-SITE GRADUATION class** (G1/G2/G3/B5 → T138): four POC-accepted risks rode into
   commercial live 2026-09-10 un-re-ratified — no moderation gate (save store DEFAULTS isPublic:true),
   T17 ToS trigger fired unrecorded, T26 uploads unbounded + preview mime unconstrained, previewUrl
   length-only into every visitor's img/texture fetch. (A fifth candidate, "zero analytics", was REFUTED
   by the final pass: the Wix headless EDGE injects site-bi/site-analytics/tag-manager + PageView on
   every served page — live-fetched; T138's (d) discharged with a dated edit.)
2. **The library pin** (A1/A2 → T136): caret `^0.4.28` over NINE patch seams (charter said five);
   installed:false drift degrades SILENT (0 console in the three lib files).
3. **C1**: the skyBudget twin models the base dome only — the T96 LOOK ships on base; margin 0.8964/0.9.

## Registry (this session's edits)
CLOSED as stale-OPEN (K1; closures were DECISIONS 2026-08-22e, never edited): **T32 T35 T36 T37 T39**
(all HEAD-verified: planElevationsM / radarBands+tangentOverlay+radarCanvas / aimAnchor /
TILE_RETRY_MS+failedAtMs / clampPlannedView). T135 re-verified note. **NEW: T136–T144** (see the report's
Backlog section). T17/T26/T30/T50 flagged for T138.

## Traps paid for (new, beyond the checklist items landed)
- Node 20 has NO global WebSocket → `node --experimental-websocket` for every CDP harness (the sweep
  header omits it — L7; my first sweep died on it).
- `--compare <label>` reads `verify-shots/golden/<label>/` — a label that only exists under `sweep/`
  fails every pose as "no golden" (the interlude never promoted; L2). Promote goldens after pixel
  changes.
- The Read tool CDN-uploads images (no inline view) → numeric pixel probes via `sharp` (the interlude's
  own luma-read method) are the eye-pass substitute for a headless session.
- Workflow fleets vs the 5-hour usage wall: 429-class deaths return null (coded fallbacks engaged);
  probe with one tiny agent before resuming; resume replays cached finders instantly.

## Next (per the report's slicing)
S1 T136 pin+fences → S2 T137 twin → **S3 T138 OWNER batch** → S4 T139 → S5 T140 guide → S6 T141 docs
(DECISIONS round 7 is AT 135.2 KB) → S7 T142 → S8 T143 (B1 first) → S9 T144 → S10 replay tails. Then the
pre-audit main plan: the farm FEATURE legs, the T77 lane.
